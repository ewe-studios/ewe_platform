//! Auto-generated API clients for supabase.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `supabase`

#![cfg(feature = "supabase")]

pub mod types;

use crate::providers::supabase::clients::types::*;
use crate::providers::supabase::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_branches_branch_id_or_ref_execute()` to send, or `get_v1_branches_branch_id_or_ref` for simplest API.

pub fn get_v1_branches_branch_id_or_ref_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/branches/{}", branch_id_or_ref,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_branches_branch_id_or_ref_execute()` or `get_v1_branches_branch_id_or_ref`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_branches_branch_id_or_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_branches_branch_id_or_ref_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchDetailResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchDetailResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_branches_branch_id_or_ref_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_branches_branch_id_or_ref_task()`.
/// For the simplest API, use `get_v1_branches_branch_id_or_ref()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_branches_branch_id_or_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_branches_branch_id_or_ref_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchDetailResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_branches_branch_id_or_ref_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_branches_branch_id_or_ref`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BranchesBranchIdOrRefArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
}

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_branches_branch_id_or_ref_builder()` + `get_v1_branches_branch_id_or_ref_execute()`.
/// For task-level control, use `get_v1_branches_branch_id_or_ref_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_branches_branch_id_or_ref(
    client: &SimpleHttpClient,
    args: &GetV1BranchesBranchIdOrRefArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchDetailResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_branches_branch_id_or_ref_builder(client, &args.branch_id_or_ref)?;
    get_v1_branches_branch_id_or_ref_execute(builder)
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_branches_branch_id_or_ref_execute()` to send, or `patch_v1_branches_branch_id_or_ref` for simplest API.

pub fn patch_v1_branches_branch_id_or_ref_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
    body: &UpdateBranchBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/branches/{}", branch_id_or_ref,);

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_branches_branch_id_or_ref_execute()` or `patch_v1_branches_branch_id_or_ref`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_branches_branch_id_or_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_branches_branch_id_or_ref_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_branches_branch_id_or_ref_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_branches_branch_id_or_ref_task()`.
/// For the simplest API, use `patch_v1_branches_branch_id_or_ref()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_branches_branch_id_or_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_branches_branch_id_or_ref_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_branches_branch_id_or_ref_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_branches_branch_id_or_ref`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1BranchesBranchIdOrRefArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: UpdateBranchBody,
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_branches_branch_id_or_ref_builder()` + `patch_v1_branches_branch_id_or_ref_execute()`.
/// For task-level control, use `patch_v1_branches_branch_id_or_ref_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_branches_branch_id_or_ref(
    client: &SimpleHttpClient,
    args: &PatchV1BranchesBranchIdOrRefArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        patch_v1_branches_branch_id_or_ref_builder(client, &args.branch_id_or_ref, &args.body)?;
    patch_v1_branches_branch_id_or_ref_execute(builder)
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_branches_branch_id_or_ref_execute()` to send, or `delete_v1_branches_branch_id_or_ref` for simplest API.

pub fn delete_v1_branches_branch_id_or_ref_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/branches/{}", branch_id_or_ref,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_branches_branch_id_or_ref_execute()` or `delete_v1_branches_branch_id_or_ref`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_branches_branch_id_or_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_branches_branch_id_or_ref_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchDeleteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchDeleteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_branches_branch_id_or_ref_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_branches_branch_id_or_ref_task()`.
/// For the simplest API, use `delete_v1_branches_branch_id_or_ref()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_branches_branch_id_or_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_branches_branch_id_or_ref_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchDeleteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_branches_branch_id_or_ref_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_branches_branch_id_or_ref`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1BranchesBranchIdOrRefArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_branches_branch_id_or_ref_builder()` + `delete_v1_branches_branch_id_or_ref_execute()`.
/// For task-level control, use `delete_v1_branches_branch_id_or_ref_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_branches_branch_id_or_ref(
    client: &SimpleHttpClient,
    args: &DeleteV1BranchesBranchIdOrRefArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchDeleteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_branches_branch_id_or_ref_builder(client, &args.branch_id_or_ref, args.force)?;
    delete_v1_branches_branch_id_or_ref_execute(builder)
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_branches_branch_id_or_ref_diff_execute()` to send, or `get_v1_branches_branch_id_or_ref_diff` for simplest API.

pub fn get_v1_branches_branch_id_or_ref_diff_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
    included_schemas: Option<&str>,
    pgdelta: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/branches/{}/diff",
        branch_id_or_ref,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = included_schemas {
        query_parts.push(format!("included_schemas={}", val));
    }
    if let Some(val) = pgdelta {
        query_parts.push(format!("pgdelta={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_branches_branch_id_or_ref_diff_execute()` or `get_v1_branches_branch_id_or_ref_diff`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_branches_branch_id_or_ref_diff_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_branches_branch_id_or_ref_diff_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_branches_branch_id_or_ref_diff_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_branches_branch_id_or_ref_diff_task()`.
/// For the simplest API, use `get_v1_branches_branch_id_or_ref_diff()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_branches_branch_id_or_ref_diff_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_branches_branch_id_or_ref_diff_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_branches_branch_id_or_ref_diff_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_branches_branch_id_or_ref_diff`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BranchesBranchIdOrRefDiffArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Query parameter: included_schemas
    pub included_schemas: Option<String>,
    /// Query parameter: pgdelta
    pub pgdelta: Option<bool>,
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_branches_branch_id_or_ref_diff_builder()` + `get_v1_branches_branch_id_or_ref_diff_execute()`.
/// For task-level control, use `get_v1_branches_branch_id_or_ref_diff_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_branches_branch_id_or_ref_diff(
    client: &SimpleHttpClient,
    args: &GetV1BranchesBranchIdOrRefDiffArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_branches_branch_id_or_ref_diff_builder(
        client,
        &args.branch_id_or_ref,
        args.included_schemas.as_deref(),
        args.pgdelta,
    )?;
    get_v1_branches_branch_id_or_ref_diff_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_branches_branch_id_or_ref_merge_execute()` to send, or `post_v1_branches_branch_id_or_ref_merge` for simplest API.

pub fn post_v1_branches_branch_id_or_ref_merge_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
    body: &BranchActionBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/branches/{}/merge",
        branch_id_or_ref,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_branches_branch_id_or_ref_merge_execute()` or `post_v1_branches_branch_id_or_ref_merge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_merge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_merge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_branches_branch_id_or_ref_merge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_merge_task()`.
/// For the simplest API, use `post_v1_branches_branch_id_or_ref_merge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_merge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_branches_branch_id_or_ref_merge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_branches_branch_id_or_ref_merge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_branches_branch_id_or_ref_merge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BranchesBranchIdOrRefMergeArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: BranchActionBody,
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_branches_branch_id_or_ref_merge_builder()` + `post_v1_branches_branch_id_or_ref_merge_execute()`.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_merge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_merge(
    client: &SimpleHttpClient,
    args: &PostV1BranchesBranchIdOrRefMergeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_branches_branch_id_or_ref_merge_builder(
        client,
        &args.branch_id_or_ref,
        &args.body,
    )?;
    post_v1_branches_branch_id_or_ref_merge_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_branches_branch_id_or_ref_push_execute()` to send, or `post_v1_branches_branch_id_or_ref_push` for simplest API.

pub fn post_v1_branches_branch_id_or_ref_push_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
    body: &BranchActionBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/branches/{}/push",
        branch_id_or_ref,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_branches_branch_id_or_ref_push_execute()` or `post_v1_branches_branch_id_or_ref_push`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_push_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_push_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_branches_branch_id_or_ref_push_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_push_task()`.
/// For the simplest API, use `post_v1_branches_branch_id_or_ref_push()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_push_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_branches_branch_id_or_ref_push_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_branches_branch_id_or_ref_push_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_branches_branch_id_or_ref_push`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BranchesBranchIdOrRefPushArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: BranchActionBody,
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_branches_branch_id_or_ref_push_builder()` + `post_v1_branches_branch_id_or_ref_push_execute()`.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_push_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_push(
    client: &SimpleHttpClient,
    args: &PostV1BranchesBranchIdOrRefPushArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_branches_branch_id_or_ref_push_builder(client, &args.branch_id_or_ref, &args.body)?;
    post_v1_branches_branch_id_or_ref_push_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_branches_branch_id_or_ref_reset_execute()` to send, or `post_v1_branches_branch_id_or_ref_reset` for simplest API.

pub fn post_v1_branches_branch_id_or_ref_reset_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
    body: &BranchActionBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/branches/{}/reset",
        branch_id_or_ref,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_branches_branch_id_or_ref_reset_execute()` or `post_v1_branches_branch_id_or_ref_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_branches_branch_id_or_ref_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_reset_task()`.
/// For the simplest API, use `post_v1_branches_branch_id_or_ref_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_branches_branch_id_or_ref_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_branches_branch_id_or_ref_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_branches_branch_id_or_ref_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BranchesBranchIdOrRefResetArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: BranchActionBody,
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_branches_branch_id_or_ref_reset_builder()` + `post_v1_branches_branch_id_or_ref_reset_execute()`.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_reset(
    client: &SimpleHttpClient,
    args: &PostV1BranchesBranchIdOrRefResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_branches_branch_id_or_ref_reset_builder(
        client,
        &args.branch_id_or_ref,
        &args.body,
    )?;
    post_v1_branches_branch_id_or_ref_reset_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_branches_branch_id_or_ref_restore_execute()` to send, or `post_v1_branches_branch_id_or_ref_restore` for simplest API.

pub fn post_v1_branches_branch_id_or_ref_restore_builder(
    client: &SimpleHttpClient,
    branch_id_or_ref: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/branches/{}/restore",
        branch_id_or_ref,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_branches_branch_id_or_ref_restore_execute()` or `post_v1_branches_branch_id_or_ref_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchRestoreResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchRestoreResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_branches_branch_id_or_ref_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_restore_task()`.
/// For the simplest API, use `post_v1_branches_branch_id_or_ref_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_branches_branch_id_or_ref_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_branches_branch_id_or_ref_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchRestoreResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_branches_branch_id_or_ref_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_branches_branch_id_or_ref_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BranchesBranchIdOrRefRestoreArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_branches_branch_id_or_ref_restore_builder()` + `post_v1_branches_branch_id_or_ref_restore_execute()`.
/// For task-level control, use `post_v1_branches_branch_id_or_ref_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_branches_branch_id_or_ref_restore(
    client: &SimpleHttpClient,
    args: &PostV1BranchesBranchIdOrRefRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchRestoreResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_branches_branch_id_or_ref_restore_builder(client, &args.branch_id_or_ref)?;
    post_v1_branches_branch_id_or_ref_restore_execute(builder)
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_oauth_authorize_execute()` to send, or `get_v1_oauth_authorize` for simplest API.

pub fn get_v1_oauth_authorize_builder(
    client: &SimpleHttpClient,
    client_id: Option<&str>,
    response_type: Option<&str>,
    redirect_uri: Option<&str>,
    scope: Option<&str>,
    state: Option<&str>,
    response_mode: Option<&str>,
    code_challenge: Option<&str>,
    code_challenge_method: Option<&str>,
    organization_slug: Option<&str>,
    resource: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/oauth/authorize",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_id {
        query_parts.push(format!("client_id={}", val));
    }
    if let Some(val) = response_type {
        query_parts.push(format!("response_type={}", val));
    }
    if let Some(val) = redirect_uri {
        query_parts.push(format!("redirect_uri={}", val));
    }
    if let Some(val) = scope {
        query_parts.push(format!("scope={}", val));
    }
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = response_mode {
        query_parts.push(format!("response_mode={}", val));
    }
    if let Some(val) = code_challenge {
        query_parts.push(format!("code_challenge={}", val));
    }
    if let Some(val) = code_challenge_method {
        query_parts.push(format!("code_challenge_method={}", val));
    }
    if let Some(val) = organization_slug {
        query_parts.push(format!("organization_slug={}", val));
    }
    if let Some(val) = resource {
        query_parts.push(format!("resource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_oauth_authorize_execute()` or `get_v1_oauth_authorize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_oauth_authorize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_oauth_authorize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_oauth_authorize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_oauth_authorize_task()`.
/// For the simplest API, use `get_v1_oauth_authorize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_oauth_authorize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_oauth_authorize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_oauth_authorize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_oauth_authorize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OauthAuthorizeArgs {
    /// Query parameter: client_id
    pub client_id: Option<String>,
    /// Query parameter: response_type
    pub response_type: Option<String>,
    /// Query parameter: redirect_uri
    pub redirect_uri: Option<String>,
    /// Query parameter: scope
    pub scope: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: response_mode
    pub response_mode: Option<String>,
    /// Query parameter: code_challenge
    pub code_challenge: Option<String>,
    /// Query parameter: code_challenge_method
    pub code_challenge_method: Option<String>,
    /// Query parameter: organization_slug
    pub organization_slug: Option<String>,
    /// Query parameter: resource
    pub resource: Option<String>,
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_oauth_authorize_builder()` + `get_v1_oauth_authorize_execute()`.
/// For task-level control, use `get_v1_oauth_authorize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_oauth_authorize(
    client: &SimpleHttpClient,
    args: &GetV1OauthAuthorizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_oauth_authorize_builder(
        client,
        args.client_id.as_deref(),
        args.response_type.as_deref(),
        args.redirect_uri.as_deref(),
        args.scope.as_deref(),
        args.state.as_deref(),
        args.response_mode.as_deref(),
        args.code_challenge.as_deref(),
        args.code_challenge_method.as_deref(),
        args.organization_slug.as_deref(),
        args.resource.as_deref(),
    )?;
    get_v1_oauth_authorize_execute(builder)
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_oauth_authorize_project_claim_execute()` to send, or `get_v1_oauth_authorize_project_claim` for simplest API.

pub fn get_v1_oauth_authorize_project_claim_builder(
    client: &SimpleHttpClient,
    project_ref: Option<&str>,
    client_id: Option<&str>,
    response_type: Option<&str>,
    redirect_uri: Option<&str>,
    state: Option<&str>,
    response_mode: Option<&str>,
    code_challenge: Option<&str>,
    code_challenge_method: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/oauth/authorize/project-claim",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = project_ref {
        query_parts.push(format!("project_ref={}", val));
    }
    if let Some(val) = client_id {
        query_parts.push(format!("client_id={}", val));
    }
    if let Some(val) = response_type {
        query_parts.push(format!("response_type={}", val));
    }
    if let Some(val) = redirect_uri {
        query_parts.push(format!("redirect_uri={}", val));
    }
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = response_mode {
        query_parts.push(format!("response_mode={}", val));
    }
    if let Some(val) = code_challenge {
        query_parts.push(format!("code_challenge={}", val));
    }
    if let Some(val) = code_challenge_method {
        query_parts.push(format!("code_challenge_method={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_oauth_authorize_project_claim_execute()` or `get_v1_oauth_authorize_project_claim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_oauth_authorize_project_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_oauth_authorize_project_claim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_oauth_authorize_project_claim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_oauth_authorize_project_claim_task()`.
/// For the simplest API, use `get_v1_oauth_authorize_project_claim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_oauth_authorize_project_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_oauth_authorize_project_claim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_oauth_authorize_project_claim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_oauth_authorize_project_claim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OauthAuthorizeProjectClaimArgs {
    /// Query parameter: project_ref
    pub project_ref: Option<String>,
    /// Query parameter: client_id
    pub client_id: Option<String>,
    /// Query parameter: response_type
    pub response_type: Option<String>,
    /// Query parameter: redirect_uri
    pub redirect_uri: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: response_mode
    pub response_mode: Option<String>,
    /// Query parameter: code_challenge
    pub code_challenge: Option<String>,
    /// Query parameter: code_challenge_method
    pub code_challenge_method: Option<String>,
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_oauth_authorize_project_claim_builder()` + `get_v1_oauth_authorize_project_claim_execute()`.
/// For task-level control, use `get_v1_oauth_authorize_project_claim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_oauth_authorize_project_claim(
    client: &SimpleHttpClient,
    args: &GetV1OauthAuthorizeProjectClaimArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_oauth_authorize_project_claim_builder(
        client,
        args.project_ref.as_deref(),
        args.client_id.as_deref(),
        args.response_type.as_deref(),
        args.redirect_uri.as_deref(),
        args.state.as_deref(),
        args.response_mode.as_deref(),
        args.code_challenge.as_deref(),
        args.code_challenge_method.as_deref(),
    )?;
    get_v1_oauth_authorize_project_claim_execute(builder)
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_oauth_revoke_execute()` to send, or `post_v1_oauth_revoke` for simplest API.

pub fn post_v1_oauth_revoke_builder(
    client: &SimpleHttpClient,
    body: &OAuthRevokeTokenBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/oauth/revoke",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_oauth_revoke_execute()` or `post_v1_oauth_revoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_oauth_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_oauth_revoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_oauth_revoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_oauth_revoke_task()`.
/// For the simplest API, use `post_v1_oauth_revoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_oauth_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_oauth_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_oauth_revoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_oauth_revoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1OauthRevokeArgs {
    /// Request body.
    pub body: OAuthRevokeTokenBody,
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_oauth_revoke_builder()` + `post_v1_oauth_revoke_execute()`.
/// For task-level control, use `post_v1_oauth_revoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_oauth_revoke(
    client: &SimpleHttpClient,
    args: &PostV1OauthRevokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_oauth_revoke_builder(client, &args.body)?;
    post_v1_oauth_revoke_execute(builder)
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_oauth_token_execute()` to send, or `post_v1_oauth_token` for simplest API.

pub fn post_v1_oauth_token_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/oauth/token",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_oauth_token_execute()` or `post_v1_oauth_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_oauth_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OAuthTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OAuthTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_oauth_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_oauth_token_task()`.
/// For the simplest API, use `post_v1_oauth_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_oauth_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OAuthTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_oauth_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_oauth_token_builder()` + `post_v1_oauth_token_execute()`.
/// For task-level control, use `post_v1_oauth_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_oauth_token(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OAuthTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_oauth_token_builder(client)?;
    post_v1_oauth_token_execute(builder)
}

/// GET /v1/organizations
/// List all organizations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_organizations_execute()` to send, or `get_v1_organizations` for simplest API.

pub fn get_v1_organizations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/organizations",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations
/// List all organizations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_organizations_execute()` or `get_v1_organizations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations
/// List all organizations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_organizations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_organizations_task()`.
/// For the simplest API, use `get_v1_organizations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_organizations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/organizations
/// List all organizations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_organizations_builder()` + `get_v1_organizations_execute()`.
/// For task-level control, use `get_v1_organizations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_organizations_builder(client)?;
    get_v1_organizations_execute(builder)
}

/// POST /v1/organizations
/// Create an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_organizations_execute()` to send, or `post_v1_organizations` for simplest API.

pub fn post_v1_organizations_builder(
    client: &SimpleHttpClient,
    body: &CreateOrganizationV1,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/organizations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/organizations
/// Create an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_organizations_execute()` or `post_v1_organizations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_organizations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OrganizationResponseV1>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationResponseV1 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/organizations
/// Create an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_organizations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_organizations_task()`.
/// For the simplest API, use `post_v1_organizations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationResponseV1>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_organizations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_organizations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1OrganizationsArgs {
    /// Request body.
    pub body: CreateOrganizationV1,
}

/// POST /v1/organizations
/// Create an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_organizations_builder()` + `post_v1_organizations_execute()`.
/// For task-level control, use `post_v1_organizations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_organizations(
    client: &SimpleHttpClient,
    args: &PostV1OrganizationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationResponseV1>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_organizations_builder(client, &args.body)?;
    post_v1_organizations_execute(builder)
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_organizations_slug_execute()` to send, or `get_v1_organizations_slug` for simplest API.

pub fn get_v1_organizations_slug_builder(
    client: &SimpleHttpClient,
    slug: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/organizations/{}", slug,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_organizations_slug_execute()` or `get_v1_organizations_slug`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1OrganizationSlugResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1OrganizationSlugResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_organizations_slug_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_organizations_slug_task()`.
/// For the simplest API, use `get_v1_organizations_slug()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_organizations_slug_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1OrganizationSlugResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_organizations_slug_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_organizations_slug`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OrganizationsSlugArgs {
    /// Path parameter: slug
    pub slug: String,
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_organizations_slug_builder()` + `get_v1_organizations_slug_execute()`.
/// For task-level control, use `get_v1_organizations_slug_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug(
    client: &SimpleHttpClient,
    args: &GetV1OrganizationsSlugArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1OrganizationSlugResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_organizations_slug_builder(client, &args.slug)?;
    get_v1_organizations_slug_execute(builder)
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_organizations_slug_entitlements_execute()` to send, or `get_v1_organizations_slug_entitlements` for simplest API.

pub fn get_v1_organizations_slug_entitlements_builder(
    client: &SimpleHttpClient,
    slug: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/organizations/{}/entitlements",
        slug,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_organizations_slug_entitlements_execute()` or `get_v1_organizations_slug_entitlements`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_entitlements_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ListEntitlementsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ListEntitlementsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_organizations_slug_entitlements_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_organizations_slug_entitlements_task()`.
/// For the simplest API, use `get_v1_organizations_slug_entitlements()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_organizations_slug_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1ListEntitlementsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_organizations_slug_entitlements_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_organizations_slug_entitlements`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OrganizationsSlugEntitlementsArgs {
    /// Path parameter: slug
    pub slug: String,
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_organizations_slug_entitlements_builder()` + `get_v1_organizations_slug_entitlements_execute()`.
/// For task-level control, use `get_v1_organizations_slug_entitlements_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_entitlements(
    client: &SimpleHttpClient,
    args: &GetV1OrganizationsSlugEntitlementsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1ListEntitlementsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_organizations_slug_entitlements_builder(client, &args.slug)?;
    get_v1_organizations_slug_entitlements_execute(builder)
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_organizations_slug_members_execute()` to send, or `get_v1_organizations_slug_members` for simplest API.

pub fn get_v1_organizations_slug_members_builder(
    client: &SimpleHttpClient,
    slug: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/organizations/{}/members", slug,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_organizations_slug_members_execute()` or `get_v1_organizations_slug_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_organizations_slug_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_organizations_slug_members_task()`.
/// For the simplest API, use `get_v1_organizations_slug_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_organizations_slug_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_organizations_slug_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_organizations_slug_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OrganizationsSlugMembersArgs {
    /// Path parameter: slug
    pub slug: String,
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_organizations_slug_members_builder()` + `get_v1_organizations_slug_members_execute()`.
/// For task-level control, use `get_v1_organizations_slug_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_members(
    client: &SimpleHttpClient,
    args: &GetV1OrganizationsSlugMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_organizations_slug_members_builder(client, &args.slug)?;
    get_v1_organizations_slug_members_execute(builder)
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_organizations_slug_project_claim_token_execute()` to send, or `get_v1_organizations_slug_project_claim_token` for simplest API.

pub fn get_v1_organizations_slug_project_claim_token_builder(
    client: &SimpleHttpClient,
    slug: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/organizations/{}/project-claim/{}",
        slug, token,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_organizations_slug_project_claim_token_execute()` or `get_v1_organizations_slug_project_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_project_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<OrganizationProjectClaimResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationProjectClaimResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_organizations_slug_project_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_organizations_slug_project_claim_token_task()`.
/// For the simplest API, use `get_v1_organizations_slug_project_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_organizations_slug_project_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationProjectClaimResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_organizations_slug_project_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_organizations_slug_project_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OrganizationsSlugProjectClaimTokenArgs {
    /// Path parameter: slug
    pub slug: String,
    /// Path parameter: token
    pub token: String,
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_organizations_slug_project_claim_token_builder()` + `get_v1_organizations_slug_project_claim_token_execute()`.
/// For task-level control, use `get_v1_organizations_slug_project_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_project_claim_token(
    client: &SimpleHttpClient,
    args: &GetV1OrganizationsSlugProjectClaimTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationProjectClaimResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_organizations_slug_project_claim_token_builder(client, &args.slug, &args.token)?;
    get_v1_organizations_slug_project_claim_token_execute(builder)
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_organizations_slug_project_claim_token_execute()` to send, or `post_v1_organizations_slug_project_claim_token` for simplest API.

pub fn post_v1_organizations_slug_project_claim_token_builder(
    client: &SimpleHttpClient,
    slug: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/organizations/{}/project-claim/{}",
        slug, token,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_organizations_slug_project_claim_token_execute()` or `post_v1_organizations_slug_project_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_organizations_slug_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_organizations_slug_project_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_organizations_slug_project_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_organizations_slug_project_claim_token_task()`.
/// For the simplest API, use `post_v1_organizations_slug_project_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_organizations_slug_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_organizations_slug_project_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_organizations_slug_project_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_organizations_slug_project_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1OrganizationsSlugProjectClaimTokenArgs {
    /// Path parameter: slug
    pub slug: String,
    /// Path parameter: token
    pub token: String,
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_organizations_slug_project_claim_token_builder()` + `post_v1_organizations_slug_project_claim_token_execute()`.
/// For task-level control, use `post_v1_organizations_slug_project_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_organizations_slug_project_claim_token(
    client: &SimpleHttpClient,
    args: &PostV1OrganizationsSlugProjectClaimTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_organizations_slug_project_claim_token_builder(client, &args.slug, &args.token)?;
    post_v1_organizations_slug_project_claim_token_execute(builder)
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_organizations_slug_projects_execute()` to send, or `get_v1_organizations_slug_projects` for simplest API.

pub fn get_v1_organizations_slug_projects_builder(
    client: &SimpleHttpClient,
    slug: &str,
    offset: Option<i32>,
    limit: Option<i32>,
    search: Option<&str>,
    sort: Option<&str>,
    statuses: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/organizations/{}/projects",
        slug,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = offset {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = search {
        query_parts.push(format!("search={}", val));
    }
    if let Some(val) = sort {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = statuses {
        query_parts.push(format!("statuses={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_organizations_slug_projects_execute()` or `get_v1_organizations_slug_projects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_projects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<OrganizationProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_organizations_slug_projects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_organizations_slug_projects_task()`.
/// For the simplest API, use `get_v1_organizations_slug_projects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_organizations_slug_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_organizations_slug_projects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_organizations_slug_projects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_organizations_slug_projects`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1OrganizationsSlugProjectsArgs {
    /// Path parameter: slug
    pub slug: String,
    /// Query parameter: offset
    pub offset: Option<i32>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: search
    pub search: Option<String>,
    /// Query parameter: sort
    pub sort: Option<String>,
    /// Query parameter: statuses
    pub statuses: Option<String>,
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_organizations_slug_projects_builder()` + `get_v1_organizations_slug_projects_execute()`.
/// For task-level control, use `get_v1_organizations_slug_projects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_organizations_slug_projects(
    client: &SimpleHttpClient,
    args: &GetV1OrganizationsSlugProjectsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_organizations_slug_projects_builder(
        client,
        &args.slug,
        args.offset,
        args.limit,
        args.search.as_deref(),
        args.sort.as_deref(),
        args.statuses.as_deref(),
    )?;
    get_v1_organizations_slug_projects_execute(builder)
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_profile_execute()` to send, or `get_v1_profile` for simplest API.

pub fn get_v1_profile_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/profile",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_profile_execute()` or `get_v1_profile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_profile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ProfileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProfileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_profile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_profile_task()`.
/// For the simplest API, use `get_v1_profile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProfileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_profile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_profile_builder()` + `get_v1_profile_execute()`.
/// For task-level control, use `get_v1_profile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_profile(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProfileResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_profile_builder(client)?;
    get_v1_profile_execute(builder)
}

/// GET /v1/projects
/// List all projects
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_execute()` to send, or `get_v1_projects` for simplest API.

pub fn get_v1_projects_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects
/// List all projects
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_execute()` or `get_v1_projects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects
/// List all projects
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_task()`.
/// For the simplest API, use `get_v1_projects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/projects
/// List all projects
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_builder()` + `get_v1_projects_execute()`.
/// For task-level control, use `get_v1_projects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_builder(client)?;
    get_v1_projects_execute(builder)
}

/// POST /v1/projects
/// Create a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_execute()` to send, or `post_v1_projects` for simplest API.

pub fn post_v1_projects_builder(
    client: &SimpleHttpClient,
    body: &V1CreateProjectBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects
/// Create a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_execute()` or `post_v1_projects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ProjectResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects
/// Create a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_task()`.
/// For the simplest API, use `post_v1_projects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsArgs {
    /// Request body.
    pub body: V1CreateProjectBody,
}

/// POST /v1/projects
/// Create a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_builder()` + `post_v1_projects_execute()`.
/// For task-level control, use `post_v1_projects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_builder(client, &args.body)?;
    post_v1_projects_execute(builder)
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_available_regions_execute()` to send, or `get_v1_projects_available_regions` for simplest API.

pub fn get_v1_projects_available_regions_builder(
    client: &SimpleHttpClient,
    organization_slug: Option<&str>,
    continent: Option<&str>,
    desired_instance_size: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/available-regions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = organization_slug {
        query_parts.push(format!("organization_slug={}", val));
    }
    if let Some(val) = continent {
        query_parts.push(format!("continent={}", val));
    }
    if let Some(val) = desired_instance_size {
        query_parts.push(format!("desired_instance_size={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_available_regions_execute()` or `get_v1_projects_available_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_available_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_available_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<RegionsInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionsInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_available_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_available_regions_task()`.
/// For the simplest API, use `get_v1_projects_available_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_available_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_available_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionsInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_available_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_available_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsAvailableRegionsArgs {
    /// Query parameter: organization_slug
    pub organization_slug: Option<String>,
    /// Query parameter: continent
    pub continent: Option<String>,
    /// Query parameter: desired_instance_size
    pub desired_instance_size: Option<String>,
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_available_regions_builder()` + `get_v1_projects_available_regions_execute()`.
/// For task-level control, use `get_v1_projects_available_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_available_regions(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsAvailableRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionsInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_available_regions_builder(
        client,
        args.organization_slug.as_deref(),
        args.continent.as_deref(),
        args.desired_instance_size.as_deref(),
    )?;
    get_v1_projects_available_regions_execute(builder)
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_execute()` to send, or `get_v1_projects_ref` for simplest API.

pub fn get_v1_projects_ref_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_execute()` or `get_v1_projects_ref`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<V1ProjectWithDatabaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectWithDatabaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_task()`.
/// For the simplest API, use `get_v1_projects_ref()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1ProjectWithDatabaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_builder()` + `get_v1_projects_ref_execute()`.
/// For task-level control, use `get_v1_projects_ref_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1ProjectWithDatabaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_execute(builder)
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_execute()` to send, or `patch_v1_projects_ref` for simplest API.

pub fn patch_v1_projects_ref_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1UpdateProjectBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}", ref_rs,);

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_execute()` or `patch_v1_projects_ref`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectRefResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_task()`.
/// For the simplest API, use `patch_v1_projects_ref()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpdateProjectBody,
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_builder()` + `patch_v1_projects_ref_execute()`.
/// For task-level control, use `patch_v1_projects_ref_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_execute(builder)
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_execute()` to send, or `delete_v1_projects_ref` for simplest API.

pub fn delete_v1_projects_ref_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}", ref_rs,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_execute()` or `delete_v1_projects_ref`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectRefResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_task()`.
/// For the simplest API, use `delete_v1_projects_ref()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_builder()` + `delete_v1_projects_ref_execute()`.
/// For task-level control, use `delete_v1_projects_ref_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_builder(client, &args.ref_rs)?;
    delete_v1_projects_ref_execute(builder)
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_actions_execute()` to send, or `get_v1_projects_ref_actions` for simplest API.

pub fn get_v1_projects_ref_actions_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    offset: Option<f64>,
    limit: Option<f64>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/actions", ref_rs,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = offset {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_actions_execute()` or `get_v1_projects_ref_actions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_actions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_actions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListActionRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListActionRunResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_actions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_actions_task()`.
/// For the simplest API, use `get_v1_projects_ref_actions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_actions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_actions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListActionRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_actions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_actions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefActionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: offset
    pub offset: Option<f64>,
    /// Query parameter: limit
    pub limit: Option<f64>,
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_actions_builder()` + `get_v1_projects_ref_actions_execute()`.
/// For task-level control, use `get_v1_projects_ref_actions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_actions(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefActionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListActionRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_actions_builder(client, &args.ref_rs, args.offset, args.limit)?;
    get_v1_projects_ref_actions_execute(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_actions_run_id_execute()` to send, or `get_v1_projects_ref_actions_run_id` for simplest API.

pub fn get_v1_projects_ref_actions_run_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    run_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/actions/{}",
        ref_rs, run_id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_actions_run_id_execute()` or `get_v1_projects_ref_actions_run_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_actions_run_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_actions_run_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ActionRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ActionRunResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_actions_run_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_actions_run_id_task()`.
/// For the simplest API, use `get_v1_projects_ref_actions_run_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_actions_run_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_actions_run_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ActionRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_actions_run_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_actions_run_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefActionsRunIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: run_id
    pub run_id: String,
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_actions_run_id_builder()` + `get_v1_projects_ref_actions_run_id_execute()`.
/// For task-level control, use `get_v1_projects_ref_actions_run_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_actions_run_id(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefActionsRunIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ActionRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_actions_run_id_builder(client, &args.ref_rs, &args.run_id)?;
    get_v1_projects_ref_actions_run_id_execute(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_actions_run_id_logs_execute()` to send, or `get_v1_projects_ref_actions_run_id_logs` for simplest API.

pub fn get_v1_projects_ref_actions_run_id_logs_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    run_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/actions/{}/logs",
        ref_rs, run_id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_actions_run_id_logs_execute()` or `get_v1_projects_ref_actions_run_id_logs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_actions_run_id_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_actions_run_id_logs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_actions_run_id_logs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_actions_run_id_logs_task()`.
/// For the simplest API, use `get_v1_projects_ref_actions_run_id_logs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_actions_run_id_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_actions_run_id_logs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_actions_run_id_logs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_actions_run_id_logs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefActionsRunIdLogsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: run_id
    pub run_id: String,
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_actions_run_id_logs_builder()` + `get_v1_projects_ref_actions_run_id_logs_execute()`.
/// For task-level control, use `get_v1_projects_ref_actions_run_id_logs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_actions_run_id_logs(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefActionsRunIdLogsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_actions_run_id_logs_builder(client, &args.ref_rs, &args.run_id)?;
    get_v1_projects_ref_actions_run_id_logs_execute(builder)
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_actions_run_id_status_execute()` to send, or `patch_v1_projects_ref_actions_run_id_status` for simplest API.

pub fn patch_v1_projects_ref_actions_run_id_status_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    run_id: &str,
    body: &UpdateRunStatusBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/actions/{}/status",
        ref_rs, run_id,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_actions_run_id_status_execute()` or `patch_v1_projects_ref_actions_run_id_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_actions_run_id_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_actions_run_id_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UpdateRunStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateRunStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_actions_run_id_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_actions_run_id_status_task()`.
/// For the simplest API, use `patch_v1_projects_ref_actions_run_id_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_actions_run_id_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_actions_run_id_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UpdateRunStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_actions_run_id_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_actions_run_id_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefActionsRunIdStatusArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: run_id
    pub run_id: String,
    /// Request body.
    pub body: UpdateRunStatusBody,
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_actions_run_id_status_builder()` + `patch_v1_projects_ref_actions_run_id_status_execute()`.
/// For task-level control, use `patch_v1_projects_ref_actions_run_id_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_actions_run_id_status(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefActionsRunIdStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UpdateRunStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_actions_run_id_status_builder(
        client,
        &args.ref_rs,
        &args.run_id,
        &args.body,
    )?;
    patch_v1_projects_ref_actions_run_id_status_execute(builder)
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_advisors_performance_execute()` to send, or `get_v1_projects_ref_advisors_performance` for simplest API.

pub fn get_v1_projects_ref_advisors_performance_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/advisors/performance",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_advisors_performance_execute()` or `get_v1_projects_ref_advisors_performance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_advisors_performance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_advisors_performance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectAdvisorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_advisors_performance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_advisors_performance_task()`.
/// For the simplest API, use `get_v1_projects_ref_advisors_performance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_advisors_performance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_advisors_performance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_advisors_performance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_advisors_performance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefAdvisorsPerformanceArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_advisors_performance_builder()` + `get_v1_projects_ref_advisors_performance_execute()`.
/// For task-level control, use `get_v1_projects_ref_advisors_performance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_advisors_performance(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefAdvisorsPerformanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_advisors_performance_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_advisors_performance_execute(builder)
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_advisors_security_execute()` to send, or `get_v1_projects_ref_advisors_security` for simplest API.

pub fn get_v1_projects_ref_advisors_security_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    lint_type: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/advisors/security",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = lint_type {
        query_parts.push(format!("lint_type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_advisors_security_execute()` or `get_v1_projects_ref_advisors_security`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_advisors_security_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_advisors_security_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectAdvisorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_advisors_security_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_advisors_security_task()`.
/// For the simplest API, use `get_v1_projects_ref_advisors_security()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_advisors_security_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_advisors_security_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_advisors_security_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_advisors_security`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefAdvisorsSecurityArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: lint_type
    pub lint_type: Option<String>,
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_advisors_security_builder()` + `get_v1_projects_ref_advisors_security_execute()`.
/// For task-level control, use `get_v1_projects_ref_advisors_security_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_advisors_security(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefAdvisorsSecurityArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_advisors_security_builder(
        client,
        &args.ref_rs,
        args.lint_type.as_deref(),
    )?;
    get_v1_projects_ref_advisors_security_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_execute()` to send, or `get_v1_projects_ref_analytics_endpoints_functions_combined_stats` for simplest API.

pub fn get_v1_projects_ref_analytics_endpoints_functions_combined_stats_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    interval: Option<&str>,
    function_id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/functions.combined-stats",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interval {
        query_parts.push(format!("interval={}", val));
    }
    if let Some(val) = function_id {
        query_parts.push(format!("function_id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_execute()` or `get_v1_projects_ref_analytics_endpoints_functions_combined_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_functions_combined_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AnalyticsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyticsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_task()`.
/// For the simplest API, use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_analytics_endpoints_functions_combined_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyticsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_analytics_endpoints_functions_combined_stats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_analytics_endpoints_functions_combined_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefAnalyticsEndpointsFunctionsCombinedStatsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: interval
    pub interval: Option<String>,
    /// Query parameter: function_id
    pub function_id: Option<String>,
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_builder()` + `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_execute()`.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_functions_combined_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_functions_combined_stats(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefAnalyticsEndpointsFunctionsCombinedStatsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyticsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_analytics_endpoints_functions_combined_stats_builder(
        client,
        &args.ref_rs,
        args.interval.as_deref(),
        args.function_id.as_deref(),
    )?;
    get_v1_projects_ref_analytics_endpoints_functions_combined_stats_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_analytics_endpoints_logs_all_execute()` to send, or `get_v1_projects_ref_analytics_endpoints_logs_all` for simplest API.

pub fn get_v1_projects_ref_analytics_endpoints_logs_all_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    sql: Option<&str>,
    iso_timestamp_start: Option<&str>,
    iso_timestamp_end: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/logs.all",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sql {
        query_parts.push(format!("sql={}", val));
    }
    if let Some(val) = iso_timestamp_start {
        query_parts.push(format!("iso_timestamp_start={}", val));
    }
    if let Some(val) = iso_timestamp_end {
        query_parts.push(format!("iso_timestamp_end={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_analytics_endpoints_logs_all_execute()` or `get_v1_projects_ref_analytics_endpoints_logs_all`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_logs_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_logs_all_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AnalyticsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyticsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_analytics_endpoints_logs_all_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_logs_all_task()`.
/// For the simplest API, use `get_v1_projects_ref_analytics_endpoints_logs_all()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_logs_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_analytics_endpoints_logs_all_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyticsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_analytics_endpoints_logs_all_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_analytics_endpoints_logs_all`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefAnalyticsEndpointsLogsAllArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: sql
    pub sql: Option<String>,
    /// Query parameter: iso_timestamp_start
    pub iso_timestamp_start: Option<String>,
    /// Query parameter: iso_timestamp_end
    pub iso_timestamp_end: Option<String>,
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_analytics_endpoints_logs_all_builder()` + `get_v1_projects_ref_analytics_endpoints_logs_all_execute()`.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_logs_all_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_logs_all(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefAnalyticsEndpointsLogsAllArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyticsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_analytics_endpoints_logs_all_builder(
        client,
        &args.ref_rs,
        args.sql.as_deref(),
        args.iso_timestamp_start.as_deref(),
        args.iso_timestamp_end.as_deref(),
    )?;
    get_v1_projects_ref_analytics_endpoints_logs_all_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_analytics_endpoints_usage_api_counts_execute()` to send, or `get_v1_projects_ref_analytics_endpoints_usage_api_counts` for simplest API.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_counts_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    interval: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/usage.api-counts",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interval {
        query_parts.push(format!("interval={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_analytics_endpoints_usage_api_counts_execute()` or `get_v1_projects_ref_analytics_endpoints_usage_api_counts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_usage_api_counts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_counts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1GetUsageApiCountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1GetUsageApiCountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_analytics_endpoints_usage_api_counts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_usage_api_counts_task()`.
/// For the simplest API, use `get_v1_projects_ref_analytics_endpoints_usage_api_counts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_usage_api_counts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_counts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1GetUsageApiCountResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_analytics_endpoints_usage_api_counts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_analytics_endpoints_usage_api_counts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefAnalyticsEndpointsUsageApiCountsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: interval
    pub interval: Option<String>,
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_analytics_endpoints_usage_api_counts_builder()` + `get_v1_projects_ref_analytics_endpoints_usage_api_counts_execute()`.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_usage_api_counts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_counts(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefAnalyticsEndpointsUsageApiCountsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1GetUsageApiCountResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_analytics_endpoints_usage_api_counts_builder(
        client,
        &args.ref_rs,
        args.interval.as_deref(),
    )?;
    get_v1_projects_ref_analytics_endpoints_usage_api_counts_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_execute()` to send, or `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count` for simplest API.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/usage.api-requests-count",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_execute()` or `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<V1GetUsageApiRequestsCountResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1GetUsageApiRequestsCountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_task()`.
/// For the simplest API, use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1GetUsageApiRequestsCountResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_analytics_endpoints_usage_api_requests_count`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefAnalyticsEndpointsUsageApiRequestsCountArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_builder()` + `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_execute()`.
/// For task-level control, use `get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_analytics_endpoints_usage_api_requests_count(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefAnalyticsEndpointsUsageApiRequestsCountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<V1GetUsageApiRequestsCountResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_builder(
        client,
        &args.ref_rs,
    )?;
    get_v1_projects_ref_analytics_endpoints_usage_api_requests_count_execute(builder)
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_api_keys_execute()` to send, or `get_v1_projects_ref_api_keys` for simplest API.

pub fn get_v1_projects_ref_api_keys_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    reveal: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/api-keys", ref_rs,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_api_keys_execute()` or `get_v1_projects_ref_api_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_api_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_api_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_api_keys_task()`.
/// For the simplest API, use `get_v1_projects_ref_api_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_api_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_api_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_api_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefApiKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: reveal
    pub reveal: Option<bool>,
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_api_keys_builder()` + `get_v1_projects_ref_api_keys_execute()`.
/// For task-level control, use `get_v1_projects_ref_api_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_api_keys(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefApiKeysArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_api_keys_builder(client, &args.ref_rs, args.reveal)?;
    get_v1_projects_ref_api_keys_execute(builder)
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_api_keys_execute()` to send, or `post_v1_projects_ref_api_keys` for simplest API.

pub fn post_v1_projects_ref_api_keys_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    reveal: Option<bool>,
    body: &CreateApiKeyBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/api-keys", ref_rs,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_api_keys_execute()` or `post_v1_projects_ref_api_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_api_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_api_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_api_keys_task()`.
/// For the simplest API, use `post_v1_projects_ref_api_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_api_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_api_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_api_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefApiKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: reveal
    pub reveal: Option<bool>,
    /// Request body.
    pub body: CreateApiKeyBody,
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_api_keys_builder()` + `post_v1_projects_ref_api_keys_execute()`.
/// For task-level control, use `post_v1_projects_ref_api_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_api_keys(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefApiKeysArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_api_keys_builder(client, &args.ref_rs, args.reveal, &args.body)?;
    post_v1_projects_ref_api_keys_execute(builder)
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_api_keys_legacy_execute()` to send, or `get_v1_projects_ref_api_keys_legacy` for simplest API.

pub fn get_v1_projects_ref_api_keys_legacy_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/legacy",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_api_keys_legacy_execute()` or `get_v1_projects_ref_api_keys_legacy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_api_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_api_keys_legacy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LegacyApiKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_api_keys_legacy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_api_keys_legacy_task()`.
/// For the simplest API, use `get_v1_projects_ref_api_keys_legacy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_api_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_api_keys_legacy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_api_keys_legacy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_api_keys_legacy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefApiKeysLegacyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_api_keys_legacy_builder()` + `get_v1_projects_ref_api_keys_legacy_execute()`.
/// For task-level control, use `get_v1_projects_ref_api_keys_legacy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_api_keys_legacy(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefApiKeysLegacyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_api_keys_legacy_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_api_keys_legacy_execute(builder)
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_api_keys_legacy_execute()` to send, or `put_v1_projects_ref_api_keys_legacy` for simplest API.

pub fn put_v1_projects_ref_api_keys_legacy_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    enabled: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/legacy",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enabled {
        query_parts.push(format!("enabled={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_api_keys_legacy_execute()` or `put_v1_projects_ref_api_keys_legacy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_api_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_api_keys_legacy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LegacyApiKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_api_keys_legacy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_api_keys_legacy_task()`.
/// For the simplest API, use `put_v1_projects_ref_api_keys_legacy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_api_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_api_keys_legacy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_api_keys_legacy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_api_keys_legacy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefApiKeysLegacyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: enabled
    pub enabled: Option<bool>,
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_api_keys_legacy_builder()` + `put_v1_projects_ref_api_keys_legacy_execute()`.
/// For task-level control, use `put_v1_projects_ref_api_keys_legacy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_api_keys_legacy(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefApiKeysLegacyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_api_keys_legacy_builder(client, &args.ref_rs, args.enabled)?;
    put_v1_projects_ref_api_keys_legacy_execute(builder)
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_api_keys_id_execute()` to send, or `get_v1_projects_ref_api_keys_id` for simplest API.

pub fn get_v1_projects_ref_api_keys_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    id: &str,
    reveal: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/{}",
        ref_rs, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_api_keys_id_execute()` or `get_v1_projects_ref_api_keys_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_api_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_api_keys_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_api_keys_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_api_keys_id_task()`.
/// For the simplest API, use `get_v1_projects_ref_api_keys_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_api_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_api_keys_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_api_keys_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_api_keys_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefApiKeysIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: reveal
    pub reveal: Option<bool>,
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_api_keys_id_builder()` + `get_v1_projects_ref_api_keys_id_execute()`.
/// For task-level control, use `get_v1_projects_ref_api_keys_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_api_keys_id(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefApiKeysIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_api_keys_id_builder(client, &args.ref_rs, &args.id, args.reveal)?;
    get_v1_projects_ref_api_keys_id_execute(builder)
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_api_keys_id_execute()` to send, or `patch_v1_projects_ref_api_keys_id` for simplest API.

pub fn patch_v1_projects_ref_api_keys_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    id: &str,
    reveal: Option<bool>,
    body: &UpdateApiKeyBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/{}",
        ref_rs, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_api_keys_id_execute()` or `patch_v1_projects_ref_api_keys_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_api_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_api_keys_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_api_keys_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_api_keys_id_task()`.
/// For the simplest API, use `patch_v1_projects_ref_api_keys_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_api_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_api_keys_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_api_keys_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_api_keys_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefApiKeysIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: reveal
    pub reveal: Option<bool>,
    /// Request body.
    pub body: UpdateApiKeyBody,
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_api_keys_id_builder()` + `patch_v1_projects_ref_api_keys_id_execute()`.
/// For task-level control, use `patch_v1_projects_ref_api_keys_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_api_keys_id(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefApiKeysIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_api_keys_id_builder(
        client,
        &args.ref_rs,
        &args.id,
        args.reveal,
        &args.body,
    )?;
    patch_v1_projects_ref_api_keys_id_execute(builder)
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_api_keys_id_execute()` to send, or `delete_v1_projects_ref_api_keys_id` for simplest API.

pub fn delete_v1_projects_ref_api_keys_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    id: &str,
    reveal: Option<bool>,
    was_compromised: Option<bool>,
    reason: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/{}",
        ref_rs, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal {
        query_parts.push(format!("reveal={}", val));
    }
    if let Some(val) = was_compromised {
        query_parts.push(format!("was_compromised={}", val));
    }
    if let Some(val) = reason {
        query_parts.push(format!("reason={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_api_keys_id_execute()` or `delete_v1_projects_ref_api_keys_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_api_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_api_keys_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_api_keys_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_api_keys_id_task()`.
/// For the simplest API, use `delete_v1_projects_ref_api_keys_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_api_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_api_keys_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_api_keys_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_api_keys_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefApiKeysIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: reveal
    pub reveal: Option<bool>,
    /// Query parameter: was_compromised
    pub was_compromised: Option<bool>,
    /// Query parameter: reason
    pub reason: Option<String>,
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_api_keys_id_builder()` + `delete_v1_projects_ref_api_keys_id_execute()`.
/// For task-level control, use `delete_v1_projects_ref_api_keys_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_api_keys_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefApiKeysIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_api_keys_id_builder(
        client,
        &args.ref_rs,
        &args.id,
        args.reveal,
        args.was_compromised,
        args.reason.as_deref(),
    )?;
    delete_v1_projects_ref_api_keys_id_execute(builder)
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_billing_addons_execute()` to send, or `get_v1_projects_ref_billing_addons` for simplest API.

pub fn get_v1_projects_ref_billing_addons_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/billing/addons",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_billing_addons_execute()` or `get_v1_projects_ref_billing_addons`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_billing_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_billing_addons_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListProjectAddonsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProjectAddonsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_billing_addons_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_billing_addons_task()`.
/// For the simplest API, use `get_v1_projects_ref_billing_addons()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_billing_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_billing_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProjectAddonsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_billing_addons_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_billing_addons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefBillingAddonsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_billing_addons_builder()` + `get_v1_projects_ref_billing_addons_execute()`.
/// For task-level control, use `get_v1_projects_ref_billing_addons_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_billing_addons(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefBillingAddonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProjectAddonsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_billing_addons_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_billing_addons_execute(builder)
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_billing_addons_execute()` to send, or `patch_v1_projects_ref_billing_addons` for simplest API.

pub fn patch_v1_projects_ref_billing_addons_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &ApplyProjectAddonBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/billing/addons",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_billing_addons_execute()` or `patch_v1_projects_ref_billing_addons`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_billing_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_billing_addons_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_billing_addons_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_billing_addons_task()`.
/// For the simplest API, use `patch_v1_projects_ref_billing_addons()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_billing_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_billing_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_billing_addons_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_billing_addons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefBillingAddonsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: ApplyProjectAddonBody,
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_billing_addons_builder()` + `patch_v1_projects_ref_billing_addons_execute()`.
/// For task-level control, use `patch_v1_projects_ref_billing_addons_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_billing_addons(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefBillingAddonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_billing_addons_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_billing_addons_execute(builder)
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_billing_addons_addon_variant_execute()` to send, or `delete_v1_projects_ref_billing_addons_addon_variant` for simplest API.

pub fn delete_v1_projects_ref_billing_addons_addon_variant_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    addon_variant: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/billing/addons/{}",
        ref_rs, addon_variant,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_billing_addons_addon_variant_execute()` or `delete_v1_projects_ref_billing_addons_addon_variant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_billing_addons_addon_variant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_billing_addons_addon_variant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_billing_addons_addon_variant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_billing_addons_addon_variant_task()`.
/// For the simplest API, use `delete_v1_projects_ref_billing_addons_addon_variant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_billing_addons_addon_variant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_billing_addons_addon_variant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_billing_addons_addon_variant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_billing_addons_addon_variant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefBillingAddonsAddonVariantArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: addon_variant
    pub addon_variant: String,
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_billing_addons_addon_variant_builder()` + `delete_v1_projects_ref_billing_addons_addon_variant_execute()`.
/// For task-level control, use `delete_v1_projects_ref_billing_addons_addon_variant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_billing_addons_addon_variant(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefBillingAddonsAddonVariantArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_billing_addons_addon_variant_builder(
        client,
        &args.ref_rs,
        &args.addon_variant,
    )?;
    delete_v1_projects_ref_billing_addons_addon_variant_execute(builder)
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_branches_execute()` to send, or `get_v1_projects_ref_branches` for simplest API.

pub fn get_v1_projects_ref_branches_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/branches", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_branches_execute()` or `get_v1_projects_ref_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_branches_task()`.
/// For the simplest API, use `get_v1_projects_ref_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefBranchesArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_branches_builder()` + `get_v1_projects_ref_branches_execute()`.
/// For task-level control, use `get_v1_projects_ref_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_branches(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefBranchesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_branches_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_branches_execute(builder)
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_branches_execute()` to send, or `post_v1_projects_ref_branches` for simplest API.

pub fn post_v1_projects_ref_branches_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &CreateBranchBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/branches", ref_rs,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_branches_execute()` or `post_v1_projects_ref_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_branches_task()`.
/// For the simplest API, use `post_v1_projects_ref_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefBranchesArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateBranchBody,
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_branches_builder()` + `post_v1_projects_ref_branches_execute()`.
/// For task-level control, use `post_v1_projects_ref_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_branches(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefBranchesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_branches_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_branches_execute(builder)
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_branches_execute()` to send, or `delete_v1_projects_ref_branches` for simplest API.

pub fn delete_v1_projects_ref_branches_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/branches", ref_rs,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_branches_execute()` or `delete_v1_projects_ref_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_branches_task()`.
/// For the simplest API, use `delete_v1_projects_ref_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefBranchesArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_branches_builder()` + `delete_v1_projects_ref_branches_execute()`.
/// For task-level control, use `delete_v1_projects_ref_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_branches(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefBranchesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_branches_builder(client, &args.ref_rs)?;
    delete_v1_projects_ref_branches_execute(builder)
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_branches_name_execute()` to send, or `get_v1_projects_ref_branches_name` for simplest API.

pub fn get_v1_projects_ref_branches_name_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/branches/{}",
        ref_rs, name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_branches_name_execute()` or `get_v1_projects_ref_branches_name`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_branches_name_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_branches_name_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_branches_name_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_branches_name_task()`.
/// For the simplest API, use `get_v1_projects_ref_branches_name()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_branches_name_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_branches_name_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_branches_name_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_branches_name`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefBranchesNameArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: name
    pub name: String,
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_branches_name_builder()` + `get_v1_projects_ref_branches_name_execute()`.
/// For task-level control, use `get_v1_projects_ref_branches_name_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_branches_name(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefBranchesNameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BranchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_branches_name_builder(client, &args.ref_rs, &args.name)?;
    get_v1_projects_ref_branches_name_execute(builder)
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_claim_token_execute()` to send, or `get_v1_projects_ref_claim_token` for simplest API.

pub fn get_v1_projects_ref_claim_token_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_claim_token_execute()` or `get_v1_projects_ref_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ProjectClaimTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectClaimTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_claim_token_task()`.
/// For the simplest API, use `get_v1_projects_ref_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectClaimTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefClaimTokenArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_claim_token_builder()` + `get_v1_projects_ref_claim_token_execute()`.
/// For task-level control, use `get_v1_projects_ref_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_claim_token(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefClaimTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectClaimTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_claim_token_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_claim_token_execute(builder)
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_claim_token_execute()` to send, or `post_v1_projects_ref_claim_token` for simplest API.

pub fn post_v1_projects_ref_claim_token_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_claim_token_execute()` or `post_v1_projects_ref_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<CreateProjectClaimTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateProjectClaimTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_claim_token_task()`.
/// For the simplest API, use `post_v1_projects_ref_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateProjectClaimTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefClaimTokenArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_claim_token_builder()` + `post_v1_projects_ref_claim_token_execute()`.
/// For task-level control, use `post_v1_projects_ref_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_claim_token(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefClaimTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateProjectClaimTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_claim_token_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_claim_token_execute(builder)
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_claim_token_execute()` to send, or `delete_v1_projects_ref_claim_token` for simplest API.

pub fn delete_v1_projects_ref_claim_token_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        ref_rs,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_claim_token_execute()` or `delete_v1_projects_ref_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_claim_token_task()`.
/// For the simplest API, use `delete_v1_projects_ref_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefClaimTokenArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_claim_token_builder()` + `delete_v1_projects_ref_claim_token_execute()`.
/// For task-level control, use `delete_v1_projects_ref_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_claim_token(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefClaimTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_claim_token_builder(client, &args.ref_rs)?;
    delete_v1_projects_ref_claim_token_execute(builder)
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_cli_login_role_execute()` to send, or `post_v1_projects_ref_cli_login_role` for simplest API.

pub fn post_v1_projects_ref_cli_login_role_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &CreateRoleBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/cli/login-role",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_cli_login_role_execute()` or `post_v1_projects_ref_cli_login_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_cli_login_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_cli_login_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CreateRoleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateRoleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_cli_login_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_cli_login_role_task()`.
/// For the simplest API, use `post_v1_projects_ref_cli_login_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_cli_login_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_cli_login_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateRoleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_cli_login_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_cli_login_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefCliLoginRoleArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateRoleBody,
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_cli_login_role_builder()` + `post_v1_projects_ref_cli_login_role_execute()`.
/// For task-level control, use `post_v1_projects_ref_cli_login_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_cli_login_role(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefCliLoginRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateRoleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_cli_login_role_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_cli_login_role_execute(builder)
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_cli_login_role_execute()` to send, or `delete_v1_projects_ref_cli_login_role` for simplest API.

pub fn delete_v1_projects_ref_cli_login_role_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/cli/login-role",
        ref_rs,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_cli_login_role_execute()` or `delete_v1_projects_ref_cli_login_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_cli_login_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_cli_login_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DeleteRolesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteRolesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_cli_login_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_cli_login_role_task()`.
/// For the simplest API, use `delete_v1_projects_ref_cli_login_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_cli_login_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_cli_login_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteRolesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_cli_login_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_cli_login_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefCliLoginRoleArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_cli_login_role_builder()` + `delete_v1_projects_ref_cli_login_role_execute()`.
/// For task-level control, use `delete_v1_projects_ref_cli_login_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_cli_login_role(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefCliLoginRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteRolesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_cli_login_role_builder(client, &args.ref_rs)?;
    delete_v1_projects_ref_cli_login_role_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_execute()` to send, or `get_v1_projects_ref_config_auth` for simplest API.

pub fn get_v1_projects_ref_config_auth_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_execute()` or `get_v1_projects_ref_config_auth`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AuthConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_builder()` + `get_v1_projects_ref_config_auth_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_auth_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_auth_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_config_auth_execute()` to send, or `patch_v1_projects_ref_config_auth` for simplest API.

pub fn patch_v1_projects_ref_config_auth_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdateAuthConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_config_auth_execute()` or `patch_v1_projects_ref_config_auth`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_auth_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AuthConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_config_auth_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_config_auth_task()`.
/// For the simplest API, use `patch_v1_projects_ref_config_auth()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_config_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_config_auth_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_config_auth`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefConfigAuthArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateAuthConfigBody,
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_config_auth_builder()` + `patch_v1_projects_ref_config_auth_execute()`.
/// For task-level control, use `patch_v1_projects_ref_config_auth_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_auth(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefConfigAuthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_config_auth_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_config_auth_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_signing_keys_execute()` to send, or `get_v1_projects_ref_config_auth_signing_keys` for simplest API.

pub fn get_v1_projects_ref_config_auth_signing_keys_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_signing_keys_execute()` or `get_v1_projects_ref_config_auth_signing_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_signing_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_signing_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_signing_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_signing_keys_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_signing_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_signing_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_signing_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_signing_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_signing_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthSigningKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_signing_keys_builder()` + `get_v1_projects_ref_config_auth_signing_keys_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_signing_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_signing_keys(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthSigningKeysArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_auth_signing_keys_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_auth_signing_keys_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_config_auth_signing_keys_execute()` to send, or `post_v1_projects_ref_config_auth_signing_keys` for simplest API.

pub fn post_v1_projects_ref_config_auth_signing_keys_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &CreateSigningKeyBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_config_auth_signing_keys_execute()` or `post_v1_projects_ref_config_auth_signing_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_signing_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_signing_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_config_auth_signing_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_config_auth_signing_keys_task()`.
/// For the simplest API, use `post_v1_projects_ref_config_auth_signing_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_signing_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_config_auth_signing_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_config_auth_signing_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_config_auth_signing_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefConfigAuthSigningKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateSigningKeyBody,
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_config_auth_signing_keys_builder()` + `post_v1_projects_ref_config_auth_signing_keys_execute()`.
/// For task-level control, use `post_v1_projects_ref_config_auth_signing_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_signing_keys(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefConfigAuthSigningKeysArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_config_auth_signing_keys_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_config_auth_signing_keys_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_signing_keys_legacy_execute()` to send, or `get_v1_projects_ref_config_auth_signing_keys_legacy` for simplest API.

pub fn get_v1_projects_ref_config_auth_signing_keys_legacy_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/legacy",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_signing_keys_legacy_execute()` or `get_v1_projects_ref_config_auth_signing_keys_legacy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_signing_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_signing_keys_legacy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_signing_keys_legacy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_signing_keys_legacy_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_signing_keys_legacy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_signing_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_signing_keys_legacy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_signing_keys_legacy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_signing_keys_legacy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthSigningKeysLegacyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_signing_keys_legacy_builder()` + `get_v1_projects_ref_config_auth_signing_keys_legacy_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_signing_keys_legacy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_signing_keys_legacy(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthSigningKeysLegacyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_config_auth_signing_keys_legacy_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_auth_signing_keys_legacy_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_config_auth_signing_keys_legacy_execute()` to send, or `post_v1_projects_ref_config_auth_signing_keys_legacy` for simplest API.

pub fn post_v1_projects_ref_config_auth_signing_keys_legacy_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/legacy",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_config_auth_signing_keys_legacy_execute()` or `post_v1_projects_ref_config_auth_signing_keys_legacy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_signing_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_signing_keys_legacy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_config_auth_signing_keys_legacy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_config_auth_signing_keys_legacy_task()`.
/// For the simplest API, use `post_v1_projects_ref_config_auth_signing_keys_legacy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_signing_keys_legacy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_config_auth_signing_keys_legacy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_config_auth_signing_keys_legacy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_config_auth_signing_keys_legacy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefConfigAuthSigningKeysLegacyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_config_auth_signing_keys_legacy_builder()` + `post_v1_projects_ref_config_auth_signing_keys_legacy_execute()`.
/// For task-level control, use `post_v1_projects_ref_config_auth_signing_keys_legacy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_signing_keys_legacy(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefConfigAuthSigningKeysLegacyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_config_auth_signing_keys_legacy_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_config_auth_signing_keys_legacy_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_signing_keys_id_execute()` to send, or `get_v1_projects_ref_config_auth_signing_keys_id` for simplest API.

pub fn get_v1_projects_ref_config_auth_signing_keys_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        id, ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_signing_keys_id_execute()` or `get_v1_projects_ref_config_auth_signing_keys_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_signing_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_signing_keys_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_signing_keys_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_signing_keys_id_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_signing_keys_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_signing_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_signing_keys_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_signing_keys_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_signing_keys_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthSigningKeysIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_signing_keys_id_builder()` + `get_v1_projects_ref_config_auth_signing_keys_id_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_signing_keys_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_signing_keys_id(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthSigningKeysIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_config_auth_signing_keys_id_builder(client, &args.id, &args.ref_rs)?;
    get_v1_projects_ref_config_auth_signing_keys_id_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_config_auth_signing_keys_id_execute()` to send, or `patch_v1_projects_ref_config_auth_signing_keys_id` for simplest API.

pub fn patch_v1_projects_ref_config_auth_signing_keys_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    ref_rs: &str,
    body: &UpdateSigningKeyBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        id, ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_config_auth_signing_keys_id_execute()` or `patch_v1_projects_ref_config_auth_signing_keys_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_auth_signing_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_auth_signing_keys_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_config_auth_signing_keys_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_config_auth_signing_keys_id_task()`.
/// For the simplest API, use `patch_v1_projects_ref_config_auth_signing_keys_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_auth_signing_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_config_auth_signing_keys_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_config_auth_signing_keys_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_config_auth_signing_keys_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefConfigAuthSigningKeysIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateSigningKeyBody,
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_config_auth_signing_keys_id_builder()` + `patch_v1_projects_ref_config_auth_signing_keys_id_execute()`.
/// For task-level control, use `patch_v1_projects_ref_config_auth_signing_keys_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_auth_signing_keys_id(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefConfigAuthSigningKeysIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_config_auth_signing_keys_id_builder(
        client,
        &args.id,
        &args.ref_rs,
        &args.body,
    )?;
    patch_v1_projects_ref_config_auth_signing_keys_id_execute(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_config_auth_signing_keys_id_execute()` to send, or `delete_v1_projects_ref_config_auth_signing_keys_id` for simplest API.

pub fn delete_v1_projects_ref_config_auth_signing_keys_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        id, ref_rs,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_config_auth_signing_keys_id_execute()` or `delete_v1_projects_ref_config_auth_signing_keys_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_config_auth_signing_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_config_auth_signing_keys_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_config_auth_signing_keys_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_config_auth_signing_keys_id_task()`.
/// For the simplest API, use `delete_v1_projects_ref_config_auth_signing_keys_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_config_auth_signing_keys_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_config_auth_signing_keys_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_config_auth_signing_keys_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_config_auth_signing_keys_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefConfigAuthSigningKeysIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_config_auth_signing_keys_id_builder()` + `delete_v1_projects_ref_config_auth_signing_keys_id_execute()`.
/// For task-level control, use `delete_v1_projects_ref_config_auth_signing_keys_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_config_auth_signing_keys_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefConfigAuthSigningKeysIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigningKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_projects_ref_config_auth_signing_keys_id_builder(client, &args.id, &args.ref_rs)?;
    delete_v1_projects_ref_config_auth_signing_keys_id_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_sso_providers_execute()` to send, or `get_v1_projects_ref_config_auth_sso_providers` for simplest API.

pub fn get_v1_projects_ref_config_auth_sso_providers_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_sso_providers_execute()` or `get_v1_projects_ref_config_auth_sso_providers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_sso_providers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_sso_providers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListProvidersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProvidersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_sso_providers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_sso_providers_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_sso_providers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_sso_providers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_sso_providers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProvidersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_sso_providers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_sso_providers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthSsoProvidersArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_sso_providers_builder()` + `get_v1_projects_ref_config_auth_sso_providers_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_sso_providers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_sso_providers(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthSsoProvidersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProvidersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_auth_sso_providers_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_auth_sso_providers_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_config_auth_sso_providers_execute()` to send, or `post_v1_projects_ref_config_auth_sso_providers` for simplest API.

pub fn post_v1_projects_ref_config_auth_sso_providers_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &CreateProviderBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_config_auth_sso_providers_execute()` or `post_v1_projects_ref_config_auth_sso_providers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_sso_providers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_sso_providers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CreateProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_config_auth_sso_providers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_config_auth_sso_providers_task()`.
/// For the simplest API, use `post_v1_projects_ref_config_auth_sso_providers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_sso_providers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_config_auth_sso_providers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_config_auth_sso_providers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_config_auth_sso_providers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefConfigAuthSsoProvidersArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateProviderBody,
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_config_auth_sso_providers_builder()` + `post_v1_projects_ref_config_auth_sso_providers_execute()`.
/// For task-level control, use `post_v1_projects_ref_config_auth_sso_providers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_sso_providers(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefConfigAuthSsoProvidersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_config_auth_sso_providers_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_config_auth_sso_providers_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_sso_providers_provider_id_execute()` to send, or `get_v1_projects_ref_config_auth_sso_providers_provider_id` for simplest API.

pub fn get_v1_projects_ref_config_auth_sso_providers_provider_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    provider_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        ref_rs, provider_id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_sso_providers_provider_id_execute()` or `get_v1_projects_ref_config_auth_sso_providers_provider_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_sso_providers_provider_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_sso_providers_provider_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GetProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_sso_providers_provider_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_sso_providers_provider_id_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_sso_providers_provider_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_sso_providers_provider_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_sso_providers_provider_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_sso_providers_provider_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_sso_providers_provider_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthSsoProvidersProviderIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: provider_id
    pub provider_id: String,
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_sso_providers_provider_id_builder()` + `get_v1_projects_ref_config_auth_sso_providers_provider_id_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_sso_providers_provider_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_sso_providers_provider_id(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthSsoProvidersProviderIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_auth_sso_providers_provider_id_builder(
        client,
        &args.ref_rs,
        &args.provider_id,
    )?;
    get_v1_projects_ref_config_auth_sso_providers_provider_id_execute(builder)
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_config_auth_sso_providers_provider_id_execute()` to send, or `put_v1_projects_ref_config_auth_sso_providers_provider_id` for simplest API.

pub fn put_v1_projects_ref_config_auth_sso_providers_provider_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    provider_id: &str,
    body: &UpdateProviderBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        ref_rs, provider_id,
    );

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_config_auth_sso_providers_provider_id_execute()` or `put_v1_projects_ref_config_auth_sso_providers_provider_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_config_auth_sso_providers_provider_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_config_auth_sso_providers_provider_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UpdateProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_config_auth_sso_providers_provider_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_config_auth_sso_providers_provider_id_task()`.
/// For the simplest API, use `put_v1_projects_ref_config_auth_sso_providers_provider_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_config_auth_sso_providers_provider_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_config_auth_sso_providers_provider_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UpdateProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_config_auth_sso_providers_provider_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_config_auth_sso_providers_provider_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefConfigAuthSsoProvidersProviderIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: provider_id
    pub provider_id: String,
    /// Request body.
    pub body: UpdateProviderBody,
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_config_auth_sso_providers_provider_id_builder()` + `put_v1_projects_ref_config_auth_sso_providers_provider_id_execute()`.
/// For task-level control, use `put_v1_projects_ref_config_auth_sso_providers_provider_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_config_auth_sso_providers_provider_id(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefConfigAuthSsoProvidersProviderIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UpdateProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_config_auth_sso_providers_provider_id_builder(
        client,
        &args.ref_rs,
        &args.provider_id,
        &args.body,
    )?;
    put_v1_projects_ref_config_auth_sso_providers_provider_id_execute(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_config_auth_sso_providers_provider_id_execute()` to send, or `delete_v1_projects_ref_config_auth_sso_providers_provider_id` for simplest API.

pub fn delete_v1_projects_ref_config_auth_sso_providers_provider_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    provider_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        ref_rs, provider_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_config_auth_sso_providers_provider_id_execute()` or `delete_v1_projects_ref_config_auth_sso_providers_provider_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_config_auth_sso_providers_provider_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_config_auth_sso_providers_provider_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DeleteProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_config_auth_sso_providers_provider_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_config_auth_sso_providers_provider_id_task()`.
/// For the simplest API, use `delete_v1_projects_ref_config_auth_sso_providers_provider_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_config_auth_sso_providers_provider_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_config_auth_sso_providers_provider_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_config_auth_sso_providers_provider_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_config_auth_sso_providers_provider_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefConfigAuthSsoProvidersProviderIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: provider_id
    pub provider_id: String,
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_config_auth_sso_providers_provider_id_builder()` + `delete_v1_projects_ref_config_auth_sso_providers_provider_id_execute()`.
/// For task-level control, use `delete_v1_projects_ref_config_auth_sso_providers_provider_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_config_auth_sso_providers_provider_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefConfigAuthSsoProvidersProviderIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteProviderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_config_auth_sso_providers_provider_id_builder(
        client,
        &args.ref_rs,
        &args.provider_id,
    )?;
    delete_v1_projects_ref_config_auth_sso_providers_provider_id_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_third_party_auth_execute()` to send, or `get_v1_projects_ref_config_auth_third_party_auth` for simplest API.

pub fn get_v1_projects_ref_config_auth_third_party_auth_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_third_party_auth_execute()` or `get_v1_projects_ref_config_auth_third_party_auth`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_third_party_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_third_party_auth_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_third_party_auth_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_third_party_auth_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_third_party_auth()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_third_party_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_third_party_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_third_party_auth_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_third_party_auth`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthThirdPartyAuthArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_third_party_auth_builder()` + `get_v1_projects_ref_config_auth_third_party_auth_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_third_party_auth_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_third_party_auth(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthThirdPartyAuthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_auth_third_party_auth_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_auth_third_party_auth_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_config_auth_third_party_auth_execute()` to send, or `post_v1_projects_ref_config_auth_third_party_auth` for simplest API.

pub fn post_v1_projects_ref_config_auth_third_party_auth_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &CreateThirdPartyAuthBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_config_auth_third_party_auth_execute()` or `post_v1_projects_ref_config_auth_third_party_auth`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_third_party_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_third_party_auth_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_config_auth_third_party_auth_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_config_auth_third_party_auth_task()`.
/// For the simplest API, use `post_v1_projects_ref_config_auth_third_party_auth()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_auth_third_party_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_config_auth_third_party_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_config_auth_third_party_auth_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_config_auth_third_party_auth`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefConfigAuthThirdPartyAuthArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateThirdPartyAuthBody,
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_config_auth_third_party_auth_builder()` + `post_v1_projects_ref_config_auth_third_party_auth_execute()`.
/// For task-level control, use `post_v1_projects_ref_config_auth_third_party_auth_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_auth_third_party_auth(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefConfigAuthThirdPartyAuthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_config_auth_third_party_auth_builder(
        client,
        &args.ref_rs,
        &args.body,
    )?;
    post_v1_projects_ref_config_auth_third_party_auth_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute()` to send, or `get_v1_projects_ref_config_auth_third_party_auth_tpa_id` for simplest API.

pub fn get_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    tpa_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth/{}",
        ref_rs, tpa_id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute()` or `get_v1_projects_ref_config_auth_third_party_auth_tpa_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_third_party_auth_tpa_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_auth_third_party_auth_tpa_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_auth_third_party_auth_tpa_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigAuthThirdPartyAuthTpaIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: tpa_id
    pub tpa_id: String,
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()` + `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_auth_third_party_auth_tpa_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_auth_third_party_auth_tpa_id(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigAuthThirdPartyAuthTpaIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder(
        client,
        &args.ref_rs,
        &args.tpa_id,
    )?;
    get_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute()` to send, or `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id` for simplest API.

pub fn delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    tpa_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth/{}",
        ref_rs, tpa_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute()` or `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_task()`.
/// For the simplest API, use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_config_auth_third_party_auth_tpa_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefConfigAuthThirdPartyAuthTpaIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: tpa_id
    pub tpa_id: String,
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder()` + `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute()`.
/// For task-level control, use `delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_config_auth_third_party_auth_tpa_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefConfigAuthThirdPartyAuthTpaIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyAuth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_builder(
        client,
        &args.ref_rs,
        &args.tpa_id,
    )?;
    delete_v1_projects_ref_config_auth_third_party_auth_tpa_id_execute(builder)
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_database_pgbouncer_execute()` to send, or `get_v1_projects_ref_config_database_pgbouncer` for simplest API.

pub fn get_v1_projects_ref_config_database_pgbouncer_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/pgbouncer",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_database_pgbouncer_execute()` or `get_v1_projects_ref_config_database_pgbouncer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_database_pgbouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_database_pgbouncer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1PgbouncerConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1PgbouncerConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_database_pgbouncer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_database_pgbouncer_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_database_pgbouncer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_database_pgbouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_database_pgbouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1PgbouncerConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_database_pgbouncer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_database_pgbouncer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigDatabasePgbouncerArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_database_pgbouncer_builder()` + `get_v1_projects_ref_config_database_pgbouncer_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_database_pgbouncer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_database_pgbouncer(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigDatabasePgbouncerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1PgbouncerConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_database_pgbouncer_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_database_pgbouncer_execute(builder)
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_database_pooler_execute()` to send, or `get_v1_projects_ref_config_database_pooler` for simplest API.

pub fn get_v1_projects_ref_config_database_pooler_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/pooler",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_database_pooler_execute()` or `get_v1_projects_ref_config_database_pooler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_database_pooler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_database_pooler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_database_pooler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_database_pooler_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_database_pooler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_database_pooler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_database_pooler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_database_pooler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_database_pooler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigDatabasePoolerArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_database_pooler_builder()` + `get_v1_projects_ref_config_database_pooler_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_database_pooler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_database_pooler(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigDatabasePoolerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_database_pooler_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_database_pooler_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_config_database_pooler_execute()` to send, or `patch_v1_projects_ref_config_database_pooler` for simplest API.

pub fn patch_v1_projects_ref_config_database_pooler_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdateSupavisorConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/pooler",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_config_database_pooler_execute()` or `patch_v1_projects_ref_config_database_pooler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_database_pooler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_database_pooler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<UpdateSupavisorConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateSupavisorConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_config_database_pooler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_config_database_pooler_task()`.
/// For the simplest API, use `patch_v1_projects_ref_config_database_pooler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_database_pooler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_config_database_pooler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateSupavisorConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_config_database_pooler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_config_database_pooler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefConfigDatabasePoolerArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateSupavisorConfigBody,
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_config_database_pooler_builder()` + `patch_v1_projects_ref_config_database_pooler_execute()`.
/// For task-level control, use `patch_v1_projects_ref_config_database_pooler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_database_pooler(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefConfigDatabasePoolerArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateSupavisorConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        patch_v1_projects_ref_config_database_pooler_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_config_database_pooler_execute(builder)
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_database_postgres_execute()` to send, or `get_v1_projects_ref_config_database_postgres` for simplest API.

pub fn get_v1_projects_ref_config_database_postgres_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/postgres",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_database_postgres_execute()` or `get_v1_projects_ref_config_database_postgres`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_database_postgres_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_database_postgres_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PostgresConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostgresConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_database_postgres_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_database_postgres_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_database_postgres()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_database_postgres_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_database_postgres_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostgresConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_database_postgres_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_database_postgres`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigDatabasePostgresArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_database_postgres_builder()` + `get_v1_projects_ref_config_database_postgres_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_database_postgres_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_database_postgres(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigDatabasePostgresArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostgresConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_database_postgres_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_database_postgres_execute(builder)
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_config_database_postgres_execute()` to send, or `put_v1_projects_ref_config_database_postgres` for simplest API.

pub fn put_v1_projects_ref_config_database_postgres_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdatePostgresConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/postgres",
        ref_rs,
    );

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_config_database_postgres_execute()` or `put_v1_projects_ref_config_database_postgres`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_config_database_postgres_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_config_database_postgres_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PostgresConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostgresConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_config_database_postgres_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_config_database_postgres_task()`.
/// For the simplest API, use `put_v1_projects_ref_config_database_postgres()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_config_database_postgres_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_config_database_postgres_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostgresConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_config_database_postgres_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_config_database_postgres`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefConfigDatabasePostgresArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdatePostgresConfigBody,
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_config_database_postgres_builder()` + `put_v1_projects_ref_config_database_postgres_execute()`.
/// For task-level control, use `put_v1_projects_ref_config_database_postgres_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_config_database_postgres(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefConfigDatabasePostgresArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostgresConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        put_v1_projects_ref_config_database_postgres_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_config_database_postgres_execute(builder)
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_disk_execute()` to send, or `get_v1_projects_ref_config_disk` for simplest API.

pub fn get_v1_projects_ref_config_disk_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_disk_execute()` or `get_v1_projects_ref_config_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DiskResponse>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_disk_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigDiskArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_disk_builder()` + `get_v1_projects_ref_config_disk_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_disk(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigDiskArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_disk_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_disk_execute(builder)
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_config_disk_execute()` to send, or `post_v1_projects_ref_config_disk` for simplest API.

pub fn post_v1_projects_ref_config_disk_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &DiskRequestBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_config_disk_execute()` or `post_v1_projects_ref_config_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_config_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_config_disk_task()`.
/// For the simplest API, use `post_v1_projects_ref_config_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_config_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_config_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_config_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefConfigDiskArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: DiskRequestBody,
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_config_disk_builder()` + `post_v1_projects_ref_config_disk_execute()`.
/// For task-level control, use `post_v1_projects_ref_config_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_disk(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefConfigDiskArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_config_disk_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_config_disk_execute(builder)
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_disk_autoscale_execute()` to send, or `get_v1_projects_ref_config_disk_autoscale` for simplest API.

pub fn get_v1_projects_ref_config_disk_autoscale_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk/autoscale",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_disk_autoscale_execute()` or `get_v1_projects_ref_config_disk_autoscale`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_disk_autoscale_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_disk_autoscale_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DiskAutoscaleConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskAutoscaleConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_disk_autoscale_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_disk_autoscale_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_disk_autoscale()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_disk_autoscale_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_disk_autoscale_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskAutoscaleConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_disk_autoscale_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_disk_autoscale`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigDiskAutoscaleArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_disk_autoscale_builder()` + `get_v1_projects_ref_config_disk_autoscale_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_disk_autoscale_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_disk_autoscale(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigDiskAutoscaleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskAutoscaleConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_disk_autoscale_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_disk_autoscale_execute(builder)
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_disk_util_execute()` to send, or `get_v1_projects_ref_config_disk_util` for simplest API.

pub fn get_v1_projects_ref_config_disk_util_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk/util",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_disk_util_execute()` or `get_v1_projects_ref_config_disk_util`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_disk_util_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_disk_util_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DiskUtilMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskUtilMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_disk_util_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_disk_util_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_disk_util()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_disk_util_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_disk_util_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskUtilMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_disk_util_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_disk_util`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigDiskUtilArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_disk_util_builder()` + `get_v1_projects_ref_config_disk_util_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_disk_util_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_disk_util(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigDiskUtilArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskUtilMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_disk_util_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_disk_util_execute(builder)
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_realtime_execute()` to send, or `get_v1_projects_ref_config_realtime` for simplest API.

pub fn get_v1_projects_ref_config_realtime_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/realtime",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_realtime_execute()` or `get_v1_projects_ref_config_realtime`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_realtime_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_realtime_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<RealtimeConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RealtimeConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_realtime_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_realtime_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_realtime()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_realtime_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_realtime_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RealtimeConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_realtime_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_realtime`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigRealtimeArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_realtime_builder()` + `get_v1_projects_ref_config_realtime_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_realtime_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_realtime(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigRealtimeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RealtimeConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_realtime_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_realtime_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_config_realtime_execute()` to send, or `patch_v1_projects_ref_config_realtime` for simplest API.

pub fn patch_v1_projects_ref_config_realtime_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdateRealtimeConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/realtime",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_config_realtime_execute()` or `patch_v1_projects_ref_config_realtime`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_realtime_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_realtime_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_config_realtime_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_config_realtime_task()`.
/// For the simplest API, use `patch_v1_projects_ref_config_realtime()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_realtime_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_config_realtime_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_config_realtime_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_config_realtime`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefConfigRealtimeArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateRealtimeConfigBody,
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_config_realtime_builder()` + `patch_v1_projects_ref_config_realtime_execute()`.
/// For task-level control, use `patch_v1_projects_ref_config_realtime_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_realtime(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefConfigRealtimeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_config_realtime_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_config_realtime_execute(builder)
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_config_realtime_shutdown_execute()` to send, or `post_v1_projects_ref_config_realtime_shutdown` for simplest API.

pub fn post_v1_projects_ref_config_realtime_shutdown_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/realtime/shutdown",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_config_realtime_shutdown_execute()` or `post_v1_projects_ref_config_realtime_shutdown`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_realtime_shutdown_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_realtime_shutdown_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_config_realtime_shutdown_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_config_realtime_shutdown_task()`.
/// For the simplest API, use `post_v1_projects_ref_config_realtime_shutdown()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_config_realtime_shutdown_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_config_realtime_shutdown_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_config_realtime_shutdown_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_config_realtime_shutdown`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefConfigRealtimeShutdownArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_config_realtime_shutdown_builder()` + `post_v1_projects_ref_config_realtime_shutdown_execute()`.
/// For task-level control, use `post_v1_projects_ref_config_realtime_shutdown_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_config_realtime_shutdown(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefConfigRealtimeShutdownArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_config_realtime_shutdown_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_config_realtime_shutdown_execute(builder)
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_config_storage_execute()` to send, or `get_v1_projects_ref_config_storage` for simplest API.

pub fn get_v1_projects_ref_config_storage_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/storage",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_config_storage_execute()` or `get_v1_projects_ref_config_storage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_storage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_storage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<StorageConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorageConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_config_storage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_config_storage_task()`.
/// For the simplest API, use `get_v1_projects_ref_config_storage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_config_storage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_config_storage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorageConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_config_storage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_config_storage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefConfigStorageArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_config_storage_builder()` + `get_v1_projects_ref_config_storage_execute()`.
/// For task-level control, use `get_v1_projects_ref_config_storage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_config_storage(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefConfigStorageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorageConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_config_storage_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_config_storage_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_config_storage_execute()` to send, or `patch_v1_projects_ref_config_storage` for simplest API.

pub fn patch_v1_projects_ref_config_storage_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdateStorageConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/config/storage",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_config_storage_execute()` or `patch_v1_projects_ref_config_storage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_storage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_storage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_config_storage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_config_storage_task()`.
/// For the simplest API, use `patch_v1_projects_ref_config_storage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_config_storage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_config_storage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_config_storage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_config_storage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefConfigStorageArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateStorageConfigBody,
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_config_storage_builder()` + `patch_v1_projects_ref_config_storage_execute()`.
/// For task-level control, use `patch_v1_projects_ref_config_storage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_config_storage(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefConfigStorageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_config_storage_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_config_storage_execute(builder)
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_custom_hostname_execute()` to send, or `get_v1_projects_ref_custom_hostname` for simplest API.

pub fn get_v1_projects_ref_custom_hostname_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_custom_hostname_execute()` or `get_v1_projects_ref_custom_hostname`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_custom_hostname_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_custom_hostname_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_custom_hostname_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_custom_hostname_task()`.
/// For the simplest API, use `get_v1_projects_ref_custom_hostname()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_custom_hostname_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_custom_hostname_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_custom_hostname_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_custom_hostname`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefCustomHostnameArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_custom_hostname_builder()` + `get_v1_projects_ref_custom_hostname_execute()`.
/// For task-level control, use `get_v1_projects_ref_custom_hostname_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_custom_hostname(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefCustomHostnameArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_custom_hostname_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_custom_hostname_execute(builder)
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_custom_hostname_execute()` to send, or `delete_v1_projects_ref_custom_hostname` for simplest API.

pub fn delete_v1_projects_ref_custom_hostname_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname",
        ref_rs,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_custom_hostname_execute()` or `delete_v1_projects_ref_custom_hostname`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_custom_hostname_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_custom_hostname_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_custom_hostname_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_custom_hostname_task()`.
/// For the simplest API, use `delete_v1_projects_ref_custom_hostname()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_custom_hostname_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_custom_hostname_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_custom_hostname_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_custom_hostname`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefCustomHostnameArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_custom_hostname_builder()` + `delete_v1_projects_ref_custom_hostname_execute()`.
/// For task-level control, use `delete_v1_projects_ref_custom_hostname_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_custom_hostname(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefCustomHostnameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_custom_hostname_builder(client, &args.ref_rs)?;
    delete_v1_projects_ref_custom_hostname_execute(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_custom_hostname_activate_execute()` to send, or `post_v1_projects_ref_custom_hostname_activate` for simplest API.

pub fn post_v1_projects_ref_custom_hostname_activate_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname/activate",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_custom_hostname_activate_execute()` or `post_v1_projects_ref_custom_hostname_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_custom_hostname_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_custom_hostname_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_custom_hostname_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_custom_hostname_activate_task()`.
/// For the simplest API, use `post_v1_projects_ref_custom_hostname_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_custom_hostname_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_custom_hostname_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_custom_hostname_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_custom_hostname_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefCustomHostnameActivateArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_custom_hostname_activate_builder()` + `post_v1_projects_ref_custom_hostname_activate_execute()`.
/// For task-level control, use `post_v1_projects_ref_custom_hostname_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_custom_hostname_activate(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefCustomHostnameActivateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_custom_hostname_activate_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_custom_hostname_activate_execute(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_custom_hostname_initialize_execute()` to send, or `post_v1_projects_ref_custom_hostname_initialize` for simplest API.

pub fn post_v1_projects_ref_custom_hostname_initialize_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdateCustomHostnameBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname/initialize",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_custom_hostname_initialize_execute()` or `post_v1_projects_ref_custom_hostname_initialize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_custom_hostname_initialize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_custom_hostname_initialize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_custom_hostname_initialize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_custom_hostname_initialize_task()`.
/// For the simplest API, use `post_v1_projects_ref_custom_hostname_initialize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_custom_hostname_initialize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_custom_hostname_initialize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_custom_hostname_initialize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_custom_hostname_initialize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefCustomHostnameInitializeArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateCustomHostnameBody,
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_custom_hostname_initialize_builder()` + `post_v1_projects_ref_custom_hostname_initialize_execute()`.
/// For task-level control, use `post_v1_projects_ref_custom_hostname_initialize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_custom_hostname_initialize(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefCustomHostnameInitializeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_custom_hostname_initialize_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_custom_hostname_initialize_execute(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_custom_hostname_reverify_execute()` to send, or `post_v1_projects_ref_custom_hostname_reverify` for simplest API.

pub fn post_v1_projects_ref_custom_hostname_reverify_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname/reverify",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_custom_hostname_reverify_execute()` or `post_v1_projects_ref_custom_hostname_reverify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_custom_hostname_reverify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_custom_hostname_reverify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_custom_hostname_reverify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_custom_hostname_reverify_task()`.
/// For the simplest API, use `post_v1_projects_ref_custom_hostname_reverify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_custom_hostname_reverify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_custom_hostname_reverify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_custom_hostname_reverify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_custom_hostname_reverify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefCustomHostnameReverifyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_custom_hostname_reverify_builder()` + `post_v1_projects_ref_custom_hostname_reverify_execute()`.
/// For task-level control, use `post_v1_projects_ref_custom_hostname_reverify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_custom_hostname_reverify(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefCustomHostnameReverifyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_custom_hostname_reverify_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_custom_hostname_reverify_execute(builder)
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_backups_execute()` to send, or `get_v1_projects_ref_database_backups` for simplest API.

pub fn get_v1_projects_ref_database_backups_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_backups_execute()` or `get_v1_projects_ref_database_backups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_backups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1BackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1BackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_backups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_backups_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_backups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1BackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_backups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseBackupsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_backups_builder()` + `get_v1_projects_ref_database_backups_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_backups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_backups(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseBackupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1BackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_backups_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_database_backups_execute(builder)
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_backups_restore_pitr_execute()` to send, or `post_v1_projects_ref_database_backups_restore_pitr` for simplest API.

pub fn post_v1_projects_ref_database_backups_restore_pitr_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1RestorePitrBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/restore-pitr",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_backups_restore_pitr_execute()` or `post_v1_projects_ref_database_backups_restore_pitr`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_backups_restore_pitr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_backups_restore_pitr_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_backups_restore_pitr_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_backups_restore_pitr_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_backups_restore_pitr()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_backups_restore_pitr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_backups_restore_pitr_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_backups_restore_pitr_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_backups_restore_pitr`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseBackupsRestorePitrArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1RestorePitrBody,
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_backups_restore_pitr_builder()` + `post_v1_projects_ref_database_backups_restore_pitr_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_backups_restore_pitr_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_backups_restore_pitr(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseBackupsRestorePitrArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_database_backups_restore_pitr_builder(
        client,
        &args.ref_rs,
        &args.body,
    )?;
    post_v1_projects_ref_database_backups_restore_pitr_execute(builder)
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_backups_restore_point_execute()` to send, or `get_v1_projects_ref_database_backups_restore_point` for simplest API.

pub fn get_v1_projects_ref_database_backups_restore_point_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/restore-point",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_backups_restore_point_execute()` or `get_v1_projects_ref_database_backups_restore_point`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_backups_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_backups_restore_point_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1RestorePointResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1RestorePointResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_backups_restore_point_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_backups_restore_point_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_backups_restore_point()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_backups_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_backups_restore_point_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1RestorePointResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_backups_restore_point_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_backups_restore_point`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseBackupsRestorePointArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_backups_restore_point_builder()` + `get_v1_projects_ref_database_backups_restore_point_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_backups_restore_point_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_backups_restore_point(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseBackupsRestorePointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1RestorePointResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_backups_restore_point_builder(
        client,
        &args.ref_rs,
        args.name.as_deref(),
    )?;
    get_v1_projects_ref_database_backups_restore_point_execute(builder)
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_backups_restore_point_execute()` to send, or `post_v1_projects_ref_database_backups_restore_point` for simplest API.

pub fn post_v1_projects_ref_database_backups_restore_point_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1RestorePointPostBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/restore-point",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_backups_restore_point_execute()` or `post_v1_projects_ref_database_backups_restore_point`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_backups_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_backups_restore_point_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1RestorePointResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1RestorePointResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_backups_restore_point_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_backups_restore_point_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_backups_restore_point()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_backups_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_backups_restore_point_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1RestorePointResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_backups_restore_point_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_backups_restore_point`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseBackupsRestorePointArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1RestorePointPostBody,
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_backups_restore_point_builder()` + `post_v1_projects_ref_database_backups_restore_point_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_backups_restore_point_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_backups_restore_point(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseBackupsRestorePointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1RestorePointResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_database_backups_restore_point_builder(
        client,
        &args.ref_rs,
        &args.body,
    )?;
    post_v1_projects_ref_database_backups_restore_point_execute(builder)
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_backups_undo_execute()` to send, or `post_v1_projects_ref_database_backups_undo` for simplest API.

pub fn post_v1_projects_ref_database_backups_undo_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1UndoBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/undo",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_backups_undo_execute()` or `post_v1_projects_ref_database_backups_undo`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_backups_undo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_backups_undo_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_backups_undo_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_backups_undo_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_backups_undo()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_backups_undo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_backups_undo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_backups_undo_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_backups_undo`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseBackupsUndoArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UndoBody,
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_backups_undo_builder()` + `post_v1_projects_ref_database_backups_undo_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_backups_undo_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_backups_undo(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseBackupsUndoArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_database_backups_undo_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_database_backups_undo_execute(builder)
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_context_execute()` to send, or `get_v1_projects_ref_database_context` for simplest API.

pub fn get_v1_projects_ref_database_context_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/context",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_context_execute()` or `get_v1_projects_ref_database_context`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_context_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GetProjectDbMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetProjectDbMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_context_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_context_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_context()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_context_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetProjectDbMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_context_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_context`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseContextArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_context_builder()` + `get_v1_projects_ref_database_context_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_context_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_context(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseContextArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetProjectDbMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_context_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_database_context_execute(builder)
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_jit_execute()` to send, or `get_v1_projects_ref_database_jit` for simplest API.

pub fn get_v1_projects_ref_database_jit_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_jit_execute()` or `get_v1_projects_ref_database_jit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_jit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_jit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_jit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_jit_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_jit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_jit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_jit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_jit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_jit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseJitArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_jit_builder()` + `get_v1_projects_ref_database_jit_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_jit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_jit(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseJitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_jit_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_database_jit_execute(builder)
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_jit_execute()` to send, or `post_v1_projects_ref_database_jit` for simplest API.

pub fn post_v1_projects_ref_database_jit_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &AuthorizeJitAccessBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_jit_execute()` or `post_v1_projects_ref_database_jit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_jit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_jit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JitAuthorizeAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAuthorizeAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_jit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_jit_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_jit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_jit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_jit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<JitAuthorizeAccessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_jit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_jit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseJitArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: AuthorizeJitAccessBody,
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_jit_builder()` + `post_v1_projects_ref_database_jit_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_jit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_jit(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseJitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<JitAuthorizeAccessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_database_jit_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_database_jit_execute(builder)
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_database_jit_execute()` to send, or `put_v1_projects_ref_database_jit` for simplest API.

pub fn put_v1_projects_ref_database_jit_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdateJitAccessBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit",
        ref_rs,
    );

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_database_jit_execute()` or `put_v1_projects_ref_database_jit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_database_jit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_database_jit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_database_jit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_database_jit_task()`.
/// For the simplest API, use `put_v1_projects_ref_database_jit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_database_jit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_database_jit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_database_jit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_database_jit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefDatabaseJitArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateJitAccessBody,
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_database_jit_builder()` + `put_v1_projects_ref_database_jit_execute()`.
/// For task-level control, use `put_v1_projects_ref_database_jit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_database_jit(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefDatabaseJitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_database_jit_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_database_jit_execute(builder)
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_jit_list_execute()` to send, or `get_v1_projects_ref_database_jit_list` for simplest API.

pub fn get_v1_projects_ref_database_jit_list_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit/list",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_jit_list_execute()` or `get_v1_projects_ref_database_jit_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_jit_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_jit_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JitListAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitListAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_jit_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_jit_list_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_jit_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_jit_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_jit_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitListAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_jit_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_jit_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseJitListArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_jit_list_builder()` + `get_v1_projects_ref_database_jit_list_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_jit_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_jit_list(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseJitListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitListAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_jit_list_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_database_jit_list_execute(builder)
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_database_jit_user_id_execute()` to send, or `delete_v1_projects_ref_database_jit_user_id` for simplest API.

pub fn delete_v1_projects_ref_database_jit_user_id_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    user_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit/{}",
        ref_rs, user_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_database_jit_user_id_execute()` or `delete_v1_projects_ref_database_jit_user_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_database_jit_user_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_database_jit_user_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_database_jit_user_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_database_jit_user_id_task()`.
/// For the simplest API, use `delete_v1_projects_ref_database_jit_user_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_database_jit_user_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_database_jit_user_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_database_jit_user_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_database_jit_user_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefDatabaseJitUserIdArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: user_id
    pub user_id: String,
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_database_jit_user_id_builder()` + `delete_v1_projects_ref_database_jit_user_id_execute()`.
/// For task-level control, use `delete_v1_projects_ref_database_jit_user_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_database_jit_user_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefDatabaseJitUserIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_v1_projects_ref_database_jit_user_id_builder(client, &args.ref_rs, &args.user_id)?;
    delete_v1_projects_ref_database_jit_user_id_execute(builder)
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_migrations_execute()` to send, or `get_v1_projects_ref_database_migrations` for simplest API.

pub fn get_v1_projects_ref_database_migrations_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_migrations_execute()` or `get_v1_projects_ref_database_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1ListMigrationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ListMigrationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_migrations_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ListMigrationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseMigrationsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_migrations_builder()` + `get_v1_projects_ref_database_migrations_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_migrations(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1ListMigrationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_migrations_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_database_migrations_execute(builder)
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_migrations_execute()` to send, or `post_v1_projects_ref_database_migrations` for simplest API.

pub fn post_v1_projects_ref_database_migrations_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1CreateMigrationBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_migrations_execute()` or `post_v1_projects_ref_database_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_migrations_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseMigrationsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1CreateMigrationBody,
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_migrations_builder()` + `post_v1_projects_ref_database_migrations_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_migrations(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_database_migrations_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_database_migrations_execute(builder)
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_database_migrations_execute()` to send, or `put_v1_projects_ref_database_migrations` for simplest API.

pub fn put_v1_projects_ref_database_migrations_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1UpsertMigrationBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_database_migrations_execute()` or `put_v1_projects_ref_database_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_database_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_database_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_database_migrations_task()`.
/// For the simplest API, use `put_v1_projects_ref_database_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_database_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_database_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_database_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefDatabaseMigrationsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpsertMigrationBody,
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_database_migrations_builder()` + `put_v1_projects_ref_database_migrations_execute()`.
/// For task-level control, use `put_v1_projects_ref_database_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_database_migrations(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefDatabaseMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        put_v1_projects_ref_database_migrations_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_database_migrations_execute(builder)
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_database_migrations_execute()` to send, or `delete_v1_projects_ref_database_migrations` for simplest API.

pub fn delete_v1_projects_ref_database_migrations_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    gte: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gte {
        query_parts.push(format!("gte={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_database_migrations_execute()` or `delete_v1_projects_ref_database_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_database_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_database_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_database_migrations_task()`.
/// For the simplest API, use `delete_v1_projects_ref_database_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_database_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_database_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_database_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_database_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefDatabaseMigrationsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: gte
    pub gte: Option<String>,
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_database_migrations_builder()` + `delete_v1_projects_ref_database_migrations_execute()`.
/// For task-level control, use `delete_v1_projects_ref_database_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_database_migrations(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefDatabaseMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_database_migrations_builder(
        client,
        &args.ref_rs,
        args.gte.as_deref(),
    )?;
    delete_v1_projects_ref_database_migrations_execute(builder)
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_migrations_version_execute()` to send, or `get_v1_projects_ref_database_migrations_version` for simplest API.

pub fn get_v1_projects_ref_database_migrations_version_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    version: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations/{}",
        ref_rs, version,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_migrations_version_execute()` or `get_v1_projects_ref_database_migrations_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_migrations_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_migrations_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1GetMigrationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1GetMigrationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_migrations_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_migrations_version_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_migrations_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_migrations_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_migrations_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1GetMigrationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_migrations_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_migrations_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseMigrationsVersionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: version
    pub version: String,
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_migrations_version_builder()` + `get_v1_projects_ref_database_migrations_version_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_migrations_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_migrations_version(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseMigrationsVersionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1GetMigrationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_database_migrations_version_builder(
        client,
        &args.ref_rs,
        &args.version,
    )?;
    get_v1_projects_ref_database_migrations_version_execute(builder)
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_database_migrations_version_execute()` to send, or `patch_v1_projects_ref_database_migrations_version` for simplest API.

pub fn patch_v1_projects_ref_database_migrations_version_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    version: &str,
    body: &V1PatchMigrationBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations/{}",
        ref_rs, version,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_database_migrations_version_execute()` or `patch_v1_projects_ref_database_migrations_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_database_migrations_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_database_migrations_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_database_migrations_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_database_migrations_version_task()`.
/// For the simplest API, use `patch_v1_projects_ref_database_migrations_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_database_migrations_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_database_migrations_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_database_migrations_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_database_migrations_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefDatabaseMigrationsVersionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: version
    pub version: String,
    /// Request body.
    pub body: V1PatchMigrationBody,
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_database_migrations_version_builder()` + `patch_v1_projects_ref_database_migrations_version_execute()`.
/// For task-level control, use `patch_v1_projects_ref_database_migrations_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_database_migrations_version(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefDatabaseMigrationsVersionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_database_migrations_version_builder(
        client,
        &args.ref_rs,
        &args.version,
        &args.body,
    )?;
    patch_v1_projects_ref_database_migrations_version_execute(builder)
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_database_openapi_execute()` to send, or `get_v1_projects_ref_database_openapi` for simplest API.

pub fn get_v1_projects_ref_database_openapi_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    schema: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/openapi",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = schema {
        query_parts.push(format!("schema={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_database_openapi_execute()` or `get_v1_projects_ref_database_openapi`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_openapi_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_openapi_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_database_openapi_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_database_openapi_task()`.
/// For the simplest API, use `get_v1_projects_ref_database_openapi()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_database_openapi_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_database_openapi_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_database_openapi_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_database_openapi`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefDatabaseOpenapiArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: schema
    pub schema: Option<String>,
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_database_openapi_builder()` + `get_v1_projects_ref_database_openapi_execute()`.
/// For task-level control, use `get_v1_projects_ref_database_openapi_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_database_openapi(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefDatabaseOpenapiArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_database_openapi_builder(client, &args.ref_rs, args.schema.as_deref())?;
    get_v1_projects_ref_database_openapi_execute(builder)
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_database_password_execute()` to send, or `patch_v1_projects_ref_database_password` for simplest API.

pub fn patch_v1_projects_ref_database_password_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1UpdatePasswordBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/password",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_database_password_execute()` or `patch_v1_projects_ref_database_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_database_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_database_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1UpdatePasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1UpdatePasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_database_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_database_password_task()`.
/// For the simplest API, use `patch_v1_projects_ref_database_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_database_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_database_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1UpdatePasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_database_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_database_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefDatabasePasswordArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpdatePasswordBody,
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_database_password_builder()` + `patch_v1_projects_ref_database_password_execute()`.
/// For task-level control, use `patch_v1_projects_ref_database_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_database_password(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefDatabasePasswordArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1UpdatePasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        patch_v1_projects_ref_database_password_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_database_password_execute(builder)
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_query_execute()` to send, or `post_v1_projects_ref_database_query` for simplest API.

pub fn post_v1_projects_ref_database_query_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1RunQueryBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/query",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_query_execute()` or `post_v1_projects_ref_database_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_query_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseQueryArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1RunQueryBody,
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_query_builder()` + `post_v1_projects_ref_database_query_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_query(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_database_query_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_database_query_execute(builder)
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_query_read_only_execute()` to send, or `post_v1_projects_ref_database_query_read_only` for simplest API.

pub fn post_v1_projects_ref_database_query_read_only_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1ReadOnlyQueryBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/query/read-only",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_query_read_only_execute()` or `post_v1_projects_ref_database_query_read_only`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_query_read_only_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_query_read_only_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_query_read_only_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_query_read_only_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_query_read_only()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_query_read_only_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_query_read_only_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_query_read_only_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_query_read_only`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseQueryReadOnlyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1ReadOnlyQueryBody,
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_query_read_only_builder()` + `post_v1_projects_ref_database_query_read_only_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_query_read_only_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_query_read_only(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseQueryReadOnlyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_database_query_read_only_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_database_query_read_only_execute(builder)
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_database_webhooks_enable_execute()` to send, or `post_v1_projects_ref_database_webhooks_enable` for simplest API.

pub fn post_v1_projects_ref_database_webhooks_enable_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/database/webhooks/enable",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_database_webhooks_enable_execute()` or `post_v1_projects_ref_database_webhooks_enable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_webhooks_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_webhooks_enable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_database_webhooks_enable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_database_webhooks_enable_task()`.
/// For the simplest API, use `post_v1_projects_ref_database_webhooks_enable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_database_webhooks_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_database_webhooks_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_database_webhooks_enable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_database_webhooks_enable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefDatabaseWebhooksEnableArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_database_webhooks_enable_builder()` + `post_v1_projects_ref_database_webhooks_enable_execute()`.
/// For task-level control, use `post_v1_projects_ref_database_webhooks_enable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_database_webhooks_enable(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefDatabaseWebhooksEnableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_database_webhooks_enable_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_database_webhooks_enable_execute(builder)
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_functions_execute()` to send, or `get_v1_projects_ref_functions` for simplest API.

pub fn get_v1_projects_ref_functions_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/functions", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_functions_execute()` or `get_v1_projects_ref_functions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_functions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_functions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_functions_task()`.
/// For the simplest API, use `get_v1_projects_ref_functions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_functions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_functions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_functions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefFunctionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_functions_builder()` + `get_v1_projects_ref_functions_execute()`.
/// For task-level control, use `get_v1_projects_ref_functions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_functions(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefFunctionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_functions_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_functions_execute(builder)
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_functions_execute()` to send, or `post_v1_projects_ref_functions` for simplest API.

pub fn post_v1_projects_ref_functions_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    slug: Option<&str>,
    name: Option<&str>,
    verify_jwt: Option<bool>,
    import_map: Option<bool>,
    entrypoint_path: Option<&str>,
    import_map_path: Option<&str>,
    ezbr_sha256: Option<&str>,
    body: &V1CreateFunctionBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/functions", ref_rs,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = slug {
        query_parts.push(format!("slug={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = verify_jwt {
        query_parts.push(format!("verify_jwt={}", val));
    }
    if let Some(val) = import_map {
        query_parts.push(format!("import_map={}", val));
    }
    if let Some(val) = entrypoint_path {
        query_parts.push(format!("entrypoint_path={}", val));
    }
    if let Some(val) = import_map_path {
        query_parts.push(format!("import_map_path={}", val));
    }
    if let Some(val) = ezbr_sha256 {
        query_parts.push(format!("ezbr_sha256={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_functions_execute()` or `post_v1_projects_ref_functions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_functions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_functions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_functions_task()`.
/// For the simplest API, use `post_v1_projects_ref_functions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_functions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_functions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_functions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefFunctionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: slug
    pub slug: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: verify_jwt
    pub verify_jwt: Option<bool>,
    /// Query parameter: import_map
    pub import_map: Option<bool>,
    /// Query parameter: entrypoint_path
    pub entrypoint_path: Option<String>,
    /// Query parameter: import_map_path
    pub import_map_path: Option<String>,
    /// Query parameter: ezbr_sha256
    pub ezbr_sha256: Option<String>,
    /// Request body.
    pub body: V1CreateFunctionBody,
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_functions_builder()` + `post_v1_projects_ref_functions_execute()`.
/// For task-level control, use `post_v1_projects_ref_functions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_functions(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefFunctionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_functions_builder(
        client,
        &args.ref_rs,
        args.slug.as_deref(),
        args.name.as_deref(),
        args.verify_jwt,
        args.import_map,
        args.entrypoint_path.as_deref(),
        args.import_map_path.as_deref(),
        args.ezbr_sha256.as_deref(),
        &args.body,
    )?;
    post_v1_projects_ref_functions_execute(builder)
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_functions_execute()` to send, or `put_v1_projects_ref_functions` for simplest API.

pub fn put_v1_projects_ref_functions_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &BulkUpdateFunctionBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/functions", ref_rs,);

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_functions_execute()` or `put_v1_projects_ref_functions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_functions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BulkUpdateFunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkUpdateFunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_functions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_functions_task()`.
/// For the simplest API, use `put_v1_projects_ref_functions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_functions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkUpdateFunctionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_functions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_functions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefFunctionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: BulkUpdateFunctionBody,
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_functions_builder()` + `put_v1_projects_ref_functions_execute()`.
/// For task-level control, use `put_v1_projects_ref_functions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_functions(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefFunctionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkUpdateFunctionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_functions_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_functions_execute(builder)
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_functions_deploy_execute()` to send, or `post_v1_projects_ref_functions_deploy` for simplest API.

pub fn post_v1_projects_ref_functions_deploy_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    slug: Option<&str>,
    bundleOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/deploy",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = slug {
        query_parts.push(format!("slug={}", val));
    }
    if let Some(val) = bundleOnly {
        query_parts.push(format!("bundleOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_functions_deploy_execute()` or `post_v1_projects_ref_functions_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_functions_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_functions_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DeployFunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeployFunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_functions_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_functions_deploy_task()`.
/// For the simplest API, use `post_v1_projects_ref_functions_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_functions_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_functions_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployFunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_functions_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_functions_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefFunctionsDeployArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: slug
    pub slug: Option<String>,
    /// Query parameter: bundleOnly
    pub bundleOnly: Option<bool>,
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_functions_deploy_builder()` + `post_v1_projects_ref_functions_deploy_execute()`.
/// For task-level control, use `post_v1_projects_ref_functions_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_functions_deploy(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefFunctionsDeployArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployFunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_functions_deploy_builder(
        client,
        &args.ref_rs,
        args.slug.as_deref(),
        args.bundleOnly,
    )?;
    post_v1_projects_ref_functions_deploy_execute(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_functions_function_slug_execute()` to send, or `get_v1_projects_ref_functions_function_slug` for simplest API.

pub fn get_v1_projects_ref_functions_function_slug_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    function_slug: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}",
        ref_rs, function_slug,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_functions_function_slug_execute()` or `get_v1_projects_ref_functions_function_slug`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_functions_function_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_functions_function_slug_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FunctionSlugResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FunctionSlugResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_functions_function_slug_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_functions_function_slug_task()`.
/// For the simplest API, use `get_v1_projects_ref_functions_function_slug()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_functions_function_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_functions_function_slug_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FunctionSlugResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_functions_function_slug_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_functions_function_slug`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefFunctionsFunctionSlugArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_functions_function_slug_builder()` + `get_v1_projects_ref_functions_function_slug_execute()`.
/// For task-level control, use `get_v1_projects_ref_functions_function_slug_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_functions_function_slug(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefFunctionsFunctionSlugArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FunctionSlugResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_functions_function_slug_builder(
        client,
        &args.ref_rs,
        &args.function_slug,
    )?;
    get_v1_projects_ref_functions_function_slug_execute(builder)
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_functions_function_slug_execute()` to send, or `patch_v1_projects_ref_functions_function_slug` for simplest API.

pub fn patch_v1_projects_ref_functions_function_slug_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    function_slug: &str,
    slug: Option<&str>,
    name: Option<&str>,
    verify_jwt: Option<bool>,
    import_map: Option<bool>,
    entrypoint_path: Option<&str>,
    import_map_path: Option<&str>,
    ezbr_sha256: Option<&str>,
    body: &V1UpdateFunctionBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}",
        ref_rs, function_slug,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = slug {
        query_parts.push(format!("slug={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = verify_jwt {
        query_parts.push(format!("verify_jwt={}", val));
    }
    if let Some(val) = import_map {
        query_parts.push(format!("import_map={}", val));
    }
    if let Some(val) = entrypoint_path {
        query_parts.push(format!("entrypoint_path={}", val));
    }
    if let Some(val) = import_map_path {
        query_parts.push(format!("import_map_path={}", val));
    }
    if let Some(val) = ezbr_sha256 {
        query_parts.push(format!("ezbr_sha256={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_functions_function_slug_execute()` or `patch_v1_projects_ref_functions_function_slug`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_functions_function_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_functions_function_slug_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_functions_function_slug_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_functions_function_slug_task()`.
/// For the simplest API, use `patch_v1_projects_ref_functions_function_slug()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_functions_function_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_functions_function_slug_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_functions_function_slug_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_functions_function_slug`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefFunctionsFunctionSlugArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
    /// Query parameter: slug
    pub slug: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: verify_jwt
    pub verify_jwt: Option<bool>,
    /// Query parameter: import_map
    pub import_map: Option<bool>,
    /// Query parameter: entrypoint_path
    pub entrypoint_path: Option<String>,
    /// Query parameter: import_map_path
    pub import_map_path: Option<String>,
    /// Query parameter: ezbr_sha256
    pub ezbr_sha256: Option<String>,
    /// Request body.
    pub body: V1UpdateFunctionBody,
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_functions_function_slug_builder()` + `patch_v1_projects_ref_functions_function_slug_execute()`.
/// For task-level control, use `patch_v1_projects_ref_functions_function_slug_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_functions_function_slug(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefFunctionsFunctionSlugArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FunctionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_functions_function_slug_builder(
        client,
        &args.ref_rs,
        &args.function_slug,
        args.slug.as_deref(),
        args.name.as_deref(),
        args.verify_jwt,
        args.import_map,
        args.entrypoint_path.as_deref(),
        args.import_map_path.as_deref(),
        args.ezbr_sha256.as_deref(),
        &args.body,
    )?;
    patch_v1_projects_ref_functions_function_slug_execute(builder)
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_functions_function_slug_execute()` to send, or `delete_v1_projects_ref_functions_function_slug` for simplest API.

pub fn delete_v1_projects_ref_functions_function_slug_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    function_slug: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}",
        ref_rs, function_slug,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_functions_function_slug_execute()` or `delete_v1_projects_ref_functions_function_slug`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_functions_function_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_functions_function_slug_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_functions_function_slug_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_functions_function_slug_task()`.
/// For the simplest API, use `delete_v1_projects_ref_functions_function_slug()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_functions_function_slug_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_functions_function_slug_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_functions_function_slug_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_functions_function_slug`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefFunctionsFunctionSlugArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_functions_function_slug_builder()` + `delete_v1_projects_ref_functions_function_slug_execute()`.
/// For task-level control, use `delete_v1_projects_ref_functions_function_slug_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_functions_function_slug(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefFunctionsFunctionSlugArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_functions_function_slug_builder(
        client,
        &args.ref_rs,
        &args.function_slug,
    )?;
    delete_v1_projects_ref_functions_function_slug_execute(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_functions_function_slug_body_execute()` to send, or `get_v1_projects_ref_functions_function_slug_body` for simplest API.

pub fn get_v1_projects_ref_functions_function_slug_body_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    function_slug: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}/body",
        ref_rs, function_slug,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_functions_function_slug_body_execute()` or `get_v1_projects_ref_functions_function_slug_body`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_functions_function_slug_body_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_functions_function_slug_body_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<StreamableFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StreamableFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_functions_function_slug_body_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_functions_function_slug_body_task()`.
/// For the simplest API, use `get_v1_projects_ref_functions_function_slug_body()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_functions_function_slug_body_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_functions_function_slug_body_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamableFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_functions_function_slug_body_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_functions_function_slug_body`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefFunctionsFunctionSlugBodyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_functions_function_slug_body_builder()` + `get_v1_projects_ref_functions_function_slug_body_execute()`.
/// For task-level control, use `get_v1_projects_ref_functions_function_slug_body_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_functions_function_slug_body(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefFunctionsFunctionSlugBodyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamableFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_functions_function_slug_body_builder(
        client,
        &args.ref_rs,
        &args.function_slug,
    )?;
    get_v1_projects_ref_functions_function_slug_body_execute(builder)
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_health_execute()` to send, or `get_v1_projects_ref_health` for simplest API.

pub fn get_v1_projects_ref_health_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    services: Option<Vec<String>>,
    timeout_ms: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/health", ref_rs,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = services {
        query_parts.push(format!("services={}", val));
    }
    if let Some(val) = timeout_ms {
        query_parts.push(format!("timeout_ms={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_health_execute()` or `get_v1_projects_ref_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_health_task()`.
/// For the simplest API, use `get_v1_projects_ref_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefHealthArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: services
    pub services: Option<Vec<String>>,
    /// Query parameter: timeout_ms
    pub timeout_ms: Option<i32>,
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_health_builder()` + `get_v1_projects_ref_health_execute()`.
/// For task-level control, use `get_v1_projects_ref_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_health(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefHealthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_v1_projects_ref_health_builder(client, &args.ref_rs, args.services, args.timeout_ms)?;
    get_v1_projects_ref_health_execute(builder)
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_jit_access_execute()` to send, or `get_v1_projects_ref_jit_access` for simplest API.

pub fn get_v1_projects_ref_jit_access_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/jit-access", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_jit_access_execute()` or `get_v1_projects_ref_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_jit_access_task()`.
/// For the simplest API, use `get_v1_projects_ref_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_jit_access_builder()` + `get_v1_projects_ref_jit_access_execute()`.
/// For task-level control, use `get_v1_projects_ref_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_jit_access(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefJitAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_jit_access_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_jit_access_execute(builder)
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_jit_access_execute()` to send, or `put_v1_projects_ref_jit_access` for simplest API.

pub fn put_v1_projects_ref_jit_access_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &JitAccessRequestRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/jit-access", ref_rs,);

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_jit_access_execute()` or `put_v1_projects_ref_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_jit_access_task()`.
/// For the simplest API, use `put_v1_projects_ref_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: JitAccessRequestRequest,
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_jit_access_builder()` + `put_v1_projects_ref_jit_access_execute()`.
/// For task-level control, use `put_v1_projects_ref_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_jit_access(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefJitAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JitAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_jit_access_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_jit_access_execute(builder)
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_network_bans_execute()` to send, or `delete_v1_projects_ref_network_bans` for simplest API.

pub fn delete_v1_projects_ref_network_bans_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &RemoveNetworkBanRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/network-bans",
        ref_rs,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_network_bans_execute()` or `delete_v1_projects_ref_network_bans`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_network_bans_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_network_bans_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_network_bans_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_network_bans_task()`.
/// For the simplest API, use `delete_v1_projects_ref_network_bans()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_network_bans_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_network_bans_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_network_bans_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_network_bans`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefNetworkBansArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: RemoveNetworkBanRequest,
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_network_bans_builder()` + `delete_v1_projects_ref_network_bans_execute()`.
/// For task-level control, use `delete_v1_projects_ref_network_bans_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_network_bans(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefNetworkBansArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_network_bans_builder(client, &args.ref_rs, &args.body)?;
    delete_v1_projects_ref_network_bans_execute(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_network_bans_retrieve_execute()` to send, or `post_v1_projects_ref_network_bans_retrieve` for simplest API.

pub fn post_v1_projects_ref_network_bans_retrieve_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/network-bans/retrieve",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_network_bans_retrieve_execute()` or `post_v1_projects_ref_network_bans_retrieve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_network_bans_retrieve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_network_bans_retrieve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<NetworkBanResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkBanResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_network_bans_retrieve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_network_bans_retrieve_task()`.
/// For the simplest API, use `post_v1_projects_ref_network_bans_retrieve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_network_bans_retrieve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_network_bans_retrieve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkBanResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_network_bans_retrieve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_network_bans_retrieve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefNetworkBansRetrieveArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_network_bans_retrieve_builder()` + `post_v1_projects_ref_network_bans_retrieve_execute()`.
/// For task-level control, use `post_v1_projects_ref_network_bans_retrieve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_network_bans_retrieve(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefNetworkBansRetrieveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkBanResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_network_bans_retrieve_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_network_bans_retrieve_execute(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_network_bans_retrieve_enriched_execute()` to send, or `post_v1_projects_ref_network_bans_retrieve_enriched` for simplest API.

pub fn post_v1_projects_ref_network_bans_retrieve_enriched_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/network-bans/retrieve/enriched",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_network_bans_retrieve_enriched_execute()` or `post_v1_projects_ref_network_bans_retrieve_enriched`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_network_bans_retrieve_enriched_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_network_bans_retrieve_enriched_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<NetworkBanResponseEnriched>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkBanResponseEnriched = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_network_bans_retrieve_enriched_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_network_bans_retrieve_enriched_task()`.
/// For the simplest API, use `post_v1_projects_ref_network_bans_retrieve_enriched()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_network_bans_retrieve_enriched_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_network_bans_retrieve_enriched_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkBanResponseEnriched>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_network_bans_retrieve_enriched_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_network_bans_retrieve_enriched`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefNetworkBansRetrieveEnrichedArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_network_bans_retrieve_enriched_builder()` + `post_v1_projects_ref_network_bans_retrieve_enriched_execute()`.
/// For task-level control, use `post_v1_projects_ref_network_bans_retrieve_enriched_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_network_bans_retrieve_enriched(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefNetworkBansRetrieveEnrichedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkBanResponseEnriched>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_network_bans_retrieve_enriched_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_network_bans_retrieve_enriched_execute(builder)
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_network_restrictions_execute()` to send, or `get_v1_projects_ref_network_restrictions` for simplest API.

pub fn get_v1_projects_ref_network_restrictions_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/network-restrictions",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_network_restrictions_execute()` or `get_v1_projects_ref_network_restrictions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_network_restrictions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkRestrictionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_network_restrictions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_network_restrictions_task()`.
/// For the simplest API, use `get_v1_projects_ref_network_restrictions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_network_restrictions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_network_restrictions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_network_restrictions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefNetworkRestrictionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_network_restrictions_builder()` + `get_v1_projects_ref_network_restrictions_execute()`.
/// For task-level control, use `get_v1_projects_ref_network_restrictions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_network_restrictions(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefNetworkRestrictionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_network_restrictions_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_network_restrictions_execute(builder)
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_network_restrictions_execute()` to send, or `patch_v1_projects_ref_network_restrictions` for simplest API.

pub fn patch_v1_projects_ref_network_restrictions_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &NetworkRestrictionsPatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/network-restrictions",
        ref_rs,
    );

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_network_restrictions_execute()` or `patch_v1_projects_ref_network_restrictions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_network_restrictions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<NetworkRestrictionsV2Response>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkRestrictionsV2Response = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_network_restrictions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_network_restrictions_task()`.
/// For the simplest API, use `patch_v1_projects_ref_network_restrictions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_network_restrictions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkRestrictionsV2Response>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_network_restrictions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_network_restrictions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefNetworkRestrictionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: NetworkRestrictionsPatchRequest,
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_network_restrictions_builder()` + `patch_v1_projects_ref_network_restrictions_execute()`.
/// For task-level control, use `patch_v1_projects_ref_network_restrictions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_network_restrictions(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefNetworkRestrictionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkRestrictionsV2Response>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        patch_v1_projects_ref_network_restrictions_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_network_restrictions_execute(builder)
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_network_restrictions_apply_execute()` to send, or `post_v1_projects_ref_network_restrictions_apply` for simplest API.

pub fn post_v1_projects_ref_network_restrictions_apply_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &NetworkRestrictionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/network-restrictions/apply",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_network_restrictions_apply_execute()` or `post_v1_projects_ref_network_restrictions_apply`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_network_restrictions_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_network_restrictions_apply_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkRestrictionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_network_restrictions_apply_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_network_restrictions_apply_task()`.
/// For the simplest API, use `post_v1_projects_ref_network_restrictions_apply()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_network_restrictions_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_network_restrictions_apply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_network_restrictions_apply_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_network_restrictions_apply`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefNetworkRestrictionsApplyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: NetworkRestrictionsRequest,
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_network_restrictions_apply_builder()` + `post_v1_projects_ref_network_restrictions_apply_execute()`.
/// For task-level control, use `post_v1_projects_ref_network_restrictions_apply_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_network_restrictions_apply(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefNetworkRestrictionsApplyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_network_restrictions_apply_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_network_restrictions_apply_execute(builder)
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_pause_execute()` to send, or `post_v1_projects_ref_pause` for simplest API.

pub fn post_v1_projects_ref_pause_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/pause", ref_rs,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_pause_execute()` or `post_v1_projects_ref_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_pause_task()`.
/// For the simplest API, use `post_v1_projects_ref_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefPauseArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_pause_builder()` + `post_v1_projects_ref_pause_execute()`.
/// For task-level control, use `post_v1_projects_ref_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_pause(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_pause_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_pause_execute(builder)
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_pgsodium_execute()` to send, or `get_v1_projects_ref_pgsodium` for simplest API.

pub fn get_v1_projects_ref_pgsodium_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/pgsodium", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_pgsodium_execute()` or `get_v1_projects_ref_pgsodium`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_pgsodium_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_pgsodium_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PgsodiumConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_pgsodium_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_pgsodium_task()`.
/// For the simplest API, use `get_v1_projects_ref_pgsodium()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_pgsodium_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_pgsodium_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_pgsodium_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_pgsodium`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefPgsodiumArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_pgsodium_builder()` + `get_v1_projects_ref_pgsodium_execute()`.
/// For task-level control, use `get_v1_projects_ref_pgsodium_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_pgsodium(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefPgsodiumArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_pgsodium_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_pgsodium_execute(builder)
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_pgsodium_execute()` to send, or `put_v1_projects_ref_pgsodium` for simplest API.

pub fn put_v1_projects_ref_pgsodium_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpdatePgsodiumConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/pgsodium", ref_rs,);

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_pgsodium_execute()` or `put_v1_projects_ref_pgsodium`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_pgsodium_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_pgsodium_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PgsodiumConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_pgsodium_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_pgsodium_task()`.
/// For the simplest API, use `put_v1_projects_ref_pgsodium()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_pgsodium_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_pgsodium_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_pgsodium_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_pgsodium`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefPgsodiumArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdatePgsodiumConfigBody,
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_pgsodium_builder()` + `put_v1_projects_ref_pgsodium_execute()`.
/// For task-level control, use `put_v1_projects_ref_pgsodium_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_pgsodium(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefPgsodiumArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_pgsodium_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_pgsodium_execute(builder)
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_postgrest_execute()` to send, or `get_v1_projects_ref_postgrest` for simplest API.

pub fn get_v1_projects_ref_postgrest_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/postgrest", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_postgrest_execute()` or `get_v1_projects_ref_postgrest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_postgrest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_postgrest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<PostgrestConfigWithJWTSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostgrestConfigWithJWTSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_postgrest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_postgrest_task()`.
/// For the simplest API, use `get_v1_projects_ref_postgrest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_postgrest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_postgrest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PostgrestConfigWithJWTSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_postgrest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_postgrest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefPostgrestArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_postgrest_builder()` + `get_v1_projects_ref_postgrest_execute()`.
/// For task-level control, use `get_v1_projects_ref_postgrest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_postgrest(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefPostgrestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PostgrestConfigWithJWTSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_postgrest_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_postgrest_execute(builder)
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_v1_projects_ref_postgrest_execute()` to send, or `patch_v1_projects_ref_postgrest` for simplest API.

pub fn patch_v1_projects_ref_postgrest_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &V1UpdatePostgrestConfigBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/postgrest", ref_rs,);

    // Build request
    let builder = client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_v1_projects_ref_postgrest_execute()` or `patch_v1_projects_ref_postgrest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_postgrest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_postgrest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<V1PostgrestConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1PostgrestConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_v1_projects_ref_postgrest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_v1_projects_ref_postgrest_task()`.
/// For the simplest API, use `patch_v1_projects_ref_postgrest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_v1_projects_ref_postgrest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_v1_projects_ref_postgrest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1PostgrestConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = patch_v1_projects_ref_postgrest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_v1_projects_ref_postgrest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1ProjectsRefPostgrestArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpdatePostgrestConfigBody,
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_v1_projects_ref_postgrest_builder()` + `patch_v1_projects_ref_postgrest_execute()`.
/// For task-level control, use `patch_v1_projects_ref_postgrest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_v1_projects_ref_postgrest(
    client: &SimpleHttpClient,
    args: &PatchV1ProjectsRefPostgrestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V1PostgrestConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = patch_v1_projects_ref_postgrest_builder(client, &args.ref_rs, &args.body)?;
    patch_v1_projects_ref_postgrest_execute(builder)
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_read_replicas_remove_execute()` to send, or `post_v1_projects_ref_read_replicas_remove` for simplest API.

pub fn post_v1_projects_ref_read_replicas_remove_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &RemoveReadReplicaBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/read-replicas/remove",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_read_replicas_remove_execute()` or `post_v1_projects_ref_read_replicas_remove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_read_replicas_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_read_replicas_remove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_read_replicas_remove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_read_replicas_remove_task()`.
/// For the simplest API, use `post_v1_projects_ref_read_replicas_remove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_read_replicas_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_read_replicas_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_read_replicas_remove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_read_replicas_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefReadReplicasRemoveArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: RemoveReadReplicaBody,
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_read_replicas_remove_builder()` + `post_v1_projects_ref_read_replicas_remove_execute()`.
/// For task-level control, use `post_v1_projects_ref_read_replicas_remove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_read_replicas_remove(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefReadReplicasRemoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_read_replicas_remove_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_read_replicas_remove_execute(builder)
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_read_replicas_setup_execute()` to send, or `post_v1_projects_ref_read_replicas_setup` for simplest API.

pub fn post_v1_projects_ref_read_replicas_setup_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &SetUpReadReplicaBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/read-replicas/setup",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_read_replicas_setup_execute()` or `post_v1_projects_ref_read_replicas_setup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_read_replicas_setup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_read_replicas_setup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_read_replicas_setup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_read_replicas_setup_task()`.
/// For the simplest API, use `post_v1_projects_ref_read_replicas_setup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_read_replicas_setup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_read_replicas_setup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_read_replicas_setup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_read_replicas_setup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefReadReplicasSetupArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: SetUpReadReplicaBody,
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_read_replicas_setup_builder()` + `post_v1_projects_ref_read_replicas_setup_execute()`.
/// For task-level control, use `post_v1_projects_ref_read_replicas_setup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_read_replicas_setup(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefReadReplicasSetupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_read_replicas_setup_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_read_replicas_setup_execute(builder)
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_readonly_execute()` to send, or `get_v1_projects_ref_readonly` for simplest API.

pub fn get_v1_projects_ref_readonly_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/readonly", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_readonly_execute()` or `get_v1_projects_ref_readonly`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_readonly_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_readonly_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ReadOnlyStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReadOnlyStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_readonly_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_readonly_task()`.
/// For the simplest API, use `get_v1_projects_ref_readonly()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_readonly_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_readonly_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadOnlyStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_readonly_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_readonly`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefReadonlyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_readonly_builder()` + `get_v1_projects_ref_readonly_execute()`.
/// For task-level control, use `get_v1_projects_ref_readonly_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_readonly(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefReadonlyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadOnlyStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_readonly_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_readonly_execute(builder)
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_readonly_temporary_disable_execute()` to send, or `post_v1_projects_ref_readonly_temporary_disable` for simplest API.

pub fn post_v1_projects_ref_readonly_temporary_disable_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/readonly/temporary-disable",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_readonly_temporary_disable_execute()` or `post_v1_projects_ref_readonly_temporary_disable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_readonly_temporary_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_readonly_temporary_disable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_readonly_temporary_disable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_readonly_temporary_disable_task()`.
/// For the simplest API, use `post_v1_projects_ref_readonly_temporary_disable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_readonly_temporary_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_readonly_temporary_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_readonly_temporary_disable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_readonly_temporary_disable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefReadonlyTemporaryDisableArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_readonly_temporary_disable_builder()` + `post_v1_projects_ref_readonly_temporary_disable_execute()`.
/// For task-level control, use `post_v1_projects_ref_readonly_temporary_disable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_readonly_temporary_disable(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefReadonlyTemporaryDisableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_readonly_temporary_disable_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_readonly_temporary_disable_execute(builder)
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_restore_execute()` to send, or `get_v1_projects_ref_restore` for simplest API.

pub fn get_v1_projects_ref_restore_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/restore", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_restore_execute()` or `get_v1_projects_ref_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GetProjectAvailableRestoreVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetProjectAvailableRestoreVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_restore_task()`.
/// For the simplest API, use `get_v1_projects_ref_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetProjectAvailableRestoreVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefRestoreArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_restore_builder()` + `get_v1_projects_ref_restore_execute()`.
/// For task-level control, use `get_v1_projects_ref_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_restore(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetProjectAvailableRestoreVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_restore_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_restore_execute(builder)
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_restore_execute()` to send, or `post_v1_projects_ref_restore` for simplest API.

pub fn post_v1_projects_ref_restore_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/restore", ref_rs,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_restore_execute()` or `post_v1_projects_ref_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_restore_task()`.
/// For the simplest API, use `post_v1_projects_ref_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefRestoreArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_restore_builder()` + `post_v1_projects_ref_restore_execute()`.
/// For task-level control, use `post_v1_projects_ref_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_restore(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_restore_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_restore_execute(builder)
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_restore_cancel_execute()` to send, or `post_v1_projects_ref_restore_cancel` for simplest API.

pub fn post_v1_projects_ref_restore_cancel_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/restore/cancel",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_restore_cancel_execute()` or `post_v1_projects_ref_restore_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_restore_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_restore_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_restore_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_restore_cancel_task()`.
/// For the simplest API, use `post_v1_projects_ref_restore_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_restore_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_restore_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_restore_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_restore_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefRestoreCancelArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_restore_cancel_builder()` + `post_v1_projects_ref_restore_cancel_execute()`.
/// For task-level control, use `post_v1_projects_ref_restore_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_restore_cancel(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefRestoreCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_restore_cancel_builder(client, &args.ref_rs)?;
    post_v1_projects_ref_restore_cancel_execute(builder)
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_secrets_execute()` to send, or `get_v1_projects_ref_secrets` for simplest API.

pub fn get_v1_projects_ref_secrets_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/secrets", ref_rs,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_secrets_execute()` or `get_v1_projects_ref_secrets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_secrets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_secrets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_secrets_task()`.
/// For the simplest API, use `get_v1_projects_ref_secrets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_secrets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefSecretsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_secrets_builder()` + `get_v1_projects_ref_secrets_execute()`.
/// For task-level control, use `get_v1_projects_ref_secrets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_secrets(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefSecretsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_secrets_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_secrets_execute(builder)
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_secrets_execute()` to send, or `post_v1_projects_ref_secrets` for simplest API.

pub fn post_v1_projects_ref_secrets_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &CreateSecretBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/secrets", ref_rs,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_secrets_execute()` or `post_v1_projects_ref_secrets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_secrets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_secrets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_secrets_task()`.
/// For the simplest API, use `post_v1_projects_ref_secrets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_secrets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefSecretsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateSecretBody,
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_secrets_builder()` + `post_v1_projects_ref_secrets_execute()`.
/// For task-level control, use `post_v1_projects_ref_secrets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_secrets(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefSecretsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_secrets_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_secrets_execute(builder)
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_secrets_execute()` to send, or `delete_v1_projects_ref_secrets` for simplest API.

pub fn delete_v1_projects_ref_secrets_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &DeleteSecretsBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/secrets", ref_rs,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_secrets_execute()` or `delete_v1_projects_ref_secrets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_secrets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_secrets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_secrets_task()`.
/// For the simplest API, use `delete_v1_projects_ref_secrets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_secrets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefSecretsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: DeleteSecretsBody,
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_secrets_builder()` + `delete_v1_projects_ref_secrets_execute()`.
/// For task-level control, use `delete_v1_projects_ref_secrets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_secrets(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefSecretsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_secrets_builder(client, &args.ref_rs, &args.body)?;
    delete_v1_projects_ref_secrets_execute(builder)
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_ssl_enforcement_execute()` to send, or `get_v1_projects_ref_ssl_enforcement` for simplest API.

pub fn get_v1_projects_ref_ssl_enforcement_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/ssl-enforcement",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_ssl_enforcement_execute()` or `get_v1_projects_ref_ssl_enforcement`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_ssl_enforcement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_ssl_enforcement_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SslEnforcementResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslEnforcementResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_ssl_enforcement_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_ssl_enforcement_task()`.
/// For the simplest API, use `get_v1_projects_ref_ssl_enforcement()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_ssl_enforcement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_ssl_enforcement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslEnforcementResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_ssl_enforcement_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_ssl_enforcement`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefSslEnforcementArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_ssl_enforcement_builder()` + `get_v1_projects_ref_ssl_enforcement_execute()`.
/// For task-level control, use `get_v1_projects_ref_ssl_enforcement_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_ssl_enforcement(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefSslEnforcementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslEnforcementResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_ssl_enforcement_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_ssl_enforcement_execute(builder)
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_v1_projects_ref_ssl_enforcement_execute()` to send, or `put_v1_projects_ref_ssl_enforcement` for simplest API.

pub fn put_v1_projects_ref_ssl_enforcement_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &SslEnforcementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/ssl-enforcement",
        ref_rs,
    );

    // Build request
    let builder = client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_v1_projects_ref_ssl_enforcement_execute()` or `put_v1_projects_ref_ssl_enforcement`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_ssl_enforcement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_ssl_enforcement_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SslEnforcementResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslEnforcementResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_v1_projects_ref_ssl_enforcement_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_v1_projects_ref_ssl_enforcement_task()`.
/// For the simplest API, use `put_v1_projects_ref_ssl_enforcement()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_v1_projects_ref_ssl_enforcement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_v1_projects_ref_ssl_enforcement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslEnforcementResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = put_v1_projects_ref_ssl_enforcement_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_v1_projects_ref_ssl_enforcement`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutV1ProjectsRefSslEnforcementArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: SslEnforcementRequest,
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_v1_projects_ref_ssl_enforcement_builder()` + `put_v1_projects_ref_ssl_enforcement_execute()`.
/// For task-level control, use `put_v1_projects_ref_ssl_enforcement_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_v1_projects_ref_ssl_enforcement(
    client: &SimpleHttpClient,
    args: &PutV1ProjectsRefSslEnforcementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslEnforcementResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = put_v1_projects_ref_ssl_enforcement_builder(client, &args.ref_rs, &args.body)?;
    put_v1_projects_ref_ssl_enforcement_execute(builder)
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_storage_buckets_execute()` to send, or `get_v1_projects_ref_storage_buckets` for simplest API.

pub fn get_v1_projects_ref_storage_buckets_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/storage/buckets",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_storage_buckets_execute()` or `get_v1_projects_ref_storage_buckets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_storage_buckets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_storage_buckets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_storage_buckets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_storage_buckets_task()`.
/// For the simplest API, use `get_v1_projects_ref_storage_buckets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_storage_buckets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_storage_buckets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_storage_buckets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_storage_buckets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefStorageBucketsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_storage_buckets_builder()` + `get_v1_projects_ref_storage_buckets_execute()`.
/// For task-level control, use `get_v1_projects_ref_storage_buckets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_storage_buckets(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefStorageBucketsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_storage_buckets_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_storage_buckets_execute(builder)
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_types_typescript_execute()` to send, or `get_v1_projects_ref_types_typescript` for simplest API.

pub fn get_v1_projects_ref_types_typescript_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    included_schemas: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/types/typescript",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = included_schemas {
        query_parts.push(format!("included_schemas={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_types_typescript_execute()` or `get_v1_projects_ref_types_typescript`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_types_typescript_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_types_typescript_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TypescriptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TypescriptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_types_typescript_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_types_typescript_task()`.
/// For the simplest API, use `get_v1_projects_ref_types_typescript()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_types_typescript_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_types_typescript_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TypescriptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_types_typescript_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_types_typescript`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefTypesTypescriptArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: included_schemas
    pub included_schemas: Option<String>,
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_types_typescript_builder()` + `get_v1_projects_ref_types_typescript_execute()`.
/// For task-level control, use `get_v1_projects_ref_types_typescript_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_types_typescript(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefTypesTypescriptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TypescriptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_types_typescript_builder(
        client,
        &args.ref_rs,
        args.included_schemas.as_deref(),
    )?;
    get_v1_projects_ref_types_typescript_execute(builder)
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_upgrade_execute()` to send, or `post_v1_projects_ref_upgrade` for simplest API.

pub fn post_v1_projects_ref_upgrade_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &UpgradeDatabaseBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/projects/{}/upgrade", ref_rs,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_upgrade_execute()` or `post_v1_projects_ref_upgrade`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_upgrade_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ProjectUpgradeInitiateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectUpgradeInitiateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_upgrade_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_upgrade_task()`.
/// For the simplest API, use `post_v1_projects_ref_upgrade()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProjectUpgradeInitiateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_upgrade_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_upgrade`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefUpgradeArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpgradeDatabaseBody,
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_upgrade_builder()` + `post_v1_projects_ref_upgrade_execute()`.
/// For task-level control, use `post_v1_projects_ref_upgrade_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_upgrade(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefUpgradeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProjectUpgradeInitiateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_upgrade_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_upgrade_execute(builder)
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_upgrade_eligibility_execute()` to send, or `get_v1_projects_ref_upgrade_eligibility` for simplest API.

pub fn get_v1_projects_ref_upgrade_eligibility_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/upgrade/eligibility",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_upgrade_eligibility_execute()` or `get_v1_projects_ref_upgrade_eligibility`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_upgrade_eligibility_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_upgrade_eligibility_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ProjectUpgradeEligibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectUpgradeEligibilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_upgrade_eligibility_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_upgrade_eligibility_task()`.
/// For the simplest API, use `get_v1_projects_ref_upgrade_eligibility()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_upgrade_eligibility_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_upgrade_eligibility_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProjectUpgradeEligibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_upgrade_eligibility_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_upgrade_eligibility`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefUpgradeEligibilityArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_upgrade_eligibility_builder()` + `get_v1_projects_ref_upgrade_eligibility_execute()`.
/// For task-level control, use `get_v1_projects_ref_upgrade_eligibility_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_upgrade_eligibility(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefUpgradeEligibilityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProjectUpgradeEligibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_upgrade_eligibility_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_upgrade_eligibility_execute(builder)
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_upgrade_status_execute()` to send, or `get_v1_projects_ref_upgrade_status` for simplest API.

pub fn get_v1_projects_ref_upgrade_status_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    tracking_id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/upgrade/status",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tracking_id {
        query_parts.push(format!("tracking_id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_upgrade_status_execute()` or `get_v1_projects_ref_upgrade_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_upgrade_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_upgrade_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<DatabaseUpgradeStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseUpgradeStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_upgrade_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_upgrade_status_task()`.
/// For the simplest API, use `get_v1_projects_ref_upgrade_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_upgrade_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_upgrade_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatabaseUpgradeStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_upgrade_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_upgrade_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefUpgradeStatusArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: tracking_id
    pub tracking_id: Option<String>,
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_upgrade_status_builder()` + `get_v1_projects_ref_upgrade_status_execute()`.
/// For task-level control, use `get_v1_projects_ref_upgrade_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_upgrade_status(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefUpgradeStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatabaseUpgradeStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_upgrade_status_builder(
        client,
        &args.ref_rs,
        args.tracking_id.as_deref(),
    )?;
    get_v1_projects_ref_upgrade_status_execute(builder)
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_projects_ref_vanity_subdomain_execute()` to send, or `get_v1_projects_ref_vanity_subdomain` for simplest API.

pub fn get_v1_projects_ref_vanity_subdomain_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain",
        ref_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_projects_ref_vanity_subdomain_execute()` or `get_v1_projects_ref_vanity_subdomain`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_vanity_subdomain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_vanity_subdomain_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<VanitySubdomainConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VanitySubdomainConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_projects_ref_vanity_subdomain_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_projects_ref_vanity_subdomain_task()`.
/// For the simplest API, use `get_v1_projects_ref_vanity_subdomain()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_projects_ref_vanity_subdomain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_projects_ref_vanity_subdomain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VanitySubdomainConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_projects_ref_vanity_subdomain_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_projects_ref_vanity_subdomain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsRefVanitySubdomainArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_projects_ref_vanity_subdomain_builder()` + `get_v1_projects_ref_vanity_subdomain_execute()`.
/// For task-level control, use `get_v1_projects_ref_vanity_subdomain_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_projects_ref_vanity_subdomain(
    client: &SimpleHttpClient,
    args: &GetV1ProjectsRefVanitySubdomainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VanitySubdomainConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_projects_ref_vanity_subdomain_builder(client, &args.ref_rs)?;
    get_v1_projects_ref_vanity_subdomain_execute(builder)
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_projects_ref_vanity_subdomain_execute()` to send, or `delete_v1_projects_ref_vanity_subdomain` for simplest API.

pub fn delete_v1_projects_ref_vanity_subdomain_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain",
        ref_rs,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_v1_projects_ref_vanity_subdomain_execute()` or `delete_v1_projects_ref_vanity_subdomain`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_vanity_subdomain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_vanity_subdomain_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_v1_projects_ref_vanity_subdomain_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_v1_projects_ref_vanity_subdomain_task()`.
/// For the simplest API, use `delete_v1_projects_ref_vanity_subdomain()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_v1_projects_ref_vanity_subdomain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_v1_projects_ref_vanity_subdomain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_v1_projects_ref_vanity_subdomain_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_projects_ref_vanity_subdomain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProjectsRefVanitySubdomainArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_projects_ref_vanity_subdomain_builder()` + `delete_v1_projects_ref_vanity_subdomain_execute()`.
/// For task-level control, use `delete_v1_projects_ref_vanity_subdomain_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_projects_ref_vanity_subdomain(
    client: &SimpleHttpClient,
    args: &DeleteV1ProjectsRefVanitySubdomainArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_projects_ref_vanity_subdomain_builder(client, &args.ref_rs)?;
    delete_v1_projects_ref_vanity_subdomain_execute(builder)
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_vanity_subdomain_activate_execute()` to send, or `post_v1_projects_ref_vanity_subdomain_activate` for simplest API.

pub fn post_v1_projects_ref_vanity_subdomain_activate_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &VanitySubdomainBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain/activate",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_vanity_subdomain_activate_execute()` or `post_v1_projects_ref_vanity_subdomain_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_vanity_subdomain_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_vanity_subdomain_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ActivateVanitySubdomainResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ActivateVanitySubdomainResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_vanity_subdomain_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_vanity_subdomain_activate_task()`.
/// For the simplest API, use `post_v1_projects_ref_vanity_subdomain_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_vanity_subdomain_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_vanity_subdomain_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ActivateVanitySubdomainResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_vanity_subdomain_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_vanity_subdomain_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefVanitySubdomainActivateArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: VanitySubdomainBody,
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_vanity_subdomain_activate_builder()` + `post_v1_projects_ref_vanity_subdomain_activate_execute()`.
/// For task-level control, use `post_v1_projects_ref_vanity_subdomain_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_vanity_subdomain_activate(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefVanitySubdomainActivateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ActivateVanitySubdomainResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_projects_ref_vanity_subdomain_activate_builder(client, &args.ref_rs, &args.body)?;
    post_v1_projects_ref_vanity_subdomain_activate_execute(builder)
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_projects_ref_vanity_subdomain_check_availability_execute()` to send, or `post_v1_projects_ref_vanity_subdomain_check_availability` for simplest API.

pub fn post_v1_projects_ref_vanity_subdomain_check_availability_builder(
    client: &SimpleHttpClient,
    ref_rs: &str,
    body: &VanitySubdomainBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain/check-availability",
        ref_rs,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_v1_projects_ref_vanity_subdomain_check_availability_execute()` or `post_v1_projects_ref_vanity_subdomain_check_availability`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_vanity_subdomain_check_availability_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_vanity_subdomain_check_availability_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SubdomainAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubdomainAvailabilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_v1_projects_ref_vanity_subdomain_check_availability_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_v1_projects_ref_vanity_subdomain_check_availability_task()`.
/// For the simplest API, use `post_v1_projects_ref_vanity_subdomain_check_availability()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_v1_projects_ref_vanity_subdomain_check_availability_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_v1_projects_ref_vanity_subdomain_check_availability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubdomainAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = post_v1_projects_ref_vanity_subdomain_check_availability_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_projects_ref_vanity_subdomain_check_availability`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsRefVanitySubdomainCheckAvailabilityArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: VanitySubdomainBody,
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_projects_ref_vanity_subdomain_check_availability_builder()` + `post_v1_projects_ref_vanity_subdomain_check_availability_execute()`.
/// For task-level control, use `post_v1_projects_ref_vanity_subdomain_check_availability_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_projects_ref_vanity_subdomain_check_availability(
    client: &SimpleHttpClient,
    args: &PostV1ProjectsRefVanitySubdomainCheckAvailabilityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubdomainAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_projects_ref_vanity_subdomain_check_availability_builder(
        client,
        &args.ref_rs,
        &args.body,
    )?;
    post_v1_projects_ref_vanity_subdomain_check_availability_execute(builder)
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_snippets_execute()` to send, or `get_v1_snippets` for simplest API.

pub fn get_v1_snippets_builder(
    client: &SimpleHttpClient,
    project_ref: Option<&str>,
    cursor: Option<&str>,
    limit: Option<&str>,
    sort_by: Option<&str>,
    sort_order: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/snippets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = project_ref {
        query_parts.push(format!("project_ref={}", val));
    }
    if let Some(val) = cursor {
        query_parts.push(format!("cursor={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = sort_by {
        query_parts.push(format!("sort_by={}", val));
    }
    if let Some(val) = sort_order {
        query_parts.push(format!("sort_order={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_snippets_execute()` or `get_v1_snippets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_snippets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_snippets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SnippetList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnippetList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_snippets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_snippets_task()`.
/// For the simplest API, use `get_v1_snippets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_snippets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_snippets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnippetList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_v1_snippets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_snippets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SnippetsArgs {
    /// Query parameter: project_ref
    pub project_ref: Option<String>,
    /// Query parameter: cursor
    pub cursor: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: sort_by
    pub sort_by: Option<String>,
    /// Query parameter: sort_order
    pub sort_order: Option<String>,
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_snippets_builder()` + `get_v1_snippets_execute()`.
/// For task-level control, use `get_v1_snippets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_snippets(
    client: &SimpleHttpClient,
    args: &GetV1SnippetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnippetList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_snippets_builder(
        client,
        args.project_ref.as_deref(),
        args.cursor.as_deref(),
        args.limit.as_deref(),
        args.sort_by.as_deref(),
        args.sort_order.as_deref(),
    )?;
    get_v1_snippets_execute(builder)
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_snippets_id_execute()` to send, or `get_v1_snippets_id` for simplest API.

pub fn get_v1_snippets_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.supabase.com/v1/snippets/{}", id,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_v1_snippets_id_execute()` or `get_v1_snippets_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_snippets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_snippets_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SnippetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnippetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_v1_snippets_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_v1_snippets_id_task()`.
/// For the simplest API, use `get_v1_snippets_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_v1_snippets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_v1_snippets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnippetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = get_v1_snippets_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_snippets_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SnippetsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_snippets_id_builder()` + `get_v1_snippets_id_execute()`.
/// For task-level control, use `get_v1_snippets_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_snippets_id(
    client: &SimpleHttpClient,
    args: &GetV1SnippetsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnippetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_snippets_id_builder(client, &args.id)?;
    get_v1_snippets_id_execute(builder)
}
