//! Auto-generated API clients for supabase.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `supabase`

#![cfg(feature = "supabase")]

use foundation_core::valtron::{execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;
use crate::providers::supabase::clients::types::*;
use crate::providers::supabase::resources::*;
use foundation_db::state::resource_identifier::ResourceIdentifier;

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_branch_config_execute()` to send, or `v1_get_a_branch_config` for simplest API.

pub fn v1_get_a_branch_config_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}",
        branch_id_or_ref,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_branch_config_execute()` or `v1_get_a_branch_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_branch_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_branch_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchDetailResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchDetailResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_branch_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_branch_config_task()`.
/// For the simplest API, use `v1_get_a_branch_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_branch_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_branch_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchDetailResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_branch_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_branch_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetABranchConfigArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
}

/// GET /v1/branches/{branch_id_or_ref}
/// Get database branch config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_branch_config_builder()` + `v1_get_a_branch_config_execute()`.
/// For task-level control, use `v1_get_a_branch_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_branch_config(
    client: &SimpleHttpClient,
    args: &V1GetABranchConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchDetailResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_branch_config_builder(client, &args.branch_id_or_ref)?;
    v1_get_a_branch_config_execute(builder)
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_a_branch_config_execute()` to send, or `v1_update_a_branch_config` for simplest API.

pub fn v1_update_a_branch_config_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
    body: &UpdateBranchBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}",
        branch_id_or_ref,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_a_branch_config_execute()` or `v1_update_a_branch_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_branch_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_branch_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_a_branch_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_a_branch_config_task()`.
/// For the simplest API, use `v1_update_a_branch_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_branch_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_a_branch_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_a_branch_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_a_branch_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateABranchConfigArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: UpdateBranchBody,
}

/// PATCH /v1/branches/{branch_id_or_ref}
/// Update database branch config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_a_branch_config_builder()` + `v1_update_a_branch_config_execute()`.
/// For task-level control, use `v1_update_a_branch_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_branch_config(
    client: &SimpleHttpClient,
    args: &V1UpdateABranchConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_a_branch_config_builder(client, &args.branch_id_or_ref, &args.body)?;
    v1_update_a_branch_config_execute(builder)
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_a_branch_execute()` to send, or `v1_delete_a_branch` for simplest API.

pub fn v1_delete_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}",
        branch_id_or_ref,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_a_branch_execute()` or `v1_delete_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchDeleteResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchDeleteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_a_branch_task()`.
/// For the simplest API, use `v1_delete_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchDeleteResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteABranchArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE /v1/branches/{branch_id_or_ref}
/// Delete a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_a_branch_builder()` + `v1_delete_a_branch_execute()`.
/// For task-level control, use `v1_delete_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_branch(
    client: &SimpleHttpClient,
    args: &V1DeleteABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchDeleteResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_a_branch_builder(client, &args.branch_id_or_ref, &args.force)?;
    v1_delete_a_branch_execute(builder)
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_diff_a_branch_execute()` to send, or `v1_diff_a_branch` for simplest API.

pub fn v1_diff_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
    included_schemas: &Option<Option<String>>,
    pgdelta: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}/diff",
        branch_id_or_ref,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = included_schemas.as_ref() {
        query_parts.push(format!("included_schemas={}", val));
    }
    if let Some(val) = pgdelta.as_ref() {
        query_parts.push(format!("pgdelta={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_diff_a_branch_execute()` or `v1_diff_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_diff_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_diff_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_diff_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_diff_a_branch_task()`.
/// For the simplest API, use `v1_diff_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_diff_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_diff_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_diff_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_diff_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DiffABranchArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Query parameter: included_schemas
    pub included_schemas: Option<Option<String>>,
    /// Query parameter: pgdelta
    pub pgdelta: Option<Option<String>>,
}

/// GET /v1/branches/{branch_id_or_ref}/diff
/// [Beta] Diffs a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_diff_a_branch_builder()` + `v1_diff_a_branch_execute()`.
/// For task-level control, use `v1_diff_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_diff_a_branch(
    client: &SimpleHttpClient,
    args: &V1DiffABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_diff_a_branch_builder(client, &args.branch_id_or_ref, &args.included_schemas, &args.pgdelta)?;
    v1_diff_a_branch_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_merge_a_branch_execute()` to send, or `v1_merge_a_branch` for simplest API.

pub fn v1_merge_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
    body: &BranchActionBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}/merge",
        branch_id_or_ref,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_merge_a_branch_execute()` or `v1_merge_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_merge_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_merge_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_merge_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_merge_a_branch_task()`.
/// For the simplest API, use `v1_merge_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_merge_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_merge_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_merge_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_merge_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1MergeABranchArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: BranchActionBody,
}

/// POST /v1/branches/{branch_id_or_ref}/merge
/// Merges a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_merge_a_branch_builder()` + `v1_merge_a_branch_execute()`.
/// For task-level control, use `v1_merge_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_merge_a_branch(
    client: &SimpleHttpClient,
    args: &V1MergeABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_merge_a_branch_builder(client, &args.branch_id_or_ref, &args.body)?;
    v1_merge_a_branch_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_push_a_branch_execute()` to send, or `v1_push_a_branch` for simplest API.

pub fn v1_push_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
    body: &BranchActionBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}/push",
        branch_id_or_ref,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_push_a_branch_execute()` or `v1_push_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_push_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_push_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_push_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_push_a_branch_task()`.
/// For the simplest API, use `v1_push_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_push_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_push_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_push_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_push_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1PushABranchArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: BranchActionBody,
}

/// POST /v1/branches/{branch_id_or_ref}/push
/// Pushes a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_push_a_branch_builder()` + `v1_push_a_branch_execute()`.
/// For task-level control, use `v1_push_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_push_a_branch(
    client: &SimpleHttpClient,
    args: &V1PushABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_push_a_branch_builder(client, &args.branch_id_or_ref, &args.body)?;
    v1_push_a_branch_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_reset_a_branch_execute()` to send, or `v1_reset_a_branch` for simplest API.

pub fn v1_reset_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
    body: &BranchActionBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}/reset",
        branch_id_or_ref,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_reset_a_branch_execute()` or `v1_reset_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_reset_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_reset_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_reset_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_reset_a_branch_task()`.
/// For the simplest API, use `v1_reset_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_reset_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_reset_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_reset_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_reset_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ResetABranchArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
    /// Request body.
    pub body: BranchActionBody,
}

/// POST /v1/branches/{branch_id_or_ref}/reset
/// Resets a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_reset_a_branch_builder()` + `v1_reset_a_branch_execute()`.
/// For task-level control, use `v1_reset_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_reset_a_branch(
    client: &SimpleHttpClient,
    args: &V1ResetABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchUpdateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_reset_a_branch_builder(client, &args.branch_id_or_ref, &args.body)?;
    v1_reset_a_branch_execute(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_restore_a_branch_execute()` to send, or `v1_restore_a_branch` for simplest API.

pub fn v1_restore_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    branch_id_or_ref: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/branches/{}/restore",
        branch_id_or_ref,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_restore_a_branch_execute()` or `v1_restore_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_restore_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_restore_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchRestoreResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchRestoreResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_restore_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_restore_a_branch_task()`.
/// For the simplest API, use `v1_restore_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_restore_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_restore_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchRestoreResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_restore_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_restore_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RestoreABranchArgs {
    /// Path parameter: branch_id_or_ref
    pub branch_id_or_ref: String,
}

/// POST /v1/branches/{branch_id_or_ref}/restore
/// Restore a scheduled branch deletion
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_restore_a_branch_builder()` + `v1_restore_a_branch_execute()`.
/// For task-level control, use `v1_restore_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_restore_a_branch(
    client: &SimpleHttpClient,
    args: &V1RestoreABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchRestoreResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_restore_a_branch_builder(client, &args.branch_id_or_ref)?;
    v1_restore_a_branch_execute(builder)
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_authorize_user_execute()` to send, or `v1_authorize_user` for simplest API.

pub fn v1_authorize_user_builder<R>(
    client: &SimpleHttpClient<R>,
    client_id: &Option<Option<String>>,
    response_type: &Option<Option<String>>,
    redirect_uri: &Option<Option<String>>,
    scope: &Option<Option<String>>,
    state: &Option<Option<String>>,
    response_mode: &Option<Option<String>>,
    code_challenge: &Option<Option<String>>,
    code_challenge_method: &Option<Option<String>>,
    organization_slug: &Option<Option<String>>,
    resource: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/oauth/authorize",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_id.as_ref() {
        query_parts.push(format!("client_id={}", val));
    }
    if let Some(val) = response_type.as_ref() {
        query_parts.push(format!("response_type={}", val));
    }
    if let Some(val) = redirect_uri.as_ref() {
        query_parts.push(format!("redirect_uri={}", val));
    }
    if let Some(val) = scope.as_ref() {
        query_parts.push(format!("scope={}", val));
    }
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = response_mode.as_ref() {
        query_parts.push(format!("response_mode={}", val));
    }
    if let Some(val) = code_challenge.as_ref() {
        query_parts.push(format!("code_challenge={}", val));
    }
    if let Some(val) = code_challenge_method.as_ref() {
        query_parts.push(format!("code_challenge_method={}", val));
    }
    if let Some(val) = organization_slug.as_ref() {
        query_parts.push(format!("organization_slug={}", val));
    }
    if let Some(val) = resource.as_ref() {
        query_parts.push(format!("resource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_authorize_user_execute()` or `v1_authorize_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_authorize_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_authorize_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_authorize_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_authorize_user_task()`.
/// For the simplest API, use `v1_authorize_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_authorize_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_authorize_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_authorize_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_authorize_user`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1AuthorizeUserArgs {
    /// Query parameter: client_id
    pub client_id: Option<Option<String>>,
    /// Query parameter: response_type
    pub response_type: Option<Option<String>>,
    /// Query parameter: redirect_uri
    pub redirect_uri: Option<Option<String>>,
    /// Query parameter: scope
    pub scope: Option<Option<String>>,
    /// Query parameter: state
    pub state: Option<Option<String>>,
    /// Query parameter: response_mode
    pub response_mode: Option<Option<String>>,
    /// Query parameter: code_challenge
    pub code_challenge: Option<Option<String>>,
    /// Query parameter: code_challenge_method
    pub code_challenge_method: Option<Option<String>>,
    /// Query parameter: organization_slug
    pub organization_slug: Option<Option<String>>,
    /// Query parameter: resource
    pub resource: Option<Option<String>>,
}

/// GET /v1/oauth/authorize
/// [Beta] Authorize user through oauth
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_authorize_user_builder()` + `v1_authorize_user_execute()`.
/// For task-level control, use `v1_authorize_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_authorize_user(
    client: &SimpleHttpClient,
    args: &V1AuthorizeUserArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_authorize_user_builder(client, &args.client_id, &args.response_type, &args.redirect_uri, &args.scope, &args.state, &args.response_mode, &args.code_challenge, &args.code_challenge_method, &args.organization_slug, &args.resource)?;
    v1_authorize_user_execute(builder)
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_oauth_authorize_project_claim_execute()` to send, or `v1_oauth_authorize_project_claim` for simplest API.

pub fn v1_oauth_authorize_project_claim_builder<R>(
    client: &SimpleHttpClient<R>,
    project_ref: &Option<Option<String>>,
    client_id: &Option<Option<String>>,
    response_type: &Option<Option<String>>,
    redirect_uri: &Option<Option<String>>,
    state: &Option<Option<String>>,
    response_mode: &Option<Option<String>>,
    code_challenge: &Option<Option<String>>,
    code_challenge_method: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/oauth/authorize/project-claim",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = project_ref.as_ref() {
        query_parts.push(format!("project_ref={}", val));
    }
    if let Some(val) = client_id.as_ref() {
        query_parts.push(format!("client_id={}", val));
    }
    if let Some(val) = response_type.as_ref() {
        query_parts.push(format!("response_type={}", val));
    }
    if let Some(val) = redirect_uri.as_ref() {
        query_parts.push(format!("redirect_uri={}", val));
    }
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = response_mode.as_ref() {
        query_parts.push(format!("response_mode={}", val));
    }
    if let Some(val) = code_challenge.as_ref() {
        query_parts.push(format!("code_challenge={}", val));
    }
    if let Some(val) = code_challenge_method.as_ref() {
        query_parts.push(format!("code_challenge_method={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_oauth_authorize_project_claim_execute()` or `v1_oauth_authorize_project_claim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_oauth_authorize_project_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_oauth_authorize_project_claim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_oauth_authorize_project_claim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_oauth_authorize_project_claim_task()`.
/// For the simplest API, use `v1_oauth_authorize_project_claim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_oauth_authorize_project_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_oauth_authorize_project_claim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_oauth_authorize_project_claim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_oauth_authorize_project_claim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1OauthAuthorizeProjectClaimArgs {
    /// Query parameter: project_ref
    pub project_ref: Option<Option<String>>,
    /// Query parameter: client_id
    pub client_id: Option<Option<String>>,
    /// Query parameter: response_type
    pub response_type: Option<Option<String>>,
    /// Query parameter: redirect_uri
    pub redirect_uri: Option<Option<String>>,
    /// Query parameter: state
    pub state: Option<Option<String>>,
    /// Query parameter: response_mode
    pub response_mode: Option<Option<String>>,
    /// Query parameter: code_challenge
    pub code_challenge: Option<Option<String>>,
    /// Query parameter: code_challenge_method
    pub code_challenge_method: Option<Option<String>>,
}

/// GET /v1/oauth/authorize/project-claim
/// Authorize user through oauth and claim a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_oauth_authorize_project_claim_builder()` + `v1_oauth_authorize_project_claim_execute()`.
/// For task-level control, use `v1_oauth_authorize_project_claim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_oauth_authorize_project_claim(
    client: &SimpleHttpClient,
    args: &V1OauthAuthorizeProjectClaimArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_oauth_authorize_project_claim_builder(client, &args.project_ref, &args.client_id, &args.response_type, &args.redirect_uri, &args.state, &args.response_mode, &args.code_challenge, &args.code_challenge_method)?;
    v1_oauth_authorize_project_claim_execute(builder)
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_revoke_token_execute()` to send, or `v1_revoke_token` for simplest API.

pub fn v1_revoke_token_builder<R>(
    client: &SimpleHttpClient<R>,
    body: &OAuthRevokeTokenBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/oauth/revoke",
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_revoke_token_execute()` or `v1_revoke_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_revoke_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_revoke_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_revoke_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_revoke_token_task()`.
/// For the simplest API, use `v1_revoke_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_revoke_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_revoke_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_revoke_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_revoke_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RevokeTokenArgs {
    /// Request body.
    pub body: OAuthRevokeTokenBody,
}

/// POST /v1/oauth/revoke
/// [Beta] Revoke oauth app authorization and it's corresponding tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_revoke_token_builder()` + `v1_revoke_token_execute()`.
/// For task-level control, use `v1_revoke_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_revoke_token(
    client: &SimpleHttpClient,
    args: &V1RevokeTokenArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_revoke_token_builder(client, &args.body)?;
    v1_revoke_token_execute(builder)
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_exchange_oauth_token_execute()` to send, or `v1_exchange_oauth_token` for simplest API.

pub fn v1_exchange_oauth_token_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/oauth/token",
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_exchange_oauth_token_execute()` or `v1_exchange_oauth_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_exchange_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_exchange_oauth_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<OAuthTokenResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OAuthTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_exchange_oauth_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_exchange_oauth_token_task()`.
/// For the simplest API, use `v1_exchange_oauth_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_exchange_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_exchange_oauth_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OAuthTokenResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_exchange_oauth_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/oauth/token
/// [Beta] Exchange auth code for user's access and refresh token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_exchange_oauth_token_builder()` + `v1_exchange_oauth_token_execute()`.
/// For task-level control, use `v1_exchange_oauth_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_exchange_oauth_token(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OAuthTokenResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_exchange_oauth_token_builder(client)?;
    v1_exchange_oauth_token_execute(builder)
}

/// GET /v1/organizations
/// List all organizations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_organizations_execute()` to send, or `v1_list_all_organizations` for simplest API.

pub fn v1_list_all_organizations_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations",
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations
/// List all organizations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_organizations_execute()` or `v1_list_all_organizations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_organizations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations
/// List all organizations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_organizations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_organizations_task()`.
/// For the simplest API, use `v1_list_all_organizations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_organizations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/organizations
/// List all organizations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_organizations_builder()` + `v1_list_all_organizations_execute()`.
/// For task-level control, use `v1_list_all_organizations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_organizations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_organizations_builder(client)?;
    v1_list_all_organizations_execute(builder)
}

/// POST /v1/organizations
/// Create an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_an_organization_execute()` to send, or `v1_create_an_organization` for simplest API.

pub fn v1_create_an_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    body: &CreateOrganizationV1
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations",
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/organizations
/// Create an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_an_organization_execute()` or `v1_create_an_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_an_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_an_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<OrganizationResponseV1>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationResponseV1 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/organizations
/// Create an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_an_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_an_organization_task()`.
/// For the simplest API, use `v1_create_an_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_an_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_an_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OrganizationResponseV1>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_an_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_an_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateAnOrganizationArgs {
    /// Request body.
    pub body: CreateOrganizationV1,
}

/// POST /v1/organizations
/// Create an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_an_organization_builder()` + `v1_create_an_organization_execute()`.
/// For task-level control, use `v1_create_an_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_an_organization(
    client: &SimpleHttpClient,
    args: &V1CreateAnOrganizationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OrganizationResponseV1>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_an_organization_builder(client, &args.body)?;
    v1_create_an_organization_execute(builder)
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_an_organization_execute()` to send, or `v1_get_an_organization` for simplest API.

pub fn v1_get_an_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    slug: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}",
        slug,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_an_organization_execute()` or `v1_get_an_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_an_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_an_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1OrganizationSlugResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1OrganizationSlugResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_an_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_an_organization_task()`.
/// For the simplest API, use `v1_get_an_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_an_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_an_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1OrganizationSlugResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_an_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_an_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAnOrganizationArgs {
    /// Path parameter: slug
    pub slug: String,
}

/// GET /v1/organizations/{slug}
/// Gets information about the organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_an_organization_builder()` + `v1_get_an_organization_execute()`.
/// For task-level control, use `v1_get_an_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_an_organization(
    client: &SimpleHttpClient,
    args: &V1GetAnOrganizationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1OrganizationSlugResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_an_organization_builder(client, &args.slug)?;
    v1_get_an_organization_execute(builder)
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_organization_entitlements_execute()` to send, or `v1_get_organization_entitlements` for simplest API.

pub fn v1_get_organization_entitlements_builder<R>(
    client: &SimpleHttpClient<R>,
    slug: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/entitlements",
        slug,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_organization_entitlements_execute()` or `v1_get_organization_entitlements`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_organization_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_organization_entitlements_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ListEntitlementsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ListEntitlementsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_organization_entitlements_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_organization_entitlements_task()`.
/// For the simplest API, use `v1_get_organization_entitlements()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_organization_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_organization_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ListEntitlementsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_organization_entitlements_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_organization_entitlements`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetOrganizationEntitlementsArgs {
    /// Path parameter: slug
    pub slug: String,
}

/// GET /v1/organizations/{slug}/entitlements
/// Get entitlements for an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_organization_entitlements_builder()` + `v1_get_organization_entitlements_execute()`.
/// For task-level control, use `v1_get_organization_entitlements_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_organization_entitlements(
    client: &SimpleHttpClient,
    args: &V1GetOrganizationEntitlementsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ListEntitlementsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_organization_entitlements_builder(client, &args.slug)?;
    v1_get_organization_entitlements_execute(builder)
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_organization_members_execute()` to send, or `v1_list_organization_members` for simplest API.

pub fn v1_list_organization_members_builder<R>(
    client: &SimpleHttpClient<R>,
    slug: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/members",
        slug,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_organization_members_execute()` or `v1_list_organization_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_organization_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_organization_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_organization_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_organization_members_task()`.
/// For the simplest API, use `v1_list_organization_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_organization_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_organization_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_organization_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_organization_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListOrganizationMembersArgs {
    /// Path parameter: slug
    pub slug: String,
}

/// GET /v1/organizations/{slug}/members
/// List members of an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_organization_members_builder()` + `v1_list_organization_members_execute()`.
/// For task-level control, use `v1_list_organization_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_organization_members(
    client: &SimpleHttpClient,
    args: &V1ListOrganizationMembersArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_organization_members_builder(client, &args.slug)?;
    v1_list_organization_members_execute(builder)
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_organization_project_claim_execute()` to send, or `v1_get_organization_project_claim` for simplest API.

pub fn v1_get_organization_project_claim_builder<R>(
    client: &SimpleHttpClient<R>,
    slug: &String,
    token: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/project-claim/{}",
        slug,
        token,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_organization_project_claim_execute()` or `v1_get_organization_project_claim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_organization_project_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_organization_project_claim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<OrganizationProjectClaimResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationProjectClaimResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_organization_project_claim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_organization_project_claim_task()`.
/// For the simplest API, use `v1_get_organization_project_claim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_organization_project_claim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_organization_project_claim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OrganizationProjectClaimResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_organization_project_claim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_organization_project_claim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetOrganizationProjectClaimArgs {
    /// Path parameter: slug
    pub slug: String,
    /// Path parameter: token
    pub token: String,
}

/// GET /v1/organizations/{slug}/project-claim/{token}
/// Gets project details for the specified organization and claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_organization_project_claim_builder()` + `v1_get_organization_project_claim_execute()`.
/// For task-level control, use `v1_get_organization_project_claim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_organization_project_claim(
    client: &SimpleHttpClient,
    args: &V1GetOrganizationProjectClaimArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OrganizationProjectClaimResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_organization_project_claim_builder(client, &args.slug, &args.token)?;
    v1_get_organization_project_claim_execute(builder)
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_claim_project_for_organization_execute()` to send, or `v1_claim_project_for_organization` for simplest API.

pub fn v1_claim_project_for_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    slug: &String,
    token: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/project-claim/{}",
        slug,
        token,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_claim_project_for_organization_execute()` or `v1_claim_project_for_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_claim_project_for_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_claim_project_for_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_claim_project_for_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_claim_project_for_organization_task()`.
/// For the simplest API, use `v1_claim_project_for_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_claim_project_for_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_claim_project_for_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_claim_project_for_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_claim_project_for_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ClaimProjectForOrganizationArgs {
    /// Path parameter: slug
    pub slug: String,
    /// Path parameter: token
    pub token: String,
}

/// POST /v1/organizations/{slug}/project-claim/{token}
/// Claims project for the specified organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_claim_project_for_organization_builder()` + `v1_claim_project_for_organization_execute()`.
/// For task-level control, use `v1_claim_project_for_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_claim_project_for_organization(
    client: &SimpleHttpClient,
    args: &V1ClaimProjectForOrganizationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_claim_project_for_organization_builder(client, &args.slug, &args.token)?;
    v1_claim_project_for_organization_execute(builder)
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_all_projects_for_organization_execute()` to send, or `v1_get_all_projects_for_organization` for simplest API.

pub fn v1_get_all_projects_for_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    slug: &String,
    offset: &Option<Option<String>>,
    limit: &Option<Option<String>>,
    search: &Option<Option<String>>,
    sort: &Option<Option<String>>,
    statuses: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/projects",
        slug,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = offset.as_ref() {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = search.as_ref() {
        query_parts.push(format!("search={}", val));
    }
    if let Some(val) = sort.as_ref() {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = statuses.as_ref() {
        query_parts.push(format!("statuses={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_all_projects_for_organization_execute()` or `v1_get_all_projects_for_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_all_projects_for_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_all_projects_for_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<OrganizationProjectsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_all_projects_for_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_all_projects_for_organization_task()`.
/// For the simplest API, use `v1_get_all_projects_for_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_all_projects_for_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_all_projects_for_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OrganizationProjectsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_all_projects_for_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_all_projects_for_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAllProjectsForOrganizationArgs {
    /// Path parameter: slug
    pub slug: String,
    /// Query parameter: offset
    pub offset: Option<Option<String>>,
    /// Query parameter: limit
    pub limit: Option<Option<String>>,
    /// Query parameter: search
    pub search: Option<Option<String>>,
    /// Query parameter: sort
    pub sort: Option<Option<String>>,
    /// Query parameter: statuses
    pub statuses: Option<Option<String>>,
}

/// GET /v1/organizations/{slug}/projects
/// Gets all projects for the given organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_all_projects_for_organization_builder()` + `v1_get_all_projects_for_organization_execute()`.
/// For task-level control, use `v1_get_all_projects_for_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_all_projects_for_organization(
    client: &SimpleHttpClient,
    args: &V1GetAllProjectsForOrganizationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<OrganizationProjectsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_all_projects_for_organization_builder(client, &args.slug, &args.offset, &args.limit, &args.search, &args.sort, &args.statuses)?;
    v1_get_all_projects_for_organization_execute(builder)
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_profile_execute()` to send, or `v1_get_profile` for simplest API.

pub fn v1_get_profile_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/profile",
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_profile_execute()` or `v1_get_profile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_profile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProfileResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProfileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_profile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_profile_task()`.
/// For the simplest API, use `v1_get_profile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProfileResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_profile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/profile
/// Gets the user's profile
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_profile_builder()` + `v1_get_profile_execute()`.
/// For task-level control, use `v1_get_profile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_profile(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProfileResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_profile_builder(client)?;
    v1_get_profile_execute(builder)
}

/// GET /v1/projects
/// List all projects
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_projects_execute()` to send, or `v1_list_all_projects` for simplest API.

pub fn v1_list_all_projects_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects",
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects
/// List all projects
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_projects_execute()` or `v1_list_all_projects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_projects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects
/// List all projects
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_projects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_projects_task()`.
/// For the simplest API, use `v1_list_all_projects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_projects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_projects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_projects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/projects
/// List all projects
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_projects_builder()` + `v1_list_all_projects_execute()`.
/// For task-level control, use `v1_list_all_projects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_projects(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_projects_builder(client)?;
    v1_list_all_projects_execute(builder)
}

/// POST /v1/projects
/// Create a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_a_project_execute()` to send, or `v1_create_a_project` for simplest API.

pub fn v1_create_a_project_builder<R>(
    client: &SimpleHttpClient<R>,
    body: &V1CreateProjectBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects",
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects
/// Create a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_a_project_execute()` or `v1_create_a_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProjectResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects
/// Create a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_a_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_a_project_task()`.
/// For the simplest API, use `v1_create_a_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_a_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_a_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_a_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateAProjectArgs {
    /// Request body.
    pub body: V1CreateProjectBody,
}

/// POST /v1/projects
/// Create a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_a_project_builder()` + `v1_create_a_project_execute()`.
/// For task-level control, use `v1_create_a_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_project(
    client: &SimpleHttpClient,
    args: &V1CreateAProjectArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_a_project_builder(client, &args.body)?;
    v1_create_a_project_execute(builder)
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_available_regions_execute()` to send, or `v1_get_available_regions` for simplest API.

pub fn v1_get_available_regions_builder<R>(
    client: &SimpleHttpClient<R>,
    organization_slug: &Option<Option<String>>,
    continent: &Option<Option<String>>,
    desired_instance_size: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/available-regions",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = organization_slug.as_ref() {
        query_parts.push(format!("organization_slug={}", val));
    }
    if let Some(val) = continent.as_ref() {
        query_parts.push(format!("continent={}", val));
    }
    if let Some(val) = desired_instance_size.as_ref() {
        query_parts.push(format!("desired_instance_size={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_available_regions_execute()` or `v1_get_available_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_available_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_available_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<RegionsInfo>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionsInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_available_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_available_regions_task()`.
/// For the simplest API, use `v1_get_available_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_available_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_available_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<RegionsInfo>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_available_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_available_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAvailableRegionsArgs {
    /// Query parameter: organization_slug
    pub organization_slug: Option<Option<String>>,
    /// Query parameter: continent
    pub continent: Option<Option<String>>,
    /// Query parameter: desired_instance_size
    pub desired_instance_size: Option<Option<String>>,
}

/// GET /v1/projects/available-regions
/// [Beta] Gets the list of available regions that can be used for a new project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_available_regions_builder()` + `v1_get_available_regions_execute()`.
/// For task-level control, use `v1_get_available_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_available_regions(
    client: &SimpleHttpClient,
    args: &V1GetAvailableRegionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<RegionsInfo>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_available_regions_builder(client, &args.organization_slug, &args.continent, &args.desired_instance_size)?;
    v1_get_available_regions_execute(builder)
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_execute()` to send, or `v1_get_project` for simplest API.

pub fn v1_get_project_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_execute()` or `v1_get_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProjectWithDatabaseResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectWithDatabaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_task()`.
/// For the simplest API, use `v1_get_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectWithDatabaseResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}
/// Gets a specific project that belongs to the authenticated user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_builder()` + `v1_get_project_execute()`.
/// For task-level control, use `v1_get_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project(
    client: &SimpleHttpClient,
    args: &V1GetProjectArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectWithDatabaseResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_builder(client, &args.ref_rs)?;
    v1_get_project_execute(builder)
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_a_project_execute()` to send, or `v1_update_a_project` for simplest API.

pub fn v1_update_a_project_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1UpdateProjectBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_a_project_execute()` or `v1_update_a_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProjectRefResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectRefResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_a_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_a_project_task()`.
/// For the simplest API, use `v1_update_a_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_a_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_a_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_a_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateAProjectArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpdateProjectBody,
}

/// PATCH /v1/projects/{ref}
/// Updates the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_a_project_builder()` + `v1_update_a_project_execute()`.
/// For task-level control, use `v1_update_a_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_project(
    client: &SimpleHttpClient,
    args: &V1UpdateAProjectArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_a_project_builder(client, &args.ref_rs, &args.body)?;
    v1_update_a_project_execute(builder)
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_a_project_execute()` to send, or `v1_delete_a_project` for simplest API.

pub fn v1_delete_a_project_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_a_project_execute()` or `v1_delete_a_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProjectRefResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectRefResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_a_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_a_project_task()`.
/// For the simplest API, use `v1_delete_a_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_a_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_a_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_a_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteAProjectArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}
/// Deletes the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_a_project_builder()` + `v1_delete_a_project_execute()`.
/// For task-level control, use `v1_delete_a_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_project(
    client: &SimpleHttpClient,
    args: &V1DeleteAProjectArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectRefResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_a_project_builder(client, &args.ref_rs)?;
    v1_delete_a_project_execute(builder)
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_action_runs_execute()` to send, or `v1_list_action_runs` for simplest API.

pub fn v1_list_action_runs_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    offset: &Option<Option<String>>,
    limit: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/actions",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = offset.as_ref() {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_action_runs_execute()` or `v1_list_action_runs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_action_runs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_action_runs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ListActionRunResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListActionRunResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_action_runs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_action_runs_task()`.
/// For the simplest API, use `v1_list_action_runs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_action_runs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_action_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListActionRunResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_action_runs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_action_runs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListActionRunsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: offset
    pub offset: Option<Option<String>>,
    /// Query parameter: limit
    pub limit: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/actions
/// List all action runs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_action_runs_builder()` + `v1_list_action_runs_execute()`.
/// For task-level control, use `v1_list_action_runs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_action_runs(
    client: &SimpleHttpClient,
    args: &V1ListActionRunsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListActionRunResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_action_runs_builder(client, &args.ref_rs, &args.offset, &args.limit)?;
    v1_list_action_runs_execute(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_action_run_execute()` to send, or `v1_get_action_run` for simplest API.

pub fn v1_get_action_run_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    run_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/actions/{}",
        ref_rs,
        run_id,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_action_run_execute()` or `v1_get_action_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_action_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_action_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ActionRunResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ActionRunResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_action_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_action_run_task()`.
/// For the simplest API, use `v1_get_action_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_action_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_action_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ActionRunResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_action_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_action_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetActionRunArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: run_id
    pub run_id: String,
}

/// GET /v1/projects/{ref}/actions/{run_id}
/// Get the status of an action run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_action_run_builder()` + `v1_get_action_run_execute()`.
/// For task-level control, use `v1_get_action_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_action_run(
    client: &SimpleHttpClient,
    args: &V1GetActionRunArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ActionRunResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_action_run_builder(client, &args.ref_rs, &args.run_id)?;
    v1_get_action_run_execute(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_action_run_logs_execute()` to send, or `v1_get_action_run_logs` for simplest API.

pub fn v1_get_action_run_logs_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    run_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/actions/{}/logs",
        ref_rs,
        run_id,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_action_run_logs_execute()` or `v1_get_action_run_logs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_action_run_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_action_run_logs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_action_run_logs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_action_run_logs_task()`.
/// For the simplest API, use `v1_get_action_run_logs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_action_run_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_action_run_logs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_action_run_logs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_action_run_logs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetActionRunLogsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: run_id
    pub run_id: String,
}

/// GET /v1/projects/{ref}/actions/{run_id}/logs
/// Get the logs of an action run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_action_run_logs_builder()` + `v1_get_action_run_logs_execute()`.
/// For task-level control, use `v1_get_action_run_logs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_action_run_logs(
    client: &SimpleHttpClient,
    args: &V1GetActionRunLogsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_action_run_logs_builder(client, &args.ref_rs, &args.run_id)?;
    v1_get_action_run_logs_execute(builder)
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_action_run_status_execute()` to send, or `v1_update_action_run_status` for simplest API.

pub fn v1_update_action_run_status_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    run_id: &String,
    body: &UpdateRunStatusBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/actions/{}/status",
        ref_rs,
        run_id,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_action_run_status_execute()` or `v1_update_action_run_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_action_run_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_action_run_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateRunStatusResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateRunStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_action_run_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_action_run_status_task()`.
/// For the simplest API, use `v1_update_action_run_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_action_run_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_action_run_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateRunStatusResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_action_run_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_action_run_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateActionRunStatusArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: run_id
    pub run_id: String,
    /// Request body.
    pub body: UpdateRunStatusBody,
}

/// PATCH /v1/projects/{ref}/actions/{run_id}/status
/// Update the status of an action run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_action_run_status_builder()` + `v1_update_action_run_status_execute()`.
/// For task-level control, use `v1_update_action_run_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_action_run_status(
    client: &SimpleHttpClient,
    args: &V1UpdateActionRunStatusArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateRunStatusResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_action_run_status_builder(client, &args.ref_rs, &args.run_id, &args.body)?;
    v1_update_action_run_status_execute(builder)
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_performance_advisors_execute()` to send, or `v1_get_performance_advisors` for simplest API.

pub fn v1_get_performance_advisors_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/advisors/performance",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_performance_advisors_execute()` or `v1_get_performance_advisors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_performance_advisors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_performance_advisors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectAdvisorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_performance_advisors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_performance_advisors_task()`.
/// For the simplest API, use `v1_get_performance_advisors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_performance_advisors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_performance_advisors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_performance_advisors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_performance_advisors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPerformanceAdvisorsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/advisors/performance
/// Gets project performance advisors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_performance_advisors_builder()` + `v1_get_performance_advisors_execute()`.
/// For task-level control, use `v1_get_performance_advisors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_performance_advisors(
    client: &SimpleHttpClient,
    args: &V1GetPerformanceAdvisorsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_performance_advisors_builder(client, &args.ref_rs)?;
    v1_get_performance_advisors_execute(builder)
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_security_advisors_execute()` to send, or `v1_get_security_advisors` for simplest API.

pub fn v1_get_security_advisors_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    lint_type: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/advisors/security",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = lint_type.as_ref() {
        query_parts.push(format!("lint_type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_security_advisors_execute()` or `v1_get_security_advisors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_security_advisors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_security_advisors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ProjectAdvisorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_security_advisors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_security_advisors_task()`.
/// For the simplest API, use `v1_get_security_advisors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_security_advisors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_security_advisors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_security_advisors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_security_advisors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetSecurityAdvisorsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: lint_type
    pub lint_type: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/advisors/security
/// Gets project security advisors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_security_advisors_builder()` + `v1_get_security_advisors_execute()`.
/// For task-level control, use `v1_get_security_advisors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_security_advisors(
    client: &SimpleHttpClient,
    args: &V1GetSecurityAdvisorsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ProjectAdvisorsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_security_advisors_builder(client, &args.ref_rs, &args.lint_type)?;
    v1_get_security_advisors_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_function_combined_stats_execute()` to send, or `v1_get_project_function_combined_stats` for simplest API.

pub fn v1_get_project_function_combined_stats_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    interval: &Option<Option<String>>,
    function_id: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/functions.combined-stats",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interval.as_ref() {
        query_parts.push(format!("interval={}", val));
    }
    if let Some(val) = function_id.as_ref() {
        query_parts.push(format!("function_id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_function_combined_stats_execute()` or `v1_get_project_function_combined_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_function_combined_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_function_combined_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<AnalyticsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyticsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_function_combined_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_function_combined_stats_task()`.
/// For the simplest API, use `v1_get_project_function_combined_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_function_combined_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_function_combined_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AnalyticsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_function_combined_stats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_function_combined_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectFunctionCombinedStatsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: interval
    pub interval: Option<Option<String>>,
    /// Query parameter: function_id
    pub function_id: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/analytics/endpoints/functions.combined-stats
/// Gets a project's function combined statistics
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_function_combined_stats_builder()` + `v1_get_project_function_combined_stats_execute()`.
/// For task-level control, use `v1_get_project_function_combined_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_function_combined_stats(
    client: &SimpleHttpClient,
    args: &V1GetProjectFunctionCombinedStatsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AnalyticsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_function_combined_stats_builder(client, &args.ref_rs, &args.interval, &args.function_id)?;
    v1_get_project_function_combined_stats_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_logs_execute()` to send, or `v1_get_project_logs` for simplest API.

pub fn v1_get_project_logs_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    sql: &Option<Option<String>>,
    iso_timestamp_start: &Option<Option<String>>,
    iso_timestamp_end: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/logs.all",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sql.as_ref() {
        query_parts.push(format!("sql={}", val));
    }
    if let Some(val) = iso_timestamp_start.as_ref() {
        query_parts.push(format!("iso_timestamp_start={}", val));
    }
    if let Some(val) = iso_timestamp_end.as_ref() {
        query_parts.push(format!("iso_timestamp_end={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_logs_execute()` or `v1_get_project_logs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_logs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<AnalyticsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyticsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_logs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_logs_task()`.
/// For the simplest API, use `v1_get_project_logs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_logs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AnalyticsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_logs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_logs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectLogsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: sql
    pub sql: Option<Option<String>>,
    /// Query parameter: iso_timestamp_start
    pub iso_timestamp_start: Option<Option<String>>,
    /// Query parameter: iso_timestamp_end
    pub iso_timestamp_end: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/analytics/endpoints/logs.all
/// Gets project's logs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_logs_builder()` + `v1_get_project_logs_execute()`.
/// For task-level control, use `v1_get_project_logs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_logs(
    client: &SimpleHttpClient,
    args: &V1GetProjectLogsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AnalyticsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_logs_builder(client, &args.ref_rs, &args.sql, &args.iso_timestamp_start, &args.iso_timestamp_end)?;
    v1_get_project_logs_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_usage_api_count_execute()` to send, or `v1_get_project_usage_api_count` for simplest API.

pub fn v1_get_project_usage_api_count_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    interval: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/usage.api-counts",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interval.as_ref() {
        query_parts.push(format!("interval={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_usage_api_count_execute()` or `v1_get_project_usage_api_count`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_usage_api_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_usage_api_count_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1GetUsageApiCountResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1GetUsageApiCountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_usage_api_count_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_usage_api_count_task()`.
/// For the simplest API, use `v1_get_project_usage_api_count()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_usage_api_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_usage_api_count_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1GetUsageApiCountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_usage_api_count_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_usage_api_count`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectUsageApiCountArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: interval
    pub interval: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-counts
/// Gets project's usage api counts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_usage_api_count_builder()` + `v1_get_project_usage_api_count_execute()`.
/// For task-level control, use `v1_get_project_usage_api_count_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_usage_api_count(
    client: &SimpleHttpClient,
    args: &V1GetProjectUsageApiCountArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1GetUsageApiCountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_usage_api_count_builder(client, &args.ref_rs, &args.interval)?;
    v1_get_project_usage_api_count_execute(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_usage_request_count_execute()` to send, or `v1_get_project_usage_request_count` for simplest API.

pub fn v1_get_project_usage_request_count_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/analytics/endpoints/usage.api-requests-count",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_usage_request_count_execute()` or `v1_get_project_usage_request_count`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_usage_request_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_usage_request_count_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1GetUsageApiRequestsCountResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1GetUsageApiRequestsCountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_usage_request_count_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_usage_request_count_task()`.
/// For the simplest API, use `v1_get_project_usage_request_count()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_usage_request_count_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_usage_request_count_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1GetUsageApiRequestsCountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_usage_request_count_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_usage_request_count`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectUsageRequestCountArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/analytics/endpoints/usage.api-requests-count
/// Gets project's usage api requests count
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_usage_request_count_builder()` + `v1_get_project_usage_request_count_execute()`.
/// For task-level control, use `v1_get_project_usage_request_count_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_usage_request_count(
    client: &SimpleHttpClient,
    args: &V1GetProjectUsageRequestCountArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1GetUsageApiRequestsCountResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_usage_request_count_builder(client, &args.ref_rs)?;
    v1_get_project_usage_request_count_execute(builder)
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_api_keys_execute()` to send, or `v1_get_project_api_keys` for simplest API.

pub fn v1_get_project_api_keys_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    reveal: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal.as_ref() {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_api_keys_execute()` or `v1_get_project_api_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_api_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_api_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_api_keys_task()`.
/// For the simplest API, use `v1_get_project_api_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_api_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_api_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_api_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectApiKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: reveal
    pub reveal: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/api-keys
/// Get project api keys
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_api_keys_builder()` + `v1_get_project_api_keys_execute()`.
/// For task-level control, use `v1_get_project_api_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_api_keys(
    client: &SimpleHttpClient,
    args: &V1GetProjectApiKeysArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_api_keys_builder(client, &args.ref_rs, &args.reveal)?;
    v1_get_project_api_keys_execute(builder)
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_project_api_key_execute()` to send, or `v1_create_project_api_key` for simplest API.

pub fn v1_create_project_api_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    reveal: &Option<Option<String>>,
    body: &CreateApiKeyBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal.as_ref() {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_project_api_key_execute()` or `v1_create_project_api_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_api_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_project_api_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_project_api_key_task()`.
/// For the simplest API, use `v1_create_project_api_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_project_api_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_project_api_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_project_api_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectApiKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: reveal
    pub reveal: Option<Option<String>>,
    /// Request body.
    pub body: CreateApiKeyBody,
}

/// POST /v1/projects/{ref}/api-keys
/// Creates a new API key for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_project_api_key_builder()` + `v1_create_project_api_key_execute()`.
/// For task-level control, use `v1_create_project_api_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_api_key(
    client: &SimpleHttpClient,
    args: &V1CreateProjectApiKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_project_api_key_builder(client, &args.ref_rs, &args.reveal, &args.body)?;
    v1_create_project_api_key_execute(builder)
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_legacy_api_keys_execute()` to send, or `v1_get_project_legacy_api_keys` for simplest API.

pub fn v1_get_project_legacy_api_keys_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/legacy",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_legacy_api_keys_execute()` or `v1_get_project_legacy_api_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_legacy_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_legacy_api_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LegacyApiKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_legacy_api_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_legacy_api_keys_task()`.
/// For the simplest API, use `v1_get_project_legacy_api_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_legacy_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_legacy_api_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_legacy_api_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_legacy_api_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectLegacyApiKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/api-keys/legacy
/// Check whether JWT based legacy (anon, service_role) API keys are enabled. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_legacy_api_keys_builder()` + `v1_get_project_legacy_api_keys_execute()`.
/// For task-level control, use `v1_get_project_legacy_api_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_legacy_api_keys(
    client: &SimpleHttpClient,
    args: &V1GetProjectLegacyApiKeysArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_legacy_api_keys_builder(client, &args.ref_rs)?;
    v1_get_project_legacy_api_keys_execute(builder)
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_project_legacy_api_keys_execute()` to send, or `v1_update_project_legacy_api_keys` for simplest API.

pub fn v1_update_project_legacy_api_keys_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    enabled: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/legacy",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enabled.as_ref() {
        query_parts.push(format!("enabled={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_project_legacy_api_keys_execute()` or `v1_update_project_legacy_api_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_project_legacy_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_project_legacy_api_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LegacyApiKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_project_legacy_api_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_project_legacy_api_keys_task()`.
/// For the simplest API, use `v1_update_project_legacy_api_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_project_legacy_api_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_project_legacy_api_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_project_legacy_api_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_project_legacy_api_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateProjectLegacyApiKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: enabled
    pub enabled: Option<Option<String>>,
}

/// PUT /v1/projects/{ref}/api-keys/legacy
/// Disable or re-enable JWT based legacy (anon, service_role) API keys. This API endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_project_legacy_api_keys_builder()` + `v1_update_project_legacy_api_keys_execute()`.
/// For task-level control, use `v1_update_project_legacy_api_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_project_legacy_api_keys(
    client: &SimpleHttpClient,
    args: &V1UpdateProjectLegacyApiKeysArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<LegacyApiKeysResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_project_legacy_api_keys_builder(client, &args.ref_rs, &args.enabled)?;
    v1_update_project_legacy_api_keys_execute(builder)
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_api_key_execute()` to send, or `v1_get_project_api_key` for simplest API.

pub fn v1_get_project_api_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    id: &String,
    reveal: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/{}",
        ref_rs,
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal.as_ref() {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_api_key_execute()` or `v1_get_project_api_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_api_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_api_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_api_key_task()`.
/// For the simplest API, use `v1_get_project_api_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_api_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_api_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_api_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectApiKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: reveal
    pub reveal: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/api-keys/{id}
/// Get API key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_api_key_builder()` + `v1_get_project_api_key_execute()`.
/// For task-level control, use `v1_get_project_api_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_api_key(
    client: &SimpleHttpClient,
    args: &V1GetProjectApiKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_api_key_builder(client, &args.ref_rs, &args.id, &args.reveal)?;
    v1_get_project_api_key_execute(builder)
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_project_api_key_execute()` to send, or `v1_update_project_api_key` for simplest API.

pub fn v1_update_project_api_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    id: &String,
    reveal: &Option<Option<String>>,
    body: &UpdateApiKeyBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/{}",
        ref_rs,
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal.as_ref() {
        query_parts.push(format!("reveal={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_project_api_key_execute()` or `v1_update_project_api_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_project_api_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_project_api_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_project_api_key_task()`.
/// For the simplest API, use `v1_update_project_api_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_project_api_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_project_api_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_project_api_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateProjectApiKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: reveal
    pub reveal: Option<Option<String>>,
    /// Request body.
    pub body: UpdateApiKeyBody,
}

/// PATCH /v1/projects/{ref}/api-keys/{id}
/// Updates an API key for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_project_api_key_builder()` + `v1_update_project_api_key_execute()`.
/// For task-level control, use `v1_update_project_api_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_project_api_key(
    client: &SimpleHttpClient,
    args: &V1UpdateProjectApiKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_project_api_key_builder(client, &args.ref_rs, &args.id, &args.reveal, &args.body)?;
    v1_update_project_api_key_execute(builder)
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_project_api_key_execute()` to send, or `v1_delete_project_api_key` for simplest API.

pub fn v1_delete_project_api_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    id: &String,
    reveal: &Option<Option<String>>,
    was_compromised: &Option<Option<String>>,
    reason: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/api-keys/{}",
        ref_rs,
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reveal.as_ref() {
        query_parts.push(format!("reveal={}", val));
    }
    if let Some(val) = was_compromised.as_ref() {
        query_parts.push(format!("was_compromised={}", val));
    }
    if let Some(val) = reason.as_ref() {
        query_parts.push(format!("reason={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_project_api_key_execute()` or `v1_delete_project_api_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_project_api_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_project_api_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_project_api_key_task()`.
/// For the simplest API, use `v1_delete_project_api_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_project_api_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_project_api_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_project_api_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_project_api_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteProjectApiKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: reveal
    pub reveal: Option<Option<String>>,
    /// Query parameter: was_compromised
    pub was_compromised: Option<Option<String>>,
    /// Query parameter: reason
    pub reason: Option<Option<String>>,
}

/// DELETE /v1/projects/{ref}/api-keys/{id}
/// Deletes an API key for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_project_api_key_builder()` + `v1_delete_project_api_key_execute()`.
/// For task-level control, use `v1_delete_project_api_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_project_api_key(
    client: &SimpleHttpClient,
    args: &V1DeleteProjectApiKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ApiKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_project_api_key_builder(client, &args.ref_rs, &args.id, &args.reveal, &args.was_compromised, &args.reason)?;
    v1_delete_project_api_key_execute(builder)
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_project_addons_execute()` to send, or `v1_list_project_addons` for simplest API.

pub fn v1_list_project_addons_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/billing/addons",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_project_addons_execute()` or `v1_list_project_addons`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_project_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_project_addons_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ListProjectAddonsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProjectAddonsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_project_addons_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_project_addons_task()`.
/// For the simplest API, use `v1_list_project_addons()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_project_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_project_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListProjectAddonsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_project_addons_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_project_addons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListProjectAddonsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/billing/addons
/// List billing addons and compute instance selections
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_project_addons_builder()` + `v1_list_project_addons_execute()`.
/// For task-level control, use `v1_list_project_addons_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_project_addons(
    client: &SimpleHttpClient,
    args: &V1ListProjectAddonsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListProjectAddonsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_project_addons_builder(client, &args.ref_rs)?;
    v1_list_project_addons_execute(builder)
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_apply_project_addon_execute()` to send, or `v1_apply_project_addon` for simplest API.

pub fn v1_apply_project_addon_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &ApplyProjectAddonBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/billing/addons",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_apply_project_addon_execute()` or `v1_apply_project_addon`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_apply_project_addon_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_apply_project_addon_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_apply_project_addon_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_apply_project_addon_task()`.
/// For the simplest API, use `v1_apply_project_addon()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_apply_project_addon_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_apply_project_addon_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_apply_project_addon_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_apply_project_addon`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ApplyProjectAddonArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: ApplyProjectAddonBody,
}

/// PATCH /v1/projects/{ref}/billing/addons
/// Apply or update billing addons, including compute instance size
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_apply_project_addon_builder()` + `v1_apply_project_addon_execute()`.
/// For task-level control, use `v1_apply_project_addon_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_apply_project_addon(
    client: &SimpleHttpClient,
    args: &V1ApplyProjectAddonArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_apply_project_addon_builder(client, &args.ref_rs, &args.body)?;
    v1_apply_project_addon_execute(builder)
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_remove_project_addon_execute()` to send, or `v1_remove_project_addon` for simplest API.

pub fn v1_remove_project_addon_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    addon_variant: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/billing/addons/{}",
        ref_rs,
        addon_variant,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_remove_project_addon_execute()` or `v1_remove_project_addon`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_remove_project_addon_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_remove_project_addon_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_remove_project_addon_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_remove_project_addon_task()`.
/// For the simplest API, use `v1_remove_project_addon()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_remove_project_addon_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_remove_project_addon_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_remove_project_addon_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_remove_project_addon`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RemoveProjectAddonArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: addon_variant
    pub addon_variant: String,
}

/// DELETE /v1/projects/{ref}/billing/addons/{addon_variant}
/// Remove billing addons or revert compute instance sizing
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_remove_project_addon_builder()` + `v1_remove_project_addon_execute()`.
/// For task-level control, use `v1_remove_project_addon_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_remove_project_addon(
    client: &SimpleHttpClient,
    args: &V1RemoveProjectAddonArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_remove_project_addon_builder(client, &args.ref_rs, &args.addon_variant)?;
    v1_remove_project_addon_execute(builder)
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_branches_execute()` to send, or `v1_list_all_branches` for simplest API.

pub fn v1_list_all_branches_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/branches",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_branches_execute()` or `v1_list_all_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_branches_task()`.
/// For the simplest API, use `v1_list_all_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllBranchesArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/branches
/// List all database branches
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_branches_builder()` + `v1_list_all_branches_execute()`.
/// For task-level control, use `v1_list_all_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_branches(
    client: &SimpleHttpClient,
    args: &V1ListAllBranchesArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_branches_builder(client, &args.ref_rs)?;
    v1_list_all_branches_execute(builder)
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_a_branch_execute()` to send, or `v1_create_a_branch` for simplest API.

pub fn v1_create_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &CreateBranchBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/branches",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_a_branch_execute()` or `v1_create_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_a_branch_task()`.
/// For the simplest API, use `v1_create_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateABranchArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateBranchBody,
}

/// POST /v1/projects/{ref}/branches
/// Create a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_a_branch_builder()` + `v1_create_a_branch_execute()`.
/// For task-level control, use `v1_create_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_branch(
    client: &SimpleHttpClient,
    args: &V1CreateABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_a_branch_builder(client, &args.ref_rs, &args.body)?;
    v1_create_a_branch_execute(builder)
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_disable_preview_branching_execute()` to send, or `v1_disable_preview_branching` for simplest API.

pub fn v1_disable_preview_branching_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/branches",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_disable_preview_branching_execute()` or `v1_disable_preview_branching`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_disable_preview_branching_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_disable_preview_branching_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_disable_preview_branching_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_disable_preview_branching_task()`.
/// For the simplest API, use `v1_disable_preview_branching()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_disable_preview_branching_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_disable_preview_branching_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_disable_preview_branching_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_disable_preview_branching`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DisablePreviewBranchingArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/branches
/// Disables preview branching
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_disable_preview_branching_builder()` + `v1_disable_preview_branching_execute()`.
/// For task-level control, use `v1_disable_preview_branching_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_disable_preview_branching(
    client: &SimpleHttpClient,
    args: &V1DisablePreviewBranchingArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_disable_preview_branching_builder(client, &args.ref_rs)?;
    v1_disable_preview_branching_execute(builder)
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_branch_execute()` to send, or `v1_get_a_branch` for simplest API.

pub fn v1_get_a_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/branches/{}",
        ref_rs,
        name,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_branch_execute()` or `v1_get_a_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BranchResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BranchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_branch_task()`.
/// For the simplest API, use `v1_get_a_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetABranchArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: name
    pub name: String,
}

/// GET /v1/projects/{ref}/branches/{name}
/// Get a database branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_branch_builder()` + `v1_get_a_branch_execute()`.
/// For task-level control, use `v1_get_a_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_branch(
    client: &SimpleHttpClient,
    args: &V1GetABranchArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BranchResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_branch_builder(client, &args.ref_rs, &args.name)?;
    v1_get_a_branch_execute(builder)
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_claim_token_execute()` to send, or `v1_get_project_claim_token` for simplest API.

pub fn v1_get_project_claim_token_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_claim_token_execute()` or `v1_get_project_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ProjectClaimTokenResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectClaimTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_claim_token_task()`.
/// For the simplest API, use `v1_get_project_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ProjectClaimTokenResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectClaimTokenArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/claim-token
/// Gets project claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_claim_token_builder()` + `v1_get_project_claim_token_execute()`.
/// For task-level control, use `v1_get_project_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_claim_token(
    client: &SimpleHttpClient,
    args: &V1GetProjectClaimTokenArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ProjectClaimTokenResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_claim_token_builder(client, &args.ref_rs)?;
    v1_get_project_claim_token_execute(builder)
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_project_claim_token_execute()` to send, or `v1_create_project_claim_token` for simplest API.

pub fn v1_create_project_claim_token_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_project_claim_token_execute()` or `v1_create_project_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<CreateProjectClaimTokenResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateProjectClaimTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_project_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_project_claim_token_task()`.
/// For the simplest API, use `v1_create_project_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_project_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CreateProjectClaimTokenResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_project_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_project_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectClaimTokenArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/claim-token
/// Creates project claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_project_claim_token_builder()` + `v1_create_project_claim_token_execute()`.
/// For task-level control, use `v1_create_project_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_claim_token(
    client: &SimpleHttpClient,
    args: &V1CreateProjectClaimTokenArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CreateProjectClaimTokenResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_project_claim_token_builder(client, &args.ref_rs)?;
    v1_create_project_claim_token_execute(builder)
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_project_claim_token_execute()` to send, or `v1_delete_project_claim_token` for simplest API.

pub fn v1_delete_project_claim_token_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_project_claim_token_execute()` or `v1_delete_project_claim_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_project_claim_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_project_claim_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_project_claim_token_task()`.
/// For the simplest API, use `v1_delete_project_claim_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_project_claim_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_project_claim_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_project_claim_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_project_claim_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteProjectClaimTokenArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/claim-token
/// Revokes project claim token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_project_claim_token_builder()` + `v1_delete_project_claim_token_execute()`.
/// For task-level control, use `v1_delete_project_claim_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_project_claim_token(
    client: &SimpleHttpClient,
    args: &V1DeleteProjectClaimTokenArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_project_claim_token_builder(client, &args.ref_rs)?;
    v1_delete_project_claim_token_execute(builder)
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_login_role_execute()` to send, or `v1_create_login_role` for simplest API.

pub fn v1_create_login_role_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &CreateRoleBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/cli/login-role",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_login_role_execute()` or `v1_create_login_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_login_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_login_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<CreateRoleResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateRoleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_login_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_login_role_task()`.
/// For the simplest API, use `v1_create_login_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_login_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_login_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CreateRoleResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_login_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_login_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateLoginRoleArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateRoleBody,
}

/// POST /v1/projects/{ref}/cli/login-role
/// [Beta] Create a login role for CLI with temporary password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_login_role_builder()` + `v1_create_login_role_execute()`.
/// For task-level control, use `v1_create_login_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_login_role(
    client: &SimpleHttpClient,
    args: &V1CreateLoginRoleArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CreateRoleResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_login_role_builder(client, &args.ref_rs, &args.body)?;
    v1_create_login_role_execute(builder)
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_login_roles_execute()` to send, or `v1_delete_login_roles` for simplest API.

pub fn v1_delete_login_roles_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/cli/login-role",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_login_roles_execute()` or `v1_delete_login_roles`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_login_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_login_roles_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DeleteRolesResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteRolesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_login_roles_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_login_roles_task()`.
/// For the simplest API, use `v1_delete_login_roles()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_login_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_login_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeleteRolesResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_login_roles_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_login_roles`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteLoginRolesArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/cli/login-role
/// [Beta] Delete existing login roles used by CLI
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_login_roles_builder()` + `v1_delete_login_roles_execute()`.
/// For task-level control, use `v1_delete_login_roles_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_login_roles(
    client: &SimpleHttpClient,
    args: &V1DeleteLoginRolesArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeleteRolesResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_login_roles_builder(client, &args.ref_rs)?;
    v1_delete_login_roles_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_auth_service_config_execute()` to send, or `v1_get_auth_service_config` for simplest API.

pub fn v1_get_auth_service_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_auth_service_config_execute()` or `v1_get_auth_service_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_auth_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_auth_service_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<AuthConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_auth_service_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_auth_service_config_task()`.
/// For the simplest API, use `v1_get_auth_service_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_auth_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_auth_service_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AuthConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_auth_service_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_auth_service_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAuthServiceConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth
/// Gets project's auth config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_auth_service_config_builder()` + `v1_get_auth_service_config_execute()`.
/// For task-level control, use `v1_get_auth_service_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_auth_service_config(
    client: &SimpleHttpClient,
    args: &V1GetAuthServiceConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AuthConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_auth_service_config_builder(client, &args.ref_rs)?;
    v1_get_auth_service_config_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_auth_service_config_execute()` to send, or `v1_update_auth_service_config` for simplest API.

pub fn v1_update_auth_service_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdateAuthConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_auth_service_config_execute()` or `v1_update_auth_service_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_auth_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_auth_service_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<AuthConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_auth_service_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_auth_service_config_task()`.
/// For the simplest API, use `v1_update_auth_service_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_auth_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_auth_service_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AuthConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_auth_service_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_auth_service_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateAuthServiceConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateAuthConfigBody,
}

/// PATCH /v1/projects/{ref}/config/auth
/// Updates a project's auth config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_auth_service_config_builder()` + `v1_update_auth_service_config_execute()`.
/// For task-level control, use `v1_update_auth_service_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_auth_service_config(
    client: &SimpleHttpClient,
    args: &V1UpdateAuthServiceConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<AuthConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_auth_service_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_auth_service_config_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_signing_keys_execute()` to send, or `v1_get_project_signing_keys` for simplest API.

pub fn v1_get_project_signing_keys_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_signing_keys_execute()` or `v1_get_project_signing_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_signing_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_signing_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeysResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_signing_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_signing_keys_task()`.
/// For the simplest API, use `v1_get_project_signing_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_signing_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_signing_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeysResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_signing_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_signing_keys`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectSigningKeysArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/signing-keys
/// List all signing keys for the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_signing_keys_builder()` + `v1_get_project_signing_keys_execute()`.
/// For task-level control, use `v1_get_project_signing_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_signing_keys(
    client: &SimpleHttpClient,
    args: &V1GetProjectSigningKeysArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeysResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_signing_keys_builder(client, &args.ref_rs)?;
    v1_get_project_signing_keys_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_project_signing_key_execute()` to send, or `v1_create_project_signing_key` for simplest API.

pub fn v1_create_project_signing_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &CreateSigningKeyBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_project_signing_key_execute()` or `v1_create_project_signing_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_signing_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_project_signing_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_project_signing_key_task()`.
/// For the simplest API, use `v1_create_project_signing_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_project_signing_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_project_signing_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_project_signing_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectSigningKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateSigningKeyBody,
}

/// POST /v1/projects/{ref}/config/auth/signing-keys
/// Create a new signing key for the project in standby status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_project_signing_key_builder()` + `v1_create_project_signing_key_execute()`.
/// For task-level control, use `v1_create_project_signing_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_signing_key(
    client: &SimpleHttpClient,
    args: &V1CreateProjectSigningKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_project_signing_key_builder(client, &args.ref_rs, &args.body)?;
    v1_create_project_signing_key_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_legacy_signing_key_execute()` to send, or `v1_get_legacy_signing_key` for simplest API.

pub fn v1_get_legacy_signing_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/legacy",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_legacy_signing_key_execute()` or `v1_get_legacy_signing_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_legacy_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_legacy_signing_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_legacy_signing_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_legacy_signing_key_task()`.
/// For the simplest API, use `v1_get_legacy_signing_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_legacy_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_legacy_signing_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_legacy_signing_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_legacy_signing_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetLegacySigningKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Get the signing key information for the JWT secret imported as signing key for this project. This endpoint will be removed in the future, check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_legacy_signing_key_builder()` + `v1_get_legacy_signing_key_execute()`.
/// For task-level control, use `v1_get_legacy_signing_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_legacy_signing_key(
    client: &SimpleHttpClient,
    args: &V1GetLegacySigningKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_legacy_signing_key_builder(client, &args.ref_rs)?;
    v1_get_legacy_signing_key_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_legacy_signing_key_execute()` to send, or `v1_create_legacy_signing_key` for simplest API.

pub fn v1_create_legacy_signing_key_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/legacy",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_legacy_signing_key_execute()` or `v1_create_legacy_signing_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_legacy_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_legacy_signing_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_legacy_signing_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_legacy_signing_key_task()`.
/// For the simplest API, use `v1_create_legacy_signing_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_legacy_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_legacy_signing_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_legacy_signing_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_legacy_signing_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateLegacySigningKeyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
/// Set up the project's existing JWT secret as an in_use JWT signing key. This endpoint will be removed in the future always check for HTTP 404 Not Found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_legacy_signing_key_builder()` + `v1_create_legacy_signing_key_execute()`.
/// For task-level control, use `v1_create_legacy_signing_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_legacy_signing_key(
    client: &SimpleHttpClient,
    args: &V1CreateLegacySigningKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_legacy_signing_key_builder(client, &args.ref_rs)?;
    v1_create_legacy_signing_key_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_signing_key_execute()` to send, or `v1_get_project_signing_key` for simplest API.

pub fn v1_get_project_signing_key_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        ref_rs,
        id,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_signing_key_execute()` or `v1_get_project_signing_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_signing_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_signing_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_signing_key_task()`.
/// For the simplest API, use `v1_get_project_signing_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_signing_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_signing_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_signing_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectSigningKeyArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Get information about a signing key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_signing_key_builder()` + `v1_get_project_signing_key_execute()`.
/// For task-level control, use `v1_get_project_signing_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_signing_key(
    client: &SimpleHttpClient,
    args: &V1GetProjectSigningKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_signing_key_builder(client, &args.id, &args.ref_rs)?;
    v1_get_project_signing_key_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_project_signing_key_execute()` to send, or `v1_update_project_signing_key` for simplest API.

pub fn v1_update_project_signing_key_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    ref_rs: &String,
    body: &UpdateSigningKeyBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        ref_rs,
        id,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_project_signing_key_execute()` or `v1_update_project_signing_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_project_signing_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_project_signing_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_project_signing_key_task()`.
/// For the simplest API, use `v1_update_project_signing_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_project_signing_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_project_signing_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_project_signing_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateProjectSigningKeyArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateSigningKeyBody,
}

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Update a signing key, mainly its status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_project_signing_key_builder()` + `v1_update_project_signing_key_execute()`.
/// For task-level control, use `v1_update_project_signing_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_project_signing_key(
    client: &SimpleHttpClient,
    args: &V1UpdateProjectSigningKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_project_signing_key_builder(client, &args.id, &args.ref_rs, &args.body)?;
    v1_update_project_signing_key_execute(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_remove_project_signing_key_execute()` to send, or `v1_remove_project_signing_key` for simplest API.

pub fn v1_remove_project_signing_key_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        ref_rs,
        id,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_remove_project_signing_key_execute()` or `v1_remove_project_signing_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_remove_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_remove_project_signing_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigningKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_remove_project_signing_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_remove_project_signing_key_task()`.
/// For the simplest API, use `v1_remove_project_signing_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_remove_project_signing_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_remove_project_signing_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_remove_project_signing_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_remove_project_signing_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RemoveProjectSigningKeyArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
/// Remove a signing key from a project. Only possible if the key has been in revoked status for a while.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_remove_project_signing_key_builder()` + `v1_remove_project_signing_key_execute()`.
/// For task-level control, use `v1_remove_project_signing_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_remove_project_signing_key(
    client: &SimpleHttpClient,
    args: &V1RemoveProjectSigningKeyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SigningKeyResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_remove_project_signing_key_builder(client, &args.id, &args.ref_rs)?;
    v1_remove_project_signing_key_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_sso_provider_execute()` to send, or `v1_list_all_sso_provider` for simplest API.

pub fn v1_list_all_sso_provider_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_sso_provider_execute()` or `v1_list_all_sso_provider`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_sso_provider_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ListProvidersResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProvidersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_sso_provider_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_sso_provider_task()`.
/// For the simplest API, use `v1_list_all_sso_provider()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_sso_provider_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListProvidersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_sso_provider_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_sso_provider`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllSsoProviderArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/sso/providers
/// Lists all SSO providers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_sso_provider_builder()` + `v1_list_all_sso_provider_execute()`.
/// For task-level control, use `v1_list_all_sso_provider_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_sso_provider(
    client: &SimpleHttpClient,
    args: &V1ListAllSsoProviderArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ListProvidersResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_sso_provider_builder(client, &args.ref_rs)?;
    v1_list_all_sso_provider_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_a_sso_provider_execute()` to send, or `v1_create_a_sso_provider` for simplest API.

pub fn v1_create_a_sso_provider_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &CreateProviderBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_a_sso_provider_execute()` or `v1_create_a_sso_provider`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_sso_provider_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<CreateProviderResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_a_sso_provider_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_a_sso_provider_task()`.
/// For the simplest API, use `v1_create_a_sso_provider()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_a_sso_provider_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CreateProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_a_sso_provider_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_a_sso_provider`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateASsoProviderArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateProviderBody,
}

/// POST /v1/projects/{ref}/config/auth/sso/providers
/// Creates a new SSO provider
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_a_sso_provider_builder()` + `v1_create_a_sso_provider_execute()`.
/// For task-level control, use `v1_create_a_sso_provider_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_sso_provider(
    client: &SimpleHttpClient,
    args: &V1CreateASsoProviderArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<CreateProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_a_sso_provider_builder(client, &args.ref_rs, &args.body)?;
    v1_create_a_sso_provider_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_sso_provider_execute()` to send, or `v1_get_a_sso_provider` for simplest API.

pub fn v1_get_a_sso_provider_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    provider_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        ref_rs,
        provider_id,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_sso_provider_execute()` or `v1_get_a_sso_provider`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_sso_provider_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<GetProviderResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_sso_provider_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_sso_provider_task()`.
/// For the simplest API, use `v1_get_a_sso_provider()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_sso_provider_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_sso_provider_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_sso_provider`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetASsoProviderArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: provider_id
    pub provider_id: String,
}

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Gets a SSO provider by its UUID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_sso_provider_builder()` + `v1_get_a_sso_provider_execute()`.
/// For task-level control, use `v1_get_a_sso_provider_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_sso_provider(
    client: &SimpleHttpClient,
    args: &V1GetASsoProviderArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_sso_provider_builder(client, &args.ref_rs, &args.provider_id)?;
    v1_get_a_sso_provider_execute(builder)
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_a_sso_provider_execute()` to send, or `v1_update_a_sso_provider` for simplest API.

pub fn v1_update_a_sso_provider_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    provider_id: &String,
    body: &UpdateProviderBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        ref_rs,
        provider_id,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_a_sso_provider_execute()` or `v1_update_a_sso_provider`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_sso_provider_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateProviderResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_a_sso_provider_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_a_sso_provider_task()`.
/// For the simplest API, use `v1_update_a_sso_provider()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_a_sso_provider_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_a_sso_provider_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_a_sso_provider`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateASsoProviderArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: provider_id
    pub provider_id: String,
    /// Request body.
    pub body: UpdateProviderBody,
}

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Updates a SSO provider by its UUID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_a_sso_provider_builder()` + `v1_update_a_sso_provider_execute()`.
/// For task-level control, use `v1_update_a_sso_provider_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_sso_provider(
    client: &SimpleHttpClient,
    args: &V1UpdateASsoProviderArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_a_sso_provider_builder(client, &args.ref_rs, &args.provider_id, &args.body)?;
    v1_update_a_sso_provider_execute(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_a_sso_provider_execute()` to send, or `v1_delete_a_sso_provider` for simplest API.

pub fn v1_delete_a_sso_provider_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    provider_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        ref_rs,
        provider_id,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_a_sso_provider_execute()` or `v1_delete_a_sso_provider`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_sso_provider_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DeleteProviderResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteProviderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_a_sso_provider_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_a_sso_provider_task()`.
/// For the simplest API, use `v1_delete_a_sso_provider()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_sso_provider_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_a_sso_provider_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeleteProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_a_sso_provider_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_a_sso_provider`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteASsoProviderArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: provider_id
    pub provider_id: String,
}

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
/// Removes a SSO provider by its UUID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_a_sso_provider_builder()` + `v1_delete_a_sso_provider_execute()`.
/// For task-level control, use `v1_delete_a_sso_provider_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_sso_provider(
    client: &SimpleHttpClient,
    args: &V1DeleteASsoProviderArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeleteProviderResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_a_sso_provider_builder(client, &args.ref_rs, &args.provider_id)?;
    v1_delete_a_sso_provider_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_project_tpa_integrations_execute()` to send, or `v1_list_project_tpa_integrations` for simplest API.

pub fn v1_list_project_tpa_integrations_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_project_tpa_integrations_execute()` or `v1_list_project_tpa_integrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_project_tpa_integrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_project_tpa_integrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_project_tpa_integrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_project_tpa_integrations_task()`.
/// For the simplest API, use `v1_list_project_tpa_integrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_project_tpa_integrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_project_tpa_integrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_project_tpa_integrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_project_tpa_integrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListProjectTpaIntegrationsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth
/// Lists all third-party auth integrations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_project_tpa_integrations_builder()` + `v1_list_project_tpa_integrations_execute()`.
/// For task-level control, use `v1_list_project_tpa_integrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_project_tpa_integrations(
    client: &SimpleHttpClient,
    args: &V1ListProjectTpaIntegrationsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_project_tpa_integrations_builder(client, &args.ref_rs)?;
    v1_list_project_tpa_integrations_execute(builder)
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_project_tpa_integration_execute()` to send, or `v1_create_project_tpa_integration` for simplest API.

pub fn v1_create_project_tpa_integration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &CreateThirdPartyAuthBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_project_tpa_integration_execute()` or `v1_create_project_tpa_integration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_tpa_integration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_tpa_integration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_project_tpa_integration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_project_tpa_integration_task()`.
/// For the simplest API, use `v1_create_project_tpa_integration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_project_tpa_integration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_project_tpa_integration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_project_tpa_integration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_project_tpa_integration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectTpaIntegrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateThirdPartyAuthBody,
}

/// POST /v1/projects/{ref}/config/auth/third-party-auth
/// Creates a new third-party auth integration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_project_tpa_integration_builder()` + `v1_create_project_tpa_integration_execute()`.
/// For task-level control, use `v1_create_project_tpa_integration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_project_tpa_integration(
    client: &SimpleHttpClient,
    args: &V1CreateProjectTpaIntegrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_project_tpa_integration_builder(client, &args.ref_rs, &args.body)?;
    v1_create_project_tpa_integration_execute(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_tpa_integration_execute()` to send, or `v1_get_project_tpa_integration` for simplest API.

pub fn v1_get_project_tpa_integration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    tpa_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth/{}",
        ref_rs,
        tpa_id,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_tpa_integration_execute()` or `v1_get_project_tpa_integration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_tpa_integration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_tpa_integration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_tpa_integration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_tpa_integration_task()`.
/// For the simplest API, use `v1_get_project_tpa_integration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_tpa_integration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_tpa_integration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_tpa_integration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_tpa_integration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectTpaIntegrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: tpa_id
    pub tpa_id: String,
}

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Get a third-party integration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_tpa_integration_builder()` + `v1_get_project_tpa_integration_execute()`.
/// For task-level control, use `v1_get_project_tpa_integration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_tpa_integration(
    client: &SimpleHttpClient,
    args: &V1GetProjectTpaIntegrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_tpa_integration_builder(client, &args.ref_rs, &args.tpa_id)?;
    v1_get_project_tpa_integration_execute(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_project_tpa_integration_execute()` to send, or `v1_delete_project_tpa_integration` for simplest API.

pub fn v1_delete_project_tpa_integration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    tpa_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth/{}",
        ref_rs,
        tpa_id,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_project_tpa_integration_execute()` or `v1_delete_project_tpa_integration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_project_tpa_integration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_project_tpa_integration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_project_tpa_integration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_project_tpa_integration_task()`.
/// For the simplest API, use `v1_delete_project_tpa_integration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_project_tpa_integration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_project_tpa_integration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_project_tpa_integration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_project_tpa_integration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteProjectTpaIntegrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: tpa_id
    pub tpa_id: String,
}

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
/// Removes a third-party auth integration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_project_tpa_integration_builder()` + `v1_delete_project_tpa_integration_execute()`.
/// For task-level control, use `v1_delete_project_tpa_integration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_project_tpa_integration(
    client: &SimpleHttpClient,
    args: &V1DeleteProjectTpaIntegrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ThirdPartyAuth>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_project_tpa_integration_builder(client, &args.ref_rs, &args.tpa_id)?;
    v1_delete_project_tpa_integration_execute(builder)
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_pgbouncer_config_execute()` to send, or `v1_get_project_pgbouncer_config` for simplest API.

pub fn v1_get_project_pgbouncer_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/pgbouncer",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_pgbouncer_config_execute()` or `v1_get_project_pgbouncer_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_pgbouncer_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_pgbouncer_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1PgbouncerConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1PgbouncerConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_pgbouncer_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_pgbouncer_config_task()`.
/// For the simplest API, use `v1_get_project_pgbouncer_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_pgbouncer_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_pgbouncer_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1PgbouncerConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_pgbouncer_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_pgbouncer_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectPgbouncerConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/database/pgbouncer
/// Get project's pgbouncer config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_pgbouncer_config_builder()` + `v1_get_project_pgbouncer_config_execute()`.
/// For task-level control, use `v1_get_project_pgbouncer_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_pgbouncer_config(
    client: &SimpleHttpClient,
    args: &V1GetProjectPgbouncerConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1PgbouncerConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_pgbouncer_config_builder(client, &args.ref_rs)?;
    v1_get_project_pgbouncer_config_execute(builder)
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_pooler_config_execute()` to send, or `v1_get_pooler_config` for simplest API.

pub fn v1_get_pooler_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/pooler",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_pooler_config_execute()` or `v1_get_pooler_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_pooler_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_pooler_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_pooler_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_pooler_config_task()`.
/// For the simplest API, use `v1_get_pooler_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_pooler_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_pooler_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_pooler_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_pooler_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPoolerConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/database/pooler
/// Gets project's supavisor config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_pooler_config_builder()` + `v1_get_pooler_config_execute()`.
/// For task-level control, use `v1_get_pooler_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_pooler_config(
    client: &SimpleHttpClient,
    args: &V1GetPoolerConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_pooler_config_builder(client, &args.ref_rs)?;
    v1_get_pooler_config_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_pooler_config_execute()` to send, or `v1_update_pooler_config` for simplest API.

pub fn v1_update_pooler_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdateSupavisorConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/pooler",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_pooler_config_execute()` or `v1_update_pooler_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_pooler_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_pooler_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateSupavisorConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateSupavisorConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_pooler_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_pooler_config_task()`.
/// For the simplest API, use `v1_update_pooler_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_pooler_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_pooler_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateSupavisorConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_pooler_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_pooler_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdatePoolerConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateSupavisorConfigBody,
}

/// PATCH /v1/projects/{ref}/config/database/pooler
/// Updates project's supavisor config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_pooler_config_builder()` + `v1_update_pooler_config_execute()`.
/// For task-level control, use `v1_update_pooler_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_pooler_config(
    client: &SimpleHttpClient,
    args: &V1UpdatePoolerConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateSupavisorConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_pooler_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_pooler_config_execute(builder)
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_postgres_config_execute()` to send, or `v1_get_postgres_config` for simplest API.

pub fn v1_get_postgres_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/postgres",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_postgres_config_execute()` or `v1_get_postgres_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgres_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgres_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<PostgresConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostgresConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_postgres_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_postgres_config_task()`.
/// For the simplest API, use `v1_get_postgres_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgres_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_postgres_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PostgresConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_postgres_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_postgres_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPostgresConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/database/postgres
/// Gets project's Postgres config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_postgres_config_builder()` + `v1_get_postgres_config_execute()`.
/// For task-level control, use `v1_get_postgres_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgres_config(
    client: &SimpleHttpClient,
    args: &V1GetPostgresConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PostgresConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_postgres_config_builder(client, &args.ref_rs)?;
    v1_get_postgres_config_execute(builder)
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_postgres_config_execute()` to send, or `v1_update_postgres_config` for simplest API.

pub fn v1_update_postgres_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdatePostgresConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/database/postgres",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_postgres_config_execute()` or `v1_update_postgres_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_postgres_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_postgres_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<PostgresConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostgresConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_postgres_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_postgres_config_task()`.
/// For the simplest API, use `v1_update_postgres_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_postgres_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_postgres_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PostgresConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_postgres_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_postgres_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdatePostgresConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdatePostgresConfigBody,
}

/// PUT /v1/projects/{ref}/config/database/postgres
/// Updates project's Postgres config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_postgres_config_builder()` + `v1_update_postgres_config_execute()`.
/// For task-level control, use `v1_update_postgres_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_postgres_config(
    client: &SimpleHttpClient,
    args: &V1UpdatePostgresConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PostgresConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_postgres_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_postgres_config_execute(builder)
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_database_disk_execute()` to send, or `v1_get_database_disk` for simplest API.

pub fn v1_get_database_disk_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_database_disk_execute()` or `v1_get_database_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_database_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_database_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DiskResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_database_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_database_disk_task()`.
/// For the simplest API, use `v1_get_database_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_database_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_database_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DiskResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_database_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_database_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetDatabaseDiskArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/disk
/// Get database disk attributes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_database_disk_builder()` + `v1_get_database_disk_execute()`.
/// For task-level control, use `v1_get_database_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_database_disk(
    client: &SimpleHttpClient,
    args: &V1GetDatabaseDiskArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DiskResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_database_disk_builder(client, &args.ref_rs)?;
    v1_get_database_disk_execute(builder)
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_modify_database_disk_execute()` to send, or `v1_modify_database_disk` for simplest API.

pub fn v1_modify_database_disk_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &DiskRequestBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_modify_database_disk_execute()` or `v1_modify_database_disk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_modify_database_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_modify_database_disk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_modify_database_disk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_modify_database_disk_task()`.
/// For the simplest API, use `v1_modify_database_disk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_modify_database_disk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_modify_database_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_modify_database_disk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_modify_database_disk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ModifyDatabaseDiskArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: DiskRequestBody,
}

/// POST /v1/projects/{ref}/config/disk
/// Modify database disk
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_modify_database_disk_builder()` + `v1_modify_database_disk_execute()`.
/// For task-level control, use `v1_modify_database_disk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_modify_database_disk(
    client: &SimpleHttpClient,
    args: &V1ModifyDatabaseDiskArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_modify_database_disk_builder(client, &args.ref_rs, &args.body)?;
    v1_modify_database_disk_execute(builder)
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_project_disk_autoscale_config_execute()` to send, or `v1_get_project_disk_autoscale_config` for simplest API.

pub fn v1_get_project_disk_autoscale_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk/autoscale",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_project_disk_autoscale_config_execute()` or `v1_get_project_disk_autoscale_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_disk_autoscale_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_disk_autoscale_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DiskAutoscaleConfig>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskAutoscaleConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_project_disk_autoscale_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_project_disk_autoscale_config_task()`.
/// For the simplest API, use `v1_get_project_disk_autoscale_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_project_disk_autoscale_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_project_disk_autoscale_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DiskAutoscaleConfig>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_project_disk_autoscale_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_project_disk_autoscale_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectDiskAutoscaleConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/disk/autoscale
/// Gets project disk autoscale config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_project_disk_autoscale_config_builder()` + `v1_get_project_disk_autoscale_config_execute()`.
/// For task-level control, use `v1_get_project_disk_autoscale_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_project_disk_autoscale_config(
    client: &SimpleHttpClient,
    args: &V1GetProjectDiskAutoscaleConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DiskAutoscaleConfig>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_project_disk_autoscale_config_builder(client, &args.ref_rs)?;
    v1_get_project_disk_autoscale_config_execute(builder)
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_disk_utilization_execute()` to send, or `v1_get_disk_utilization` for simplest API.

pub fn v1_get_disk_utilization_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/disk/util",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_disk_utilization_execute()` or `v1_get_disk_utilization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_disk_utilization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_disk_utilization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DiskUtilMetricsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskUtilMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_disk_utilization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_disk_utilization_task()`.
/// For the simplest API, use `v1_get_disk_utilization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_disk_utilization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_disk_utilization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DiskUtilMetricsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_disk_utilization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_disk_utilization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetDiskUtilizationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/disk/util
/// Get disk utilization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_disk_utilization_builder()` + `v1_get_disk_utilization_execute()`.
/// For task-level control, use `v1_get_disk_utilization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_disk_utilization(
    client: &SimpleHttpClient,
    args: &V1GetDiskUtilizationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DiskUtilMetricsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_disk_utilization_builder(client, &args.ref_rs)?;
    v1_get_disk_utilization_execute(builder)
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_realtime_config_execute()` to send, or `v1_get_realtime_config` for simplest API.

pub fn v1_get_realtime_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/realtime",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_realtime_config_execute()` or `v1_get_realtime_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_realtime_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_realtime_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<RealtimeConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RealtimeConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_realtime_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_realtime_config_task()`.
/// For the simplest API, use `v1_get_realtime_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_realtime_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_realtime_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<RealtimeConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_realtime_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_realtime_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetRealtimeConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/realtime
/// Gets realtime configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_realtime_config_builder()` + `v1_get_realtime_config_execute()`.
/// For task-level control, use `v1_get_realtime_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_realtime_config(
    client: &SimpleHttpClient,
    args: &V1GetRealtimeConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<RealtimeConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_realtime_config_builder(client, &args.ref_rs)?;
    v1_get_realtime_config_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_realtime_config_execute()` to send, or `v1_update_realtime_config` for simplest API.

pub fn v1_update_realtime_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdateRealtimeConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/realtime",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_realtime_config_execute()` or `v1_update_realtime_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_realtime_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_realtime_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_realtime_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_realtime_config_task()`.
/// For the simplest API, use `v1_update_realtime_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_realtime_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_realtime_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_realtime_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_realtime_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateRealtimeConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateRealtimeConfigBody,
}

/// PATCH /v1/projects/{ref}/config/realtime
/// Updates realtime configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_realtime_config_builder()` + `v1_update_realtime_config_execute()`.
/// For task-level control, use `v1_update_realtime_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_realtime_config(
    client: &SimpleHttpClient,
    args: &V1UpdateRealtimeConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_realtime_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_realtime_config_execute(builder)
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_shutdown_realtime_execute()` to send, or `v1_shutdown_realtime` for simplest API.

pub fn v1_shutdown_realtime_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/realtime/shutdown",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_shutdown_realtime_execute()` or `v1_shutdown_realtime`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_shutdown_realtime_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_shutdown_realtime_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_shutdown_realtime_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_shutdown_realtime_task()`.
/// For the simplest API, use `v1_shutdown_realtime()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_shutdown_realtime_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_shutdown_realtime_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_shutdown_realtime_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_shutdown_realtime`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ShutdownRealtimeArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/config/realtime/shutdown
/// Shutdowns realtime connections for a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_shutdown_realtime_builder()` + `v1_shutdown_realtime_execute()`.
/// For task-level control, use `v1_shutdown_realtime_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_shutdown_realtime(
    client: &SimpleHttpClient,
    args: &V1ShutdownRealtimeArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_shutdown_realtime_builder(client, &args.ref_rs)?;
    v1_shutdown_realtime_execute(builder)
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_storage_config_execute()` to send, or `v1_get_storage_config` for simplest API.

pub fn v1_get_storage_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/storage",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_storage_config_execute()` or `v1_get_storage_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_storage_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_storage_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<StorageConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorageConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_storage_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_storage_config_task()`.
/// For the simplest API, use `v1_get_storage_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_storage_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_storage_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<StorageConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_storage_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_storage_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetStorageConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/config/storage
/// Gets project's storage config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_storage_config_builder()` + `v1_get_storage_config_execute()`.
/// For task-level control, use `v1_get_storage_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_storage_config(
    client: &SimpleHttpClient,
    args: &V1GetStorageConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<StorageConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_storage_config_builder(client, &args.ref_rs)?;
    v1_get_storage_config_execute(builder)
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_storage_config_execute()` to send, or `v1_update_storage_config` for simplest API.

pub fn v1_update_storage_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdateStorageConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/storage",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_storage_config_execute()` or `v1_update_storage_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_storage_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_storage_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_storage_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_storage_config_task()`.
/// For the simplest API, use `v1_update_storage_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_storage_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_storage_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_storage_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_storage_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateStorageConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateStorageConfigBody,
}

/// PATCH /v1/projects/{ref}/config/storage
/// Updates project's storage config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_storage_config_builder()` + `v1_update_storage_config_execute()`.
/// For task-level control, use `v1_update_storage_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_storage_config(
    client: &SimpleHttpClient,
    args: &V1UpdateStorageConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_storage_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_storage_config_execute(builder)
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_hostname_config_execute()` to send, or `v1_get_hostname_config` for simplest API.

pub fn v1_get_hostname_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_hostname_config_execute()` or `v1_get_hostname_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_hostname_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_hostname_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_hostname_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_hostname_config_task()`.
/// For the simplest API, use `v1_get_hostname_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_hostname_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_hostname_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_hostname_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_hostname_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetHostnameConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/custom-hostname
/// [Beta] Gets project's custom hostname config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_hostname_config_builder()` + `v1_get_hostname_config_execute()`.
/// For task-level control, use `v1_get_hostname_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_hostname_config(
    client: &SimpleHttpClient,
    args: &V1GetHostnameConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_hostname_config_builder(client, &args.ref_rs)?;
    v1_get_hostname_config_execute(builder)
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete hostname config_execute()` to send, or `v1_delete hostname config` for simplest API.

pub fn v1_delete hostname config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete hostname config_execute()` or `v1_delete hostname config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete hostname config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete hostname config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete hostname config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete hostname config_task()`.
/// For the simplest API, use `v1_delete hostname config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete hostname config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete hostname config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete hostname config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete hostname config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1Delete hostname configArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/custom-hostname
/// [Beta] Deletes a project's custom hostname configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete hostname config_builder()` + `v1_delete hostname config_execute()`.
/// For task-level control, use `v1_delete hostname config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete hostname config(
    client: &SimpleHttpClient,
    args: &V1Delete hostname configArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete hostname config_builder(client, &args.ref_rs)?;
    v1_delete hostname config_execute(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_activate_custom_hostname_execute()` to send, or `v1_activate_custom_hostname` for simplest API.

pub fn v1_activate_custom_hostname_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname/activate",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_activate_custom_hostname_execute()` or `v1_activate_custom_hostname`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_activate_custom_hostname_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_activate_custom_hostname_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_activate_custom_hostname_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_activate_custom_hostname_task()`.
/// For the simplest API, use `v1_activate_custom_hostname()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_activate_custom_hostname_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_activate_custom_hostname_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_activate_custom_hostname_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_activate_custom_hostname`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ActivateCustomHostnameArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/custom-hostname/activate
/// [Beta] Activates a custom hostname for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_activate_custom_hostname_builder()` + `v1_activate_custom_hostname_execute()`.
/// For task-level control, use `v1_activate_custom_hostname_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_activate_custom_hostname(
    client: &SimpleHttpClient,
    args: &V1ActivateCustomHostnameArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_activate_custom_hostname_builder(client, &args.ref_rs)?;
    v1_activate_custom_hostname_execute(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_hostname_config_execute()` to send, or `v1_update_hostname_config` for simplest API.

pub fn v1_update_hostname_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdateCustomHostnameBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname/initialize",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_hostname_config_execute()` or `v1_update_hostname_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_hostname_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_hostname_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_hostname_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_hostname_config_task()`.
/// For the simplest API, use `v1_update_hostname_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_hostname_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_hostname_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_hostname_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_hostname_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateHostnameConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateCustomHostnameBody,
}

/// POST /v1/projects/{ref}/custom-hostname/initialize
/// [Beta] Updates project's custom hostname configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_hostname_config_builder()` + `v1_update_hostname_config_execute()`.
/// For task-level control, use `v1_update_hostname_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_hostname_config(
    client: &SimpleHttpClient,
    args: &V1UpdateHostnameConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_hostname_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_hostname_config_execute(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_verify_dns_config_execute()` to send, or `v1_verify_dns_config` for simplest API.

pub fn v1_verify_dns_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/custom-hostname/reverify",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_verify_dns_config_execute()` or `v1_verify_dns_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_verify_dns_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_verify_dns_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateCustomHostnameResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_verify_dns_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_verify_dns_config_task()`.
/// For the simplest API, use `v1_verify_dns_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_verify_dns_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_verify_dns_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_verify_dns_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_verify_dns_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1VerifyDnsConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/custom-hostname/reverify
/// [Beta] Attempts to verify the DNS configuration for project's custom hostname configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_verify_dns_config_builder()` + `v1_verify_dns_config_execute()`.
/// For task-level control, use `v1_verify_dns_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_verify_dns_config(
    client: &SimpleHttpClient,
    args: &V1VerifyDnsConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<UpdateCustomHostnameResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_verify_dns_config_builder(client, &args.ref_rs)?;
    v1_verify_dns_config_execute(builder)
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_backups_execute()` to send, or `v1_list_all_backups` for simplest API.

pub fn v1_list_all_backups_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_backups_execute()` or `v1_list_all_backups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_backups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1BackupsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1BackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_backups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_backups_task()`.
/// For the simplest API, use `v1_list_all_backups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1BackupsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_backups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllBackupsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/backups
/// Lists all backups
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_backups_builder()` + `v1_list_all_backups_execute()`.
/// For task-level control, use `v1_list_all_backups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_backups(
    client: &SimpleHttpClient,
    args: &V1ListAllBackupsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1BackupsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_backups_builder(client, &args.ref_rs)?;
    v1_list_all_backups_execute(builder)
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_restore_pitr_backup_execute()` to send, or `v1_restore_pitr_backup` for simplest API.

pub fn v1_restore_pitr_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1RestorePitrBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/restore-pitr",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_restore_pitr_backup_execute()` or `v1_restore_pitr_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_restore_pitr_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_restore_pitr_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_restore_pitr_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_restore_pitr_backup_task()`.
/// For the simplest API, use `v1_restore_pitr_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_restore_pitr_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_restore_pitr_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_restore_pitr_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_restore_pitr_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RestorePitrBackupArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1RestorePitrBody,
}

/// POST /v1/projects/{ref}/database/backups/restore-pitr
/// Restores a PITR backup for a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_restore_pitr_backup_builder()` + `v1_restore_pitr_backup_execute()`.
/// For task-level control, use `v1_restore_pitr_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_restore_pitr_backup(
    client: &SimpleHttpClient,
    args: &V1RestorePitrBackupArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_restore_pitr_backup_builder(client, &args.ref_rs, &args.body)?;
    v1_restore_pitr_backup_execute(builder)
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_restore_point_execute()` to send, or `v1_get_restore_point` for simplest API.

pub fn v1_get_restore_point_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    name: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/restore-point",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_restore_point_execute()` or `v1_get_restore_point`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_restore_point_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1RestorePointResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1RestorePointResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_restore_point_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_restore_point_task()`.
/// For the simplest API, use `v1_get_restore_point()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_restore_point_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1RestorePointResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_restore_point_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_restore_point`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetRestorePointArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: name
    pub name: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/database/backups/restore-point
/// Get restore points for project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_restore_point_builder()` + `v1_get_restore_point_execute()`.
/// For task-level control, use `v1_get_restore_point_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_restore_point(
    client: &SimpleHttpClient,
    args: &V1GetRestorePointArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1RestorePointResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_restore_point_builder(client, &args.ref_rs, &args.name)?;
    v1_get_restore_point_execute(builder)
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_restore_point_execute()` to send, or `v1_create_restore_point` for simplest API.

pub fn v1_create_restore_point_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1RestorePointPostBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/restore-point",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_restore_point_execute()` or `v1_create_restore_point`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_restore_point_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1RestorePointResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1RestorePointResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_restore_point_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_restore_point_task()`.
/// For the simplest API, use `v1_create_restore_point()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_restore_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_restore_point_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1RestorePointResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_restore_point_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_restore_point`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateRestorePointArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1RestorePointPostBody,
}

/// POST /v1/projects/{ref}/database/backups/restore-point
/// Initiates a creation of a restore point for a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_restore_point_builder()` + `v1_create_restore_point_execute()`.
/// For task-level control, use `v1_create_restore_point_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_restore_point(
    client: &SimpleHttpClient,
    args: &V1CreateRestorePointArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1RestorePointResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_restore_point_builder(client, &args.ref_rs, &args.body)?;
    v1_create_restore_point_execute(builder)
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_undo_execute()` to send, or `v1_undo` for simplest API.

pub fn v1_undo_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1UndoBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/backups/undo",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_undo_execute()` or `v1_undo`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_undo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_undo_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_undo_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_undo_task()`.
/// For the simplest API, use `v1_undo()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_undo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_undo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_undo_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_undo`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UndoArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UndoBody,
}

/// POST /v1/projects/{ref}/database/backups/undo
/// Initiates an undo to a given restore point
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_undo_builder()` + `v1_undo_execute()`.
/// For task-level control, use `v1_undo_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_undo(
    client: &SimpleHttpClient,
    args: &V1UndoArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_undo_builder(client, &args.ref_rs, &args.body)?;
    v1_undo_execute(builder)
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_database_metadata_execute()` to send, or `v1_get_database_metadata` for simplest API.

pub fn v1_get_database_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/context",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_database_metadata_execute()` or `v1_get_database_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_database_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_database_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<GetProjectDbMetadataResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetProjectDbMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_database_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_database_metadata_task()`.
/// For the simplest API, use `v1_get_database_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_database_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_database_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetProjectDbMetadataResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_database_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_database_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetDatabaseMetadataArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/context
/// Gets database metadata for the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_database_metadata_builder()` + `v1_get_database_metadata_execute()`.
/// For task-level control, use `v1_get_database_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_database_metadata(
    client: &SimpleHttpClient,
    args: &V1GetDatabaseMetadataArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetProjectDbMetadataResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_database_metadata_builder(client, &args.ref_rs)?;
    v1_get_database_metadata_execute(builder)
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_jit_access_execute()` to send, or `v1_get_jit_access` for simplest API.

pub fn v1_get_jit_access_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_jit_access_execute()` or `v1_get_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<JitAccessResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_jit_access_task()`.
/// For the simplest API, use `v1_get_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/jit
/// Get user-id to role mappings for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_jit_access_builder()` + `v1_get_jit_access_execute()`.
/// For task-level control, use `v1_get_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_jit_access(
    client: &SimpleHttpClient,
    args: &V1GetJitAccessArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_jit_access_builder(client, &args.ref_rs)?;
    v1_get_jit_access_execute(builder)
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_authorize_jit_access_execute()` to send, or `v1_authorize_jit_access` for simplest API.

pub fn v1_authorize_jit_access_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &AuthorizeJitAccessBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_authorize_jit_access_execute()` or `v1_authorize_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_authorize_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_authorize_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<JitAuthorizeAccessResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAuthorizeAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_authorize_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_authorize_jit_access_task()`.
/// For the simplest API, use `v1_authorize_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_authorize_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_authorize_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAuthorizeAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_authorize_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_authorize_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1AuthorizeJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: AuthorizeJitAccessBody,
}

/// POST /v1/projects/{ref}/database/jit
/// Authorize user-id to role mappings for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_authorize_jit_access_builder()` + `v1_authorize_jit_access_execute()`.
/// For task-level control, use `v1_authorize_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_authorize_jit_access(
    client: &SimpleHttpClient,
    args: &V1AuthorizeJitAccessArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAuthorizeAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_authorize_jit_access_builder(client, &args.ref_rs, &args.body)?;
    v1_authorize_jit_access_execute(builder)
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_jit_access_execute()` to send, or `v1_update_jit_access` for simplest API.

pub fn v1_update_jit_access_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdateJitAccessBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_jit_access_execute()` or `v1_update_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<JitAccessResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_jit_access_task()`.
/// For the simplest API, use `v1_update_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdateJitAccessBody,
}

/// PUT /v1/projects/{ref}/database/jit
/// Updates a user mapping for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_jit_access_builder()` + `v1_update_jit_access_execute()`.
/// For task-level control, use `v1_update_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_jit_access(
    client: &SimpleHttpClient,
    args: &V1UpdateJitAccessArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_jit_access_builder(client, &args.ref_rs, &args.body)?;
    v1_update_jit_access_execute(builder)
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_jit_access_execute()` to send, or `v1_list_jit_access` for simplest API.

pub fn v1_list_jit_access_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit/list",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_jit_access_execute()` or `v1_list_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<JitListAccessResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitListAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_jit_access_task()`.
/// For the simplest API, use `v1_list_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitListAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/jit/list
/// List all user-id to role mappings for JIT access
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_jit_access_builder()` + `v1_list_jit_access_execute()`.
/// For task-level control, use `v1_list_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_jit_access(
    client: &SimpleHttpClient,
    args: &V1ListJitAccessArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitListAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_jit_access_builder(client, &args.ref_rs)?;
    v1_list_jit_access_execute(builder)
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_jit_access_execute()` to send, or `v1_delete_jit_access` for simplest API.

pub fn v1_delete_jit_access_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    user_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/jit/{}",
        ref_rs,
        user_id,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_jit_access_execute()` or `v1_delete_jit_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_jit_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_jit_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_jit_access_task()`.
/// For the simplest API, use `v1_delete_jit_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_jit_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_jit_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_jit_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_jit_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteJitAccessArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: user_id
    pub user_id: String,
}

/// DELETE /v1/projects/{ref}/database/jit/{user_id}
/// Delete JIT access by user-id
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_jit_access_builder()` + `v1_delete_jit_access_execute()`.
/// For task-level control, use `v1_delete_jit_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_jit_access(
    client: &SimpleHttpClient,
    args: &V1DeleteJitAccessArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_jit_access_builder(client, &args.ref_rs, &args.user_id)?;
    v1_delete_jit_access_execute(builder)
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_migration_history_execute()` to send, or `v1_list_migration_history` for simplest API.

pub fn v1_list_migration_history_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_migration_history_execute()` or `v1_list_migration_history`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_migration_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_migration_history_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1ListMigrationsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1ListMigrationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_migration_history_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_migration_history_task()`.
/// For the simplest API, use `v1_list_migration_history()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_migration_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_migration_history_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ListMigrationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_migration_history_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_migration_history`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListMigrationHistoryArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/database/migrations
/// List applied migration versions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_migration_history_builder()` + `v1_list_migration_history_execute()`.
/// For task-level control, use `v1_list_migration_history_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_migration_history(
    client: &SimpleHttpClient,
    args: &V1ListMigrationHistoryArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1ListMigrationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_migration_history_builder(client, &args.ref_rs)?;
    v1_list_migration_history_execute(builder)
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_apply_a_migration_execute()` to send, or `v1_apply_a_migration` for simplest API.

pub fn v1_apply_a_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1CreateMigrationBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_apply_a_migration_execute()` or `v1_apply_a_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_apply_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_apply_a_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_apply_a_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_apply_a_migration_task()`.
/// For the simplest API, use `v1_apply_a_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_apply_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_apply_a_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_apply_a_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_apply_a_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ApplyAMigrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1CreateMigrationBody,
}

/// POST /v1/projects/{ref}/database/migrations
/// Apply a database migration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_apply_a_migration_builder()` + `v1_apply_a_migration_execute()`.
/// For task-level control, use `v1_apply_a_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_apply_a_migration(
    client: &SimpleHttpClient,
    args: &V1ApplyAMigrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_apply_a_migration_builder(client, &args.ref_rs, &args.body)?;
    v1_apply_a_migration_execute(builder)
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_upsert_a_migration_execute()` to send, or `v1_upsert_a_migration` for simplest API.

pub fn v1_upsert_a_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1UpsertMigrationBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_upsert_a_migration_execute()` or `v1_upsert_a_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_upsert_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_upsert_a_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_upsert_a_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_upsert_a_migration_task()`.
/// For the simplest API, use `v1_upsert_a_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_upsert_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_upsert_a_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_upsert_a_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_upsert_a_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpsertAMigrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpsertMigrationBody,
}

/// PUT /v1/projects/{ref}/database/migrations
/// Upsert a database migration without applying
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_upsert_a_migration_builder()` + `v1_upsert_a_migration_execute()`.
/// For task-level control, use `v1_upsert_a_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_upsert_a_migration(
    client: &SimpleHttpClient,
    args: &V1UpsertAMigrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_upsert_a_migration_builder(client, &args.ref_rs, &args.body)?;
    v1_upsert_a_migration_execute(builder)
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_rollback_migrations_execute()` to send, or `v1_rollback_migrations` for simplest API.

pub fn v1_rollback_migrations_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    gte: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gte.as_ref() {
        query_parts.push(format!("gte={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_rollback_migrations_execute()` or `v1_rollback_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_rollback_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_rollback_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_rollback_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_rollback_migrations_task()`.
/// For the simplest API, use `v1_rollback_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_rollback_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_rollback_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_rollback_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_rollback_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RollbackMigrationsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: gte
    pub gte: Option<Option<String>>,
}

/// DELETE /v1/projects/{ref}/database/migrations
/// Rollback database migrations and remove them from history table
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_rollback_migrations_builder()` + `v1_rollback_migrations_execute()`.
/// For task-level control, use `v1_rollback_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_rollback_migrations(
    client: &SimpleHttpClient,
    args: &V1RollbackMigrationsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_rollback_migrations_builder(client, &args.ref_rs, &args.gte)?;
    v1_rollback_migrations_execute(builder)
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_migration_execute()` to send, or `v1_get_a_migration` for simplest API.

pub fn v1_get_a_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    version: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations/{}",
        ref_rs,
        version,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_migration_execute()` or `v1_get_a_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1GetMigrationResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1GetMigrationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_migration_task()`.
/// For the simplest API, use `v1_get_a_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1GetMigrationResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAMigrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: version
    pub version: String,
}

/// GET /v1/projects/{ref}/database/migrations/{version}
/// Fetch an existing entry from migration history
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_migration_builder()` + `v1_get_a_migration_execute()`.
/// For task-level control, use `v1_get_a_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_migration(
    client: &SimpleHttpClient,
    args: &V1GetAMigrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1GetMigrationResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_migration_builder(client, &args.ref_rs, &args.version)?;
    v1_get_a_migration_execute(builder)
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_patch_a_migration_execute()` to send, or `v1_patch_a_migration` for simplest API.

pub fn v1_patch_a_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    version: &String,
    body: &V1PatchMigrationBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/migrations/{}",
        ref_rs,
        version,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_patch_a_migration_execute()` or `v1_patch_a_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_patch_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_patch_a_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_patch_a_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_patch_a_migration_task()`.
/// For the simplest API, use `v1_patch_a_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_patch_a_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_patch_a_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_patch_a_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_patch_a_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1PatchAMigrationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: version
    pub version: String,
    /// Request body.
    pub body: V1PatchMigrationBody,
}

/// PATCH /v1/projects/{ref}/database/migrations/{version}
/// Patch an existing entry in migration history
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_patch_a_migration_builder()` + `v1_patch_a_migration_execute()`.
/// For task-level control, use `v1_patch_a_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_patch_a_migration(
    client: &SimpleHttpClient,
    args: &V1PatchAMigrationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_patch_a_migration_builder(client, &args.ref_rs, &args.version, &args.body)?;
    v1_patch_a_migration_execute(builder)
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_database_openapi_execute()` to send, or `v1_get_database_openapi` for simplest API.

pub fn v1_get_database_openapi_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    schema: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/openapi",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = schema.as_ref() {
        query_parts.push(format!("schema={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_database_openapi_execute()` or `v1_get_database_openapi`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_database_openapi_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_database_openapi_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_database_openapi_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_database_openapi_task()`.
/// For the simplest API, use `v1_get_database_openapi()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_database_openapi_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_database_openapi_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_database_openapi_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_database_openapi`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetDatabaseOpenapiArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: schema
    pub schema: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/database/openapi
/// Get PostgREST OpenAPI spec
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_database_openapi_builder()` + `v1_get_database_openapi_execute()`.
/// For task-level control, use `v1_get_database_openapi_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_database_openapi(
    client: &SimpleHttpClient,
    args: &V1GetDatabaseOpenapiArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_database_openapi_builder(client, &args.ref_rs, &args.schema)?;
    v1_get_database_openapi_execute(builder)
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_database_password_execute()` to send, or `v1_update_database_password` for simplest API.

pub fn v1_update_database_password_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1UpdatePasswordBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/password",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_database_password_execute()` or `v1_update_database_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_database_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_database_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1UpdatePasswordResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1UpdatePasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_database_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_database_password_task()`.
/// For the simplest API, use `v1_update_database_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_database_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_database_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1UpdatePasswordResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_database_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_database_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateDatabasePasswordArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpdatePasswordBody,
}

/// PATCH /v1/projects/{ref}/database/password
/// Updates the database password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_database_password_builder()` + `v1_update_database_password_execute()`.
/// For task-level control, use `v1_update_database_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_database_password(
    client: &SimpleHttpClient,
    args: &V1UpdateDatabasePasswordArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1UpdatePasswordResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_database_password_builder(client, &args.ref_rs, &args.body)?;
    v1_update_database_password_execute(builder)
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_run_a_query_execute()` to send, or `v1_run_a_query` for simplest API.

pub fn v1_run_a_query_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1RunQueryBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/query",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_run_a_query_execute()` or `v1_run_a_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_run_a_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_run_a_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_run_a_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_run_a_query_task()`.
/// For the simplest API, use `v1_run_a_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_run_a_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_run_a_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_run_a_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_run_a_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RunAQueryArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1RunQueryBody,
}

/// POST /v1/projects/{ref}/database/query
/// [Beta] Run sql query
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_run_a_query_builder()` + `v1_run_a_query_execute()`.
/// For task-level control, use `v1_run_a_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_run_a_query(
    client: &SimpleHttpClient,
    args: &V1RunAQueryArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_run_a_query_builder(client, &args.ref_rs, &args.body)?;
    v1_run_a_query_execute(builder)
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_read_only_query_execute()` to send, or `v1_read_only_query` for simplest API.

pub fn v1_read_only_query_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1ReadOnlyQueryBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/query/read-only",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_read_only_query_execute()` or `v1_read_only_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_read_only_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_read_only_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_read_only_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_read_only_query_task()`.
/// For the simplest API, use `v1_read_only_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_read_only_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_read_only_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_read_only_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_read_only_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ReadOnlyQueryArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1ReadOnlyQueryBody,
}

/// POST /v1/projects/{ref}/database/query/read-only
/// [Beta] Run a sql query as supabase_read_only_user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_read_only_query_builder()` + `v1_read_only_query_execute()`.
/// For task-level control, use `v1_read_only_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_read_only_query(
    client: &SimpleHttpClient,
    args: &V1ReadOnlyQueryArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_read_only_query_builder(client, &args.ref_rs, &args.body)?;
    v1_read_only_query_execute(builder)
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_enable_database_webhook_execute()` to send, or `v1_enable_database_webhook` for simplest API.

pub fn v1_enable_database_webhook_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/database/webhooks/enable",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_enable_database_webhook_execute()` or `v1_enable_database_webhook`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_enable_database_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_enable_database_webhook_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_enable_database_webhook_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_enable_database_webhook_task()`.
/// For the simplest API, use `v1_enable_database_webhook()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_enable_database_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_enable_database_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_enable_database_webhook_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_enable_database_webhook`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1EnableDatabaseWebhookArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/database/webhooks/enable
/// [Beta] Enables Database Webhooks on the project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_enable_database_webhook_builder()` + `v1_enable_database_webhook_execute()`.
/// For task-level control, use `v1_enable_database_webhook_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_enable_database_webhook(
    client: &SimpleHttpClient,
    args: &V1EnableDatabaseWebhookArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_enable_database_webhook_builder(client, &args.ref_rs)?;
    v1_enable_database_webhook_execute(builder)
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_functions_execute()` to send, or `v1_list_all_functions` for simplest API.

pub fn v1_list_all_functions_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_functions_execute()` or `v1_list_all_functions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_functions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_functions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_functions_task()`.
/// For the simplest API, use `v1_list_all_functions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_functions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_functions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_functions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllFunctionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/functions
/// List all functions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_functions_builder()` + `v1_list_all_functions_execute()`.
/// For task-level control, use `v1_list_all_functions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_functions(
    client: &SimpleHttpClient,
    args: &V1ListAllFunctionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_functions_builder(client, &args.ref_rs)?;
    v1_list_all_functions_execute(builder)
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_create_a_function_execute()` to send, or `v1_create_a_function` for simplest API.

pub fn v1_create_a_function_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    slug: &Option<Option<String>>,
    name: &Option<Option<String>>,
    verify_jwt: &Option<Option<String>>,
    import_map: &Option<Option<String>>,
    entrypoint_path: &Option<Option<String>>,
    import_map_path: &Option<Option<String>>,
    ezbr_sha256: &Option<Option<String>>,
    body: &V1CreateFunctionBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = slug.as_ref() {
        query_parts.push(format!("slug={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = verify_jwt.as_ref() {
        query_parts.push(format!("verify_jwt={}", val));
    }
    if let Some(val) = import_map.as_ref() {
        query_parts.push(format!("import_map={}", val));
    }
    if let Some(val) = entrypoint_path.as_ref() {
        query_parts.push(format!("entrypoint_path={}", val));
    }
    if let Some(val) = import_map_path.as_ref() {
        query_parts.push(format!("import_map_path={}", val));
    }
    if let Some(val) = ezbr_sha256.as_ref() {
        query_parts.push(format!("ezbr_sha256={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_create_a_function_execute()` or `v1_create_a_function`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_function_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<FunctionResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_create_a_function_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_create_a_function_task()`.
/// For the simplest API, use `v1_create_a_function()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_create_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_create_a_function_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_create_a_function_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_create_a_function`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateAFunctionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: slug
    pub slug: Option<Option<String>>,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: verify_jwt
    pub verify_jwt: Option<Option<String>>,
    /// Query parameter: import_map
    pub import_map: Option<Option<String>>,
    /// Query parameter: entrypoint_path
    pub entrypoint_path: Option<Option<String>>,
    /// Query parameter: import_map_path
    pub import_map_path: Option<Option<String>>,
    /// Query parameter: ezbr_sha256
    pub ezbr_sha256: Option<Option<String>>,
    /// Request body.
    pub body: V1CreateFunctionBody,
}

/// POST /v1/projects/{ref}/functions
/// Create a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_create_a_function_builder()` + `v1_create_a_function_execute()`.
/// For task-level control, use `v1_create_a_function_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_create_a_function(
    client: &SimpleHttpClient,
    args: &V1CreateAFunctionArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_create_a_function_builder(client, &args.ref_rs, &args.slug, &args.name, &args.verify_jwt, &args.import_map, &args.entrypoint_path, &args.import_map_path, &args.ezbr_sha256, &args.body)?;
    v1_create_a_function_execute(builder)
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_bulk_update_functions_execute()` to send, or `v1_bulk_update_functions` for simplest API.

pub fn v1_bulk_update_functions_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &BulkUpdateFunctionBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_bulk_update_functions_execute()` or `v1_bulk_update_functions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_bulk_update_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_bulk_update_functions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<BulkUpdateFunctionResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkUpdateFunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_bulk_update_functions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_bulk_update_functions_task()`.
/// For the simplest API, use `v1_bulk_update_functions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_bulk_update_functions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_bulk_update_functions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BulkUpdateFunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_bulk_update_functions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_bulk_update_functions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1BulkUpdateFunctionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: BulkUpdateFunctionBody,
}

/// PUT /v1/projects/{ref}/functions
/// Bulk update functions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_bulk_update_functions_builder()` + `v1_bulk_update_functions_execute()`.
/// For task-level control, use `v1_bulk_update_functions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_bulk_update_functions(
    client: &SimpleHttpClient,
    args: &V1BulkUpdateFunctionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<BulkUpdateFunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_bulk_update_functions_builder(client, &args.ref_rs, &args.body)?;
    v1_bulk_update_functions_execute(builder)
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_deploy_a_function_execute()` to send, or `v1_deploy_a_function` for simplest API.

pub fn v1_deploy_a_function_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    slug: &Option<Option<String>>,
    bundleOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/deploy",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = slug.as_ref() {
        query_parts.push(format!("slug={}", val));
    }
    if let Some(val) = bundleOnly.as_ref() {
        query_parts.push(format!("bundleOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_deploy_a_function_execute()` or `v1_deploy_a_function`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_deploy_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_deploy_a_function_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DeployFunctionResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeployFunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_deploy_a_function_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_deploy_a_function_task()`.
/// For the simplest API, use `v1_deploy_a_function()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_deploy_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_deploy_a_function_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeployFunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_deploy_a_function_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_deploy_a_function`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeployAFunctionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: slug
    pub slug: Option<Option<String>>,
    /// Query parameter: bundleOnly
    pub bundleOnly: Option<Option<String>>,
}

/// POST /v1/projects/{ref}/functions/deploy
/// Deploy a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_deploy_a_function_builder()` + `v1_deploy_a_function_execute()`.
/// For task-level control, use `v1_deploy_a_function_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_deploy_a_function(
    client: &SimpleHttpClient,
    args: &V1DeployAFunctionArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DeployFunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_deploy_a_function_builder(client, &args.ref_rs, &args.slug, &args.bundleOnly)?;
    v1_deploy_a_function_execute(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_function_execute()` to send, or `v1_get_a_function` for simplest API.

pub fn v1_get_a_function_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    function_slug: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}",
        ref_rs,
        function_slug,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_function_execute()` or `v1_get_a_function`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_function_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<FunctionSlugResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FunctionSlugResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_function_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_function_task()`.
/// For the simplest API, use `v1_get_a_function()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_function_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FunctionSlugResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_function_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_function`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAFunctionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
}

/// GET /v1/projects/{ref}/functions/{function_slug}
/// Retrieve a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_function_builder()` + `v1_get_a_function_execute()`.
/// For task-level control, use `v1_get_a_function_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_function(
    client: &SimpleHttpClient,
    args: &V1GetAFunctionArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FunctionSlugResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_function_builder(client, &args.ref_rs, &args.function_slug)?;
    v1_get_a_function_execute(builder)
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_a_function_execute()` to send, or `v1_update_a_function` for simplest API.

pub fn v1_update_a_function_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    function_slug: &String,
    slug: &Option<Option<String>>,
    name: &Option<Option<String>>,
    verify_jwt: &Option<Option<String>>,
    import_map: &Option<Option<String>>,
    entrypoint_path: &Option<Option<String>>,
    import_map_path: &Option<Option<String>>,
    ezbr_sha256: &Option<Option<String>>,
    body: &V1UpdateFunctionBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}",
        ref_rs,
        function_slug,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = slug.as_ref() {
        query_parts.push(format!("slug={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = verify_jwt.as_ref() {
        query_parts.push(format!("verify_jwt={}", val));
    }
    if let Some(val) = import_map.as_ref() {
        query_parts.push(format!("import_map={}", val));
    }
    if let Some(val) = entrypoint_path.as_ref() {
        query_parts.push(format!("entrypoint_path={}", val));
    }
    if let Some(val) = import_map_path.as_ref() {
        query_parts.push(format!("import_map_path={}", val));
    }
    if let Some(val) = ezbr_sha256.as_ref() {
        query_parts.push(format!("ezbr_sha256={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_a_function_execute()` or `v1_update_a_function`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_function_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<FunctionResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FunctionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_a_function_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_a_function_task()`.
/// For the simplest API, use `v1_update_a_function()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_a_function_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_a_function_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_a_function`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateAFunctionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
    /// Query parameter: slug
    pub slug: Option<Option<String>>,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: verify_jwt
    pub verify_jwt: Option<Option<String>>,
    /// Query parameter: import_map
    pub import_map: Option<Option<String>>,
    /// Query parameter: entrypoint_path
    pub entrypoint_path: Option<Option<String>>,
    /// Query parameter: import_map_path
    pub import_map_path: Option<Option<String>>,
    /// Query parameter: ezbr_sha256
    pub ezbr_sha256: Option<Option<String>>,
    /// Request body.
    pub body: V1UpdateFunctionBody,
}

/// PATCH /v1/projects/{ref}/functions/{function_slug}
/// Update a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_a_function_builder()` + `v1_update_a_function_execute()`.
/// For task-level control, use `v1_update_a_function_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_a_function(
    client: &SimpleHttpClient,
    args: &V1UpdateAFunctionArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<FunctionResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_a_function_builder(client, &args.ref_rs, &args.function_slug, &args.slug, &args.name, &args.verify_jwt, &args.import_map, &args.entrypoint_path, &args.import_map_path, &args.ezbr_sha256, &args.body)?;
    v1_update_a_function_execute(builder)
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_a_function_execute()` to send, or `v1_delete_a_function` for simplest API.

pub fn v1_delete_a_function_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    function_slug: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}",
        ref_rs,
        function_slug,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_a_function_execute()` or `v1_delete_a_function`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_function_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_a_function_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_a_function_task()`.
/// For the simplest API, use `v1_delete_a_function()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_a_function_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_a_function_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_a_function_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_a_function`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteAFunctionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
}

/// DELETE /v1/projects/{ref}/functions/{function_slug}
/// Delete a function
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_a_function_builder()` + `v1_delete_a_function_execute()`.
/// For task-level control, use `v1_delete_a_function_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_a_function(
    client: &SimpleHttpClient,
    args: &V1DeleteAFunctionArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_a_function_builder(client, &args.ref_rs, &args.function_slug)?;
    v1_delete_a_function_execute(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_function_body_execute()` to send, or `v1_get_a_function_body` for simplest API.

pub fn v1_get_a_function_body_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    function_slug: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/functions/{}/body",
        ref_rs,
        function_slug,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_function_body_execute()` or `v1_get_a_function_body`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_function_body_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_function_body_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<StreamableFile>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StreamableFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_function_body_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_function_body_task()`.
/// For the simplest API, use `v1_get_a_function_body()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_function_body_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_function_body_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<StreamableFile>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_function_body_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_function_body`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAFunctionBodyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Path parameter: function_slug
    pub function_slug: String,
}

/// GET /v1/projects/{ref}/functions/{function_slug}/body
/// Retrieve a function body
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_function_body_builder()` + `v1_get_a_function_body_execute()`.
/// For task-level control, use `v1_get_a_function_body_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_function_body(
    client: &SimpleHttpClient,
    args: &V1GetAFunctionBodyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<StreamableFile>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_function_body_builder(client, &args.ref_rs, &args.function_slug)?;
    v1_get_a_function_body_execute(builder)
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_services_health_execute()` to send, or `v1_get_services_health` for simplest API.

pub fn v1_get_services_health_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    services: &Option<Option<String>>,
    timeout_ms: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/health",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = services.as_ref() {
        query_parts.push(format!("services={}", val));
    }
    if let Some(val) = timeout_ms.as_ref() {
        query_parts.push(format!("timeout_ms={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_services_health_execute()` or `v1_get_services_health`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_services_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_services_health_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_services_health_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_services_health_task()`.
/// For the simplest API, use `v1_get_services_health()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_services_health_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_services_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_services_health_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_services_health`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetServicesHealthArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: services
    pub services: Option<Option<String>>,
    /// Query parameter: timeout_ms
    pub timeout_ms: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/health
/// Gets project's service health status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_services_health_builder()` + `v1_get_services_health_execute()`.
/// For task-level control, use `v1_get_services_health_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_services_health(
    client: &SimpleHttpClient,
    args: &V1GetServicesHealthArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_services_health_builder(client, &args.ref_rs, &args.services, &args.timeout_ms)?;
    v1_get_services_health_execute(builder)
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_jit_access_config_execute()` to send, or `v1_get_jit_access_config` for simplest API.

pub fn v1_get_jit_access_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/jit-access",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_jit_access_config_execute()` or `v1_get_jit_access_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_jit_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_jit_access_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<JitAccessResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_jit_access_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_jit_access_config_task()`.
/// For the simplest API, use `v1_get_jit_access_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_jit_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_jit_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_jit_access_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_jit_access_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetJitAccessConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/jit-access
/// [Beta] Get project's just-in-time access configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_jit_access_config_builder()` + `v1_get_jit_access_config_execute()`.
/// For task-level control, use `v1_get_jit_access_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_jit_access_config(
    client: &SimpleHttpClient,
    args: &V1GetJitAccessConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_jit_access_config_builder(client, &args.ref_rs)?;
    v1_get_jit_access_config_execute(builder)
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_jit_access_config_execute()` to send, or `v1_update_jit_access_config` for simplest API.

pub fn v1_update_jit_access_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &JitAccessRequestRequest
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/jit-access",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_jit_access_config_execute()` or `v1_update_jit_access_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_jit_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_jit_access_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<JitAccessResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JitAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_jit_access_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_jit_access_config_task()`.
/// For the simplest API, use `v1_update_jit_access_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_jit_access_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_jit_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_jit_access_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_jit_access_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateJitAccessConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: JitAccessRequestRequest,
}

/// PUT /v1/projects/{ref}/jit-access
/// [Beta] Update project's just-in-time access configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_jit_access_config_builder()` + `v1_update_jit_access_config_execute()`.
/// For task-level control, use `v1_update_jit_access_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_jit_access_config(
    client: &SimpleHttpClient,
    args: &V1UpdateJitAccessConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<JitAccessResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_jit_access_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_jit_access_config_execute(builder)
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_delete_network_bans_execute()` to send, or `v1_delete_network_bans` for simplest API.

pub fn v1_delete_network_bans_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &RemoveNetworkBanRequest
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/network-bans",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_delete_network_bans_execute()` or `v1_delete_network_bans`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_network_bans_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_network_bans_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_delete_network_bans_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_delete_network_bans_task()`.
/// For the simplest API, use `v1_delete_network_bans()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_delete_network_bans_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_delete_network_bans_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_delete_network_bans_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_delete_network_bans`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteNetworkBansArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: RemoveNetworkBanRequest,
}

/// DELETE /v1/projects/{ref}/network-bans
/// [Beta] Remove network bans.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_delete_network_bans_builder()` + `v1_delete_network_bans_execute()`.
/// For task-level control, use `v1_delete_network_bans_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_delete_network_bans(
    client: &SimpleHttpClient,
    args: &V1DeleteNetworkBansArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_delete_network_bans_builder(client, &args.ref_rs, &args.body)?;
    v1_delete_network_bans_execute(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_network_bans_execute()` to send, or `v1_list_all_network_bans` for simplest API.

pub fn v1_list_all_network_bans_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/network-bans/retrieve",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_network_bans_execute()` or `v1_list_all_network_bans`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_network_bans_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_network_bans_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<NetworkBanResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkBanResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_network_bans_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_network_bans_task()`.
/// For the simplest API, use `v1_list_all_network_bans()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_network_bans_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_network_bans_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkBanResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_network_bans_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_network_bans`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllNetworkBansArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/network-bans/retrieve
/// [Beta] Gets project's network bans
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_network_bans_builder()` + `v1_list_all_network_bans_execute()`.
/// For task-level control, use `v1_list_all_network_bans_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_network_bans(
    client: &SimpleHttpClient,
    args: &V1ListAllNetworkBansArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkBanResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_network_bans_builder(client, &args.ref_rs)?;
    v1_list_all_network_bans_execute(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_network_bans_enriched_execute()` to send, or `v1_list_all_network_bans_enriched` for simplest API.

pub fn v1_list_all_network_bans_enriched_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/network-bans/retrieve/enriched",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_network_bans_enriched_execute()` or `v1_list_all_network_bans_enriched`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_network_bans_enriched_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_network_bans_enriched_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<NetworkBanResponseEnriched>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkBanResponseEnriched = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_network_bans_enriched_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_network_bans_enriched_task()`.
/// For the simplest API, use `v1_list_all_network_bans_enriched()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_network_bans_enriched_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_network_bans_enriched_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkBanResponseEnriched>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_network_bans_enriched_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_network_bans_enriched`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllNetworkBansEnrichedArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/network-bans/retrieve/enriched
/// [Beta] Gets project's network bans with additional information about which databases they affect
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_network_bans_enriched_builder()` + `v1_list_all_network_bans_enriched_execute()`.
/// For task-level control, use `v1_list_all_network_bans_enriched_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_network_bans_enriched(
    client: &SimpleHttpClient,
    args: &V1ListAllNetworkBansEnrichedArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkBanResponseEnriched>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_network_bans_enriched_builder(client, &args.ref_rs)?;
    v1_list_all_network_bans_enriched_execute(builder)
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_network_restrictions_execute()` to send, or `v1_get_network_restrictions` for simplest API.

pub fn v1_get_network_restrictions_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/network-restrictions",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_network_restrictions_execute()` or `v1_get_network_restrictions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_network_restrictions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkRestrictionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_network_restrictions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_network_restrictions_task()`.
/// For the simplest API, use `v1_get_network_restrictions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_network_restrictions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_network_restrictions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_network_restrictions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetNetworkRestrictionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/network-restrictions
/// [Beta] Gets project's network restrictions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_network_restrictions_builder()` + `v1_get_network_restrictions_execute()`.
/// For task-level control, use `v1_get_network_restrictions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_network_restrictions(
    client: &SimpleHttpClient,
    args: &V1GetNetworkRestrictionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_network_restrictions_builder(client, &args.ref_rs)?;
    v1_get_network_restrictions_execute(builder)
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_patch_network_restrictions_execute()` to send, or `v1_patch_network_restrictions` for simplest API.

pub fn v1_patch_network_restrictions_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &NetworkRestrictionsPatchRequest
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/network-restrictions",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_patch_network_restrictions_execute()` or `v1_patch_network_restrictions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_patch_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_patch_network_restrictions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<NetworkRestrictionsV2Response>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkRestrictionsV2Response = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_patch_network_restrictions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_patch_network_restrictions_task()`.
/// For the simplest API, use `v1_patch_network_restrictions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_patch_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_patch_network_restrictions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkRestrictionsV2Response>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_patch_network_restrictions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_patch_network_restrictions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1PatchNetworkRestrictionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: NetworkRestrictionsPatchRequest,
}

/// PATCH /v1/projects/{ref}/network-restrictions
/// [Alpha] Updates project's network restrictions by adding or removing CIDRs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_patch_network_restrictions_builder()` + `v1_patch_network_restrictions_execute()`.
/// For task-level control, use `v1_patch_network_restrictions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_patch_network_restrictions(
    client: &SimpleHttpClient,
    args: &V1PatchNetworkRestrictionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkRestrictionsV2Response>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_patch_network_restrictions_builder(client, &args.ref_rs, &args.body)?;
    v1_patch_network_restrictions_execute(builder)
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_network_restrictions_execute()` to send, or `v1_update_network_restrictions` for simplest API.

pub fn v1_update_network_restrictions_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &NetworkRestrictionsRequest
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/network-restrictions/apply",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_network_restrictions_execute()` or `v1_update_network_restrictions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_network_restrictions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkRestrictionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_network_restrictions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_network_restrictions_task()`.
/// For the simplest API, use `v1_update_network_restrictions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_network_restrictions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_network_restrictions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_network_restrictions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_network_restrictions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateNetworkRestrictionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: NetworkRestrictionsRequest,
}

/// POST /v1/projects/{ref}/network-restrictions/apply
/// [Beta] Updates project's network restrictions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_network_restrictions_builder()` + `v1_update_network_restrictions_execute()`.
/// For task-level control, use `v1_update_network_restrictions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_network_restrictions(
    client: &SimpleHttpClient,
    args: &V1UpdateNetworkRestrictionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<NetworkRestrictionsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_network_restrictions_builder(client, &args.ref_rs, &args.body)?;
    v1_update_network_restrictions_execute(builder)
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_pause_a_project_execute()` to send, or `v1_pause_a_project` for simplest API.

pub fn v1_pause_a_project_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/pause",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_pause_a_project_execute()` or `v1_pause_a_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_pause_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_pause_a_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_pause_a_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_pause_a_project_task()`.
/// For the simplest API, use `v1_pause_a_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_pause_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_pause_a_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_pause_a_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_pause_a_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1PauseAProjectArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/pause
/// Pauses the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_pause_a_project_builder()` + `v1_pause_a_project_execute()`.
/// For task-level control, use `v1_pause_a_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_pause_a_project(
    client: &SimpleHttpClient,
    args: &V1PauseAProjectArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_pause_a_project_builder(client, &args.ref_rs)?;
    v1_pause_a_project_execute(builder)
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_pgsodium_config_execute()` to send, or `v1_get_pgsodium_config` for simplest API.

pub fn v1_get_pgsodium_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/pgsodium",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_pgsodium_config_execute()` or `v1_get_pgsodium_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_pgsodium_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_pgsodium_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PgsodiumConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_pgsodium_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_pgsodium_config_task()`.
/// For the simplest API, use `v1_get_pgsodium_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_pgsodium_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_pgsodium_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_pgsodium_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_pgsodium_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPgsodiumConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/pgsodium
/// [Beta] Gets project's pgsodium config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_pgsodium_config_builder()` + `v1_get_pgsodium_config_execute()`.
/// For task-level control, use `v1_get_pgsodium_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_pgsodium_config(
    client: &SimpleHttpClient,
    args: &V1GetPgsodiumConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_pgsodium_config_builder(client, &args.ref_rs)?;
    v1_get_pgsodium_config_execute(builder)
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_pgsodium_config_execute()` to send, or `v1_update_pgsodium_config` for simplest API.

pub fn v1_update_pgsodium_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpdatePgsodiumConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/pgsodium",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_pgsodium_config_execute()` or `v1_update_pgsodium_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_pgsodium_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_pgsodium_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PgsodiumConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_pgsodium_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_pgsodium_config_task()`.
/// For the simplest API, use `v1_update_pgsodium_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_pgsodium_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_pgsodium_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_pgsodium_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_pgsodium_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdatePgsodiumConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpdatePgsodiumConfigBody,
}

/// PUT /v1/projects/{ref}/pgsodium
/// [Beta] Updates project's pgsodium config. Updating the root_key can cause all data encrypted with the older key to become inaccessible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_pgsodium_config_builder()` + `v1_update_pgsodium_config_execute()`.
/// For task-level control, use `v1_update_pgsodium_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_pgsodium_config(
    client: &SimpleHttpClient,
    args: &V1UpdatePgsodiumConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PgsodiumConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_pgsodium_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_pgsodium_config_execute(builder)
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_postgrest_service_config_execute()` to send, or `v1_get_postgrest_service_config` for simplest API.

pub fn v1_get_postgrest_service_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/postgrest",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_postgrest_service_config_execute()` or `v1_get_postgrest_service_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgrest_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgrest_service_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<PostgrestConfigWithJWTSecretResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostgrestConfigWithJWTSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_postgrest_service_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_postgrest_service_config_task()`.
/// For the simplest API, use `v1_get_postgrest_service_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgrest_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_postgrest_service_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PostgrestConfigWithJWTSecretResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_postgrest_service_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_postgrest_service_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPostgrestServiceConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/postgrest
/// Gets project's postgrest config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_postgrest_service_config_builder()` + `v1_get_postgrest_service_config_execute()`.
/// For task-level control, use `v1_get_postgrest_service_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgrest_service_config(
    client: &SimpleHttpClient,
    args: &V1GetPostgrestServiceConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<PostgrestConfigWithJWTSecretResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_postgrest_service_config_builder(client, &args.ref_rs)?;
    v1_get_postgrest_service_config_execute(builder)
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_postgrest_service_config_execute()` to send, or `v1_update_postgrest_service_config` for simplest API.

pub fn v1_update_postgrest_service_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &V1UpdatePostgrestConfigBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/postgrest",
        ref_rs,
    );

    // Build request
    let builder = client.patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_postgrest_service_config_execute()` or `v1_update_postgrest_service_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_postgrest_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_postgrest_service_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<V1PostgrestConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V1PostgrestConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_postgrest_service_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_postgrest_service_config_task()`.
/// For the simplest API, use `v1_update_postgrest_service_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_postgrest_service_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_postgrest_service_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1PostgrestConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_postgrest_service_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_postgrest_service_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdatePostgrestServiceConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: V1UpdatePostgrestConfigBody,
}

/// PATCH /v1/projects/{ref}/postgrest
/// Updates project's postgrest config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_postgrest_service_config_builder()` + `v1_update_postgrest_service_config_execute()`.
/// For task-level control, use `v1_update_postgrest_service_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_postgrest_service_config(
    client: &SimpleHttpClient,
    args: &V1UpdatePostgrestServiceConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<V1PostgrestConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_postgrest_service_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_postgrest_service_config_execute(builder)
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_remove_a_read_replica_execute()` to send, or `v1_remove_a_read_replica` for simplest API.

pub fn v1_remove_a_read_replica_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &RemoveReadReplicaBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/read-replicas/remove",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_remove_a_read_replica_execute()` or `v1_remove_a_read_replica`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_remove_a_read_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_remove_a_read_replica_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_remove_a_read_replica_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_remove_a_read_replica_task()`.
/// For the simplest API, use `v1_remove_a_read_replica()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_remove_a_read_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_remove_a_read_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_remove_a_read_replica_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_remove_a_read_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RemoveAReadReplicaArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: RemoveReadReplicaBody,
}

/// POST /v1/projects/{ref}/read-replicas/remove
/// [Beta] Remove a read replica
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_remove_a_read_replica_builder()` + `v1_remove_a_read_replica_execute()`.
/// For task-level control, use `v1_remove_a_read_replica_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_remove_a_read_replica(
    client: &SimpleHttpClient,
    args: &V1RemoveAReadReplicaArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_remove_a_read_replica_builder(client, &args.ref_rs, &args.body)?;
    v1_remove_a_read_replica_execute(builder)
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_setup_a_read_replica_execute()` to send, or `v1_setup_a_read_replica` for simplest API.

pub fn v1_setup_a_read_replica_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &SetUpReadReplicaBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/read-replicas/setup",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_setup_a_read_replica_execute()` or `v1_setup_a_read_replica`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_setup_a_read_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_setup_a_read_replica_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_setup_a_read_replica_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_setup_a_read_replica_task()`.
/// For the simplest API, use `v1_setup_a_read_replica()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_setup_a_read_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_setup_a_read_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_setup_a_read_replica_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_setup_a_read_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1SetupAReadReplicaArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: SetUpReadReplicaBody,
}

/// POST /v1/projects/{ref}/read-replicas/setup
/// [Beta] Set up a read replica
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_setup_a_read_replica_builder()` + `v1_setup_a_read_replica_execute()`.
/// For task-level control, use `v1_setup_a_read_replica_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_setup_a_read_replica(
    client: &SimpleHttpClient,
    args: &V1SetupAReadReplicaArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_setup_a_read_replica_builder(client, &args.ref_rs, &args.body)?;
    v1_setup_a_read_replica_execute(builder)
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_readonly_mode_status_execute()` to send, or `v1_get_readonly_mode_status` for simplest API.

pub fn v1_get_readonly_mode_status_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/readonly",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_readonly_mode_status_execute()` or `v1_get_readonly_mode_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_readonly_mode_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_readonly_mode_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ReadOnlyStatusResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReadOnlyStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_readonly_mode_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_readonly_mode_status_task()`.
/// For the simplest API, use `v1_get_readonly_mode_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_readonly_mode_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_readonly_mode_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ReadOnlyStatusResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_readonly_mode_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_readonly_mode_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetReadonlyModeStatusArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/readonly
/// Returns project's readonly mode status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_readonly_mode_status_builder()` + `v1_get_readonly_mode_status_execute()`.
/// For task-level control, use `v1_get_readonly_mode_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_readonly_mode_status(
    client: &SimpleHttpClient,
    args: &V1GetReadonlyModeStatusArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ReadOnlyStatusResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_readonly_mode_status_builder(client, &args.ref_rs)?;
    v1_get_readonly_mode_status_execute(builder)
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_disable_readonly_mode_temporarily_execute()` to send, or `v1_disable_readonly_mode_temporarily` for simplest API.

pub fn v1_disable_readonly_mode_temporarily_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/readonly/temporary-disable",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_disable_readonly_mode_temporarily_execute()` or `v1_disable_readonly_mode_temporarily`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_disable_readonly_mode_temporarily_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_disable_readonly_mode_temporarily_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_disable_readonly_mode_temporarily_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_disable_readonly_mode_temporarily_task()`.
/// For the simplest API, use `v1_disable_readonly_mode_temporarily()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_disable_readonly_mode_temporarily_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_disable_readonly_mode_temporarily_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_disable_readonly_mode_temporarily_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_disable_readonly_mode_temporarily`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DisableReadonlyModeTemporarilyArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/readonly/temporary-disable
/// Disables project's readonly mode for the next 15 minutes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_disable_readonly_mode_temporarily_builder()` + `v1_disable_readonly_mode_temporarily_execute()`.
/// For task-level control, use `v1_disable_readonly_mode_temporarily_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_disable_readonly_mode_temporarily(
    client: &SimpleHttpClient,
    args: &V1DisableReadonlyModeTemporarilyArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_disable_readonly_mode_temporarily_builder(client, &args.ref_rs)?;
    v1_disable_readonly_mode_temporarily_execute(builder)
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_available_restore_versions_execute()` to send, or `v1_list_available_restore_versions` for simplest API.

pub fn v1_list_available_restore_versions_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/restore",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_available_restore_versions_execute()` or `v1_list_available_restore_versions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_available_restore_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_available_restore_versions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<GetProjectAvailableRestoreVersionsResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetProjectAvailableRestoreVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_available_restore_versions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_available_restore_versions_task()`.
/// For the simplest API, use `v1_list_available_restore_versions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_available_restore_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_available_restore_versions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetProjectAvailableRestoreVersionsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_available_restore_versions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_available_restore_versions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAvailableRestoreVersionsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/restore
/// Lists available restore versions for the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_available_restore_versions_builder()` + `v1_list_available_restore_versions_execute()`.
/// For task-level control, use `v1_list_available_restore_versions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_available_restore_versions(
    client: &SimpleHttpClient,
    args: &V1ListAvailableRestoreVersionsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GetProjectAvailableRestoreVersionsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_available_restore_versions_builder(client, &args.ref_rs)?;
    v1_list_available_restore_versions_execute(builder)
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_restore_a_project_execute()` to send, or `v1_restore_a_project` for simplest API.

pub fn v1_restore_a_project_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/restore",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_restore_a_project_execute()` or `v1_restore_a_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_restore_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_restore_a_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_restore_a_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_restore_a_project_task()`.
/// For the simplest API, use `v1_restore_a_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_restore_a_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_restore_a_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_restore_a_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_restore_a_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RestoreAProjectArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/restore
/// Restores the given project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_restore_a_project_builder()` + `v1_restore_a_project_execute()`.
/// For task-level control, use `v1_restore_a_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_restore_a_project(
    client: &SimpleHttpClient,
    args: &V1RestoreAProjectArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_restore_a_project_builder(client, &args.ref_rs)?;
    v1_restore_a_project_execute(builder)
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_cancel_a_project_restoration_execute()` to send, or `v1_cancel_a_project_restoration` for simplest API.

pub fn v1_cancel_a_project_restoration_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/restore/cancel",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_cancel_a_project_restoration_execute()` or `v1_cancel_a_project_restoration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_cancel_a_project_restoration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_cancel_a_project_restoration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_cancel_a_project_restoration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_cancel_a_project_restoration_task()`.
/// For the simplest API, use `v1_cancel_a_project_restoration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_cancel_a_project_restoration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_cancel_a_project_restoration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_cancel_a_project_restoration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_cancel_a_project_restoration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CancelAProjectRestorationArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// POST /v1/projects/{ref}/restore/cancel
/// Cancels the given project restoration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_cancel_a_project_restoration_builder()` + `v1_cancel_a_project_restoration_execute()`.
/// For task-level control, use `v1_cancel_a_project_restoration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_cancel_a_project_restoration(
    client: &SimpleHttpClient,
    args: &V1CancelAProjectRestorationArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_cancel_a_project_restoration_builder(client, &args.ref_rs)?;
    v1_cancel_a_project_restoration_execute(builder)
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_secrets_execute()` to send, or `v1_list_all_secrets` for simplest API.

pub fn v1_list_all_secrets_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/secrets",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_secrets_execute()` or `v1_list_all_secrets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_secrets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_secrets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_secrets_task()`.
/// For the simplest API, use `v1_list_all_secrets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_secrets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllSecretsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/secrets
/// List all secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_secrets_builder()` + `v1_list_all_secrets_execute()`.
/// For task-level control, use `v1_list_all_secrets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_secrets(
    client: &SimpleHttpClient,
    args: &V1ListAllSecretsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_secrets_builder(client, &args.ref_rs)?;
    v1_list_all_secrets_execute(builder)
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_bulk_create_secrets_execute()` to send, or `v1_bulk_create_secrets` for simplest API.

pub fn v1_bulk_create_secrets_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &CreateSecretBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/secrets",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_bulk_create_secrets_execute()` or `v1_bulk_create_secrets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_bulk_create_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_bulk_create_secrets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_bulk_create_secrets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_bulk_create_secrets_task()`.
/// For the simplest API, use `v1_bulk_create_secrets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_bulk_create_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_bulk_create_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_bulk_create_secrets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_bulk_create_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1BulkCreateSecretsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: CreateSecretBody,
}

/// POST /v1/projects/{ref}/secrets
/// Bulk create secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_bulk_create_secrets_builder()` + `v1_bulk_create_secrets_execute()`.
/// For task-level control, use `v1_bulk_create_secrets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_bulk_create_secrets(
    client: &SimpleHttpClient,
    args: &V1BulkCreateSecretsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_bulk_create_secrets_builder(client, &args.ref_rs, &args.body)?;
    v1_bulk_create_secrets_execute(builder)
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_bulk_delete_secrets_execute()` to send, or `v1_bulk_delete_secrets` for simplest API.

pub fn v1_bulk_delete_secrets_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &DeleteSecretsBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/secrets",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_bulk_delete_secrets_execute()` or `v1_bulk_delete_secrets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_bulk_delete_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_bulk_delete_secrets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_bulk_delete_secrets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_bulk_delete_secrets_task()`.
/// For the simplest API, use `v1_bulk_delete_secrets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_bulk_delete_secrets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_bulk_delete_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_bulk_delete_secrets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_bulk_delete_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1BulkDeleteSecretsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: DeleteSecretsBody,
}

/// DELETE /v1/projects/{ref}/secrets
/// Bulk delete secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_bulk_delete_secrets_builder()` + `v1_bulk_delete_secrets_execute()`.
/// For task-level control, use `v1_bulk_delete_secrets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_bulk_delete_secrets(
    client: &SimpleHttpClient,
    args: &V1BulkDeleteSecretsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_bulk_delete_secrets_builder(client, &args.ref_rs, &args.body)?;
    v1_bulk_delete_secrets_execute(builder)
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_ssl_enforcement_config_execute()` to send, or `v1_get_ssl_enforcement_config` for simplest API.

pub fn v1_get_ssl_enforcement_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/ssl-enforcement",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_ssl_enforcement_config_execute()` or `v1_get_ssl_enforcement_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_ssl_enforcement_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_ssl_enforcement_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SslEnforcementResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslEnforcementResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_ssl_enforcement_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_ssl_enforcement_config_task()`.
/// For the simplest API, use `v1_get_ssl_enforcement_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_ssl_enforcement_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_ssl_enforcement_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SslEnforcementResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_ssl_enforcement_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_ssl_enforcement_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetSslEnforcementConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/ssl-enforcement
/// [Beta] Get project's SSL enforcement configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_ssl_enforcement_config_builder()` + `v1_get_ssl_enforcement_config_execute()`.
/// For task-level control, use `v1_get_ssl_enforcement_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_ssl_enforcement_config(
    client: &SimpleHttpClient,
    args: &V1GetSslEnforcementConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SslEnforcementResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_ssl_enforcement_config_builder(client, &args.ref_rs)?;
    v1_get_ssl_enforcement_config_execute(builder)
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_update_ssl_enforcement_config_execute()` to send, or `v1_update_ssl_enforcement_config` for simplest API.

pub fn v1_update_ssl_enforcement_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &SslEnforcementRequest
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/ssl-enforcement",
        ref_rs,
    );

    // Build request
    let builder = client.put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_update_ssl_enforcement_config_execute()` or `v1_update_ssl_enforcement_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_ssl_enforcement_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_ssl_enforcement_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SslEnforcementResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslEnforcementResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_update_ssl_enforcement_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_update_ssl_enforcement_config_task()`.
/// For the simplest API, use `v1_update_ssl_enforcement_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_update_ssl_enforcement_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_update_ssl_enforcement_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SslEnforcementResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_update_ssl_enforcement_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_update_ssl_enforcement_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateSslEnforcementConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: SslEnforcementRequest,
}

/// PUT /v1/projects/{ref}/ssl-enforcement
/// [Beta] Update project's SSL enforcement configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_update_ssl_enforcement_config_builder()` + `v1_update_ssl_enforcement_config_execute()`.
/// For task-level control, use `v1_update_ssl_enforcement_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_update_ssl_enforcement_config(
    client: &SimpleHttpClient,
    args: &V1UpdateSslEnforcementConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SslEnforcementResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_update_ssl_enforcement_config_builder(client, &args.ref_rs, &args.body)?;
    v1_update_ssl_enforcement_config_execute(builder)
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_buckets_execute()` to send, or `v1_list_all_buckets` for simplest API.

pub fn v1_list_all_buckets_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/storage/buckets",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_buckets_execute()` or `v1_list_all_buckets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_buckets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_buckets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<serde_json::Value>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_buckets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_buckets_task()`.
/// For the simplest API, use `v1_list_all_buckets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_buckets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_buckets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_buckets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_buckets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllBucketsArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/storage/buckets
/// Lists all buckets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_buckets_builder()` + `v1_list_all_buckets_execute()`.
/// For task-level control, use `v1_list_all_buckets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_buckets(
    client: &SimpleHttpClient,
    args: &V1ListAllBucketsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<serde_json::Value>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_buckets_builder(client, &args.ref_rs)?;
    v1_list_all_buckets_execute(builder)
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_generate_typescript_types_execute()` to send, or `v1_generate_typescript_types` for simplest API.

pub fn v1_generate_typescript_types_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    included_schemas: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/types/typescript",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = included_schemas.as_ref() {
        query_parts.push(format!("included_schemas={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_generate_typescript_types_execute()` or `v1_generate_typescript_types`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_generate_typescript_types_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_generate_typescript_types_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<TypescriptResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TypescriptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_generate_typescript_types_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_generate_typescript_types_task()`.
/// For the simplest API, use `v1_generate_typescript_types()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_generate_typescript_types_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_generate_typescript_types_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<TypescriptResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_generate_typescript_types_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_generate_typescript_types`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GenerateTypescriptTypesArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: included_schemas
    pub included_schemas: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/types/typescript
/// Generate TypeScript types
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_generate_typescript_types_builder()` + `v1_generate_typescript_types_execute()`.
/// For task-level control, use `v1_generate_typescript_types_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_generate_typescript_types(
    client: &SimpleHttpClient,
    args: &V1GenerateTypescriptTypesArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<TypescriptResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_generate_typescript_types_builder(client, &args.ref_rs, &args.included_schemas)?;
    v1_generate_typescript_types_execute(builder)
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_upgrade_postgres_version_execute()` to send, or `v1_upgrade_postgres_version` for simplest API.

pub fn v1_upgrade_postgres_version_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &UpgradeDatabaseBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/upgrade",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_upgrade_postgres_version_execute()` or `v1_upgrade_postgres_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_upgrade_postgres_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_upgrade_postgres_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ProjectUpgradeInitiateResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectUpgradeInitiateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_upgrade_postgres_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_upgrade_postgres_version_task()`.
/// For the simplest API, use `v1_upgrade_postgres_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_upgrade_postgres_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_upgrade_postgres_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ProjectUpgradeInitiateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_upgrade_postgres_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_upgrade_postgres_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpgradePostgresVersionArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: UpgradeDatabaseBody,
}

/// POST /v1/projects/{ref}/upgrade
/// [Beta] Upgrades the project's Postgres version
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_upgrade_postgres_version_builder()` + `v1_upgrade_postgres_version_execute()`.
/// For task-level control, use `v1_upgrade_postgres_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_upgrade_postgres_version(
    client: &SimpleHttpClient,
    args: &V1UpgradePostgresVersionArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ProjectUpgradeInitiateResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_upgrade_postgres_version_builder(client, &args.ref_rs, &args.body)?;
    v1_upgrade_postgres_version_execute(builder)
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_postgres_upgrade_eligibility_execute()` to send, or `v1_get_postgres_upgrade_eligibility` for simplest API.

pub fn v1_get_postgres_upgrade_eligibility_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/upgrade/eligibility",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_postgres_upgrade_eligibility_execute()` or `v1_get_postgres_upgrade_eligibility`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgres_upgrade_eligibility_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgres_upgrade_eligibility_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ProjectUpgradeEligibilityResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectUpgradeEligibilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_postgres_upgrade_eligibility_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_postgres_upgrade_eligibility_task()`.
/// For the simplest API, use `v1_get_postgres_upgrade_eligibility()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgres_upgrade_eligibility_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_postgres_upgrade_eligibility_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ProjectUpgradeEligibilityResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_postgres_upgrade_eligibility_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_postgres_upgrade_eligibility`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPostgresUpgradeEligibilityArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/upgrade/eligibility
/// [Beta] Returns the project's eligibility for upgrades
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_postgres_upgrade_eligibility_builder()` + `v1_get_postgres_upgrade_eligibility_execute()`.
/// For task-level control, use `v1_get_postgres_upgrade_eligibility_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgres_upgrade_eligibility(
    client: &SimpleHttpClient,
    args: &V1GetPostgresUpgradeEligibilityArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ProjectUpgradeEligibilityResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_postgres_upgrade_eligibility_builder(client, &args.ref_rs)?;
    v1_get_postgres_upgrade_eligibility_execute(builder)
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_postgres_upgrade_status_execute()` to send, or `v1_get_postgres_upgrade_status` for simplest API.

pub fn v1_get_postgres_upgrade_status_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    tracking_id: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/upgrade/status",
        ref_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tracking_id.as_ref() {
        query_parts.push(format!("tracking_id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_postgres_upgrade_status_execute()` or `v1_get_postgres_upgrade_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgres_upgrade_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgres_upgrade_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<DatabaseUpgradeStatusResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseUpgradeStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_postgres_upgrade_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_postgres_upgrade_status_task()`.
/// For the simplest API, use `v1_get_postgres_upgrade_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_postgres_upgrade_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_postgres_upgrade_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DatabaseUpgradeStatusResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_postgres_upgrade_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_postgres_upgrade_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetPostgresUpgradeStatusArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Query parameter: tracking_id
    pub tracking_id: Option<Option<String>>,
}

/// GET /v1/projects/{ref}/upgrade/status
/// [Beta] Gets the latest status of the project's upgrade
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_postgres_upgrade_status_builder()` + `v1_get_postgres_upgrade_status_execute()`.
/// For task-level control, use `v1_get_postgres_upgrade_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_postgres_upgrade_status(
    client: &SimpleHttpClient,
    args: &V1GetPostgresUpgradeStatusArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<DatabaseUpgradeStatusResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_postgres_upgrade_status_builder(client, &args.ref_rs, &args.tracking_id)?;
    v1_get_postgres_upgrade_status_execute(builder)
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_vanity_subdomain_config_execute()` to send, or `v1_get_vanity_subdomain_config` for simplest API.

pub fn v1_get_vanity_subdomain_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain",
        ref_rs,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_vanity_subdomain_config_execute()` or `v1_get_vanity_subdomain_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_vanity_subdomain_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_vanity_subdomain_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<VanitySubdomainConfigResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VanitySubdomainConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_vanity_subdomain_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_vanity_subdomain_config_task()`.
/// For the simplest API, use `v1_get_vanity_subdomain_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_vanity_subdomain_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_vanity_subdomain_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<VanitySubdomainConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_vanity_subdomain_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_vanity_subdomain_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetVanitySubdomainConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// GET /v1/projects/{ref}/vanity-subdomain
/// [Beta] Gets current vanity subdomain config
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_vanity_subdomain_config_builder()` + `v1_get_vanity_subdomain_config_execute()`.
/// For task-level control, use `v1_get_vanity_subdomain_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_vanity_subdomain_config(
    client: &SimpleHttpClient,
    args: &V1GetVanitySubdomainConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<VanitySubdomainConfigResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_vanity_subdomain_config_builder(client, &args.ref_rs)?;
    v1_get_vanity_subdomain_config_execute(builder)
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_deactivate_vanity_subdomain_config_execute()` to send, or `v1_deactivate_vanity_subdomain_config` for simplest API.

pub fn v1_deactivate_vanity_subdomain_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain",
        ref_rs,
    );

    // Build request
    let builder = client.delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_deactivate_vanity_subdomain_config_execute()` or `v1_deactivate_vanity_subdomain_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_deactivate_vanity_subdomain_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_deactivate_vanity_subdomain_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<()>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_deactivate_vanity_subdomain_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_deactivate_vanity_subdomain_config_task()`.
/// For the simplest API, use `v1_deactivate_vanity_subdomain_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_deactivate_vanity_subdomain_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_deactivate_vanity_subdomain_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_deactivate_vanity_subdomain_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_deactivate_vanity_subdomain_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeactivateVanitySubdomainConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
}

/// DELETE /v1/projects/{ref}/vanity-subdomain
/// [Beta] Deletes a project's vanity subdomain configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_deactivate_vanity_subdomain_config_builder()` + `v1_deactivate_vanity_subdomain_config_execute()`.
/// For task-level control, use `v1_deactivate_vanity_subdomain_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_deactivate_vanity_subdomain_config(
    client: &SimpleHttpClient,
    args: &V1DeactivateVanitySubdomainConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<()>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_deactivate_vanity_subdomain_config_builder(client, &args.ref_rs)?;
    v1_deactivate_vanity_subdomain_config_execute(builder)
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_activate_vanity_subdomain_config_execute()` to send, or `v1_activate_vanity_subdomain_config` for simplest API.

pub fn v1_activate_vanity_subdomain_config_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &VanitySubdomainBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain/activate",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_activate_vanity_subdomain_config_execute()` or `v1_activate_vanity_subdomain_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_activate_vanity_subdomain_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_activate_vanity_subdomain_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<ActivateVanitySubdomainResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ActivateVanitySubdomainResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_activate_vanity_subdomain_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_activate_vanity_subdomain_config_task()`.
/// For the simplest API, use `v1_activate_vanity_subdomain_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_activate_vanity_subdomain_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_activate_vanity_subdomain_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ActivateVanitySubdomainResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_activate_vanity_subdomain_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_activate_vanity_subdomain_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ActivateVanitySubdomainConfigArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: VanitySubdomainBody,
}

/// POST /v1/projects/{ref}/vanity-subdomain/activate
/// [Beta] Activates a vanity subdomain for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_activate_vanity_subdomain_config_builder()` + `v1_activate_vanity_subdomain_config_execute()`.
/// For task-level control, use `v1_activate_vanity_subdomain_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_activate_vanity_subdomain_config(
    client: &SimpleHttpClient,
    args: &V1ActivateVanitySubdomainConfigArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<ActivateVanitySubdomainResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_activate_vanity_subdomain_config_builder(client, &args.ref_rs, &args.body)?;
    v1_activate_vanity_subdomain_config_execute(builder)
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_check_vanity_subdomain_availability_execute()` to send, or `v1_check_vanity_subdomain_availability` for simplest API.

pub fn v1_check_vanity_subdomain_availability_builder<R>(
    client: &SimpleHttpClient<R>,
    ref_rs: &String,
    body: &VanitySubdomainBody
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/vanity-subdomain/check-availability",
        ref_rs,
    );

    // Build request
    let builder = client.post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder.body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_check_vanity_subdomain_availability_execute()` or `v1_check_vanity_subdomain_availability`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_check_vanity_subdomain_availability_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_check_vanity_subdomain_availability_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SubdomainAvailabilityResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubdomainAvailabilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_check_vanity_subdomain_availability_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_check_vanity_subdomain_availability_task()`.
/// For the simplest API, use `v1_check_vanity_subdomain_availability()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_check_vanity_subdomain_availability_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_check_vanity_subdomain_availability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SubdomainAvailabilityResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_check_vanity_subdomain_availability_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_check_vanity_subdomain_availability`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CheckVanitySubdomainAvailabilityArgs {
    /// Path parameter: ref
    pub ref_rs: String,
    /// Request body.
    pub body: VanitySubdomainBody,
}

/// POST /v1/projects/{ref}/vanity-subdomain/check-availability
/// [Beta] Checks vanity subdomain availability
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_check_vanity_subdomain_availability_builder()` + `v1_check_vanity_subdomain_availability_execute()`.
/// For task-level control, use `v1_check_vanity_subdomain_availability_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_check_vanity_subdomain_availability(
    client: &SimpleHttpClient,
    args: &V1CheckVanitySubdomainAvailabilityArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SubdomainAvailabilityResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_check_vanity_subdomain_availability_builder(client, &args.ref_rs, &args.body)?;
    v1_check_vanity_subdomain_availability_execute(builder)
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_list_all_snippets_execute()` to send, or `v1_list_all_snippets` for simplest API.

pub fn v1_list_all_snippets_builder<R>(
    client: &SimpleHttpClient<R>,
    project_ref: &Option<Option<String>>,
    cursor: &Option<Option<String>>,
    limit: &Option<Option<String>>,
    sort_by: &Option<Option<String>>,
    sort_order: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/snippets",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = project_ref.as_ref() {
        query_parts.push(format!("project_ref={}", val));
    }
    if let Some(val) = cursor.as_ref() {
        query_parts.push(format!("cursor={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = sort_by.as_ref() {
        query_parts.push(format!("sort_by={}", val));
    }
    if let Some(val) = sort_order.as_ref() {
        query_parts.push(format!("sort_order={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client.get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_list_all_snippets_execute()` or `v1_list_all_snippets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_snippets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_snippets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SnippetList>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnippetList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_list_all_snippets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_list_all_snippets_task()`.
/// For the simplest API, use `v1_list_all_snippets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_list_all_snippets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_list_all_snippets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SnippetList>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_list_all_snippets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_list_all_snippets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllSnippetsArgs {
    /// Query parameter: project_ref
    pub project_ref: Option<Option<String>>,
    /// Query parameter: cursor
    pub cursor: Option<Option<String>>,
    /// Query parameter: limit
    pub limit: Option<Option<String>>,
    /// Query parameter: sort_by
    pub sort_by: Option<Option<String>>,
    /// Query parameter: sort_order
    pub sort_order: Option<Option<String>>,
}

/// GET /v1/snippets
/// Lists SQL snippets for the logged in user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_list_all_snippets_builder()` + `v1_list_all_snippets_execute()`.
/// For task-level control, use `v1_list_all_snippets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_list_all_snippets(
    client: &SimpleHttpClient,
    args: &V1ListAllSnippetsArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SnippetList>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_list_all_snippets_builder(client, &args.project_ref, &args.cursor, &args.limit, &args.sort_by, &args.sort_order)?;
    v1_list_all_snippets_execute(builder)
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `v1_get_a_snippet_execute()` to send, or `v1_get_a_snippet` for simplest API.

pub fn v1_get_a_snippet_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{

    // Build URL
    let endpoint_url = format!(
        "https://api.supabase.com/v1/snippets/{}",
        id,
    );

    // Build request
    let builder = client.get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `v1_get_a_snippet_execute()` or `v1_get_a_snippet`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_snippet_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_snippet_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<SnippetResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
> {

    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnippetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `v1_get_a_snippet_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `v1_get_a_snippet_task()`.
/// For the simplest API, use `v1_get_a_snippet()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `v1_get_a_snippet_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn v1_get_a_snippet_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SnippetResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let task = v1_get_a_snippet_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`v1_get_a_snippet`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetASnippetArgs {
    /// Path parameter: id
    pub id: String,
}

/// GET /v1/snippets/{id}
/// Gets a specific SQL snippet
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `v1_get_a_snippet_builder()` + `v1_get_a_snippet_execute()`.
/// For task-level control, use `v1_get_a_snippet_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn v1_get_a_snippet(
    client: &SimpleHttpClient,
    args: &V1GetASnippetArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<SnippetResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = v1_get_a_snippet_builder(client, &args.id)?;
    v1_get_a_snippet_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BranchDetailResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchDetailResponse with V1GetABranchConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetABranchConfigArgs> for BranchDetailResponse {
    fn generate_resource_id(&self, input: &V1GetABranchConfigArgs) -> String {
        format!("supabase::BranchDetailResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchDetailResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchResponse with V1UpdateABranchConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateABranchConfigArgs> for BranchResponse {
    fn generate_resource_id(&self, input: &V1UpdateABranchConfigArgs) -> String {
        format!("supabase::BranchResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchDeleteResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchDeleteResponse with V1DeleteABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteABranchArgs> for BranchDeleteResponse {
    fn generate_resource_id(&self, input: &V1DeleteABranchArgs) -> String {
        format!("supabase::BranchDeleteResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchDeleteResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchUpdateResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchUpdateResponse with V1MergeABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1MergeABranchArgs> for BranchUpdateResponse {
    fn generate_resource_id(&self, input: &V1MergeABranchArgs) -> String {
        format!("supabase::BranchUpdateResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchUpdateResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchUpdateResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchUpdateResponse with V1PushABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1PushABranchArgs> for BranchUpdateResponse {
    fn generate_resource_id(&self, input: &V1PushABranchArgs) -> String {
        format!("supabase::BranchUpdateResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchUpdateResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchUpdateResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchUpdateResponse with V1ResetABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ResetABranchArgs> for BranchUpdateResponse {
    fn generate_resource_id(&self, input: &V1ResetABranchArgs) -> String {
        format!("supabase::BranchUpdateResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchUpdateResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchRestoreResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchRestoreResponse with V1RestoreABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1RestoreABranchArgs> for BranchRestoreResponse {
    fn generate_resource_id(&self, input: &V1RestoreABranchArgs) -> String {
        format!("supabase::BranchRestoreResponse/{}", input.branch_id_or_ref)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchRestoreResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1AuthorizeUserArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1AuthorizeUserArgs> for () {
    fn generate_resource_id(&self, input: &V1AuthorizeUserArgs) -> String {
        "supabase::()".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1OauthAuthorizeProjectClaimArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1OauthAuthorizeProjectClaimArgs> for () {
    fn generate_resource_id(&self, input: &V1OauthAuthorizeProjectClaimArgs) -> String {
        "supabase::()".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1RevokeTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1RevokeTokenArgs> for () {
    fn generate_resource_id(&self, input: &V1RevokeTokenArgs) -> String {
        "supabase::()".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OAuthTokenResponse
// =============================================================================

/// ResourceIdentifier implementation for OAuthTokenResponse with V1ExchangeOauthTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ExchangeOauthTokenArgs> for OAuthTokenResponse {
    fn generate_resource_id(&self, input: &V1ExchangeOauthTokenArgs) -> String {
        "supabase::OAuthTokenResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::OAuthTokenResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListAllOrganizationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllOrganizationsArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListAllOrganizationsArgs) -> String {
        "supabase::serde_json::Value".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OrganizationResponseV1
// =============================================================================

/// ResourceIdentifier implementation for OrganizationResponseV1 with V1CreateAnOrganizationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateAnOrganizationArgs> for OrganizationResponseV1 {
    fn generate_resource_id(&self, input: &V1CreateAnOrganizationArgs) -> String {
        "supabase::OrganizationResponseV1".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::OrganizationResponseV1"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1OrganizationSlugResponse
// =============================================================================

/// ResourceIdentifier implementation for V1OrganizationSlugResponse with V1GetAnOrganizationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAnOrganizationArgs> for V1OrganizationSlugResponse {
    fn generate_resource_id(&self, input: &V1GetAnOrganizationArgs) -> String {
        format!("supabase::V1OrganizationSlugResponse/{}", input.slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1OrganizationSlugResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ListEntitlementsResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ListEntitlementsResponse with V1GetOrganizationEntitlementsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetOrganizationEntitlementsArgs> for V1ListEntitlementsResponse {
    fn generate_resource_id(&self, input: &V1GetOrganizationEntitlementsArgs) -> String {
        format!("supabase::V1ListEntitlementsResponse/{}", input.slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ListEntitlementsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListOrganizationMembersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListOrganizationMembersArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListOrganizationMembersArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OrganizationProjectClaimResponse
// =============================================================================

/// ResourceIdentifier implementation for OrganizationProjectClaimResponse with V1GetOrganizationProjectClaimArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetOrganizationProjectClaimArgs> for OrganizationProjectClaimResponse {
    fn generate_resource_id(&self, input: &V1GetOrganizationProjectClaimArgs) -> String {
        format!("supabase::OrganizationProjectClaimResponse/{}/{}", input.slug, input.token)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::OrganizationProjectClaimResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1ClaimProjectForOrganizationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ClaimProjectForOrganizationArgs> for () {
    fn generate_resource_id(&self, input: &V1ClaimProjectForOrganizationArgs) -> String {
        format!("supabase::()/{}/{}", input.slug, input.token)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OrganizationProjectsResponse
// =============================================================================

/// ResourceIdentifier implementation for OrganizationProjectsResponse with V1GetAllProjectsForOrganizationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAllProjectsForOrganizationArgs> for OrganizationProjectsResponse {
    fn generate_resource_id(&self, input: &V1GetAllProjectsForOrganizationArgs) -> String {
        format!("supabase::OrganizationProjectsResponse/{}", input.slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::OrganizationProjectsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProfileResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProfileResponse with V1GetProfileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProfileArgs> for V1ProfileResponse {
    fn generate_resource_id(&self, input: &V1GetProfileArgs) -> String {
        "supabase::V1ProfileResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProfileResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListAllProjectsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllProjectsArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListAllProjectsArgs) -> String {
        "supabase::serde_json::Value".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProjectResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProjectResponse with V1CreateAProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateAProjectArgs> for V1ProjectResponse {
    fn generate_resource_id(&self, input: &V1CreateAProjectArgs) -> String {
        "supabase::V1ProjectResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProjectResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionsInfo
// =============================================================================

/// ResourceIdentifier implementation for RegionsInfo with V1GetAvailableRegionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAvailableRegionsArgs> for RegionsInfo {
    fn generate_resource_id(&self, input: &V1GetAvailableRegionsArgs) -> String {
        "supabase::RegionsInfo".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::RegionsInfo"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProjectWithDatabaseResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProjectWithDatabaseResponse with V1GetProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectArgs> for V1ProjectWithDatabaseResponse {
    fn generate_resource_id(&self, input: &V1GetProjectArgs) -> String {
        format!("supabase::V1ProjectWithDatabaseResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProjectWithDatabaseResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProjectRefResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProjectRefResponse with V1UpdateAProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateAProjectArgs> for V1ProjectRefResponse {
    fn generate_resource_id(&self, input: &V1UpdateAProjectArgs) -> String {
        format!("supabase::V1ProjectRefResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProjectRefResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProjectRefResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProjectRefResponse with V1DeleteAProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteAProjectArgs> for V1ProjectRefResponse {
    fn generate_resource_id(&self, input: &V1DeleteAProjectArgs) -> String {
        format!("supabase::V1ProjectRefResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProjectRefResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListActionRunResponse
// =============================================================================

/// ResourceIdentifier implementation for ListActionRunResponse with V1ListActionRunsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListActionRunsArgs> for ListActionRunResponse {
    fn generate_resource_id(&self, input: &V1ListActionRunsArgs) -> String {
        format!("supabase::ListActionRunResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ListActionRunResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ActionRunResponse
// =============================================================================

/// ResourceIdentifier implementation for ActionRunResponse with V1GetActionRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetActionRunArgs> for ActionRunResponse {
    fn generate_resource_id(&self, input: &V1GetActionRunArgs) -> String {
        format!("supabase::ActionRunResponse/{}/{}", input.ref_rs, input.run_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ActionRunResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateRunStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateRunStatusResponse with V1UpdateActionRunStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateActionRunStatusArgs> for UpdateRunStatusResponse {
    fn generate_resource_id(&self, input: &V1UpdateActionRunStatusArgs) -> String {
        format!("supabase::UpdateRunStatusResponse/{}/{}", input.ref_rs, input.run_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateRunStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProjectAdvisorsResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProjectAdvisorsResponse with V1GetPerformanceAdvisorsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPerformanceAdvisorsArgs> for V1ProjectAdvisorsResponse {
    fn generate_resource_id(&self, input: &V1GetPerformanceAdvisorsArgs) -> String {
        format!("supabase::V1ProjectAdvisorsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProjectAdvisorsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ProjectAdvisorsResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ProjectAdvisorsResponse with V1GetSecurityAdvisorsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetSecurityAdvisorsArgs> for V1ProjectAdvisorsResponse {
    fn generate_resource_id(&self, input: &V1GetSecurityAdvisorsArgs) -> String {
        format!("supabase::V1ProjectAdvisorsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ProjectAdvisorsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnalyticsResponse
// =============================================================================

/// ResourceIdentifier implementation for AnalyticsResponse with V1GetProjectFunctionCombinedStatsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectFunctionCombinedStatsArgs> for AnalyticsResponse {
    fn generate_resource_id(&self, input: &V1GetProjectFunctionCombinedStatsArgs) -> String {
        format!("supabase::AnalyticsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::AnalyticsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnalyticsResponse
// =============================================================================

/// ResourceIdentifier implementation for AnalyticsResponse with V1GetProjectLogsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectLogsArgs> for AnalyticsResponse {
    fn generate_resource_id(&self, input: &V1GetProjectLogsArgs) -> String {
        format!("supabase::AnalyticsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::AnalyticsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1GetUsageApiCountResponse
// =============================================================================

/// ResourceIdentifier implementation for V1GetUsageApiCountResponse with V1GetProjectUsageApiCountArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectUsageApiCountArgs> for V1GetUsageApiCountResponse {
    fn generate_resource_id(&self, input: &V1GetProjectUsageApiCountArgs) -> String {
        format!("supabase::V1GetUsageApiCountResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1GetUsageApiCountResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1GetUsageApiRequestsCountResponse
// =============================================================================

/// ResourceIdentifier implementation for V1GetUsageApiRequestsCountResponse with V1GetProjectUsageRequestCountArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectUsageRequestCountArgs> for V1GetUsageApiRequestsCountResponse {
    fn generate_resource_id(&self, input: &V1GetProjectUsageRequestCountArgs) -> String {
        format!("supabase::V1GetUsageApiRequestsCountResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1GetUsageApiRequestsCountResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1GetProjectApiKeysArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectApiKeysArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1GetProjectApiKeysArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for ApiKeyResponse with V1CreateProjectApiKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateProjectApiKeyArgs> for ApiKeyResponse {
    fn generate_resource_id(&self, input: &V1CreateProjectApiKeyArgs) -> String {
        format!("supabase::ApiKeyResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ApiKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LegacyApiKeysResponse
// =============================================================================

/// ResourceIdentifier implementation for LegacyApiKeysResponse with V1GetProjectLegacyApiKeysArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectLegacyApiKeysArgs> for LegacyApiKeysResponse {
    fn generate_resource_id(&self, input: &V1GetProjectLegacyApiKeysArgs) -> String {
        format!("supabase::LegacyApiKeysResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::LegacyApiKeysResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LegacyApiKeysResponse
// =============================================================================

/// ResourceIdentifier implementation for LegacyApiKeysResponse with V1UpdateProjectLegacyApiKeysArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateProjectLegacyApiKeysArgs> for LegacyApiKeysResponse {
    fn generate_resource_id(&self, input: &V1UpdateProjectLegacyApiKeysArgs) -> String {
        format!("supabase::LegacyApiKeysResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::LegacyApiKeysResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for ApiKeyResponse with V1GetProjectApiKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectApiKeyArgs> for ApiKeyResponse {
    fn generate_resource_id(&self, input: &V1GetProjectApiKeyArgs) -> String {
        format!("supabase::ApiKeyResponse/{}/{}", input.ref_rs, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ApiKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for ApiKeyResponse with V1UpdateProjectApiKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateProjectApiKeyArgs> for ApiKeyResponse {
    fn generate_resource_id(&self, input: &V1UpdateProjectApiKeyArgs) -> String {
        format!("supabase::ApiKeyResponse/{}/{}", input.ref_rs, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ApiKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for ApiKeyResponse with V1DeleteProjectApiKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteProjectApiKeyArgs> for ApiKeyResponse {
    fn generate_resource_id(&self, input: &V1DeleteProjectApiKeyArgs) -> String {
        format!("supabase::ApiKeyResponse/{}/{}", input.ref_rs, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ApiKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListProjectAddonsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListProjectAddonsResponse with V1ListProjectAddonsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListProjectAddonsArgs> for ListProjectAddonsResponse {
    fn generate_resource_id(&self, input: &V1ListProjectAddonsArgs) -> String {
        format!("supabase::ListProjectAddonsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ListProjectAddonsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListAllBranchesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllBranchesArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListAllBranchesArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchResponse with V1CreateABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateABranchArgs> for BranchResponse {
    fn generate_resource_id(&self, input: &V1CreateABranchArgs) -> String {
        format!("supabase::BranchResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BranchResponse
// =============================================================================

/// ResourceIdentifier implementation for BranchResponse with V1GetABranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetABranchArgs> for BranchResponse {
    fn generate_resource_id(&self, input: &V1GetABranchArgs) -> String {
        format!("supabase::BranchResponse/{}/{}", input.ref_rs, input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BranchResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProjectClaimTokenResponse
// =============================================================================

/// ResourceIdentifier implementation for ProjectClaimTokenResponse with V1GetProjectClaimTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectClaimTokenArgs> for ProjectClaimTokenResponse {
    fn generate_resource_id(&self, input: &V1GetProjectClaimTokenArgs) -> String {
        format!("supabase::ProjectClaimTokenResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ProjectClaimTokenResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreateProjectClaimTokenResponse
// =============================================================================

/// ResourceIdentifier implementation for CreateProjectClaimTokenResponse with V1CreateProjectClaimTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateProjectClaimTokenArgs> for CreateProjectClaimTokenResponse {
    fn generate_resource_id(&self, input: &V1CreateProjectClaimTokenArgs) -> String {
        format!("supabase::CreateProjectClaimTokenResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::CreateProjectClaimTokenResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1DeleteProjectClaimTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteProjectClaimTokenArgs> for () {
    fn generate_resource_id(&self, input: &V1DeleteProjectClaimTokenArgs) -> String {
        format!("supabase::()/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreateRoleResponse
// =============================================================================

/// ResourceIdentifier implementation for CreateRoleResponse with V1CreateLoginRoleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateLoginRoleArgs> for CreateRoleResponse {
    fn generate_resource_id(&self, input: &V1CreateLoginRoleArgs) -> String {
        format!("supabase::CreateRoleResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::CreateRoleResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DeleteRolesResponse
// =============================================================================

/// ResourceIdentifier implementation for DeleteRolesResponse with V1DeleteLoginRolesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteLoginRolesArgs> for DeleteRolesResponse {
    fn generate_resource_id(&self, input: &V1DeleteLoginRolesArgs) -> String {
        format!("supabase::DeleteRolesResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DeleteRolesResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuthConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for AuthConfigResponse with V1GetAuthServiceConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAuthServiceConfigArgs> for AuthConfigResponse {
    fn generate_resource_id(&self, input: &V1GetAuthServiceConfigArgs) -> String {
        format!("supabase::AuthConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::AuthConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuthConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for AuthConfigResponse with V1UpdateAuthServiceConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateAuthServiceConfigArgs> for AuthConfigResponse {
    fn generate_resource_id(&self, input: &V1UpdateAuthServiceConfigArgs) -> String {
        format!("supabase::AuthConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::AuthConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeysResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeysResponse with V1GetProjectSigningKeysArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectSigningKeysArgs> for SigningKeysResponse {
    fn generate_resource_id(&self, input: &V1GetProjectSigningKeysArgs) -> String {
        format!("supabase::SigningKeysResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeysResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeyResponse with V1CreateProjectSigningKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateProjectSigningKeyArgs> for SigningKeyResponse {
    fn generate_resource_id(&self, input: &V1CreateProjectSigningKeyArgs) -> String {
        format!("supabase::SigningKeyResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeyResponse with V1GetLegacySigningKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetLegacySigningKeyArgs> for SigningKeyResponse {
    fn generate_resource_id(&self, input: &V1GetLegacySigningKeyArgs) -> String {
        format!("supabase::SigningKeyResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeyResponse with V1CreateLegacySigningKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateLegacySigningKeyArgs> for SigningKeyResponse {
    fn generate_resource_id(&self, input: &V1CreateLegacySigningKeyArgs) -> String {
        format!("supabase::SigningKeyResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeyResponse with V1GetProjectSigningKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectSigningKeyArgs> for SigningKeyResponse {
    fn generate_resource_id(&self, input: &V1GetProjectSigningKeyArgs) -> String {
        format!("supabase::SigningKeyResponse/{}/{}", input.id, input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeyResponse with V1UpdateProjectSigningKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateProjectSigningKeyArgs> for SigningKeyResponse {
    fn generate_resource_id(&self, input: &V1UpdateProjectSigningKeyArgs) -> String {
        format!("supabase::SigningKeyResponse/{}/{}", input.id, input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SigningKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for SigningKeyResponse with V1RemoveProjectSigningKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1RemoveProjectSigningKeyArgs> for SigningKeyResponse {
    fn generate_resource_id(&self, input: &V1RemoveProjectSigningKeyArgs) -> String {
        format!("supabase::SigningKeyResponse/{}/{}", input.id, input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SigningKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListProvidersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListProvidersResponse with V1ListAllSsoProviderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllSsoProviderArgs> for ListProvidersResponse {
    fn generate_resource_id(&self, input: &V1ListAllSsoProviderArgs) -> String {
        format!("supabase::ListProvidersResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ListProvidersResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreateProviderResponse
// =============================================================================

/// ResourceIdentifier implementation for CreateProviderResponse with V1CreateASsoProviderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateASsoProviderArgs> for CreateProviderResponse {
    fn generate_resource_id(&self, input: &V1CreateASsoProviderArgs) -> String {
        format!("supabase::CreateProviderResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::CreateProviderResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetProviderResponse
// =============================================================================

/// ResourceIdentifier implementation for GetProviderResponse with V1GetASsoProviderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetASsoProviderArgs> for GetProviderResponse {
    fn generate_resource_id(&self, input: &V1GetASsoProviderArgs) -> String {
        format!("supabase::GetProviderResponse/{}/{}", input.ref_rs, input.provider_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::GetProviderResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateProviderResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateProviderResponse with V1UpdateASsoProviderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateASsoProviderArgs> for UpdateProviderResponse {
    fn generate_resource_id(&self, input: &V1UpdateASsoProviderArgs) -> String {
        format!("supabase::UpdateProviderResponse/{}/{}", input.ref_rs, input.provider_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateProviderResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DeleteProviderResponse
// =============================================================================

/// ResourceIdentifier implementation for DeleteProviderResponse with V1DeleteASsoProviderArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteASsoProviderArgs> for DeleteProviderResponse {
    fn generate_resource_id(&self, input: &V1DeleteASsoProviderArgs) -> String {
        format!("supabase::DeleteProviderResponse/{}/{}", input.ref_rs, input.provider_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DeleteProviderResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListProjectTpaIntegrationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListProjectTpaIntegrationsArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListProjectTpaIntegrationsArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ThirdPartyAuth
// =============================================================================

/// ResourceIdentifier implementation for ThirdPartyAuth with V1CreateProjectTpaIntegrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateProjectTpaIntegrationArgs> for ThirdPartyAuth {
    fn generate_resource_id(&self, input: &V1CreateProjectTpaIntegrationArgs) -> String {
        format!("supabase::ThirdPartyAuth/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ThirdPartyAuth"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ThirdPartyAuth
// =============================================================================

/// ResourceIdentifier implementation for ThirdPartyAuth with V1GetProjectTpaIntegrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectTpaIntegrationArgs> for ThirdPartyAuth {
    fn generate_resource_id(&self, input: &V1GetProjectTpaIntegrationArgs) -> String {
        format!("supabase::ThirdPartyAuth/{}/{}", input.ref_rs, input.tpa_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ThirdPartyAuth"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ThirdPartyAuth
// =============================================================================

/// ResourceIdentifier implementation for ThirdPartyAuth with V1DeleteProjectTpaIntegrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeleteProjectTpaIntegrationArgs> for ThirdPartyAuth {
    fn generate_resource_id(&self, input: &V1DeleteProjectTpaIntegrationArgs) -> String {
        format!("supabase::ThirdPartyAuth/{}/{}", input.ref_rs, input.tpa_id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ThirdPartyAuth"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1PgbouncerConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for V1PgbouncerConfigResponse with V1GetProjectPgbouncerConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectPgbouncerConfigArgs> for V1PgbouncerConfigResponse {
    fn generate_resource_id(&self, input: &V1GetProjectPgbouncerConfigArgs) -> String {
        format!("supabase::V1PgbouncerConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1PgbouncerConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1GetPoolerConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPoolerConfigArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1GetPoolerConfigArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateSupavisorConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateSupavisorConfigResponse with V1UpdatePoolerConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdatePoolerConfigArgs> for UpdateSupavisorConfigResponse {
    fn generate_resource_id(&self, input: &V1UpdatePoolerConfigArgs) -> String {
        format!("supabase::UpdateSupavisorConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateSupavisorConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostgresConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for PostgresConfigResponse with V1GetPostgresConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPostgresConfigArgs> for PostgresConfigResponse {
    fn generate_resource_id(&self, input: &V1GetPostgresConfigArgs) -> String {
        format!("supabase::PostgresConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::PostgresConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostgresConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for PostgresConfigResponse with V1UpdatePostgresConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdatePostgresConfigArgs> for PostgresConfigResponse {
    fn generate_resource_id(&self, input: &V1UpdatePostgresConfigArgs) -> String {
        format!("supabase::PostgresConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::PostgresConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskResponse
// =============================================================================

/// ResourceIdentifier implementation for DiskResponse with V1GetDatabaseDiskArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetDatabaseDiskArgs> for DiskResponse {
    fn generate_resource_id(&self, input: &V1GetDatabaseDiskArgs) -> String {
        format!("supabase::DiskResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DiskResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskAutoscaleConfig
// =============================================================================

/// ResourceIdentifier implementation for DiskAutoscaleConfig with V1GetProjectDiskAutoscaleConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetProjectDiskAutoscaleConfigArgs> for DiskAutoscaleConfig {
    fn generate_resource_id(&self, input: &V1GetProjectDiskAutoscaleConfigArgs) -> String {
        format!("supabase::DiskAutoscaleConfig/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DiskAutoscaleConfig"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskUtilMetricsResponse
// =============================================================================

/// ResourceIdentifier implementation for DiskUtilMetricsResponse with V1GetDiskUtilizationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetDiskUtilizationArgs> for DiskUtilMetricsResponse {
    fn generate_resource_id(&self, input: &V1GetDiskUtilizationArgs) -> String {
        format!("supabase::DiskUtilMetricsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DiskUtilMetricsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RealtimeConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for RealtimeConfigResponse with V1GetRealtimeConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetRealtimeConfigArgs> for RealtimeConfigResponse {
    fn generate_resource_id(&self, input: &V1GetRealtimeConfigArgs) -> String {
        format!("supabase::RealtimeConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::RealtimeConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1UpdateRealtimeConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateRealtimeConfigArgs> for () {
    fn generate_resource_id(&self, input: &V1UpdateRealtimeConfigArgs) -> String {
        format!("supabase::()/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with V1ShutdownRealtimeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ShutdownRealtimeArgs> for () {
    fn generate_resource_id(&self, input: &V1ShutdownRealtimeArgs) -> String {
        format!("supabase::()/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::()"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StorageConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for StorageConfigResponse with V1GetStorageConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetStorageConfigArgs> for StorageConfigResponse {
    fn generate_resource_id(&self, input: &V1GetStorageConfigArgs) -> String {
        format!("supabase::StorageConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::StorageConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateCustomHostnameResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateCustomHostnameResponse with V1GetHostnameConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetHostnameConfigArgs> for UpdateCustomHostnameResponse {
    fn generate_resource_id(&self, input: &V1GetHostnameConfigArgs) -> String {
        format!("supabase::UpdateCustomHostnameResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateCustomHostnameResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateCustomHostnameResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateCustomHostnameResponse with V1ActivateCustomHostnameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ActivateCustomHostnameArgs> for UpdateCustomHostnameResponse {
    fn generate_resource_id(&self, input: &V1ActivateCustomHostnameArgs) -> String {
        format!("supabase::UpdateCustomHostnameResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateCustomHostnameResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateCustomHostnameResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateCustomHostnameResponse with V1UpdateHostnameConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateHostnameConfigArgs> for UpdateCustomHostnameResponse {
    fn generate_resource_id(&self, input: &V1UpdateHostnameConfigArgs) -> String {
        format!("supabase::UpdateCustomHostnameResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateCustomHostnameResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateCustomHostnameResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateCustomHostnameResponse with V1VerifyDnsConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1VerifyDnsConfigArgs> for UpdateCustomHostnameResponse {
    fn generate_resource_id(&self, input: &V1VerifyDnsConfigArgs) -> String {
        format!("supabase::UpdateCustomHostnameResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::UpdateCustomHostnameResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1BackupsResponse
// =============================================================================

/// ResourceIdentifier implementation for V1BackupsResponse with V1ListAllBackupsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllBackupsArgs> for V1BackupsResponse {
    fn generate_resource_id(&self, input: &V1ListAllBackupsArgs) -> String {
        format!("supabase::V1BackupsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1BackupsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1RestorePointResponse
// =============================================================================

/// ResourceIdentifier implementation for V1RestorePointResponse with V1GetRestorePointArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetRestorePointArgs> for V1RestorePointResponse {
    fn generate_resource_id(&self, input: &V1GetRestorePointArgs) -> String {
        format!("supabase::V1RestorePointResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1RestorePointResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1RestorePointResponse
// =============================================================================

/// ResourceIdentifier implementation for V1RestorePointResponse with V1CreateRestorePointArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateRestorePointArgs> for V1RestorePointResponse {
    fn generate_resource_id(&self, input: &V1CreateRestorePointArgs) -> String {
        format!("supabase::V1RestorePointResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1RestorePointResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetProjectDbMetadataResponse
// =============================================================================

/// ResourceIdentifier implementation for GetProjectDbMetadataResponse with V1GetDatabaseMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetDatabaseMetadataArgs> for GetProjectDbMetadataResponse {
    fn generate_resource_id(&self, input: &V1GetDatabaseMetadataArgs) -> String {
        format!("supabase::GetProjectDbMetadataResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::GetProjectDbMetadataResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JitAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for JitAccessResponse with V1GetJitAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetJitAccessArgs> for JitAccessResponse {
    fn generate_resource_id(&self, input: &V1GetJitAccessArgs) -> String {
        format!("supabase::JitAccessResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::JitAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JitAuthorizeAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for JitAuthorizeAccessResponse with V1AuthorizeJitAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1AuthorizeJitAccessArgs> for JitAuthorizeAccessResponse {
    fn generate_resource_id(&self, input: &V1AuthorizeJitAccessArgs) -> String {
        format!("supabase::JitAuthorizeAccessResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::JitAuthorizeAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JitAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for JitAccessResponse with V1UpdateJitAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateJitAccessArgs> for JitAccessResponse {
    fn generate_resource_id(&self, input: &V1UpdateJitAccessArgs) -> String {
        format!("supabase::JitAccessResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::JitAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JitListAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for JitListAccessResponse with V1ListJitAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListJitAccessArgs> for JitListAccessResponse {
    fn generate_resource_id(&self, input: &V1ListJitAccessArgs) -> String {
        format!("supabase::JitListAccessResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::JitListAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1ListMigrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for V1ListMigrationsResponse with V1ListMigrationHistoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListMigrationHistoryArgs> for V1ListMigrationsResponse {
    fn generate_resource_id(&self, input: &V1ListMigrationHistoryArgs) -> String {
        format!("supabase::V1ListMigrationsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1ListMigrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1GetMigrationResponse
// =============================================================================

/// ResourceIdentifier implementation for V1GetMigrationResponse with V1GetAMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAMigrationArgs> for V1GetMigrationResponse {
    fn generate_resource_id(&self, input: &V1GetAMigrationArgs) -> String {
        format!("supabase::V1GetMigrationResponse/{}/{}", input.ref_rs, input.version)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1GetMigrationResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1GetDatabaseOpenapiArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetDatabaseOpenapiArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1GetDatabaseOpenapiArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1UpdatePasswordResponse
// =============================================================================

/// ResourceIdentifier implementation for V1UpdatePasswordResponse with V1UpdateDatabasePasswordArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateDatabasePasswordArgs> for V1UpdatePasswordResponse {
    fn generate_resource_id(&self, input: &V1UpdateDatabasePasswordArgs) -> String {
        format!("supabase::V1UpdatePasswordResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1UpdatePasswordResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListAllFunctionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllFunctionsArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListAllFunctionsArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FunctionResponse
// =============================================================================

/// ResourceIdentifier implementation for FunctionResponse with V1CreateAFunctionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CreateAFunctionArgs> for FunctionResponse {
    fn generate_resource_id(&self, input: &V1CreateAFunctionArgs) -> String {
        format!("supabase::FunctionResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::FunctionResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkUpdateFunctionResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkUpdateFunctionResponse with V1BulkUpdateFunctionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1BulkUpdateFunctionsArgs> for BulkUpdateFunctionResponse {
    fn generate_resource_id(&self, input: &V1BulkUpdateFunctionsArgs) -> String {
        format!("supabase::BulkUpdateFunctionResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::BulkUpdateFunctionResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DeployFunctionResponse
// =============================================================================

/// ResourceIdentifier implementation for DeployFunctionResponse with V1DeployAFunctionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1DeployAFunctionArgs> for DeployFunctionResponse {
    fn generate_resource_id(&self, input: &V1DeployAFunctionArgs) -> String {
        format!("supabase::DeployFunctionResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DeployFunctionResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FunctionSlugResponse
// =============================================================================

/// ResourceIdentifier implementation for FunctionSlugResponse with V1GetAFunctionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAFunctionArgs> for FunctionSlugResponse {
    fn generate_resource_id(&self, input: &V1GetAFunctionArgs) -> String {
        format!("supabase::FunctionSlugResponse/{}/{}", input.ref_rs, input.function_slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::FunctionSlugResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FunctionResponse
// =============================================================================

/// ResourceIdentifier implementation for FunctionResponse with V1UpdateAFunctionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateAFunctionArgs> for FunctionResponse {
    fn generate_resource_id(&self, input: &V1UpdateAFunctionArgs) -> String {
        format!("supabase::FunctionResponse/{}/{}", input.ref_rs, input.function_slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::FunctionResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StreamableFile
// =============================================================================

/// ResourceIdentifier implementation for StreamableFile with V1GetAFunctionBodyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetAFunctionBodyArgs> for StreamableFile {
    fn generate_resource_id(&self, input: &V1GetAFunctionBodyArgs) -> String {
        format!("supabase::StreamableFile/{}/{}", input.ref_rs, input.function_slug)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::StreamableFile"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1GetServicesHealthArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetServicesHealthArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1GetServicesHealthArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JitAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for JitAccessResponse with V1GetJitAccessConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetJitAccessConfigArgs> for JitAccessResponse {
    fn generate_resource_id(&self, input: &V1GetJitAccessConfigArgs) -> String {
        format!("supabase::JitAccessResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::JitAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JitAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for JitAccessResponse with V1UpdateJitAccessConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateJitAccessConfigArgs> for JitAccessResponse {
    fn generate_resource_id(&self, input: &V1UpdateJitAccessConfigArgs) -> String {
        format!("supabase::JitAccessResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::JitAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkBanResponse
// =============================================================================

/// ResourceIdentifier implementation for NetworkBanResponse with V1ListAllNetworkBansArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllNetworkBansArgs> for NetworkBanResponse {
    fn generate_resource_id(&self, input: &V1ListAllNetworkBansArgs) -> String {
        format!("supabase::NetworkBanResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::NetworkBanResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkBanResponseEnriched
// =============================================================================

/// ResourceIdentifier implementation for NetworkBanResponseEnriched with V1ListAllNetworkBansEnrichedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllNetworkBansEnrichedArgs> for NetworkBanResponseEnriched {
    fn generate_resource_id(&self, input: &V1ListAllNetworkBansEnrichedArgs) -> String {
        format!("supabase::NetworkBanResponseEnriched/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::NetworkBanResponseEnriched"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkRestrictionsResponse
// =============================================================================

/// ResourceIdentifier implementation for NetworkRestrictionsResponse with V1GetNetworkRestrictionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetNetworkRestrictionsArgs> for NetworkRestrictionsResponse {
    fn generate_resource_id(&self, input: &V1GetNetworkRestrictionsArgs) -> String {
        format!("supabase::NetworkRestrictionsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::NetworkRestrictionsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkRestrictionsV2Response
// =============================================================================

/// ResourceIdentifier implementation for NetworkRestrictionsV2Response with V1PatchNetworkRestrictionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1PatchNetworkRestrictionsArgs> for NetworkRestrictionsV2Response {
    fn generate_resource_id(&self, input: &V1PatchNetworkRestrictionsArgs) -> String {
        format!("supabase::NetworkRestrictionsV2Response/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::NetworkRestrictionsV2Response"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NetworkRestrictionsResponse
// =============================================================================

/// ResourceIdentifier implementation for NetworkRestrictionsResponse with V1UpdateNetworkRestrictionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateNetworkRestrictionsArgs> for NetworkRestrictionsResponse {
    fn generate_resource_id(&self, input: &V1UpdateNetworkRestrictionsArgs) -> String {
        format!("supabase::NetworkRestrictionsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::NetworkRestrictionsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PgsodiumConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for PgsodiumConfigResponse with V1GetPgsodiumConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPgsodiumConfigArgs> for PgsodiumConfigResponse {
    fn generate_resource_id(&self, input: &V1GetPgsodiumConfigArgs) -> String {
        format!("supabase::PgsodiumConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::PgsodiumConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PgsodiumConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for PgsodiumConfigResponse with V1UpdatePgsodiumConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdatePgsodiumConfigArgs> for PgsodiumConfigResponse {
    fn generate_resource_id(&self, input: &V1UpdatePgsodiumConfigArgs) -> String {
        format!("supabase::PgsodiumConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::PgsodiumConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostgrestConfigWithJWTSecretResponse
// =============================================================================

/// ResourceIdentifier implementation for PostgrestConfigWithJWTSecretResponse with V1GetPostgrestServiceConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPostgrestServiceConfigArgs> for PostgrestConfigWithJWTSecretResponse {
    fn generate_resource_id(&self, input: &V1GetPostgrestServiceConfigArgs) -> String {
        format!("supabase::PostgrestConfigWithJWTSecretResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::PostgrestConfigWithJWTSecretResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V1PostgrestConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for V1PostgrestConfigResponse with V1UpdatePostgrestServiceConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdatePostgrestServiceConfigArgs> for V1PostgrestConfigResponse {
    fn generate_resource_id(&self, input: &V1UpdatePostgrestServiceConfigArgs) -> String {
        format!("supabase::V1PostgrestConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::V1PostgrestConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReadOnlyStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for ReadOnlyStatusResponse with V1GetReadonlyModeStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetReadonlyModeStatusArgs> for ReadOnlyStatusResponse {
    fn generate_resource_id(&self, input: &V1GetReadonlyModeStatusArgs) -> String {
        format!("supabase::ReadOnlyStatusResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ReadOnlyStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetProjectAvailableRestoreVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetProjectAvailableRestoreVersionsResponse with V1ListAvailableRestoreVersionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAvailableRestoreVersionsArgs> for GetProjectAvailableRestoreVersionsResponse {
    fn generate_resource_id(&self, input: &V1ListAvailableRestoreVersionsArgs) -> String {
        format!("supabase::GetProjectAvailableRestoreVersionsResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::GetProjectAvailableRestoreVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListAllSecretsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllSecretsArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListAllSecretsArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslEnforcementResponse
// =============================================================================

/// ResourceIdentifier implementation for SslEnforcementResponse with V1GetSslEnforcementConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetSslEnforcementConfigArgs> for SslEnforcementResponse {
    fn generate_resource_id(&self, input: &V1GetSslEnforcementConfigArgs) -> String {
        format!("supabase::SslEnforcementResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SslEnforcementResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslEnforcementResponse
// =============================================================================

/// ResourceIdentifier implementation for SslEnforcementResponse with V1UpdateSslEnforcementConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpdateSslEnforcementConfigArgs> for SslEnforcementResponse {
    fn generate_resource_id(&self, input: &V1UpdateSslEnforcementConfigArgs) -> String {
        format!("supabase::SslEnforcementResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SslEnforcementResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for serde_json::Value
// =============================================================================

/// ResourceIdentifier implementation for serde_json::Value with V1ListAllBucketsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllBucketsArgs> for serde_json::Value {
    fn generate_resource_id(&self, input: &V1ListAllBucketsArgs) -> String {
        format!("supabase::serde_json::Value/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::serde_json::Value"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TypescriptResponse
// =============================================================================

/// ResourceIdentifier implementation for TypescriptResponse with V1GenerateTypescriptTypesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GenerateTypescriptTypesArgs> for TypescriptResponse {
    fn generate_resource_id(&self, input: &V1GenerateTypescriptTypesArgs) -> String {
        format!("supabase::TypescriptResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::TypescriptResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProjectUpgradeInitiateResponse
// =============================================================================

/// ResourceIdentifier implementation for ProjectUpgradeInitiateResponse with V1UpgradePostgresVersionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1UpgradePostgresVersionArgs> for ProjectUpgradeInitiateResponse {
    fn generate_resource_id(&self, input: &V1UpgradePostgresVersionArgs) -> String {
        format!("supabase::ProjectUpgradeInitiateResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ProjectUpgradeInitiateResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProjectUpgradeEligibilityResponse
// =============================================================================

/// ResourceIdentifier implementation for ProjectUpgradeEligibilityResponse with V1GetPostgresUpgradeEligibilityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPostgresUpgradeEligibilityArgs> for ProjectUpgradeEligibilityResponse {
    fn generate_resource_id(&self, input: &V1GetPostgresUpgradeEligibilityArgs) -> String {
        format!("supabase::ProjectUpgradeEligibilityResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ProjectUpgradeEligibilityResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DatabaseUpgradeStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for DatabaseUpgradeStatusResponse with V1GetPostgresUpgradeStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetPostgresUpgradeStatusArgs> for DatabaseUpgradeStatusResponse {
    fn generate_resource_id(&self, input: &V1GetPostgresUpgradeStatusArgs) -> String {
        format!("supabase::DatabaseUpgradeStatusResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::DatabaseUpgradeStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VanitySubdomainConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for VanitySubdomainConfigResponse with V1GetVanitySubdomainConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetVanitySubdomainConfigArgs> for VanitySubdomainConfigResponse {
    fn generate_resource_id(&self, input: &V1GetVanitySubdomainConfigArgs) -> String {
        format!("supabase::VanitySubdomainConfigResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::VanitySubdomainConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ActivateVanitySubdomainResponse
// =============================================================================

/// ResourceIdentifier implementation for ActivateVanitySubdomainResponse with V1ActivateVanitySubdomainConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ActivateVanitySubdomainConfigArgs> for ActivateVanitySubdomainResponse {
    fn generate_resource_id(&self, input: &V1ActivateVanitySubdomainConfigArgs) -> String {
        format!("supabase::ActivateVanitySubdomainResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::ActivateVanitySubdomainResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SubdomainAvailabilityResponse
// =============================================================================

/// ResourceIdentifier implementation for SubdomainAvailabilityResponse with V1CheckVanitySubdomainAvailabilityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1CheckVanitySubdomainAvailabilityArgs> for SubdomainAvailabilityResponse {
    fn generate_resource_id(&self, input: &V1CheckVanitySubdomainAvailabilityArgs) -> String {
        format!("supabase::SubdomainAvailabilityResponse/{}", input.ref_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SubdomainAvailabilityResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SnippetList
// =============================================================================

/// ResourceIdentifier implementation for SnippetList with V1ListAllSnippetsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1ListAllSnippetsArgs> for SnippetList {
    fn generate_resource_id(&self, input: &V1ListAllSnippetsArgs) -> String {
        "supabase::SnippetList".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SnippetList"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SnippetResponse
// =============================================================================

/// ResourceIdentifier implementation for SnippetResponse with V1GetASnippetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<V1GetASnippetArgs> for SnippetResponse {
    fn generate_resource_id(&self, input: &V1GetASnippetArgs) -> String {
        format!("supabase::SnippetResponse/{}", input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "supabase::SnippetResponse"
    }

    fn provider(&self) -> &'static str {
        "supabase"
    }
}

