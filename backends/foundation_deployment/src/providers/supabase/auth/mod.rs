//! Auto-generated API module for supabase auth.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `supabase_auth `

#![cfg(feature = "supabase_auth")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `AuthConfigResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuthConfigResponse {
    /// api_max_request_duration property.
    pub api_max_request_duration: i64,
    /// custom_oauth_enabled property.
    pub custom_oauth_enabled: bool,
    /// custom_oauth_max_providers property.
    pub custom_oauth_max_providers: i64,
    /// db_max_pool_size property.
    pub db_max_pool_size: i64,
    /// db_max_pool_size_unit property.
    pub db_max_pool_size_unit: String,
    /// disable_signup property.
    pub disable_signup: bool,
    /// external_anonymous_users_enabled property.
    pub external_anonymous_users_enabled: bool,
    /// external_apple_additional_client_ids property.
    pub external_apple_additional_client_ids: String,
    /// external_apple_client_id property.
    pub external_apple_client_id: String,
    /// external_apple_email_optional property.
    pub external_apple_email_optional: bool,
    /// external_apple_enabled property.
    pub external_apple_enabled: bool,
    /// external_apple_secret property.
    pub external_apple_secret: String,
    /// external_azure_client_id property.
    pub external_azure_client_id: String,
    /// external_azure_email_optional property.
    pub external_azure_email_optional: bool,
    /// external_azure_enabled property.
    pub external_azure_enabled: bool,
    /// external_azure_secret property.
    pub external_azure_secret: String,
    /// external_azure_url property.
    pub external_azure_url: String,
    /// external_bitbucket_client_id property.
    pub external_bitbucket_client_id: String,
    /// external_bitbucket_email_optional property.
    pub external_bitbucket_email_optional: bool,
    /// external_bitbucket_enabled property.
    pub external_bitbucket_enabled: bool,
    /// external_bitbucket_secret property.
    pub external_bitbucket_secret: String,
    /// external_discord_client_id property.
    pub external_discord_client_id: String,
    /// external_discord_email_optional property.
    pub external_discord_email_optional: bool,
    /// external_discord_enabled property.
    pub external_discord_enabled: bool,
    /// external_discord_secret property.
    pub external_discord_secret: String,
    /// external_email_enabled property.
    pub external_email_enabled: bool,
    /// external_facebook_client_id property.
    pub external_facebook_client_id: String,
    /// external_facebook_email_optional property.
    pub external_facebook_email_optional: bool,
    /// external_facebook_enabled property.
    pub external_facebook_enabled: bool,
    /// external_facebook_secret property.
    pub external_facebook_secret: String,
    /// external_figma_client_id property.
    pub external_figma_client_id: String,
    /// external_figma_email_optional property.
    pub external_figma_email_optional: bool,
    /// external_figma_enabled property.
    pub external_figma_enabled: bool,
    /// external_figma_secret property.
    pub external_figma_secret: String,
    /// external_github_client_id property.
    pub external_github_client_id: String,
    /// external_github_email_optional property.
    pub external_github_email_optional: bool,
    /// external_github_enabled property.
    pub external_github_enabled: bool,
    /// external_github_secret property.
    pub external_github_secret: String,
    /// external_gitlab_client_id property.
    pub external_gitlab_client_id: String,
    /// external_gitlab_email_optional property.
    pub external_gitlab_email_optional: bool,
    /// external_gitlab_enabled property.
    pub external_gitlab_enabled: bool,
    /// external_gitlab_secret property.
    pub external_gitlab_secret: String,
    /// external_gitlab_url property.
    pub external_gitlab_url: String,
    /// external_google_additional_client_ids property.
    pub external_google_additional_client_ids: String,
    /// external_google_client_id property.
    pub external_google_client_id: String,
    /// external_google_email_optional property.
    pub external_google_email_optional: bool,
    /// external_google_enabled property.
    pub external_google_enabled: bool,
    /// external_google_secret property.
    pub external_google_secret: String,
    /// external_google_skip_nonce_check property.
    pub external_google_skip_nonce_check: bool,
    /// external_kakao_client_id property.
    pub external_kakao_client_id: String,
    /// external_kakao_email_optional property.
    pub external_kakao_email_optional: bool,
    /// external_kakao_enabled property.
    pub external_kakao_enabled: bool,
    /// external_kakao_secret property.
    pub external_kakao_secret: String,
    /// external_keycloak_client_id property.
    pub external_keycloak_client_id: String,
    /// external_keycloak_email_optional property.
    pub external_keycloak_email_optional: bool,
    /// external_keycloak_enabled property.
    pub external_keycloak_enabled: bool,
    /// external_keycloak_secret property.
    pub external_keycloak_secret: String,
    /// external_keycloak_url property.
    pub external_keycloak_url: String,
    /// external_linkedin_oidc_client_id property.
    pub external_linkedin_oidc_client_id: String,
    /// external_linkedin_oidc_email_optional property.
    pub external_linkedin_oidc_email_optional: bool,
    /// external_linkedin_oidc_enabled property.
    pub external_linkedin_oidc_enabled: bool,
    /// external_linkedin_oidc_secret property.
    pub external_linkedin_oidc_secret: String,
    /// external_notion_client_id property.
    pub external_notion_client_id: String,
    /// external_notion_email_optional property.
    pub external_notion_email_optional: bool,
    /// external_notion_enabled property.
    pub external_notion_enabled: bool,
    /// external_notion_secret property.
    pub external_notion_secret: String,
    /// external_phone_enabled property.
    pub external_phone_enabled: bool,
    /// external_slack_client_id property.
    pub external_slack_client_id: String,
    /// external_slack_email_optional property.
    pub external_slack_email_optional: bool,
    /// external_slack_enabled property.
    pub external_slack_enabled: bool,
    /// external_slack_oidc_client_id property.
    pub external_slack_oidc_client_id: String,
    /// external_slack_oidc_email_optional property.
    pub external_slack_oidc_email_optional: bool,
    /// external_slack_oidc_enabled property.
    pub external_slack_oidc_enabled: bool,
    /// external_slack_oidc_secret property.
    pub external_slack_oidc_secret: String,
    /// external_slack_secret property.
    pub external_slack_secret: String,
    /// external_spotify_client_id property.
    pub external_spotify_client_id: String,
    /// external_spotify_email_optional property.
    pub external_spotify_email_optional: bool,
    /// external_spotify_enabled property.
    pub external_spotify_enabled: bool,
    /// external_spotify_secret property.
    pub external_spotify_secret: String,
    /// external_twitch_client_id property.
    pub external_twitch_client_id: String,
    /// external_twitch_email_optional property.
    pub external_twitch_email_optional: bool,
    /// external_twitch_enabled property.
    pub external_twitch_enabled: bool,
    /// external_twitch_secret property.
    pub external_twitch_secret: String,
    /// external_twitter_client_id property.
    pub external_twitter_client_id: String,
    /// external_twitter_email_optional property.
    pub external_twitter_email_optional: bool,
    /// external_twitter_enabled property.
    pub external_twitter_enabled: bool,
    /// external_twitter_secret property.
    pub external_twitter_secret: String,
    /// external_web3_ethereum_enabled property.
    pub external_web3_ethereum_enabled: bool,
    /// external_web3_solana_enabled property.
    pub external_web3_solana_enabled: bool,
    /// external_workos_client_id property.
    pub external_workos_client_id: String,
    /// external_workos_enabled property.
    pub external_workos_enabled: bool,
    /// external_workos_secret property.
    pub external_workos_secret: String,
    /// external_workos_url property.
    pub external_workos_url: String,
    /// external_x_client_id property.
    pub external_x_client_id: String,
    /// external_x_email_optional property.
    pub external_x_email_optional: bool,
    /// external_x_enabled property.
    pub external_x_enabled: bool,
    /// external_x_secret property.
    pub external_x_secret: String,
    /// external_zoom_client_id property.
    pub external_zoom_client_id: String,
    /// external_zoom_email_optional property.
    pub external_zoom_email_optional: bool,
    /// external_zoom_enabled property.
    pub external_zoom_enabled: bool,
    /// external_zoom_secret property.
    pub external_zoom_secret: String,
    /// hook_after_user_created_enabled property.
    pub hook_after_user_created_enabled: bool,
    /// hook_after_user_created_secrets property.
    pub hook_after_user_created_secrets: String,
    /// hook_after_user_created_uri property.
    pub hook_after_user_created_uri: String,
    /// hook_before_user_created_enabled property.
    pub hook_before_user_created_enabled: bool,
    /// hook_before_user_created_secrets property.
    pub hook_before_user_created_secrets: String,
    /// hook_before_user_created_uri property.
    pub hook_before_user_created_uri: String,
    /// hook_custom_access_token_enabled property.
    pub hook_custom_access_token_enabled: bool,
    /// hook_custom_access_token_secrets property.
    pub hook_custom_access_token_secrets: String,
    /// hook_custom_access_token_uri property.
    pub hook_custom_access_token_uri: String,
    /// hook_mfa_verification_attempt_enabled property.
    pub hook_mfa_verification_attempt_enabled: bool,
    /// hook_mfa_verification_attempt_secrets property.
    pub hook_mfa_verification_attempt_secrets: String,
    /// hook_mfa_verification_attempt_uri property.
    pub hook_mfa_verification_attempt_uri: String,
    /// hook_password_verification_attempt_enabled property.
    pub hook_password_verification_attempt_enabled: bool,
    /// hook_password_verification_attempt_secrets property.
    pub hook_password_verification_attempt_secrets: String,
    /// hook_password_verification_attempt_uri property.
    pub hook_password_verification_attempt_uri: String,
    /// hook_send_email_enabled property.
    pub hook_send_email_enabled: bool,
    /// hook_send_email_secrets property.
    pub hook_send_email_secrets: String,
    /// hook_send_email_uri property.
    pub hook_send_email_uri: String,
    /// hook_send_sms_enabled property.
    pub hook_send_sms_enabled: bool,
    /// hook_send_sms_secrets property.
    pub hook_send_sms_secrets: String,
    /// hook_send_sms_uri property.
    pub hook_send_sms_uri: String,
    /// jwt_exp property.
    pub jwt_exp: i64,
    /// mailer_allow_unverified_email_sign_ins property.
    pub mailer_allow_unverified_email_sign_ins: bool,
    /// mailer_autoconfirm property.
    pub mailer_autoconfirm: bool,
    /// mailer_notifications_email_changed_enabled property.
    pub mailer_notifications_email_changed_enabled: bool,
    /// mailer_notifications_identity_linked_enabled property.
    pub mailer_notifications_identity_linked_enabled: bool,
    /// mailer_notifications_identity_unlinked_enabled property.
    pub mailer_notifications_identity_unlinked_enabled: bool,
    /// mailer_notifications_mfa_factor_enrolled_enabled property.
    pub mailer_notifications_mfa_factor_enrolled_enabled: bool,
    /// mailer_notifications_mfa_factor_unenrolled_enabled property.
    pub mailer_notifications_mfa_factor_unenrolled_enabled: bool,
    /// mailer_notifications_password_changed_enabled property.
    pub mailer_notifications_password_changed_enabled: bool,
    /// mailer_notifications_phone_changed_enabled property.
    pub mailer_notifications_phone_changed_enabled: bool,
    /// mailer_otp_exp property.
    pub mailer_otp_exp: i64,
    /// mailer_otp_length property.
    pub mailer_otp_length: i64,
    /// mailer_secure_email_change_enabled property.
    pub mailer_secure_email_change_enabled: bool,
    /// mailer_subjects_confirmation property.
    pub mailer_subjects_confirmation: String,
    /// mailer_subjects_email_change property.
    pub mailer_subjects_email_change: String,
    /// mailer_subjects_email_changed_notification property.
    pub mailer_subjects_email_changed_notification: String,
    /// mailer_subjects_identity_linked_notification property.
    pub mailer_subjects_identity_linked_notification: String,
    /// mailer_subjects_identity_unlinked_notification property.
    pub mailer_subjects_identity_unlinked_notification: String,
    /// mailer_subjects_invite property.
    pub mailer_subjects_invite: String,
    /// mailer_subjects_magic_link property.
    pub mailer_subjects_magic_link: String,
    /// mailer_subjects_mfa_factor_enrolled_notification property.
    pub mailer_subjects_mfa_factor_enrolled_notification: String,
    /// mailer_subjects_mfa_factor_unenrolled_notification property.
    pub mailer_subjects_mfa_factor_unenrolled_notification: String,
    /// mailer_subjects_password_changed_notification property.
    pub mailer_subjects_password_changed_notification: String,
    /// mailer_subjects_phone_changed_notification property.
    pub mailer_subjects_phone_changed_notification: String,
    /// mailer_subjects_reauthentication property.
    pub mailer_subjects_reauthentication: String,
    /// mailer_subjects_recovery property.
    pub mailer_subjects_recovery: String,
    /// mailer_templates_confirmation_content property.
    pub mailer_templates_confirmation_content: String,
    /// mailer_templates_email_change_content property.
    pub mailer_templates_email_change_content: String,
    /// mailer_templates_email_changed_notification_content property.
    pub mailer_templates_email_changed_notification_content: String,
    /// mailer_templates_identity_linked_notification_content property.
    pub mailer_templates_identity_linked_notification_content: String,
    /// mailer_templates_identity_unlinked_notification_content property.
    pub mailer_templates_identity_unlinked_notification_content: String,
    /// mailer_templates_invite_content property.
    pub mailer_templates_invite_content: String,
    /// mailer_templates_magic_link_content property.
    pub mailer_templates_magic_link_content: String,
    /// mailer_templates_mfa_factor_enrolled_notification_content property.
    pub mailer_templates_mfa_factor_enrolled_notification_content: String,
    /// mailer_templates_mfa_factor_unenrolled_notification_content property.
    pub mailer_templates_mfa_factor_unenrolled_notification_content: String,
    /// mailer_templates_password_changed_notification_content property.
    pub mailer_templates_password_changed_notification_content: String,
    /// mailer_templates_phone_changed_notification_content property.
    pub mailer_templates_phone_changed_notification_content: String,
    /// mailer_templates_reauthentication_content property.
    pub mailer_templates_reauthentication_content: String,
    /// mailer_templates_recovery_content property.
    pub mailer_templates_recovery_content: String,
    /// mfa_max_enrolled_factors property.
    pub mfa_max_enrolled_factors: i64,
    /// mfa_phone_enroll_enabled property.
    pub mfa_phone_enroll_enabled: bool,
    /// mfa_phone_max_frequency property.
    pub mfa_phone_max_frequency: i64,
    /// mfa_phone_otp_length property.
    pub mfa_phone_otp_length: i64,
    /// mfa_phone_template property.
    pub mfa_phone_template: String,
    /// mfa_phone_verify_enabled property.
    pub mfa_phone_verify_enabled: bool,
    /// mfa_totp_enroll_enabled property.
    pub mfa_totp_enroll_enabled: bool,
    /// mfa_totp_verify_enabled property.
    pub mfa_totp_verify_enabled: bool,
    /// mfa_web_authn_enroll_enabled property.
    pub mfa_web_authn_enroll_enabled: bool,
    /// mfa_web_authn_verify_enabled property.
    pub mfa_web_authn_verify_enabled: bool,
    /// nimbus_oauth_client_id property.
    pub nimbus_oauth_client_id: String,
    /// nimbus_oauth_client_secret property.
    pub nimbus_oauth_client_secret: String,
    /// nimbus_oauth_email_optional property.
    pub nimbus_oauth_email_optional: bool,
    /// oauth_server_allow_dynamic_registration property.
    pub oauth_server_allow_dynamic_registration: bool,
    /// oauth_server_authorization_path property.
    pub oauth_server_authorization_path: String,
    /// oauth_server_enabled property.
    pub oauth_server_enabled: bool,
    /// passkey_enabled property.
    pub passkey_enabled: bool,
    /// password_hibp_enabled property.
    pub password_hibp_enabled: bool,
    /// password_min_length property.
    pub password_min_length: i64,
    /// password_required_characters property.
    pub password_required_characters: String,
    /// rate_limit_anonymous_users property.
    pub rate_limit_anonymous_users: i64,
    /// rate_limit_email_sent property.
    pub rate_limit_email_sent: i64,
    /// rate_limit_otp property.
    pub rate_limit_otp: i64,
    /// rate_limit_sms_sent property.
    pub rate_limit_sms_sent: i64,
    /// rate_limit_token_refresh property.
    pub rate_limit_token_refresh: i64,
    /// rate_limit_verify property.
    pub rate_limit_verify: i64,
    /// rate_limit_web3 property.
    pub rate_limit_web3: i64,
    /// refresh_token_rotation_enabled property.
    pub refresh_token_rotation_enabled: bool,
    /// saml_allow_encrypted_assertions property.
    pub saml_allow_encrypted_assertions: bool,
    /// saml_enabled property.
    pub saml_enabled: bool,
    /// saml_external_url property.
    pub saml_external_url: String,
    /// security_captcha_enabled property.
    pub security_captcha_enabled: bool,
    /// security_captcha_provider property.
    pub security_captcha_provider: String,
    /// security_captcha_secret property.
    pub security_captcha_secret: String,
    /// security_manual_linking_enabled property.
    pub security_manual_linking_enabled: bool,
    /// security_refresh_token_reuse_interval property.
    pub security_refresh_token_reuse_interval: i64,
    /// security_sb_forwarded_for_enabled property.
    pub security_sb_forwarded_for_enabled: bool,
    /// security_update_password_require_reauthentication property.
    pub security_update_password_require_reauthentication: bool,
    /// sessions_inactivity_timeout property.
    pub sessions_inactivity_timeout: f64,
    /// sessions_single_per_user property.
    pub sessions_single_per_user: bool,
    /// sessions_tags property.
    pub sessions_tags: String,
    /// sessions_timebox property.
    pub sessions_timebox: f64,
    /// site_url property.
    pub site_url: String,
    /// sms_autoconfirm property.
    pub sms_autoconfirm: bool,
    /// sms_max_frequency property.
    pub sms_max_frequency: i64,
    /// sms_messagebird_access_key property.
    pub sms_messagebird_access_key: String,
    /// sms_messagebird_originator property.
    pub sms_messagebird_originator: String,
    /// sms_otp_exp property.
    pub sms_otp_exp: i64,
    /// sms_otp_length property.
    pub sms_otp_length: i64,
    /// sms_provider property.
    pub sms_provider: String,
    /// sms_template property.
    pub sms_template: String,
    /// sms_test_otp property.
    pub sms_test_otp: String,
    /// sms_test_otp_valid_until property.
    pub sms_test_otp_valid_until: String,
    /// sms_textlocal_api_key property.
    pub sms_textlocal_api_key: String,
    /// sms_textlocal_sender property.
    pub sms_textlocal_sender: String,
    /// sms_twilio_account_sid property.
    pub sms_twilio_account_sid: String,
    /// sms_twilio_auth_token property.
    pub sms_twilio_auth_token: String,
    /// sms_twilio_content_sid property.
    pub sms_twilio_content_sid: String,
    /// sms_twilio_message_service_sid property.
    pub sms_twilio_message_service_sid: String,
    /// sms_twilio_verify_account_sid property.
    pub sms_twilio_verify_account_sid: String,
    /// sms_twilio_verify_auth_token property.
    pub sms_twilio_verify_auth_token: String,
    /// sms_twilio_verify_message_service_sid property.
    pub sms_twilio_verify_message_service_sid: String,
    /// sms_vonage_api_key property.
    pub sms_vonage_api_key: String,
    /// sms_vonage_api_secret property.
    pub sms_vonage_api_secret: String,
    /// sms_vonage_from property.
    pub sms_vonage_from: String,
    /// smtp_admin_email property.
    pub smtp_admin_email: String,
    /// smtp_host property.
    pub smtp_host: String,
    /// smtp_max_frequency property.
    pub smtp_max_frequency: i64,
    /// smtp_pass property.
    pub smtp_pass: String,
    /// smtp_port property.
    pub smtp_port: String,
    /// smtp_sender_name property.
    pub smtp_sender_name: String,
    /// smtp_user property.
    pub smtp_user: String,
    /// uri_allow_list property.
    pub uri_allow_list: String,
    /// webauthn_rp_display_name property.
    pub webauthn_rp_display_name: String,
    /// webauthn_rp_id property.
    pub webauthn_rp_id: String,
    /// webauthn_rp_origins property.
    pub webauthn_rp_origins: String,
}

/// `CreateSigningKeyBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateSigningKeyBody {
    /// algorithm property.
    pub algorithm: String,
    /// private_jwk property.
    pub private_jwk: Option<serde_json::Value>,
    /// status property.
    pub status: Option<String>,
}

/// `CreateProviderBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateProviderBody {
    /// attribute_mapping property.
    pub attribute_mapping: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// domains property.
    pub domains: Option<Vec<String>>,
    /// metadata_url property.
    pub metadata_url: Option<String>,
    /// metadata_xml property.
    pub metadata_xml: Option<String>,
    /// name_id_format property.
    pub name_id_format: Option<String>,
    /// type property.
    pub r#type: String,
}

/// `SigningKeysResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SigningKeysResponse {
    /// keys property.
    pub keys: Vec<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ListProvidersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListProvidersResponse {
    /// items property.
    pub items: Vec<std::collections::HashMap<String, serde_json::Value>>,
}

/// `CreateProviderResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateProviderResponse {
    /// created_at property.
    pub created_at: Option<String>,
    /// domains property.
    pub domains: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// id property.
    pub id: String,
    /// saml property.
    pub saml: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// updated_at property.
    pub updated_at: Option<String>,
}

/// `UpdateAuthConfigBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateAuthConfigBody {
    /// api_max_request_duration property.
    pub api_max_request_duration: Option<i64>,
    /// custom_oauth_enabled property.
    pub custom_oauth_enabled: Option<bool>,
    /// db_max_pool_size property.
    pub db_max_pool_size: Option<i64>,
    /// db_max_pool_size_unit property.
    pub db_max_pool_size_unit: Option<String>,
    /// disable_signup property.
    pub disable_signup: Option<bool>,
    /// external_anonymous_users_enabled property.
    pub external_anonymous_users_enabled: Option<bool>,
    /// external_apple_additional_client_ids property.
    pub external_apple_additional_client_ids: Option<String>,
    /// external_apple_client_id property.
    pub external_apple_client_id: Option<String>,
    /// external_apple_email_optional property.
    pub external_apple_email_optional: Option<bool>,
    /// external_apple_enabled property.
    pub external_apple_enabled: Option<bool>,
    /// external_apple_secret property.
    pub external_apple_secret: Option<String>,
    /// external_azure_client_id property.
    pub external_azure_client_id: Option<String>,
    /// external_azure_email_optional property.
    pub external_azure_email_optional: Option<bool>,
    /// external_azure_enabled property.
    pub external_azure_enabled: Option<bool>,
    /// external_azure_secret property.
    pub external_azure_secret: Option<String>,
    /// external_azure_url property.
    pub external_azure_url: Option<String>,
    /// external_bitbucket_client_id property.
    pub external_bitbucket_client_id: Option<String>,
    /// external_bitbucket_email_optional property.
    pub external_bitbucket_email_optional: Option<bool>,
    /// external_bitbucket_enabled property.
    pub external_bitbucket_enabled: Option<bool>,
    /// external_bitbucket_secret property.
    pub external_bitbucket_secret: Option<String>,
    /// external_discord_client_id property.
    pub external_discord_client_id: Option<String>,
    /// external_discord_email_optional property.
    pub external_discord_email_optional: Option<bool>,
    /// external_discord_enabled property.
    pub external_discord_enabled: Option<bool>,
    /// external_discord_secret property.
    pub external_discord_secret: Option<String>,
    /// external_email_enabled property.
    pub external_email_enabled: Option<bool>,
    /// external_facebook_client_id property.
    pub external_facebook_client_id: Option<String>,
    /// external_facebook_email_optional property.
    pub external_facebook_email_optional: Option<bool>,
    /// external_facebook_enabled property.
    pub external_facebook_enabled: Option<bool>,
    /// external_facebook_secret property.
    pub external_facebook_secret: Option<String>,
    /// external_figma_client_id property.
    pub external_figma_client_id: Option<String>,
    /// external_figma_email_optional property.
    pub external_figma_email_optional: Option<bool>,
    /// external_figma_enabled property.
    pub external_figma_enabled: Option<bool>,
    /// external_figma_secret property.
    pub external_figma_secret: Option<String>,
    /// external_github_client_id property.
    pub external_github_client_id: Option<String>,
    /// external_github_email_optional property.
    pub external_github_email_optional: Option<bool>,
    /// external_github_enabled property.
    pub external_github_enabled: Option<bool>,
    /// external_github_secret property.
    pub external_github_secret: Option<String>,
    /// external_gitlab_client_id property.
    pub external_gitlab_client_id: Option<String>,
    /// external_gitlab_email_optional property.
    pub external_gitlab_email_optional: Option<bool>,
    /// external_gitlab_enabled property.
    pub external_gitlab_enabled: Option<bool>,
    /// external_gitlab_secret property.
    pub external_gitlab_secret: Option<String>,
    /// external_gitlab_url property.
    pub external_gitlab_url: Option<String>,
    /// external_google_additional_client_ids property.
    pub external_google_additional_client_ids: Option<String>,
    /// external_google_client_id property.
    pub external_google_client_id: Option<String>,
    /// external_google_email_optional property.
    pub external_google_email_optional: Option<bool>,
    /// external_google_enabled property.
    pub external_google_enabled: Option<bool>,
    /// external_google_secret property.
    pub external_google_secret: Option<String>,
    /// external_google_skip_nonce_check property.
    pub external_google_skip_nonce_check: Option<bool>,
    /// external_kakao_client_id property.
    pub external_kakao_client_id: Option<String>,
    /// external_kakao_email_optional property.
    pub external_kakao_email_optional: Option<bool>,
    /// external_kakao_enabled property.
    pub external_kakao_enabled: Option<bool>,
    /// external_kakao_secret property.
    pub external_kakao_secret: Option<String>,
    /// external_keycloak_client_id property.
    pub external_keycloak_client_id: Option<String>,
    /// external_keycloak_email_optional property.
    pub external_keycloak_email_optional: Option<bool>,
    /// external_keycloak_enabled property.
    pub external_keycloak_enabled: Option<bool>,
    /// external_keycloak_secret property.
    pub external_keycloak_secret: Option<String>,
    /// external_keycloak_url property.
    pub external_keycloak_url: Option<String>,
    /// external_linkedin_oidc_client_id property.
    pub external_linkedin_oidc_client_id: Option<String>,
    /// external_linkedin_oidc_email_optional property.
    pub external_linkedin_oidc_email_optional: Option<bool>,
    /// external_linkedin_oidc_enabled property.
    pub external_linkedin_oidc_enabled: Option<bool>,
    /// external_linkedin_oidc_secret property.
    pub external_linkedin_oidc_secret: Option<String>,
    /// external_notion_client_id property.
    pub external_notion_client_id: Option<String>,
    /// external_notion_email_optional property.
    pub external_notion_email_optional: Option<bool>,
    /// external_notion_enabled property.
    pub external_notion_enabled: Option<bool>,
    /// external_notion_secret property.
    pub external_notion_secret: Option<String>,
    /// external_phone_enabled property.
    pub external_phone_enabled: Option<bool>,
    /// external_slack_client_id property.
    pub external_slack_client_id: Option<String>,
    /// external_slack_email_optional property.
    pub external_slack_email_optional: Option<bool>,
    /// external_slack_enabled property.
    pub external_slack_enabled: Option<bool>,
    /// external_slack_oidc_client_id property.
    pub external_slack_oidc_client_id: Option<String>,
    /// external_slack_oidc_email_optional property.
    pub external_slack_oidc_email_optional: Option<bool>,
    /// external_slack_oidc_enabled property.
    pub external_slack_oidc_enabled: Option<bool>,
    /// external_slack_oidc_secret property.
    pub external_slack_oidc_secret: Option<String>,
    /// external_slack_secret property.
    pub external_slack_secret: Option<String>,
    /// external_spotify_client_id property.
    pub external_spotify_client_id: Option<String>,
    /// external_spotify_email_optional property.
    pub external_spotify_email_optional: Option<bool>,
    /// external_spotify_enabled property.
    pub external_spotify_enabled: Option<bool>,
    /// external_spotify_secret property.
    pub external_spotify_secret: Option<String>,
    /// external_twitch_client_id property.
    pub external_twitch_client_id: Option<String>,
    /// external_twitch_email_optional property.
    pub external_twitch_email_optional: Option<bool>,
    /// external_twitch_enabled property.
    pub external_twitch_enabled: Option<bool>,
    /// external_twitch_secret property.
    pub external_twitch_secret: Option<String>,
    /// external_twitter_client_id property.
    pub external_twitter_client_id: Option<String>,
    /// external_twitter_email_optional property.
    pub external_twitter_email_optional: Option<bool>,
    /// external_twitter_enabled property.
    pub external_twitter_enabled: Option<bool>,
    /// external_twitter_secret property.
    pub external_twitter_secret: Option<String>,
    /// external_web3_ethereum_enabled property.
    pub external_web3_ethereum_enabled: Option<bool>,
    /// external_web3_solana_enabled property.
    pub external_web3_solana_enabled: Option<bool>,
    /// external_workos_client_id property.
    pub external_workos_client_id: Option<String>,
    /// external_workos_enabled property.
    pub external_workos_enabled: Option<bool>,
    /// external_workos_secret property.
    pub external_workos_secret: Option<String>,
    /// external_workos_url property.
    pub external_workos_url: Option<String>,
    /// external_x_client_id property.
    pub external_x_client_id: Option<String>,
    /// external_x_email_optional property.
    pub external_x_email_optional: Option<bool>,
    /// external_x_enabled property.
    pub external_x_enabled: Option<bool>,
    /// external_x_secret property.
    pub external_x_secret: Option<String>,
    /// external_zoom_client_id property.
    pub external_zoom_client_id: Option<String>,
    /// external_zoom_email_optional property.
    pub external_zoom_email_optional: Option<bool>,
    /// external_zoom_enabled property.
    pub external_zoom_enabled: Option<bool>,
    /// external_zoom_secret property.
    pub external_zoom_secret: Option<String>,
    /// hook_after_user_created_enabled property.
    pub hook_after_user_created_enabled: Option<bool>,
    /// hook_after_user_created_secrets property.
    pub hook_after_user_created_secrets: Option<String>,
    /// hook_after_user_created_uri property.
    pub hook_after_user_created_uri: Option<String>,
    /// hook_before_user_created_enabled property.
    pub hook_before_user_created_enabled: Option<bool>,
    /// hook_before_user_created_secrets property.
    pub hook_before_user_created_secrets: Option<String>,
    /// hook_before_user_created_uri property.
    pub hook_before_user_created_uri: Option<String>,
    /// hook_custom_access_token_enabled property.
    pub hook_custom_access_token_enabled: Option<bool>,
    /// hook_custom_access_token_secrets property.
    pub hook_custom_access_token_secrets: Option<String>,
    /// hook_custom_access_token_uri property.
    pub hook_custom_access_token_uri: Option<String>,
    /// hook_mfa_verification_attempt_enabled property.
    pub hook_mfa_verification_attempt_enabled: Option<bool>,
    /// hook_mfa_verification_attempt_secrets property.
    pub hook_mfa_verification_attempt_secrets: Option<String>,
    /// hook_mfa_verification_attempt_uri property.
    pub hook_mfa_verification_attempt_uri: Option<String>,
    /// hook_password_verification_attempt_enabled property.
    pub hook_password_verification_attempt_enabled: Option<bool>,
    /// hook_password_verification_attempt_secrets property.
    pub hook_password_verification_attempt_secrets: Option<String>,
    /// hook_password_verification_attempt_uri property.
    pub hook_password_verification_attempt_uri: Option<String>,
    /// hook_send_email_enabled property.
    pub hook_send_email_enabled: Option<bool>,
    /// hook_send_email_secrets property.
    pub hook_send_email_secrets: Option<String>,
    /// hook_send_email_uri property.
    pub hook_send_email_uri: Option<String>,
    /// hook_send_sms_enabled property.
    pub hook_send_sms_enabled: Option<bool>,
    /// hook_send_sms_secrets property.
    pub hook_send_sms_secrets: Option<String>,
    /// hook_send_sms_uri property.
    pub hook_send_sms_uri: Option<String>,
    /// jwt_exp property.
    pub jwt_exp: Option<i64>,
    /// mailer_allow_unverified_email_sign_ins property.
    pub mailer_allow_unverified_email_sign_ins: Option<bool>,
    /// mailer_autoconfirm property.
    pub mailer_autoconfirm: Option<bool>,
    /// mailer_notifications_email_changed_enabled property.
    pub mailer_notifications_email_changed_enabled: Option<bool>,
    /// mailer_notifications_identity_linked_enabled property.
    pub mailer_notifications_identity_linked_enabled: Option<bool>,
    /// mailer_notifications_identity_unlinked_enabled property.
    pub mailer_notifications_identity_unlinked_enabled: Option<bool>,
    /// mailer_notifications_mfa_factor_enrolled_enabled property.
    pub mailer_notifications_mfa_factor_enrolled_enabled: Option<bool>,
    /// mailer_notifications_mfa_factor_unenrolled_enabled property.
    pub mailer_notifications_mfa_factor_unenrolled_enabled: Option<bool>,
    /// mailer_notifications_password_changed_enabled property.
    pub mailer_notifications_password_changed_enabled: Option<bool>,
    /// mailer_notifications_phone_changed_enabled property.
    pub mailer_notifications_phone_changed_enabled: Option<bool>,
    /// mailer_otp_exp property.
    pub mailer_otp_exp: Option<i64>,
    /// mailer_otp_length property.
    pub mailer_otp_length: Option<i64>,
    /// mailer_secure_email_change_enabled property.
    pub mailer_secure_email_change_enabled: Option<bool>,
    /// mailer_subjects_confirmation property.
    pub mailer_subjects_confirmation: Option<String>,
    /// mailer_subjects_email_change property.
    pub mailer_subjects_email_change: Option<String>,
    /// mailer_subjects_email_changed_notification property.
    pub mailer_subjects_email_changed_notification: Option<String>,
    /// mailer_subjects_identity_linked_notification property.
    pub mailer_subjects_identity_linked_notification: Option<String>,
    /// mailer_subjects_identity_unlinked_notification property.
    pub mailer_subjects_identity_unlinked_notification: Option<String>,
    /// mailer_subjects_invite property.
    pub mailer_subjects_invite: Option<String>,
    /// mailer_subjects_magic_link property.
    pub mailer_subjects_magic_link: Option<String>,
    /// mailer_subjects_mfa_factor_enrolled_notification property.
    pub mailer_subjects_mfa_factor_enrolled_notification: Option<String>,
    /// mailer_subjects_mfa_factor_unenrolled_notification property.
    pub mailer_subjects_mfa_factor_unenrolled_notification: Option<String>,
    /// mailer_subjects_password_changed_notification property.
    pub mailer_subjects_password_changed_notification: Option<String>,
    /// mailer_subjects_phone_changed_notification property.
    pub mailer_subjects_phone_changed_notification: Option<String>,
    /// mailer_subjects_reauthentication property.
    pub mailer_subjects_reauthentication: Option<String>,
    /// mailer_subjects_recovery property.
    pub mailer_subjects_recovery: Option<String>,
    /// mailer_templates_confirmation_content property.
    pub mailer_templates_confirmation_content: Option<String>,
    /// mailer_templates_email_change_content property.
    pub mailer_templates_email_change_content: Option<String>,
    /// mailer_templates_email_changed_notification_content property.
    pub mailer_templates_email_changed_notification_content: Option<String>,
    /// mailer_templates_identity_linked_notification_content property.
    pub mailer_templates_identity_linked_notification_content: Option<String>,
    /// mailer_templates_identity_unlinked_notification_content property.
    pub mailer_templates_identity_unlinked_notification_content: Option<String>,
    /// mailer_templates_invite_content property.
    pub mailer_templates_invite_content: Option<String>,
    /// mailer_templates_magic_link_content property.
    pub mailer_templates_magic_link_content: Option<String>,
    /// mailer_templates_mfa_factor_enrolled_notification_content property.
    pub mailer_templates_mfa_factor_enrolled_notification_content: Option<String>,
    /// mailer_templates_mfa_factor_unenrolled_notification_content property.
    pub mailer_templates_mfa_factor_unenrolled_notification_content: Option<String>,
    /// mailer_templates_password_changed_notification_content property.
    pub mailer_templates_password_changed_notification_content: Option<String>,
    /// mailer_templates_phone_changed_notification_content property.
    pub mailer_templates_phone_changed_notification_content: Option<String>,
    /// mailer_templates_reauthentication_content property.
    pub mailer_templates_reauthentication_content: Option<String>,
    /// mailer_templates_recovery_content property.
    pub mailer_templates_recovery_content: Option<String>,
    /// mfa_max_enrolled_factors property.
    pub mfa_max_enrolled_factors: Option<i64>,
    /// mfa_phone_enroll_enabled property.
    pub mfa_phone_enroll_enabled: Option<bool>,
    /// mfa_phone_max_frequency property.
    pub mfa_phone_max_frequency: Option<i64>,
    /// mfa_phone_otp_length property.
    pub mfa_phone_otp_length: Option<i64>,
    /// mfa_phone_template property.
    pub mfa_phone_template: Option<String>,
    /// mfa_phone_verify_enabled property.
    pub mfa_phone_verify_enabled: Option<bool>,
    /// mfa_totp_enroll_enabled property.
    pub mfa_totp_enroll_enabled: Option<bool>,
    /// mfa_totp_verify_enabled property.
    pub mfa_totp_verify_enabled: Option<bool>,
    /// mfa_web_authn_enroll_enabled property.
    pub mfa_web_authn_enroll_enabled: Option<bool>,
    /// mfa_web_authn_verify_enabled property.
    pub mfa_web_authn_verify_enabled: Option<bool>,
    /// nimbus_oauth_client_id property.
    pub nimbus_oauth_client_id: Option<String>,
    /// nimbus_oauth_client_secret property.
    pub nimbus_oauth_client_secret: Option<String>,
    /// oauth_server_allow_dynamic_registration property.
    pub oauth_server_allow_dynamic_registration: Option<bool>,
    /// oauth_server_authorization_path property.
    pub oauth_server_authorization_path: Option<String>,
    /// oauth_server_enabled property.
    pub oauth_server_enabled: Option<bool>,
    /// passkey_enabled property.
    pub passkey_enabled: Option<bool>,
    /// password_hibp_enabled property.
    pub password_hibp_enabled: Option<bool>,
    /// password_min_length property.
    pub password_min_length: Option<i64>,
    /// password_required_characters property.
    pub password_required_characters: Option<String>,
    /// rate_limit_anonymous_users property.
    pub rate_limit_anonymous_users: Option<i64>,
    /// rate_limit_email_sent property.
    pub rate_limit_email_sent: Option<i64>,
    /// rate_limit_otp property.
    pub rate_limit_otp: Option<i64>,
    /// rate_limit_sms_sent property.
    pub rate_limit_sms_sent: Option<i64>,
    /// rate_limit_token_refresh property.
    pub rate_limit_token_refresh: Option<i64>,
    /// rate_limit_verify property.
    pub rate_limit_verify: Option<i64>,
    /// rate_limit_web3 property.
    pub rate_limit_web3: Option<i64>,
    /// refresh_token_rotation_enabled property.
    pub refresh_token_rotation_enabled: Option<bool>,
    /// saml_enabled property.
    pub saml_enabled: Option<bool>,
    /// saml_external_url property.
    pub saml_external_url: Option<String>,
    /// security_captcha_enabled property.
    pub security_captcha_enabled: Option<bool>,
    /// security_captcha_provider property.
    pub security_captcha_provider: Option<String>,
    /// security_captcha_secret property.
    pub security_captcha_secret: Option<String>,
    /// security_manual_linking_enabled property.
    pub security_manual_linking_enabled: Option<bool>,
    /// security_refresh_token_reuse_interval property.
    pub security_refresh_token_reuse_interval: Option<i64>,
    /// security_sb_forwarded_for_enabled property.
    pub security_sb_forwarded_for_enabled: Option<bool>,
    /// security_update_password_require_reauthentication property.
    pub security_update_password_require_reauthentication: Option<bool>,
    /// sessions_inactivity_timeout property.
    pub sessions_inactivity_timeout: Option<f64>,
    /// sessions_single_per_user property.
    pub sessions_single_per_user: Option<bool>,
    /// sessions_tags property.
    pub sessions_tags: Option<String>,
    /// sessions_timebox property.
    pub sessions_timebox: Option<f64>,
    /// site_url property.
    pub site_url: Option<String>,
    /// sms_autoconfirm property.
    pub sms_autoconfirm: Option<bool>,
    /// sms_max_frequency property.
    pub sms_max_frequency: Option<i64>,
    /// sms_messagebird_access_key property.
    pub sms_messagebird_access_key: Option<String>,
    /// sms_messagebird_originator property.
    pub sms_messagebird_originator: Option<String>,
    /// sms_otp_exp property.
    pub sms_otp_exp: Option<i64>,
    /// sms_otp_length property.
    pub sms_otp_length: Option<i64>,
    /// sms_provider property.
    pub sms_provider: Option<String>,
    /// sms_template property.
    pub sms_template: Option<String>,
    /// sms_test_otp property.
    pub sms_test_otp: Option<String>,
    /// sms_test_otp_valid_until property.
    pub sms_test_otp_valid_until: Option<String>,
    /// sms_textlocal_api_key property.
    pub sms_textlocal_api_key: Option<String>,
    /// sms_textlocal_sender property.
    pub sms_textlocal_sender: Option<String>,
    /// sms_twilio_account_sid property.
    pub sms_twilio_account_sid: Option<String>,
    /// sms_twilio_auth_token property.
    pub sms_twilio_auth_token: Option<String>,
    /// sms_twilio_content_sid property.
    pub sms_twilio_content_sid: Option<String>,
    /// sms_twilio_message_service_sid property.
    pub sms_twilio_message_service_sid: Option<String>,
    /// sms_twilio_verify_account_sid property.
    pub sms_twilio_verify_account_sid: Option<String>,
    /// sms_twilio_verify_auth_token property.
    pub sms_twilio_verify_auth_token: Option<String>,
    /// sms_twilio_verify_message_service_sid property.
    pub sms_twilio_verify_message_service_sid: Option<String>,
    /// sms_vonage_api_key property.
    pub sms_vonage_api_key: Option<String>,
    /// sms_vonage_api_secret property.
    pub sms_vonage_api_secret: Option<String>,
    /// sms_vonage_from property.
    pub sms_vonage_from: Option<String>,
    /// smtp_admin_email property.
    pub smtp_admin_email: Option<String>,
    /// smtp_host property.
    pub smtp_host: Option<String>,
    /// smtp_max_frequency property.
    pub smtp_max_frequency: Option<i64>,
    /// smtp_pass property.
    pub smtp_pass: Option<String>,
    /// smtp_port property.
    pub smtp_port: Option<String>,
    /// smtp_sender_name property.
    pub smtp_sender_name: Option<String>,
    /// smtp_user property.
    pub smtp_user: Option<String>,
    /// uri_allow_list property.
    pub uri_allow_list: Option<String>,
    /// webauthn_rp_display_name property.
    pub webauthn_rp_display_name: Option<String>,
    /// webauthn_rp_id property.
    pub webauthn_rp_id: Option<String>,
    /// webauthn_rp_origins property.
    pub webauthn_rp_origins: Option<String>,
}

/// `OrganizationProjectClaimResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrganizationProjectClaimResponse {
    /// created_at property.
    pub created_at: String,
    /// created_by property.
    pub created_by: String,
    /// expires_at property.
    pub expires_at: String,
    /// preview property.
    pub preview: std::collections::HashMap<String, serde_json::Value>,
    /// project property.
    pub project: std::collections::HashMap<String, serde_json::Value>,
}

/// `UpdateSigningKeyBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateSigningKeyBody {
    /// status property.
    pub status: String,
}

/// `ThirdPartyAuth` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyAuth {
    /// custom_jwks property.
    pub custom_jwks: Option<serde_json::Value>,
    /// id property.
    pub id: String,
    /// inserted_at property.
    pub inserted_at: String,
    /// jwks_url property.
    pub jwks_url: Option<String>,
    /// oidc_issuer_url property.
    pub oidc_issuer_url: Option<String>,
    /// resolved_at property.
    pub resolved_at: Option<String>,
    /// resolved_jwks property.
    pub resolved_jwks: Option<serde_json::Value>,
    /// type property.
    pub r#type: String,
    /// updated_at property.
    pub updated_at: String,
}

/// `UpdateProviderResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateProviderResponse {
    /// created_at property.
    pub created_at: Option<String>,
    /// domains property.
    pub domains: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// id property.
    pub id: String,
    /// saml property.
    pub saml: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// updated_at property.
    pub updated_at: Option<String>,
}

/// `OAuthTokenResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuthTokenResponse {
    /// access_token property.
    pub access_token: String,
    /// expires_in property.
    pub expires_in: i64,
    /// refresh_token property.
    pub refresh_token: String,
    /// token_type property.
    pub token_type: String,
}

/// `OAuthRevokeTokenBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuthRevokeTokenBody {
    /// client_id property.
    pub client_id: String,
    /// client_secret property.
    pub client_secret: String,
    /// refresh_token property.
    pub refresh_token: String,
}

/// `SigningKeyResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SigningKeyResponse {
    /// algorithm property.
    pub algorithm: String,
    /// created_at property.
    pub created_at: String,
    /// id property.
    pub id: String,
    /// public_jwk property.
    pub public_jwk: Option<serde_json::Value>,
    /// status property.
    pub status: String,
    /// updated_at property.
    pub updated_at: String,
}

/// `DeleteProviderResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeleteProviderResponse {
    /// created_at property.
    pub created_at: Option<String>,
    /// domains property.
    pub domains: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// id property.
    pub id: String,
    /// saml property.
    pub saml: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// updated_at property.
    pub updated_at: Option<String>,
}

/// `ProjectClaimTokenResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectClaimTokenResponse {
    /// created_at property.
    pub created_at: String,
    /// created_by property.
    pub created_by: String,
    /// expires_at property.
    pub expires_at: String,
    /// token_alias property.
    pub token_alias: String,
}

/// `CreateProjectClaimTokenResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateProjectClaimTokenResponse {
    /// created_at property.
    pub created_at: String,
    /// created_by property.
    pub created_by: String,
    /// expires_at property.
    pub expires_at: String,
    /// token property.
    pub token: String,
    /// token_alias property.
    pub token_alias: String,
}

/// `UpdateProviderBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateProviderBody {
    /// attribute_mapping property.
    pub attribute_mapping: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// domains property.
    pub domains: Option<Vec<String>>,
    /// metadata_url property.
    pub metadata_url: Option<String>,
    /// metadata_xml property.
    pub metadata_xml: Option<String>,
    /// name_id_format property.
    pub name_id_format: Option<String>,
}

/// `GetProviderResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GetProviderResponse {
    /// created_at property.
    pub created_at: Option<String>,
    /// domains property.
    pub domains: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// id property.
    pub id: String,
    /// saml property.
    pub saml: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// updated_at property.
    pub updated_at: Option<String>,
}

/// `CreateThirdPartyAuthBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateThirdPartyAuthBody {
    /// custom_jwks property.
    pub custom_jwks: Option<serde_json::Value>,
    /// jwks_url property.
    pub jwks_url: Option<String>,
    /// oidc_issuer_url property.
    pub oidc_issuer_url: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`v1-authorize-user_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1AuthorizeUserArgs {
    /// Query parameter: `client_id`.
    pub client_id: Option<String>,
    /// Query parameter: `response_type`.
    pub response_type: Option<String>,
    /// Query parameter: `redirect_uri`.
    pub redirect_uri: Option<String>,
    /// Query parameter: `scope`.
    pub scope: Option<String>,
    /// Query parameter: `state`.
    pub state: Option<String>,
    /// Query parameter: `response_mode`.
    pub response_mode: Option<String>,
    /// Query parameter: `code_challenge`.
    pub code_challenge: Option<String>,
    /// Query parameter: `code_challenge_method`.
    pub code_challenge_method: Option<String>,
    /// Query parameter: `organization_slug`.
    pub organization_slug: Option<String>,
    /// Query parameter: `resource`.
    pub resource: Option<String>,
}

/// Arguments for [`v1-oauth-authorize-project-claim_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1OauthAuthorizeProjectClaimArgs {
    /// Query parameter: `project_ref`.
    pub project_ref: Option<String>,
    /// Query parameter: `client_id`.
    pub client_id: Option<String>,
    /// Query parameter: `response_type`.
    pub response_type: Option<String>,
    /// Query parameter: `redirect_uri`.
    pub redirect_uri: Option<String>,
    /// Query parameter: `state`.
    pub state: Option<String>,
    /// Query parameter: `response_mode`.
    pub response_mode: Option<String>,
    /// Query parameter: `code_challenge`.
    pub code_challenge: Option<String>,
    /// Query parameter: `code_challenge_method`.
    pub code_challenge_method: Option<String>,
}

/// Arguments for [`v1-revoke-token_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RevokeTokenArgs {
    /// Request body.
    pub body: OAuthRevokeTokenBody,
}

/// Arguments for [`v1-get-organization-project-claim_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetOrganizationProjectClaimArgs {
    /// Path parameter: `slug`.
    pub slug: String,
    /// Path parameter: `token`.
    pub token: String,
}

/// Arguments for [`v1-claim-project-for-organization_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ClaimProjectForOrganizationArgs {
    /// Path parameter: `slug`.
    pub slug: String,
    /// Path parameter: `token`.
    pub token: String,
}

/// Arguments for [`v1-get-project-claim-token_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectClaimTokenArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-create-project-claim-token_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectClaimTokenArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-delete-project-claim-token_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteProjectClaimTokenArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-get-auth-service-config_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetAuthServiceConfigArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-update-auth-service-config_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateAuthServiceConfigArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Request body.
    pub body: UpdateAuthConfigBody,
}

/// Arguments for [`v1-get-project-signing-keys_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectSigningKeysArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-create-project-signing-key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectSigningKeyArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Request body.
    pub body: CreateSigningKeyBody,
}

/// Arguments for [`v1-get-legacy-signing-key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetLegacySigningKeyArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-create-legacy-signing-key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateLegacySigningKeyArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-get-project-signing-key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectSigningKeyArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-update-project-signing-key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateProjectSigningKeyArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Request body.
    pub body: UpdateSigningKeyBody,
}

/// Arguments for [`v1-remove-project-signing-key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1RemoveProjectSigningKeyArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-list-all-sso-provider_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListAllSsoProviderArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-create-a-sso-provider_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateASsoProviderArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Request body.
    pub body: CreateProviderBody,
}

/// Arguments for [`v1-get-a-sso-provider_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetASsoProviderArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Path parameter: `provider_id`.
    pub provider_id: String,
}

/// Arguments for [`v1-update-a-sso-provider_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1UpdateASsoProviderArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Path parameter: `provider_id`.
    pub provider_id: String,
    /// Request body.
    pub body: UpdateProviderBody,
}

/// Arguments for [`v1-delete-a-sso-provider_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteASsoProviderArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Path parameter: `provider_id`.
    pub provider_id: String,
}

/// Arguments for [`v1-list-project-tpa-integrations_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1ListProjectTpaIntegrationsArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
}

/// Arguments for [`v1-create-project-tpa-integration_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1CreateProjectTpaIntegrationArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Request body.
    pub body: CreateThirdPartyAuthBody,
}

/// Arguments for [`v1-get-project-tpa-integration_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1GetProjectTpaIntegrationArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Path parameter: `tpa_id`.
    pub tpa_id: String,
}

/// Arguments for [`v1-delete-project-tpa-integration_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct V1DeleteProjectTpaIntegrationArgs {
    /// Path parameter: `ref`.
    pub r#ref: String,
    /// Path parameter: `tpa_id`.
    pub tpa_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/oauth/authorize
// -----------------------------------------------------------------------------

/// GET /v1/oauth/authorize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_authorize_user_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_authorize_user_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.supabase.com/v1/oauth/authorize",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/oauth/authorize/project-claim
// -----------------------------------------------------------------------------

/// GET /v1/oauth/authorize/project-claim.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_oauth_authorize_project_claim_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_oauth_authorize_project_claim_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.supabase.com/v1/oauth/authorize/project-claim",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/oauth/revoke
// -----------------------------------------------------------------------------

/// POST /v1/oauth/revoke.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_revoke_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_revoke_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1RevokeTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.supabase.com/v1/oauth/revoke",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/oauth/token
// -----------------------------------------------------------------------------

/// POST /v1/oauth/token.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_exchange_oauth_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_exchange_oauth_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OAuthTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.supabase.com/v1/oauth/token",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OAuthTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/organizations/{slug}/project-claim/{token}
// -----------------------------------------------------------------------------

/// GET /v1/organizations/{slug}/project-claim/{token}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_organization_project_claim_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_organization_project_claim_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetOrganizationProjectClaimArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrganizationProjectClaimResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/project-claim/{}",
        args.slug, args.token,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OrganizationProjectClaimResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/organizations/{slug}/project-claim/{token}
// -----------------------------------------------------------------------------

/// POST /v1/organizations/{slug}/project-claim/{token}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_claim_project_for_organization_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_claim_project_for_organization_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1ClaimProjectForOrganizationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/organizations/{}/project-claim/{}",
        args.slug, args.token,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/claim-token
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/claim-token.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_project_claim_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_project_claim_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetProjectClaimTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectClaimTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        args.r#ref,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProjectClaimTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/projects/{ref}/claim-token
// -----------------------------------------------------------------------------

/// POST /v1/projects/{ref}/claim-token.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_create_project_claim_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_create_project_claim_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1CreateProjectClaimTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateProjectClaimTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        args.r#ref,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CreateProjectClaimTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /v1/projects/{ref}/claim-token
// -----------------------------------------------------------------------------

/// DELETE /v1/projects/{ref}/claim-token.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_delete_project_claim_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_delete_project_claim_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1DeleteProjectClaimTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/claim-token",
        args.r#ref,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_auth_service_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_auth_service_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetAuthServiceConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuthConfigResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth",
        args.r#ref,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AuthConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /v1/projects/{ref}/config/auth
// -----------------------------------------------------------------------------

/// PATCH /v1/projects/{ref}/config/auth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_update_auth_service_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_update_auth_service_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1UpdateAuthServiceConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuthConfigResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth",
        args.r#ref,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AuthConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/signing-keys
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/signing-keys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_project_signing_keys_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_project_signing_keys_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetProjectSigningKeysArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeysResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys",
        args.r#ref,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeysResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/projects/{ref}/config/auth/signing-keys
// -----------------------------------------------------------------------------

/// POST /v1/projects/{ref}/config/auth/signing-keys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_create_project_signing_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_create_project_signing_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1CreateProjectSigningKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys",
        args.r#ref,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/signing-keys/legacy
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/signing-keys/legacy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_legacy_signing_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_legacy_signing_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetLegacySigningKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/legacy",
        args.r#ref,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/projects/{ref}/config/auth/signing-keys/legacy
// -----------------------------------------------------------------------------

/// POST /v1/projects/{ref}/config/auth/signing-keys/legacy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_create_legacy_signing_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_create_legacy_signing_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1CreateLegacySigningKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/legacy",
        args.r#ref,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/signing-keys/{id}
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/signing-keys/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_project_signing_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_project_signing_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetProjectSigningKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        args.r#ref, args.id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}
// -----------------------------------------------------------------------------

/// PATCH /v1/projects/{ref}/config/auth/signing-keys/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_update_project_signing_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_update_project_signing_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1UpdateProjectSigningKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        args.r#ref, args.id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}
// -----------------------------------------------------------------------------

/// DELETE /v1/projects/{ref}/config/auth/signing-keys/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_remove_project_signing_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_remove_project_signing_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1RemoveProjectSigningKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SigningKeyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/signing-keys/{}",
        args.r#ref, args.id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SigningKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/sso/providers
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/sso/providers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_list_all_sso_provider_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_list_all_sso_provider_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1ListAllSsoProviderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProvidersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers",
        args.r#ref,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProvidersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/projects/{ref}/config/auth/sso/providers
// -----------------------------------------------------------------------------

/// POST /v1/projects/{ref}/config/auth/sso/providers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_create_a_sso_provider_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_create_a_sso_provider_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1CreateASsoProviderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateProviderResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers",
        args.r#ref,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CreateProviderResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/sso/providers/{provider_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_a_sso_provider_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_a_sso_provider_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetASsoProviderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetProviderResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        args.r#ref, args.provider_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetProviderResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
// -----------------------------------------------------------------------------

/// PUT /v1/projects/{ref}/config/auth/sso/providers/{provider_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_update_a_sso_provider_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_update_a_sso_provider_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1UpdateASsoProviderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UpdateProviderResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        args.r#ref, args.provider_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UpdateProviderResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}
// -----------------------------------------------------------------------------

/// DELETE /v1/projects/{ref}/config/auth/sso/providers/{provider_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_delete_a_sso_provider_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_delete_a_sso_provider_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1DeleteASsoProviderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteProviderResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/sso/providers/{}",
        args.r#ref, args.provider_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeleteProviderResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/third-party-auth
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/third-party-auth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_list_project_tpa_integrations_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_list_project_tpa_integrations_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1ListProjectTpaIntegrationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth",
        args.r#ref,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: serde_json::Value =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/projects/{ref}/config/auth/third-party-auth
// -----------------------------------------------------------------------------

/// POST /v1/projects/{ref}/config/auth/third-party-auth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_create_project_tpa_integration_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_create_project_tpa_integration_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1CreateProjectTpaIntegrationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThirdPartyAuth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth",
        args.r#ref,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
// -----------------------------------------------------------------------------

/// GET /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_get_project_tpa_integration_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_get_project_tpa_integration_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1GetProjectTpaIntegrationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThirdPartyAuth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth/{}",
        args.r#ref, args.tpa_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}
// -----------------------------------------------------------------------------

/// DELETE /v1/projects/{ref}/config/auth/third-party-auth/{tpa_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = v1_delete_project_tpa_integration_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn v1_delete_project_tpa_integration_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &V1DeleteProjectTpaIntegrationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThirdPartyAuth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.supabase.com/v1/projects/{}/config/auth/third-party-auth/{}",
        args.r#ref, args.tpa_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThirdPartyAuth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
