//! Auto-generated API module for fly_io volumes.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `fly_io_volumes `

#![cfg(feature = "fly_io_volumes")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `OrgVolumesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrgVolumesResponse {
    /// last_updated_at property.
    pub last_updated_at: Option<String>,
    /// last_volume_id property.
    pub last_volume_id: Option<String>,
    /// next_cursor property.
    pub next_cursor: Option<String>,
    /// volumes property.
    pub volumes: Option<Vec<OrgVolume>>,
}

/// `OrgVolume` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrgVolume {
    /// app_name property.
    pub app_name: Option<String>,
    /// attached_alloc_id property.
    pub attached_alloc_id: Option<String>,
    /// attached_machine_id property.
    pub attached_machine_id: Option<String>,
    /// auto_backup_enabled property.
    pub auto_backup_enabled: Option<bool>,
    /// block_size property.
    pub block_size: Option<i64>,
    /// blocks property.
    pub blocks: Option<i64>,
    /// blocks_avail property.
    pub blocks_avail: Option<i64>,
    /// blocks_free property.
    pub blocks_free: Option<i64>,
    /// bytes_total property.
    pub bytes_total: Option<i64>,
    /// bytes_used property.
    pub bytes_used: Option<i64>,
    /// created_at property.
    pub created_at: Option<String>,
    /// encrypted property.
    pub encrypted: Option<bool>,
    /// fstype property.
    pub fstype: Option<String>,
    /// host_status property.
    pub host_status: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// size_gb property.
    pub size_gb: Option<i64>,
    /// snapshot_retention property.
    pub snapshot_retention: Option<i64>,
    /// state property.
    pub state: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated_at property.
    pub updated_at: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`Volumes_org_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumesOrgListArgs {
    /// Path parameter: `org_slug`.
    pub org_slug: String,
    /// Query parameter: `include_deleted`.
    pub include_deleted: Option<String>,
    /// Query parameter: `region`.
    pub region: Option<String>,
    /// Query parameter: `state`.
    pub state: Option<String>,
    /// Query parameter: `summary`.
    pub summary: Option<String>,
    /// Query parameter: `updated_after`.
    pub updated_after: Option<String>,
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /orgs/{org_slug}/volumes
// -----------------------------------------------------------------------------

/// GET /orgs/{org_slug}/volumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = volumes_org_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn volumes_org_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &VolumesOrgListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrgVolumesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.machines.dev/v1/orgs/{}/volumes", args.org_slug,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OrgVolumesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
