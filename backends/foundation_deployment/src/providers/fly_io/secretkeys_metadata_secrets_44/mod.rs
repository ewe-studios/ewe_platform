//! Auto-generated API module for fly_io secretkeys_metadata_secrets_machines_volumes.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `fly_io_secretkeys_metadata_secrets_44 `

#![cfg(feature = "fly_io_secretkeys_metadata_secrets_44")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// AppSecret response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppSecret {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// AppSecrets response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppSecrets {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// AppSecretsUpdateRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppSecretsUpdateRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// AppSecretsUpdateResp response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppSecretsUpdateResp {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// CreateMachineRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateMachineRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// CreateVolumeRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreateVolumeRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeleteAppSecretResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeleteAppSecretResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeleteSecretkeyResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeleteSecretkeyResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Machine response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Machine {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// MetadataValueResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetadataValueResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// OrgMachinesResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrgMachinesResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// OrgVolumesResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrgVolumesResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SecretKey response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecretKey {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SecretKeys response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecretKeys {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SetAppSecretRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetAppSecretRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SetAppSecretResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetAppSecretResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SetSecretkeyRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetSecretkeyRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SetSecretkeyResponse response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetSecretkeyResponse {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// UpdateMachineRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateMachineRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// UpdateMetadataRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateMetadataRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// UpdateVolumeRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateVolumeRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// UpsertMetadataKeyRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpsertMetadataKeyRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Volume response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volume {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`Secretkeys_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretkeysListArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Query parameter: min_version
    pub min_version: Option<String>,
    /// Query parameter: types
    pub types: Option<String>,
}

/// Arguments for [`Secretkey_get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretkeyGetArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: secret_name
    pub secret_name: String,
    /// Query parameter: min_version
    pub min_version: Option<String>,
}

/// Arguments for [`Secretkey_set_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretkeySetArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: secret_name
    pub secret_name: String,
    /// Request body.
    pub body: SetSecretkeyRequest,
}

/// Arguments for [`Secretkey_delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretkeyDeleteArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: secret_name
    pub secret_name: String,
}

/// Arguments for [`Machines_show_metadata_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesShowMetadataArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
}

/// Arguments for [`Machines_update_metadata_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesUpdateMetadataArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
    /// Request body.
    pub body: UpdateMetadataRequest,
}

/// Arguments for [`Machines_get_metadata_key_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesGetMetadataKeyArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
    /// Path parameter: key
    pub key: String,
}

/// Arguments for [`Machines_upsert_metadata_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesUpsertMetadataArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
    /// Path parameter: key
    pub key: String,
    /// Request body.
    pub body: UpsertMetadataKeyRequest,
}

/// Arguments for [`Machines_delete_metadata_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesDeleteMetadataArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
    /// Path parameter: key
    pub key: String,
}

/// Arguments for [`Secrets_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretsListArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Query parameter: min_version
    pub min_version: Option<String>,
    /// Query parameter: show_secrets
    pub show_secrets: Option<String>,
}

/// Arguments for [`Secrets_update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretsUpdateArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Request body.
    pub body: AppSecretsUpdateRequest,
}

/// Arguments for [`Secret_get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretGetArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: secret_name
    pub secret_name: String,
    /// Query parameter: min_version
    pub min_version: Option<String>,
    /// Query parameter: show_secrets
    pub show_secrets: Option<String>,
}

/// Arguments for [`Secret_create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretCreateArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: secret_name
    pub secret_name: String,
    /// Request body.
    pub body: SetAppSecretRequest,
}

/// Arguments for [`Secret_delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecretDeleteArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: secret_name
    pub secret_name: String,
}

/// Arguments for [`Machines_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesListArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Query parameter: include_deleted
    pub include_deleted: Option<String>,
    /// Query parameter: region
    pub region: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: summary
    pub summary: Option<String>,
}

/// Arguments for [`Machines_create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesCreateArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Request body.
    pub body: CreateMachineRequest,
}

/// Arguments for [`Machines_show_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesShowArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
}

/// Arguments for [`Machines_update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesUpdateArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
    /// Request body.
    pub body: UpdateMachineRequest,
}

/// Arguments for [`Machines_delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesDeleteArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: machine_id
    pub machine_id: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// Arguments for [`Machines_org_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MachinesOrgListArgs {
    /// Path parameter: org_slug
    pub org_slug: String,
    /// Query parameter: include_deleted
    pub include_deleted: Option<String>,
    /// Query parameter: region
    pub region: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: summary
    pub summary: Option<String>,
    /// Query parameter: updated_after
    pub updated_after: Option<String>,
    /// Query parameter: cursor
    pub cursor: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
}

/// Arguments for [`Volumes_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumesListArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Query parameter: summary
    pub summary: Option<String>,
}

/// Arguments for [`Volumes_create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumesCreateArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Request body.
    pub body: CreateVolumeRequest,
}

/// Arguments for [`Volumes_get_by_id_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumesGetByIdArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: volume_id
    pub volume_id: String,
}

/// Arguments for [`Volumes_update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumesUpdateArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: volume_id
    pub volume_id: String,
    /// Request body.
    pub body: UpdateVolumeRequest,
}

/// Arguments for [`Volume_delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumeDeleteArgs {
    /// Path parameter: app_name
    pub app_name: String,
    /// Path parameter: volume_id
    pub volume_id: String,
}

/// Arguments for [`Volumes_org_list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VolumesOrgListArgs {
    /// Path parameter: org_slug
    pub org_slug: String,
    /// Query parameter: include_deleted
    pub include_deleted: Option<String>,
    /// Query parameter: region
    pub region: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: summary
    pub summary: Option<String>,
    /// Query parameter: updated_after
    pub updated_after: Option<String>,
    /// Query parameter: cursor
    pub cursor: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/secretkeys
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/secretkeys - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secretkeys_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretkeysListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secretkeys",
        args.app_name,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/secretkeys - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secretkeys_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecretKeys>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SecretKeys = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/secretkeys - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secretkeys_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretKeys>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = secretkeys_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/secretkeys - convenience function.
///
/// Combines builder and execute in one call.

pub fn secretkeys_list(
    client: &SimpleHttpClient,
    args: &SecretkeysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretKeys>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secretkeys_list_builder(client, args)?;
    secretkeys_list_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/secretkeys/{secret_name}
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/secretkeys/{secret_name} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secretkey_get_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretkeyGetArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secretkeys/{secret_name}",
        args.app_name, args.secret_name,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/secretkeys/{secret_name} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secretkey_get_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecretKey>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SecretKey = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/secretkeys/{secret_name} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secretkey_get_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretKey>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = secretkey_get_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/secretkeys/{secret_name} - convenience function.
///
/// Combines builder and execute in one call.

pub fn secretkey_get(
    client: &SimpleHttpClient,
    args: &SecretkeyGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecretKey>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secretkey_get_builder(client, args)?;
    secretkey_get_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/secretkeys/{secret_name}
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/secretkeys/{secret_name} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secretkey_set_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretkeySetArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secretkeys/{secret_name}",
        args.app_name, args.secret_name,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/secretkeys/{secret_name} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secretkey_set_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetSecretkeyResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SetSecretkeyResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/secretkeys/{secret_name} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secretkey_set_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetSecretkeyResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = secretkey_set_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/secretkeys/{secret_name} - convenience function.
///
/// Combines builder and execute in one call.

pub fn secretkey_set(
    client: &SimpleHttpClient,
    args: &SecretkeySetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetSecretkeyResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secretkey_set_builder(client, args)?;
    secretkey_set_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /apps/{app_name}/secretkeys/{secret_name}
// -----------------------------------------------------------------------------

/// DELETE /apps/{app_name}/secretkeys/{secret_name} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secretkey_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretkeyDeleteArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secretkeys/{secret_name}",
        args.app_name, args.secret_name,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /apps/{app_name}/secretkeys/{secret_name} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secretkey_delete_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteSecretkeyResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeleteSecretkeyResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /apps/{app_name}/secretkeys/{secret_name} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secretkey_delete_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteSecretkeyResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = secretkey_delete_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /apps/{app_name}/secretkeys/{secret_name} - convenience function.
///
/// Combines builder and execute in one call.

pub fn secretkey_delete(
    client: &SimpleHttpClient,
    args: &SecretkeyDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteSecretkeyResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secretkey_delete_builder(client, args)?;
    secretkey_delete_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/machines/{machine_id}/metadata
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/machines/{machine_id}/metadata - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_show_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesShowMetadataArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}/metadata",
        args.app_name, args.machine_id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/machines/{machine_id}/metadata - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_show_metadata_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/machines/{machine_id}/metadata - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_show_metadata_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_show_metadata_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/machines/{machine_id}/metadata - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_show_metadata(
    client: &SimpleHttpClient,
    args: &MachinesShowMetadataArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_show_metadata_builder(client, args)?;
    machines_show_metadata_execute(builder)
}

// -----------------------------------------------------------------------------
// PUT /apps/{app_name}/machines/{machine_id}/metadata
// -----------------------------------------------------------------------------

/// PUT /apps/{app_name}/machines/{machine_id}/metadata - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_update_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesUpdateMetadataArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}/metadata",
        args.app_name, args.machine_id,
    );

    let builder = client
        .put(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /apps/{app_name}/machines/{machine_id}/metadata - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_update_metadata_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// PUT /apps/{app_name}/machines/{machine_id}/metadata - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_update_metadata_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_update_metadata_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /apps/{app_name}/machines/{machine_id}/metadata - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_update_metadata(
    client: &SimpleHttpClient,
    args: &MachinesUpdateMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_update_metadata_builder(client, args)?;
    machines_update_metadata_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/machines/{machine_id}/metadata/{key}
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/machines/{machine_id}/metadata/{key} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_get_metadata_key_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesGetMetadataKeyArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}/metadata/{key}",
        args.app_name, args.machine_id, args.key,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/machines/{machine_id}/metadata/{key} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_get_metadata_key_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MetadataValueResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: MetadataValueResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/machines/{machine_id}/metadata/{key} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_get_metadata_key_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MetadataValueResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_get_metadata_key_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/machines/{machine_id}/metadata/{key} - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_get_metadata_key(
    client: &SimpleHttpClient,
    args: &MachinesGetMetadataKeyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MetadataValueResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_get_metadata_key_builder(client, args)?;
    machines_get_metadata_key_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/machines/{machine_id}/metadata/{key}
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/machines/{machine_id}/metadata/{key} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_upsert_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesUpsertMetadataArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}/metadata/{key}",
        args.app_name, args.machine_id, args.key,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/machines/{machine_id}/metadata/{key} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_upsert_metadata_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/machines/{machine_id}/metadata/{key} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_upsert_metadata_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_upsert_metadata_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/machines/{machine_id}/metadata/{key} - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_upsert_metadata(
    client: &SimpleHttpClient,
    args: &MachinesUpsertMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_upsert_metadata_builder(client, args)?;
    machines_upsert_metadata_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /apps/{app_name}/machines/{machine_id}/metadata/{key}
// -----------------------------------------------------------------------------

/// DELETE /apps/{app_name}/machines/{machine_id}/metadata/{key} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_delete_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesDeleteMetadataArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}/metadata/{key}",
        args.app_name, args.machine_id, args.key,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /apps/{app_name}/machines/{machine_id}/metadata/{key} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_delete_metadata_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /apps/{app_name}/machines/{machine_id}/metadata/{key} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_delete_metadata_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_delete_metadata_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /apps/{app_name}/machines/{machine_id}/metadata/{key} - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_delete_metadata(
    client: &SimpleHttpClient,
    args: &MachinesDeleteMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_delete_metadata_builder(client, args)?;
    machines_delete_metadata_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/secrets
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/secrets - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secrets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretsListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secrets",
        args.app_name,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/secrets - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secrets_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppSecrets>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: AppSecrets = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/secrets - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secrets_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppSecrets>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = secrets_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/secrets - convenience function.
///
/// Combines builder and execute in one call.

pub fn secrets_list(
    client: &SimpleHttpClient,
    args: &SecretsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppSecrets>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secrets_list_builder(client, args)?;
    secrets_list_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/secrets
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/secrets - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secrets_update_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretsUpdateArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secrets",
        args.app_name,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/secrets - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secrets_update_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppSecretsUpdateResp>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: AppSecretsUpdateResp = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/secrets - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secrets_update_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AppSecretsUpdateResp>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = secrets_update_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/secrets - convenience function.
///
/// Combines builder and execute in one call.

pub fn secrets_update(
    client: &SimpleHttpClient,
    args: &SecretsUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AppSecretsUpdateResp>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secrets_update_builder(client, args)?;
    secrets_update_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/secrets/{secret_name}
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/secrets/{secret_name} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secret_get_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretGetArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secrets/{secret_name}",
        args.app_name, args.secret_name,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/secrets/{secret_name} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secret_get_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppSecret>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: AppSecret = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/secrets/{secret_name} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secret_get_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppSecret>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = secret_get_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/secrets/{secret_name} - convenience function.
///
/// Combines builder and execute in one call.

pub fn secret_get(
    client: &SimpleHttpClient,
    args: &SecretGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppSecret>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secret_get_builder(client, args)?;
    secret_get_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/secrets/{secret_name}
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/secrets/{secret_name} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secret_create_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretCreateArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secrets/{secret_name}",
        args.app_name, args.secret_name,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/secrets/{secret_name} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secret_create_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetAppSecretResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SetAppSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/secrets/{secret_name} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secret_create_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetAppSecretResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = secret_create_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/secrets/{secret_name} - convenience function.
///
/// Combines builder and execute in one call.

pub fn secret_create(
    client: &SimpleHttpClient,
    args: &SecretCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetAppSecretResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secret_create_builder(client, args)?;
    secret_create_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /apps/{app_name}/secrets/{secret_name}
// -----------------------------------------------------------------------------

/// DELETE /apps/{app_name}/secrets/{secret_name} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn secret_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &SecretDeleteArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/secrets/{secret_name}",
        args.app_name, args.secret_name,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /apps/{app_name}/secrets/{secret_name} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn secret_delete_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteAppSecretResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeleteAppSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /apps/{app_name}/secrets/{secret_name} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn secret_delete_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteAppSecretResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = secret_delete_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /apps/{app_name}/secrets/{secret_name} - convenience function.
///
/// Combines builder and execute in one call.

pub fn secret_delete(
    client: &SimpleHttpClient,
    args: &SecretDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteAppSecretResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = secret_delete_builder(client, args)?;
    secret_delete_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/machines
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/machines - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines",
        args.app_name,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/machines - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/machines - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/machines - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_list(
    client: &SimpleHttpClient,
    args: &MachinesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_list_builder(client, args)?;
    machines_list_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/machines
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/machines - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_create_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesCreateArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines",
        args.app_name,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/machines - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_create_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Machine>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Machine = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/machines - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_create_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_create_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/machines - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_create(
    client: &SimpleHttpClient,
    args: &MachinesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_create_builder(client, args)?;
    machines_create_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/machines/{machine_id}
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/machines/{machine_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_show_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesShowArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}",
        args.app_name, args.machine_id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/machines/{machine_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_show_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Machine>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Machine = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/machines/{machine_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_show_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_show_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/machines/{machine_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_show(
    client: &SimpleHttpClient,
    args: &MachinesShowArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_show_builder(client, args)?;
    machines_show_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/machines/{machine_id}
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/machines/{machine_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_update_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesUpdateArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}",
        args.app_name, args.machine_id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/machines/{machine_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_update_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Machine>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Machine = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/machines/{machine_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_update_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_update_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/machines/{machine_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_update(
    client: &SimpleHttpClient,
    args: &MachinesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_update_builder(client, args)?;
    machines_update_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /apps/{app_name}/machines/{machine_id}
// -----------------------------------------------------------------------------

/// DELETE /apps/{app_name}/machines/{machine_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesDeleteArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/machines/{machine_id}",
        args.app_name, args.machine_id,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /apps/{app_name}/machines/{machine_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_delete_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /apps/{app_name}/machines/{machine_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_delete_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_delete_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /apps/{app_name}/machines/{machine_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_delete(
    client: &SimpleHttpClient,
    args: &MachinesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_delete_builder(client, args)?;
    machines_delete_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /orgs/{org_slug}/machines
// -----------------------------------------------------------------------------

/// GET /orgs/{org_slug}/machines - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn machines_org_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &MachinesOrgListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/orgs/{org_slug}/machines",
        args.org_slug,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /orgs/{org_slug}/machines - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn machines_org_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrgMachinesResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: OrgMachinesResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /orgs/{org_slug}/machines - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn machines_org_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrgMachinesResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = machines_org_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /orgs/{org_slug}/machines - convenience function.
///
/// Combines builder and execute in one call.

pub fn machines_org_list(
    client: &SimpleHttpClient,
    args: &MachinesOrgListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrgMachinesResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = machines_org_list_builder(client, args)?;
    machines_org_list_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/volumes
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/volumes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn volumes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &VolumesListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/volumes",
        args.app_name,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/volumes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn volumes_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/volumes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn volumes_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = volumes_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/volumes - convenience function.
///
/// Combines builder and execute in one call.

pub fn volumes_list(
    client: &SimpleHttpClient,
    args: &VolumesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = volumes_list_builder(client, args)?;
    volumes_list_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /apps/{app_name}/volumes
// -----------------------------------------------------------------------------

/// POST /apps/{app_name}/volumes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn volumes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &VolumesCreateArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/volumes",
        args.app_name,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/volumes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn volumes_create_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /apps/{app_name}/volumes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn volumes_create_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = volumes_create_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /apps/{app_name}/volumes - convenience function.
///
/// Combines builder and execute in one call.

pub fn volumes_create(
    client: &SimpleHttpClient,
    args: &VolumesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = volumes_create_builder(client, args)?;
    volumes_create_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /apps/{app_name}/volumes/{volume_id}
// -----------------------------------------------------------------------------

/// GET /apps/{app_name}/volumes/{volume_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn volumes_get_by_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &VolumesGetByIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/volumes/{volume_id}",
        args.app_name, args.volume_id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /apps/{app_name}/volumes/{volume_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn volumes_get_by_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /apps/{app_name}/volumes/{volume_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn volumes_get_by_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = volumes_get_by_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /apps/{app_name}/volumes/{volume_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn volumes_get_by_id(
    client: &SimpleHttpClient,
    args: &VolumesGetByIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = volumes_get_by_id_builder(client, args)?;
    volumes_get_by_id_execute(builder)
}

// -----------------------------------------------------------------------------
// PUT /apps/{app_name}/volumes/{volume_id}
// -----------------------------------------------------------------------------

/// PUT /apps/{app_name}/volumes/{volume_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn volumes_update_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &VolumesUpdateArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/volumes/{volume_id}",
        args.app_name, args.volume_id,
    );

    let builder = client
        .put(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(&args.body)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /apps/{app_name}/volumes/{volume_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn volumes_update_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// PUT /apps/{app_name}/volumes/{volume_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn volumes_update_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = volumes_update_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /apps/{app_name}/volumes/{volume_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn volumes_update(
    client: &SimpleHttpClient,
    args: &VolumesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = volumes_update_builder(client, args)?;
    volumes_update_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /apps/{app_name}/volumes/{volume_id}
// -----------------------------------------------------------------------------

/// DELETE /apps/{app_name}/volumes/{volume_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn volume_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &VolumeDeleteArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/apps/{app_name}/volumes/{volume_id}",
        args.app_name, args.volume_id,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /apps/{app_name}/volumes/{volume_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn volume_delete_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /apps/{app_name}/volumes/{volume_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn volume_delete_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = volume_delete_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /apps/{app_name}/volumes/{volume_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn volume_delete(
    client: &SimpleHttpClient,
    args: &VolumeDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = volume_delete_builder(client, args)?;
    volume_delete_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /orgs/{org_slug}/volumes
// -----------------------------------------------------------------------------

/// GET /orgs/{org_slug}/volumes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn volumes_org_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &VolumesOrgListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.machines.dev/v1/orgs/{org_slug}/volumes",
        args.org_slug,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /orgs/{org_slug}/volumes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn volumes_org_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrgVolumesResponse>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: OrgVolumesResponse = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /orgs/{org_slug}/volumes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn volumes_org_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrgVolumesResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = volumes_org_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /orgs/{org_slug}/volumes - convenience function.
///
/// Combines builder and execute in one call.

pub fn volumes_org_list(
    client: &SimpleHttpClient,
    args: &VolumesOrgListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrgVolumesResponse>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = volumes_org_list_builder(client, args)?;
    volumes_org_list_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for fly_io secretkeys_metadata_secrets_machines_volumes.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// GET /apps/{app_name}/secretkeys.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secretkeys_list(
        &self,
        args: &SecretkeysListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SecretKeys>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secretkeys_list_builder(&self.http_client, args)?;
        secretkeys_list_execute(builder)
    }

    /// GET /apps/{app_name}/secretkeys/{secret_name}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secretkey_get(
        &self,
        args: &SecretkeyGetArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SecretKey>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secretkey_get_builder(&self.http_client, args)?;
        secretkey_get_execute(builder)
    }

    /// POST /apps/{app_name}/secretkeys/{secret_name}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secretkey_set(
        &self,
        args: &SecretkeySetArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SetSecretkeyResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secretkey_set_builder(&self.http_client, args)?;
        secretkey_set_execute(builder)
    }

    /// DELETE /apps/{app_name}/secretkeys/{secret_name}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secretkey_delete(
        &self,
        args: &SecretkeyDeleteArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeleteSecretkeyResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secretkey_delete_builder(&self.http_client, args)?;
        secretkey_delete_execute(builder)
    }

    /// GET /apps/{app_name}/machines/{machine_id}/metadata.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_show_metadata(
        &self,
        args: &MachinesShowMetadataArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_show_metadata_builder(&self.http_client, args)?;
        machines_show_metadata_execute(builder)
    }

    /// PUT /apps/{app_name}/machines/{machine_id}/metadata.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_update_metadata(
        &self,
        args: &MachinesUpdateMetadataArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_update_metadata_builder(&self.http_client, args)?;
        machines_update_metadata_execute(builder)
    }

    /// GET /apps/{app_name}/machines/{machine_id}/metadata/{key}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_get_metadata_key(
        &self,
        args: &MachinesGetMetadataKeyArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<MetadataValueResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_get_metadata_key_builder(&self.http_client, args)?;
        machines_get_metadata_key_execute(builder)
    }

    /// POST /apps/{app_name}/machines/{machine_id}/metadata/{key}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_upsert_metadata(
        &self,
        args: &MachinesUpsertMetadataArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_upsert_metadata_builder(&self.http_client, args)?;
        machines_upsert_metadata_execute(builder)
    }

    /// DELETE /apps/{app_name}/machines/{machine_id}/metadata/{key}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_delete_metadata(
        &self,
        args: &MachinesDeleteMetadataArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_delete_metadata_builder(&self.http_client, args)?;
        machines_delete_metadata_execute(builder)
    }

    /// GET /apps/{app_name}/secrets.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secrets_list(
        &self,
        args: &SecretsListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<AppSecrets>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secrets_list_builder(&self.http_client, args)?;
        secrets_list_execute(builder)
    }

    /// POST /apps/{app_name}/secrets.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secrets_update(
        &self,
        args: &SecretsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<AppSecretsUpdateResp>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secrets_update_builder(&self.http_client, args)?;
        secrets_update_execute(builder)
    }

    /// GET /apps/{app_name}/secrets/{secret_name}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secret_get(
        &self,
        args: &SecretGetArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<AppSecret>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secret_get_builder(&self.http_client, args)?;
        secret_get_execute(builder)
    }

    /// POST /apps/{app_name}/secrets/{secret_name}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secret_create(
        &self,
        args: &SecretCreateArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SetAppSecretResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secret_create_builder(&self.http_client, args)?;
        secret_create_execute(builder)
    }

    /// DELETE /apps/{app_name}/secrets/{secret_name}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_secret_delete(
        &self,
        args: &SecretDeleteArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeleteAppSecretResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = secret_delete_builder(&self.http_client, args)?;
        secret_delete_execute(builder)
    }

    /// GET /apps/{app_name}/machines.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_list(
        &self,
        args: &MachinesListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_list_builder(&self.http_client, args)?;
        machines_list_execute(builder)
    }

    /// POST /apps/{app_name}/machines.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_create(
        &self,
        args: &MachinesCreateArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_create_builder(&self.http_client, args)?;
        machines_create_execute(builder)
    }

    /// GET /apps/{app_name}/machines/{machine_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_show(
        &self,
        args: &MachinesShowArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_show_builder(&self.http_client, args)?;
        machines_show_execute(builder)
    }

    /// POST /apps/{app_name}/machines/{machine_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_update(
        &self,
        args: &MachinesUpdateArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Machine>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_update_builder(&self.http_client, args)?;
        machines_update_execute(builder)
    }

    /// DELETE /apps/{app_name}/machines/{machine_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_delete(
        &self,
        args: &MachinesDeleteArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_delete_builder(&self.http_client, args)?;
        machines_delete_execute(builder)
    }

    /// GET /orgs/{org_slug}/machines.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_machines_org_list(
        &self,
        args: &MachinesOrgListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<OrgMachinesResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = machines_org_list_builder(&self.http_client, args)?;
        machines_org_list_execute(builder)
    }

    /// GET /apps/{app_name}/volumes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_volumes_list(
        &self,
        args: &VolumesListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = volumes_list_builder(&self.http_client, args)?;
        volumes_list_execute(builder)
    }

    /// POST /apps/{app_name}/volumes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_volumes_create(
        &self,
        args: &VolumesCreateArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = volumes_create_builder(&self.http_client, args)?;
        volumes_create_execute(builder)
    }

    /// GET /apps/{app_name}/volumes/{volume_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_volumes_get_by_id(
        &self,
        args: &VolumesGetByIdArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = volumes_get_by_id_builder(&self.http_client, args)?;
        volumes_get_by_id_execute(builder)
    }

    /// PUT /apps/{app_name}/volumes/{volume_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_volumes_update(
        &self,
        args: &VolumesUpdateArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = volumes_update_builder(&self.http_client, args)?;
        volumes_update_execute(builder)
    }

    /// DELETE /apps/{app_name}/volumes/{volume_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_volume_delete(
        &self,
        args: &VolumeDeleteArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Volume>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = volume_delete_builder(&self.http_client, args)?;
        volume_delete_execute(builder)
    }

    /// GET /orgs/{org_slug}/volumes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn fly_io_secretkeys_metadata_secrets_machines_volumes_volumes_org_list(
        &self,
        args: &VolumesOrgListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<OrgVolumesResponse>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = volumes_org_list_builder(&self.http_client, args)?;
        volumes_org_list_execute(builder)
    }
}
