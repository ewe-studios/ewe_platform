//! Auto-generated API module for stripe locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_locations_people_persons_94 `

#![cfg(feature = "stripe_locations_people_persons_94")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// DeletedPerson response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedPerson {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedPlan response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedPlan {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedRadarValueList response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedRadarValueList {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedSubscriptionItem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedSubscriptionItem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedTerminalLocation response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedTerminalLocation {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedTerminalReader response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedTerminalReader {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedWebhookEndpoint response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedWebhookEndpoint {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingToken response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingToken {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Person response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Person {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Plan response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Plan {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// RadarValueList response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RadarValueList {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SubscriptionItem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionItem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TerminalLocation response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TerminalLocation {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TerminalReader response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TerminalReader {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Token response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Token {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// WebhookEndpoint response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebhookEndpoint {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`GetTerminalLocations_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTerminalLocationsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetTerminalLocationsLocation_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTerminalLocationsLocationArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTerminalLocationsLocation_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTerminalLocationsLocationArgs {
    /// Path parameter: location
    pub location: String,
}

/// Arguments for [`DeleteTerminalLocationsLocation_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteTerminalLocationsLocationArgs {
    /// Path parameter: location
    pub location: String,
}

/// Arguments for [`GetAccountsAccountPeople_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetAccountsAccountPeopleArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: relationship
    pub relationship: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`PostAccountsAccountPeople_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostAccountsAccountPeopleArgs {
    /// Path parameter: account
    pub account: String,
}

/// Arguments for [`GetAccountsAccountPeoplePerson_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetAccountsAccountPeoplePersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostAccountsAccountPeoplePerson_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostAccountsAccountPeoplePersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// Arguments for [`DeleteAccountsAccountPeoplePerson_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteAccountsAccountPeoplePersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// Arguments for [`GetAccountsAccountPersons_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetAccountsAccountPersonsArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: relationship
    pub relationship: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`PostAccountsAccountPersons_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostAccountsAccountPersonsArgs {
    /// Path parameter: account
    pub account: String,
}

/// Arguments for [`GetAccountsAccountPersonsPerson_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetAccountsAccountPersonsPersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostAccountsAccountPersonsPerson_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostAccountsAccountPersonsPersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// Arguments for [`DeleteAccountsAccountPersonsPerson_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteAccountsAccountPersonsPersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// Arguments for [`GetPlans_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPlansArgs {
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: product
    pub product: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPlansPlan_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPlansPlanArgs {
    /// Path parameter: plan
    pub plan: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPlansPlan_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPlansPlanArgs {
    /// Path parameter: plan
    pub plan: String,
}

/// Arguments for [`DeletePlansPlan_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeletePlansPlanArgs {
    /// Path parameter: plan
    pub plan: String,
}

/// Arguments for [`GetTerminalReaders_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTerminalReadersArgs {
    /// Query parameter: device_type
    pub device_type: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: serial_number
    pub serial_number: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTerminalReadersReader_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTerminalReadersReaderArgs {
    /// Path parameter: reader
    pub reader: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTerminalReadersReader_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTerminalReadersReaderArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// Arguments for [`DeleteTerminalReadersReader_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteTerminalReadersReaderArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// Arguments for [`GetSubscriptionItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSubscriptionItemsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: subscription
    pub subscription: Option<String>,
}

/// Arguments for [`GetSubscriptionItemsItem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSubscriptionItemsItemArgs {
    /// Path parameter: item
    pub item: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostSubscriptionItemsItem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostSubscriptionItemsItemArgs {
    /// Path parameter: item
    pub item: String,
}

/// Arguments for [`DeleteSubscriptionItemsItem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteSubscriptionItemsItemArgs {
    /// Path parameter: item
    pub item: String,
}

/// Arguments for [`GetIssuingTokens_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingTokensArgs {
    /// Query parameter: card
    pub card: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetIssuingTokensToken_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingTokensTokenArgs {
    /// Path parameter: token
    pub token: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostIssuingTokensToken_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIssuingTokensTokenArgs {
    /// Path parameter: token
    pub token: String,
}

/// Arguments for [`GetTokensToken_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTokensTokenArgs {
    /// Path parameter: token
    pub token: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`GetRadarValueLists_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRadarValueListsArgs {
    /// Query parameter: alias
    pub alias: Option<String>,
    /// Query parameter: contains
    pub contains: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetRadarValueListsValueList_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRadarValueListsValueListArgs {
    /// Path parameter: value_list
    pub value_list: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostRadarValueListsValueList_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostRadarValueListsValueListArgs {
    /// Path parameter: value_list
    pub value_list: String,
}

/// Arguments for [`DeleteRadarValueListsValueList_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteRadarValueListsValueListArgs {
    /// Path parameter: value_list
    pub value_list: String,
}

/// Arguments for [`GetWebhookEndpoints_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetWebhookEndpointsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetWebhookEndpointsWebhookEndpoint_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetWebhookEndpointsWebhookEndpointArgs {
    /// Path parameter: webhook_endpoint
    pub webhook_endpoint: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostWebhookEndpointsWebhookEndpoint_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostWebhookEndpointsWebhookEndpointArgs {
    /// Path parameter: webhook_endpoint
    pub webhook_endpoint: String,
}

/// Arguments for [`DeleteWebhookEndpointsWebhookEndpoint_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteWebhookEndpointsWebhookEndpointArgs {
    /// Path parameter: webhook_endpoint
    pub webhook_endpoint: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/terminal/locations
// -----------------------------------------------------------------------------

/// GET /v1/terminal/locations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_terminal_locations_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTerminalLocationsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/terminal/locations",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/locations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_terminal_locations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/terminal/locations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_terminal_locations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_terminal_locations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/terminal/locations - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_terminal_locations(
    client: &SimpleHttpClient,
    args: &GetTerminalLocationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_terminal_locations_builder(client, args)?;
    get_terminal_locations_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/terminal/locations
// -----------------------------------------------------------------------------

/// POST /v1/terminal/locations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_terminal_locations_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/terminal/locations",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/locations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_terminal_locations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TerminalLocation>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TerminalLocation = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/terminal/locations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_terminal_locations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TerminalLocation>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_terminal_locations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/terminal/locations/{location}
// -----------------------------------------------------------------------------

/// GET /v1/terminal/locations/{location} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_terminal_locations_location_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTerminalLocationsLocationArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/terminal/locations/{location}",
        args.location,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/locations/{location} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_terminal_locations_location_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/terminal/locations/{location} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_terminal_locations_location_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_terminal_locations_location_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/terminal/locations/{location} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_terminal_locations_location(
    client: &SimpleHttpClient,
    args: &GetTerminalLocationsLocationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_terminal_locations_location_builder(client, args)?;
    get_terminal_locations_location_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/terminal/locations/{location}
// -----------------------------------------------------------------------------

/// POST /v1/terminal/locations/{location} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_terminal_locations_location_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTerminalLocationsLocationArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/terminal/locations/{location}",
        args.location,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/locations/{location} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_terminal_locations_location_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/terminal/locations/{location} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_terminal_locations_location_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_terminal_locations_location_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/locations/{location} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_terminal_locations_location(
    client: &SimpleHttpClient,
    args: &PostTerminalLocationsLocationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_terminal_locations_location_builder(client, args)?;
    post_terminal_locations_location_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/terminal/locations/{location}
// -----------------------------------------------------------------------------

/// DELETE /v1/terminal/locations/{location} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_terminal_locations_location_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteTerminalLocationsLocationArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/terminal/locations/{location}",
        args.location,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/terminal/locations/{location} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_terminal_locations_location_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedTerminalLocation>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedTerminalLocation = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/terminal/locations/{location} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_terminal_locations_location_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedTerminalLocation>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_terminal_locations_location_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/terminal/locations/{location} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_terminal_locations_location(
    client: &SimpleHttpClient,
    args: &DeleteTerminalLocationsLocationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedTerminalLocation>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_terminal_locations_location_builder(client, args)?;
    delete_terminal_locations_location_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/accounts/{account}/people
// -----------------------------------------------------------------------------

/// GET /v1/accounts/{account}/people - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_accounts_account_people_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetAccountsAccountPeopleArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/people",
        args.account,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/people - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_accounts_account_people_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/accounts/{account}/people - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_accounts_account_people_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_accounts_account_people_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/accounts/{account}/people - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_accounts_account_people(
    client: &SimpleHttpClient,
    args: &GetAccountsAccountPeopleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_accounts_account_people_builder(client, args)?;
    get_accounts_account_people_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/accounts/{account}/people
// -----------------------------------------------------------------------------

/// POST /v1/accounts/{account}/people - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_accounts_account_people_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostAccountsAccountPeopleArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/people",
        args.account,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/people - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_accounts_account_people_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/accounts/{account}/people - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_accounts_account_people_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_accounts_account_people_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/accounts/{account}/people - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_accounts_account_people(
    client: &SimpleHttpClient,
    args: &PostAccountsAccountPeopleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_accounts_account_people_builder(client, args)?;
    post_accounts_account_people_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/accounts/{account}/people/{person}
// -----------------------------------------------------------------------------

/// GET /v1/accounts/{account}/people/{person} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_accounts_account_people_person_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetAccountsAccountPeoplePersonArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/people/{person}",
        args.account, args.person,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/people/{person} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_accounts_account_people_person_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/accounts/{account}/people/{person} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_accounts_account_people_person_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_accounts_account_people_person_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/accounts/{account}/people/{person} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_accounts_account_people_person(
    client: &SimpleHttpClient,
    args: &GetAccountsAccountPeoplePersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_accounts_account_people_person_builder(client, args)?;
    get_accounts_account_people_person_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/accounts/{account}/people/{person}
// -----------------------------------------------------------------------------

/// POST /v1/accounts/{account}/people/{person} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_accounts_account_people_person_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostAccountsAccountPeoplePersonArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/people/{person}",
        args.account, args.person,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/people/{person} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_accounts_account_people_person_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/accounts/{account}/people/{person} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_accounts_account_people_person_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_accounts_account_people_person_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/accounts/{account}/people/{person} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_accounts_account_people_person(
    client: &SimpleHttpClient,
    args: &PostAccountsAccountPeoplePersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_accounts_account_people_person_builder(client, args)?;
    post_accounts_account_people_person_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/accounts/{account}/people/{person}
// -----------------------------------------------------------------------------

/// DELETE /v1/accounts/{account}/people/{person} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_accounts_account_people_person_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteAccountsAccountPeoplePersonArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/people/{person}",
        args.account, args.person,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}/people/{person} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_accounts_account_people_person_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedPerson = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/accounts/{account}/people/{person} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_accounts_account_people_person_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_accounts_account_people_person_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/accounts/{account}/people/{person} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_accounts_account_people_person(
    client: &SimpleHttpClient,
    args: &DeleteAccountsAccountPeoplePersonArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_accounts_account_people_person_builder(client, args)?;
    delete_accounts_account_people_person_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/accounts/{account}/persons
// -----------------------------------------------------------------------------

/// GET /v1/accounts/{account}/persons - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_accounts_account_persons_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetAccountsAccountPersonsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/persons",
        args.account,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/persons - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_accounts_account_persons_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/accounts/{account}/persons - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_accounts_account_persons_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_accounts_account_persons_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/accounts/{account}/persons - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_accounts_account_persons(
    client: &SimpleHttpClient,
    args: &GetAccountsAccountPersonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_accounts_account_persons_builder(client, args)?;
    get_accounts_account_persons_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/accounts/{account}/persons
// -----------------------------------------------------------------------------

/// POST /v1/accounts/{account}/persons - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_accounts_account_persons_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostAccountsAccountPersonsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/persons",
        args.account,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/persons - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_accounts_account_persons_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/accounts/{account}/persons - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_accounts_account_persons_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_accounts_account_persons_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/accounts/{account}/persons - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_accounts_account_persons(
    client: &SimpleHttpClient,
    args: &PostAccountsAccountPersonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_accounts_account_persons_builder(client, args)?;
    post_accounts_account_persons_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/accounts/{account}/persons/{person}
// -----------------------------------------------------------------------------

/// GET /v1/accounts/{account}/persons/{person} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_accounts_account_persons_person_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetAccountsAccountPersonsPersonArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/persons/{person}",
        args.account, args.person,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/persons/{person} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_accounts_account_persons_person_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/accounts/{account}/persons/{person} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_accounts_account_persons_person_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_accounts_account_persons_person_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/accounts/{account}/persons/{person} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_accounts_account_persons_person(
    client: &SimpleHttpClient,
    args: &GetAccountsAccountPersonsPersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_accounts_account_persons_person_builder(client, args)?;
    get_accounts_account_persons_person_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/accounts/{account}/persons/{person}
// -----------------------------------------------------------------------------

/// POST /v1/accounts/{account}/persons/{person} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_accounts_account_persons_person_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostAccountsAccountPersonsPersonArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/persons/{person}",
        args.account, args.person,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/persons/{person} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_accounts_account_persons_person_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/accounts/{account}/persons/{person} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_accounts_account_persons_person_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_accounts_account_persons_person_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/accounts/{account}/persons/{person} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_accounts_account_persons_person(
    client: &SimpleHttpClient,
    args: &PostAccountsAccountPersonsPersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_accounts_account_persons_person_builder(client, args)?;
    post_accounts_account_persons_person_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/accounts/{account}/persons/{person}
// -----------------------------------------------------------------------------

/// DELETE /v1/accounts/{account}/persons/{person} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_accounts_account_persons_person_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteAccountsAccountPersonsPersonArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/accounts/{account}/persons/{person}",
        args.account, args.person,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}/persons/{person} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_accounts_account_persons_person_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedPerson = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/accounts/{account}/persons/{person} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_accounts_account_persons_person_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_accounts_account_persons_person_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/accounts/{account}/persons/{person} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_accounts_account_persons_person(
    client: &SimpleHttpClient,
    args: &DeleteAccountsAccountPersonsPersonArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_accounts_account_persons_person_builder(client, args)?;
    delete_accounts_account_persons_person_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/plans
// -----------------------------------------------------------------------------

/// GET /v1/plans - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_plans_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPlansArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/plans",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/plans - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_plans_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/plans - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_plans_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_plans_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/plans - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_plans(
    client: &SimpleHttpClient,
    args: &GetPlansArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_plans_builder(client, args)?;
    get_plans_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/plans
// -----------------------------------------------------------------------------

/// POST /v1/plans - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_plans_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/plans",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/plans - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_plans_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Plan>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Plan = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/plans - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_plans_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_plans_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/plans/{plan}
// -----------------------------------------------------------------------------

/// GET /v1/plans/{plan} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_plans_plan_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPlansPlanArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/plans/{plan}", args.plan,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/plans/{plan} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_plans_plan_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Plan>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Plan = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/plans/{plan} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_plans_plan_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_plans_plan_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/plans/{plan} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_plans_plan(
    client: &SimpleHttpClient,
    args: &GetPlansPlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_plans_plan_builder(client, args)?;
    get_plans_plan_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/plans/{plan}
// -----------------------------------------------------------------------------

/// POST /v1/plans/{plan} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_plans_plan_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPlansPlanArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/plans/{plan}", args.plan,);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/plans/{plan} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_plans_plan_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Plan>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Plan = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/plans/{plan} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_plans_plan_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_plans_plan_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/plans/{plan} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_plans_plan(
    client: &SimpleHttpClient,
    args: &PostPlansPlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_plans_plan_builder(client, args)?;
    post_plans_plan_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/plans/{plan}
// -----------------------------------------------------------------------------

/// DELETE /v1/plans/{plan} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_plans_plan_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeletePlansPlanArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/plans/{plan}", args.plan,);

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/plans/{plan} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_plans_plan_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedPlan>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedPlan = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/plans/{plan} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_plans_plan_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeletedPlan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_plans_plan_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/plans/{plan} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_plans_plan(
    client: &SimpleHttpClient,
    args: &DeletePlansPlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeletedPlan>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_plans_plan_builder(client, args)?;
    delete_plans_plan_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/terminal/readers
// -----------------------------------------------------------------------------

/// GET /v1/terminal/readers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_terminal_readers_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTerminalReadersArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/terminal/readers",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/readers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_terminal_readers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/terminal/readers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_terminal_readers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_terminal_readers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/terminal/readers - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_terminal_readers(
    client: &SimpleHttpClient,
    args: &GetTerminalReadersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_terminal_readers_builder(client, args)?;
    get_terminal_readers_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/terminal/readers
// -----------------------------------------------------------------------------

/// POST /v1/terminal/readers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_terminal_readers_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/terminal/readers",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_terminal_readers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TerminalReader>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/terminal/readers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_terminal_readers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TerminalReader>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_terminal_readers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/terminal/readers/{reader}
// -----------------------------------------------------------------------------

/// GET /v1/terminal/readers/{reader} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_terminal_readers_reader_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTerminalReadersReaderArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/terminal/readers/{reader}",
        args.reader,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/readers/{reader} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_terminal_readers_reader_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/terminal/readers/{reader} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_terminal_readers_reader_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_terminal_readers_reader_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/terminal/readers/{reader} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_terminal_readers_reader(
    client: &SimpleHttpClient,
    args: &GetTerminalReadersReaderArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_terminal_readers_reader_builder(client, args)?;
    get_terminal_readers_reader_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/terminal/readers/{reader}
// -----------------------------------------------------------------------------

/// POST /v1/terminal/readers/{reader} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_terminal_readers_reader_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTerminalReadersReaderArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/terminal/readers/{reader}",
        args.reader,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_terminal_readers_reader_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/terminal/readers/{reader} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_terminal_readers_reader_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_terminal_readers_reader_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/readers/{reader} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_terminal_readers_reader(
    client: &SimpleHttpClient,
    args: &PostTerminalReadersReaderArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_terminal_readers_reader_builder(client, args)?;
    post_terminal_readers_reader_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/terminal/readers/{reader}
// -----------------------------------------------------------------------------

/// DELETE /v1/terminal/readers/{reader} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_terminal_readers_reader_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteTerminalReadersReaderArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/terminal/readers/{reader}",
        args.reader,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/terminal/readers/{reader} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_terminal_readers_reader_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedTerminalReader>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedTerminalReader = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/terminal/readers/{reader} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_terminal_readers_reader_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedTerminalReader>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_terminal_readers_reader_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/terminal/readers/{reader} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_terminal_readers_reader(
    client: &SimpleHttpClient,
    args: &DeleteTerminalReadersReaderArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedTerminalReader>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_terminal_readers_reader_builder(client, args)?;
    delete_terminal_readers_reader_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/subscription_items
// -----------------------------------------------------------------------------

/// GET /v1/subscription_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_subscription_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetSubscriptionItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/subscription_items",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_subscription_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/subscription_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_subscription_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_subscription_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/subscription_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_subscription_items(
    client: &SimpleHttpClient,
    args: &GetSubscriptionItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_subscription_items_builder(client, args)?;
    get_subscription_items_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/subscription_items
// -----------------------------------------------------------------------------

/// POST /v1/subscription_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_subscription_items_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/subscription_items",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_subscription_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SubscriptionItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/subscription_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_subscription_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_subscription_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/subscription_items/{item}
// -----------------------------------------------------------------------------

/// GET /v1/subscription_items/{item} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_subscription_items_item_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetSubscriptionItemsItemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_items/{item}",
        args.item,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_items/{item} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_subscription_items_item_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SubscriptionItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/subscription_items/{item} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_subscription_items_item_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_subscription_items_item_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/subscription_items/{item} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_subscription_items_item(
    client: &SimpleHttpClient,
    args: &GetSubscriptionItemsItemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_subscription_items_item_builder(client, args)?;
    get_subscription_items_item_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/subscription_items/{item}
// -----------------------------------------------------------------------------

/// POST /v1/subscription_items/{item} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_subscription_items_item_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostSubscriptionItemsItemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_items/{item}",
        args.item,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_items/{item} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_subscription_items_item_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SubscriptionItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/subscription_items/{item} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_subscription_items_item_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_subscription_items_item_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/subscription_items/{item} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_subscription_items_item(
    client: &SimpleHttpClient,
    args: &PostSubscriptionItemsItemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_subscription_items_item_builder(client, args)?;
    post_subscription_items_item_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/subscription_items/{item}
// -----------------------------------------------------------------------------

/// DELETE /v1/subscription_items/{item} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_subscription_items_item_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteSubscriptionItemsItemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_items/{item}",
        args.item,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/subscription_items/{item} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_subscription_items_item_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedSubscriptionItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedSubscriptionItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/subscription_items/{item} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_subscription_items_item_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedSubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_subscription_items_item_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/subscription_items/{item} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_subscription_items_item(
    client: &SimpleHttpClient,
    args: &DeleteSubscriptionItemsItemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedSubscriptionItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_subscription_items_item_builder(client, args)?;
    delete_subscription_items_item_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/tokens
// -----------------------------------------------------------------------------

/// GET /v1/issuing/tokens - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_tokens_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetIssuingTokensArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/issuing/tokens",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/tokens - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_tokens_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/issuing/tokens - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_tokens_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_issuing_tokens_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/tokens - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_tokens(
    client: &SimpleHttpClient,
    args: &GetIssuingTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_issuing_tokens_builder(client, args)?;
    get_issuing_tokens_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/tokens/{token}
// -----------------------------------------------------------------------------

/// GET /v1/issuing/tokens/{token} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_tokens_token_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetIssuingTokensTokenArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/tokens/{token}",
        args.token,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/tokens/{token} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_tokens_token_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IssuingToken>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IssuingToken = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/issuing/tokens/{token} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_tokens_token_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingToken>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_issuing_tokens_token_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/tokens/{token} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_tokens_token(
    client: &SimpleHttpClient,
    args: &GetIssuingTokensTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingToken>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_issuing_tokens_token_builder(client, args)?;
    get_issuing_tokens_token_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/tokens/{token}
// -----------------------------------------------------------------------------

/// POST /v1/issuing/tokens/{token} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_tokens_token_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostIssuingTokensTokenArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/tokens/{token}",
        args.token,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/tokens/{token} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_tokens_token_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IssuingToken>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IssuingToken = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/issuing/tokens/{token} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_tokens_token_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingToken>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_issuing_tokens_token_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/tokens/{token} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_issuing_tokens_token(
    client: &SimpleHttpClient,
    args: &PostIssuingTokensTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingToken>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_issuing_tokens_token_builder(client, args)?;
    post_issuing_tokens_token_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/tokens
// -----------------------------------------------------------------------------

/// POST /v1/tokens - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_tokens_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/tokens",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tokens - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_tokens_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Token>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Token = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/tokens - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_tokens_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_tokens_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/tokens/{token}
// -----------------------------------------------------------------------------

/// GET /v1/tokens/{token} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tokens_token_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTokensTokenArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/tokens/{token}", args.token,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tokens/{token} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tokens_token_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Token>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Token = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/tokens/{token} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tokens_token_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_tokens_token_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tokens/{token} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tokens_token(
    client: &SimpleHttpClient,
    args: &GetTokensTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_tokens_token_builder(client, args)?;
    get_tokens_token_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/radar/value_lists
// -----------------------------------------------------------------------------

/// GET /v1/radar/value_lists - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_radar_value_lists_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetRadarValueListsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/radar/value_lists",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_lists - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_radar_value_lists_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/radar/value_lists - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_radar_value_lists_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_radar_value_lists_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/radar/value_lists - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_radar_value_lists(
    client: &SimpleHttpClient,
    args: &GetRadarValueListsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_radar_value_lists_builder(client, args)?;
    get_radar_value_lists_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/radar/value_lists
// -----------------------------------------------------------------------------

/// POST /v1/radar/value_lists - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_radar_value_lists_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/radar/value_lists",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/value_lists - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_radar_value_lists_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: RadarValueList = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/radar/value_lists - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_radar_value_lists_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_radar_value_lists_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/radar/value_lists/{value_list}
// -----------------------------------------------------------------------------

/// GET /v1/radar/value_lists/{value_list} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_radar_value_lists_value_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetRadarValueListsValueListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/radar/value_lists/{value_list}",
        args.value_list,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_lists/{value_list} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_radar_value_lists_value_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: RadarValueList = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/radar/value_lists/{value_list} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_radar_value_lists_value_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_radar_value_lists_value_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/radar/value_lists/{value_list} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_radar_value_lists_value_list(
    client: &SimpleHttpClient,
    args: &GetRadarValueListsValueListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_radar_value_lists_value_list_builder(client, args)?;
    get_radar_value_lists_value_list_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/radar/value_lists/{value_list}
// -----------------------------------------------------------------------------

/// POST /v1/radar/value_lists/{value_list} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_radar_value_lists_value_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostRadarValueListsValueListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/radar/value_lists/{value_list}",
        args.value_list,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/value_lists/{value_list} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_radar_value_lists_value_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: RadarValueList = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/radar/value_lists/{value_list} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_radar_value_lists_value_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_radar_value_lists_value_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/radar/value_lists/{value_list} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_radar_value_lists_value_list(
    client: &SimpleHttpClient,
    args: &PostRadarValueListsValueListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_radar_value_lists_value_list_builder(client, args)?;
    post_radar_value_lists_value_list_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/radar/value_lists/{value_list}
// -----------------------------------------------------------------------------

/// DELETE /v1/radar/value_lists/{value_list} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_radar_value_lists_value_list_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteRadarValueListsValueListArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/radar/value_lists/{value_list}",
        args.value_list,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/radar/value_lists/{value_list} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_radar_value_lists_value_list_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedRadarValueList>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedRadarValueList = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/radar/value_lists/{value_list} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_radar_value_lists_value_list_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedRadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_radar_value_lists_value_list_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/radar/value_lists/{value_list} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_radar_value_lists_value_list(
    client: &SimpleHttpClient,
    args: &DeleteRadarValueListsValueListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedRadarValueList>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_radar_value_lists_value_list_builder(client, args)?;
    delete_radar_value_lists_value_list_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/webhook_endpoints
// -----------------------------------------------------------------------------

/// GET /v1/webhook_endpoints - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_webhook_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetWebhookEndpointsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/webhook_endpoints",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/webhook_endpoints - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_webhook_endpoints_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/webhook_endpoints - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_webhook_endpoints_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_webhook_endpoints_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/webhook_endpoints - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_webhook_endpoints(
    client: &SimpleHttpClient,
    args: &GetWebhookEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_webhook_endpoints_builder(client, args)?;
    get_webhook_endpoints_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/webhook_endpoints
// -----------------------------------------------------------------------------

/// POST /v1/webhook_endpoints - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_webhook_endpoints_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/webhook_endpoints",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/webhook_endpoints - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_webhook_endpoints_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: WebhookEndpoint = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/webhook_endpoints - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_webhook_endpoints_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_webhook_endpoints_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/webhook_endpoints/{webhook_endpoint}
// -----------------------------------------------------------------------------

/// GET /v1/webhook_endpoints/{webhook_endpoint} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_webhook_endpoints_webhook_endpoint_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetWebhookEndpointsWebhookEndpointArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/webhook_endpoints/{webhook_endpoint}",
        args.webhook_endpoint,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/webhook_endpoints/{webhook_endpoint} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_webhook_endpoints_webhook_endpoint_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: WebhookEndpoint = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/webhook_endpoints/{webhook_endpoint} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_webhook_endpoints_webhook_endpoint_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_webhook_endpoints_webhook_endpoint_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/webhook_endpoints/{webhook_endpoint} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_webhook_endpoints_webhook_endpoint(
    client: &SimpleHttpClient,
    args: &GetWebhookEndpointsWebhookEndpointArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_webhook_endpoints_webhook_endpoint_builder(client, args)?;
    get_webhook_endpoints_webhook_endpoint_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/webhook_endpoints/{webhook_endpoint}
// -----------------------------------------------------------------------------

/// POST /v1/webhook_endpoints/{webhook_endpoint} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_webhook_endpoints_webhook_endpoint_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostWebhookEndpointsWebhookEndpointArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/webhook_endpoints/{webhook_endpoint}",
        args.webhook_endpoint,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/webhook_endpoints/{webhook_endpoint} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_webhook_endpoints_webhook_endpoint_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: WebhookEndpoint = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/webhook_endpoints/{webhook_endpoint} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_webhook_endpoints_webhook_endpoint_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_webhook_endpoints_webhook_endpoint_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/webhook_endpoints/{webhook_endpoint} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_webhook_endpoints_webhook_endpoint(
    client: &SimpleHttpClient,
    args: &PostWebhookEndpointsWebhookEndpointArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_webhook_endpoints_webhook_endpoint_builder(client, args)?;
    post_webhook_endpoints_webhook_endpoint_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/webhook_endpoints/{webhook_endpoint}
// -----------------------------------------------------------------------------

/// DELETE /v1/webhook_endpoints/{webhook_endpoint} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_webhook_endpoints_webhook_endpoint_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteWebhookEndpointsWebhookEndpointArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/webhook_endpoints/{webhook_endpoint}",
        args.webhook_endpoint,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/webhook_endpoints/{webhook_endpoint} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_webhook_endpoints_webhook_endpoint_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedWebhookEndpoint>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedWebhookEndpoint = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/webhook_endpoints/{webhook_endpoint} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_webhook_endpoints_webhook_endpoint_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedWebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_webhook_endpoints_webhook_endpoint_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/webhook_endpoints/{webhook_endpoint} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_webhook_endpoints_webhook_endpoint(
    client: &SimpleHttpClient,
    args: &DeleteWebhookEndpointsWebhookEndpointArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedWebhookEndpoint>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_webhook_endpoints_webhook_endpoint_builder(client, args)?;
    delete_webhook_endpoints_webhook_endpoint_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// GET /v1/terminal/locations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_terminal_locations(
        &self,
        args: &GetTerminalLocationsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_terminal_locations_builder(&self.http_client, args)?;
        get_terminal_locations_execute(builder)
    }

    /// POST /v1/terminal/locations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_terminal_locations(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TerminalLocation>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_terminal_locations_builder(&self.http_client)?;
        post_terminal_locations_execute(builder)
    }

    /// GET /v1/terminal/locations/{location}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_terminal_locations_location(
        &self,
        args: &GetTerminalLocationsLocationArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_terminal_locations_location_builder(&self.http_client, args)?;
        get_terminal_locations_location_execute(builder)
    }

    /// POST /v1/terminal/locations/{location}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_terminal_locations_location(
        &self,
        args: &PostTerminalLocationsLocationArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_terminal_locations_location_builder(&self.http_client, args)?;
        post_terminal_locations_location_execute(builder)
    }

    /// DELETE /v1/terminal/locations/{location}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_terminal_locations_location(
        &self,
        args: &DeleteTerminalLocationsLocationArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedTerminalLocation>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_terminal_locations_location_builder(&self.http_client, args)?;
        delete_terminal_locations_location_execute(builder)
    }

    /// GET /v1/accounts/{account}/people.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_accounts_account_people(
        &self,
        args: &GetAccountsAccountPeopleArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_accounts_account_people_builder(&self.http_client, args)?;
        get_accounts_account_people_execute(builder)
    }

    /// POST /v1/accounts/{account}/people.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_accounts_account_people(
        &self,
        args: &PostAccountsAccountPeopleArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_accounts_account_people_builder(&self.http_client, args)?;
        post_accounts_account_people_execute(builder)
    }

    /// GET /v1/accounts/{account}/people/{person}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_accounts_account_people_person(
        &self,
        args: &GetAccountsAccountPeoplePersonArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_accounts_account_people_person_builder(&self.http_client, args)?;
        get_accounts_account_people_person_execute(builder)
    }

    /// POST /v1/accounts/{account}/people/{person}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_accounts_account_people_person(
        &self,
        args: &PostAccountsAccountPeoplePersonArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_accounts_account_people_person_builder(&self.http_client, args)?;
        post_accounts_account_people_person_execute(builder)
    }

    /// DELETE /v1/accounts/{account}/people/{person}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_accounts_account_people_person(
        &self,
        args: &DeleteAccountsAccountPeoplePersonArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_accounts_account_people_person_builder(&self.http_client, args)?;
        delete_accounts_account_people_person_execute(builder)
    }

    /// GET /v1/accounts/{account}/persons.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_accounts_account_persons(
        &self,
        args: &GetAccountsAccountPersonsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_accounts_account_persons_builder(&self.http_client, args)?;
        get_accounts_account_persons_execute(builder)
    }

    /// POST /v1/accounts/{account}/persons.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_accounts_account_persons(
        &self,
        args: &PostAccountsAccountPersonsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_accounts_account_persons_builder(&self.http_client, args)?;
        post_accounts_account_persons_execute(builder)
    }

    /// GET /v1/accounts/{account}/persons/{person}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_accounts_account_persons_person(
        &self,
        args: &GetAccountsAccountPersonsPersonArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_accounts_account_persons_person_builder(&self.http_client, args)?;
        get_accounts_account_persons_person_execute(builder)
    }

    /// POST /v1/accounts/{account}/persons/{person}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_accounts_account_persons_person(
        &self,
        args: &PostAccountsAccountPersonsPersonArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Person>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_accounts_account_persons_person_builder(&self.http_client, args)?;
        post_accounts_account_persons_person_execute(builder)
    }

    /// DELETE /v1/accounts/{account}/persons/{person}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_accounts_account_persons_person(
        &self,
        args: &DeleteAccountsAccountPersonsPersonArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedPerson>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_accounts_account_persons_person_builder(&self.http_client, args)?;
        delete_accounts_account_persons_person_execute(builder)
    }

    /// GET /v1/plans.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_plans(
        &self,
        args: &GetPlansArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_plans_builder(&self.http_client, args)?;
        get_plans_execute(builder)
    }

    /// POST /v1/plans.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_plans(
        &self,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_plans_builder(&self.http_client)?;
        post_plans_execute(builder)
    }

    /// GET /v1/plans/{plan}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_plans_plan(
        &self,
        args: &GetPlansPlanArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_plans_plan_builder(&self.http_client, args)?;
        get_plans_plan_execute(builder)
    }

    /// POST /v1/plans/{plan}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_plans_plan(
        &self,
        args: &PostPlansPlanArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Plan>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_plans_plan_builder(&self.http_client, args)?;
        post_plans_plan_execute(builder)
    }

    /// DELETE /v1/plans/{plan}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_plans_plan(
        &self,
        args: &DeletePlansPlanArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedPlan>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_plans_plan_builder(&self.http_client, args)?;
        delete_plans_plan_execute(builder)
    }

    /// GET /v1/terminal/readers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_terminal_readers(
        &self,
        args: &GetTerminalReadersArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_terminal_readers_builder(&self.http_client, args)?;
        get_terminal_readers_execute(builder)
    }

    /// POST /v1/terminal/readers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_terminal_readers(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TerminalReader>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_terminal_readers_builder(&self.http_client)?;
        post_terminal_readers_execute(builder)
    }

    /// GET /v1/terminal/readers/{reader}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_terminal_readers_reader(
        &self,
        args: &GetTerminalReadersReaderArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_terminal_readers_reader_builder(&self.http_client, args)?;
        get_terminal_readers_reader_execute(builder)
    }

    /// POST /v1/terminal/readers/{reader}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_terminal_readers_reader(
        &self,
        args: &PostTerminalReadersReaderArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_terminal_readers_reader_builder(&self.http_client, args)?;
        post_terminal_readers_reader_execute(builder)
    }

    /// DELETE /v1/terminal/readers/{reader}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_terminal_readers_reader(
        &self,
        args: &DeleteTerminalReadersReaderArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedTerminalReader>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_terminal_readers_reader_builder(&self.http_client, args)?;
        delete_terminal_readers_reader_execute(builder)
    }

    /// GET /v1/subscription_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_subscription_items(
        &self,
        args: &GetSubscriptionItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_subscription_items_builder(&self.http_client, args)?;
        get_subscription_items_execute(builder)
    }

    /// POST /v1/subscription_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_subscription_items(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_subscription_items_builder(&self.http_client)?;
        post_subscription_items_execute(builder)
    }

    /// GET /v1/subscription_items/{item}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_subscription_items_item(
        &self,
        args: &GetSubscriptionItemsItemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_subscription_items_item_builder(&self.http_client, args)?;
        get_subscription_items_item_execute(builder)
    }

    /// POST /v1/subscription_items/{item}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_subscription_items_item(
        &self,
        args: &PostSubscriptionItemsItemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SubscriptionItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_subscription_items_item_builder(&self.http_client, args)?;
        post_subscription_items_item_execute(builder)
    }

    /// DELETE /v1/subscription_items/{item}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_subscription_items_item(
        &self,
        args: &DeleteSubscriptionItemsItemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedSubscriptionItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_subscription_items_item_builder(&self.http_client, args)?;
        delete_subscription_items_item_execute(builder)
    }

    /// GET /v1/issuing/tokens.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_issuing_tokens(
        &self,
        args: &GetIssuingTokensArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_issuing_tokens_builder(&self.http_client, args)?;
        get_issuing_tokens_execute(builder)
    }

    /// GET /v1/issuing/tokens/{token}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_issuing_tokens_token(
        &self,
        args: &GetIssuingTokensTokenArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IssuingToken>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_issuing_tokens_token_builder(&self.http_client, args)?;
        get_issuing_tokens_token_execute(builder)
    }

    /// POST /v1/issuing/tokens/{token}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_issuing_tokens_token(
        &self,
        args: &PostIssuingTokensTokenArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IssuingToken>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_issuing_tokens_token_builder(&self.http_client, args)?;
        post_issuing_tokens_token_execute(builder)
    }

    /// POST /v1/tokens.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_tokens(
        &self,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Token>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_tokens_builder(&self.http_client)?;
        post_tokens_execute(builder)
    }

    /// GET /v1/tokens/{token}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_tokens_token(
        &self,
        args: &GetTokensTokenArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Token>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_tokens_token_builder(&self.http_client, args)?;
        get_tokens_token_execute(builder)
    }

    /// GET /v1/radar/value_lists.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_radar_value_lists(
        &self,
        args: &GetRadarValueListsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_radar_value_lists_builder(&self.http_client, args)?;
        get_radar_value_lists_execute(builder)
    }

    /// POST /v1/radar/value_lists.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_radar_value_lists(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_radar_value_lists_builder(&self.http_client)?;
        post_radar_value_lists_execute(builder)
    }

    /// GET /v1/radar/value_lists/{value_list}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_radar_value_lists_value_list(
        &self,
        args: &GetRadarValueListsValueListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_radar_value_lists_value_list_builder(&self.http_client, args)?;
        get_radar_value_lists_value_list_execute(builder)
    }

    /// POST /v1/radar/value_lists/{value_list}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_radar_value_lists_value_list(
        &self,
        args: &PostRadarValueListsValueListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<RadarValueList>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_radar_value_lists_value_list_builder(&self.http_client, args)?;
        post_radar_value_lists_value_list_execute(builder)
    }

    /// DELETE /v1/radar/value_lists/{value_list}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_radar_value_lists_value_list(
        &self,
        args: &DeleteRadarValueListsValueListArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedRadarValueList>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_radar_value_lists_value_list_builder(&self.http_client, args)?;
        delete_radar_value_lists_value_list_execute(builder)
    }

    /// GET /v1/webhook_endpoints.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_webhook_endpoints(
        &self,
        args: &GetWebhookEndpointsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_webhook_endpoints_builder(&self.http_client, args)?;
        get_webhook_endpoints_execute(builder)
    }

    /// POST /v1/webhook_endpoints.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_webhook_endpoints(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_webhook_endpoints_builder(&self.http_client)?;
        post_webhook_endpoints_execute(builder)
    }

    /// GET /v1/webhook_endpoints/{webhook_endpoint}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_get_webhook_endpoints_webhook_endpoint(
        &self,
        args: &GetWebhookEndpointsWebhookEndpointArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_webhook_endpoints_webhook_endpoint_builder(&self.http_client, args)?;
        get_webhook_endpoints_webhook_endpoint_execute(builder)
    }

    /// POST /v1/webhook_endpoints/{webhook_endpoint}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_post_webhook_endpoints_webhook_endpoint(
        &self,
        args: &PostWebhookEndpointsWebhookEndpointArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<WebhookEndpoint>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_webhook_endpoints_webhook_endpoint_builder(&self.http_client, args)?;
        post_webhook_endpoints_webhook_endpoint_execute(builder)
    }

    /// DELETE /v1/webhook_endpoints/{webhook_endpoint}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_locations_people_persons_plans_readers_subscription_items_tokens_value_lists_webhook_endpoints_delete_webhook_endpoints_webhook_endpoint(
        &self,
        args: &DeleteWebhookEndpointsWebhookEndpointArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedWebhookEndpoint>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_webhook_endpoints_webhook_endpoint_builder(&self.http_client, args)?;
        delete_webhook_endpoints_webhook_endpoint_execute(builder)
    }
}
