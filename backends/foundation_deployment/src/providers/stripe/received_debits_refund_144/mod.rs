//! Auto-generated API module for stripe received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_received_debits_refund_144 `

#![cfg(feature = "stripe_received_debits_refund_144")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// ApplePayDomain response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplePayDomain {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// ApplicationFee response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationFee {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// BankAccount response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BankAccount {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// BillingCreditGrant response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BillingCreditGrant {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Charge response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Charge {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// CheckoutSession response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CheckoutSession {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// CreditNote response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreditNote {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedApplePayDomain response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedApplePayDomain {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// ForwardingRequest response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ForwardingRequest {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Invoice response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Invoice {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingAuthorization response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingAuthorization {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingCardholder response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingCardholder {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingSettlement response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingSettlement {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingTransaction response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingTransaction {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Refund response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Refund {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// ReportingReportRun response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportingReportRun {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Source response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Source {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryReceivedDebit response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryReceivedDebit {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`GetTreasuryReceivedDebits_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryReceivedDebitsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTreasuryReceivedDebitsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryReceivedDebitsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostApplicationFeesIdRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostApplicationFeesIdRefundArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostChargesChargeRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostChargesChargeRefundArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// Arguments for [`PostTestHelpersIssuingTransactionsTransactionRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersIssuingTransactionsTransactionRefundArgs {
    /// Path parameter: transaction
    pub transaction: String,
}

/// Arguments for [`GetReportingReportRuns_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetReportingReportRunsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetReportingReportRunsReportRun_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetReportingReportRunsReportRunArgs {
    /// Path parameter: report_run
    pub report_run: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`GetForwardingRequests_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetForwardingRequestsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetForwardingRequestsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetForwardingRequestsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`GetIssuingSettlementsSettlement_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingSettlementsSettlementArgs {
    /// Path parameter: settlement
    pub settlement: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostIssuingSettlementsSettlement_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIssuingSettlementsSettlementArgs {
    /// Path parameter: settlement
    pub settlement: String,
}

/// Arguments for [`PostCustomersCustomerBankAccountsIdVerify_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCustomersCustomerBankAccountsIdVerifyArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostCustomersCustomerSourcesIdVerify_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCustomersCustomerSourcesIdVerifyArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostSourcesSourceVerify_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostSourcesSourceVerifyArgs {
    /// Path parameter: source
    pub source: String,
}

/// Arguments for [`PostBillingCreditGrantsIdVoid_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostBillingCreditGrantsIdVoidArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostCreditNotesIdVoid_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCreditNotesIdVoidArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostInvoicesInvoiceVoid_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostInvoicesInvoiceVoidArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// Arguments for [`GetIssuingAuthorizations_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingAuthorizationsArgs {
    /// Query parameter: card
    pub card: Option<String>,
    /// Query parameter: cardholder
    pub cardholder: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetIssuingAuthorizationsAuthorization_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingAuthorizationsAuthorizationArgs {
    /// Path parameter: authorization
    pub authorization: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostIssuingAuthorizationsAuthorization_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIssuingAuthorizationsAuthorizationArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// Arguments for [`GetIssuingCardholders_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingCardholdersArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: email
    pub email: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: phone_number
    pub phone_number: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: type
    pub type: Option<String>,
}

/// Arguments for [`GetIssuingCardholdersCardholder_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingCardholdersCardholderArgs {
    /// Path parameter: cardholder
    pub cardholder: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostIssuingCardholdersCardholder_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIssuingCardholdersCardholderArgs {
    /// Path parameter: cardholder
    pub cardholder: String,
}

/// Arguments for [`GetCharges_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetChargesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: transfer_group
    pub transfer_group: Option<String>,
}

/// Arguments for [`GetChargesCharge_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetChargesChargeArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostChargesCharge_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostChargesChargeArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// Arguments for [`GetBillingCreditGrants_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBillingCreditGrantsArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetBillingCreditGrantsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBillingCreditGrantsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostBillingCreditGrantsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostBillingCreditGrantsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetCreditNotes_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCreditNotesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetCreditNotesId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCreditNotesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostCreditNotesId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCreditNotesIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetApplePayDomains_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetApplePayDomainsArgs {
    /// Query parameter: domain_name
    pub domain_name: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetApplePayDomainsDomain_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetApplePayDomainsDomainArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`DeleteApplePayDomainsDomain_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteApplePayDomainsDomainArgs {
    /// Path parameter: domain
    pub domain: String,
}

/// Arguments for [`PostBillingCreditGrantsIdExpire_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostBillingCreditGrantsIdExpireArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostCheckoutSessionsSessionExpire_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCheckoutSessionsSessionExpireArgs {
    /// Path parameter: session
    pub session: String,
}

/// Arguments for [`PostTestHelpersIssuingAuthorizationsAuthorizationExpire_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationExpireArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// Arguments for [`PostTestHelpersRefundsRefundExpire_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersRefundsRefundExpireArgs {
    /// Path parameter: refund
    pub refund: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/received_debits
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/received_debits - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_received_debits_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/received_debits",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/received_debits - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_received_debits_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TreasuryReceivedDebit = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/treasury/received_debits - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_received_debits_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_treasury_received_debits_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/received_debits
// -----------------------------------------------------------------------------

/// GET /v1/treasury/received_debits - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_received_debits_builder<R>(client: &SimpleHttpClient<R>, args: &GetTreasuryReceivedDebitsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/received_debits",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/received_debits - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_received_debits_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/treasury/received_debits - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_received_debits_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_treasury_received_debits_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/received_debits - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_received_debits(
    client: &SimpleHttpClient,
    args: &GetTreasuryReceivedDebitsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_treasury_received_debits_builder(client, args)?;
    get_treasury_received_debits_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/received_debits/{id}
// -----------------------------------------------------------------------------

/// GET /v1/treasury/received_debits/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_received_debits_id_builder<R>(client: &SimpleHttpClient<R>, args: &GetTreasuryReceivedDebitsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/received_debits/{id}",
        args.id,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/received_debits/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_received_debits_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TreasuryReceivedDebit = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/treasury/received_debits/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_received_debits_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_treasury_received_debits_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/received_debits/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_received_debits_id(
    client: &SimpleHttpClient,
    args: &GetTreasuryReceivedDebitsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_treasury_received_debits_id_builder(client, args)?;
    get_treasury_received_debits_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/application_fees/{id}/refund
// -----------------------------------------------------------------------------

/// POST /v1/application_fees/{id}/refund - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_application_fees_id_refund_builder<R>(client: &SimpleHttpClient<R>, args: &PostApplicationFeesIdRefundArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/application_fees/{id}/refund",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/application_fees/{id}/refund - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_application_fees_id_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ApplicationFee>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ApplicationFee = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/application_fees/{id}/refund - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_application_fees_id_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplicationFee>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_application_fees_id_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/application_fees/{id}/refund - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_application_fees_id_refund(
    client: &SimpleHttpClient,
    args: &PostApplicationFeesIdRefundArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplicationFee>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_application_fees_id_refund_builder(client, args)?;
    post_application_fees_id_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/charges/{charge}/refund
// -----------------------------------------------------------------------------

/// POST /v1/charges/{charge}/refund - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_charges_charge_refund_builder<R>(client: &SimpleHttpClient<R>, args: &PostChargesChargeRefundArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}/refund",
        args.charge,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/refund - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_charges_charge_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Charge>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Charge = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/charges/{charge}/refund - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_charges_charge_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_charges_charge_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/charges/{charge}/refund - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_charges_charge_refund(
    client: &SimpleHttpClient,
    args: &PostChargesChargeRefundArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_charges_charge_refund_builder(client, args)?;
    post_charges_charge_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/issuing/transactions/{transaction}/refund
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_issuing_transactions_transaction_refund_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersIssuingTransactionsTransactionRefundArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/transactions/{transaction}/refund",
        args.transaction,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_issuing_transactions_transaction_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingTransaction>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingTransaction = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_issuing_transactions_transaction_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_issuing_transactions_transaction_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_issuing_transactions_transaction_refund(
    client: &SimpleHttpClient,
    args: &PostTestHelpersIssuingTransactionsTransactionRefundArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_issuing_transactions_transaction_refund_builder(client, args)?;
    post_test_helpers_issuing_transactions_transaction_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/reporting/report_runs
// -----------------------------------------------------------------------------

/// GET /v1/reporting/report_runs - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_reporting_report_runs_builder<R>(client: &SimpleHttpClient<R>, args: &GetReportingReportRunsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/reporting/report_runs",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reporting/report_runs - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_reporting_report_runs_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/reporting/report_runs - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_reporting_report_runs_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_reporting_report_runs_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/reporting/report_runs - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_reporting_report_runs(
    client: &SimpleHttpClient,
    args: &GetReportingReportRunsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_reporting_report_runs_builder(client, args)?;
    get_reporting_report_runs_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/reporting/report_runs
// -----------------------------------------------------------------------------

/// POST /v1/reporting/report_runs - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_reporting_report_runs_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/reporting/report_runs",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/reporting/report_runs - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_reporting_report_runs_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ReportingReportRun = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/reporting/report_runs - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_reporting_report_runs_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_reporting_report_runs_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/reporting/report_runs/{report_run}
// -----------------------------------------------------------------------------

/// GET /v1/reporting/report_runs/{report_run} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_reporting_report_runs_report_run_builder<R>(client: &SimpleHttpClient<R>, args: &GetReportingReportRunsReportRunArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/reporting/report_runs/{report_run}",
        args.report_run,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reporting/report_runs/{report_run} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_reporting_report_runs_report_run_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ReportingReportRun = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/reporting/report_runs/{report_run} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_reporting_report_runs_report_run_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_reporting_report_runs_report_run_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/reporting/report_runs/{report_run} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_reporting_report_runs_report_run(
    client: &SimpleHttpClient,
    args: &GetReportingReportRunsReportRunArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_reporting_report_runs_report_run_builder(client, args)?;
    get_reporting_report_runs_report_run_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/forwarding/requests
// -----------------------------------------------------------------------------

/// GET /v1/forwarding/requests - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_forwarding_requests_builder<R>(client: &SimpleHttpClient<R>, args: &GetForwardingRequestsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/forwarding/requests",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/forwarding/requests - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_forwarding_requests_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/forwarding/requests - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_forwarding_requests_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_forwarding_requests_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/forwarding/requests - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_forwarding_requests(
    client: &SimpleHttpClient,
    args: &GetForwardingRequestsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_forwarding_requests_builder(client, args)?;
    get_forwarding_requests_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/forwarding/requests
// -----------------------------------------------------------------------------

/// POST /v1/forwarding/requests - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_forwarding_requests_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/forwarding/requests",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/forwarding/requests - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_forwarding_requests_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ForwardingRequest = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/forwarding/requests - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_forwarding_requests_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_forwarding_requests_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/forwarding/requests/{id}
// -----------------------------------------------------------------------------

/// GET /v1/forwarding/requests/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_forwarding_requests_id_builder<R>(client: &SimpleHttpClient<R>, args: &GetForwardingRequestsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/forwarding/requests/{id}",
        args.id,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/forwarding/requests/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_forwarding_requests_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ForwardingRequest = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/forwarding/requests/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_forwarding_requests_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_forwarding_requests_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/forwarding/requests/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_forwarding_requests_id(
    client: &SimpleHttpClient,
    args: &GetForwardingRequestsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_forwarding_requests_id_builder(client, args)?;
    get_forwarding_requests_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/settlements/{settlement}
// -----------------------------------------------------------------------------

/// GET /v1/issuing/settlements/{settlement} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_settlements_settlement_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingSettlementsSettlementArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/settlements/{settlement}",
        args.settlement,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/settlements/{settlement} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_settlements_settlement_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingSettlement = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/settlements/{settlement} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_settlements_settlement_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_settlements_settlement_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/settlements/{settlement} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_settlements_settlement(
    client: &SimpleHttpClient,
    args: &GetIssuingSettlementsSettlementArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_settlements_settlement_builder(client, args)?;
    get_issuing_settlements_settlement_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/settlements/{settlement}
// -----------------------------------------------------------------------------

/// POST /v1/issuing/settlements/{settlement} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_settlements_settlement_builder<R>(client: &SimpleHttpClient<R>, args: &PostIssuingSettlementsSettlementArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/settlements/{settlement}",
        args.settlement,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/settlements/{settlement} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_settlements_settlement_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingSettlement = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/issuing/settlements/{settlement} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_settlements_settlement_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_issuing_settlements_settlement_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/settlements/{settlement} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_issuing_settlements_settlement(
    client: &SimpleHttpClient,
    args: &PostIssuingSettlementsSettlementArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_issuing_settlements_settlement_builder(client, args)?;
    post_issuing_settlements_settlement_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/issuing/settlements
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/issuing/settlements - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_issuing_settlements_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/settlements",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/settlements - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_issuing_settlements_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingSettlement = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/issuing/settlements - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_issuing_settlements_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_issuing_settlements_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// POST /v1/customers/{customer}/bank_accounts/{id}/verify
// -----------------------------------------------------------------------------

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_customers_customer_bank_accounts_id_verify_builder<R>(client: &SimpleHttpClient<R>, args: &PostCustomersCustomerBankAccountsIdVerifyArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}/bank_accounts/{id}/verify",
        args.customer,
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_customers_customer_bank_accounts_id_verify_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BankAccount>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BankAccount = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_customers_customer_bank_accounts_id_verify_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BankAccount>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_customers_customer_bank_accounts_id_verify_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_customers_customer_bank_accounts_id_verify(
    client: &SimpleHttpClient,
    args: &PostCustomersCustomerBankAccountsIdVerifyArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BankAccount>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_customers_customer_bank_accounts_id_verify_builder(client, args)?;
    post_customers_customer_bank_accounts_id_verify_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/customers/{customer}/sources/{id}/verify
// -----------------------------------------------------------------------------

/// POST /v1/customers/{customer}/sources/{id}/verify - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_customers_customer_sources_id_verify_builder<R>(client: &SimpleHttpClient<R>, args: &PostCustomersCustomerSourcesIdVerifyArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}/sources/{id}/verify",
        args.customer,
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/sources/{id}/verify - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_customers_customer_sources_id_verify_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BankAccount>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BankAccount = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/customers/{customer}/sources/{id}/verify - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_customers_customer_sources_id_verify_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BankAccount>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_customers_customer_sources_id_verify_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/customers/{customer}/sources/{id}/verify - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_customers_customer_sources_id_verify(
    client: &SimpleHttpClient,
    args: &PostCustomersCustomerSourcesIdVerifyArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BankAccount>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_customers_customer_sources_id_verify_builder(client, args)?;
    post_customers_customer_sources_id_verify_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/sources/{source}/verify
// -----------------------------------------------------------------------------

/// POST /v1/sources/{source}/verify - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_sources_source_verify_builder<R>(client: &SimpleHttpClient<R>, args: &PostSourcesSourceVerifyArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/sources/{source}/verify",
        args.source,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/sources/{source}/verify - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_sources_source_verify_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Source>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Source = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/sources/{source}/verify - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_sources_source_verify_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Source>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_sources_source_verify_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/sources/{source}/verify - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_sources_source_verify(
    client: &SimpleHttpClient,
    args: &PostSourcesSourceVerifyArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Source>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_sources_source_verify_builder(client, args)?;
    post_sources_source_verify_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/billing/credit_grants/{id}/void
// -----------------------------------------------------------------------------

/// POST /v1/billing/credit_grants/{id}/void - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_billing_credit_grants_id_void_builder<R>(client: &SimpleHttpClient<R>, args: &PostBillingCreditGrantsIdVoidArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/billing/credit_grants/{id}/void",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants/{id}/void - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_billing_credit_grants_id_void_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BillingCreditGrant = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/billing/credit_grants/{id}/void - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_billing_credit_grants_id_void_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_billing_credit_grants_id_void_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/credit_grants/{id}/void - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_billing_credit_grants_id_void(
    client: &SimpleHttpClient,
    args: &PostBillingCreditGrantsIdVoidArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_billing_credit_grants_id_void_builder(client, args)?;
    post_billing_credit_grants_id_void_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/credit_notes/{id}/void
// -----------------------------------------------------------------------------

/// POST /v1/credit_notes/{id}/void - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_credit_notes_id_void_builder<R>(client: &SimpleHttpClient<R>, args: &PostCreditNotesIdVoidArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/credit_notes/{id}/void",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/credit_notes/{id}/void - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_credit_notes_id_void_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<CreditNote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: CreditNote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/credit_notes/{id}/void - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_credit_notes_id_void_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_credit_notes_id_void_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/credit_notes/{id}/void - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_credit_notes_id_void(
    client: &SimpleHttpClient,
    args: &PostCreditNotesIdVoidArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_credit_notes_id_void_builder(client, args)?;
    post_credit_notes_id_void_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/invoices/{invoice}/void
// -----------------------------------------------------------------------------

/// POST /v1/invoices/{invoice}/void - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_invoices_invoice_void_builder<R>(client: &SimpleHttpClient<R>, args: &PostInvoicesInvoiceVoidArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoices/{invoice}/void",
        args.invoice,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/void - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_invoices_invoice_void_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Invoice>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Invoice = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/invoices/{invoice}/void - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_invoices_invoice_void_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_invoices_invoice_void_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoices/{invoice}/void - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_invoices_invoice_void(
    client: &SimpleHttpClient,
    args: &PostInvoicesInvoiceVoidArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_invoices_invoice_void_builder(client, args)?;
    post_invoices_invoice_void_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/authorizations
// -----------------------------------------------------------------------------

/// GET /v1/issuing/authorizations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_authorizations_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingAuthorizationsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/authorizations",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/authorizations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_authorizations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/authorizations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_authorizations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_authorizations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/authorizations - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_authorizations(
    client: &SimpleHttpClient,
    args: &GetIssuingAuthorizationsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_authorizations_builder(client, args)?;
    get_issuing_authorizations_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/authorizations/{authorization}
// -----------------------------------------------------------------------------

/// GET /v1/issuing/authorizations/{authorization} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_authorizations_authorization_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingAuthorizationsAuthorizationArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/authorizations/{authorization}",
        args.authorization,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/authorizations/{authorization} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_authorizations_authorization_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingAuthorization = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/authorizations/{authorization} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_authorizations_authorization_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_authorizations_authorization_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/authorizations/{authorization} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_authorizations_authorization(
    client: &SimpleHttpClient,
    args: &GetIssuingAuthorizationsAuthorizationArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_authorizations_authorization_builder(client, args)?;
    get_issuing_authorizations_authorization_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/authorizations/{authorization}
// -----------------------------------------------------------------------------

/// POST /v1/issuing/authorizations/{authorization} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_authorizations_authorization_builder<R>(client: &SimpleHttpClient<R>, args: &PostIssuingAuthorizationsAuthorizationArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/authorizations/{authorization}",
        args.authorization,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/authorizations/{authorization} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_authorizations_authorization_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingAuthorization = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/issuing/authorizations/{authorization} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_authorizations_authorization_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_issuing_authorizations_authorization_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/authorizations/{authorization} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_issuing_authorizations_authorization(
    client: &SimpleHttpClient,
    args: &PostIssuingAuthorizationsAuthorizationArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_issuing_authorizations_authorization_builder(client, args)?;
    post_issuing_authorizations_authorization_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/issuing/authorizations
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/issuing/authorizations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_issuing_authorizations_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_issuing_authorizations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingAuthorization = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/issuing/authorizations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_issuing_authorizations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_issuing_authorizations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/cardholders
// -----------------------------------------------------------------------------

/// GET /v1/issuing/cardholders - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_cardholders_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingCardholdersArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/cardholders",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/cardholders - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_cardholders_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/cardholders - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_cardholders_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_cardholders_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/cardholders - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_cardholders(
    client: &SimpleHttpClient,
    args: &GetIssuingCardholdersArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_cardholders_builder(client, args)?;
    get_issuing_cardholders_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/cardholders
// -----------------------------------------------------------------------------

/// POST /v1/issuing/cardholders - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_cardholders_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/cardholders",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/cardholders - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_cardholders_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingCardholder = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/issuing/cardholders - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_cardholders_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_issuing_cardholders_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/cardholders/{cardholder}
// -----------------------------------------------------------------------------

/// GET /v1/issuing/cardholders/{cardholder} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_cardholders_cardholder_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingCardholdersCardholderArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/cardholders/{cardholder}",
        args.cardholder,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/cardholders/{cardholder} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_cardholders_cardholder_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingCardholder = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/cardholders/{cardholder} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_cardholders_cardholder_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_cardholders_cardholder_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/cardholders/{cardholder} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_cardholders_cardholder(
    client: &SimpleHttpClient,
    args: &GetIssuingCardholdersCardholderArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_cardholders_cardholder_builder(client, args)?;
    get_issuing_cardholders_cardholder_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/cardholders/{cardholder}
// -----------------------------------------------------------------------------

/// POST /v1/issuing/cardholders/{cardholder} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_cardholders_cardholder_builder<R>(client: &SimpleHttpClient<R>, args: &PostIssuingCardholdersCardholderArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/cardholders/{cardholder}",
        args.cardholder,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/cardholders/{cardholder} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_cardholders_cardholder_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingCardholder = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/issuing/cardholders/{cardholder} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_cardholders_cardholder_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_issuing_cardholders_cardholder_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/cardholders/{cardholder} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_issuing_cardholders_cardholder(
    client: &SimpleHttpClient,
    args: &PostIssuingCardholdersCardholderArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_issuing_cardholders_cardholder_builder(client, args)?;
    post_issuing_cardholders_cardholder_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/charges
// -----------------------------------------------------------------------------

/// GET /v1/charges - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_charges_builder<R>(client: &SimpleHttpClient<R>, args: &GetChargesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_charges_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/charges - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_charges_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_charges_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/charges - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_charges(
    client: &SimpleHttpClient,
    args: &GetChargesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_charges_builder(client, args)?;
    get_charges_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/charges
// -----------------------------------------------------------------------------

/// POST /v1/charges - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_charges_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_charges_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Charge>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Charge = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/charges - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_charges_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_charges_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/charges/{charge}
// -----------------------------------------------------------------------------

/// GET /v1/charges/{charge} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_charges_charge_builder<R>(client: &SimpleHttpClient<R>, args: &GetChargesChargeArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}",
        args.charge,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_charges_charge_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Charge>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Charge = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/charges/{charge} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_charges_charge_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_charges_charge_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/charges/{charge} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_charges_charge(
    client: &SimpleHttpClient,
    args: &GetChargesChargeArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_charges_charge_builder(client, args)?;
    get_charges_charge_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/charges/{charge}
// -----------------------------------------------------------------------------

/// POST /v1/charges/{charge} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_charges_charge_builder<R>(client: &SimpleHttpClient<R>, args: &PostChargesChargeArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}",
        args.charge,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_charges_charge_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Charge>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Charge = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/charges/{charge} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_charges_charge_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_charges_charge_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/charges/{charge} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_charges_charge(
    client: &SimpleHttpClient,
    args: &PostChargesChargeArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_charges_charge_builder(client, args)?;
    post_charges_charge_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/billing/credit_grants
// -----------------------------------------------------------------------------

/// GET /v1/billing/credit_grants - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_billing_credit_grants_builder<R>(client: &SimpleHttpClient<R>, args: &GetBillingCreditGrantsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/billing/credit_grants",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_grants - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_billing_credit_grants_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/billing/credit_grants - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_billing_credit_grants_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_billing_credit_grants_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/billing/credit_grants - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_billing_credit_grants(
    client: &SimpleHttpClient,
    args: &GetBillingCreditGrantsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_billing_credit_grants_builder(client, args)?;
    get_billing_credit_grants_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/billing/credit_grants
// -----------------------------------------------------------------------------

/// POST /v1/billing/credit_grants - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_billing_credit_grants_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/billing/credit_grants",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_billing_credit_grants_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BillingCreditGrant = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/billing/credit_grants - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_billing_credit_grants_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_billing_credit_grants_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/billing/credit_grants/{id}
// -----------------------------------------------------------------------------

/// GET /v1/billing/credit_grants/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_billing_credit_grants_id_builder<R>(client: &SimpleHttpClient<R>, args: &GetBillingCreditGrantsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/billing/credit_grants/{id}",
        args.id,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_grants/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_billing_credit_grants_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BillingCreditGrant = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/billing/credit_grants/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_billing_credit_grants_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_billing_credit_grants_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/billing/credit_grants/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_billing_credit_grants_id(
    client: &SimpleHttpClient,
    args: &GetBillingCreditGrantsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_billing_credit_grants_id_builder(client, args)?;
    get_billing_credit_grants_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/billing/credit_grants/{id}
// -----------------------------------------------------------------------------

/// POST /v1/billing/credit_grants/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_billing_credit_grants_id_builder<R>(client: &SimpleHttpClient<R>, args: &PostBillingCreditGrantsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/billing/credit_grants/{id}",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_billing_credit_grants_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BillingCreditGrant = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/billing/credit_grants/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_billing_credit_grants_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_billing_credit_grants_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/credit_grants/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_billing_credit_grants_id(
    client: &SimpleHttpClient,
    args: &PostBillingCreditGrantsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_billing_credit_grants_id_builder(client, args)?;
    post_billing_credit_grants_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/credit_notes
// -----------------------------------------------------------------------------

/// GET /v1/credit_notes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_credit_notes_builder<R>(client: &SimpleHttpClient<R>, args: &GetCreditNotesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/credit_notes",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_credit_notes_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/credit_notes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_credit_notes_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_credit_notes_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/credit_notes - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_credit_notes(
    client: &SimpleHttpClient,
    args: &GetCreditNotesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_credit_notes_builder(client, args)?;
    get_credit_notes_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/credit_notes
// -----------------------------------------------------------------------------

/// POST /v1/credit_notes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_credit_notes_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/credit_notes",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/credit_notes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_credit_notes_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<CreditNote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: CreditNote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/credit_notes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_credit_notes_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_credit_notes_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/credit_notes/{id}
// -----------------------------------------------------------------------------

/// GET /v1/credit_notes/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_credit_notes_id_builder<R>(client: &SimpleHttpClient<R>, args: &GetCreditNotesIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/credit_notes/{id}",
        args.id,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_credit_notes_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<CreditNote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: CreditNote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/credit_notes/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_credit_notes_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_credit_notes_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/credit_notes/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_credit_notes_id(
    client: &SimpleHttpClient,
    args: &GetCreditNotesIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_credit_notes_id_builder(client, args)?;
    get_credit_notes_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/credit_notes/{id}
// -----------------------------------------------------------------------------

/// POST /v1/credit_notes/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_credit_notes_id_builder<R>(client: &SimpleHttpClient<R>, args: &PostCreditNotesIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/credit_notes/{id}",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/credit_notes/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_credit_notes_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<CreditNote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: CreditNote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/credit_notes/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_credit_notes_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_credit_notes_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/credit_notes/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_credit_notes_id(
    client: &SimpleHttpClient,
    args: &PostCreditNotesIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_credit_notes_id_builder(client, args)?;
    post_credit_notes_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/apple_pay/domains
// -----------------------------------------------------------------------------

/// GET /v1/apple_pay/domains - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_apple_pay_domains_builder<R>(client: &SimpleHttpClient<R>, args: &GetApplePayDomainsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/apple_pay/domains",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/apple_pay/domains - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_apple_pay_domains_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/apple_pay/domains - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_apple_pay_domains_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_apple_pay_domains_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/apple_pay/domains - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_apple_pay_domains(
    client: &SimpleHttpClient,
    args: &GetApplePayDomainsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_apple_pay_domains_builder(client, args)?;
    get_apple_pay_domains_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/apple_pay/domains
// -----------------------------------------------------------------------------

/// POST /v1/apple_pay/domains - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_apple_pay_domains_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/apple_pay/domains",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/apple_pay/domains - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_apple_pay_domains_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ApplePayDomain = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/apple_pay/domains - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_apple_pay_domains_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_apple_pay_domains_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/apple_pay/domains/{domain}
// -----------------------------------------------------------------------------

/// GET /v1/apple_pay/domains/{domain} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_apple_pay_domains_domain_builder<R>(client: &SimpleHttpClient<R>, args: &GetApplePayDomainsDomainArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/apple_pay/domains/{domain}",
        args.domain,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/apple_pay/domains/{domain} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_apple_pay_domains_domain_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ApplePayDomain = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/apple_pay/domains/{domain} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_apple_pay_domains_domain_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_apple_pay_domains_domain_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/apple_pay/domains/{domain} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_apple_pay_domains_domain(
    client: &SimpleHttpClient,
    args: &GetApplePayDomainsDomainArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_apple_pay_domains_domain_builder(client, args)?;
    get_apple_pay_domains_domain_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/apple_pay/domains/{domain}
// -----------------------------------------------------------------------------

/// DELETE /v1/apple_pay/domains/{domain} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_apple_pay_domains_domain_builder<R>(client: &SimpleHttpClient<R>, args: &DeleteApplePayDomainsDomainArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/apple_pay/domains/{domain}",
        args.domain,
    );

    let builder = client.delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/apple_pay/domains/{domain} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_apple_pay_domains_domain_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<DeletedApplePayDomain>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: DeletedApplePayDomain = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// DELETE /v1/apple_pay/domains/{domain} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_apple_pay_domains_domain_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<DeletedApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = delete_apple_pay_domains_domain_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/apple_pay/domains/{domain} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_apple_pay_domains_domain(
    client: &SimpleHttpClient,
    args: &DeleteApplePayDomainsDomainArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<DeletedApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = delete_apple_pay_domains_domain_builder(client, args)?;
    delete_apple_pay_domains_domain_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/billing/credit_grants/{id}/expire
// -----------------------------------------------------------------------------

/// POST /v1/billing/credit_grants/{id}/expire - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_billing_credit_grants_id_expire_builder<R>(client: &SimpleHttpClient<R>, args: &PostBillingCreditGrantsIdExpireArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/billing/credit_grants/{id}/expire",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants/{id}/expire - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_billing_credit_grants_id_expire_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: BillingCreditGrant = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/billing/credit_grants/{id}/expire - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_billing_credit_grants_id_expire_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_billing_credit_grants_id_expire_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/credit_grants/{id}/expire - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_billing_credit_grants_id_expire(
    client: &SimpleHttpClient,
    args: &PostBillingCreditGrantsIdExpireArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_billing_credit_grants_id_expire_builder(client, args)?;
    post_billing_credit_grants_id_expire_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/checkout/sessions/{session}/expire
// -----------------------------------------------------------------------------

/// POST /v1/checkout/sessions/{session}/expire - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_checkout_sessions_session_expire_builder<R>(client: &SimpleHttpClient<R>, args: &PostCheckoutSessionsSessionExpireArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/checkout/sessions/{session}/expire",
        args.session,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/checkout/sessions/{session}/expire - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_checkout_sessions_session_expire_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<CheckoutSession>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: CheckoutSession = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/checkout/sessions/{session}/expire - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_checkout_sessions_session_expire_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_checkout_sessions_session_expire_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/checkout/sessions/{session}/expire - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_checkout_sessions_session_expire(
    client: &SimpleHttpClient,
    args: &PostCheckoutSessionsSessionExpireArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_checkout_sessions_session_expire_builder(client, args)?;
    post_checkout_sessions_session_expire_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_issuing_authorizations_authorization_expire_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersIssuingAuthorizationsAuthorizationExpireArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{authorization}/expire",
        args.authorization,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_issuing_authorizations_authorization_expire_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingAuthorization = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_issuing_authorizations_authorization_expire_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_issuing_authorizations_authorization_expire_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_issuing_authorizations_authorization_expire(
    client: &SimpleHttpClient,
    args: &PostTestHelpersIssuingAuthorizationsAuthorizationExpireArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_issuing_authorizations_authorization_expire_builder(client, args)?;
    post_test_helpers_issuing_authorizations_authorization_expire_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/refunds/{refund}/expire
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/refunds/{refund}/expire - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_refunds_refund_expire_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersRefundsRefundExpireArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/refunds/{refund}/expire",
        args.refund,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/refunds/{refund}/expire - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_refunds_refund_expire_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Refund>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Refund = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/refunds/{refund}/expire - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_refunds_refund_expire_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_refunds_refund_expire_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/refunds/{refund}/expire - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_refunds_refund_expire(
    client: &SimpleHttpClient,
    args: &PostTestHelpersRefundsRefundExpireArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_refunds_refund_expire_builder(client, args)?;
    post_test_helpers_refunds_refund_expire_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// POST /v1/test_helpers/treasury/received_debits.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_test_helpers_treasury_received_debits(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_treasury_received_debits_builder(&self.http_client
        )?;
        post_test_helpers_treasury_received_debits_execute(builder)
    }

    /// GET /v1/treasury/received_debits.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_treasury_received_debits(&self, 
        args: &GetTreasuryReceivedDebitsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_treasury_received_debits_builder(&self.http_client
            , args
        )?;
        get_treasury_received_debits_execute(builder)
    }

    /// GET /v1/treasury/received_debits/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_treasury_received_debits_id(&self, 
        args: &GetTreasuryReceivedDebitsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryReceivedDebit>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_treasury_received_debits_id_builder(&self.http_client
            , args
        )?;
        get_treasury_received_debits_id_execute(builder)
    }

    /// POST /v1/application_fees/{id}/refund.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_application_fees_id_refund(&self, 
        args: &PostApplicationFeesIdRefundArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplicationFee>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_application_fees_id_refund_builder(&self.http_client
            , args
        )?;
        post_application_fees_id_refund_execute(builder)
    }

    /// POST /v1/charges/{charge}/refund.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_charges_charge_refund(&self, 
        args: &PostChargesChargeRefundArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_charges_charge_refund_builder(&self.http_client
            , args
        )?;
        post_charges_charge_refund_execute(builder)
    }

    /// POST /v1/test_helpers/issuing/transactions/{transaction}/refund.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_test_helpers_issuing_transactions_transaction_refund(&self, 
        args: &PostTestHelpersIssuingTransactionsTransactionRefundArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_issuing_transactions_transaction_refund_builder(&self.http_client
            , args
        )?;
        post_test_helpers_issuing_transactions_transaction_refund_execute(builder)
    }

    /// GET /v1/reporting/report_runs.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_reporting_report_runs(&self, 
        args: &GetReportingReportRunsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_reporting_report_runs_builder(&self.http_client
            , args
        )?;
        get_reporting_report_runs_execute(builder)
    }

    /// POST /v1/reporting/report_runs.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_reporting_report_runs(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_reporting_report_runs_builder(&self.http_client
        )?;
        post_reporting_report_runs_execute(builder)
    }

    /// GET /v1/reporting/report_runs/{report_run}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_reporting_report_runs_report_run(&self, 
        args: &GetReportingReportRunsReportRunArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ReportingReportRun>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_reporting_report_runs_report_run_builder(&self.http_client
            , args
        )?;
        get_reporting_report_runs_report_run_execute(builder)
    }

    /// GET /v1/forwarding/requests.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_forwarding_requests(&self, 
        args: &GetForwardingRequestsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_forwarding_requests_builder(&self.http_client
            , args
        )?;
        get_forwarding_requests_execute(builder)
    }

    /// POST /v1/forwarding/requests.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_forwarding_requests(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_forwarding_requests_builder(&self.http_client
        )?;
        post_forwarding_requests_execute(builder)
    }

    /// GET /v1/forwarding/requests/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_forwarding_requests_id(&self, 
        args: &GetForwardingRequestsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_forwarding_requests_id_builder(&self.http_client
            , args
        )?;
        get_forwarding_requests_id_execute(builder)
    }

    /// GET /v1/issuing/settlements/{settlement}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_issuing_settlements_settlement(&self, 
        args: &GetIssuingSettlementsSettlementArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_settlements_settlement_builder(&self.http_client
            , args
        )?;
        get_issuing_settlements_settlement_execute(builder)
    }

    /// POST /v1/issuing/settlements/{settlement}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_issuing_settlements_settlement(&self, 
        args: &PostIssuingSettlementsSettlementArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_issuing_settlements_settlement_builder(&self.http_client
            , args
        )?;
        post_issuing_settlements_settlement_execute(builder)
    }

    /// POST /v1/test_helpers/issuing/settlements.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_test_helpers_issuing_settlements(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_issuing_settlements_builder(&self.http_client
        )?;
        post_test_helpers_issuing_settlements_execute(builder)
    }

    /// POST /v1/customers/{customer}/bank_accounts/{id}/verify.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_customers_customer_bank_accounts_id_verify(&self, 
        args: &PostCustomersCustomerBankAccountsIdVerifyArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BankAccount>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_customers_customer_bank_accounts_id_verify_builder(&self.http_client
            , args
        )?;
        post_customers_customer_bank_accounts_id_verify_execute(builder)
    }

    /// POST /v1/customers/{customer}/sources/{id}/verify.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_customers_customer_sources_id_verify(&self, 
        args: &PostCustomersCustomerSourcesIdVerifyArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BankAccount>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_customers_customer_sources_id_verify_builder(&self.http_client
            , args
        )?;
        post_customers_customer_sources_id_verify_execute(builder)
    }

    /// POST /v1/sources/{source}/verify.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_sources_source_verify(&self, 
        args: &PostSourcesSourceVerifyArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Source>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_sources_source_verify_builder(&self.http_client
            , args
        )?;
        post_sources_source_verify_execute(builder)
    }

    /// POST /v1/billing/credit_grants/{id}/void.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_billing_credit_grants_id_void(&self, 
        args: &PostBillingCreditGrantsIdVoidArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_billing_credit_grants_id_void_builder(&self.http_client
            , args
        )?;
        post_billing_credit_grants_id_void_execute(builder)
    }

    /// POST /v1/credit_notes/{id}/void.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_credit_notes_id_void(&self, 
        args: &PostCreditNotesIdVoidArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_credit_notes_id_void_builder(&self.http_client
            , args
        )?;
        post_credit_notes_id_void_execute(builder)
    }

    /// POST /v1/invoices/{invoice}/void.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_invoices_invoice_void(&self, 
        args: &PostInvoicesInvoiceVoidArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_invoices_invoice_void_builder(&self.http_client
            , args
        )?;
        post_invoices_invoice_void_execute(builder)
    }

    /// GET /v1/issuing/authorizations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_issuing_authorizations(&self, 
        args: &GetIssuingAuthorizationsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_authorizations_builder(&self.http_client
            , args
        )?;
        get_issuing_authorizations_execute(builder)
    }

    /// GET /v1/issuing/authorizations/{authorization}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_issuing_authorizations_authorization(&self, 
        args: &GetIssuingAuthorizationsAuthorizationArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_authorizations_authorization_builder(&self.http_client
            , args
        )?;
        get_issuing_authorizations_authorization_execute(builder)
    }

    /// POST /v1/issuing/authorizations/{authorization}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_issuing_authorizations_authorization(&self, 
        args: &PostIssuingAuthorizationsAuthorizationArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_issuing_authorizations_authorization_builder(&self.http_client
            , args
        )?;
        post_issuing_authorizations_authorization_execute(builder)
    }

    /// POST /v1/test_helpers/issuing/authorizations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_test_helpers_issuing_authorizations(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_issuing_authorizations_builder(&self.http_client
        )?;
        post_test_helpers_issuing_authorizations_execute(builder)
    }

    /// GET /v1/issuing/cardholders.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_issuing_cardholders(&self, 
        args: &GetIssuingCardholdersArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_cardholders_builder(&self.http_client
            , args
        )?;
        get_issuing_cardholders_execute(builder)
    }

    /// POST /v1/issuing/cardholders.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_issuing_cardholders(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_issuing_cardholders_builder(&self.http_client
        )?;
        post_issuing_cardholders_execute(builder)
    }

    /// GET /v1/issuing/cardholders/{cardholder}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_issuing_cardholders_cardholder(&self, 
        args: &GetIssuingCardholdersCardholderArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_cardholders_cardholder_builder(&self.http_client
            , args
        )?;
        get_issuing_cardholders_cardholder_execute(builder)
    }

    /// POST /v1/issuing/cardholders/{cardholder}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_issuing_cardholders_cardholder(&self, 
        args: &PostIssuingCardholdersCardholderArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_issuing_cardholders_cardholder_builder(&self.http_client
            , args
        )?;
        post_issuing_cardholders_cardholder_execute(builder)
    }

    /// GET /v1/charges.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_charges(&self, 
        args: &GetChargesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_charges_builder(&self.http_client
            , args
        )?;
        get_charges_execute(builder)
    }

    /// POST /v1/charges.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_charges(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_charges_builder(&self.http_client
        )?;
        post_charges_execute(builder)
    }

    /// GET /v1/charges/{charge}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_charges_charge(&self, 
        args: &GetChargesChargeArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_charges_charge_builder(&self.http_client
            , args
        )?;
        get_charges_charge_execute(builder)
    }

    /// POST /v1/charges/{charge}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_charges_charge(&self, 
        args: &PostChargesChargeArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Charge>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_charges_charge_builder(&self.http_client
            , args
        )?;
        post_charges_charge_execute(builder)
    }

    /// GET /v1/billing/credit_grants.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_billing_credit_grants(&self, 
        args: &GetBillingCreditGrantsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_billing_credit_grants_builder(&self.http_client
            , args
        )?;
        get_billing_credit_grants_execute(builder)
    }

    /// POST /v1/billing/credit_grants.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_billing_credit_grants(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_billing_credit_grants_builder(&self.http_client
        )?;
        post_billing_credit_grants_execute(builder)
    }

    /// GET /v1/billing/credit_grants/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_billing_credit_grants_id(&self, 
        args: &GetBillingCreditGrantsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_billing_credit_grants_id_builder(&self.http_client
            , args
        )?;
        get_billing_credit_grants_id_execute(builder)
    }

    /// POST /v1/billing/credit_grants/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_billing_credit_grants_id(&self, 
        args: &PostBillingCreditGrantsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_billing_credit_grants_id_builder(&self.http_client
            , args
        )?;
        post_billing_credit_grants_id_execute(builder)
    }

    /// GET /v1/credit_notes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_credit_notes(&self, 
        args: &GetCreditNotesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_credit_notes_builder(&self.http_client
            , args
        )?;
        get_credit_notes_execute(builder)
    }

    /// POST /v1/credit_notes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_credit_notes(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_credit_notes_builder(&self.http_client
        )?;
        post_credit_notes_execute(builder)
    }

    /// GET /v1/credit_notes/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_credit_notes_id(&self, 
        args: &GetCreditNotesIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_credit_notes_id_builder(&self.http_client
            , args
        )?;
        get_credit_notes_id_execute(builder)
    }

    /// POST /v1/credit_notes/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_credit_notes_id(&self, 
        args: &PostCreditNotesIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<CreditNote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_credit_notes_id_builder(&self.http_client
            , args
        )?;
        post_credit_notes_id_execute(builder)
    }

    /// GET /v1/apple_pay/domains.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_apple_pay_domains(&self, 
        args: &GetApplePayDomainsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_apple_pay_domains_builder(&self.http_client
            , args
        )?;
        get_apple_pay_domains_execute(builder)
    }

    /// POST /v1/apple_pay/domains.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_apple_pay_domains(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_apple_pay_domains_builder(&self.http_client
        )?;
        post_apple_pay_domains_execute(builder)
    }

    /// GET /v1/apple_pay/domains/{domain}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_get_apple_pay_domains_domain(&self, 
        args: &GetApplePayDomainsDomainArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_apple_pay_domains_domain_builder(&self.http_client
            , args
        )?;
        get_apple_pay_domains_domain_execute(builder)
    }

    /// DELETE /v1/apple_pay/domains/{domain}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_delete_apple_pay_domains_domain(&self, 
        args: &DeleteApplePayDomainsDomainArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<DeletedApplePayDomain>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = delete_apple_pay_domains_domain_builder(&self.http_client
            , args
        )?;
        delete_apple_pay_domains_domain_execute(builder)
    }

    /// POST /v1/billing/credit_grants/{id}/expire.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_billing_credit_grants_id_expire(&self, 
        args: &PostBillingCreditGrantsIdExpireArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<BillingCreditGrant>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_billing_credit_grants_id_expire_builder(&self.http_client
            , args
        )?;
        post_billing_credit_grants_id_expire_execute(builder)
    }

    /// POST /v1/checkout/sessions/{session}/expire.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_checkout_sessions_session_expire(&self, 
        args: &PostCheckoutSessionsSessionExpireArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_checkout_sessions_session_expire_builder(&self.http_client
            , args
        )?;
        post_checkout_sessions_session_expire_execute(builder)
    }

    /// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_test_helpers_issuing_authorizations_authorization_expire(&self, 
        args: &PostTestHelpersIssuingAuthorizationsAuthorizationExpireArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_issuing_authorizations_authorization_expire_builder(&self.http_client
            , args
        )?;
        post_test_helpers_issuing_authorizations_authorization_expire_execute(builder)
    }

    /// POST /v1/test_helpers/refunds/{refund}/expire.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_received_debits_refund_report_runs_requests_settlements_verify_void_authorizations_cardholders_charges_credit_grants_credit_notes_domains_expire_post_test_helpers_refunds_refund_expire(&self, 
        args: &PostTestHelpersRefundsRefundExpireArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_refunds_refund_expire_builder(&self.http_client
            , args
        )?;
        post_test_helpers_refunds_refund_expire_execute(builder)
    }

}

