//! Auto-generated API module for stripe fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_fail_file_links_174 `

#![cfg(feature = "stripe_fail_file_links_174")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// BillingMeter response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BillingMeter {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// ClimateOrder response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClimateOrder {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// FileLink response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FileLink {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingCard response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingCard {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// LineItem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LineItem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// PaymentIntent response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentIntent {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// PaymentLink response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentLink {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// PaymentMethodConfiguration response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentMethodConfiguration {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// PaymentMethodDomain response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentMethodDomain {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryFinancialAccount response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryFinancialAccount {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryInboundTransfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryInboundTransfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryOutboundPayment response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryOutboundPayment {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryOutboundTransfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryOutboundTransfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`PostTestHelpersIssuingCardsCardShippingFail_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersIssuingCardsCardShippingFailArgs {
    /// Path parameter: card
    pub card: String,
}

/// Arguments for [`PostTestHelpersTreasuryInboundTransfersIdFail_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryInboundTransfersIdFailArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostTestHelpersTreasuryOutboundPaymentsIdFail_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryOutboundPaymentsIdFailArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostTestHelpersTreasuryOutboundTransfersOutboundTransferFail_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransferFailArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// Arguments for [`GetFileLinks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetFileLinksArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: expired
    pub expired: Option<String>,
    /// Query parameter: file
    pub file: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetFileLinksLink_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetFileLinksLinkArgs {
    /// Path parameter: link
    pub link: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostFileLinksLink_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostFileLinksLinkArgs {
    /// Path parameter: link
    pub link: String,
}

/// Arguments for [`GetTreasuryFinancialAccounts_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryFinancialAccountsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTreasuryFinancialAccountsFinancialAccount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryFinancialAccountsFinancialAccountArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTreasuryFinancialAccountsFinancialAccount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTreasuryFinancialAccountsFinancialAccountArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
}

/// Arguments for [`GetCreditNotesPreviewLines_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCreditNotesPreviewLinesArgs {
    /// Query parameter: amount
    pub amount: Option<String>,
    /// Query parameter: credit_amount
    pub credit_amount: Option<String>,
    /// Query parameter: effective_at
    pub effective_at: Option<String>,
    /// Query parameter: email_type
    pub email_type: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: lines
    pub lines: Option<String>,
    /// Query parameter: memo
    pub memo: Option<String>,
    /// Query parameter: metadata
    pub metadata: Option<String>,
    /// Query parameter: out_of_band_amount
    pub out_of_band_amount: Option<String>,
    /// Query parameter: reason
    pub reason: Option<String>,
    /// Query parameter: refund_amount
    pub refund_amount: Option<String>,
    /// Query parameter: refunds
    pub refunds: Option<String>,
    /// Query parameter: shipping_cost
    pub shipping_cost: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetCreditNotesCreditNoteLines_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCreditNotesCreditNoteLinesArgs {
    /// Path parameter: credit_note
    pub credit_note: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetInvoicesInvoiceLines_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoicesInvoiceLinesArgs {
    /// Path parameter: invoice
    pub invoice: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`PostInvoicesInvoiceLinesLineItemId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostInvoicesInvoiceLinesLineItemIdArgs {
    /// Path parameter: invoice
    pub invoice: String,
    /// Path parameter: line_item_id
    pub line_item_id: String,
}

/// Arguments for [`GetBillingMeters_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBillingMetersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetBillingMetersId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBillingMetersIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostBillingMetersId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostBillingMetersIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetClimateOrders_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetClimateOrdersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetClimateOrdersOrder_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetClimateOrdersOrderArgs {
    /// Path parameter: order
    pub order: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostClimateOrdersOrder_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostClimateOrdersOrderArgs {
    /// Path parameter: order
    pub order: String,
}

/// Arguments for [`PostTestHelpersTreasuryOutboundPaymentsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryOutboundPaymentsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetTreasuryOutboundPayments_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryOutboundPaymentsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTreasuryOutboundPaymentsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryOutboundPaymentsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTestHelpersTreasuryOutboundTransfersOutboundTransfer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransferArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// Arguments for [`GetTreasuryOutboundTransfers_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryOutboundTransfersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTreasuryOutboundTransfersOutboundTransfer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTreasuryOutboundTransfersOutboundTransferArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`GetPaymentIntents_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentIntentsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPaymentIntentsIntent_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
    /// Query parameter: client_secret
    pub client_secret: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPaymentIntentsIntent_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPaymentIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// Arguments for [`GetPaymentLinks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentLinksArgs {
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPaymentLinksPaymentLink_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentLinksPaymentLinkArgs {
    /// Path parameter: payment_link
    pub payment_link: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPaymentLinksPaymentLink_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPaymentLinksPaymentLinkArgs {
    /// Path parameter: payment_link
    pub payment_link: String,
}

/// Arguments for [`GetPaymentMethodConfigurations_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentMethodConfigurationsArgs {
    /// Query parameter: application
    pub application: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPaymentMethodConfigurationsConfiguration_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentMethodConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPaymentMethodConfigurationsConfiguration_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPaymentMethodConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
}

/// Arguments for [`GetPaymentMethodDomains_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentMethodDomainsArgs {
    /// Query parameter: domain_name
    pub domain_name: Option<String>,
    /// Query parameter: enabled
    pub enabled: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPaymentMethodDomainsPaymentMethodDomain_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentMethodDomainsPaymentMethodDomainArgs {
    /// Path parameter: payment_method_domain
    pub payment_method_domain: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPaymentMethodDomainsPaymentMethodDomain_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPaymentMethodDomainsPaymentMethodDomainArgs {
    /// Path parameter: payment_method_domain
    pub payment_method_domain: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_issuing_cards_card_shipping_fail_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTestHelpersIssuingCardsCardShippingFailArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{card}/shipping/fail",
        args.card,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_issuing_cards_card_shipping_fail_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IssuingCard>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_issuing_cards_card_shipping_fail_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_issuing_cards_card_shipping_fail_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_issuing_cards_card_shipping_fail(
    client: &SimpleHttpClient,
    args: &PostTestHelpersIssuingCardsCardShippingFailArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_test_helpers_issuing_cards_card_shipping_fail_builder(client, args)?;
    post_test_helpers_issuing_cards_card_shipping_fail_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_inbound_transfers_id_fail_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTestHelpersTreasuryInboundTransfersIdFailArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/inbound_transfers/{id}/fail",
        args.id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_inbound_transfers_id_fail_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryInboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_inbound_transfers_id_fail_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_treasury_inbound_transfers_id_fail_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_inbound_transfers_id_fail(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryInboundTransfersIdFailArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_test_helpers_treasury_inbound_transfers_id_fail_builder(client, args)?;
    post_test_helpers_treasury_inbound_transfers_id_fail_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_outbound_payments_id_fail_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTestHelpersTreasuryOutboundPaymentsIdFailArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{id}/fail",
        args.id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_outbound_payments_id_fail_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundPayment = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_outbound_payments_id_fail_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_treasury_outbound_payments_id_fail_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_outbound_payments_id_fail(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryOutboundPaymentsIdFailArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_test_helpers_treasury_outbound_payments_id_fail_builder(client, args)?;
    post_test_helpers_treasury_outbound_payments_id_fail_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferFailArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail",
        args.outbound_transfer,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferFailArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder =
        post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder(client, args)?;
    post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/file_links
// -----------------------------------------------------------------------------

/// GET /v1/file_links - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_file_links_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetFileLinksArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/file_links",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/file_links - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_file_links_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/file_links - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_file_links_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_file_links_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/file_links - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_file_links(
    client: &SimpleHttpClient,
    args: &GetFileLinksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_file_links_builder(client, args)?;
    get_file_links_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/file_links
// -----------------------------------------------------------------------------

/// POST /v1/file_links - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_file_links_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/file_links",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/file_links - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_file_links_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FileLink>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: FileLink = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/file_links - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_file_links_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_file_links_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/file_links/{link}
// -----------------------------------------------------------------------------

/// GET /v1/file_links/{link} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_file_links_link_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetFileLinksLinkArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/file_links/{link}", args.link,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/file_links/{link} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_file_links_link_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FileLink>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: FileLink = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/file_links/{link} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_file_links_link_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_file_links_link_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/file_links/{link} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_file_links_link(
    client: &SimpleHttpClient,
    args: &GetFileLinksLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_file_links_link_builder(client, args)?;
    get_file_links_link_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/file_links/{link}
// -----------------------------------------------------------------------------

/// POST /v1/file_links/{link} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_file_links_link_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostFileLinksLinkArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/file_links/{link}", args.link,);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/file_links/{link} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_file_links_link_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FileLink>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: FileLink = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/file_links/{link} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_file_links_link_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_file_links_link_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/file_links/{link} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_file_links_link(
    client: &SimpleHttpClient,
    args: &PostFileLinksLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_file_links_link_builder(client, args)?;
    post_file_links_link_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/financial_accounts
// -----------------------------------------------------------------------------

/// GET /v1/treasury/financial_accounts - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_financial_accounts_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTreasuryFinancialAccountsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/treasury/financial_accounts",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/financial_accounts - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_financial_accounts_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/treasury/financial_accounts - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_financial_accounts_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_treasury_financial_accounts_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/financial_accounts - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_financial_accounts(
    client: &SimpleHttpClient,
    args: &GetTreasuryFinancialAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_treasury_financial_accounts_builder(client, args)?;
    get_treasury_financial_accounts_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/financial_accounts
// -----------------------------------------------------------------------------

/// POST /v1/treasury/financial_accounts - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_financial_accounts_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/treasury/financial_accounts",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/financial_accounts - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_financial_accounts_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryFinancialAccount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/financial_accounts - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_financial_accounts_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_financial_accounts_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/financial_accounts/{financial_account}
// -----------------------------------------------------------------------------

/// GET /v1/treasury/financial_accounts/{financial_account} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_financial_accounts_financial_account_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTreasuryFinancialAccountsFinancialAccountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{financial_account}",
        args.financial_account,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/financial_accounts/{financial_account} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_financial_accounts_financial_account_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryFinancialAccount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/treasury/financial_accounts/{financial_account} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_financial_accounts_financial_account_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_treasury_financial_accounts_financial_account_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/financial_accounts/{financial_account} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_financial_accounts_financial_account(
    client: &SimpleHttpClient,
    args: &GetTreasuryFinancialAccountsFinancialAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_treasury_financial_accounts_financial_account_builder(client, args)?;
    get_treasury_financial_accounts_financial_account_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/financial_accounts/{financial_account}
// -----------------------------------------------------------------------------

/// POST /v1/treasury/financial_accounts/{financial_account} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_financial_accounts_financial_account_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTreasuryFinancialAccountsFinancialAccountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{financial_account}",
        args.financial_account,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_financial_accounts_financial_account_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryFinancialAccount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/financial_accounts/{financial_account} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_financial_accounts_financial_account_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_financial_accounts_financial_account_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/financial_accounts/{financial_account} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_treasury_financial_accounts_financial_account(
    client: &SimpleHttpClient,
    args: &PostTreasuryFinancialAccountsFinancialAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_treasury_financial_accounts_financial_account_builder(client, args)?;
    post_treasury_financial_accounts_financial_account_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/credit_notes/preview/lines
// -----------------------------------------------------------------------------

/// GET /v1/credit_notes/preview/lines - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_credit_notes_preview_lines_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCreditNotesPreviewLinesArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/credit_notes/preview/lines",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/preview/lines - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_credit_notes_preview_lines_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/credit_notes/preview/lines - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_credit_notes_preview_lines_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_credit_notes_preview_lines_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/credit_notes/preview/lines - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_credit_notes_preview_lines(
    client: &SimpleHttpClient,
    args: &GetCreditNotesPreviewLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_credit_notes_preview_lines_builder(client, args)?;
    get_credit_notes_preview_lines_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/credit_notes/{credit_note}/lines
// -----------------------------------------------------------------------------

/// GET /v1/credit_notes/{credit_note}/lines - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_credit_notes_credit_note_lines_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCreditNotesCreditNoteLinesArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/credit_notes/{credit_note}/lines",
        args.credit_note,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/{credit_note}/lines - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_credit_notes_credit_note_lines_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/credit_notes/{credit_note}/lines - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_credit_notes_credit_note_lines_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_credit_notes_credit_note_lines_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/credit_notes/{credit_note}/lines - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_credit_notes_credit_note_lines(
    client: &SimpleHttpClient,
    args: &GetCreditNotesCreditNoteLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_credit_notes_credit_note_lines_builder(client, args)?;
    get_credit_notes_credit_note_lines_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/invoices/{invoice}/lines
// -----------------------------------------------------------------------------

/// GET /v1/invoices/{invoice}/lines - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_invoices_invoice_lines_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetInvoicesInvoiceLinesArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoices/{invoice}/lines",
        args.invoice,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices/{invoice}/lines - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_invoices_invoice_lines_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/invoices/{invoice}/lines - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_invoices_invoice_lines_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_invoices_invoice_lines_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/invoices/{invoice}/lines - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_invoices_invoice_lines(
    client: &SimpleHttpClient,
    args: &GetInvoicesInvoiceLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_invoices_invoice_lines_builder(client, args)?;
    get_invoices_invoice_lines_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/invoices/{invoice}/lines/{line_item_id}
// -----------------------------------------------------------------------------

/// POST /v1/invoices/{invoice}/lines/{line_item_id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_invoices_invoice_lines_line_item_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostInvoicesInvoiceLinesLineItemIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoices/{invoice}/lines/{line_item_id}",
        args.invoice, args.line_item_id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/lines/{line_item_id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_invoices_invoice_lines_line_item_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: LineItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/invoices/{invoice}/lines/{line_item_id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_invoices_invoice_lines_line_item_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_invoices_invoice_lines_line_item_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoices/{invoice}/lines/{line_item_id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_invoices_invoice_lines_line_item_id(
    client: &SimpleHttpClient,
    args: &PostInvoicesInvoiceLinesLineItemIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_invoices_invoice_lines_line_item_id_builder(client, args)?;
    post_invoices_invoice_lines_line_item_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/billing/meters
// -----------------------------------------------------------------------------

/// GET /v1/billing/meters - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_billing_meters_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetBillingMetersArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/billing/meters",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/meters - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_billing_meters_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/billing/meters - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_billing_meters_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_billing_meters_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/billing/meters - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_billing_meters(
    client: &SimpleHttpClient,
    args: &GetBillingMetersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_billing_meters_builder(client, args)?;
    get_billing_meters_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/billing/meters
// -----------------------------------------------------------------------------

/// POST /v1/billing/meters - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_billing_meters_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/billing/meters",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meters - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_billing_meters_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/billing/meters - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_billing_meters_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_billing_meters_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/billing/meters/{id}
// -----------------------------------------------------------------------------

/// GET /v1/billing/meters/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_billing_meters_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetBillingMetersIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/billing/meters/{id}", args.id,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/meters/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_billing_meters_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/billing/meters/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_billing_meters_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_billing_meters_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/billing/meters/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_billing_meters_id(
    client: &SimpleHttpClient,
    args: &GetBillingMetersIdArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_billing_meters_id_builder(client, args)?;
    get_billing_meters_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/billing/meters/{id}
// -----------------------------------------------------------------------------

/// POST /v1/billing/meters/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_billing_meters_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostBillingMetersIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/billing/meters/{id}", args.id,);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meters/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_billing_meters_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/billing/meters/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_billing_meters_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_billing_meters_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/meters/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_billing_meters_id(
    client: &SimpleHttpClient,
    args: &PostBillingMetersIdArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_billing_meters_id_builder(client, args)?;
    post_billing_meters_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/climate/orders
// -----------------------------------------------------------------------------

/// GET /v1/climate/orders - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_climate_orders_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetClimateOrdersArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/climate/orders",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/orders - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_climate_orders_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/climate/orders - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_climate_orders_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_climate_orders_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/climate/orders - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_climate_orders(
    client: &SimpleHttpClient,
    args: &GetClimateOrdersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_climate_orders_builder(client, args)?;
    get_climate_orders_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/climate/orders
// -----------------------------------------------------------------------------

/// POST /v1/climate/orders - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_climate_orders_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/climate/orders",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/climate/orders - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_climate_orders_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/climate/orders - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_climate_orders_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_climate_orders_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/climate/orders/{order}
// -----------------------------------------------------------------------------

/// GET /v1/climate/orders/{order} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_climate_orders_order_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetClimateOrdersOrderArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/climate/orders/{order}",
        args.order,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/orders/{order} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_climate_orders_order_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/climate/orders/{order} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_climate_orders_order_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_climate_orders_order_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/climate/orders/{order} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_climate_orders_order(
    client: &SimpleHttpClient,
    args: &GetClimateOrdersOrderArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_climate_orders_order_builder(client, args)?;
    get_climate_orders_order_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/climate/orders/{order}
// -----------------------------------------------------------------------------

/// POST /v1/climate/orders/{order} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_climate_orders_order_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostClimateOrdersOrderArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/climate/orders/{order}",
        args.order,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/climate/orders/{order} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_climate_orders_order_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/climate/orders/{order} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_climate_orders_order_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_climate_orders_order_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/climate/orders/{order} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_climate_orders_order(
    client: &SimpleHttpClient,
    args: &PostClimateOrdersOrderArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_climate_orders_order_builder(client, args)?;
    post_climate_orders_order_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/outbound_payments/{id}
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/outbound_payments/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_outbound_payments_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTestHelpersTreasuryOutboundPaymentsIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{id}",
        args.id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_outbound_payments_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundPayment = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_outbound_payments_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_treasury_outbound_payments_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_outbound_payments_id(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryOutboundPaymentsIdArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_test_helpers_treasury_outbound_payments_id_builder(client, args)?;
    post_test_helpers_treasury_outbound_payments_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/outbound_payments
// -----------------------------------------------------------------------------

/// GET /v1/treasury/outbound_payments - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_outbound_payments_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTreasuryOutboundPaymentsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/treasury/outbound_payments",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_payments - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_outbound_payments_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/treasury/outbound_payments - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_outbound_payments_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_treasury_outbound_payments_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/outbound_payments - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_outbound_payments(
    client: &SimpleHttpClient,
    args: &GetTreasuryOutboundPaymentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_treasury_outbound_payments_builder(client, args)?;
    get_treasury_outbound_payments_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/outbound_payments
// -----------------------------------------------------------------------------

/// POST /v1/treasury/outbound_payments - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_outbound_payments_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/treasury/outbound_payments",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_payments - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_outbound_payments_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundPayment = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/outbound_payments - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_outbound_payments_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_outbound_payments_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/outbound_payments/{id}
// -----------------------------------------------------------------------------

/// GET /v1/treasury/outbound_payments/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_outbound_payments_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTreasuryOutboundPaymentsIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/outbound_payments/{id}",
        args.id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_payments/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_outbound_payments_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundPayment = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/treasury/outbound_payments/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_outbound_payments_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_treasury_outbound_payments_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/outbound_payments/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_outbound_payments_id(
    client: &SimpleHttpClient,
    args: &GetTreasuryOutboundPaymentsIdArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_treasury_outbound_payments_id_builder(client, args)?;
    get_treasury_outbound_payments_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}",
        args.outbound_transfer,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_treasury_outbound_transfers_outbound_transfer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder =
        post_test_helpers_treasury_outbound_transfers_outbound_transfer_builder(client, args)?;
    post_test_helpers_treasury_outbound_transfers_outbound_transfer_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/outbound_transfers
// -----------------------------------------------------------------------------

/// GET /v1/treasury/outbound_transfers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_outbound_transfers_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTreasuryOutboundTransfersArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/treasury/outbound_transfers",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_transfers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_outbound_transfers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/treasury/outbound_transfers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_outbound_transfers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_treasury_outbound_transfers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/outbound_transfers - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_outbound_transfers(
    client: &SimpleHttpClient,
    args: &GetTreasuryOutboundTransfersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_treasury_outbound_transfers_builder(client, args)?;
    get_treasury_outbound_transfers_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/outbound_transfers
// -----------------------------------------------------------------------------

/// POST /v1/treasury/outbound_transfers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_outbound_transfers_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/treasury/outbound_transfers",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_transfers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_outbound_transfers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/outbound_transfers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_outbound_transfers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_outbound_transfers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/treasury/outbound_transfers/{outbound_transfer}
// -----------------------------------------------------------------------------

/// GET /v1/treasury/outbound_transfers/{outbound_transfer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_treasury_outbound_transfers_outbound_transfer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTreasuryOutboundTransfersOutboundTransferArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/outbound_transfers/{outbound_transfer}",
        args.outbound_transfer,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_transfers/{outbound_transfer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_treasury_outbound_transfers_outbound_transfer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/treasury/outbound_transfers/{outbound_transfer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_treasury_outbound_transfers_outbound_transfer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_treasury_outbound_transfers_outbound_transfer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/treasury/outbound_transfers/{outbound_transfer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_treasury_outbound_transfers_outbound_transfer(
    client: &SimpleHttpClient,
    args: &GetTreasuryOutboundTransfersOutboundTransferArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_treasury_outbound_transfers_outbound_transfer_builder(client, args)?;
    get_treasury_outbound_transfers_outbound_transfer_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/payment_intents
// -----------------------------------------------------------------------------

/// GET /v1/payment_intents - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_intents_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentIntentsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_intents",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_intents - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_intents_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_intents - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_intents_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_intents_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_intents - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_intents(
    client: &SimpleHttpClient,
    args: &GetPaymentIntentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_intents_builder(client, args)?;
    get_payment_intents_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_intents
// -----------------------------------------------------------------------------

/// POST /v1/payment_intents - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_intents_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_intents",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_intents_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentIntent = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_intents - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_intents_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_intents_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/payment_intents/{intent}
// -----------------------------------------------------------------------------

/// GET /v1/payment_intents/{intent} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_intents_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentIntentsIntentArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_intents/{intent}",
        args.intent,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_intents/{intent} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_intents_intent_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentIntent = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_intents/{intent} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_intents_intent_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_intents_intent_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_intents/{intent} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_intents_intent(
    client: &SimpleHttpClient,
    args: &GetPaymentIntentsIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_intents_intent_builder(client, args)?;
    get_payment_intents_intent_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_intents/{intent}
// -----------------------------------------------------------------------------

/// POST /v1/payment_intents/{intent} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_intents_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPaymentIntentsIntentArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_intents/{intent}",
        args.intent,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_intents_intent_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentIntent = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_intents/{intent} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_intents_intent_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_intents_intent_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_intents/{intent} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payment_intents_intent(
    client: &SimpleHttpClient,
    args: &PostPaymentIntentsIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_payment_intents_intent_builder(client, args)?;
    post_payment_intents_intent_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/payment_links
// -----------------------------------------------------------------------------

/// GET /v1/payment_links - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_links_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentLinksArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_links",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_links - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_links_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_links - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_links_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_links_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_links - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_links(
    client: &SimpleHttpClient,
    args: &GetPaymentLinksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_links_builder(client, args)?;
    get_payment_links_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_links
// -----------------------------------------------------------------------------

/// POST /v1/payment_links - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_links_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_links",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_links - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_links_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentLink>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentLink = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_links - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_links_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PaymentLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_links_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/payment_links/{payment_link}
// -----------------------------------------------------------------------------

/// GET /v1/payment_links/{payment_link} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_links_payment_link_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentLinksPaymentLinkArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_links/{payment_link}",
        args.payment_link,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_links/{payment_link} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_links_payment_link_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentLink>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentLink = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_links/{payment_link} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_links_payment_link_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PaymentLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_links_payment_link_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_links/{payment_link} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_links_payment_link(
    client: &SimpleHttpClient,
    args: &GetPaymentLinksPaymentLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PaymentLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_links_payment_link_builder(client, args)?;
    get_payment_links_payment_link_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_links/{payment_link}
// -----------------------------------------------------------------------------

/// POST /v1/payment_links/{payment_link} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_links_payment_link_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPaymentLinksPaymentLinkArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_links/{payment_link}",
        args.payment_link,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_links/{payment_link} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_links_payment_link_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentLink>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentLink = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_links/{payment_link} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_links_payment_link_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PaymentLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_links_payment_link_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_links/{payment_link} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payment_links_payment_link(
    client: &SimpleHttpClient,
    args: &PostPaymentLinksPaymentLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PaymentLink>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_payment_links_payment_link_builder(client, args)?;
    post_payment_links_payment_link_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/payment_method_configurations
// -----------------------------------------------------------------------------

/// GET /v1/payment_method_configurations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_method_configurations_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentMethodConfigurationsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_method_configurations",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_configurations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_method_configurations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_method_configurations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_method_configurations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_method_configurations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_method_configurations - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_method_configurations(
    client: &SimpleHttpClient,
    args: &GetPaymentMethodConfigurationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_method_configurations_builder(client, args)?;
    get_payment_method_configurations_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_method_configurations
// -----------------------------------------------------------------------------

/// POST /v1/payment_method_configurations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_method_configurations_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_method_configurations",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_configurations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_method_configurations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentMethodConfiguration = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_method_configurations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_method_configurations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_method_configurations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/payment_method_configurations/{configuration}
// -----------------------------------------------------------------------------

/// GET /v1/payment_method_configurations/{configuration} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_method_configurations_configuration_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentMethodConfigurationsConfigurationArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_method_configurations/{configuration}",
        args.configuration,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_configurations/{configuration} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_method_configurations_configuration_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentMethodConfiguration = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_method_configurations/{configuration} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_method_configurations_configuration_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_method_configurations_configuration_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_method_configurations/{configuration} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_method_configurations_configuration(
    client: &SimpleHttpClient,
    args: &GetPaymentMethodConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_method_configurations_configuration_builder(client, args)?;
    get_payment_method_configurations_configuration_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_method_configurations/{configuration}
// -----------------------------------------------------------------------------

/// POST /v1/payment_method_configurations/{configuration} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_method_configurations_configuration_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPaymentMethodConfigurationsConfigurationArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_method_configurations/{configuration}",
        args.configuration,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_configurations/{configuration} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_method_configurations_configuration_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentMethodConfiguration = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_method_configurations/{configuration} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_method_configurations_configuration_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_method_configurations_configuration_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_method_configurations/{configuration} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payment_method_configurations_configuration(
    client: &SimpleHttpClient,
    args: &PostPaymentMethodConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_payment_method_configurations_configuration_builder(client, args)?;
    post_payment_method_configurations_configuration_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/payment_method_domains
// -----------------------------------------------------------------------------

/// GET /v1/payment_method_domains - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_method_domains_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentMethodDomainsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_method_domains",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_domains - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_method_domains_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_method_domains - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_method_domains_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_method_domains_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_method_domains - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_method_domains(
    client: &SimpleHttpClient,
    args: &GetPaymentMethodDomainsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_method_domains_builder(client, args)?;
    get_payment_method_domains_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_method_domains
// -----------------------------------------------------------------------------

/// POST /v1/payment_method_domains - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_method_domains_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/payment_method_domains",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_domains - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_method_domains_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentMethodDomain = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_method_domains - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_method_domains_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_method_domains_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/payment_method_domains/{payment_method_domain}
// -----------------------------------------------------------------------------

/// GET /v1/payment_method_domains/{payment_method_domain} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_method_domains_payment_method_domain_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentMethodDomainsPaymentMethodDomainArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_method_domains/{payment_method_domain}",
        args.payment_method_domain,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_domains/{payment_method_domain} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_method_domains_payment_method_domain_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentMethodDomain = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_method_domains/{payment_method_domain} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_method_domains_payment_method_domain_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_method_domains_payment_method_domain_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_method_domains/{payment_method_domain} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_method_domains_payment_method_domain(
    client: &SimpleHttpClient,
    args: &GetPaymentMethodDomainsPaymentMethodDomainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_method_domains_payment_method_domain_builder(client, args)?;
    get_payment_method_domains_payment_method_domain_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_method_domains/{payment_method_domain}
// -----------------------------------------------------------------------------

/// POST /v1/payment_method_domains/{payment_method_domain} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_method_domains_payment_method_domain_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPaymentMethodDomainsPaymentMethodDomainArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_method_domains/{payment_method_domain}",
        args.payment_method_domain,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_domains/{payment_method_domain} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_method_domains_payment_method_domain_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentMethodDomain = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_method_domains/{payment_method_domain} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_method_domains_payment_method_domain_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_method_domains_payment_method_domain_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_method_domains/{payment_method_domain} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payment_method_domains_payment_method_domain(
    client: &SimpleHttpClient,
    args: &PostPaymentMethodDomainsPaymentMethodDomainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_payment_method_domains_payment_method_domain_builder(client, args)?;
    post_payment_method_domains_payment_method_domain_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_test_helpers_issuing_cards_card_shipping_fail(
        &self,
        args: &PostTestHelpersIssuingCardsCardShippingFailArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IssuingCard>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_test_helpers_issuing_cards_card_shipping_fail_builder(&self.http_client, args)?;
        post_test_helpers_issuing_cards_card_shipping_fail_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_test_helpers_treasury_inbound_transfers_id_fail(
        &self,
        args: &PostTestHelpersTreasuryInboundTransfersIdFailArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_test_helpers_treasury_inbound_transfers_id_fail_builder(&self.http_client, args)?;
        post_test_helpers_treasury_inbound_transfers_id_fail_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_test_helpers_treasury_outbound_payments_id_fail(
        &self,
        args: &PostTestHelpersTreasuryOutboundPaymentsIdFailArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_test_helpers_treasury_outbound_payments_id_fail_builder(&self.http_client, args)?;
        post_test_helpers_treasury_outbound_payments_id_fail_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail(
        &self,
        args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferFailArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder(
            &self.http_client,
            args,
        )?;
        post_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute(builder)
    }

    /// GET /v1/file_links.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_file_links(
        &self,
        args: &GetFileLinksArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_file_links_builder(&self.http_client, args)?;
        get_file_links_execute(builder)
    }

    /// POST /v1/file_links.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_file_links(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<FileLink>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_file_links_builder(&self.http_client)?;
        post_file_links_execute(builder)
    }

    /// GET /v1/file_links/{link}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_file_links_link(
        &self,
        args: &GetFileLinksLinkArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<FileLink>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_file_links_link_builder(&self.http_client, args)?;
        get_file_links_link_execute(builder)
    }

    /// POST /v1/file_links/{link}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_file_links_link(
        &self,
        args: &PostFileLinksLinkArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<FileLink>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_file_links_link_builder(&self.http_client, args)?;
        post_file_links_link_execute(builder)
    }

    /// GET /v1/treasury/financial_accounts.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_treasury_financial_accounts(
        &self,
        args: &GetTreasuryFinancialAccountsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_treasury_financial_accounts_builder(&self.http_client, args)?;
        get_treasury_financial_accounts_execute(builder)
    }

    /// POST /v1/treasury/financial_accounts.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_treasury_financial_accounts(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_treasury_financial_accounts_builder(&self.http_client)?;
        post_treasury_financial_accounts_execute(builder)
    }

    /// GET /v1/treasury/financial_accounts/{financial_account}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_treasury_financial_accounts_financial_account(
        &self,
        args: &GetTreasuryFinancialAccountsFinancialAccountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            get_treasury_financial_accounts_financial_account_builder(&self.http_client, args)?;
        get_treasury_financial_accounts_financial_account_execute(builder)
    }

    /// POST /v1/treasury/financial_accounts/{financial_account}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_treasury_financial_accounts_financial_account(
        &self,
        args: &PostTreasuryFinancialAccountsFinancialAccountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryFinancialAccount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_treasury_financial_accounts_financial_account_builder(&self.http_client, args)?;
        post_treasury_financial_accounts_financial_account_execute(builder)
    }

    /// GET /v1/credit_notes/preview/lines.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_credit_notes_preview_lines(
        &self,
        args: &GetCreditNotesPreviewLinesArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_credit_notes_preview_lines_builder(&self.http_client, args)?;
        get_credit_notes_preview_lines_execute(builder)
    }

    /// GET /v1/credit_notes/{credit_note}/lines.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_credit_notes_credit_note_lines(
        &self,
        args: &GetCreditNotesCreditNoteLinesArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_credit_notes_credit_note_lines_builder(&self.http_client, args)?;
        get_credit_notes_credit_note_lines_execute(builder)
    }

    /// GET /v1/invoices/{invoice}/lines.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_invoices_invoice_lines(
        &self,
        args: &GetInvoicesInvoiceLinesArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_invoices_invoice_lines_builder(&self.http_client, args)?;
        get_invoices_invoice_lines_execute(builder)
    }

    /// POST /v1/invoices/{invoice}/lines/{line_item_id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_invoices_invoice_lines_line_item_id(
        &self,
        args: &PostInvoicesInvoiceLinesLineItemIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<LineItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_invoices_invoice_lines_line_item_id_builder(&self.http_client, args)?;
        post_invoices_invoice_lines_line_item_id_execute(builder)
    }

    /// GET /v1/billing/meters.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_billing_meters(
        &self,
        args: &GetBillingMetersArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_billing_meters_builder(&self.http_client, args)?;
        get_billing_meters_execute(builder)
    }

    /// POST /v1/billing/meters.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_billing_meters(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_billing_meters_builder(&self.http_client)?;
        post_billing_meters_execute(builder)
    }

    /// GET /v1/billing/meters/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_billing_meters_id(
        &self,
        args: &GetBillingMetersIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_billing_meters_id_builder(&self.http_client, args)?;
        get_billing_meters_id_execute(builder)
    }

    /// POST /v1/billing/meters/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_billing_meters_id(
        &self,
        args: &PostBillingMetersIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<BillingMeter>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_billing_meters_id_builder(&self.http_client, args)?;
        post_billing_meters_id_execute(builder)
    }

    /// GET /v1/climate/orders.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_climate_orders(
        &self,
        args: &GetClimateOrdersArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_climate_orders_builder(&self.http_client, args)?;
        get_climate_orders_execute(builder)
    }

    /// POST /v1/climate/orders.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_climate_orders(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_climate_orders_builder(&self.http_client)?;
        post_climate_orders_execute(builder)
    }

    /// GET /v1/climate/orders/{order}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_climate_orders_order(
        &self,
        args: &GetClimateOrdersOrderArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_climate_orders_order_builder(&self.http_client, args)?;
        get_climate_orders_order_execute(builder)
    }

    /// POST /v1/climate/orders/{order}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_climate_orders_order(
        &self,
        args: &PostClimateOrdersOrderArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_climate_orders_order_builder(&self.http_client, args)?;
        post_climate_orders_order_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/outbound_payments/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_test_helpers_treasury_outbound_payments_id(
        &self,
        args: &PostTestHelpersTreasuryOutboundPaymentsIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_test_helpers_treasury_outbound_payments_id_builder(&self.http_client, args)?;
        post_test_helpers_treasury_outbound_payments_id_execute(builder)
    }

    /// GET /v1/treasury/outbound_payments.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_treasury_outbound_payments(
        &self,
        args: &GetTreasuryOutboundPaymentsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_treasury_outbound_payments_builder(&self.http_client, args)?;
        get_treasury_outbound_payments_execute(builder)
    }

    /// POST /v1/treasury/outbound_payments.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_treasury_outbound_payments(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_treasury_outbound_payments_builder(&self.http_client)?;
        post_treasury_outbound_payments_execute(builder)
    }

    /// GET /v1/treasury/outbound_payments/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_treasury_outbound_payments_id(
        &self,
        args: &GetTreasuryOutboundPaymentsIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_treasury_outbound_payments_id_builder(&self.http_client, args)?;
        get_treasury_outbound_payments_id_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_test_helpers_treasury_outbound_transfers_outbound_transfer(
        &self,
        args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_test_helpers_treasury_outbound_transfers_outbound_transfer_builder(
            &self.http_client,
            args,
        )?;
        post_test_helpers_treasury_outbound_transfers_outbound_transfer_execute(builder)
    }

    /// GET /v1/treasury/outbound_transfers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_treasury_outbound_transfers(
        &self,
        args: &GetTreasuryOutboundTransfersArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_treasury_outbound_transfers_builder(&self.http_client, args)?;
        get_treasury_outbound_transfers_execute(builder)
    }

    /// POST /v1/treasury/outbound_transfers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_treasury_outbound_transfers(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_treasury_outbound_transfers_builder(&self.http_client)?;
        post_treasury_outbound_transfers_execute(builder)
    }

    /// GET /v1/treasury/outbound_transfers/{outbound_transfer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_treasury_outbound_transfers_outbound_transfer(
        &self,
        args: &GetTreasuryOutboundTransfersOutboundTransferArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            get_treasury_outbound_transfers_outbound_transfer_builder(&self.http_client, args)?;
        get_treasury_outbound_transfers_outbound_transfer_execute(builder)
    }

    /// GET /v1/payment_intents.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_intents(
        &self,
        args: &GetPaymentIntentsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_intents_builder(&self.http_client, args)?;
        get_payment_intents_execute(builder)
    }

    /// POST /v1/payment_intents.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_intents(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_intents_builder(&self.http_client)?;
        post_payment_intents_execute(builder)
    }

    /// GET /v1/payment_intents/{intent}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_intents_intent(
        &self,
        args: &GetPaymentIntentsIntentArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_intents_intent_builder(&self.http_client, args)?;
        get_payment_intents_intent_execute(builder)
    }

    /// POST /v1/payment_intents/{intent}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_intents_intent(
        &self,
        args: &PostPaymentIntentsIntentArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_intents_intent_builder(&self.http_client, args)?;
        post_payment_intents_intent_execute(builder)
    }

    /// GET /v1/payment_links.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_links(
        &self,
        args: &GetPaymentLinksArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_links_builder(&self.http_client, args)?;
        get_payment_links_execute(builder)
    }

    /// POST /v1/payment_links.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_links(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentLink>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_links_builder(&self.http_client)?;
        post_payment_links_execute(builder)
    }

    /// GET /v1/payment_links/{payment_link}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_links_payment_link(
        &self,
        args: &GetPaymentLinksPaymentLinkArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentLink>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_links_payment_link_builder(&self.http_client, args)?;
        get_payment_links_payment_link_execute(builder)
    }

    /// POST /v1/payment_links/{payment_link}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_links_payment_link(
        &self,
        args: &PostPaymentLinksPaymentLinkArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentLink>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_links_payment_link_builder(&self.http_client, args)?;
        post_payment_links_payment_link_execute(builder)
    }

    /// GET /v1/payment_method_configurations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_method_configurations(
        &self,
        args: &GetPaymentMethodConfigurationsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_method_configurations_builder(&self.http_client, args)?;
        get_payment_method_configurations_execute(builder)
    }

    /// POST /v1/payment_method_configurations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_method_configurations(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_method_configurations_builder(&self.http_client)?;
        post_payment_method_configurations_execute(builder)
    }

    /// GET /v1/payment_method_configurations/{configuration}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_method_configurations_configuration(
        &self,
        args: &GetPaymentMethodConfigurationsConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            get_payment_method_configurations_configuration_builder(&self.http_client, args)?;
        get_payment_method_configurations_configuration_execute(builder)
    }

    /// POST /v1/payment_method_configurations/{configuration}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_method_configurations_configuration(
        &self,
        args: &PostPaymentMethodConfigurationsConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentMethodConfiguration>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_payment_method_configurations_configuration_builder(&self.http_client, args)?;
        post_payment_method_configurations_configuration_execute(builder)
    }

    /// GET /v1/payment_method_domains.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_method_domains(
        &self,
        args: &GetPaymentMethodDomainsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_method_domains_builder(&self.http_client, args)?;
        get_payment_method_domains_execute(builder)
    }

    /// POST /v1/payment_method_domains.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_method_domains(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_method_domains_builder(&self.http_client)?;
        post_payment_method_domains_execute(builder)
    }

    /// GET /v1/payment_method_domains/{payment_method_domain}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_get_payment_method_domains_payment_method_domain(
        &self,
        args: &GetPaymentMethodDomainsPaymentMethodDomainArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            get_payment_method_domains_payment_method_domain_builder(&self.http_client, args)?;
        get_payment_method_domains_payment_method_domain_execute(builder)
    }

    /// POST /v1/payment_method_domains/{payment_method_domain}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_fail_file_links_financial_accounts_lines_meters_orders_outbound_payments_outbound_transfers_payment_intents_payment_links_payment_method_configurations_payment_method_domains_post_payment_method_domains_payment_method_domain(
        &self,
        args: &PostPaymentMethodDomainsPaymentMethodDomainArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentMethodDomain>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_payment_method_domains_payment_method_domain_builder(&self.http_client, args)?;
        post_payment_method_domains_payment_method_domain_execute(builder)
    }
}
