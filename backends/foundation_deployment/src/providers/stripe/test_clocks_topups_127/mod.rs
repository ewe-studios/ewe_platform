//! Auto-generated API module for stripe test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_test_clocks_topups_127 `

#![cfg(feature = "stripe_test_clocks_topups_127")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// Coupon response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Coupon {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Customer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Customer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedCoupon response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedCoupon {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedCustomer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedCustomer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedDiscount response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedDiscount {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedInvoice response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedInvoice {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedInvoiceitem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedInvoiceitem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedRadarValueListItem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedRadarValueListItem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// DeletedTestHelpersTestClock response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeletedTestHelpersTestClock {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Discount response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Discount {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IdentityVerificationSession response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IdentityVerificationSession {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Invoice response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Invoice {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Invoiceitem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Invoiceitem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// RadarValueListItem response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RadarValueListItem {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TestHelpersTestClock response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TestHelpersTestClock {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Topup response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Topup {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Transfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Transfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`GetTestHelpersTestClocks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTestHelpersTestClocksArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetTestHelpersTestClocksTestClock_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTestHelpersTestClocksTestClockArgs {
    /// Path parameter: test_clock
    pub test_clock: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`DeleteTestHelpersTestClocksTestClock_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteTestHelpersTestClocksTestClockArgs {
    /// Path parameter: test_clock
    pub test_clock: String,
}

/// Arguments for [`GetTopups_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTopupsArgs {
    /// Query parameter: amount
    pub amount: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTopupsTopup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTopupsTopupArgs {
    /// Path parameter: topup
    pub topup: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTopupsTopup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTopupsTopupArgs {
    /// Path parameter: topup
    pub topup: String,
}

/// Arguments for [`GetTransfers_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTransfersArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: destination
    pub destination: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: transfer_group
    pub transfer_group: Option<String>,
}

/// Arguments for [`GetTransfersTransfer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTransfersTransferArgs {
    /// Path parameter: transfer
    pub transfer: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTransfersTransfer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTransfersTransferArgs {
    /// Path parameter: transfer
    pub transfer: String,
}

/// Arguments for [`GetRadarValueListItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRadarValueListItemsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: value
    pub value: Option<String>,
    /// Query parameter: value_list
    pub value_list: Option<String>,
}

/// Arguments for [`GetRadarValueListItemsItem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRadarValueListItemsItemArgs {
    /// Path parameter: item
    pub item: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`DeleteRadarValueListItemsItem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteRadarValueListItemsItemArgs {
    /// Path parameter: item
    pub item: String,
}

/// Arguments for [`GetIdentityVerificationSessions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIdentityVerificationSessionsArgs {
    /// Query parameter: client_reference_id
    pub client_reference_id: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: related_customer
    pub related_customer: Option<String>,
    /// Query parameter: related_customer_account
    pub related_customer_account: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetIdentityVerificationSessionsSession_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIdentityVerificationSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostIdentityVerificationSessionsSession_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIdentityVerificationSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
}

/// Arguments for [`GetCoupons_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCouponsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetCouponsCoupon_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCouponsCouponArgs {
    /// Path parameter: coupon
    pub coupon: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostCouponsCoupon_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCouponsCouponArgs {
    /// Path parameter: coupon
    pub coupon: String,
}

/// Arguments for [`DeleteCouponsCoupon_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteCouponsCouponArgs {
    /// Path parameter: coupon
    pub coupon: String,
}

/// Arguments for [`GetCustomers_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCustomersArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: email
    pub email: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: test_clock
    pub test_clock: Option<String>,
}

/// Arguments for [`GetCustomersCustomer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCustomersCustomerArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostCustomersCustomer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostCustomersCustomerArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// Arguments for [`DeleteCustomersCustomer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteCustomersCustomerArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// Arguments for [`GetCustomersCustomerDiscount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCustomersCustomerDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`DeleteCustomersCustomerDiscount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteCustomersCustomerDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// Arguments for [`GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// Arguments for [`DeleteSubscriptionsSubscriptionExposedIdDiscount_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteSubscriptionsSubscriptionExposedIdDiscountArgs {
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// Arguments for [`GetInvoiceitems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoiceitemsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: pending
    pub pending: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetInvoiceitemsInvoiceitem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoiceitemsInvoiceitemArgs {
    /// Path parameter: invoiceitem
    pub invoiceitem: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostInvoiceitemsInvoiceitem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostInvoiceitemsInvoiceitemArgs {
    /// Path parameter: invoiceitem
    pub invoiceitem: String,
}

/// Arguments for [`DeleteInvoiceitemsInvoiceitem_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteInvoiceitemsInvoiceitemArgs {
    /// Path parameter: invoiceitem
    pub invoiceitem: String,
}

/// Arguments for [`GetInvoices_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoicesArgs {
    /// Query parameter: collection_method
    pub collection_method: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: due_date
    pub due_date: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: subscription
    pub subscription: Option<String>,
}

/// Arguments for [`GetInvoicesInvoice_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoicesInvoiceArgs {
    /// Path parameter: invoice
    pub invoice: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostInvoicesInvoice_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostInvoicesInvoiceArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// Arguments for [`DeleteInvoicesInvoice_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteInvoicesInvoiceArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// Arguments for [`GetCheckoutSessionsSessionLineItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetCheckoutSessionsSessionLineItemsArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPaymentLinksPaymentLinkLineItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPaymentLinksPaymentLinkLineItemsArgs {
    /// Path parameter: payment_link
    pub payment_link: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetQuotesQuoteLineItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetQuotesQuoteLineItemsArgs {
    /// Path parameter: quote
    pub quote: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetTaxCalculationsCalculationLineItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTaxCalculationsCalculationLineItemsArgs {
    /// Path parameter: calculation
    pub calculation: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetTaxTransactionsTransactionLineItems_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTaxTransactionsTransactionLineItemsArgs {
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/test_helpers/test_clocks
// -----------------------------------------------------------------------------

/// GET /v1/test_helpers/test_clocks - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_test_helpers_test_clocks_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTestHelpersTestClocksArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/test_helpers/test_clocks",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/test_helpers/test_clocks - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_test_helpers_test_clocks_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/test_helpers/test_clocks - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_test_helpers_test_clocks_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_test_helpers_test_clocks_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/test_helpers/test_clocks - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_test_helpers_test_clocks(
    client: &SimpleHttpClient,
    args: &GetTestHelpersTestClocksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_test_helpers_test_clocks_builder(client, args)?;
    get_test_helpers_test_clocks_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/test_clocks
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/test_clocks - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_test_clocks_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/test_helpers/test_clocks",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/test_clocks - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_test_clocks_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TestHelpersTestClock = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/test_helpers/test_clocks - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_test_clocks_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_test_helpers_test_clocks_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/test_helpers/test_clocks/{test_clock}
// -----------------------------------------------------------------------------

/// GET /v1/test_helpers/test_clocks/{test_clock} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_test_helpers_test_clocks_test_clock_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTestHelpersTestClocksTestClockArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/test_clocks/{test_clock}",
        args.test_clock,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/test_helpers/test_clocks/{test_clock} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_test_helpers_test_clocks_test_clock_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TestHelpersTestClock = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/test_helpers/test_clocks/{test_clock} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_test_helpers_test_clocks_test_clock_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_test_helpers_test_clocks_test_clock_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/test_helpers/test_clocks/{test_clock} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_test_helpers_test_clocks_test_clock(
    client: &SimpleHttpClient,
    args: &GetTestHelpersTestClocksTestClockArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_test_helpers_test_clocks_test_clock_builder(client, args)?;
    get_test_helpers_test_clocks_test_clock_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/test_helpers/test_clocks/{test_clock}
// -----------------------------------------------------------------------------

/// DELETE /v1/test_helpers/test_clocks/{test_clock} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_test_helpers_test_clocks_test_clock_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteTestHelpersTestClocksTestClockArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/test_clocks/{test_clock}",
        args.test_clock,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/test_helpers/test_clocks/{test_clock} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_test_helpers_test_clocks_test_clock_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedTestHelpersTestClock>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedTestHelpersTestClock = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/test_helpers/test_clocks/{test_clock} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_test_helpers_test_clocks_test_clock_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedTestHelpersTestClock>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_test_helpers_test_clocks_test_clock_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/test_helpers/test_clocks/{test_clock} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_test_helpers_test_clocks_test_clock(
    client: &SimpleHttpClient,
    args: &DeleteTestHelpersTestClocksTestClockArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedTestHelpersTestClock>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_test_helpers_test_clocks_test_clock_builder(client, args)?;
    delete_test_helpers_test_clocks_test_clock_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/topups
// -----------------------------------------------------------------------------

/// GET /v1/topups - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_topups_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTopupsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/topups",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/topups - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_topups_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/topups - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_topups_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_topups_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/topups - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_topups(
    client: &SimpleHttpClient,
    args: &GetTopupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_topups_builder(client, args)?;
    get_topups_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/topups
// -----------------------------------------------------------------------------

/// POST /v1/topups - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_topups_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/topups",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/topups - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_topups_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topup>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/topups - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_topups_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_topups_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/topups/{topup}
// -----------------------------------------------------------------------------

/// GET /v1/topups/{topup} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_topups_topup_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTopupsTopupArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/topups/{topup}", args.topup,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/topups/{topup} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_topups_topup_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topup>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/topups/{topup} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_topups_topup_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_topups_topup_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/topups/{topup} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_topups_topup(
    client: &SimpleHttpClient,
    args: &GetTopupsTopupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_topups_topup_builder(client, args)?;
    get_topups_topup_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/topups/{topup}
// -----------------------------------------------------------------------------

/// POST /v1/topups/{topup} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_topups_topup_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTopupsTopupArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/topups/{topup}", args.topup,);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/topups/{topup} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_topups_topup_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topup>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/topups/{topup} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_topups_topup_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_topups_topup_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/topups/{topup} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_topups_topup(
    client: &SimpleHttpClient,
    args: &PostTopupsTopupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_topups_topup_builder(client, args)?;
    post_topups_topup_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/transfers
// -----------------------------------------------------------------------------

/// GET /v1/transfers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_transfers_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTransfersArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/transfers",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_transfers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/transfers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_transfers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_transfers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/transfers - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_transfers(
    client: &SimpleHttpClient,
    args: &GetTransfersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_transfers_builder(client, args)?;
    get_transfers_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/transfers
// -----------------------------------------------------------------------------

/// POST /v1/transfers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_transfers_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/transfers",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_transfers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Transfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Transfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/transfers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_transfers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_transfers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/transfers/{transfer}
// -----------------------------------------------------------------------------

/// GET /v1/transfers/{transfer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_transfers_transfer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTransfersTransferArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/transfers/{transfer}",
        args.transfer,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers/{transfer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_transfers_transfer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Transfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Transfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/transfers/{transfer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_transfers_transfer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_transfers_transfer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/transfers/{transfer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_transfers_transfer(
    client: &SimpleHttpClient,
    args: &GetTransfersTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_transfers_transfer_builder(client, args)?;
    get_transfers_transfer_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/transfers/{transfer}
// -----------------------------------------------------------------------------

/// POST /v1/transfers/{transfer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_transfers_transfer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTransfersTransferArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/transfers/{transfer}",
        args.transfer,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers/{transfer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_transfers_transfer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Transfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Transfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/transfers/{transfer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_transfers_transfer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_transfers_transfer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/transfers/{transfer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_transfers_transfer(
    client: &SimpleHttpClient,
    args: &PostTransfersTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_transfers_transfer_builder(client, args)?;
    post_transfers_transfer_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/radar/value_list_items
// -----------------------------------------------------------------------------

/// GET /v1/radar/value_list_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_radar_value_list_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetRadarValueListItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/radar/value_list_items",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_list_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_radar_value_list_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/radar/value_list_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_radar_value_list_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_radar_value_list_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/radar/value_list_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_radar_value_list_items(
    client: &SimpleHttpClient,
    args: &GetRadarValueListItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_radar_value_list_items_builder(client, args)?;
    get_radar_value_list_items_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/radar/value_list_items
// -----------------------------------------------------------------------------

/// POST /v1/radar/value_list_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_radar_value_list_items_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/radar/value_list_items",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/value_list_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_radar_value_list_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: RadarValueListItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/radar/value_list_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_radar_value_list_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_radar_value_list_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/radar/value_list_items/{item}
// -----------------------------------------------------------------------------

/// GET /v1/radar/value_list_items/{item} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_radar_value_list_items_item_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetRadarValueListItemsItemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/radar/value_list_items/{item}",
        args.item,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_list_items/{item} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_radar_value_list_items_item_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: RadarValueListItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/radar/value_list_items/{item} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_radar_value_list_items_item_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_radar_value_list_items_item_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/radar/value_list_items/{item} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_radar_value_list_items_item(
    client: &SimpleHttpClient,
    args: &GetRadarValueListItemsItemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_radar_value_list_items_item_builder(client, args)?;
    get_radar_value_list_items_item_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/radar/value_list_items/{item}
// -----------------------------------------------------------------------------

/// DELETE /v1/radar/value_list_items/{item} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_radar_value_list_items_item_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteRadarValueListItemsItemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/radar/value_list_items/{item}",
        args.item,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/radar/value_list_items/{item} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_radar_value_list_items_item_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedRadarValueListItem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedRadarValueListItem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/radar/value_list_items/{item} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_radar_value_list_items_item_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedRadarValueListItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_radar_value_list_items_item_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/radar/value_list_items/{item} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_radar_value_list_items_item(
    client: &SimpleHttpClient,
    args: &DeleteRadarValueListItemsItemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedRadarValueListItem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_radar_value_list_items_item_builder(client, args)?;
    delete_radar_value_list_items_item_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/identity/verification_sessions
// -----------------------------------------------------------------------------

/// GET /v1/identity/verification_sessions - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_identity_verification_sessions_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetIdentityVerificationSessionsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/identity/verification_sessions",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/identity/verification_sessions - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_identity_verification_sessions_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/identity/verification_sessions - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_identity_verification_sessions_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_identity_verification_sessions_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/identity/verification_sessions - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_identity_verification_sessions(
    client: &SimpleHttpClient,
    args: &GetIdentityVerificationSessionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_identity_verification_sessions_builder(client, args)?;
    get_identity_verification_sessions_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/identity/verification_sessions
// -----------------------------------------------------------------------------

/// POST /v1/identity/verification_sessions - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_identity_verification_sessions_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/identity/verification_sessions",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_identity_verification_sessions_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IdentityVerificationSession = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/identity/verification_sessions - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_identity_verification_sessions_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_identity_verification_sessions_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/identity/verification_sessions/{session}
// -----------------------------------------------------------------------------

/// GET /v1/identity/verification_sessions/{session} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_identity_verification_sessions_session_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetIdentityVerificationSessionsSessionArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{session}",
        args.session,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/identity/verification_sessions/{session} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_identity_verification_sessions_session_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IdentityVerificationSession = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/identity/verification_sessions/{session} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_identity_verification_sessions_session_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_identity_verification_sessions_session_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/identity/verification_sessions/{session} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_identity_verification_sessions_session(
    client: &SimpleHttpClient,
    args: &GetIdentityVerificationSessionsSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_identity_verification_sessions_session_builder(client, args)?;
    get_identity_verification_sessions_session_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/identity/verification_sessions/{session}
// -----------------------------------------------------------------------------

/// POST /v1/identity/verification_sessions/{session} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_identity_verification_sessions_session_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostIdentityVerificationSessionsSessionArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{session}",
        args.session,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions/{session} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_identity_verification_sessions_session_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IdentityVerificationSession = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/identity/verification_sessions/{session} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_identity_verification_sessions_session_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_identity_verification_sessions_session_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/identity/verification_sessions/{session} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_identity_verification_sessions_session(
    client: &SimpleHttpClient,
    args: &PostIdentityVerificationSessionsSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_identity_verification_sessions_session_builder(client, args)?;
    post_identity_verification_sessions_session_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/coupons
// -----------------------------------------------------------------------------

/// GET /v1/coupons - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_coupons_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCouponsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/coupons",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/coupons - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_coupons_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/coupons - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_coupons_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_coupons_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/coupons - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_coupons(
    client: &SimpleHttpClient,
    args: &GetCouponsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_coupons_builder(client, args)?;
    get_coupons_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/coupons
// -----------------------------------------------------------------------------

/// POST /v1/coupons - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_coupons_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/coupons",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/coupons - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_coupons_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Coupon>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Coupon = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/coupons - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_coupons_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_coupons_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/coupons/{coupon}
// -----------------------------------------------------------------------------

/// GET /v1/coupons/{coupon} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_coupons_coupon_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCouponsCouponArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/coupons/{coupon}", args.coupon,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/coupons/{coupon} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_coupons_coupon_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Coupon>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Coupon = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/coupons/{coupon} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_coupons_coupon_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_coupons_coupon_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/coupons/{coupon} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_coupons_coupon(
    client: &SimpleHttpClient,
    args: &GetCouponsCouponArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_coupons_coupon_builder(client, args)?;
    get_coupons_coupon_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/coupons/{coupon}
// -----------------------------------------------------------------------------

/// POST /v1/coupons/{coupon} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_coupons_coupon_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostCouponsCouponArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/coupons/{coupon}", args.coupon,);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/coupons/{coupon} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_coupons_coupon_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Coupon>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Coupon = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/coupons/{coupon} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_coupons_coupon_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_coupons_coupon_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/coupons/{coupon} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_coupons_coupon(
    client: &SimpleHttpClient,
    args: &PostCouponsCouponArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_coupons_coupon_builder(client, args)?;
    post_coupons_coupon_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/coupons/{coupon}
// -----------------------------------------------------------------------------

/// DELETE /v1/coupons/{coupon} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_coupons_coupon_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteCouponsCouponArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/coupons/{coupon}", args.coupon,);

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/coupons/{coupon} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_coupons_coupon_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedCoupon>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedCoupon = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/coupons/{coupon} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_coupons_coupon_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedCoupon>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_coupons_coupon_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/coupons/{coupon} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_coupons_coupon(
    client: &SimpleHttpClient,
    args: &DeleteCouponsCouponArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedCoupon>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_coupons_coupon_builder(client, args)?;
    delete_coupons_coupon_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/customers
// -----------------------------------------------------------------------------

/// GET /v1/customers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_customers_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCustomersArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/customers",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_customers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/customers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_customers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_customers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/customers - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_customers(
    client: &SimpleHttpClient,
    args: &GetCustomersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_customers_builder(client, args)?;
    get_customers_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/customers
// -----------------------------------------------------------------------------

/// POST /v1/customers - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_customers_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/customers",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_customers_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Customer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/customers - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_customers_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_customers_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/customers/{customer}
// -----------------------------------------------------------------------------

/// GET /v1/customers/{customer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_customers_customer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCustomersCustomerArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}",
        args.customer,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_customers_customer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: serde_json::Value = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/customers/{customer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_customers_customer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_customers_customer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/customers/{customer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_customers_customer(
    client: &SimpleHttpClient,
    args: &GetCustomersCustomerArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_customers_customer_builder(client, args)?;
    get_customers_customer_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/customers/{customer}
// -----------------------------------------------------------------------------

/// POST /v1/customers/{customer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_customers_customer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostCustomersCustomerArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}",
        args.customer,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_customers_customer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Customer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/customers/{customer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_customers_customer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_customers_customer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/customers/{customer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_customers_customer(
    client: &SimpleHttpClient,
    args: &PostCustomersCustomerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_customers_customer_builder(client, args)?;
    post_customers_customer_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/customers/{customer}
// -----------------------------------------------------------------------------

/// DELETE /v1/customers/{customer} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_customers_customer_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteCustomersCustomerArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}",
        args.customer,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_customers_customer_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedCustomer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedCustomer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/customers/{customer} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_customers_customer_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedCustomer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_customers_customer_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/customers/{customer} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_customers_customer(
    client: &SimpleHttpClient,
    args: &DeleteCustomersCustomerArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedCustomer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_customers_customer_builder(client, args)?;
    delete_customers_customer_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/customers/{customer}/discount
// -----------------------------------------------------------------------------

/// GET /v1/customers/{customer}/discount - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_customers_customer_discount_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCustomersCustomerDiscountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}/discount",
        args.customer,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/discount - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_customers_customer_discount_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Discount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Discount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/customers/{customer}/discount - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_customers_customer_discount_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_customers_customer_discount_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/customers/{customer}/discount - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_customers_customer_discount(
    client: &SimpleHttpClient,
    args: &GetCustomersCustomerDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_customers_customer_discount_builder(client, args)?;
    get_customers_customer_discount_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/customers/{customer}/discount
// -----------------------------------------------------------------------------

/// DELETE /v1/customers/{customer}/discount - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_customers_customer_discount_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteCustomersCustomerDiscountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}/discount",
        args.customer,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/discount - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_customers_customer_discount_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedDiscount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/customers/{customer}/discount - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_customers_customer_discount_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_customers_customer_discount_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/customers/{customer}/discount - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_customers_customer_discount(
    client: &SimpleHttpClient,
    args: &DeleteCustomersCustomerDiscountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_customers_customer_discount_builder(client, args)?;
    delete_customers_customer_discount_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
// -----------------------------------------------------------------------------

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_customers_customer_subscriptions_subscription_exposed_id_discount_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount",
        args.customer,
        args.subscription_exposed_id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_customers_customer_subscriptions_subscription_exposed_id_discount_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Discount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Discount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_customers_customer_subscriptions_subscription_exposed_id_discount_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_customers_customer_subscriptions_subscription_exposed_id_discount_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_customers_customer_subscriptions_subscription_exposed_id_discount(
    client: &SimpleHttpClient,
    args: &GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
        client, args,
    )?;
    get_customers_customer_subscriptions_subscription_exposed_id_discount_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
// -----------------------------------------------------------------------------

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_customers_customer_subscriptions_subscription_exposed_id_discount_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount",
        args.customer,
        args.subscription_exposed_id,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_customers_customer_subscriptions_subscription_exposed_id_discount_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedDiscount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_customers_customer_subscriptions_subscription_exposed_id_discount_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task =
        delete_customers_customer_subscriptions_subscription_exposed_id_discount_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_customers_customer_subscriptions_subscription_exposed_id_discount(
    client: &SimpleHttpClient,
    args: &DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
        client, args,
    )?;
    delete_customers_customer_subscriptions_subscription_exposed_id_discount_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/subscriptions/{subscription_exposed_id}/discount
// -----------------------------------------------------------------------------

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_subscriptions_subscription_exposed_id_discount_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscriptions/{subscription_exposed_id}/discount",
        args.subscription_exposed_id,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_subscriptions_subscription_exposed_id_discount_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedDiscount = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_subscriptions_subscription_exposed_id_discount_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_subscriptions_subscription_exposed_id_discount_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_subscriptions_subscription_exposed_id_discount(
    client: &SimpleHttpClient,
    args: &DeleteSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_subscriptions_subscription_exposed_id_discount_builder(client, args)?;
    delete_subscriptions_subscription_exposed_id_discount_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/invoiceitems
// -----------------------------------------------------------------------------

/// GET /v1/invoiceitems - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_invoiceitems_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetInvoiceitemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/invoiceitems",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoiceitems - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_invoiceitems_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/invoiceitems - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_invoiceitems_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_invoiceitems_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/invoiceitems - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_invoiceitems(
    client: &SimpleHttpClient,
    args: &GetInvoiceitemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_invoiceitems_builder(client, args)?;
    get_invoiceitems_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/invoiceitems
// -----------------------------------------------------------------------------

/// POST /v1/invoiceitems - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_invoiceitems_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/invoiceitems",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoiceitems - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_invoiceitems_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invoiceitem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/invoiceitems - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_invoiceitems_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_invoiceitems_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/invoiceitems/{invoiceitem}
// -----------------------------------------------------------------------------

/// GET /v1/invoiceitems/{invoiceitem} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_invoiceitems_invoiceitem_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetInvoiceitemsInvoiceitemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoiceitems/{invoiceitem}",
        args.invoiceitem,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoiceitems/{invoiceitem} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_invoiceitems_invoiceitem_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invoiceitem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/invoiceitems/{invoiceitem} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_invoiceitems_invoiceitem_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_invoiceitems_invoiceitem_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/invoiceitems/{invoiceitem} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_invoiceitems_invoiceitem(
    client: &SimpleHttpClient,
    args: &GetInvoiceitemsInvoiceitemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_invoiceitems_invoiceitem_builder(client, args)?;
    get_invoiceitems_invoiceitem_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/invoiceitems/{invoiceitem}
// -----------------------------------------------------------------------------

/// POST /v1/invoiceitems/{invoiceitem} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_invoiceitems_invoiceitem_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostInvoiceitemsInvoiceitemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoiceitems/{invoiceitem}",
        args.invoiceitem,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoiceitems/{invoiceitem} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_invoiceitems_invoiceitem_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invoiceitem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/invoiceitems/{invoiceitem} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_invoiceitems_invoiceitem_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_invoiceitems_invoiceitem_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoiceitems/{invoiceitem} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_invoiceitems_invoiceitem(
    client: &SimpleHttpClient,
    args: &PostInvoiceitemsInvoiceitemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_invoiceitems_invoiceitem_builder(client, args)?;
    post_invoiceitems_invoiceitem_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/invoiceitems/{invoiceitem}
// -----------------------------------------------------------------------------

/// DELETE /v1/invoiceitems/{invoiceitem} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_invoiceitems_invoiceitem_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteInvoiceitemsInvoiceitemArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoiceitems/{invoiceitem}",
        args.invoiceitem,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/invoiceitems/{invoiceitem} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_invoiceitems_invoiceitem_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedInvoiceitem>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedInvoiceitem = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/invoiceitems/{invoiceitem} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_invoiceitems_invoiceitem_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedInvoiceitem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_invoiceitems_invoiceitem_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/invoiceitems/{invoiceitem} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_invoiceitems_invoiceitem(
    client: &SimpleHttpClient,
    args: &DeleteInvoiceitemsInvoiceitemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedInvoiceitem>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_invoiceitems_invoiceitem_builder(client, args)?;
    delete_invoiceitems_invoiceitem_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/invoices
// -----------------------------------------------------------------------------

/// GET /v1/invoices - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_invoices_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetInvoicesArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/invoices",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_invoices_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/invoices - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_invoices_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_invoices_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/invoices - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_invoices(
    client: &SimpleHttpClient,
    args: &GetInvoicesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_invoices_builder(client, args)?;
    get_invoices_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/invoices
// -----------------------------------------------------------------------------

/// POST /v1/invoices - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_invoices_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/invoices",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_invoices_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invoice>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/invoices - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_invoices_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_invoices_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/invoices/{invoice}
// -----------------------------------------------------------------------------

/// GET /v1/invoices/{invoice} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_invoices_invoice_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetInvoicesInvoiceArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoices/{invoice}",
        args.invoice,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices/{invoice} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_invoices_invoice_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invoice>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/invoices/{invoice} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_invoices_invoice_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_invoices_invoice_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/invoices/{invoice} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_invoices_invoice(
    client: &SimpleHttpClient,
    args: &GetInvoicesInvoiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_invoices_invoice_builder(client, args)?;
    get_invoices_invoice_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/invoices/{invoice}
// -----------------------------------------------------------------------------

/// POST /v1/invoices/{invoice} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_invoices_invoice_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostInvoicesInvoiceArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoices/{invoice}",
        args.invoice,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_invoices_invoice_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invoice>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/invoices/{invoice} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_invoices_invoice_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_invoices_invoice_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoices/{invoice} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_invoices_invoice(
    client: &SimpleHttpClient,
    args: &PostInvoicesInvoiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_invoices_invoice_builder(client, args)?;
    post_invoices_invoice_execute(builder)
}

// -----------------------------------------------------------------------------
// DELETE /v1/invoices/{invoice}
// -----------------------------------------------------------------------------

/// DELETE /v1/invoices/{invoice} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn delete_invoices_invoice_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &DeleteInvoicesInvoiceArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/invoices/{invoice}",
        args.invoice,
    );

    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/invoices/{invoice} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn delete_invoices_invoice_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeletedInvoice>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: DeletedInvoice = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// DELETE /v1/invoices/{invoice} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn delete_invoices_invoice_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedInvoice>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = delete_invoices_invoice_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /v1/invoices/{invoice} - convenience function.
///
/// Combines builder and execute in one call.

pub fn delete_invoices_invoice(
    client: &SimpleHttpClient,
    args: &DeleteInvoicesInvoiceArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeletedInvoice>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = delete_invoices_invoice_builder(client, args)?;
    delete_invoices_invoice_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/checkout/sessions/{session}/line_items
// -----------------------------------------------------------------------------

/// GET /v1/checkout/sessions/{session}/line_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_checkout_sessions_session_line_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetCheckoutSessionsSessionLineItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/checkout/sessions/{session}/line_items",
        args.session,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/checkout/sessions/{session}/line_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_checkout_sessions_session_line_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/checkout/sessions/{session}/line_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_checkout_sessions_session_line_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_checkout_sessions_session_line_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/checkout/sessions/{session}/line_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_checkout_sessions_session_line_items(
    client: &SimpleHttpClient,
    args: &GetCheckoutSessionsSessionLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_checkout_sessions_session_line_items_builder(client, args)?;
    get_checkout_sessions_session_line_items_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/payment_links/{payment_link}/line_items
// -----------------------------------------------------------------------------

/// GET /v1/payment_links/{payment_link}/line_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payment_links_payment_link_line_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetPaymentLinksPaymentLinkLineItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_links/{payment_link}/line_items",
        args.payment_link,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_links/{payment_link}/line_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payment_links_payment_link_line_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/payment_links/{payment_link}/line_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payment_links_payment_link_line_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_payment_links_payment_link_line_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payment_links/{payment_link}/line_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payment_links_payment_link_line_items(
    client: &SimpleHttpClient,
    args: &GetPaymentLinksPaymentLinkLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_payment_links_payment_link_line_items_builder(client, args)?;
    get_payment_links_payment_link_line_items_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/quotes/{quote}/line_items
// -----------------------------------------------------------------------------

/// GET /v1/quotes/{quote}/line_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_quotes_quote_line_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetQuotesQuoteLineItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/quotes/{quote}/line_items",
        args.quote,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes/{quote}/line_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_quotes_quote_line_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/quotes/{quote}/line_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_quotes_quote_line_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_quotes_quote_line_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/quotes/{quote}/line_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_quotes_quote_line_items(
    client: &SimpleHttpClient,
    args: &GetQuotesQuoteLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_quotes_quote_line_items_builder(client, args)?;
    get_quotes_quote_line_items_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/tax/calculations/{calculation}/line_items
// -----------------------------------------------------------------------------

/// GET /v1/tax/calculations/{calculation}/line_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tax_calculations_calculation_line_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTaxCalculationsCalculationLineItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax/calculations/{calculation}/line_items",
        args.calculation,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/calculations/{calculation}/line_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tax_calculations_calculation_line_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/tax/calculations/{calculation}/line_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tax_calculations_calculation_line_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_tax_calculations_calculation_line_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tax/calculations/{calculation}/line_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tax_calculations_calculation_line_items(
    client: &SimpleHttpClient,
    args: &GetTaxCalculationsCalculationLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_tax_calculations_calculation_line_items_builder(client, args)?;
    get_tax_calculations_calculation_line_items_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/tax/transactions/{transaction}/line_items
// -----------------------------------------------------------------------------

/// GET /v1/tax/transactions/{transaction}/line_items - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tax_transactions_transaction_line_items_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetTaxTransactionsTransactionLineItemsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax/transactions/{transaction}/line_items",
        args.transaction,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/transactions/{transaction}/line_items - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tax_transactions_transaction_line_items_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/tax/transactions/{transaction}/line_items - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tax_transactions_transaction_line_items_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_tax_transactions_transaction_line_items_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tax/transactions/{transaction}/line_items - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tax_transactions_transaction_line_items(
    client: &SimpleHttpClient,
    args: &GetTaxTransactionsTransactionLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_tax_transactions_transaction_line_items_builder(client, args)?;
    get_tax_transactions_transaction_line_items_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// GET /v1/test_helpers/test_clocks.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_test_helpers_test_clocks(
        &self,
        args: &GetTestHelpersTestClocksArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_test_helpers_test_clocks_builder(&self.http_client, args)?;
        get_test_helpers_test_clocks_execute(builder)
    }

    /// POST /v1/test_helpers/test_clocks.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_test_helpers_test_clocks(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_test_helpers_test_clocks_builder(&self.http_client)?;
        post_test_helpers_test_clocks_execute(builder)
    }

    /// GET /v1/test_helpers/test_clocks/{test_clock}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_test_helpers_test_clocks_test_clock(
        &self,
        args: &GetTestHelpersTestClocksTestClockArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TestHelpersTestClock>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_test_helpers_test_clocks_test_clock_builder(&self.http_client, args)?;
        get_test_helpers_test_clocks_test_clock_execute(builder)
    }

    /// DELETE /v1/test_helpers/test_clocks/{test_clock}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_test_helpers_test_clocks_test_clock(
        &self,
        args: &DeleteTestHelpersTestClocksTestClockArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedTestHelpersTestClock>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_test_helpers_test_clocks_test_clock_builder(&self.http_client, args)?;
        delete_test_helpers_test_clocks_test_clock_execute(builder)
    }

    /// GET /v1/topups.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_topups(
        &self,
        args: &GetTopupsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_topups_builder(&self.http_client, args)?;
        get_topups_execute(builder)
    }

    /// POST /v1/topups.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_topups(
        &self,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_topups_builder(&self.http_client)?;
        post_topups_execute(builder)
    }

    /// GET /v1/topups/{topup}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_topups_topup(
        &self,
        args: &GetTopupsTopupArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_topups_topup_builder(&self.http_client, args)?;
        get_topups_topup_execute(builder)
    }

    /// POST /v1/topups/{topup}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_topups_topup(
        &self,
        args: &PostTopupsTopupArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_topups_topup_builder(&self.http_client, args)?;
        post_topups_topup_execute(builder)
    }

    /// GET /v1/transfers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_transfers(
        &self,
        args: &GetTransfersArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_transfers_builder(&self.http_client, args)?;
        get_transfers_execute(builder)
    }

    /// POST /v1/transfers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_transfers(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Transfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_transfers_builder(&self.http_client)?;
        post_transfers_execute(builder)
    }

    /// GET /v1/transfers/{transfer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_transfers_transfer(
        &self,
        args: &GetTransfersTransferArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Transfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_transfers_transfer_builder(&self.http_client, args)?;
        get_transfers_transfer_execute(builder)
    }

    /// POST /v1/transfers/{transfer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_transfers_transfer(
        &self,
        args: &PostTransfersTransferArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Transfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_transfers_transfer_builder(&self.http_client, args)?;
        post_transfers_transfer_execute(builder)
    }

    /// GET /v1/radar/value_list_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_radar_value_list_items(
        &self,
        args: &GetRadarValueListItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_radar_value_list_items_builder(&self.http_client, args)?;
        get_radar_value_list_items_execute(builder)
    }

    /// POST /v1/radar/value_list_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_radar_value_list_items(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_radar_value_list_items_builder(&self.http_client)?;
        post_radar_value_list_items_execute(builder)
    }

    /// GET /v1/radar/value_list_items/{item}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_radar_value_list_items_item(
        &self,
        args: &GetRadarValueListItemsItemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<RadarValueListItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_radar_value_list_items_item_builder(&self.http_client, args)?;
        get_radar_value_list_items_item_execute(builder)
    }

    /// DELETE /v1/radar/value_list_items/{item}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_radar_value_list_items_item(
        &self,
        args: &DeleteRadarValueListItemsItemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedRadarValueListItem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_radar_value_list_items_item_builder(&self.http_client, args)?;
        delete_radar_value_list_items_item_execute(builder)
    }

    /// GET /v1/identity/verification_sessions.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_identity_verification_sessions(
        &self,
        args: &GetIdentityVerificationSessionsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_identity_verification_sessions_builder(&self.http_client, args)?;
        get_identity_verification_sessions_execute(builder)
    }

    /// POST /v1/identity/verification_sessions.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_identity_verification_sessions(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_identity_verification_sessions_builder(&self.http_client)?;
        post_identity_verification_sessions_execute(builder)
    }

    /// GET /v1/identity/verification_sessions/{session}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_identity_verification_sessions_session(
        &self,
        args: &GetIdentityVerificationSessionsSessionArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_identity_verification_sessions_session_builder(&self.http_client, args)?;
        get_identity_verification_sessions_session_execute(builder)
    }

    /// POST /v1/identity/verification_sessions/{session}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_identity_verification_sessions_session(
        &self,
        args: &PostIdentityVerificationSessionsSessionArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_identity_verification_sessions_session_builder(&self.http_client, args)?;
        post_identity_verification_sessions_session_execute(builder)
    }

    /// GET /v1/coupons.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_coupons(
        &self,
        args: &GetCouponsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_coupons_builder(&self.http_client, args)?;
        get_coupons_execute(builder)
    }

    /// POST /v1/coupons.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_coupons(
        &self,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_coupons_builder(&self.http_client)?;
        post_coupons_execute(builder)
    }

    /// GET /v1/coupons/{coupon}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_coupons_coupon(
        &self,
        args: &GetCouponsCouponArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_coupons_coupon_builder(&self.http_client, args)?;
        get_coupons_coupon_execute(builder)
    }

    /// POST /v1/coupons/{coupon}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_coupons_coupon(
        &self,
        args: &PostCouponsCouponArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Coupon>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_coupons_coupon_builder(&self.http_client, args)?;
        post_coupons_coupon_execute(builder)
    }

    /// DELETE /v1/coupons/{coupon}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_coupons_coupon(
        &self,
        args: &DeleteCouponsCouponArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedCoupon>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_coupons_coupon_builder(&self.http_client, args)?;
        delete_coupons_coupon_execute(builder)
    }

    /// GET /v1/customers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_customers(
        &self,
        args: &GetCustomersArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_customers_builder(&self.http_client, args)?;
        get_customers_execute(builder)
    }

    /// POST /v1/customers.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_customers(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Customer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_customers_builder(&self.http_client)?;
        post_customers_execute(builder)
    }

    /// GET /v1/customers/{customer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_customers_customer(
        &self,
        args: &GetCustomersCustomerArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<serde_json::Value>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_customers_customer_builder(&self.http_client, args)?;
        get_customers_customer_execute(builder)
    }

    /// POST /v1/customers/{customer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_customers_customer(
        &self,
        args: &PostCustomersCustomerArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Customer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_customers_customer_builder(&self.http_client, args)?;
        post_customers_customer_execute(builder)
    }

    /// DELETE /v1/customers/{customer}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_customers_customer(
        &self,
        args: &DeleteCustomersCustomerArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedCustomer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_customers_customer_builder(&self.http_client, args)?;
        delete_customers_customer_execute(builder)
    }

    /// GET /v1/customers/{customer}/discount.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_customers_customer_discount(
        &self,
        args: &GetCustomersCustomerDiscountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Discount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_customers_customer_discount_builder(&self.http_client, args)?;
        get_customers_customer_discount_execute(builder)
    }

    /// DELETE /v1/customers/{customer}/discount.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_customers_customer_discount(
        &self,
        args: &DeleteCustomersCustomerDiscountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_customers_customer_discount_builder(&self.http_client, args)?;
        delete_customers_customer_discount_execute(builder)
    }

    /// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_customers_customer_subscriptions_subscription_exposed_id_discount(
        &self,
        args: &GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Discount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            get_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
                &self.http_client,
                args,
            )?;
        get_customers_customer_subscriptions_subscription_exposed_id_discount_execute(builder)
    }

    /// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_customers_customer_subscriptions_subscription_exposed_id_discount(
        &self,
        args: &DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            delete_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
                &self.http_client,
                args,
            )?;
        delete_customers_customer_subscriptions_subscription_exposed_id_discount_execute(builder)
    }

    /// DELETE /v1/subscriptions/{subscription_exposed_id}/discount.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_subscriptions_subscription_exposed_id_discount(
        &self,
        args: &DeleteSubscriptionsSubscriptionExposedIdDiscountArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedDiscount>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            delete_subscriptions_subscription_exposed_id_discount_builder(&self.http_client, args)?;
        delete_subscriptions_subscription_exposed_id_discount_execute(builder)
    }

    /// GET /v1/invoiceitems.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_invoiceitems(
        &self,
        args: &GetInvoiceitemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_invoiceitems_builder(&self.http_client, args)?;
        get_invoiceitems_execute(builder)
    }

    /// POST /v1/invoiceitems.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_invoiceitems(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Invoiceitem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_invoiceitems_builder(&self.http_client)?;
        post_invoiceitems_execute(builder)
    }

    /// GET /v1/invoiceitems/{invoiceitem}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_invoiceitems_invoiceitem(
        &self,
        args: &GetInvoiceitemsInvoiceitemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Invoiceitem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_invoiceitems_invoiceitem_builder(&self.http_client, args)?;
        get_invoiceitems_invoiceitem_execute(builder)
    }

    /// POST /v1/invoiceitems/{invoiceitem}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_invoiceitems_invoiceitem(
        &self,
        args: &PostInvoiceitemsInvoiceitemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<Invoiceitem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_invoiceitems_invoiceitem_builder(&self.http_client, args)?;
        post_invoiceitems_invoiceitem_execute(builder)
    }

    /// DELETE /v1/invoiceitems/{invoiceitem}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_invoiceitems_invoiceitem(
        &self,
        args: &DeleteInvoiceitemsInvoiceitemArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedInvoiceitem>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_invoiceitems_invoiceitem_builder(&self.http_client, args)?;
        delete_invoiceitems_invoiceitem_execute(builder)
    }

    /// GET /v1/invoices.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_invoices(
        &self,
        args: &GetInvoicesArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_invoices_builder(&self.http_client, args)?;
        get_invoices_execute(builder)
    }

    /// POST /v1/invoices.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_invoices(
        &self,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_invoices_builder(&self.http_client)?;
        post_invoices_execute(builder)
    }

    /// GET /v1/invoices/{invoice}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_invoices_invoice(
        &self,
        args: &GetInvoicesInvoiceArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_invoices_invoice_builder(&self.http_client, args)?;
        get_invoices_invoice_execute(builder)
    }

    /// POST /v1/invoices/{invoice}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_post_invoices_invoice(
        &self,
        args: &PostInvoicesInvoiceArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Invoice>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_invoices_invoice_builder(&self.http_client, args)?;
        post_invoices_invoice_execute(builder)
    }

    /// DELETE /v1/invoices/{invoice}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_delete_invoices_invoice(
        &self,
        args: &DeleteInvoicesInvoiceArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<DeletedInvoice>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = delete_invoices_invoice_builder(&self.http_client, args)?;
        delete_invoices_invoice_execute(builder)
    }

    /// GET /v1/checkout/sessions/{session}/line_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_checkout_sessions_session_line_items(
        &self,
        args: &GetCheckoutSessionsSessionLineItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_checkout_sessions_session_line_items_builder(&self.http_client, args)?;
        get_checkout_sessions_session_line_items_execute(builder)
    }

    /// GET /v1/payment_links/{payment_link}/line_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_payment_links_payment_link_line_items(
        &self,
        args: &GetPaymentLinksPaymentLinkLineItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_payment_links_payment_link_line_items_builder(&self.http_client, args)?;
        get_payment_links_payment_link_line_items_execute(builder)
    }

    /// GET /v1/quotes/{quote}/line_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_quotes_quote_line_items(
        &self,
        args: &GetQuotesQuoteLineItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_quotes_quote_line_items_builder(&self.http_client, args)?;
        get_quotes_quote_line_items_execute(builder)
    }

    /// GET /v1/tax/calculations/{calculation}/line_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_tax_calculations_calculation_line_items(
        &self,
        args: &GetTaxCalculationsCalculationLineItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_tax_calculations_calculation_line_items_builder(&self.http_client, args)?;
        get_tax_calculations_calculation_line_items_execute(builder)
    }

    /// GET /v1/tax/transactions/{transaction}/line_items.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_test_clocks_topups_transfers_value_list_items_verification_sessions_coupons_customers_discount_invoiceitems_invoices_line_items_get_tax_transactions_transaction_line_items(
        &self,
        args: &GetTaxTransactionsTransactionLineItemsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_tax_transactions_transaction_line_items_builder(&self.http_client, args)?;
        get_tax_transactions_transaction_line_items_execute(builder)
    }
}
