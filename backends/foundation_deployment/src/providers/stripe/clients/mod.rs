//! Auto-generated API clients for stripe.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe`

#![cfg(feature = "stripe")]

pub mod types;

use crate::providers::stripe::clients::types::*;
use crate::providers::stripe::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET /v1/account
/// Retrieve account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_account_execute()` to send, or `get_v1_account` for simplest API.

pub fn get_v1_account_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/account",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/account
/// Retrieve account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/account
/// Retrieve account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_account_builder()` + `get_v1_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_account(
    client: &SimpleHttpClient,
    args: &GetV1AccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_account_builder(client, args.expand)?;
    get_v1_account_execute(builder)
}

/// POST /v1/account_links
/// Create an account link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_account_links_execute()` to send, or `post_v1_account_links` for simplest API.

pub fn post_v1_account_links_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/account_links",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/account_links
/// Create an account link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_account_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_account_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_account_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/account_links
/// Create an account link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_account_links_builder()` + `post_v1_account_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_account_links(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_account_links_builder(client)?;
    post_v1_account_links_execute(builder)
}

/// POST /v1/account_sessions
/// Create an Account Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_account_sessions_execute()` to send, or `post_v1_account_sessions` for simplest API.

pub fn post_v1_account_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/account_sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/account_sessions
/// Create an Account Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_account_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_account_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_account_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account_session>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/account_sessions
/// Create an Account Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_account_sessions_builder()` + `post_v1_account_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_account_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account_session>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_account_sessions_builder(client)?;
    post_v1_account_sessions_execute(builder)
}

/// GET /v1/accounts
/// List all connected accounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_execute()` to send, or `get_v1_accounts` for simplest API.

pub fn get_v1_accounts_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts
/// List all connected accounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/accounts
/// List all connected accounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_builder()` + `get_v1_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts(
    client: &SimpleHttpClient,
    args: &GetV1AccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_accounts_execute(builder)
}

/// POST /v1/accounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_execute()` to send, or `post_v1_accounts` for simplest API.

pub fn post_v1_accounts_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/accounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_builder()` + `post_v1_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_builder(client)?;
    post_v1_accounts_execute(builder)
}

/// GET /v1/accounts/{account}
/// Retrieve account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_execute()` to send, or `get_v1_accounts_account` for simplest API.

pub fn get_v1_accounts_account_builder(
    client: &SimpleHttpClient,
    account: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}", account,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}
/// Retrieve account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}
/// Retrieve account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_builder()` + `get_v1_accounts_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_builder(client, &args.account, args.expand)?;
    get_v1_accounts_account_execute(builder)
}

/// POST /v1/accounts/{account}
/// Update an account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_execute()` to send, or `post_v1_accounts_account` for simplest API.

pub fn post_v1_accounts_account_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}", account,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}
/// Update an account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}
/// Update an account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_builder()` + `post_v1_accounts_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_builder(client, &args.account)?;
    post_v1_accounts_account_execute(builder)
}

/// DELETE /v1/accounts/{account}
/// Delete an account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_accounts_account_execute()` to send, or `delete_v1_accounts_account` for simplest API.

pub fn delete_v1_accounts_account_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}", account,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}
/// Delete an account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_accounts_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_accounts_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_accounts_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1AccountsAccountArgs {
    /// Path parameter: account
    pub account: String,
}

/// DELETE /v1/accounts/{account}
/// Delete an account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_accounts_account_builder()` + `delete_v1_accounts_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account(
    client: &SimpleHttpClient,
    args: &DeleteV1AccountsAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_accounts_account_builder(client, &args.account)?;
    delete_v1_accounts_account_execute(builder)
}

/// POST /v1/accounts/{account}/bank_accounts
/// Create an external account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_bank_accounts_execute()` to send, or `post_v1_accounts_account_bank_accounts` for simplest API.

pub fn post_v1_accounts_account_bank_accounts_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/bank_accounts",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/bank_accounts
/// Create an external account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_bank_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_bank_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_bank_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_bank_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountBankAccountsArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}/bank_accounts
/// Create an external account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_bank_accounts_builder()` + `post_v1_accounts_account_bank_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_bank_accounts(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountBankAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_bank_accounts_builder(client, &args.account)?;
    post_v1_accounts_account_bank_accounts_execute(builder)
}

/// GET /v1/accounts/{account}/bank_accounts/{id}
/// Retrieve an external account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_bank_accounts_id_execute()` to send, or `get_v1_accounts_account_bank_accounts_id` for simplest API.

pub fn get_v1_accounts_account_bank_accounts_id_builder(
    client: &SimpleHttpClient,
    account: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/bank_accounts/{}",
        account, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/bank_accounts/{id}
/// Retrieve an external account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_bank_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_bank_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_bank_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_bank_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountBankAccountsIdArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}/bank_accounts/{id}
/// Retrieve an external account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_bank_accounts_id_builder()` + `get_v1_accounts_account_bank_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_bank_accounts_id(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountBankAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_bank_accounts_id_builder(
        client,
        &args.account,
        &args.id,
        args.expand,
    )?;
    get_v1_accounts_account_bank_accounts_id_execute(builder)
}

/// POST /v1/accounts/{account}/bank_accounts/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_bank_accounts_id_execute()` to send, or `post_v1_accounts_account_bank_accounts_id` for simplest API.

pub fn post_v1_accounts_account_bank_accounts_id_builder(
    client: &SimpleHttpClient,
    account: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/bank_accounts/{}",
        account, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/bank_accounts/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_bank_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_bank_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_bank_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_bank_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountBankAccountsIdArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/accounts/{account}/bank_accounts/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_bank_accounts_id_builder()` + `post_v1_accounts_account_bank_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_bank_accounts_id(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountBankAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_accounts_account_bank_accounts_id_builder(client, &args.account, &args.id)?;
    post_v1_accounts_account_bank_accounts_id_execute(builder)
}

/// DELETE /v1/accounts/{account}/bank_accounts/{id}
/// Delete an external account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_accounts_account_bank_accounts_id_execute()` to send, or `delete_v1_accounts_account_bank_accounts_id` for simplest API.

pub fn delete_v1_accounts_account_bank_accounts_id_builder(
    client: &SimpleHttpClient,
    account: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/bank_accounts/{}",
        account, id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}/bank_accounts/{id}
/// Delete an external account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_accounts_account_bank_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_accounts_account_bank_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_bank_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_external_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_external_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_accounts_account_bank_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1AccountsAccountBankAccountsIdArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/accounts/{account}/bank_accounts/{id}
/// Delete an external account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_accounts_account_bank_accounts_id_builder()` + `delete_v1_accounts_account_bank_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_bank_accounts_id(
    client: &SimpleHttpClient,
    args: &DeleteV1AccountsAccountBankAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_external_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_accounts_account_bank_accounts_id_builder(client, &args.account, &args.id)?;
    delete_v1_accounts_account_bank_accounts_id_execute(builder)
}

/// GET /v1/accounts/{account}/capabilities
/// List all account capabilities
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_capabilities_execute()` to send, or `get_v1_accounts_account_capabilities` for simplest API.

pub fn get_v1_accounts_account_capabilities_builder(
    client: &SimpleHttpClient,
    account: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/capabilities",
        account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/capabilities
/// List all account capabilities
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_capabilities_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_capabilities_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_capabilities_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_capabilities`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountCapabilitiesArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}/capabilities
/// List all account capabilities
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_capabilities_builder()` + `get_v1_accounts_account_capabilities_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_capabilities(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountCapabilitiesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_capabilities_builder(client, &args.account, args.expand)?;
    get_v1_accounts_account_capabilities_execute(builder)
}

/// GET /v1/accounts/{account}/capabilities/{capability}
/// Retrieve an Account Capability
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_capabilities_capability_execute()` to send, or `get_v1_accounts_account_capabilities_capability` for simplest API.

pub fn get_v1_accounts_account_capabilities_capability_builder(
    client: &SimpleHttpClient,
    account: &str,
    capability: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/capabilities/{}",
        account, capability,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/capabilities/{capability}
/// Retrieve an Account Capability
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_capabilities_capability_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_capabilities_capability_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_capabilities_capability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Capability>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Capability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_capabilities_capability`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountCapabilitiesCapabilityArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: capability
    pub capability: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}/capabilities/{capability}
/// Retrieve an Account Capability
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_capabilities_capability_builder()` + `get_v1_accounts_account_capabilities_capability_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_capabilities_capability(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountCapabilitiesCapabilityArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Capability>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_capabilities_capability_builder(
        client,
        &args.account,
        &args.capability,
        args.expand,
    )?;
    get_v1_accounts_account_capabilities_capability_execute(builder)
}

/// POST /v1/accounts/{account}/capabilities/{capability}
/// Update an Account Capability
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_capabilities_capability_execute()` to send, or `post_v1_accounts_account_capabilities_capability` for simplest API.

pub fn post_v1_accounts_account_capabilities_capability_builder(
    client: &SimpleHttpClient,
    account: &str,
    capability: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/capabilities/{}",
        account, capability,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/capabilities/{capability}
/// Update an Account Capability
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_capabilities_capability_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_capabilities_capability_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_capabilities_capability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Capability>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Capability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_capabilities_capability`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountCapabilitiesCapabilityArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: capability
    pub capability: String,
}

/// POST /v1/accounts/{account}/capabilities/{capability}
/// Update an Account Capability
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_capabilities_capability_builder()` + `post_v1_accounts_account_capabilities_capability_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_capabilities_capability(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountCapabilitiesCapabilityArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Capability>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_capabilities_capability_builder(
        client,
        &args.account,
        &args.capability,
    )?;
    post_v1_accounts_account_capabilities_capability_execute(builder)
}

/// GET /v1/accounts/{account}/external_accounts
/// List all external accounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_external_accounts_execute()` to send, or `get_v1_accounts_account_external_accounts` for simplest API.

pub fn get_v1_accounts_account_external_accounts_builder(
    client: &SimpleHttpClient,
    account: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    object: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/external_accounts",
        account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = object {
        query_parts.push(format!("object={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/external_accounts
/// List all external accounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_external_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_external_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_external_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_external_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountExternalAccountsArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: object
    pub object: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/accounts/{account}/external_accounts
/// List all external accounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_external_accounts_builder()` + `get_v1_accounts_account_external_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_external_accounts(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountExternalAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_external_accounts_builder(
        client,
        &args.account,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.object.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_accounts_account_external_accounts_execute(builder)
}

/// POST /v1/accounts/{account}/external_accounts
/// Create an external account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_external_accounts_execute()` to send, or `post_v1_accounts_account_external_accounts` for simplest API.

pub fn post_v1_accounts_account_external_accounts_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/external_accounts",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/external_accounts
/// Create an external account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_external_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_external_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_external_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_external_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountExternalAccountsArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}/external_accounts
/// Create an external account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_external_accounts_builder()` + `post_v1_accounts_account_external_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_external_accounts(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountExternalAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_external_accounts_builder(client, &args.account)?;
    post_v1_accounts_account_external_accounts_execute(builder)
}

/// GET /v1/accounts/{account}/external_accounts/{id}
/// Retrieve an external account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_external_accounts_id_execute()` to send, or `get_v1_accounts_account_external_accounts_id` for simplest API.

pub fn get_v1_accounts_account_external_accounts_id_builder(
    client: &SimpleHttpClient,
    account: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/external_accounts/{}",
        account, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/external_accounts/{id}
/// Retrieve an external account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_external_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_external_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_external_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_external_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountExternalAccountsIdArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}/external_accounts/{id}
/// Retrieve an external account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_external_accounts_id_builder()` + `get_v1_accounts_account_external_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_external_accounts_id(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountExternalAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_external_accounts_id_builder(
        client,
        &args.account,
        &args.id,
        args.expand,
    )?;
    get_v1_accounts_account_external_accounts_id_execute(builder)
}

/// POST /v1/accounts/{account}/external_accounts/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_external_accounts_id_execute()` to send, or `post_v1_accounts_account_external_accounts_id` for simplest API.

pub fn post_v1_accounts_account_external_accounts_id_builder(
    client: &SimpleHttpClient,
    account: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/external_accounts/{}",
        account, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/external_accounts/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_external_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_external_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_external_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_external_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountExternalAccountsIdArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/accounts/{account}/external_accounts/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_external_accounts_id_builder()` + `post_v1_accounts_account_external_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_external_accounts_id(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountExternalAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_accounts_account_external_accounts_id_builder(client, &args.account, &args.id)?;
    post_v1_accounts_account_external_accounts_id_execute(builder)
}

/// DELETE /v1/accounts/{account}/external_accounts/{id}
/// Delete an external account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_accounts_account_external_accounts_id_execute()` to send, or `delete_v1_accounts_account_external_accounts_id` for simplest API.

pub fn delete_v1_accounts_account_external_accounts_id_builder(
    client: &SimpleHttpClient,
    account: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/external_accounts/{}",
        account, id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}/external_accounts/{id}
/// Delete an external account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_accounts_account_external_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_accounts_account_external_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_external_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_external_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_external_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_accounts_account_external_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1AccountsAccountExternalAccountsIdArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/accounts/{account}/external_accounts/{id}
/// Delete an external account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_accounts_account_external_accounts_id_builder()` + `delete_v1_accounts_account_external_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_external_accounts_id(
    client: &SimpleHttpClient,
    args: &DeleteV1AccountsAccountExternalAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_external_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_accounts_account_external_accounts_id_builder(client, &args.account, &args.id)?;
    delete_v1_accounts_account_external_accounts_id_execute(builder)
}

/// POST /v1/accounts/{account}/login_links
/// Create a login link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_login_links_execute()` to send, or `post_v1_accounts_account_login_links` for simplest API.

pub fn post_v1_accounts_account_login_links_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/login_links",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/login_links
/// Create a login link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_login_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_login_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_login_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Login_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Login_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_login_links`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountLoginLinksArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}/login_links
/// Create a login link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_login_links_builder()` + `post_v1_accounts_account_login_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_login_links(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountLoginLinksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Login_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_login_links_builder(client, &args.account)?;
    post_v1_accounts_account_login_links_execute(builder)
}

/// GET /v1/accounts/{account}/people
/// List all persons
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_people_execute()` to send, or `get_v1_accounts_account_people` for simplest API.

pub fn get_v1_accounts_account_people_builder(
    client: &SimpleHttpClient,
    account: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    relationship: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}/people", account,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = relationship {
        query_parts.push(format!("relationship={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/people
/// List all persons
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_people_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_people_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_people_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_people`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountPeopleArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: relationship
    pub relationship: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/accounts/{account}/people
/// List all persons
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_people_builder()` + `get_v1_accounts_account_people_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_people(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountPeopleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_people_builder(
        client,
        &args.account,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.relationship.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_accounts_account_people_execute(builder)
}

/// POST /v1/accounts/{account}/people
/// Create a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_people_execute()` to send, or `post_v1_accounts_account_people` for simplest API.

pub fn post_v1_accounts_account_people_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}/people", account,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/people
/// Create a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_people_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_people_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_people_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_people`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountPeopleArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}/people
/// Create a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_people_builder()` + `post_v1_accounts_account_people_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_people(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountPeopleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_people_builder(client, &args.account)?;
    post_v1_accounts_account_people_execute(builder)
}

/// GET /v1/accounts/{account}/people/{person}
/// Retrieve a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_people_person_execute()` to send, or `get_v1_accounts_account_people_person` for simplest API.

pub fn get_v1_accounts_account_people_person_builder(
    client: &SimpleHttpClient,
    account: &str,
    person: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/people/{}",
        account, person,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/people/{person}
/// Retrieve a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_people_person_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_people_person_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_people_person_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_people_person`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountPeoplePersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}/people/{person}
/// Retrieve a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_people_person_builder()` + `get_v1_accounts_account_people_person_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_people_person(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountPeoplePersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_people_person_builder(
        client,
        &args.account,
        &args.person,
        args.expand,
    )?;
    get_v1_accounts_account_people_person_execute(builder)
}

/// POST /v1/accounts/{account}/people/{person}
/// Update a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_people_person_execute()` to send, or `post_v1_accounts_account_people_person` for simplest API.

pub fn post_v1_accounts_account_people_person_builder(
    client: &SimpleHttpClient,
    account: &str,
    person: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/people/{}",
        account, person,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/people/{person}
/// Update a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_people_person_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_people_person_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_people_person_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_people_person`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountPeoplePersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// POST /v1/accounts/{account}/people/{person}
/// Update a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_people_person_builder()` + `post_v1_accounts_account_people_person_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_people_person(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountPeoplePersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_accounts_account_people_person_builder(client, &args.account, &args.person)?;
    post_v1_accounts_account_people_person_execute(builder)
}

/// DELETE /v1/accounts/{account}/people/{person}
/// Delete a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_accounts_account_people_person_execute()` to send, or `delete_v1_accounts_account_people_person` for simplest API.

pub fn delete_v1_accounts_account_people_person_builder(
    client: &SimpleHttpClient,
    account: &str,
    person: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/people/{}",
        account, person,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}/people/{person}
/// Delete a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_accounts_account_people_person_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_accounts_account_people_person_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_people_person_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_person>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_accounts_account_people_person`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1AccountsAccountPeoplePersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// DELETE /v1/accounts/{account}/people/{person}
/// Delete a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_accounts_account_people_person_builder()` + `delete_v1_accounts_account_people_person_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_people_person(
    client: &SimpleHttpClient,
    args: &DeleteV1AccountsAccountPeoplePersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_person>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_accounts_account_people_person_builder(client, &args.account, &args.person)?;
    delete_v1_accounts_account_people_person_execute(builder)
}

/// GET /v1/accounts/{account}/persons
/// List all persons
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_persons_execute()` to send, or `get_v1_accounts_account_persons` for simplest API.

pub fn get_v1_accounts_account_persons_builder(
    client: &SimpleHttpClient,
    account: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    relationship: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}/persons", account,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = relationship {
        query_parts.push(format!("relationship={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/persons
/// List all persons
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_persons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_persons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_persons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_persons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountPersonsArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: relationship
    pub relationship: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/accounts/{account}/persons
/// List all persons
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_persons_builder()` + `get_v1_accounts_account_persons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_persons(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountPersonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_persons_builder(
        client,
        &args.account,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.relationship.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_accounts_account_persons_execute(builder)
}

/// POST /v1/accounts/{account}/persons
/// Create a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_persons_execute()` to send, or `post_v1_accounts_account_persons` for simplest API.

pub fn post_v1_accounts_account_persons_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}/persons", account,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/persons
/// Create a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_persons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_persons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_persons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_persons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountPersonsArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}/persons
/// Create a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_persons_builder()` + `post_v1_accounts_account_persons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_persons(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountPersonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_persons_builder(client, &args.account)?;
    post_v1_accounts_account_persons_execute(builder)
}

/// GET /v1/accounts/{account}/persons/{person}
/// Retrieve a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_accounts_account_persons_person_execute()` to send, or `get_v1_accounts_account_persons_person` for simplest API.

pub fn get_v1_accounts_account_persons_person_builder(
    client: &SimpleHttpClient,
    account: &str,
    person: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/persons/{}",
        account, person,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/accounts/{account}/persons/{person}
/// Retrieve a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_accounts_account_persons_person_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_accounts_account_persons_person_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_persons_person_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_accounts_account_persons_person`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AccountsAccountPersonsPersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/accounts/{account}/persons/{person}
/// Retrieve a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_accounts_account_persons_person_builder()` + `get_v1_accounts_account_persons_person_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_accounts_account_persons_person(
    client: &SimpleHttpClient,
    args: &GetV1AccountsAccountPersonsPersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_accounts_account_persons_person_builder(
        client,
        &args.account,
        &args.person,
        args.expand,
    )?;
    get_v1_accounts_account_persons_person_execute(builder)
}

/// POST /v1/accounts/{account}/persons/{person}
/// Update a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_persons_person_execute()` to send, or `post_v1_accounts_account_persons_person` for simplest API.

pub fn post_v1_accounts_account_persons_person_builder(
    client: &SimpleHttpClient,
    account: &str,
    person: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/persons/{}",
        account, person,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/persons/{person}
/// Update a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_persons_person_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_persons_person_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_persons_person_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_persons_person`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountPersonsPersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// POST /v1/accounts/{account}/persons/{person}
/// Update a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_persons_person_builder()` + `post_v1_accounts_account_persons_person_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_persons_person(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountPersonsPersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_accounts_account_persons_person_builder(client, &args.account, &args.person)?;
    post_v1_accounts_account_persons_person_execute(builder)
}

/// DELETE /v1/accounts/{account}/persons/{person}
/// Delete a person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_accounts_account_persons_person_execute()` to send, or `delete_v1_accounts_account_persons_person` for simplest API.

pub fn delete_v1_accounts_account_persons_person_builder(
    client: &SimpleHttpClient,
    account: &str,
    person: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/accounts/{}/persons/{}",
        account, person,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/accounts/{account}/persons/{person}
/// Delete a person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_accounts_account_persons_person_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_accounts_account_persons_person_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_persons_person_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_person>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_accounts_account_persons_person`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1AccountsAccountPersonsPersonArgs {
    /// Path parameter: account
    pub account: String,
    /// Path parameter: person
    pub person: String,
}

/// DELETE /v1/accounts/{account}/persons/{person}
/// Delete a person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_accounts_account_persons_person_builder()` + `delete_v1_accounts_account_persons_person_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_accounts_account_persons_person(
    client: &SimpleHttpClient,
    args: &DeleteV1AccountsAccountPersonsPersonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_person>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_accounts_account_persons_person_builder(client, &args.account, &args.person)?;
    delete_v1_accounts_account_persons_person_execute(builder)
}

/// POST /v1/accounts/{account}/reject
/// Reject an account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_accounts_account_reject_execute()` to send, or `post_v1_accounts_account_reject` for simplest API.

pub fn post_v1_accounts_account_reject_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/accounts/{}/reject", account,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/accounts/{account}/reject
/// Reject an account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_accounts_account_reject_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_accounts_account_reject_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_reject_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_accounts_account_reject`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1AccountsAccountRejectArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/accounts/{account}/reject
/// Reject an account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_accounts_account_reject_builder()` + `post_v1_accounts_account_reject_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_accounts_account_reject(
    client: &SimpleHttpClient,
    args: &PostV1AccountsAccountRejectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_accounts_account_reject_builder(client, &args.account)?;
    post_v1_accounts_account_reject_execute(builder)
}

/// GET /v1/apple_pay/domains
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_apple_pay_domains_execute()` to send, or `get_v1_apple_pay_domains` for simplest API.

pub fn get_v1_apple_pay_domains_builder(
    client: &SimpleHttpClient,
    domain_name: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apple_pay/domains",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = domain_name {
        query_parts.push(format!("domain_name={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/apple_pay/domains
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_apple_pay_domains_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_apple_pay_domains_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apple_pay_domains_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_apple_pay_domains`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ApplePayDomainsArgs {
    /// Query parameter: domain_name
    pub domain_name: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/apple_pay/domains
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_apple_pay_domains_builder()` + `get_v1_apple_pay_domains_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apple_pay_domains(
    client: &SimpleHttpClient,
    args: &GetV1ApplePayDomainsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_apple_pay_domains_builder(
        client,
        args.domain_name.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_apple_pay_domains_execute(builder)
}

/// POST /v1/apple_pay/domains
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_apple_pay_domains_execute()` to send, or `post_v1_apple_pay_domains` for simplest API.

pub fn post_v1_apple_pay_domains_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apple_pay/domains",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/apple_pay/domains
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_apple_pay_domains_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_apple_pay_domains_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_apple_pay_domains_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Apple_pay_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Apple_pay_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/apple_pay/domains
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_apple_pay_domains_builder()` + `post_v1_apple_pay_domains_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_apple_pay_domains(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Apple_pay_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_apple_pay_domains_builder(client)?;
    post_v1_apple_pay_domains_execute(builder)
}

/// GET /v1/apple_pay/domains/{domain}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_apple_pay_domains_domain_execute()` to send, or `get_v1_apple_pay_domains_domain` for simplest API.

pub fn get_v1_apple_pay_domains_domain_builder(
    client: &SimpleHttpClient,
    domain: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apple_pay/domains/{}", domain,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/apple_pay/domains/{domain}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_apple_pay_domains_domain_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_apple_pay_domains_domain_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apple_pay_domains_domain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Apple_pay_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Apple_pay_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_apple_pay_domains_domain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ApplePayDomainsDomainArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/apple_pay/domains/{domain}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_apple_pay_domains_domain_builder()` + `get_v1_apple_pay_domains_domain_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apple_pay_domains_domain(
    client: &SimpleHttpClient,
    args: &GetV1ApplePayDomainsDomainArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Apple_pay_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_apple_pay_domains_domain_builder(client, &args.domain, args.expand)?;
    get_v1_apple_pay_domains_domain_execute(builder)
}

/// DELETE /v1/apple_pay/domains/{domain}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_apple_pay_domains_domain_execute()` to send, or `delete_v1_apple_pay_domains_domain` for simplest API.

pub fn delete_v1_apple_pay_domains_domain_builder(
    client: &SimpleHttpClient,
    domain: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apple_pay/domains/{}", domain,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/apple_pay/domains/{domain}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_apple_pay_domains_domain_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_apple_pay_domains_domain_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_apple_pay_domains_domain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_apple_pay_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_apple_pay_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_apple_pay_domains_domain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ApplePayDomainsDomainArgs {
    /// Path parameter: domain
    pub domain: String,
}

/// DELETE /v1/apple_pay/domains/{domain}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_apple_pay_domains_domain_builder()` + `delete_v1_apple_pay_domains_domain_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_apple_pay_domains_domain(
    client: &SimpleHttpClient,
    args: &DeleteV1ApplePayDomainsDomainArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_apple_pay_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_apple_pay_domains_domain_builder(client, &args.domain)?;
    delete_v1_apple_pay_domains_domain_execute(builder)
}

/// GET /v1/application_fees
/// List all application fees
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_application_fees_execute()` to send, or `get_v1_application_fees` for simplest API.

pub fn get_v1_application_fees_builder(
    client: &SimpleHttpClient,
    charge: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/application_fees",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = charge {
        query_parts.push(format!("charge={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/application_fees
/// List all application fees
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_application_fees_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_application_fees_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_application_fees`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ApplicationFeesArgs {
    /// Query parameter: charge
    pub charge: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/application_fees
/// List all application fees
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_application_fees_builder()` + `get_v1_application_fees_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees(
    client: &SimpleHttpClient,
    args: &GetV1ApplicationFeesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_application_fees_builder(
        client,
        args.charge.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_application_fees_execute(builder)
}

/// GET /v1/application_fees/{fee}/refunds/{id}
/// Retrieve an application fee refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_application_fees_fee_refunds_id_execute()` to send, or `get_v1_application_fees_fee_refunds_id` for simplest API.

pub fn get_v1_application_fees_fee_refunds_id_builder(
    client: &SimpleHttpClient,
    fee: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/application_fees/{}/refunds/{}",
        fee, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/application_fees/{fee}/refunds/{id}
/// Retrieve an application fee refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_application_fees_fee_refunds_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_application_fees_fee_refunds_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_fee_refunds_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Fee_refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Fee_refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_application_fees_fee_refunds_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ApplicationFeesFeeRefundsIdArgs {
    /// Path parameter: fee
    pub fee: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/application_fees/{fee}/refunds/{id}
/// Retrieve an application fee refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_application_fees_fee_refunds_id_builder()` + `get_v1_application_fees_fee_refunds_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_fee_refunds_id(
    client: &SimpleHttpClient,
    args: &GetV1ApplicationFeesFeeRefundsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Fee_refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_v1_application_fees_fee_refunds_id_builder(client, &args.fee, &args.id, args.expand)?;
    get_v1_application_fees_fee_refunds_id_execute(builder)
}

/// POST /v1/application_fees/{fee}/refunds/{id}
/// Update an application fee refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_application_fees_fee_refunds_id_execute()` to send, or `post_v1_application_fees_fee_refunds_id` for simplest API.

pub fn post_v1_application_fees_fee_refunds_id_builder(
    client: &SimpleHttpClient,
    fee: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/application_fees/{}/refunds/{}",
        fee, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/application_fees/{fee}/refunds/{id}
/// Update an application fee refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_application_fees_fee_refunds_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_application_fees_fee_refunds_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_application_fees_fee_refunds_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Fee_refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Fee_refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_application_fees_fee_refunds_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ApplicationFeesFeeRefundsIdArgs {
    /// Path parameter: fee
    pub fee: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/application_fees/{fee}/refunds/{id}
/// Update an application fee refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_application_fees_fee_refunds_id_builder()` + `post_v1_application_fees_fee_refunds_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_application_fees_fee_refunds_id(
    client: &SimpleHttpClient,
    args: &PostV1ApplicationFeesFeeRefundsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Fee_refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_application_fees_fee_refunds_id_builder(client, &args.fee, &args.id)?;
    post_v1_application_fees_fee_refunds_id_execute(builder)
}

/// GET /v1/application_fees/{id}
/// Retrieve an application fee
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_application_fees_id_execute()` to send, or `get_v1_application_fees_id` for simplest API.

pub fn get_v1_application_fees_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/application_fees/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/application_fees/{id}
/// Retrieve an application fee
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_application_fees_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_application_fees_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application_fee>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Application_fee = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_application_fees_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ApplicationFeesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/application_fees/{id}
/// Retrieve an application fee
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_application_fees_id_builder()` + `get_v1_application_fees_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_id(
    client: &SimpleHttpClient,
    args: &GetV1ApplicationFeesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application_fee>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_application_fees_id_builder(client, &args.id, args.expand)?;
    get_v1_application_fees_id_execute(builder)
}

/// POST /v1/application_fees/{id}/refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_application_fees_id_refund_execute()` to send, or `post_v1_application_fees_id_refund` for simplest API.

pub fn post_v1_application_fees_id_refund_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/application_fees/{}/refund", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/application_fees/{id}/refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_application_fees_id_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_application_fees_id_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_application_fees_id_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application_fee>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Application_fee = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_application_fees_id_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ApplicationFeesIdRefundArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/application_fees/{id}/refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_application_fees_id_refund_builder()` + `post_v1_application_fees_id_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_application_fees_id_refund(
    client: &SimpleHttpClient,
    args: &PostV1ApplicationFeesIdRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application_fee>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_application_fees_id_refund_builder(client, &args.id)?;
    post_v1_application_fees_id_refund_execute(builder)
}

/// GET /v1/application_fees/{id}/refunds
/// List all application fee refunds
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_application_fees_id_refunds_execute()` to send, or `get_v1_application_fees_id_refunds` for simplest API.

pub fn get_v1_application_fees_id_refunds_builder(
    client: &SimpleHttpClient,
    id: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/application_fees/{}/refunds", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/application_fees/{id}/refunds
/// List all application fee refunds
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_application_fees_id_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_application_fees_id_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_id_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_application_fees_id_refunds`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ApplicationFeesIdRefundsArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/application_fees/{id}/refunds
/// List all application fee refunds
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_application_fees_id_refunds_builder()` + `get_v1_application_fees_id_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_application_fees_id_refunds(
    client: &SimpleHttpClient,
    args: &GetV1ApplicationFeesIdRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_application_fees_id_refunds_builder(
        client,
        &args.id,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_application_fees_id_refunds_execute(builder)
}

/// POST /v1/application_fees/{id}/refunds
/// Create an application fee refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_application_fees_id_refunds_execute()` to send, or `post_v1_application_fees_id_refunds` for simplest API.

pub fn post_v1_application_fees_id_refunds_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/application_fees/{}/refunds", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/application_fees/{id}/refunds
/// Create an application fee refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_application_fees_id_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_application_fees_id_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_application_fees_id_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Fee_refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Fee_refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_application_fees_id_refunds`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ApplicationFeesIdRefundsArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/application_fees/{id}/refunds
/// Create an application fee refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_application_fees_id_refunds_builder()` + `post_v1_application_fees_id_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_application_fees_id_refunds(
    client: &SimpleHttpClient,
    args: &PostV1ApplicationFeesIdRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Fee_refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_application_fees_id_refunds_builder(client, &args.id)?;
    post_v1_application_fees_id_refunds_execute(builder)
}

/// GET /v1/apps/secrets
/// List secrets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_apps_secrets_execute()` to send, or `get_v1_apps_secrets` for simplest API.

pub fn get_v1_apps_secrets_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    scope: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apps/secrets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = scope {
        query_parts.push(format!("scope={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/apps/secrets
/// List secrets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_apps_secrets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_apps_secrets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apps_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_apps_secrets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AppsSecretsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: scope
    pub scope: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/apps/secrets
/// List secrets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_apps_secrets_builder()` + `get_v1_apps_secrets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apps_secrets(
    client: &SimpleHttpClient,
    args: &GetV1AppsSecretsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_apps_secrets_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.scope.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_apps_secrets_execute(builder)
}

/// POST /v1/apps/secrets
/// Set a Secret
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_apps_secrets_execute()` to send, or `post_v1_apps_secrets` for simplest API.

pub fn post_v1_apps_secrets_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apps/secrets",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/apps/secrets
/// Set a Secret
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_apps_secrets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_apps_secrets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_apps_secrets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppsSecret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppsSecret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/apps/secrets
/// Set a Secret
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_apps_secrets_builder()` + `post_v1_apps_secrets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_apps_secrets(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppsSecret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_apps_secrets_builder(client)?;
    post_v1_apps_secrets_execute(builder)
}

/// POST /v1/apps/secrets/delete
/// Delete a Secret
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_apps_secrets_delete_execute()` to send, or `post_v1_apps_secrets_delete` for simplest API.

pub fn post_v1_apps_secrets_delete_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apps/secrets/delete",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/apps/secrets/delete
/// Delete a Secret
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_apps_secrets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_apps_secrets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_apps_secrets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppsSecret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppsSecret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/apps/secrets/delete
/// Delete a Secret
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_apps_secrets_delete_builder()` + `post_v1_apps_secrets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_apps_secrets_delete(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppsSecret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_apps_secrets_delete_builder(client)?;
    post_v1_apps_secrets_delete_execute(builder)
}

/// GET /v1/apps/secrets/find
/// Find a Secret
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_apps_secrets_find_execute()` to send, or `get_v1_apps_secrets_find` for simplest API.

pub fn get_v1_apps_secrets_find_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    name: Option<&str>,
    scope: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/apps/secrets/find",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = scope {
        query_parts.push(format!("scope={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/apps/secrets/find
/// Find a Secret
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_apps_secrets_find_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_apps_secrets_find_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apps_secrets_find_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppsSecret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppsSecret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_apps_secrets_find`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1AppsSecretsFindArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: scope
    pub scope: Option<String>,
}

/// GET /v1/apps/secrets/find
/// Find a Secret
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_apps_secrets_find_builder()` + `get_v1_apps_secrets_find_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_apps_secrets_find(
    client: &SimpleHttpClient,
    args: &GetV1AppsSecretsFindArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppsSecret>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_apps_secrets_find_builder(
        client,
        args.expand,
        args.name.as_deref(),
        args.scope.as_deref(),
    )?;
    get_v1_apps_secrets_find_execute(builder)
}

/// GET /v1/balance
/// Retrieve balance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_balance_execute()` to send, or `get_v1_balance` for simplest API.

pub fn get_v1_balance_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/balance
/// Retrieve balance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Balance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_balance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BalanceArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/balance
/// Retrieve balance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_balance_builder()` + `get_v1_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance(
    client: &SimpleHttpClient,
    args: &GetV1BalanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_balance_builder(client, args.expand)?;
    get_v1_balance_execute(builder)
}

/// GET /v1/balance/history
/// List all balance transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_balance_history_execute()` to send, or `get_v1_balance_history` for simplest API.

pub fn get_v1_balance_history_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    currency: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payout: Option<&str>,
    source: Option<&str>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance/history",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = currency {
        query_parts.push(format!("currency={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payout {
        query_parts.push(format!("payout={}", val));
    }
    if let Some(val) = source {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/balance/history
/// List all balance transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_balance_history_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_balance_history_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_history_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_balance_history`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BalanceHistoryArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payout
    pub payout: Option<String>,
    /// Query parameter: source
    pub source: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/balance/history
/// List all balance transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_balance_history_builder()` + `get_v1_balance_history_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_history(
    client: &SimpleHttpClient,
    args: &GetV1BalanceHistoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_balance_history_builder(
        client,
        args.created.as_deref(),
        args.currency.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payout.as_deref(),
        args.source.as_deref(),
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_balance_history_execute(builder)
}

/// GET /v1/balance/history/{id}
/// Retrieve a balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_balance_history_id_execute()` to send, or `get_v1_balance_history_id` for simplest API.

pub fn get_v1_balance_history_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance/history/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/balance/history/{id}
/// Retrieve a balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_balance_history_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_balance_history_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_history_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_transaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_balance_history_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BalanceHistoryIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/balance/history/{id}
/// Retrieve a balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_balance_history_id_builder()` + `get_v1_balance_history_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_history_id(
    client: &SimpleHttpClient,
    args: &GetV1BalanceHistoryIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_transaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_balance_history_id_builder(client, &args.id, args.expand)?;
    get_v1_balance_history_id_execute(builder)
}

/// GET /v1/balance_settings
/// Retrieve balance settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_balance_settings_execute()` to send, or `get_v1_balance_settings` for simplest API.

pub fn get_v1_balance_settings_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance_settings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/balance_settings
/// Retrieve balance settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_balance_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_balance_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_settings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Balance_settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_balance_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BalanceSettingsArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/balance_settings
/// Retrieve balance settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_balance_settings_builder()` + `get_v1_balance_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_settings(
    client: &SimpleHttpClient,
    args: &GetV1BalanceSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_settings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_balance_settings_builder(client, args.expand)?;
    get_v1_balance_settings_execute(builder)
}

/// POST /v1/balance_settings
/// Update balance settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_balance_settings_execute()` to send, or `post_v1_balance_settings` for simplest API.

pub fn post_v1_balance_settings_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance_settings",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/balance_settings
/// Update balance settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_balance_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_balance_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_balance_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_settings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Balance_settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/balance_settings
/// Update balance settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_balance_settings_builder()` + `post_v1_balance_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_balance_settings(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_settings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_balance_settings_builder(client)?;
    post_v1_balance_settings_execute(builder)
}

/// GET /v1/balance_transactions
/// List all balance transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_balance_transactions_execute()` to send, or `get_v1_balance_transactions` for simplest API.

pub fn get_v1_balance_transactions_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    currency: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payout: Option<&str>,
    source: Option<&str>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance_transactions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = currency {
        query_parts.push(format!("currency={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payout {
        query_parts.push(format!("payout={}", val));
    }
    if let Some(val) = source {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/balance_transactions
/// List all balance transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_balance_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_balance_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_balance_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BalanceTransactionsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payout
    pub payout: Option<String>,
    /// Query parameter: source
    pub source: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/balance_transactions
/// List all balance transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_balance_transactions_builder()` + `get_v1_balance_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_transactions(
    client: &SimpleHttpClient,
    args: &GetV1BalanceTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_balance_transactions_builder(
        client,
        args.created.as_deref(),
        args.currency.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payout.as_deref(),
        args.source.as_deref(),
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_balance_transactions_execute(builder)
}

/// GET /v1/balance_transactions/{id}
/// Retrieve a balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_balance_transactions_id_execute()` to send, or `get_v1_balance_transactions_id` for simplest API.

pub fn get_v1_balance_transactions_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/balance_transactions/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/balance_transactions/{id}
/// Retrieve a balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_balance_transactions_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_balance_transactions_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_transactions_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_transaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_balance_transactions_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BalanceTransactionsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/balance_transactions/{id}
/// Retrieve a balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_balance_transactions_id_builder()` + `get_v1_balance_transactions_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_balance_transactions_id(
    client: &SimpleHttpClient,
    args: &GetV1BalanceTransactionsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Balance_transaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_balance_transactions_id_builder(client, &args.id, args.expand)?;
    get_v1_balance_transactions_id_execute(builder)
}

/// GET /v1/billing/alerts
/// List billing alerts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_alerts_execute()` to send, or `get_v1_billing_alerts` for simplest API.

pub fn get_v1_billing_alerts_builder(
    client: &SimpleHttpClient,
    alert_type: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    meter: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/alerts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = alert_type {
        query_parts.push(format!("alert_type={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = meter {
        query_parts.push(format!("meter={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/alerts
/// List billing alerts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_alerts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_alerts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_alerts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_alerts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingAlertsArgs {
    /// Query parameter: alert_type
    pub alert_type: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: meter
    pub meter: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/billing/alerts
/// List billing alerts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_alerts_builder()` + `get_v1_billing_alerts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_alerts(
    client: &SimpleHttpClient,
    args: &GetV1BillingAlertsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_billing_alerts_builder(
        client,
        args.alert_type.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.meter.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_billing_alerts_execute(builder)
}

/// POST /v1/billing/alerts
/// Create a billing alert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_alerts_execute()` to send, or `post_v1_billing_alerts` for simplest API.

pub fn post_v1_billing_alerts_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/alerts",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/alerts
/// Create a billing alert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_alerts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_alerts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAlert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/alerts
/// Create a billing alert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_alerts_builder()` + `post_v1_billing_alerts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_alerts_builder(client)?;
    post_v1_billing_alerts_execute(builder)
}

/// GET /v1/billing/alerts/{id}
/// Retrieve a billing alert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_alerts_id_execute()` to send, or `get_v1_billing_alerts_id` for simplest API.

pub fn get_v1_billing_alerts_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/alerts/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/alerts/{id}
/// Retrieve a billing alert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_alerts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_alerts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_alerts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAlert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_alerts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingAlertsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/billing/alerts/{id}
/// Retrieve a billing alert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_alerts_id_builder()` + `get_v1_billing_alerts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_alerts_id(
    client: &SimpleHttpClient,
    args: &GetV1BillingAlertsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_billing_alerts_id_builder(client, &args.id, args.expand)?;
    get_v1_billing_alerts_id_execute(builder)
}

/// POST /v1/billing/alerts/{id}/activate
/// Activate a billing alert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_alerts_id_activate_execute()` to send, or `post_v1_billing_alerts_id_activate` for simplest API.

pub fn post_v1_billing_alerts_id_activate_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/alerts/{}/activate", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/alerts/{id}/activate
/// Activate a billing alert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_alerts_id_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_alerts_id_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_id_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAlert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_alerts_id_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingAlertsIdActivateArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/alerts/{id}/activate
/// Activate a billing alert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_alerts_id_activate_builder()` + `post_v1_billing_alerts_id_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_id_activate(
    client: &SimpleHttpClient,
    args: &PostV1BillingAlertsIdActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_alerts_id_activate_builder(client, &args.id)?;
    post_v1_billing_alerts_id_activate_execute(builder)
}

/// POST /v1/billing/alerts/{id}/archive
/// Archive a billing alert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_alerts_id_archive_execute()` to send, or `post_v1_billing_alerts_id_archive` for simplest API.

pub fn post_v1_billing_alerts_id_archive_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/alerts/{}/archive", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/alerts/{id}/archive
/// Archive a billing alert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_alerts_id_archive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_alerts_id_archive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_id_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAlert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_alerts_id_archive`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingAlertsIdArchiveArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/alerts/{id}/archive
/// Archive a billing alert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_alerts_id_archive_builder()` + `post_v1_billing_alerts_id_archive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_id_archive(
    client: &SimpleHttpClient,
    args: &PostV1BillingAlertsIdArchiveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_alerts_id_archive_builder(client, &args.id)?;
    post_v1_billing_alerts_id_archive_execute(builder)
}

/// POST /v1/billing/alerts/{id}/deactivate
/// Deactivate a billing alert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_alerts_id_deactivate_execute()` to send, or `post_v1_billing_alerts_id_deactivate` for simplest API.

pub fn post_v1_billing_alerts_id_deactivate_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/alerts/{}/deactivate",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/alerts/{id}/deactivate
/// Deactivate a billing alert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_alerts_id_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_alerts_id_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_id_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAlert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_alerts_id_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingAlertsIdDeactivateArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/alerts/{id}/deactivate
/// Deactivate a billing alert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_alerts_id_deactivate_builder()` + `post_v1_billing_alerts_id_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_alerts_id_deactivate(
    client: &SimpleHttpClient,
    args: &PostV1BillingAlertsIdDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAlert>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_alerts_id_deactivate_builder(client, &args.id)?;
    post_v1_billing_alerts_id_deactivate_execute(builder)
}

/// GET /v1/billing/credit_balance_summary
/// Retrieve the credit balance summary for a customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_credit_balance_summary_execute()` to send, or `get_v1_billing_credit_balance_summary` for simplest API.

pub fn get_v1_billing_credit_balance_summary_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    customer_account: Option<&str>,
    expand: Option<Vec<String>>,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/credit_balance_summary",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_balance_summary
/// Retrieve the credit balance summary for a customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_credit_balance_summary_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_credit_balance_summary_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_balance_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingCredit_balance_summary>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_balance_summary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_credit_balance_summary`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingCreditBalanceSummaryArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET /v1/billing/credit_balance_summary
/// Retrieve the credit balance summary for a customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_credit_balance_summary_builder()` + `get_v1_billing_credit_balance_summary_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_balance_summary(
    client: &SimpleHttpClient,
    args: &GetV1BillingCreditBalanceSummaryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingCredit_balance_summary>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_billing_credit_balance_summary_builder(
        client,
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.expand,
        args.filter.as_deref(),
    )?;
    get_v1_billing_credit_balance_summary_execute(builder)
}

/// GET /v1/billing/credit_balance_transactions
/// List credit balance transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_credit_balance_transactions_execute()` to send, or `get_v1_billing_credit_balance_transactions` for simplest API.

pub fn get_v1_billing_credit_balance_transactions_builder(
    client: &SimpleHttpClient,
    credit_grant: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/credit_balance_transactions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = credit_grant {
        query_parts.push(format!("credit_grant={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_balance_transactions
/// List credit balance transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_credit_balance_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_credit_balance_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_balance_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_credit_balance_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingCreditBalanceTransactionsArgs {
    /// Query parameter: credit_grant
    pub credit_grant: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/billing/credit_balance_transactions
/// List credit balance transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_credit_balance_transactions_builder()` + `get_v1_billing_credit_balance_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_balance_transactions(
    client: &SimpleHttpClient,
    args: &GetV1BillingCreditBalanceTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_billing_credit_balance_transactions_builder(
        client,
        args.credit_grant.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_billing_credit_balance_transactions_execute(builder)
}

/// GET /v1/billing/credit_balance_transactions/{id}
/// Retrieve a credit balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_credit_balance_transactions_id_execute()` to send, or `get_v1_billing_credit_balance_transactions_id` for simplest API.

pub fn get_v1_billing_credit_balance_transactions_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/credit_balance_transactions/{}",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_balance_transactions/{id}
/// Retrieve a credit balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_credit_balance_transactions_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_credit_balance_transactions_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_balance_transactions_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingCredit_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_credit_balance_transactions_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingCreditBalanceTransactionsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/billing/credit_balance_transactions/{id}
/// Retrieve a credit balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_credit_balance_transactions_id_builder()` + `get_v1_billing_credit_balance_transactions_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_balance_transactions_id(
    client: &SimpleHttpClient,
    args: &GetV1BillingCreditBalanceTransactionsIdArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingCredit_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_billing_credit_balance_transactions_id_builder(client, &args.id, args.expand)?;
    get_v1_billing_credit_balance_transactions_id_execute(builder)
}

/// GET /v1/billing/credit_grants
/// List credit grants
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_credit_grants_execute()` to send, or `get_v1_billing_credit_grants` for simplest API.

pub fn get_v1_billing_credit_grants_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/credit_grants",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_grants
/// List credit grants
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_credit_grants_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_credit_grants_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_grants_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_credit_grants`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingCreditGrantsArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/billing/credit_grants
/// List credit grants
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_credit_grants_builder()` + `get_v1_billing_credit_grants_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_grants(
    client: &SimpleHttpClient,
    args: &GetV1BillingCreditGrantsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_billing_credit_grants_builder(
        client,
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_billing_credit_grants_execute(builder)
}

/// POST /v1/billing/credit_grants
/// Create a credit grant
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_credit_grants_execute()` to send, or `post_v1_billing_credit_grants` for simplest API.

pub fn post_v1_billing_credit_grants_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/credit_grants",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants
/// Create a credit grant
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_credit_grants_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_credit_grants_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/credit_grants
/// Create a credit grant
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_credit_grants_builder()` + `post_v1_billing_credit_grants_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_credit_grants_builder(client)?;
    post_v1_billing_credit_grants_execute(builder)
}

/// GET /v1/billing/credit_grants/{id}
/// Retrieve a credit grant
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_credit_grants_id_execute()` to send, or `get_v1_billing_credit_grants_id` for simplest API.

pub fn get_v1_billing_credit_grants_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/credit_grants/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/credit_grants/{id}
/// Retrieve a credit grant
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_credit_grants_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_credit_grants_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_grants_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_credit_grants_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingCreditGrantsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/billing/credit_grants/{id}
/// Retrieve a credit grant
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_credit_grants_id_builder()` + `get_v1_billing_credit_grants_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_credit_grants_id(
    client: &SimpleHttpClient,
    args: &GetV1BillingCreditGrantsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_billing_credit_grants_id_builder(client, &args.id, args.expand)?;
    get_v1_billing_credit_grants_id_execute(builder)
}

/// POST /v1/billing/credit_grants/{id}
/// Update a credit grant
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_credit_grants_id_execute()` to send, or `post_v1_billing_credit_grants_id` for simplest API.

pub fn post_v1_billing_credit_grants_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/credit_grants/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants/{id}
/// Update a credit grant
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_credit_grants_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_credit_grants_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_credit_grants_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingCreditGrantsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/credit_grants/{id}
/// Update a credit grant
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_credit_grants_id_builder()` + `post_v1_billing_credit_grants_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_id(
    client: &SimpleHttpClient,
    args: &PostV1BillingCreditGrantsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_credit_grants_id_builder(client, &args.id)?;
    post_v1_billing_credit_grants_id_execute(builder)
}

/// POST /v1/billing/credit_grants/{id}/expire
/// Expire a credit grant
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_credit_grants_id_expire_execute()` to send, or `post_v1_billing_credit_grants_id_expire` for simplest API.

pub fn post_v1_billing_credit_grants_id_expire_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/credit_grants/{}/expire",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants/{id}/expire
/// Expire a credit grant
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_credit_grants_id_expire_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_credit_grants_id_expire_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_id_expire_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_credit_grants_id_expire`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingCreditGrantsIdExpireArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/credit_grants/{id}/expire
/// Expire a credit grant
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_credit_grants_id_expire_builder()` + `post_v1_billing_credit_grants_id_expire_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_id_expire(
    client: &SimpleHttpClient,
    args: &PostV1BillingCreditGrantsIdExpireArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_credit_grants_id_expire_builder(client, &args.id)?;
    post_v1_billing_credit_grants_id_expire_execute(builder)
}

/// POST /v1/billing/credit_grants/{id}/void
/// Void a credit grant
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_credit_grants_id_void_execute()` to send, or `post_v1_billing_credit_grants_id_void` for simplest API.

pub fn post_v1_billing_credit_grants_id_void_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/credit_grants/{}/void",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/credit_grants/{id}/void
/// Void a credit grant
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_credit_grants_id_void_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_credit_grants_id_void_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_id_void_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingCredit_grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_credit_grants_id_void`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingCreditGrantsIdVoidArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/credit_grants/{id}/void
/// Void a credit grant
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_credit_grants_id_void_builder()` + `post_v1_billing_credit_grants_id_void_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_credit_grants_id_void(
    client: &SimpleHttpClient,
    args: &PostV1BillingCreditGrantsIdVoidArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingCredit_grant>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_credit_grants_id_void_builder(client, &args.id)?;
    post_v1_billing_credit_grants_id_void_execute(builder)
}

/// POST /v1/billing/meter_event_adjustments
/// Create a billing meter event adjustment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_meter_event_adjustments_execute()` to send, or `post_v1_billing_meter_event_adjustments` for simplest API.

pub fn post_v1_billing_meter_event_adjustments_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/meter_event_adjustments",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meter_event_adjustments
/// Create a billing meter event adjustment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_meter_event_adjustments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_meter_event_adjustments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meter_event_adjustments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter_event_adjustment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter_event_adjustment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/meter_event_adjustments
/// Create a billing meter event adjustment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_meter_event_adjustments_builder()` + `post_v1_billing_meter_event_adjustments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meter_event_adjustments(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingMeter_event_adjustment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_meter_event_adjustments_builder(client)?;
    post_v1_billing_meter_event_adjustments_execute(builder)
}

/// POST /v1/billing/meter_events
/// Create a billing meter event
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_meter_events_execute()` to send, or `post_v1_billing_meter_events` for simplest API.

pub fn post_v1_billing_meter_events_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/meter_events",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meter_events
/// Create a billing meter event
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_meter_events_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_meter_events_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meter_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter_event>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter_event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/meter_events
/// Create a billing meter event
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_meter_events_builder()` + `post_v1_billing_meter_events_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meter_events(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter_event>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_meter_events_builder(client)?;
    post_v1_billing_meter_events_execute(builder)
}

/// GET /v1/billing/meters
/// List billing meters
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_meters_execute()` to send, or `get_v1_billing_meters` for simplest API.

pub fn get_v1_billing_meters_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/meters",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/meters
/// List billing meters
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_meters_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_meters_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_meters_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_meters`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingMetersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/billing/meters
/// List billing meters
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_meters_builder()` + `get_v1_billing_meters_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_meters(
    client: &SimpleHttpClient,
    args: &GetV1BillingMetersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_billing_meters_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_billing_meters_execute(builder)
}

/// POST /v1/billing/meters
/// Create a billing meter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_meters_execute()` to send, or `post_v1_billing_meters` for simplest API.

pub fn post_v1_billing_meters_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/meters",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meters
/// Create a billing meter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_meters_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_meters_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing/meters
/// Create a billing meter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_meters_builder()` + `post_v1_billing_meters_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_meters_builder(client)?;
    post_v1_billing_meters_execute(builder)
}

/// GET /v1/billing/meters/{id}
/// Retrieve a billing meter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_meters_id_execute()` to send, or `get_v1_billing_meters_id` for simplest API.

pub fn get_v1_billing_meters_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/meters/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/meters/{id}
/// Retrieve a billing meter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_meters_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_meters_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_meters_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_meters_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingMetersIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/billing/meters/{id}
/// Retrieve a billing meter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_meters_id_builder()` + `get_v1_billing_meters_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_meters_id(
    client: &SimpleHttpClient,
    args: &GetV1BillingMetersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_billing_meters_id_builder(client, &args.id, args.expand)?;
    get_v1_billing_meters_id_execute(builder)
}

/// POST /v1/billing/meters/{id}
/// Update a billing meter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_meters_id_execute()` to send, or `post_v1_billing_meters_id` for simplest API.

pub fn post_v1_billing_meters_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing/meters/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meters/{id}
/// Update a billing meter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_meters_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_meters_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_meters_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingMetersIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/meters/{id}
/// Update a billing meter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_meters_id_builder()` + `post_v1_billing_meters_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_id(
    client: &SimpleHttpClient,
    args: &PostV1BillingMetersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_meters_id_builder(client, &args.id)?;
    post_v1_billing_meters_id_execute(builder)
}

/// POST /v1/billing/meters/{id}/deactivate
/// Deactivate a billing meter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_meters_id_deactivate_execute()` to send, or `post_v1_billing_meters_id_deactivate` for simplest API.

pub fn post_v1_billing_meters_id_deactivate_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/meters/{}/deactivate",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meters/{id}/deactivate
/// Deactivate a billing meter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_meters_id_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_meters_id_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_id_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_meters_id_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingMetersIdDeactivateArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/meters/{id}/deactivate
/// Deactivate a billing meter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_meters_id_deactivate_builder()` + `post_v1_billing_meters_id_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_id_deactivate(
    client: &SimpleHttpClient,
    args: &PostV1BillingMetersIdDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_meters_id_deactivate_builder(client, &args.id)?;
    post_v1_billing_meters_id_deactivate_execute(builder)
}

/// GET /v1/billing/meters/{id}/event_summaries
/// List billing meter event summaries
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_meters_id_event_summaries_execute()` to send, or `get_v1_billing_meters_id_event_summaries` for simplest API.

pub fn get_v1_billing_meters_id_event_summaries_builder(
    client: &SimpleHttpClient,
    id: &str,
    customer: Option<&str>,
    end_time: Option<i32>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    start_time: Option<i32>,
    starting_after: Option<&str>,
    value_grouping_window: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/meters/{}/event_summaries",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = end_time {
        query_parts.push(format!("end_time={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = start_time {
        query_parts.push(format!("start_time={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = value_grouping_window {
        query_parts.push(format!("value_grouping_window={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing/meters/{id}/event_summaries
/// List billing meter event summaries
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_meters_id_event_summaries_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_meters_id_event_summaries_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_meters_id_event_summaries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_meters_id_event_summaries`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingMetersIdEventSummariesArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: end_time
    pub end_time: Option<i32>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: start_time
    pub start_time: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: value_grouping_window
    pub value_grouping_window: Option<String>,
}

/// GET /v1/billing/meters/{id}/event_summaries
/// List billing meter event summaries
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_meters_id_event_summaries_builder()` + `get_v1_billing_meters_id_event_summaries_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_meters_id_event_summaries(
    client: &SimpleHttpClient,
    args: &GetV1BillingMetersIdEventSummariesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_billing_meters_id_event_summaries_builder(
        client,
        &args.id,
        args.customer.as_deref(),
        args.end_time,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.start_time,
        args.starting_after.as_deref(),
        args.value_grouping_window.as_deref(),
    )?;
    get_v1_billing_meters_id_event_summaries_execute(builder)
}

/// POST /v1/billing/meters/{id}/reactivate
/// Reactivate a billing meter
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_meters_id_reactivate_execute()` to send, or `post_v1_billing_meters_id_reactivate` for simplest API.

pub fn post_v1_billing_meters_id_reactivate_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing/meters/{}/reactivate",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing/meters/{id}/reactivate
/// Reactivate a billing meter
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_meters_id_reactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_meters_id_reactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_id_reactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingMeter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_meters_id_reactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingMetersIdReactivateArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/billing/meters/{id}/reactivate
/// Reactivate a billing meter
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_meters_id_reactivate_builder()` + `post_v1_billing_meters_id_reactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_meters_id_reactivate(
    client: &SimpleHttpClient,
    args: &PostV1BillingMetersIdReactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingMeter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_meters_id_reactivate_builder(client, &args.id)?;
    post_v1_billing_meters_id_reactivate_execute(builder)
}

/// GET /v1/billing_portal/configurations
/// List portal configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_portal_configurations_execute()` to send, or `get_v1_billing_portal_configurations` for simplest API.

pub fn get_v1_billing_portal_configurations_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    is_default: Option<bool>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing_portal/configurations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = is_default {
        query_parts.push(format!("is_default={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing_portal/configurations
/// List portal configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_portal_configurations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_portal_configurations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_portal_configurations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_portal_configurations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingPortalConfigurationsArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: is_default
    pub is_default: Option<bool>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/billing_portal/configurations
/// List portal configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_portal_configurations_builder()` + `get_v1_billing_portal_configurations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_portal_configurations(
    client: &SimpleHttpClient,
    args: &GetV1BillingPortalConfigurationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_billing_portal_configurations_builder(
        client,
        args.active,
        args.ending_before.as_deref(),
        args.expand,
        args.is_default,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_billing_portal_configurations_execute(builder)
}

/// POST /v1/billing_portal/configurations
/// Create a portal configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_portal_configurations_execute()` to send, or `post_v1_billing_portal_configurations` for simplest API.

pub fn post_v1_billing_portal_configurations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing_portal/configurations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing_portal/configurations
/// Create a portal configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_portal_configurations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_portal_configurations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_portal_configurations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Billing_portalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Billing_portalConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing_portal/configurations
/// Create a portal configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_portal_configurations_builder()` + `post_v1_billing_portal_configurations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_portal_configurations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Billing_portalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_portal_configurations_builder(client)?;
    post_v1_billing_portal_configurations_execute(builder)
}

/// GET /v1/billing_portal/configurations/{configuration}
/// Retrieve a portal configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_billing_portal_configurations_configuration_execute()` to send, or `get_v1_billing_portal_configurations_configuration` for simplest API.

pub fn get_v1_billing_portal_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing_portal/configurations/{}",
        configuration,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/billing_portal/configurations/{configuration}
/// Retrieve a portal configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_billing_portal_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_billing_portal_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_portal_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Billing_portalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Billing_portalConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_billing_portal_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1BillingPortalConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/billing_portal/configurations/{configuration}
/// Retrieve a portal configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_billing_portal_configurations_configuration_builder()` + `get_v1_billing_portal_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_billing_portal_configurations_configuration(
    client: &SimpleHttpClient,
    args: &GetV1BillingPortalConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Billing_portalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_billing_portal_configurations_configuration_builder(
        client,
        &args.configuration,
        args.expand,
    )?;
    get_v1_billing_portal_configurations_configuration_execute(builder)
}

/// POST /v1/billing_portal/configurations/{configuration}
/// Update a portal configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_portal_configurations_configuration_execute()` to send, or `post_v1_billing_portal_configurations_configuration` for simplest API.

pub fn post_v1_billing_portal_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/billing_portal/configurations/{}",
        configuration,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing_portal/configurations/{configuration}
/// Update a portal configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_portal_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_portal_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_portal_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Billing_portalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Billing_portalConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_billing_portal_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1BillingPortalConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
}

/// POST /v1/billing_portal/configurations/{configuration}
/// Update a portal configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_portal_configurations_configuration_builder()` + `post_v1_billing_portal_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_portal_configurations_configuration(
    client: &SimpleHttpClient,
    args: &PostV1BillingPortalConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Billing_portalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_billing_portal_configurations_configuration_builder(client, &args.configuration)?;
    post_v1_billing_portal_configurations_configuration_execute(builder)
}

/// POST /v1/billing_portal/sessions
/// Create a portal session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_billing_portal_sessions_execute()` to send, or `post_v1_billing_portal_sessions` for simplest API.

pub fn post_v1_billing_portal_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/billing_portal/sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/billing_portal/sessions
/// Create a portal session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_billing_portal_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_billing_portal_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_portal_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Billing_portalSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Billing_portalSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/billing_portal/sessions
/// Create a portal session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_billing_portal_sessions_builder()` + `post_v1_billing_portal_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_billing_portal_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Billing_portalSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_billing_portal_sessions_builder(client)?;
    post_v1_billing_portal_sessions_execute(builder)
}

/// GET /v1/charges
/// List all charges
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_charges_execute()` to send, or `get_v1_charges` for simplest API.

pub fn get_v1_charges_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    customer: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_intent: Option<&str>,
    starting_after: Option<&str>,
    transfer_group: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_intent {
        query_parts.push(format!("payment_intent={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = transfer_group {
        query_parts.push(format!("transfer_group={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges
/// List all charges
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_charges_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_charges_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_charges`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ChargesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: transfer_group
    pub transfer_group: Option<String>,
}

/// GET /v1/charges
/// List all charges
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_charges_builder()` + `get_v1_charges_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges(
    client: &SimpleHttpClient,
    args: &GetV1ChargesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_charges_builder(
        client,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payment_intent.as_deref(),
        args.starting_after.as_deref(),
        args.transfer_group.as_deref(),
    )?;
    get_v1_charges_execute(builder)
}

/// POST /v1/charges
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_execute()` to send, or `post_v1_charges` for simplest API.

pub fn post_v1_charges_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Charge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/charges
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_builder()` + `post_v1_charges_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_builder(client)?;
    post_v1_charges_execute(builder)
}

/// GET /v1/charges/search
/// Search charges
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_charges_search_execute()` to send, or `get_v1_charges_search` for simplest API.

pub fn get_v1_charges_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/search
/// Search charges
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_charges_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_charges_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_charges_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ChargesSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/charges/search
/// Search charges
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_charges_search_builder()` + `get_v1_charges_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_search(
    client: &SimpleHttpClient,
    args: &GetV1ChargesSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_charges_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_charges_search_execute(builder)
}

/// GET /v1/charges/{charge}
/// Retrieve a charge
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_charges_charge_execute()` to send, or `get_v1_charges_charge` for simplest API.

pub fn get_v1_charges_charge_builder(
    client: &SimpleHttpClient,
    charge: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}", charge,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge}
/// Retrieve a charge
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_charges_charge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_charges_charge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Charge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_charges_charge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ChargesChargeArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/charges/{charge}
/// Retrieve a charge
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_charges_charge_builder()` + `get_v1_charges_charge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge(
    client: &SimpleHttpClient,
    args: &GetV1ChargesChargeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_charges_charge_builder(client, &args.charge, args.expand)?;
    get_v1_charges_charge_execute(builder)
}

/// POST /v1/charges/{charge}
/// Update a charge
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_execute()` to send, or `post_v1_charges_charge` for simplest API.

pub fn post_v1_charges_charge_builder(
    client: &SimpleHttpClient,
    charge: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}", charge,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}
/// Update a charge
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Charge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// POST /v1/charges/{charge}
/// Update a charge
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_builder()` + `post_v1_charges_charge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_charge_builder(client, &args.charge)?;
    post_v1_charges_charge_execute(builder)
}

/// POST /v1/charges/{charge}/capture
/// Capture a payment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_capture_execute()` to send, or `post_v1_charges_charge_capture` for simplest API.

pub fn post_v1_charges_charge_capture_builder(
    client: &SimpleHttpClient,
    charge: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}/capture", charge,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/capture
/// Capture a payment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_capture_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_capture_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Charge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge_capture`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeCaptureArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// POST /v1/charges/{charge}/capture
/// Capture a payment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_capture_builder()` + `post_v1_charges_charge_capture_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_capture(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeCaptureArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_charge_capture_builder(client, &args.charge)?;
    post_v1_charges_charge_capture_execute(builder)
}

/// GET /v1/charges/{charge}/dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_charges_charge_dispute_execute()` to send, or `get_v1_charges_charge_dispute` for simplest API.

pub fn get_v1_charges_charge_dispute_builder(
    client: &SimpleHttpClient,
    charge: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}/dispute", charge,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge}/dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_charges_charge_dispute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_charges_charge_dispute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_dispute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_charges_charge_dispute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ChargesChargeDisputeArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/charges/{charge}/dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_charges_charge_dispute_builder()` + `get_v1_charges_charge_dispute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_dispute(
    client: &SimpleHttpClient,
    args: &GetV1ChargesChargeDisputeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_charges_charge_dispute_builder(client, &args.charge, args.expand)?;
    get_v1_charges_charge_dispute_execute(builder)
}

/// POST /v1/charges/{charge}/dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_dispute_execute()` to send, or `post_v1_charges_charge_dispute` for simplest API.

pub fn post_v1_charges_charge_dispute_builder(
    client: &SimpleHttpClient,
    charge: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}/dispute", charge,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_dispute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_dispute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_dispute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge_dispute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeDisputeArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// POST /v1/charges/{charge}/dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_dispute_builder()` + `post_v1_charges_charge_dispute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_dispute(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeDisputeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_charge_dispute_builder(client, &args.charge)?;
    post_v1_charges_charge_dispute_execute(builder)
}

/// POST /v1/charges/{charge}/dispute/close
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_dispute_close_execute()` to send, or `post_v1_charges_charge_dispute_close` for simplest API.

pub fn post_v1_charges_charge_dispute_close_builder(
    client: &SimpleHttpClient,
    charge: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/charges/{}/dispute/close",
        charge,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/dispute/close
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_dispute_close_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_dispute_close_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_dispute_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge_dispute_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeDisputeCloseArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// POST /v1/charges/{charge}/dispute/close
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_dispute_close_builder()` + `post_v1_charges_charge_dispute_close_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_dispute_close(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeDisputeCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_charge_dispute_close_builder(client, &args.charge)?;
    post_v1_charges_charge_dispute_close_execute(builder)
}

/// POST /v1/charges/{charge}/refund
/// Create a refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_refund_execute()` to send, or `post_v1_charges_charge_refund` for simplest API.

pub fn post_v1_charges_charge_refund_builder(
    client: &SimpleHttpClient,
    charge: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}/refund", charge,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/refund
/// Create a refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Charge = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeRefundArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// POST /v1/charges/{charge}/refund
/// Create a refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_refund_builder()` + `post_v1_charges_charge_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_refund(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Charge>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_charge_refund_builder(client, &args.charge)?;
    post_v1_charges_charge_refund_execute(builder)
}

/// GET /v1/charges/{charge}/refunds
/// List all refunds
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_charges_charge_refunds_execute()` to send, or `get_v1_charges_charge_refunds` for simplest API.

pub fn get_v1_charges_charge_refunds_builder(
    client: &SimpleHttpClient,
    charge: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}/refunds", charge,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge}/refunds
/// List all refunds
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_charges_charge_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_charges_charge_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_charges_charge_refunds`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ChargesChargeRefundsArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/charges/{charge}/refunds
/// List all refunds
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_charges_charge_refunds_builder()` + `get_v1_charges_charge_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_refunds(
    client: &SimpleHttpClient,
    args: &GetV1ChargesChargeRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_charges_charge_refunds_builder(
        client,
        &args.charge,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_charges_charge_refunds_execute(builder)
}

/// POST /v1/charges/{charge}/refunds
/// Create customer balance refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_refunds_execute()` to send, or `post_v1_charges_charge_refunds` for simplest API.

pub fn post_v1_charges_charge_refunds_builder(
    client: &SimpleHttpClient,
    charge: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/charges/{}/refunds", charge,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/refunds
/// Create customer balance refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge_refunds`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeRefundsArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// POST /v1/charges/{charge}/refunds
/// Create customer balance refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_refunds_builder()` + `post_v1_charges_charge_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_refunds(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_charges_charge_refunds_builder(client, &args.charge)?;
    post_v1_charges_charge_refunds_execute(builder)
}

/// GET /v1/charges/{charge}/refunds/{refund}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_charges_charge_refunds_refund_execute()` to send, or `get_v1_charges_charge_refunds_refund` for simplest API.

pub fn get_v1_charges_charge_refunds_refund_builder(
    client: &SimpleHttpClient,
    charge: &str,
    refund: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/charges/{}/refunds/{}",
        charge, refund,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge}/refunds/{refund}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_charges_charge_refunds_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_charges_charge_refunds_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_refunds_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_charges_charge_refunds_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ChargesChargeRefundsRefundArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Path parameter: refund
    pub refund: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/charges/{charge}/refunds/{refund}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_charges_charge_refunds_refund_builder()` + `get_v1_charges_charge_refunds_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_charges_charge_refunds_refund(
    client: &SimpleHttpClient,
    args: &GetV1ChargesChargeRefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_charges_charge_refunds_refund_builder(
        client,
        &args.charge,
        &args.refund,
        args.expand,
    )?;
    get_v1_charges_charge_refunds_refund_execute(builder)
}

/// POST /v1/charges/{charge}/refunds/{refund}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_charges_charge_refunds_refund_execute()` to send, or `post_v1_charges_charge_refunds_refund` for simplest API.

pub fn post_v1_charges_charge_refunds_refund_builder(
    client: &SimpleHttpClient,
    charge: &str,
    refund: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/charges/{}/refunds/{}",
        charge, refund,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/refunds/{refund}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_charges_charge_refunds_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_charges_charge_refunds_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_refunds_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_charges_charge_refunds_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ChargesChargeRefundsRefundArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Path parameter: refund
    pub refund: String,
}

/// POST /v1/charges/{charge}/refunds/{refund}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_charges_charge_refunds_refund_builder()` + `post_v1_charges_charge_refunds_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_charges_charge_refunds_refund(
    client: &SimpleHttpClient,
    args: &PostV1ChargesChargeRefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_charges_charge_refunds_refund_builder(client, &args.charge, &args.refund)?;
    post_v1_charges_charge_refunds_refund_execute(builder)
}

/// GET /v1/checkout/sessions
/// List all Checkout Sessions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_checkout_sessions_execute()` to send, or `get_v1_checkout_sessions` for simplest API.

pub fn get_v1_checkout_sessions_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    customer_details: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_intent: Option<&str>,
    payment_link: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
    subscription: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/checkout/sessions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = customer_details {
        query_parts.push(format!("customer_details={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_intent {
        query_parts.push(format!("payment_intent={}", val));
    }
    if let Some(val) = payment_link {
        query_parts.push(format!("payment_link={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = subscription {
        query_parts.push(format!("subscription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/checkout/sessions
/// List all Checkout Sessions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_checkout_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_checkout_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_checkout_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_checkout_sessions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CheckoutSessionsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: customer_details
    pub customer_details: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: payment_link
    pub payment_link: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: subscription
    pub subscription: Option<String>,
}

/// GET /v1/checkout/sessions
/// List all Checkout Sessions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_checkout_sessions_builder()` + `get_v1_checkout_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_checkout_sessions(
    client: &SimpleHttpClient,
    args: &GetV1CheckoutSessionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_checkout_sessions_builder(
        client,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.customer_details.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payment_intent.as_deref(),
        args.payment_link.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.subscription.as_deref(),
    )?;
    get_v1_checkout_sessions_execute(builder)
}

/// POST /v1/checkout/sessions
/// Create a Checkout Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_checkout_sessions_execute()` to send, or `post_v1_checkout_sessions` for simplest API.

pub fn post_v1_checkout_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/checkout/sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/checkout/sessions
/// Create a Checkout Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_checkout_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_checkout_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_checkout_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckoutSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/checkout/sessions
/// Create a Checkout Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_checkout_sessions_builder()` + `post_v1_checkout_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_checkout_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_checkout_sessions_builder(client)?;
    post_v1_checkout_sessions_execute(builder)
}

/// GET /v1/checkout/sessions/{session}
/// Retrieve a Checkout Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_checkout_sessions_session_execute()` to send, or `get_v1_checkout_sessions_session` for simplest API.

pub fn get_v1_checkout_sessions_session_builder(
    client: &SimpleHttpClient,
    session: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/checkout/sessions/{}", session,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/checkout/sessions/{session}
/// Retrieve a Checkout Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_checkout_sessions_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_checkout_sessions_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_checkout_sessions_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckoutSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_checkout_sessions_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CheckoutSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/checkout/sessions/{session}
/// Retrieve a Checkout Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_checkout_sessions_session_builder()` + `get_v1_checkout_sessions_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_checkout_sessions_session(
    client: &SimpleHttpClient,
    args: &GetV1CheckoutSessionsSessionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_checkout_sessions_session_builder(client, &args.session, args.expand)?;
    get_v1_checkout_sessions_session_execute(builder)
}

/// POST /v1/checkout/sessions/{session}
/// Update a Checkout Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_checkout_sessions_session_execute()` to send, or `post_v1_checkout_sessions_session` for simplest API.

pub fn post_v1_checkout_sessions_session_builder(
    client: &SimpleHttpClient,
    session: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/checkout/sessions/{}", session,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/checkout/sessions/{session}
/// Update a Checkout Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_checkout_sessions_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_checkout_sessions_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_checkout_sessions_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckoutSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_checkout_sessions_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CheckoutSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST /v1/checkout/sessions/{session}
/// Update a Checkout Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_checkout_sessions_session_builder()` + `post_v1_checkout_sessions_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_checkout_sessions_session(
    client: &SimpleHttpClient,
    args: &PostV1CheckoutSessionsSessionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_checkout_sessions_session_builder(client, &args.session)?;
    post_v1_checkout_sessions_session_execute(builder)
}

/// POST /v1/checkout/sessions/{session}/expire
/// Expire a Checkout Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_checkout_sessions_session_expire_execute()` to send, or `post_v1_checkout_sessions_session_expire` for simplest API.

pub fn post_v1_checkout_sessions_session_expire_builder(
    client: &SimpleHttpClient,
    session: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/checkout/sessions/{}/expire",
        session,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/checkout/sessions/{session}/expire
/// Expire a Checkout Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_checkout_sessions_session_expire_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_checkout_sessions_session_expire_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_checkout_sessions_session_expire_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckoutSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_checkout_sessions_session_expire`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CheckoutSessionsSessionExpireArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST /v1/checkout/sessions/{session}/expire
/// Expire a Checkout Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_checkout_sessions_session_expire_builder()` + `post_v1_checkout_sessions_session_expire_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_checkout_sessions_session_expire(
    client: &SimpleHttpClient,
    args: &PostV1CheckoutSessionsSessionExpireArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckoutSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_checkout_sessions_session_expire_builder(client, &args.session)?;
    post_v1_checkout_sessions_session_expire_execute(builder)
}

/// GET /v1/checkout/sessions/{session}/line_items
/// Retrieve a Checkout Session's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_checkout_sessions_session_line_items_execute()` to send, or `get_v1_checkout_sessions_session_line_items` for simplest API.

pub fn get_v1_checkout_sessions_session_line_items_builder(
    client: &SimpleHttpClient,
    session: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/checkout/sessions/{}/line_items",
        session,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/checkout/sessions/{session}/line_items
/// Retrieve a Checkout Session's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_checkout_sessions_session_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_checkout_sessions_session_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_checkout_sessions_session_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_checkout_sessions_session_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CheckoutSessionsSessionLineItemsArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/checkout/sessions/{session}/line_items
/// Retrieve a Checkout Session's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_checkout_sessions_session_line_items_builder()` + `get_v1_checkout_sessions_session_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_checkout_sessions_session_line_items(
    client: &SimpleHttpClient,
    args: &GetV1CheckoutSessionsSessionLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_checkout_sessions_session_line_items_builder(
        client,
        &args.session,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_checkout_sessions_session_line_items_execute(builder)
}

/// GET /v1/climate/orders
/// List orders
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_climate_orders_execute()` to send, or `get_v1_climate_orders` for simplest API.

pub fn get_v1_climate_orders_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/orders",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/orders
/// List orders
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_climate_orders_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_climate_orders_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_orders_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_climate_orders`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ClimateOrdersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/climate/orders
/// List orders
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_climate_orders_builder()` + `get_v1_climate_orders_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_orders(
    client: &SimpleHttpClient,
    args: &GetV1ClimateOrdersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_climate_orders_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_climate_orders_execute(builder)
}

/// POST /v1/climate/orders
/// Create an order
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_climate_orders_execute()` to send, or `post_v1_climate_orders` for simplest API.

pub fn post_v1_climate_orders_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/orders",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/climate/orders
/// Create an order
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_climate_orders_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_climate_orders_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_climate_orders_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/climate/orders
/// Create an order
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_climate_orders_builder()` + `post_v1_climate_orders_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_climate_orders(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_climate_orders_builder(client)?;
    post_v1_climate_orders_execute(builder)
}

/// GET /v1/climate/orders/{order}
/// Retrieve an order
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_climate_orders_order_execute()` to send, or `get_v1_climate_orders_order` for simplest API.

pub fn get_v1_climate_orders_order_builder(
    client: &SimpleHttpClient,
    order: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/orders/{}", order,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/orders/{order}
/// Retrieve an order
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_climate_orders_order_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_climate_orders_order_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_orders_order_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_climate_orders_order`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ClimateOrdersOrderArgs {
    /// Path parameter: order
    pub order: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/climate/orders/{order}
/// Retrieve an order
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_climate_orders_order_builder()` + `get_v1_climate_orders_order_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_orders_order(
    client: &SimpleHttpClient,
    args: &GetV1ClimateOrdersOrderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_climate_orders_order_builder(client, &args.order, args.expand)?;
    get_v1_climate_orders_order_execute(builder)
}

/// POST /v1/climate/orders/{order}
/// Update an order
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_climate_orders_order_execute()` to send, or `post_v1_climate_orders_order` for simplest API.

pub fn post_v1_climate_orders_order_builder(
    client: &SimpleHttpClient,
    order: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/orders/{}", order,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/climate/orders/{order}
/// Update an order
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_climate_orders_order_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_climate_orders_order_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_climate_orders_order_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_climate_orders_order`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ClimateOrdersOrderArgs {
    /// Path parameter: order
    pub order: String,
}

/// POST /v1/climate/orders/{order}
/// Update an order
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_climate_orders_order_builder()` + `post_v1_climate_orders_order_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_climate_orders_order(
    client: &SimpleHttpClient,
    args: &PostV1ClimateOrdersOrderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_climate_orders_order_builder(client, &args.order)?;
    post_v1_climate_orders_order_execute(builder)
}

/// POST /v1/climate/orders/{order}/cancel
/// Cancel an order
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_climate_orders_order_cancel_execute()` to send, or `post_v1_climate_orders_order_cancel` for simplest API.

pub fn post_v1_climate_orders_order_cancel_builder(
    client: &SimpleHttpClient,
    order: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/orders/{}/cancel", order,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/climate/orders/{order}/cancel
/// Cancel an order
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_climate_orders_order_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_climate_orders_order_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_climate_orders_order_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_climate_orders_order_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ClimateOrdersOrderCancelArgs {
    /// Path parameter: order
    pub order: String,
}

/// POST /v1/climate/orders/{order}/cancel
/// Cancel an order
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_climate_orders_order_cancel_builder()` + `post_v1_climate_orders_order_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_climate_orders_order_cancel(
    client: &SimpleHttpClient,
    args: &PostV1ClimateOrdersOrderCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_climate_orders_order_cancel_builder(client, &args.order)?;
    post_v1_climate_orders_order_cancel_execute(builder)
}

/// GET /v1/climate/products
/// List products
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_climate_products_execute()` to send, or `get_v1_climate_products` for simplest API.

pub fn get_v1_climate_products_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/products",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/products
/// List products
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_climate_products_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_climate_products_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_products_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_climate_products`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ClimateProductsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/climate/products
/// List products
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_climate_products_builder()` + `get_v1_climate_products_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_products(
    client: &SimpleHttpClient,
    args: &GetV1ClimateProductsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_climate_products_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_climate_products_execute(builder)
}

/// GET /v1/climate/products/{product}
/// Retrieve a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_climate_products_product_execute()` to send, or `get_v1_climate_products_product` for simplest API.

pub fn get_v1_climate_products_product_builder(
    client: &SimpleHttpClient,
    product: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/products/{}", product,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/products/{product}
/// Retrieve a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_climate_products_product_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_climate_products_product_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_products_product_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClimateProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_climate_products_product`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ClimateProductsProductArgs {
    /// Path parameter: product
    pub product: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/climate/products/{product}
/// Retrieve a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_climate_products_product_builder()` + `get_v1_climate_products_product_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_products_product(
    client: &SimpleHttpClient,
    args: &GetV1ClimateProductsProductArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_climate_products_product_builder(client, &args.product, args.expand)?;
    get_v1_climate_products_product_execute(builder)
}

/// GET /v1/climate/suppliers
/// List suppliers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_climate_suppliers_execute()` to send, or `get_v1_climate_suppliers` for simplest API.

pub fn get_v1_climate_suppliers_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/suppliers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/suppliers
/// List suppliers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_climate_suppliers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_climate_suppliers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_suppliers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_climate_suppliers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ClimateSuppliersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/climate/suppliers
/// List suppliers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_climate_suppliers_builder()` + `get_v1_climate_suppliers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_suppliers(
    client: &SimpleHttpClient,
    args: &GetV1ClimateSuppliersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_climate_suppliers_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_climate_suppliers_execute(builder)
}

/// GET /v1/climate/suppliers/{supplier}
/// Retrieve a supplier
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_climate_suppliers_supplier_execute()` to send, or `get_v1_climate_suppliers_supplier` for simplest API.

pub fn get_v1_climate_suppliers_supplier_builder(
    client: &SimpleHttpClient,
    supplier: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/climate/suppliers/{}", supplier,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/climate/suppliers/{supplier}
/// Retrieve a supplier
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_climate_suppliers_supplier_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_climate_suppliers_supplier_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_suppliers_supplier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateSupplier>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClimateSupplier = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_climate_suppliers_supplier`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ClimateSuppliersSupplierArgs {
    /// Path parameter: supplier
    pub supplier: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/climate/suppliers/{supplier}
/// Retrieve a supplier
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_climate_suppliers_supplier_builder()` + `get_v1_climate_suppliers_supplier_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_climate_suppliers_supplier(
    client: &SimpleHttpClient,
    args: &GetV1ClimateSuppliersSupplierArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClimateSupplier>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_climate_suppliers_supplier_builder(client, &args.supplier, args.expand)?;
    get_v1_climate_suppliers_supplier_execute(builder)
}

/// GET /v1/confirmation_tokens/{confirmation_token}
/// Retrieve a ConfirmationToken
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_confirmation_tokens_confirmation_token_execute()` to send, or `get_v1_confirmation_tokens_confirmation_token` for simplest API.

pub fn get_v1_confirmation_tokens_confirmation_token_builder(
    client: &SimpleHttpClient,
    confirmation_token: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/confirmation_tokens/{}",
        confirmation_token,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/confirmation_tokens/{confirmation_token}
/// Retrieve a ConfirmationToken
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_confirmation_tokens_confirmation_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_confirmation_tokens_confirmation_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_confirmation_tokens_confirmation_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Confirmation_token>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Confirmation_token = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_confirmation_tokens_confirmation_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ConfirmationTokensConfirmationTokenArgs {
    /// Path parameter: confirmation_token
    pub confirmation_token: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/confirmation_tokens/{confirmation_token}
/// Retrieve a ConfirmationToken
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_confirmation_tokens_confirmation_token_builder()` + `get_v1_confirmation_tokens_confirmation_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_confirmation_tokens_confirmation_token(
    client: &SimpleHttpClient,
    args: &GetV1ConfirmationTokensConfirmationTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Confirmation_token>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_confirmation_tokens_confirmation_token_builder(
        client,
        &args.confirmation_token,
        args.expand,
    )?;
    get_v1_confirmation_tokens_confirmation_token_execute(builder)
}

/// GET /v1/country_specs
/// List Country Specs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_country_specs_execute()` to send, or `get_v1_country_specs` for simplest API.

pub fn get_v1_country_specs_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/country_specs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/country_specs
/// List Country Specs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_country_specs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_country_specs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_country_specs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_country_specs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CountrySpecsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/country_specs
/// List Country Specs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_country_specs_builder()` + `get_v1_country_specs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_country_specs(
    client: &SimpleHttpClient,
    args: &GetV1CountrySpecsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_country_specs_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_country_specs_execute(builder)
}

/// GET /v1/country_specs/{country}
/// Retrieve a Country Spec
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_country_specs_country_execute()` to send, or `get_v1_country_specs_country` for simplest API.

pub fn get_v1_country_specs_country_builder(
    client: &SimpleHttpClient,
    country: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/country_specs/{}", country,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/country_specs/{country}
/// Retrieve a Country Spec
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_country_specs_country_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_country_specs_country_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_country_specs_country_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Country_spec>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Country_spec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_country_specs_country`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CountrySpecsCountryArgs {
    /// Path parameter: country
    pub country: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/country_specs/{country}
/// Retrieve a Country Spec
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_country_specs_country_builder()` + `get_v1_country_specs_country_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_country_specs_country(
    client: &SimpleHttpClient,
    args: &GetV1CountrySpecsCountryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Country_spec>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_country_specs_country_builder(client, &args.country, args.expand)?;
    get_v1_country_specs_country_execute(builder)
}

/// GET /v1/coupons
/// List all coupons
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_coupons_execute()` to send, or `get_v1_coupons` for simplest API.

pub fn get_v1_coupons_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/coupons",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/coupons
/// List all coupons
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_coupons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_coupons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_coupons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_coupons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CouponsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/coupons
/// List all coupons
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_coupons_builder()` + `get_v1_coupons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_coupons(
    client: &SimpleHttpClient,
    args: &GetV1CouponsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_coupons_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_coupons_execute(builder)
}

/// POST /v1/coupons
/// Create a coupon
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_coupons_execute()` to send, or `post_v1_coupons` for simplest API.

pub fn post_v1_coupons_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/coupons",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/coupons
/// Create a coupon
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_coupons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_coupons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_coupons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Coupon = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/coupons
/// Create a coupon
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_coupons_builder()` + `post_v1_coupons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_coupons(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_coupons_builder(client)?;
    post_v1_coupons_execute(builder)
}

/// GET /v1/coupons/{coupon}
/// Retrieve a coupon
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_coupons_coupon_execute()` to send, or `get_v1_coupons_coupon` for simplest API.

pub fn get_v1_coupons_coupon_builder(
    client: &SimpleHttpClient,
    coupon: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/coupons/{}", coupon,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/coupons/{coupon}
/// Retrieve a coupon
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_coupons_coupon_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_coupons_coupon_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_coupons_coupon_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Coupon = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_coupons_coupon`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CouponsCouponArgs {
    /// Path parameter: coupon
    pub coupon: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/coupons/{coupon}
/// Retrieve a coupon
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_coupons_coupon_builder()` + `get_v1_coupons_coupon_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_coupons_coupon(
    client: &SimpleHttpClient,
    args: &GetV1CouponsCouponArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_coupons_coupon_builder(client, &args.coupon, args.expand)?;
    get_v1_coupons_coupon_execute(builder)
}

/// POST /v1/coupons/{coupon}
/// Update a coupon
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_coupons_coupon_execute()` to send, or `post_v1_coupons_coupon` for simplest API.

pub fn post_v1_coupons_coupon_builder(
    client: &SimpleHttpClient,
    coupon: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/coupons/{}", coupon,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/coupons/{coupon}
/// Update a coupon
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_coupons_coupon_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_coupons_coupon_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_coupons_coupon_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Coupon = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_coupons_coupon`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CouponsCouponArgs {
    /// Path parameter: coupon
    pub coupon: String,
}

/// POST /v1/coupons/{coupon}
/// Update a coupon
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_coupons_coupon_builder()` + `post_v1_coupons_coupon_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_coupons_coupon(
    client: &SimpleHttpClient,
    args: &PostV1CouponsCouponArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Coupon>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_coupons_coupon_builder(client, &args.coupon)?;
    post_v1_coupons_coupon_execute(builder)
}

/// DELETE /v1/coupons/{coupon}
/// Delete a coupon
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_coupons_coupon_execute()` to send, or `delete_v1_coupons_coupon` for simplest API.

pub fn delete_v1_coupons_coupon_builder(
    client: &SimpleHttpClient,
    coupon: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/coupons/{}", coupon,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/coupons/{coupon}
/// Delete a coupon
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_coupons_coupon_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_coupons_coupon_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_coupons_coupon_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_coupon>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_coupon = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_coupons_coupon`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CouponsCouponArgs {
    /// Path parameter: coupon
    pub coupon: String,
}

/// DELETE /v1/coupons/{coupon}
/// Delete a coupon
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_coupons_coupon_builder()` + `delete_v1_coupons_coupon_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_coupons_coupon(
    client: &SimpleHttpClient,
    args: &DeleteV1CouponsCouponArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_coupon>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_coupons_coupon_builder(client, &args.coupon)?;
    delete_v1_coupons_coupon_execute(builder)
}

/// GET /v1/credit_notes
/// List all credit notes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_credit_notes_execute()` to send, or `get_v1_credit_notes` for simplest API.

pub fn get_v1_credit_notes_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    invoice: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = invoice {
        query_parts.push(format!("invoice={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes
/// List all credit notes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_credit_notes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_credit_notes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_credit_notes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CreditNotesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/credit_notes
/// List all credit notes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_credit_notes_builder()` + `get_v1_credit_notes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes(
    client: &SimpleHttpClient,
    args: &GetV1CreditNotesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_credit_notes_builder(
        client,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.invoice.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_credit_notes_execute(builder)
}

/// POST /v1/credit_notes
/// Create a credit note
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_credit_notes_execute()` to send, or `post_v1_credit_notes` for simplest API.

pub fn post_v1_credit_notes_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/credit_notes
/// Create a credit note
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_credit_notes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_credit_notes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_credit_notes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Credit_note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/credit_notes
/// Create a credit note
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_credit_notes_builder()` + `post_v1_credit_notes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_credit_notes(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_credit_notes_builder(client)?;
    post_v1_credit_notes_execute(builder)
}

/// GET /v1/credit_notes/preview
/// Preview a credit note
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_credit_notes_preview_execute()` to send, or `get_v1_credit_notes_preview` for simplest API.

pub fn get_v1_credit_notes_preview_builder(
    client: &SimpleHttpClient,
    amount: Option<i32>,
    credit_amount: Option<i32>,
    effective_at: Option<i32>,
    email_type: Option<&str>,
    expand: Option<Vec<String>>,
    invoice: Option<&str>,
    lines: Option<Vec<String>>,
    memo: Option<&str>,
    metadata: Option<&str>,
    out_of_band_amount: Option<i32>,
    reason: Option<&str>,
    refund_amount: Option<i32>,
    refunds: Option<Vec<String>>,
    shipping_cost: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes/preview",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = amount {
        query_parts.push(format!("amount={}", val));
    }
    if let Some(val) = credit_amount {
        query_parts.push(format!("credit_amount={}", val));
    }
    if let Some(val) = effective_at {
        query_parts.push(format!("effective_at={}", val));
    }
    if let Some(val) = email_type {
        query_parts.push(format!("email_type={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = invoice {
        query_parts.push(format!("invoice={}", val));
    }
    if let Some(val) = lines {
        query_parts.push(format!("lines={}", val));
    }
    if let Some(val) = memo {
        query_parts.push(format!("memo={}", val));
    }
    if let Some(val) = metadata {
        query_parts.push(format!("metadata={}", val));
    }
    if let Some(val) = out_of_band_amount {
        query_parts.push(format!("out_of_band_amount={}", val));
    }
    if let Some(val) = reason {
        query_parts.push(format!("reason={}", val));
    }
    if let Some(val) = refund_amount {
        query_parts.push(format!("refund_amount={}", val));
    }
    if let Some(val) = refunds {
        query_parts.push(format!("refunds={}", val));
    }
    if let Some(val) = shipping_cost {
        query_parts.push(format!("shipping_cost={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/preview
/// Preview a credit note
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_credit_notes_preview_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_credit_notes_preview_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_preview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Credit_note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_credit_notes_preview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CreditNotesPreviewArgs {
    /// Query parameter: amount
    pub amount: Option<i32>,
    /// Query parameter: credit_amount
    pub credit_amount: Option<i32>,
    /// Query parameter: effective_at
    pub effective_at: Option<i32>,
    /// Query parameter: email_type
    pub email_type: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: lines
    pub lines: Option<Vec<String>>,
    /// Query parameter: memo
    pub memo: Option<String>,
    /// Query parameter: metadata
    pub metadata: Option<String>,
    /// Query parameter: out_of_band_amount
    pub out_of_band_amount: Option<i32>,
    /// Query parameter: reason
    pub reason: Option<String>,
    /// Query parameter: refund_amount
    pub refund_amount: Option<i32>,
    /// Query parameter: refunds
    pub refunds: Option<Vec<String>>,
    /// Query parameter: shipping_cost
    pub shipping_cost: Option<String>,
}

/// GET /v1/credit_notes/preview
/// Preview a credit note
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_credit_notes_preview_builder()` + `get_v1_credit_notes_preview_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_preview(
    client: &SimpleHttpClient,
    args: &GetV1CreditNotesPreviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_credit_notes_preview_builder(
        client,
        args.amount,
        args.credit_amount,
        args.effective_at,
        args.email_type.as_deref(),
        args.expand,
        args.invoice.as_deref(),
        args.lines,
        args.memo.as_deref(),
        args.metadata.as_deref(),
        args.out_of_band_amount,
        args.reason.as_deref(),
        args.refund_amount,
        args.refunds,
        args.shipping_cost.as_deref(),
    )?;
    get_v1_credit_notes_preview_execute(builder)
}

/// GET /v1/credit_notes/preview/lines
/// Retrieve a credit note preview's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_credit_notes_preview_lines_execute()` to send, or `get_v1_credit_notes_preview_lines` for simplest API.

pub fn get_v1_credit_notes_preview_lines_builder(
    client: &SimpleHttpClient,
    amount: Option<i32>,
    credit_amount: Option<i32>,
    effective_at: Option<i32>,
    email_type: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    invoice: Option<&str>,
    limit: Option<i32>,
    lines: Option<Vec<String>>,
    memo: Option<&str>,
    metadata: Option<&str>,
    out_of_band_amount: Option<i32>,
    reason: Option<&str>,
    refund_amount: Option<i32>,
    refunds: Option<Vec<String>>,
    shipping_cost: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes/preview/lines",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = amount {
        query_parts.push(format!("amount={}", val));
    }
    if let Some(val) = credit_amount {
        query_parts.push(format!("credit_amount={}", val));
    }
    if let Some(val) = effective_at {
        query_parts.push(format!("effective_at={}", val));
    }
    if let Some(val) = email_type {
        query_parts.push(format!("email_type={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = invoice {
        query_parts.push(format!("invoice={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = lines {
        query_parts.push(format!("lines={}", val));
    }
    if let Some(val) = memo {
        query_parts.push(format!("memo={}", val));
    }
    if let Some(val) = metadata {
        query_parts.push(format!("metadata={}", val));
    }
    if let Some(val) = out_of_band_amount {
        query_parts.push(format!("out_of_band_amount={}", val));
    }
    if let Some(val) = reason {
        query_parts.push(format!("reason={}", val));
    }
    if let Some(val) = refund_amount {
        query_parts.push(format!("refund_amount={}", val));
    }
    if let Some(val) = refunds {
        query_parts.push(format!("refunds={}", val));
    }
    if let Some(val) = shipping_cost {
        query_parts.push(format!("shipping_cost={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/preview/lines
/// Retrieve a credit note preview's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_credit_notes_preview_lines_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_credit_notes_preview_lines_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_preview_lines_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_credit_notes_preview_lines`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CreditNotesPreviewLinesArgs {
    /// Query parameter: amount
    pub amount: Option<i32>,
    /// Query parameter: credit_amount
    pub credit_amount: Option<i32>,
    /// Query parameter: effective_at
    pub effective_at: Option<i32>,
    /// Query parameter: email_type
    pub email_type: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: lines
    pub lines: Option<Vec<String>>,
    /// Query parameter: memo
    pub memo: Option<String>,
    /// Query parameter: metadata
    pub metadata: Option<String>,
    /// Query parameter: out_of_band_amount
    pub out_of_band_amount: Option<i32>,
    /// Query parameter: reason
    pub reason: Option<String>,
    /// Query parameter: refund_amount
    pub refund_amount: Option<i32>,
    /// Query parameter: refunds
    pub refunds: Option<Vec<String>>,
    /// Query parameter: shipping_cost
    pub shipping_cost: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/credit_notes/preview/lines
/// Retrieve a credit note preview's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_credit_notes_preview_lines_builder()` + `get_v1_credit_notes_preview_lines_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_preview_lines(
    client: &SimpleHttpClient,
    args: &GetV1CreditNotesPreviewLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_credit_notes_preview_lines_builder(
        client,
        args.amount,
        args.credit_amount,
        args.effective_at,
        args.email_type.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.invoice.as_deref(),
        args.limit,
        args.lines,
        args.memo.as_deref(),
        args.metadata.as_deref(),
        args.out_of_band_amount,
        args.reason.as_deref(),
        args.refund_amount,
        args.refunds,
        args.shipping_cost.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_credit_notes_preview_lines_execute(builder)
}

/// GET /v1/credit_notes/{credit_note}/lines
/// Retrieve a credit note's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_credit_notes_credit_note_lines_execute()` to send, or `get_v1_credit_notes_credit_note_lines` for simplest API.

pub fn get_v1_credit_notes_credit_note_lines_builder(
    client: &SimpleHttpClient,
    credit_note: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/credit_notes/{}/lines",
        credit_note,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/{credit_note}/lines
/// Retrieve a credit note's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_credit_notes_credit_note_lines_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_credit_notes_credit_note_lines_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_credit_note_lines_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_credit_notes_credit_note_lines`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CreditNotesCreditNoteLinesArgs {
    /// Path parameter: credit_note
    pub credit_note: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/credit_notes/{credit_note}/lines
/// Retrieve a credit note's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_credit_notes_credit_note_lines_builder()` + `get_v1_credit_notes_credit_note_lines_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_credit_note_lines(
    client: &SimpleHttpClient,
    args: &GetV1CreditNotesCreditNoteLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_credit_notes_credit_note_lines_builder(
        client,
        &args.credit_note,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_credit_notes_credit_note_lines_execute(builder)
}

/// GET /v1/credit_notes/{id}
/// Retrieve a credit note
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_credit_notes_id_execute()` to send, or `get_v1_credit_notes_id` for simplest API.

pub fn get_v1_credit_notes_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/credit_notes/{id}
/// Retrieve a credit note
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_credit_notes_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_credit_notes_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Credit_note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_credit_notes_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CreditNotesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/credit_notes/{id}
/// Retrieve a credit note
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_credit_notes_id_builder()` + `get_v1_credit_notes_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_credit_notes_id(
    client: &SimpleHttpClient,
    args: &GetV1CreditNotesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_credit_notes_id_builder(client, &args.id, args.expand)?;
    get_v1_credit_notes_id_execute(builder)
}

/// POST /v1/credit_notes/{id}
/// Update a credit note
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_credit_notes_id_execute()` to send, or `post_v1_credit_notes_id` for simplest API.

pub fn post_v1_credit_notes_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/credit_notes/{id}
/// Update a credit note
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_credit_notes_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_credit_notes_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_credit_notes_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Credit_note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_credit_notes_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CreditNotesIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/credit_notes/{id}
/// Update a credit note
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_credit_notes_id_builder()` + `post_v1_credit_notes_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_credit_notes_id(
    client: &SimpleHttpClient,
    args: &PostV1CreditNotesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_credit_notes_id_builder(client, &args.id)?;
    post_v1_credit_notes_id_execute(builder)
}

/// POST /v1/credit_notes/{id}/void
/// Void a credit note
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_credit_notes_id_void_execute()` to send, or `post_v1_credit_notes_id_void` for simplest API.

pub fn post_v1_credit_notes_id_void_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/credit_notes/{}/void", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/credit_notes/{id}/void
/// Void a credit note
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_credit_notes_id_void_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_credit_notes_id_void_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_credit_notes_id_void_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Credit_note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_credit_notes_id_void`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CreditNotesIdVoidArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/credit_notes/{id}/void
/// Void a credit note
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_credit_notes_id_void_builder()` + `post_v1_credit_notes_id_void_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_credit_notes_id_void(
    client: &SimpleHttpClient,
    args: &PostV1CreditNotesIdVoidArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Credit_note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_credit_notes_id_void_builder(client, &args.id)?;
    post_v1_credit_notes_id_void_execute(builder)
}

/// POST /v1/customer_sessions
/// Create a Customer Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customer_sessions_execute()` to send, or `post_v1_customer_sessions` for simplest API.

pub fn post_v1_customer_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customer_sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customer_sessions
/// Create a Customer Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customer_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customer_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customer_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer_session>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/customer_sessions
/// Create a Customer Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customer_sessions_builder()` + `post_v1_customer_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customer_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer_session>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customer_sessions_builder(client)?;
    post_v1_customer_sessions_execute(builder)
}

/// GET /v1/customers
/// List all customers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_execute()` to send, or `get_v1_customers` for simplest API.

pub fn get_v1_customers_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    email: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    test_clock: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = email {
        query_parts.push(format!("email={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = test_clock {
        query_parts.push(format!("test_clock={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers
/// List all customers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: email
    pub email: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: test_clock
    pub test_clock: Option<String>,
}

/// GET /v1/customers
/// List all customers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_builder()` + `get_v1_customers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers(
    client: &SimpleHttpClient,
    args: &GetV1CustomersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_builder(
        client,
        args.created.as_deref(),
        args.email.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.test_clock.as_deref(),
    )?;
    get_v1_customers_execute(builder)
}

/// POST /v1/customers
/// Create a customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_execute()` to send, or `post_v1_customers` for simplest API.

pub fn post_v1_customers_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers
/// Create a customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/customers
/// Create a customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_builder()` + `post_v1_customers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_customers_builder(client)?;
    post_v1_customers_execute(builder)
}

/// GET /v1/customers/search
/// Search customers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_search_execute()` to send, or `get_v1_customers_search` for simplest API.

pub fn get_v1_customers_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/search
/// Search customers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/customers/search
/// Search customers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_search_builder()` + `get_v1_customers_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_search(
    client: &SimpleHttpClient,
    args: &GetV1CustomersSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_customers_search_execute(builder)
}

/// GET /v1/customers/{customer}
/// Retrieve a customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_execute()` to send, or `get_v1_customers_customer` for simplest API.

pub fn get_v1_customers_customer_builder(
    client: &SimpleHttpClient,
    customer: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}", customer,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}
/// Retrieve a customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}
/// Retrieve a customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_builder()` + `get_v1_customers_customer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_builder(client, &args.customer, args.expand)?;
    get_v1_customers_customer_execute(builder)
}

/// POST /v1/customers/{customer}
/// Update a customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_execute()` to send, or `post_v1_customers_customer` for simplest API.

pub fn post_v1_customers_customer_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}", customer,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}
/// Update a customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}
/// Update a customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_builder()` + `post_v1_customers_customer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_builder(client, &args.customer)?;
    post_v1_customers_customer_execute(builder)
}

/// DELETE /v1/customers/{customer}
/// Delete a customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_execute()` to send, or `delete_v1_customers_customer` for simplest API.

pub fn delete_v1_customers_customer_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}", customer,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}
/// Delete a customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_customer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// DELETE /v1/customers/{customer}
/// Delete a customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_builder()` + `delete_v1_customers_customer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_customer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_customers_customer_builder(client, &args.customer)?;
    delete_v1_customers_customer_execute(builder)
}

/// GET /v1/customers/{customer}/balance_transactions
/// List customer balance transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_balance_transactions_execute()` to send, or `get_v1_customers_customer_balance_transactions` for simplest API.

pub fn get_v1_customers_customer_balance_transactions_builder(
    client: &SimpleHttpClient,
    customer: &str,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    invoice: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/balance_transactions",
        customer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = invoice {
        query_parts.push(format!("invoice={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/balance_transactions
/// List customer balance transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_balance_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_balance_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_balance_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_balance_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerBalanceTransactionsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/balance_transactions
/// List customer balance transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_balance_transactions_builder()` + `get_v1_customers_customer_balance_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_balance_transactions(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerBalanceTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_balance_transactions_builder(
        client,
        &args.customer,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.invoice.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_balance_transactions_execute(builder)
}

/// POST /v1/customers/{customer}/balance_transactions
/// Create a customer balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_balance_transactions_execute()` to send, or `post_v1_customers_customer_balance_transactions` for simplest API.

pub fn post_v1_customers_customer_balance_transactions_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/balance_transactions",
        customer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/balance_transactions
/// Create a customer balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_balance_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_balance_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_balance_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer_balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_balance_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerBalanceTransactionsArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/balance_transactions
/// Create a customer balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_balance_transactions_builder()` + `post_v1_customers_customer_balance_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_balance_transactions(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerBalanceTransactionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_balance_transactions_builder(client, &args.customer)?;
    post_v1_customers_customer_balance_transactions_execute(builder)
}

/// GET /v1/customers/{customer}/balance_transactions/{transaction}
/// Retrieve a customer balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_balance_transactions_transaction_execute()` to send, or `get_v1_customers_customer_balance_transactions_transaction` for simplest API.

pub fn get_v1_customers_customer_balance_transactions_transaction_builder(
    client: &SimpleHttpClient,
    customer: &str,
    transaction: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/balance_transactions/{}",
        customer, transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/balance_transactions/{transaction}
/// Retrieve a customer balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_balance_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_balance_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_balance_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer_balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_balance_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerBalanceTransactionsTransactionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/balance_transactions/{transaction}
/// Retrieve a customer balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_balance_transactions_transaction_builder()` + `get_v1_customers_customer_balance_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_balance_transactions_transaction(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerBalanceTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_balance_transactions_transaction_builder(
        client,
        &args.customer,
        &args.transaction,
        args.expand,
    )?;
    get_v1_customers_customer_balance_transactions_transaction_execute(builder)
}

/// POST /v1/customers/{customer}/balance_transactions/{transaction}
/// Update a customer credit balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_balance_transactions_transaction_execute()` to send, or `post_v1_customers_customer_balance_transactions_transaction` for simplest API.

pub fn post_v1_customers_customer_balance_transactions_transaction_builder(
    client: &SimpleHttpClient,
    customer: &str,
    transaction: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/balance_transactions/{}",
        customer, transaction,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/balance_transactions/{transaction}
/// Update a customer credit balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_balance_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_balance_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_balance_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer_balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_balance_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerBalanceTransactionsTransactionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: transaction
    pub transaction: String,
}

/// POST /v1/customers/{customer}/balance_transactions/{transaction}
/// Update a customer credit balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_balance_transactions_transaction_builder()` + `post_v1_customers_customer_balance_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_balance_transactions_transaction(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerBalanceTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_balance_transactions_transaction_builder(
        client,
        &args.customer,
        &args.transaction,
    )?;
    post_v1_customers_customer_balance_transactions_transaction_execute(builder)
}

/// GET /v1/customers/{customer}/bank_accounts
/// List all bank accounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_bank_accounts_execute()` to send, or `get_v1_customers_customer_bank_accounts` for simplest API.

pub fn get_v1_customers_customer_bank_accounts_builder(
    client: &SimpleHttpClient,
    customer: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/bank_accounts",
        customer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/bank_accounts
/// List all bank accounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_bank_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_bank_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_bank_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_bank_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerBankAccountsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/bank_accounts
/// List all bank accounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_bank_accounts_builder()` + `get_v1_customers_customer_bank_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_bank_accounts(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerBankAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_bank_accounts_builder(
        client,
        &args.customer,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_bank_accounts_execute(builder)
}

/// POST /v1/customers/{customer}/bank_accounts
/// Create a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_bank_accounts_execute()` to send, or `post_v1_customers_customer_bank_accounts` for simplest API.

pub fn post_v1_customers_customer_bank_accounts_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/bank_accounts",
        customer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/bank_accounts
/// Create a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_bank_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_bank_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_bank_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_bank_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerBankAccountsArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/bank_accounts
/// Create a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_bank_accounts_builder()` + `post_v1_customers_customer_bank_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_bank_accounts(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerBankAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_bank_accounts_builder(client, &args.customer)?;
    post_v1_customers_customer_bank_accounts_execute(builder)
}

/// GET /v1/customers/{customer}/bank_accounts/{id}
/// Retrieve a bank account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_bank_accounts_id_execute()` to send, or `get_v1_customers_customer_bank_accounts_id` for simplest API.

pub fn get_v1_customers_customer_bank_accounts_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/bank_accounts/{}",
        customer, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/bank_accounts/{id}
/// Retrieve a bank account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_bank_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_bank_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_bank_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bank_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bank_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_bank_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerBankAccountsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/bank_accounts/{id}
/// Retrieve a bank account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_bank_accounts_id_builder()` + `get_v1_customers_customer_bank_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_bank_accounts_id(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerBankAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bank_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_bank_accounts_id_builder(
        client,
        &args.customer,
        &args.id,
        args.expand,
    )?;
    get_v1_customers_customer_bank_accounts_id_execute(builder)
}

/// POST /v1/customers/{customer}/bank_accounts/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_bank_accounts_id_execute()` to send, or `post_v1_customers_customer_bank_accounts_id` for simplest API.

pub fn post_v1_customers_customer_bank_accounts_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/bank_accounts/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/bank_accounts/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_bank_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_bank_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_bank_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_bank_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerBankAccountsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/customers/{customer}/bank_accounts/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_bank_accounts_id_builder()` + `post_v1_customers_customer_bank_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_bank_accounts_id(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerBankAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_customers_customer_bank_accounts_id_builder(client, &args.customer, &args.id)?;
    post_v1_customers_customer_bank_accounts_id_execute(builder)
}

/// DELETE /v1/customers/{customer}/bank_accounts/{id}
/// Delete a customer source
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_bank_accounts_id_execute()` to send, or `delete_v1_customers_customer_bank_accounts_id` for simplest API.

pub fn delete_v1_customers_customer_bank_accounts_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/bank_accounts/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/bank_accounts/{id}
/// Delete a customer source
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_bank_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_bank_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_bank_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_bank_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerBankAccountsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/customers/{customer}/bank_accounts/{id}
/// Delete a customer source
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_bank_accounts_id_builder()` + `delete_v1_customers_customer_bank_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_bank_accounts_id(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerBankAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_v1_customers_customer_bank_accounts_id_builder(client, &args.customer, &args.id)?;
    delete_v1_customers_customer_bank_accounts_id_execute(builder)
}

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify
/// Verify a bank account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_bank_accounts_id_verify_execute()` to send, or `post_v1_customers_customer_bank_accounts_id_verify` for simplest API.

pub fn post_v1_customers_customer_bank_accounts_id_verify_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/bank_accounts/{}/verify",
        customer, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify
/// Verify a bank account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_bank_accounts_id_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_bank_accounts_id_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_bank_accounts_id_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bank_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bank_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_bank_accounts_id_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerBankAccountsIdVerifyArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/customers/{customer}/bank_accounts/{id}/verify
/// Verify a bank account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_bank_accounts_id_verify_builder()` + `post_v1_customers_customer_bank_accounts_id_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_bank_accounts_id_verify(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerBankAccountsIdVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bank_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_bank_accounts_id_verify_builder(
        client,
        &args.customer,
        &args.id,
    )?;
    post_v1_customers_customer_bank_accounts_id_verify_execute(builder)
}

/// GET /v1/customers/{customer}/cards
/// List all cards
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_cards_execute()` to send, or `get_v1_customers_customer_cards` for simplest API.

pub fn get_v1_customers_customer_cards_builder(
    client: &SimpleHttpClient,
    customer: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/cards", customer,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/cards
/// List all cards
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_cards_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_cards_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cards_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_cards`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerCardsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/cards
/// List all cards
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_cards_builder()` + `get_v1_customers_customer_cards_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cards(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerCardsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_cards_builder(
        client,
        &args.customer,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_cards_execute(builder)
}

/// POST /v1/customers/{customer}/cards
/// Create a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_cards_execute()` to send, or `post_v1_customers_customer_cards` for simplest API.

pub fn post_v1_customers_customer_cards_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/cards", customer,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/cards
/// Create a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_cards_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_cards_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_cards_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_cards`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerCardsArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/cards
/// Create a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_cards_builder()` + `post_v1_customers_customer_cards_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_cards(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerCardsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_cards_builder(client, &args.customer)?;
    post_v1_customers_customer_cards_execute(builder)
}

/// GET /v1/customers/{customer}/cards/{id}
/// Retrieve a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_cards_id_execute()` to send, or `get_v1_customers_customer_cards_id` for simplest API.

pub fn get_v1_customers_customer_cards_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cards/{}",
        customer, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/cards/{id}
/// Retrieve a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_cards_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_cards_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cards_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Card>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Card = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_cards_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerCardsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/cards/{id}
/// Retrieve a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_cards_id_builder()` + `get_v1_customers_customer_cards_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cards_id(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerCardsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Card>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_v1_customers_customer_cards_id_builder(client, &args.customer, &args.id, args.expand)?;
    get_v1_customers_customer_cards_id_execute(builder)
}

/// POST /v1/customers/{customer}/cards/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_cards_id_execute()` to send, or `post_v1_customers_customer_cards_id` for simplest API.

pub fn post_v1_customers_customer_cards_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cards/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/cards/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_cards_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_cards_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_cards_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_cards_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerCardsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/customers/{customer}/cards/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_cards_id_builder()` + `post_v1_customers_customer_cards_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_cards_id(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerCardsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_cards_id_builder(client, &args.customer, &args.id)?;
    post_v1_customers_customer_cards_id_execute(builder)
}

/// DELETE /v1/customers/{customer}/cards/{id}
/// Delete a customer source
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_cards_id_execute()` to send, or `delete_v1_customers_customer_cards_id` for simplest API.

pub fn delete_v1_customers_customer_cards_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cards/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/cards/{id}
/// Delete a customer source
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_cards_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_cards_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_cards_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_cards_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerCardsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/customers/{customer}/cards/{id}
/// Delete a customer source
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_cards_id_builder()` + `delete_v1_customers_customer_cards_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_cards_id(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerCardsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_v1_customers_customer_cards_id_builder(client, &args.customer, &args.id)?;
    delete_v1_customers_customer_cards_id_execute(builder)
}

/// GET /v1/customers/{customer}/cash_balance
/// Retrieve a cash balance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_cash_balance_execute()` to send, or `get_v1_customers_customer_cash_balance` for simplest API.

pub fn get_v1_customers_customer_cash_balance_builder(
    client: &SimpleHttpClient,
    customer: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cash_balance",
        customer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/cash_balance
/// Retrieve a cash balance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_cash_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_cash_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cash_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cash_balance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cash_balance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_cash_balance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerCashBalanceArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/cash_balance
/// Retrieve a cash balance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_cash_balance_builder()` + `get_v1_customers_customer_cash_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cash_balance(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerCashBalanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cash_balance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_customers_customer_cash_balance_builder(client, &args.customer, args.expand)?;
    get_v1_customers_customer_cash_balance_execute(builder)
}

/// POST /v1/customers/{customer}/cash_balance
/// Update a cash balance's settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_cash_balance_execute()` to send, or `post_v1_customers_customer_cash_balance` for simplest API.

pub fn post_v1_customers_customer_cash_balance_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cash_balance",
        customer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/cash_balance
/// Update a cash balance's settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_cash_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_cash_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_cash_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cash_balance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cash_balance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_cash_balance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerCashBalanceArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/cash_balance
/// Update a cash balance's settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_cash_balance_builder()` + `post_v1_customers_customer_cash_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_cash_balance(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerCashBalanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cash_balance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_cash_balance_builder(client, &args.customer)?;
    post_v1_customers_customer_cash_balance_execute(builder)
}

/// GET /v1/customers/{customer}/cash_balance_transactions
/// List cash balance transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_cash_balance_transactions_execute()` to send, or `get_v1_customers_customer_cash_balance_transactions` for simplest API.

pub fn get_v1_customers_customer_cash_balance_transactions_builder(
    client: &SimpleHttpClient,
    customer: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cash_balance_transactions",
        customer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/cash_balance_transactions
/// List cash balance transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_cash_balance_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_cash_balance_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cash_balance_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_cash_balance_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerCashBalanceTransactionsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/cash_balance_transactions
/// List cash balance transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_cash_balance_transactions_builder()` + `get_v1_customers_customer_cash_balance_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cash_balance_transactions(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerCashBalanceTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_cash_balance_transactions_builder(
        client,
        &args.customer,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_cash_balance_transactions_execute(builder)
}

/// GET /v1/customers/{customer}/cash_balance_transactions/{transaction}
/// Retrieve a cash balance transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_cash_balance_transactions_transaction_execute()` to send, or `get_v1_customers_customer_cash_balance_transactions_transaction` for simplest API.

pub fn get_v1_customers_customer_cash_balance_transactions_transaction_builder(
    client: &SimpleHttpClient,
    customer: &str,
    transaction: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/cash_balance_transactions/{}",
        customer, transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/cash_balance_transactions/{transaction}
/// Retrieve a cash balance transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_cash_balance_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_cash_balance_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cash_balance_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_cash_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer_cash_balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_cash_balance_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerCashBalanceTransactionsTransactionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/cash_balance_transactions/{transaction}
/// Retrieve a cash balance transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_cash_balance_transactions_transaction_builder()` + `get_v1_customers_customer_cash_balance_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_cash_balance_transactions_transaction(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerCashBalanceTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_cash_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_cash_balance_transactions_transaction_builder(
        client,
        &args.customer,
        &args.transaction,
        args.expand,
    )?;
    get_v1_customers_customer_cash_balance_transactions_transaction_execute(builder)
}

/// GET /v1/customers/{customer}/discount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_discount_execute()` to send, or `get_v1_customers_customer_discount` for simplest API.

pub fn get_v1_customers_customer_discount_builder(
    client: &SimpleHttpClient,
    customer: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/discount", customer,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/discount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_discount_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_discount_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_discount_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Discount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_discount`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/discount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_discount_builder()` + `get_v1_customers_customer_discount_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_discount(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_discount_builder(client, &args.customer, args.expand)?;
    get_v1_customers_customer_discount_execute(builder)
}

/// DELETE /v1/customers/{customer}/discount
/// Delete a customer discount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_discount_execute()` to send, or `delete_v1_customers_customer_discount` for simplest API.

pub fn delete_v1_customers_customer_discount_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/discount", customer,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/discount
/// Delete a customer discount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_discount_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_discount_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_discount_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_discount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_discount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_discount`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// DELETE /v1/customers/{customer}/discount
/// Delete a customer discount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_discount_builder()` + `delete_v1_customers_customer_discount_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_discount(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_discount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_customers_customer_discount_builder(client, &args.customer)?;
    delete_v1_customers_customer_discount_execute(builder)
}

/// POST /v1/customers/{customer}/funding_instructions
/// Create or retrieve funding instructions for a customer cash balance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_funding_instructions_execute()` to send, or `post_v1_customers_customer_funding_instructions` for simplest API.

pub fn post_v1_customers_customer_funding_instructions_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/funding_instructions",
        customer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/funding_instructions
/// Create or retrieve funding instructions for a customer cash balance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_funding_instructions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_funding_instructions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_funding_instructions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Funding_instructions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Funding_instructions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_funding_instructions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerFundingInstructionsArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/funding_instructions
/// Create or retrieve funding instructions for a customer cash balance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_funding_instructions_builder()` + `post_v1_customers_customer_funding_instructions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_funding_instructions(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerFundingInstructionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Funding_instructions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_funding_instructions_builder(client, &args.customer)?;
    post_v1_customers_customer_funding_instructions_execute(builder)
}

/// GET /v1/customers/{customer}/payment_methods
/// List a Customer's PaymentMethods
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_payment_methods_execute()` to send, or `get_v1_customers_customer_payment_methods` for simplest API.

pub fn get_v1_customers_customer_payment_methods_builder(
    client: &SimpleHttpClient,
    customer: &str,
    allow_redisplay: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/payment_methods",
        customer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allow_redisplay {
        query_parts.push(format!("allow_redisplay={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/payment_methods
/// List a Customer's PaymentMethods
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_payment_methods_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_payment_methods_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_payment_methods_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_payment_methods`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerPaymentMethodsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: allow_redisplay
    pub allow_redisplay: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/customers/{customer}/payment_methods
/// List a Customer's PaymentMethods
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_payment_methods_builder()` + `get_v1_customers_customer_payment_methods_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_payment_methods(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerPaymentMethodsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_payment_methods_builder(
        client,
        &args.customer,
        args.allow_redisplay.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_customers_customer_payment_methods_execute(builder)
}

/// GET /v1/customers/{customer}/payment_methods/{payment_method}
/// Retrieve a Customer's PaymentMethod
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_payment_methods_payment_method_execute()` to send, or `get_v1_customers_customer_payment_methods_payment_method` for simplest API.

pub fn get_v1_customers_customer_payment_methods_payment_method_builder(
    client: &SimpleHttpClient,
    customer: &str,
    payment_method: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/payment_methods/{}",
        customer, payment_method,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/payment_methods/{payment_method}
/// Retrieve a Customer's PaymentMethod
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_payment_methods_payment_method_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_payment_methods_payment_method_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_payment_methods_payment_method_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_payment_methods_payment_method`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerPaymentMethodsPaymentMethodArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: payment_method
    pub payment_method: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/payment_methods/{payment_method}
/// Retrieve a Customer's PaymentMethod
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_payment_methods_payment_method_builder()` + `get_v1_customers_customer_payment_methods_payment_method_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_payment_methods_payment_method(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerPaymentMethodsPaymentMethodArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_payment_methods_payment_method_builder(
        client,
        &args.customer,
        &args.payment_method,
        args.expand,
    )?;
    get_v1_customers_customer_payment_methods_payment_method_execute(builder)
}

/// GET /v1/customers/{customer}/sources
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_sources_execute()` to send, or `get_v1_customers_customer_sources` for simplest API.

pub fn get_v1_customers_customer_sources_builder(
    client: &SimpleHttpClient,
    customer: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    object: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/sources", customer,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = object {
        query_parts.push(format!("object={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/sources
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_sources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerSourcesArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: object
    pub object: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/sources
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_sources_builder()` + `get_v1_customers_customer_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_sources(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerSourcesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_sources_builder(
        client,
        &args.customer,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.object.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_sources_execute(builder)
}

/// POST /v1/customers/{customer}/sources
/// Create a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_sources_execute()` to send, or `post_v1_customers_customer_sources` for simplest API.

pub fn post_v1_customers_customer_sources_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/sources", customer,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/sources
/// Create a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_sources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerSourcesArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/sources
/// Create a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_sources_builder()` + `post_v1_customers_customer_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_sources(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerSourcesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_sources_builder(client, &args.customer)?;
    post_v1_customers_customer_sources_execute(builder)
}

/// GET /v1/customers/{customer}/sources/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_sources_id_execute()` to send, or `get_v1_customers_customer_sources_id` for simplest API.

pub fn get_v1_customers_customer_sources_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/sources/{}",
        customer, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/sources/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_sources_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_sources_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_sources_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_sources_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerSourcesIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/sources/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_sources_id_builder()` + `get_v1_customers_customer_sources_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_sources_id(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerSourcesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_source>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_sources_id_builder(
        client,
        &args.customer,
        &args.id,
        args.expand,
    )?;
    get_v1_customers_customer_sources_id_execute(builder)
}

/// POST /v1/customers/{customer}/sources/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_sources_id_execute()` to send, or `post_v1_customers_customer_sources_id` for simplest API.

pub fn post_v1_customers_customer_sources_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/sources/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/sources/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_sources_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_sources_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_sources_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_sources_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerSourcesIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/customers/{customer}/sources/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_sources_id_builder()` + `post_v1_customers_customer_sources_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_sources_id(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerSourcesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_sources_id_builder(client, &args.customer, &args.id)?;
    post_v1_customers_customer_sources_id_execute(builder)
}

/// DELETE /v1/customers/{customer}/sources/{id}
/// Delete a customer source
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_sources_id_execute()` to send, or `delete_v1_customers_customer_sources_id` for simplest API.

pub fn delete_v1_customers_customer_sources_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/sources/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/sources/{id}
/// Delete a customer source
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_sources_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_sources_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_sources_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_sources_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerSourcesIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/customers/{customer}/sources/{id}
/// Delete a customer source
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_sources_id_builder()` + `delete_v1_customers_customer_sources_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_sources_id(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerSourcesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_v1_customers_customer_sources_id_builder(client, &args.customer, &args.id)?;
    delete_v1_customers_customer_sources_id_execute(builder)
}

/// POST /v1/customers/{customer}/sources/{id}/verify
/// Verify a bank account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_sources_id_verify_execute()` to send, or `post_v1_customers_customer_sources_id_verify` for simplest API.

pub fn post_v1_customers_customer_sources_id_verify_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/sources/{}/verify",
        customer, id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/sources/{id}/verify
/// Verify a bank account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_sources_id_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_sources_id_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_sources_id_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bank_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bank_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_sources_id_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerSourcesIdVerifyArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/customers/{customer}/sources/{id}/verify
/// Verify a bank account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_sources_id_verify_builder()` + `post_v1_customers_customer_sources_id_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_sources_id_verify(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerSourcesIdVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bank_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_customers_customer_sources_id_verify_builder(client, &args.customer, &args.id)?;
    post_v1_customers_customer_sources_id_verify_execute(builder)
}

/// GET /v1/customers/{customer}/subscriptions
/// List active subscriptions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_subscriptions_execute()` to send, or `get_v1_customers_customer_subscriptions` for simplest API.

pub fn get_v1_customers_customer_subscriptions_builder(
    client: &SimpleHttpClient,
    customer: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions",
        customer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/subscriptions
/// List active subscriptions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_subscriptions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_subscriptions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_subscriptions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_subscriptions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerSubscriptionsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/subscriptions
/// List active subscriptions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_subscriptions_builder()` + `get_v1_customers_customer_subscriptions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_subscriptions(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerSubscriptionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_subscriptions_builder(
        client,
        &args.customer,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_subscriptions_execute(builder)
}

/// POST /v1/customers/{customer}/subscriptions
/// Create a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_subscriptions_execute()` to send, or `post_v1_customers_customer_subscriptions` for simplest API.

pub fn post_v1_customers_customer_subscriptions_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions",
        customer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/subscriptions
/// Create a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_subscriptions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_subscriptions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_subscriptions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_subscriptions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerSubscriptionsArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/subscriptions
/// Create a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_subscriptions_builder()` + `post_v1_customers_customer_subscriptions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_subscriptions(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerSubscriptionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_subscriptions_builder(client, &args.customer)?;
    post_v1_customers_customer_subscriptions_execute(builder)
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Retrieve a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_subscriptions_subscription_exposed_id_execute()` to send, or `get_v1_customers_customer_subscriptions_subscription_exposed_id` for simplest API.

pub fn get_v1_customers_customer_subscriptions_subscription_exposed_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    subscription_exposed_id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions/{}",
        customer, subscription_exposed_id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Retrieve a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_subscriptions_subscription_exposed_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_subscriptions_subscription_exposed_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_subscriptions_subscription_exposed_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_subscriptions_subscription_exposed_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerSubscriptionsSubscriptionExposedIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Retrieve a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_subscriptions_subscription_exposed_id_builder()` + `get_v1_customers_customer_subscriptions_subscription_exposed_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_subscriptions_subscription_exposed_id(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerSubscriptionsSubscriptionExposedIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_subscriptions_subscription_exposed_id_builder(
        client,
        &args.customer,
        &args.subscription_exposed_id,
        args.expand,
    )?;
    get_v1_customers_customer_subscriptions_subscription_exposed_id_execute(builder)
}

/// POST /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Update a subscription on a customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_subscriptions_subscription_exposed_id_execute()` to send, or `post_v1_customers_customer_subscriptions_subscription_exposed_id` for simplest API.

pub fn post_v1_customers_customer_subscriptions_subscription_exposed_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    subscription_exposed_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions/{}",
        customer, subscription_exposed_id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Update a subscription on a customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_subscriptions_subscription_exposed_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_subscriptions_subscription_exposed_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_subscriptions_subscription_exposed_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_subscriptions_subscription_exposed_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerSubscriptionsSubscriptionExposedIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// POST /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Update a subscription on a customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_subscriptions_subscription_exposed_id_builder()` + `post_v1_customers_customer_subscriptions_subscription_exposed_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_subscriptions_subscription_exposed_id(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerSubscriptionsSubscriptionExposedIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_subscriptions_subscription_exposed_id_builder(
        client,
        &args.customer,
        &args.subscription_exposed_id,
    )?;
    post_v1_customers_customer_subscriptions_subscription_exposed_id_execute(builder)
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Cancel a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_subscriptions_subscription_exposed_id_execute()` to send, or `delete_v1_customers_customer_subscriptions_subscription_exposed_id` for simplest API.

pub fn delete_v1_customers_customer_subscriptions_subscription_exposed_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    subscription_exposed_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions/{}",
        customer, subscription_exposed_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Cancel a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_subscriptions_subscription_exposed_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_subscriptions_subscription_exposed_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_subscriptions_subscription_exposed_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_subscriptions_subscription_exposed_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerSubscriptionsSubscriptionExposedIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}
/// Cancel a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_subscriptions_subscription_exposed_id_builder()` + `delete_v1_customers_customer_subscriptions_subscription_exposed_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_subscriptions_subscription_exposed_id(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerSubscriptionsSubscriptionExposedIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_customers_customer_subscriptions_subscription_exposed_id_builder(
        client,
        &args.customer,
        &args.subscription_exposed_id,
    )?;
    delete_v1_customers_customer_subscriptions_subscription_exposed_id_execute(builder)
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute()` to send, or `get_v1_customers_customer_subscriptions_subscription_exposed_id_discount` for simplest API.

pub fn get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
    client: &SimpleHttpClient,
    customer: &str,
    subscription_exposed_id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions/{}/discount",
        customer, subscription_exposed_id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Discount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_subscriptions_subscription_exposed_id_discount`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder()` + `get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_subscriptions_subscription_exposed_id_discount(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
        client,
        &args.customer,
        &args.subscription_exposed_id,
        args.expand,
    )?;
    get_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute(builder)
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
/// Delete a customer discount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute()` to send, or `delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount` for simplest API.

pub fn delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
    client: &SimpleHttpClient,
    customer: &str,
    subscription_exposed_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/subscriptions/{}/discount",
        customer, subscription_exposed_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
/// Delete a customer discount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_discount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_discount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount
/// Delete a customer discount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder()` + `delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerSubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_discount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_builder(
            client,
            &args.customer,
            &args.subscription_exposed_id,
        )?;
    delete_v1_customers_customer_subscriptions_subscription_exposed_id_discount_execute(builder)
}

/// GET /v1/customers/{customer}/tax_ids
/// List all Customer tax IDs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_tax_ids_execute()` to send, or `get_v1_customers_customer_tax_ids` for simplest API.

pub fn get_v1_customers_customer_tax_ids_builder(
    client: &SimpleHttpClient,
    customer: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/tax_ids", customer,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/tax_ids
/// List all Customer tax IDs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_tax_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_tax_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_tax_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_tax_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerTaxIdsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/customers/{customer}/tax_ids
/// List all Customer tax IDs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_tax_ids_builder()` + `get_v1_customers_customer_tax_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_tax_ids(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerTaxIdsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_tax_ids_builder(
        client,
        &args.customer,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_customers_customer_tax_ids_execute(builder)
}

/// POST /v1/customers/{customer}/tax_ids
/// Create a Customer tax ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_customers_customer_tax_ids_execute()` to send, or `post_v1_customers_customer_tax_ids` for simplest API.

pub fn post_v1_customers_customer_tax_ids_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/customers/{}/tax_ids", customer,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/customers/{customer}/tax_ids
/// Create a Customer tax ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_customers_customer_tax_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_customers_customer_tax_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_tax_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_id = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_customers_customer_tax_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1CustomersCustomerTaxIdsArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/customers/{customer}/tax_ids
/// Create a Customer tax ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_customers_customer_tax_ids_builder()` + `post_v1_customers_customer_tax_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_customers_customer_tax_ids(
    client: &SimpleHttpClient,
    args: &PostV1CustomersCustomerTaxIdsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_customers_customer_tax_ids_builder(client, &args.customer)?;
    post_v1_customers_customer_tax_ids_execute(builder)
}

/// GET /v1/customers/{customer}/tax_ids/{id}
/// Retrieve a Customer tax ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_customers_customer_tax_ids_id_execute()` to send, or `get_v1_customers_customer_tax_ids_id` for simplest API.

pub fn get_v1_customers_customer_tax_ids_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/tax_ids/{}",
        customer, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/customers/{customer}/tax_ids/{id}
/// Retrieve a Customer tax ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_customers_customer_tax_ids_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_customers_customer_tax_ids_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_tax_ids_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_id = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_customers_customer_tax_ids_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1CustomersCustomerTaxIdsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/customers/{customer}/tax_ids/{id}
/// Retrieve a Customer tax ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_customers_customer_tax_ids_id_builder()` + `get_v1_customers_customer_tax_ids_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_customers_customer_tax_ids_id(
    client: &SimpleHttpClient,
    args: &GetV1CustomersCustomerTaxIdsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_customers_customer_tax_ids_id_builder(
        client,
        &args.customer,
        &args.id,
        args.expand,
    )?;
    get_v1_customers_customer_tax_ids_id_execute(builder)
}

/// DELETE /v1/customers/{customer}/tax_ids/{id}
/// Delete a Customer tax ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_customers_customer_tax_ids_id_execute()` to send, or `delete_v1_customers_customer_tax_ids_id` for simplest API.

pub fn delete_v1_customers_customer_tax_ids_id_builder(
    client: &SimpleHttpClient,
    customer: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/customers/{}/tax_ids/{}",
        customer, id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/customers/{customer}/tax_ids/{id}
/// Delete a Customer tax ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_customers_customer_tax_ids_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_customers_customer_tax_ids_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_tax_ids_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_tax_id>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_tax_id = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_customers_customer_tax_ids_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1CustomersCustomerTaxIdsIdArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/customers/{customer}/tax_ids/{id}
/// Delete a Customer tax ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_customers_customer_tax_ids_id_builder()` + `delete_v1_customers_customer_tax_ids_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_customers_customer_tax_ids_id(
    client: &SimpleHttpClient,
    args: &DeleteV1CustomersCustomerTaxIdsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_tax_id>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_customers_customer_tax_ids_id_builder(client, &args.customer, &args.id)?;
    delete_v1_customers_customer_tax_ids_id_execute(builder)
}

/// GET /v1/disputes
/// List all disputes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_disputes_execute()` to send, or `get_v1_disputes` for simplest API.

pub fn get_v1_disputes_builder(
    client: &SimpleHttpClient,
    charge: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_intent: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/disputes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = charge {
        query_parts.push(format!("charge={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_intent {
        query_parts.push(format!("payment_intent={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/disputes
/// List all disputes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_disputes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_disputes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_disputes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_disputes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1DisputesArgs {
    /// Query parameter: charge
    pub charge: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/disputes
/// List all disputes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_disputes_builder()` + `get_v1_disputes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_disputes(
    client: &SimpleHttpClient,
    args: &GetV1DisputesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_disputes_builder(
        client,
        args.charge.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payment_intent.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_disputes_execute(builder)
}

/// GET /v1/disputes/{dispute}
/// Retrieve a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_disputes_dispute_execute()` to send, or `get_v1_disputes_dispute` for simplest API.

pub fn get_v1_disputes_dispute_builder(
    client: &SimpleHttpClient,
    dispute: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/disputes/{}", dispute,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/disputes/{dispute}
/// Retrieve a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_disputes_dispute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_disputes_dispute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_disputes_dispute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_disputes_dispute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1DisputesDisputeArgs {
    /// Path parameter: dispute
    pub dispute: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/disputes/{dispute}
/// Retrieve a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_disputes_dispute_builder()` + `get_v1_disputes_dispute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_disputes_dispute(
    client: &SimpleHttpClient,
    args: &GetV1DisputesDisputeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_disputes_dispute_builder(client, &args.dispute, args.expand)?;
    get_v1_disputes_dispute_execute(builder)
}

/// POST /v1/disputes/{dispute}
/// Update a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_disputes_dispute_execute()` to send, or `post_v1_disputes_dispute` for simplest API.

pub fn post_v1_disputes_dispute_builder(
    client: &SimpleHttpClient,
    dispute: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/disputes/{}", dispute,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/disputes/{dispute}
/// Update a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_disputes_dispute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_disputes_dispute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_disputes_dispute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_disputes_dispute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1DisputesDisputeArgs {
    /// Path parameter: dispute
    pub dispute: String,
}

/// POST /v1/disputes/{dispute}
/// Update a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_disputes_dispute_builder()` + `post_v1_disputes_dispute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_disputes_dispute(
    client: &SimpleHttpClient,
    args: &PostV1DisputesDisputeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_disputes_dispute_builder(client, &args.dispute)?;
    post_v1_disputes_dispute_execute(builder)
}

/// POST /v1/disputes/{dispute}/close
/// Close a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_disputes_dispute_close_execute()` to send, or `post_v1_disputes_dispute_close` for simplest API.

pub fn post_v1_disputes_dispute_close_builder(
    client: &SimpleHttpClient,
    dispute: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/disputes/{}/close", dispute,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/disputes/{dispute}/close
/// Close a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_disputes_dispute_close_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_disputes_dispute_close_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_disputes_dispute_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_disputes_dispute_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1DisputesDisputeCloseArgs {
    /// Path parameter: dispute
    pub dispute: String,
}

/// POST /v1/disputes/{dispute}/close
/// Close a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_disputes_dispute_close_builder()` + `post_v1_disputes_dispute_close_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_disputes_dispute_close(
    client: &SimpleHttpClient,
    args: &PostV1DisputesDisputeCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dispute>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_disputes_dispute_close_builder(client, &args.dispute)?;
    post_v1_disputes_dispute_close_execute(builder)
}

/// GET /v1/entitlements/active_entitlements
/// List all active entitlements
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_entitlements_active_entitlements_execute()` to send, or `get_v1_entitlements_active_entitlements` for simplest API.

pub fn get_v1_entitlements_active_entitlements_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/entitlements/active_entitlements",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/entitlements/active_entitlements
/// List all active entitlements
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_entitlements_active_entitlements_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_entitlements_active_entitlements_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_active_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_entitlements_active_entitlements`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1EntitlementsActiveEntitlementsArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/entitlements/active_entitlements
/// List all active entitlements
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_entitlements_active_entitlements_builder()` + `get_v1_entitlements_active_entitlements_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_active_entitlements(
    client: &SimpleHttpClient,
    args: &GetV1EntitlementsActiveEntitlementsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_entitlements_active_entitlements_builder(
        client,
        args.customer.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_entitlements_active_entitlements_execute(builder)
}

/// GET /v1/entitlements/active_entitlements/{id}
/// Retrieve an active entitlement
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_entitlements_active_entitlements_id_execute()` to send, or `get_v1_entitlements_active_entitlements_id` for simplest API.

pub fn get_v1_entitlements_active_entitlements_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/entitlements/active_entitlements/{}",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/entitlements/active_entitlements/{id}
/// Retrieve an active entitlement
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_entitlements_active_entitlements_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_entitlements_active_entitlements_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_active_entitlements_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EntitlementsActive_entitlement>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitlementsActive_entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_entitlements_active_entitlements_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1EntitlementsActiveEntitlementsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/entitlements/active_entitlements/{id}
/// Retrieve an active entitlement
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_entitlements_active_entitlements_id_builder()` + `get_v1_entitlements_active_entitlements_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_active_entitlements_id(
    client: &SimpleHttpClient,
    args: &GetV1EntitlementsActiveEntitlementsIdArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EntitlementsActive_entitlement>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_entitlements_active_entitlements_id_builder(client, &args.id, args.expand)?;
    get_v1_entitlements_active_entitlements_id_execute(builder)
}

/// GET /v1/entitlements/features
/// List all features
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_entitlements_features_execute()` to send, or `get_v1_entitlements_features` for simplest API.

pub fn get_v1_entitlements_features_builder(
    client: &SimpleHttpClient,
    archived: Option<bool>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    lookup_key: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/entitlements/features",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = archived {
        query_parts.push(format!("archived={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = lookup_key {
        query_parts.push(format!("lookup_key={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/entitlements/features
/// List all features
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_entitlements_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_entitlements_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_entitlements_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1EntitlementsFeaturesArgs {
    /// Query parameter: archived
    pub archived: Option<bool>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: lookup_key
    pub lookup_key: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/entitlements/features
/// List all features
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_entitlements_features_builder()` + `get_v1_entitlements_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_features(
    client: &SimpleHttpClient,
    args: &GetV1EntitlementsFeaturesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_entitlements_features_builder(
        client,
        args.archived,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.lookup_key.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_entitlements_features_execute(builder)
}

/// POST /v1/entitlements/features
/// Create a feature
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_entitlements_features_execute()` to send, or `post_v1_entitlements_features` for simplest API.

pub fn post_v1_entitlements_features_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/entitlements/features",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/entitlements/features
/// Create a feature
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_entitlements_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_entitlements_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_entitlements_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitlementsFeature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/entitlements/features
/// Create a feature
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_entitlements_features_builder()` + `post_v1_entitlements_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_entitlements_features(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_entitlements_features_builder(client)?;
    post_v1_entitlements_features_execute(builder)
}

/// GET /v1/entitlements/features/{id}
/// Retrieve a feature
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_entitlements_features_id_execute()` to send, or `get_v1_entitlements_features_id` for simplest API.

pub fn get_v1_entitlements_features_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/entitlements/features/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/entitlements/features/{id}
/// Retrieve a feature
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_entitlements_features_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_entitlements_features_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_features_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitlementsFeature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_entitlements_features_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1EntitlementsFeaturesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/entitlements/features/{id}
/// Retrieve a feature
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_entitlements_features_id_builder()` + `get_v1_entitlements_features_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_entitlements_features_id(
    client: &SimpleHttpClient,
    args: &GetV1EntitlementsFeaturesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_entitlements_features_id_builder(client, &args.id, args.expand)?;
    get_v1_entitlements_features_id_execute(builder)
}

/// POST /v1/entitlements/features/{id}
/// Updates a feature
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_entitlements_features_id_execute()` to send, or `post_v1_entitlements_features_id` for simplest API.

pub fn post_v1_entitlements_features_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/entitlements/features/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/entitlements/features/{id}
/// Updates a feature
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_entitlements_features_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_entitlements_features_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_entitlements_features_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitlementsFeature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_entitlements_features_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1EntitlementsFeaturesIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/entitlements/features/{id}
/// Updates a feature
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_entitlements_features_id_builder()` + `post_v1_entitlements_features_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_entitlements_features_id(
    client: &SimpleHttpClient,
    args: &PostV1EntitlementsFeaturesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EntitlementsFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_entitlements_features_id_builder(client, &args.id)?;
    post_v1_entitlements_features_id_execute(builder)
}

/// POST /v1/ephemeral_keys
/// Create an ephemeral key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_ephemeral_keys_execute()` to send, or `post_v1_ephemeral_keys` for simplest API.

pub fn post_v1_ephemeral_keys_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/ephemeral_keys",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/ephemeral_keys
/// Create an ephemeral key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_ephemeral_keys_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_ephemeral_keys_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_ephemeral_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ephemeral_key>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ephemeral_key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/ephemeral_keys
/// Create an ephemeral key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_ephemeral_keys_builder()` + `post_v1_ephemeral_keys_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_ephemeral_keys(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ephemeral_key>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_ephemeral_keys_builder(client)?;
    post_v1_ephemeral_keys_execute(builder)
}

/// DELETE /v1/ephemeral_keys/{key}
/// Immediately invalidate an ephemeral key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_ephemeral_keys_key_execute()` to send, or `delete_v1_ephemeral_keys_key` for simplest API.

pub fn delete_v1_ephemeral_keys_key_builder(
    client: &SimpleHttpClient,
    key: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/ephemeral_keys/{}", key,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/ephemeral_keys/{key}
/// Immediately invalidate an ephemeral key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_ephemeral_keys_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_ephemeral_keys_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_ephemeral_keys_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ephemeral_key>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ephemeral_key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_ephemeral_keys_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1EphemeralKeysKeyArgs {
    /// Path parameter: key
    pub key: String,
}

/// DELETE /v1/ephemeral_keys/{key}
/// Immediately invalidate an ephemeral key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_ephemeral_keys_key_builder()` + `delete_v1_ephemeral_keys_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_ephemeral_keys_key(
    client: &SimpleHttpClient,
    args: &DeleteV1EphemeralKeysKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ephemeral_key>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_ephemeral_keys_key_builder(client, &args.key)?;
    delete_v1_ephemeral_keys_key_execute(builder)
}

/// GET /v1/events
/// List all events
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_events_execute()` to send, or `get_v1_events` for simplest API.

pub fn get_v1_events_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    delivery_success: Option<bool>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
    types: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/events",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = delivery_success {
        query_parts.push(format!("delivery_success={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }
    if let Some(val) = types {
        query_parts.push(format!("types={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/events
/// List all events
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_events_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_events_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_events`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1EventsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: delivery_success
    pub delivery_success: Option<bool>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
    /// Query parameter: types
    pub types: Option<Vec<String>>,
}

/// GET /v1/events
/// List all events
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_events_builder()` + `get_v1_events_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_events(
    client: &SimpleHttpClient,
    args: &GetV1EventsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_events_builder(
        client,
        args.created.as_deref(),
        args.delivery_success,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
        args.types,
    )?;
    get_v1_events_execute(builder)
}

/// GET /v1/events/{id}
/// Retrieve an event
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_events_id_execute()` to send, or `get_v1_events_id` for simplest API.

pub fn get_v1_events_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/events/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/events/{id}
/// Retrieve an event
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_events_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_events_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_events_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_events_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1EventsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/events/{id}
/// Retrieve an event
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_events_id_builder()` + `get_v1_events_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_events_id(
    client: &SimpleHttpClient,
    args: &GetV1EventsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_events_id_builder(client, &args.id, args.expand)?;
    get_v1_events_id_execute(builder)
}

/// GET /v1/exchange_rates
/// List all exchange rates
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_exchange_rates_execute()` to send, or `get_v1_exchange_rates` for simplest API.

pub fn get_v1_exchange_rates_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/exchange_rates",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/exchange_rates
/// List all exchange rates
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_exchange_rates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_exchange_rates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_exchange_rates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_exchange_rates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ExchangeRatesArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/exchange_rates
/// List all exchange rates
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_exchange_rates_builder()` + `get_v1_exchange_rates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_exchange_rates(
    client: &SimpleHttpClient,
    args: &GetV1ExchangeRatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_exchange_rates_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_exchange_rates_execute(builder)
}

/// GET /v1/exchange_rates/{rate_id}
/// Retrieve an exchange rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_exchange_rates_rate_id_execute()` to send, or `get_v1_exchange_rates_rate_id` for simplest API.

pub fn get_v1_exchange_rates_rate_id_builder(
    client: &SimpleHttpClient,
    rate_id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/exchange_rates/{}", rate_id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/exchange_rates/{rate_id}
/// Retrieve an exchange rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_exchange_rates_rate_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_exchange_rates_rate_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_exchange_rates_rate_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Exchange_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Exchange_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_exchange_rates_rate_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ExchangeRatesRateIdArgs {
    /// Path parameter: rate_id
    pub rate_id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/exchange_rates/{rate_id}
/// Retrieve an exchange rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_exchange_rates_rate_id_builder()` + `get_v1_exchange_rates_rate_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_exchange_rates_rate_id(
    client: &SimpleHttpClient,
    args: &GetV1ExchangeRatesRateIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Exchange_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_exchange_rates_rate_id_builder(client, &args.rate_id, args.expand)?;
    get_v1_exchange_rates_rate_id_execute(builder)
}

/// POST /v1/external_accounts/{id}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_external_accounts_id_execute()` to send, or `post_v1_external_accounts_id` for simplest API.

pub fn post_v1_external_accounts_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/external_accounts/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/external_accounts/{id}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_external_accounts_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_external_accounts_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_external_accounts_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: External_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_external_accounts_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ExternalAccountsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/external_accounts/{id}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_external_accounts_id_builder()` + `post_v1_external_accounts_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_external_accounts_id(
    client: &SimpleHttpClient,
    args: &PostV1ExternalAccountsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<External_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_external_accounts_id_builder(client, &args.id)?;
    post_v1_external_accounts_id_execute(builder)
}

/// GET /v1/file_links
/// List all file links
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_file_links_execute()` to send, or `get_v1_file_links` for simplest API.

pub fn get_v1_file_links_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    expired: Option<bool>,
    file: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/file_links",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = expired {
        query_parts.push(format!("expired={}", val));
    }
    if let Some(val) = file {
        query_parts.push(format!("file={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/file_links
/// List all file links
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_file_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_file_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_file_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_file_links`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FileLinksArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: expired
    pub expired: Option<bool>,
    /// Query parameter: file
    pub file: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/file_links
/// List all file links
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_file_links_builder()` + `get_v1_file_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_file_links(
    client: &SimpleHttpClient,
    args: &GetV1FileLinksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_file_links_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.expired,
        args.file.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_file_links_execute(builder)
}

/// POST /v1/file_links
/// Create a file link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_file_links_execute()` to send, or `post_v1_file_links` for simplest API.

pub fn post_v1_file_links_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/file_links",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/file_links
/// Create a file link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_file_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_file_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_file_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/file_links
/// Create a file link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_file_links_builder()` + `post_v1_file_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_file_links(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_file_links_builder(client)?;
    post_v1_file_links_execute(builder)
}

/// GET /v1/file_links/{link}
/// Retrieve a file link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_file_links_link_execute()` to send, or `get_v1_file_links_link` for simplest API.

pub fn get_v1_file_links_link_builder(
    client: &SimpleHttpClient,
    link: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/file_links/{}", link,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/file_links/{link}
/// Retrieve a file link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_file_links_link_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_file_links_link_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_file_links_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_file_links_link`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FileLinksLinkArgs {
    /// Path parameter: link
    pub link: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/file_links/{link}
/// Retrieve a file link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_file_links_link_builder()` + `get_v1_file_links_link_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_file_links_link(
    client: &SimpleHttpClient,
    args: &GetV1FileLinksLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_file_links_link_builder(client, &args.link, args.expand)?;
    get_v1_file_links_link_execute(builder)
}

/// POST /v1/file_links/{link}
/// Update a file link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_file_links_link_execute()` to send, or `post_v1_file_links_link` for simplest API.

pub fn post_v1_file_links_link_builder(
    client: &SimpleHttpClient,
    link: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/file_links/{}", link,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/file_links/{link}
/// Update a file link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_file_links_link_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_file_links_link_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_file_links_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_file_links_link`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1FileLinksLinkArgs {
    /// Path parameter: link
    pub link: String,
}

/// POST /v1/file_links/{link}
/// Update a file link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_file_links_link_builder()` + `post_v1_file_links_link_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_file_links_link(
    client: &SimpleHttpClient,
    args: &PostV1FileLinksLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File_link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_file_links_link_builder(client, &args.link)?;
    post_v1_file_links_link_execute(builder)
}

/// GET /v1/files
/// List all files
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_files_execute()` to send, or `get_v1_files` for simplest API.

pub fn get_v1_files_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    purpose: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/files",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = purpose {
        query_parts.push(format!("purpose={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/files
/// List all files
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_files_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_files_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_files_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_files`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FilesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: purpose
    pub purpose: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/files
/// List all files
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_files_builder()` + `get_v1_files_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_files(
    client: &SimpleHttpClient,
    args: &GetV1FilesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_files_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.purpose.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_files_execute(builder)
}

/// POST /v1/files
/// Create a file
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_files_execute()` to send, or `post_v1_files` for simplest API.

pub fn post_v1_files_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/files",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/files
/// Create a file
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_files_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_files_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_files_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/files
/// Create a file
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_files_builder()` + `post_v1_files_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_files(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_files_builder(client)?;
    post_v1_files_execute(builder)
}

/// GET /v1/files/{file}
/// Retrieve a file
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_files_file_execute()` to send, or `get_v1_files_file` for simplest API.

pub fn get_v1_files_file_builder(
    client: &SimpleHttpClient,
    file: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/files/{}", file,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/files/{file}
/// Retrieve a file
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_files_file_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_files_file_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_files_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_files_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FilesFileArgs {
    /// Path parameter: file
    pub file: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/files/{file}
/// Retrieve a file
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_files_file_builder()` + `get_v1_files_file_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_files_file(
    client: &SimpleHttpClient,
    args: &GetV1FilesFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_files_file_builder(client, &args.file, args.expand)?;
    get_v1_files_file_execute(builder)
}

/// GET /v1/financial_connections/accounts
/// List Accounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_financial_connections_accounts_execute()` to send, or `get_v1_financial_connections_accounts` for simplest API.

pub fn get_v1_financial_connections_accounts_builder(
    client: &SimpleHttpClient,
    account_holder: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    session: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/financial_connections/accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = account_holder {
        query_parts.push(format!("account_holder={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = session {
        query_parts.push(format!("session={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/financial_connections/accounts
/// List Accounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_financial_connections_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_financial_connections_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_financial_connections_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FinancialConnectionsAccountsArgs {
    /// Query parameter: account_holder
    pub account_holder: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: session
    pub session: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/financial_connections/accounts
/// List Accounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_financial_connections_accounts_builder()` + `get_v1_financial_connections_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_accounts(
    client: &SimpleHttpClient,
    args: &GetV1FinancialConnectionsAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_financial_connections_accounts_builder(
        client,
        args.account_holder.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.session.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_financial_connections_accounts_execute(builder)
}

/// GET /v1/financial_connections/accounts/{account}
/// Retrieve an Account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_financial_connections_accounts_account_execute()` to send, or `get_v1_financial_connections_accounts_account` for simplest API.

pub fn get_v1_financial_connections_accounts_account_builder(
    client: &SimpleHttpClient,
    account: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/accounts/{}",
        account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/financial_connections/accounts/{account}
/// Retrieve an Account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_financial_connections_accounts_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_financial_connections_accounts_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_accounts_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_financial_connections_accounts_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FinancialConnectionsAccountsAccountArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/financial_connections/accounts/{account}
/// Retrieve an Account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_financial_connections_accounts_account_builder()` + `get_v1_financial_connections_accounts_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_accounts_account(
    client: &SimpleHttpClient,
    args: &GetV1FinancialConnectionsAccountsAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_financial_connections_accounts_account_builder(client, &args.account, args.expand)?;
    get_v1_financial_connections_accounts_account_execute(builder)
}

/// POST /v1/financial_connections/accounts/{account}/disconnect
/// Disconnect an Account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_financial_connections_accounts_account_disconnect_execute()` to send, or `post_v1_financial_connections_accounts_account_disconnect` for simplest API.

pub fn post_v1_financial_connections_accounts_account_disconnect_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/accounts/{}/disconnect",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/financial_connections/accounts/{account}/disconnect
/// Disconnect an Account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_financial_connections_accounts_account_disconnect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_financial_connections_accounts_account_disconnect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_disconnect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_financial_connections_accounts_account_disconnect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1FinancialConnectionsAccountsAccountDisconnectArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/financial_connections/accounts/{account}/disconnect
/// Disconnect an Account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_financial_connections_accounts_account_disconnect_builder()` + `post_v1_financial_connections_accounts_account_disconnect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_disconnect(
    client: &SimpleHttpClient,
    args: &PostV1FinancialConnectionsAccountsAccountDisconnectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_financial_connections_accounts_account_disconnect_builder(client, &args.account)?;
    post_v1_financial_connections_accounts_account_disconnect_execute(builder)
}

/// GET /v1/financial_connections/accounts/{account}/owners
/// List Account Owners
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_financial_connections_accounts_account_owners_execute()` to send, or `get_v1_financial_connections_accounts_account_owners` for simplest API.

pub fn get_v1_financial_connections_accounts_account_owners_builder(
    client: &SimpleHttpClient,
    account: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    ownership: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/accounts/{}/owners",
        account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = ownership {
        query_parts.push(format!("ownership={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/financial_connections/accounts/{account}/owners
/// List Account Owners
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_financial_connections_accounts_account_owners_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_financial_connections_accounts_account_owners_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_accounts_account_owners_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_financial_connections_accounts_account_owners`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FinancialConnectionsAccountsAccountOwnersArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: ownership
    pub ownership: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/financial_connections/accounts/{account}/owners
/// List Account Owners
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_financial_connections_accounts_account_owners_builder()` + `get_v1_financial_connections_accounts_account_owners_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_accounts_account_owners(
    client: &SimpleHttpClient,
    args: &GetV1FinancialConnectionsAccountsAccountOwnersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_financial_connections_accounts_account_owners_builder(
        client,
        &args.account,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.ownership.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_financial_connections_accounts_account_owners_execute(builder)
}

/// POST /v1/financial_connections/accounts/{account}/refresh
/// Refresh Account data
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_financial_connections_accounts_account_refresh_execute()` to send, or `post_v1_financial_connections_accounts_account_refresh` for simplest API.

pub fn post_v1_financial_connections_accounts_account_refresh_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/accounts/{}/refresh",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/financial_connections/accounts/{account}/refresh
/// Refresh Account data
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_financial_connections_accounts_account_refresh_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_financial_connections_accounts_account_refresh_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_refresh_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_financial_connections_accounts_account_refresh`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1FinancialConnectionsAccountsAccountRefreshArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/financial_connections/accounts/{account}/refresh
/// Refresh Account data
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_financial_connections_accounts_account_refresh_builder()` + `post_v1_financial_connections_accounts_account_refresh_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_refresh(
    client: &SimpleHttpClient,
    args: &PostV1FinancialConnectionsAccountsAccountRefreshArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_financial_connections_accounts_account_refresh_builder(client, &args.account)?;
    post_v1_financial_connections_accounts_account_refresh_execute(builder)
}

/// POST /v1/financial_connections/accounts/{account}/subscribe
/// Subscribe to data refreshes for an Account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_financial_connections_accounts_account_subscribe_execute()` to send, or `post_v1_financial_connections_accounts_account_subscribe` for simplest API.

pub fn post_v1_financial_connections_accounts_account_subscribe_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/accounts/{}/subscribe",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/financial_connections/accounts/{account}/subscribe
/// Subscribe to data refreshes for an Account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_financial_connections_accounts_account_subscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_financial_connections_accounts_account_subscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_subscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_financial_connections_accounts_account_subscribe`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1FinancialConnectionsAccountsAccountSubscribeArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/financial_connections/accounts/{account}/subscribe
/// Subscribe to data refreshes for an Account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_financial_connections_accounts_account_subscribe_builder()` + `post_v1_financial_connections_accounts_account_subscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_subscribe(
    client: &SimpleHttpClient,
    args: &PostV1FinancialConnectionsAccountsAccountSubscribeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_financial_connections_accounts_account_subscribe_builder(client, &args.account)?;
    post_v1_financial_connections_accounts_account_subscribe_execute(builder)
}

/// POST /v1/financial_connections/accounts/{account}/unsubscribe
/// Unsubscribe from data refreshes for an Account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_financial_connections_accounts_account_unsubscribe_execute()` to send, or `post_v1_financial_connections_accounts_account_unsubscribe` for simplest API.

pub fn post_v1_financial_connections_accounts_account_unsubscribe_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/accounts/{}/unsubscribe",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/financial_connections/accounts/{account}/unsubscribe
/// Unsubscribe from data refreshes for an Account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_financial_connections_accounts_account_unsubscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_financial_connections_accounts_account_unsubscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_unsubscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_financial_connections_accounts_account_unsubscribe`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1FinancialConnectionsAccountsAccountUnsubscribeArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/financial_connections/accounts/{account}/unsubscribe
/// Unsubscribe from data refreshes for an Account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_financial_connections_accounts_account_unsubscribe_builder()` + `post_v1_financial_connections_accounts_account_unsubscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_accounts_account_unsubscribe(
    client: &SimpleHttpClient,
    args: &PostV1FinancialConnectionsAccountsAccountUnsubscribeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_financial_connections_accounts_account_unsubscribe_builder(client, &args.account)?;
    post_v1_financial_connections_accounts_account_unsubscribe_execute(builder)
}

/// POST /v1/financial_connections/sessions
/// Create a Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_financial_connections_sessions_execute()` to send, or `post_v1_financial_connections_sessions` for simplest API.

pub fn post_v1_financial_connections_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/financial_connections/sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/financial_connections/sessions
/// Create a Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_financial_connections_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_financial_connections_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/financial_connections/sessions
/// Create a Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_financial_connections_sessions_builder()` + `post_v1_financial_connections_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_financial_connections_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_financial_connections_sessions_builder(client)?;
    post_v1_financial_connections_sessions_execute(builder)
}

/// GET /v1/financial_connections/sessions/{session}
/// Retrieve a Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_financial_connections_sessions_session_execute()` to send, or `get_v1_financial_connections_sessions_session` for simplest API.

pub fn get_v1_financial_connections_sessions_session_builder(
    client: &SimpleHttpClient,
    session: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/sessions/{}",
        session,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/financial_connections/sessions/{session}
/// Retrieve a Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_financial_connections_sessions_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_financial_connections_sessions_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_sessions_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_financial_connections_sessions_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FinancialConnectionsSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/financial_connections/sessions/{session}
/// Retrieve a Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_financial_connections_sessions_session_builder()` + `get_v1_financial_connections_sessions_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_sessions_session(
    client: &SimpleHttpClient,
    args: &GetV1FinancialConnectionsSessionsSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_financial_connections_sessions_session_builder(client, &args.session, args.expand)?;
    get_v1_financial_connections_sessions_session_execute(builder)
}

/// GET /v1/financial_connections/transactions
/// List Transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_financial_connections_transactions_execute()` to send, or `get_v1_financial_connections_transactions` for simplest API.

pub fn get_v1_financial_connections_transactions_builder(
    client: &SimpleHttpClient,
    account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    transacted_at: Option<&str>,
    transaction_refresh: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/financial_connections/transactions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = account {
        query_parts.push(format!("account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = transacted_at {
        query_parts.push(format!("transacted_at={}", val));
    }
    if let Some(val) = transaction_refresh {
        query_parts.push(format!("transaction_refresh={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/financial_connections/transactions
/// List Transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_financial_connections_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_financial_connections_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_financial_connections_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FinancialConnectionsTransactionsArgs {
    /// Query parameter: account
    pub account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: transacted_at
    pub transacted_at: Option<String>,
    /// Query parameter: transaction_refresh
    pub transaction_refresh: Option<String>,
}

/// GET /v1/financial_connections/transactions
/// List Transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_financial_connections_transactions_builder()` + `get_v1_financial_connections_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_transactions(
    client: &SimpleHttpClient,
    args: &GetV1FinancialConnectionsTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_financial_connections_transactions_builder(
        client,
        args.account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.transacted_at.as_deref(),
        args.transaction_refresh.as_deref(),
    )?;
    get_v1_financial_connections_transactions_execute(builder)
}

/// GET /v1/financial_connections/transactions/{transaction}
/// Retrieve a Transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_financial_connections_transactions_transaction_execute()` to send, or `get_v1_financial_connections_transactions_transaction` for simplest API.

pub fn get_v1_financial_connections_transactions_transaction_builder(
    client: &SimpleHttpClient,
    transaction: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/financial_connections/transactions/{}",
        transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/financial_connections/transactions/{transaction}
/// Retrieve a Transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_financial_connections_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_financial_connections_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsTransaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_financial_connections_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1FinancialConnectionsTransactionsTransactionArgs {
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/financial_connections/transactions/{transaction}
/// Retrieve a Transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_financial_connections_transactions_transaction_builder()` + `get_v1_financial_connections_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_financial_connections_transactions_transaction(
    client: &SimpleHttpClient,
    args: &GetV1FinancialConnectionsTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsTransaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_financial_connections_transactions_transaction_builder(
        client,
        &args.transaction,
        args.expand,
    )?;
    get_v1_financial_connections_transactions_transaction_execute(builder)
}

/// GET /v1/forwarding/requests
/// List all ForwardingRequests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_forwarding_requests_execute()` to send, or `get_v1_forwarding_requests` for simplest API.

pub fn get_v1_forwarding_requests_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/forwarding/requests",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/forwarding/requests
/// List all ForwardingRequests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_forwarding_requests_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_forwarding_requests_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_forwarding_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_forwarding_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ForwardingRequestsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/forwarding/requests
/// List all ForwardingRequests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_forwarding_requests_builder()` + `get_v1_forwarding_requests_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_forwarding_requests(
    client: &SimpleHttpClient,
    args: &GetV1ForwardingRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_forwarding_requests_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_forwarding_requests_execute(builder)
}

/// POST /v1/forwarding/requests
/// Create a ForwardingRequest
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_forwarding_requests_execute()` to send, or `post_v1_forwarding_requests` for simplest API.

pub fn post_v1_forwarding_requests_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/forwarding/requests",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/forwarding/requests
/// Create a ForwardingRequest
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_forwarding_requests_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_forwarding_requests_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_forwarding_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRequest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/forwarding/requests
/// Create a ForwardingRequest
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_forwarding_requests_builder()` + `post_v1_forwarding_requests_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_forwarding_requests(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_forwarding_requests_builder(client)?;
    post_v1_forwarding_requests_execute(builder)
}

/// GET /v1/forwarding/requests/{id}
/// Retrieve a ForwardingRequest
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_forwarding_requests_id_execute()` to send, or `get_v1_forwarding_requests_id` for simplest API.

pub fn get_v1_forwarding_requests_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/forwarding/requests/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/forwarding/requests/{id}
/// Retrieve a ForwardingRequest
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_forwarding_requests_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_forwarding_requests_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_forwarding_requests_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRequest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_forwarding_requests_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ForwardingRequestsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/forwarding/requests/{id}
/// Retrieve a ForwardingRequest
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_forwarding_requests_id_builder()` + `get_v1_forwarding_requests_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_forwarding_requests_id(
    client: &SimpleHttpClient,
    args: &GetV1ForwardingRequestsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRequest>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_forwarding_requests_id_builder(client, &args.id, args.expand)?;
    get_v1_forwarding_requests_id_execute(builder)
}

/// GET /v1/identity/verification_reports
/// List VerificationReports
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_identity_verification_reports_execute()` to send, or `get_v1_identity_verification_reports` for simplest API.

pub fn get_v1_identity_verification_reports_builder(
    client: &SimpleHttpClient,
    client_reference_id: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
    verification_session: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/identity/verification_reports",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_reference_id {
        query_parts.push(format!("client_reference_id={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }
    if let Some(val) = verification_session {
        query_parts.push(format!("verification_session={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/identity/verification_reports
/// List VerificationReports
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_identity_verification_reports_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_identity_verification_reports_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_reports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_identity_verification_reports`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IdentityVerificationReportsArgs {
    /// Query parameter: client_reference_id
    pub client_reference_id: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
    /// Query parameter: verification_session
    pub verification_session: Option<String>,
}

/// GET /v1/identity/verification_reports
/// List VerificationReports
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_identity_verification_reports_builder()` + `get_v1_identity_verification_reports_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_reports(
    client: &SimpleHttpClient,
    args: &GetV1IdentityVerificationReportsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_identity_verification_reports_builder(
        client,
        args.client_reference_id.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
        args.verification_session.as_deref(),
    )?;
    get_v1_identity_verification_reports_execute(builder)
}

/// GET /v1/identity/verification_reports/{report}
/// Retrieve a VerificationReport
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_identity_verification_reports_report_execute()` to send, or `get_v1_identity_verification_reports_report` for simplest API.

pub fn get_v1_identity_verification_reports_report_builder(
    client: &SimpleHttpClient,
    report: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/identity/verification_reports/{}",
        report,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/identity/verification_reports/{report}
/// Retrieve a VerificationReport
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_identity_verification_reports_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_identity_verification_reports_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_reports_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_report>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityVerification_report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_identity_verification_reports_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IdentityVerificationReportsReportArgs {
    /// Path parameter: report
    pub report: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/identity/verification_reports/{report}
/// Retrieve a VerificationReport
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_identity_verification_reports_report_builder()` + `get_v1_identity_verification_reports_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_reports_report(
    client: &SimpleHttpClient,
    args: &GetV1IdentityVerificationReportsReportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_report>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_identity_verification_reports_report_builder(client, &args.report, args.expand)?;
    get_v1_identity_verification_reports_report_execute(builder)
}

/// GET /v1/identity/verification_sessions
/// List VerificationSessions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_identity_verification_sessions_execute()` to send, or `get_v1_identity_verification_sessions` for simplest API.

pub fn get_v1_identity_verification_sessions_builder(
    client: &SimpleHttpClient,
    client_reference_id: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    related_customer: Option<&str>,
    related_customer_account: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/identity/verification_sessions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_reference_id {
        query_parts.push(format!("client_reference_id={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = related_customer {
        query_parts.push(format!("related_customer={}", val));
    }
    if let Some(val) = related_customer_account {
        query_parts.push(format!("related_customer_account={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/identity/verification_sessions
/// List VerificationSessions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_identity_verification_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_identity_verification_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_identity_verification_sessions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IdentityVerificationSessionsArgs {
    /// Query parameter: client_reference_id
    pub client_reference_id: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: related_customer
    pub related_customer: Option<String>,
    /// Query parameter: related_customer_account
    pub related_customer_account: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/identity/verification_sessions
/// List VerificationSessions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_identity_verification_sessions_builder()` + `get_v1_identity_verification_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_sessions(
    client: &SimpleHttpClient,
    args: &GetV1IdentityVerificationSessionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_identity_verification_sessions_builder(
        client,
        args.client_reference_id.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.related_customer.as_deref(),
        args.related_customer_account.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_identity_verification_sessions_execute(builder)
}

/// POST /v1/identity/verification_sessions
/// Create a VerificationSession
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_identity_verification_sessions_execute()` to send, or `post_v1_identity_verification_sessions` for simplest API.

pub fn post_v1_identity_verification_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/identity/verification_sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions
/// Create a VerificationSession
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_identity_verification_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_identity_verification_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityVerification_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/identity/verification_sessions
/// Create a VerificationSession
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_identity_verification_sessions_builder()` + `post_v1_identity_verification_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_identity_verification_sessions_builder(client)?;
    post_v1_identity_verification_sessions_execute(builder)
}

/// GET /v1/identity/verification_sessions/{session}
/// Retrieve a VerificationSession
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_identity_verification_sessions_session_execute()` to send, or `get_v1_identity_verification_sessions_session` for simplest API.

pub fn get_v1_identity_verification_sessions_session_builder(
    client: &SimpleHttpClient,
    session: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{}",
        session,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/identity/verification_sessions/{session}
/// Retrieve a VerificationSession
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_identity_verification_sessions_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_identity_verification_sessions_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_sessions_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityVerification_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_identity_verification_sessions_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IdentityVerificationSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/identity/verification_sessions/{session}
/// Retrieve a VerificationSession
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_identity_verification_sessions_session_builder()` + `get_v1_identity_verification_sessions_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_identity_verification_sessions_session(
    client: &SimpleHttpClient,
    args: &GetV1IdentityVerificationSessionsSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_identity_verification_sessions_session_builder(client, &args.session, args.expand)?;
    get_v1_identity_verification_sessions_session_execute(builder)
}

/// POST /v1/identity/verification_sessions/{session}
/// Update a VerificationSession
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_identity_verification_sessions_session_execute()` to send, or `post_v1_identity_verification_sessions_session` for simplest API.

pub fn post_v1_identity_verification_sessions_session_builder(
    client: &SimpleHttpClient,
    session: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{}",
        session,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions/{session}
/// Update a VerificationSession
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_identity_verification_sessions_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_identity_verification_sessions_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityVerification_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_identity_verification_sessions_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IdentityVerificationSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST /v1/identity/verification_sessions/{session}
/// Update a VerificationSession
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_identity_verification_sessions_session_builder()` + `post_v1_identity_verification_sessions_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_session(
    client: &SimpleHttpClient,
    args: &PostV1IdentityVerificationSessionsSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_identity_verification_sessions_session_builder(client, &args.session)?;
    post_v1_identity_verification_sessions_session_execute(builder)
}

/// POST /v1/identity/verification_sessions/{session}/cancel
/// Cancel a VerificationSession
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_identity_verification_sessions_session_cancel_execute()` to send, or `post_v1_identity_verification_sessions_session_cancel` for simplest API.

pub fn post_v1_identity_verification_sessions_session_cancel_builder(
    client: &SimpleHttpClient,
    session: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{}/cancel",
        session,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions/{session}/cancel
/// Cancel a VerificationSession
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_identity_verification_sessions_session_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_identity_verification_sessions_session_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_session_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityVerification_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_identity_verification_sessions_session_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IdentityVerificationSessionsSessionCancelArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST /v1/identity/verification_sessions/{session}/cancel
/// Cancel a VerificationSession
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_identity_verification_sessions_session_cancel_builder()` + `post_v1_identity_verification_sessions_session_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_session_cancel(
    client: &SimpleHttpClient,
    args: &PostV1IdentityVerificationSessionsSessionCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_identity_verification_sessions_session_cancel_builder(client, &args.session)?;
    post_v1_identity_verification_sessions_session_cancel_execute(builder)
}

/// POST /v1/identity/verification_sessions/{session}/redact
/// Redact a VerificationSession
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_identity_verification_sessions_session_redact_execute()` to send, or `post_v1_identity_verification_sessions_session_redact` for simplest API.

pub fn post_v1_identity_verification_sessions_session_redact_builder(
    client: &SimpleHttpClient,
    session: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{}/redact",
        session,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions/{session}/redact
/// Redact a VerificationSession
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_identity_verification_sessions_session_redact_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_identity_verification_sessions_session_redact_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_session_redact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityVerification_session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_identity_verification_sessions_session_redact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IdentityVerificationSessionsSessionRedactArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST /v1/identity/verification_sessions/{session}/redact
/// Redact a VerificationSession
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_identity_verification_sessions_session_redact_builder()` + `post_v1_identity_verification_sessions_session_redact_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_identity_verification_sessions_session_redact(
    client: &SimpleHttpClient,
    args: &PostV1IdentityVerificationSessionsSessionRedactArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerification_session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_identity_verification_sessions_session_redact_builder(client, &args.session)?;
    post_v1_identity_verification_sessions_session_redact_execute(builder)
}

/// GET /v1/invoice_payments
/// List all payments for an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoice_payments_execute()` to send, or `get_v1_invoice_payments` for simplest API.

pub fn get_v1_invoice_payments_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    invoice: Option<&str>,
    limit: Option<i32>,
    payment: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoice_payments",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = invoice {
        query_parts.push(format!("invoice={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment {
        query_parts.push(format!("payment={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoice_payments
/// List all payments for an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoice_payments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoice_payments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_payments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoice_payments`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoicePaymentsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment
    pub payment: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/invoice_payments
/// List all payments for an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoice_payments_builder()` + `get_v1_invoice_payments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_payments(
    client: &SimpleHttpClient,
    args: &GetV1InvoicePaymentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoice_payments_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.invoice.as_deref(),
        args.limit,
        args.payment.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_invoice_payments_execute(builder)
}

/// GET /v1/invoice_payments/{invoice_payment}
/// Retrieve an InvoicePayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoice_payments_invoice_payment_execute()` to send, or `get_v1_invoice_payments_invoice_payment` for simplest API.

pub fn get_v1_invoice_payments_invoice_payment_builder(
    client: &SimpleHttpClient,
    invoice_payment: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoice_payments/{}",
        invoice_payment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoice_payments/{invoice_payment}
/// Retrieve an InvoicePayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoice_payments_invoice_payment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoice_payments_invoice_payment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_payments_invoice_payment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoice_payments_invoice_payment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoicePaymentsInvoicePaymentArgs {
    /// Path parameter: invoice_payment
    pub invoice_payment: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/invoice_payments/{invoice_payment}
/// Retrieve an InvoicePayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoice_payments_invoice_payment_builder()` + `get_v1_invoice_payments_invoice_payment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_payments_invoice_payment(
    client: &SimpleHttpClient,
    args: &GetV1InvoicePaymentsInvoicePaymentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_invoice_payments_invoice_payment_builder(
        client,
        &args.invoice_payment,
        args.expand,
    )?;
    get_v1_invoice_payments_invoice_payment_execute(builder)
}

/// GET /v1/invoice_rendering_templates
/// List all invoice rendering templates
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoice_rendering_templates_execute()` to send, or `get_v1_invoice_rendering_templates` for simplest API.

pub fn get_v1_invoice_rendering_templates_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoice_rendering_templates",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoice_rendering_templates
/// List all invoice rendering templates
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoice_rendering_templates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoice_rendering_templates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_rendering_templates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoice_rendering_templates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoiceRenderingTemplatesArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/invoice_rendering_templates
/// List all invoice rendering templates
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoice_rendering_templates_builder()` + `get_v1_invoice_rendering_templates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_rendering_templates(
    client: &SimpleHttpClient,
    args: &GetV1InvoiceRenderingTemplatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoice_rendering_templates_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_invoice_rendering_templates_execute(builder)
}

/// GET /v1/invoice_rendering_templates/{template}
/// Retrieve an invoice rendering template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoice_rendering_templates_template_execute()` to send, or `get_v1_invoice_rendering_templates_template` for simplest API.

pub fn get_v1_invoice_rendering_templates_template_builder(
    client: &SimpleHttpClient,
    template: &str,
    expand: Option<Vec<String>>,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoice_rendering_templates/{}",
        template,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = version {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoice_rendering_templates/{template}
/// Retrieve an invoice rendering template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoice_rendering_templates_template_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoice_rendering_templates_template_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_rendering_templates_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Invoice_rendering_template>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice_rendering_template = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoice_rendering_templates_template`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoiceRenderingTemplatesTemplateArgs {
    /// Path parameter: template
    pub template: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: version
    pub version: Option<i32>,
}

/// GET /v1/invoice_rendering_templates/{template}
/// Retrieve an invoice rendering template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoice_rendering_templates_template_builder()` + `get_v1_invoice_rendering_templates_template_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoice_rendering_templates_template(
    client: &SimpleHttpClient,
    args: &GetV1InvoiceRenderingTemplatesTemplateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Invoice_rendering_template>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_invoice_rendering_templates_template_builder(
        client,
        &args.template,
        args.expand,
        args.version,
    )?;
    get_v1_invoice_rendering_templates_template_execute(builder)
}

/// POST /v1/invoice_rendering_templates/{template}/archive
/// Archive an invoice rendering template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoice_rendering_templates_template_archive_execute()` to send, or `post_v1_invoice_rendering_templates_template_archive` for simplest API.

pub fn post_v1_invoice_rendering_templates_template_archive_builder(
    client: &SimpleHttpClient,
    template: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoice_rendering_templates/{}/archive",
        template,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoice_rendering_templates/{template}/archive
/// Archive an invoice rendering template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoice_rendering_templates_template_archive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoice_rendering_templates_template_archive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoice_rendering_templates_template_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Invoice_rendering_template>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice_rendering_template = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoice_rendering_templates_template_archive`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoiceRenderingTemplatesTemplateArchiveArgs {
    /// Path parameter: template
    pub template: String,
}

/// POST /v1/invoice_rendering_templates/{template}/archive
/// Archive an invoice rendering template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoice_rendering_templates_template_archive_builder()` + `post_v1_invoice_rendering_templates_template_archive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoice_rendering_templates_template_archive(
    client: &SimpleHttpClient,
    args: &PostV1InvoiceRenderingTemplatesTemplateArchiveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Invoice_rendering_template>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_invoice_rendering_templates_template_archive_builder(client, &args.template)?;
    post_v1_invoice_rendering_templates_template_archive_execute(builder)
}

/// POST /v1/invoice_rendering_templates/{template}/unarchive
/// Unarchive an invoice rendering template
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoice_rendering_templates_template_unarchive_execute()` to send, or `post_v1_invoice_rendering_templates_template_unarchive` for simplest API.

pub fn post_v1_invoice_rendering_templates_template_unarchive_builder(
    client: &SimpleHttpClient,
    template: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoice_rendering_templates/{}/unarchive",
        template,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoice_rendering_templates/{template}/unarchive
/// Unarchive an invoice rendering template
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoice_rendering_templates_template_unarchive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoice_rendering_templates_template_unarchive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoice_rendering_templates_template_unarchive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Invoice_rendering_template>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice_rendering_template = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoice_rendering_templates_template_unarchive`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoiceRenderingTemplatesTemplateUnarchiveArgs {
    /// Path parameter: template
    pub template: String,
}

/// POST /v1/invoice_rendering_templates/{template}/unarchive
/// Unarchive an invoice rendering template
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoice_rendering_templates_template_unarchive_builder()` + `post_v1_invoice_rendering_templates_template_unarchive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoice_rendering_templates_template_unarchive(
    client: &SimpleHttpClient,
    args: &PostV1InvoiceRenderingTemplatesTemplateUnarchiveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Invoice_rendering_template>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_invoice_rendering_templates_template_unarchive_builder(client, &args.template)?;
    post_v1_invoice_rendering_templates_template_unarchive_execute(builder)
}

/// GET /v1/invoiceitems
/// List all invoice items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoiceitems_execute()` to send, or `get_v1_invoiceitems` for simplest API.

pub fn get_v1_invoiceitems_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    invoice: Option<&str>,
    limit: Option<i32>,
    pending: Option<bool>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoiceitems",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = invoice {
        query_parts.push(format!("invoice={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = pending {
        query_parts.push(format!("pending={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoiceitems
/// List all invoice items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoiceitems_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoiceitems_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoiceitems_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoiceitems`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoiceitemsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: invoice
    pub invoice: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: pending
    pub pending: Option<bool>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/invoiceitems
/// List all invoice items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoiceitems_builder()` + `get_v1_invoiceitems_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoiceitems(
    client: &SimpleHttpClient,
    args: &GetV1InvoiceitemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoiceitems_builder(
        client,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.invoice.as_deref(),
        args.limit,
        args.pending,
        args.starting_after.as_deref(),
    )?;
    get_v1_invoiceitems_execute(builder)
}

/// POST /v1/invoiceitems
/// Create an invoice item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoiceitems_execute()` to send, or `post_v1_invoiceitems` for simplest API.

pub fn post_v1_invoiceitems_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoiceitems",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoiceitems
/// Create an invoice item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoiceitems_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoiceitems_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoiceitems_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoiceitems
/// Create an invoice item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoiceitems_builder()` + `post_v1_invoiceitems_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoiceitems(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoiceitems_builder(client)?;
    post_v1_invoiceitems_execute(builder)
}

/// GET /v1/invoiceitems/{invoiceitem}
/// Retrieve an invoice item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoiceitems_invoiceitem_execute()` to send, or `get_v1_invoiceitems_invoiceitem` for simplest API.

pub fn get_v1_invoiceitems_invoiceitem_builder(
    client: &SimpleHttpClient,
    invoiceitem: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoiceitems/{}", invoiceitem,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoiceitems/{invoiceitem}
/// Retrieve an invoice item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoiceitems_invoiceitem_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoiceitems_invoiceitem_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoiceitems_invoiceitem_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoiceitems_invoiceitem`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoiceitemsInvoiceitemArgs {
    /// Path parameter: invoiceitem
    pub invoiceitem: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/invoiceitems/{invoiceitem}
/// Retrieve an invoice item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoiceitems_invoiceitem_builder()` + `get_v1_invoiceitems_invoiceitem_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoiceitems_invoiceitem(
    client: &SimpleHttpClient,
    args: &GetV1InvoiceitemsInvoiceitemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoiceitems_invoiceitem_builder(client, &args.invoiceitem, args.expand)?;
    get_v1_invoiceitems_invoiceitem_execute(builder)
}

/// POST /v1/invoiceitems/{invoiceitem}
/// Update an invoice item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoiceitems_invoiceitem_execute()` to send, or `post_v1_invoiceitems_invoiceitem` for simplest API.

pub fn post_v1_invoiceitems_invoiceitem_builder(
    client: &SimpleHttpClient,
    invoiceitem: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoiceitems/{}", invoiceitem,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoiceitems/{invoiceitem}
/// Update an invoice item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoiceitems_invoiceitem_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoiceitems_invoiceitem_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoiceitems_invoiceitem_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoiceitems_invoiceitem`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoiceitemsInvoiceitemArgs {
    /// Path parameter: invoiceitem
    pub invoiceitem: String,
}

/// POST /v1/invoiceitems/{invoiceitem}
/// Update an invoice item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoiceitems_invoiceitem_builder()` + `post_v1_invoiceitems_invoiceitem_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoiceitems_invoiceitem(
    client: &SimpleHttpClient,
    args: &PostV1InvoiceitemsInvoiceitemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoiceitem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoiceitems_invoiceitem_builder(client, &args.invoiceitem)?;
    post_v1_invoiceitems_invoiceitem_execute(builder)
}

/// DELETE /v1/invoiceitems/{invoiceitem}
/// Delete an invoice item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_invoiceitems_invoiceitem_execute()` to send, or `delete_v1_invoiceitems_invoiceitem` for simplest API.

pub fn delete_v1_invoiceitems_invoiceitem_builder(
    client: &SimpleHttpClient,
    invoiceitem: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoiceitems/{}", invoiceitem,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/invoiceitems/{invoiceitem}
/// Delete an invoice item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_invoiceitems_invoiceitem_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_invoiceitems_invoiceitem_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_invoiceitems_invoiceitem_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_invoiceitem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_invoiceitem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_invoiceitems_invoiceitem`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1InvoiceitemsInvoiceitemArgs {
    /// Path parameter: invoiceitem
    pub invoiceitem: String,
}

/// DELETE /v1/invoiceitems/{invoiceitem}
/// Delete an invoice item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_invoiceitems_invoiceitem_builder()` + `delete_v1_invoiceitems_invoiceitem_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_invoiceitems_invoiceitem(
    client: &SimpleHttpClient,
    args: &DeleteV1InvoiceitemsInvoiceitemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_invoiceitem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_invoiceitems_invoiceitem_builder(client, &args.invoiceitem)?;
    delete_v1_invoiceitems_invoiceitem_execute(builder)
}

/// GET /v1/invoices
/// List all invoices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoices_execute()` to send, or `get_v1_invoices` for simplest API.

pub fn get_v1_invoices_builder(
    client: &SimpleHttpClient,
    collection_method: Option<&str>,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    due_date: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
    subscription: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = collection_method {
        query_parts.push(format!("collection_method={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = due_date {
        query_parts.push(format!("due_date={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = subscription {
        query_parts.push(format!("subscription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices
/// List all invoices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoicesArgs {
    /// Query parameter: collection_method
    pub collection_method: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: due_date
    pub due_date: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: subscription
    pub subscription: Option<String>,
}

/// GET /v1/invoices
/// List all invoices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoices_builder()` + `get_v1_invoices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices(
    client: &SimpleHttpClient,
    args: &GetV1InvoicesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoices_builder(
        client,
        args.collection_method.as_deref(),
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.due_date.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.subscription.as_deref(),
    )?;
    get_v1_invoices_execute(builder)
}

/// POST /v1/invoices
/// Create an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_execute()` to send, or `post_v1_invoices` for simplest API.

pub fn post_v1_invoices_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices
/// Create an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoices
/// Create an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_builder()` + `post_v1_invoices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_builder(client)?;
    post_v1_invoices_execute(builder)
}

/// POST /v1/invoices/create_preview
/// Create a preview invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_create_preview_execute()` to send, or `post_v1_invoices_create_preview` for simplest API.

pub fn post_v1_invoices_create_preview_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/create_preview",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/create_preview
/// Create a preview invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_create_preview_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_create_preview_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_create_preview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/invoices/create_preview
/// Create a preview invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_create_preview_builder()` + `post_v1_invoices_create_preview_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_create_preview(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_create_preview_builder(client)?;
    post_v1_invoices_create_preview_execute(builder)
}

/// GET /v1/invoices/search
/// Search invoices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoices_search_execute()` to send, or `get_v1_invoices_search` for simplest API.

pub fn get_v1_invoices_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices/search
/// Search invoices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoices_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoices_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoices_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoicesSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/invoices/search
/// Search invoices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoices_search_builder()` + `get_v1_invoices_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_search(
    client: &SimpleHttpClient,
    args: &GetV1InvoicesSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoices_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_invoices_search_execute(builder)
}

/// GET /v1/invoices/{invoice}
/// Retrieve an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoices_invoice_execute()` to send, or `get_v1_invoices_invoice` for simplest API.

pub fn get_v1_invoices_invoice_builder(
    client: &SimpleHttpClient,
    invoice: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}", invoice,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices/{invoice}
/// Retrieve an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoices_invoice_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoices_invoice_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_invoice_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoices_invoice`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoicesInvoiceArgs {
    /// Path parameter: invoice
    pub invoice: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/invoices/{invoice}
/// Retrieve an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoices_invoice_builder()` + `get_v1_invoices_invoice_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_invoice(
    client: &SimpleHttpClient,
    args: &GetV1InvoicesInvoiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoices_invoice_builder(client, &args.invoice, args.expand)?;
    get_v1_invoices_invoice_execute(builder)
}

/// POST /v1/invoices/{invoice}
/// Update an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_execute()` to send, or `post_v1_invoices_invoice` for simplest API.

pub fn post_v1_invoices_invoice_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}", invoice,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}
/// Update an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}
/// Update an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_builder()` + `post_v1_invoices_invoice_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_execute(builder)
}

/// DELETE /v1/invoices/{invoice}
/// Delete a draft invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_invoices_invoice_execute()` to send, or `delete_v1_invoices_invoice` for simplest API.

pub fn delete_v1_invoices_invoice_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}", invoice,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/invoices/{invoice}
/// Delete a draft invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_invoices_invoice_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_invoices_invoice_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_invoices_invoice_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_invoice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_invoices_invoice`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1InvoicesInvoiceArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// DELETE /v1/invoices/{invoice}
/// Delete a draft invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_invoices_invoice_builder()` + `delete_v1_invoices_invoice_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_invoices_invoice(
    client: &SimpleHttpClient,
    args: &DeleteV1InvoicesInvoiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_invoice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_invoices_invoice_builder(client, &args.invoice)?;
    delete_v1_invoices_invoice_execute(builder)
}

/// POST /v1/invoices/{invoice}/add_lines
/// Bulk add invoice line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_add_lines_execute()` to send, or `post_v1_invoices_invoice_add_lines` for simplest API.

pub fn post_v1_invoices_invoice_add_lines_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}/add_lines", invoice,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/add_lines
/// Bulk add invoice line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_add_lines_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_add_lines_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_add_lines_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_add_lines`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceAddLinesArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/add_lines
/// Bulk add invoice line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_add_lines_builder()` + `post_v1_invoices_invoice_add_lines_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_add_lines(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceAddLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_add_lines_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_add_lines_execute(builder)
}

/// POST /v1/invoices/{invoice}/attach_payment
/// Attach a payment to an Invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_attach_payment_execute()` to send, or `post_v1_invoices_invoice_attach_payment` for simplest API.

pub fn post_v1_invoices_invoice_attach_payment_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoices/{}/attach_payment",
        invoice,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/attach_payment
/// Attach a payment to an Invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_attach_payment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_attach_payment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_attach_payment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_attach_payment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceAttachPaymentArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/attach_payment
/// Attach a payment to an Invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_attach_payment_builder()` + `post_v1_invoices_invoice_attach_payment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_attach_payment(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceAttachPaymentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_attach_payment_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_attach_payment_execute(builder)
}

/// POST /v1/invoices/{invoice}/finalize
/// Finalize an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_finalize_execute()` to send, or `post_v1_invoices_invoice_finalize` for simplest API.

pub fn post_v1_invoices_invoice_finalize_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}/finalize", invoice,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/finalize
/// Finalize an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_finalize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_finalize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_finalize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_finalize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceFinalizeArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/finalize
/// Finalize an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_finalize_builder()` + `post_v1_invoices_invoice_finalize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_finalize(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceFinalizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_finalize_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_finalize_execute(builder)
}

/// GET /v1/invoices/{invoice}/lines
/// Retrieve an invoice's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_invoices_invoice_lines_execute()` to send, or `get_v1_invoices_invoice_lines` for simplest API.

pub fn get_v1_invoices_invoice_lines_builder(
    client: &SimpleHttpClient,
    invoice: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}/lines", invoice,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/invoices/{invoice}/lines
/// Retrieve an invoice's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_invoices_invoice_lines_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_invoices_invoice_lines_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_invoice_lines_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_invoices_invoice_lines`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1InvoicesInvoiceLinesArgs {
    /// Path parameter: invoice
    pub invoice: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/invoices/{invoice}/lines
/// Retrieve an invoice's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_invoices_invoice_lines_builder()` + `get_v1_invoices_invoice_lines_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_invoices_invoice_lines(
    client: &SimpleHttpClient,
    args: &GetV1InvoicesInvoiceLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_invoices_invoice_lines_builder(
        client,
        &args.invoice,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_invoices_invoice_lines_execute(builder)
}

/// POST /v1/invoices/{invoice}/lines/{line_item_id}
/// Update an invoice's line item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_lines_line_item_id_execute()` to send, or `post_v1_invoices_invoice_lines_line_item_id` for simplest API.

pub fn post_v1_invoices_invoice_lines_line_item_id_builder(
    client: &SimpleHttpClient,
    invoice: &str,
    line_item_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoices/{}/lines/{}",
        invoice, line_item_id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/lines/{line_item_id}
/// Update an invoice's line item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_lines_line_item_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_lines_line_item_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_lines_line_item_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Line_item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Line_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_lines_line_item_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceLinesLineItemIdArgs {
    /// Path parameter: invoice
    pub invoice: String,
    /// Path parameter: line_item_id
    pub line_item_id: String,
}

/// POST /v1/invoices/{invoice}/lines/{line_item_id}
/// Update an invoice's line item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_lines_line_item_id_builder()` + `post_v1_invoices_invoice_lines_line_item_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_lines_line_item_id(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceLinesLineItemIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Line_item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_lines_line_item_id_builder(
        client,
        &args.invoice,
        &args.line_item_id,
    )?;
    post_v1_invoices_invoice_lines_line_item_id_execute(builder)
}

/// POST /v1/invoices/{invoice}/mark_uncollectible
/// Mark an invoice as uncollectible
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_mark_uncollectible_execute()` to send, or `post_v1_invoices_invoice_mark_uncollectible` for simplest API.

pub fn post_v1_invoices_invoice_mark_uncollectible_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoices/{}/mark_uncollectible",
        invoice,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/mark_uncollectible
/// Mark an invoice as uncollectible
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_mark_uncollectible_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_mark_uncollectible_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_mark_uncollectible_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_mark_uncollectible`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceMarkUncollectibleArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/mark_uncollectible
/// Mark an invoice as uncollectible
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_mark_uncollectible_builder()` + `post_v1_invoices_invoice_mark_uncollectible_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_mark_uncollectible(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceMarkUncollectibleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_mark_uncollectible_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_mark_uncollectible_execute(builder)
}

/// POST /v1/invoices/{invoice}/pay
/// Pay an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_pay_execute()` to send, or `post_v1_invoices_invoice_pay` for simplest API.

pub fn post_v1_invoices_invoice_pay_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}/pay", invoice,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/pay
/// Pay an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_pay_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_pay_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_pay_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_pay`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoicePayArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/pay
/// Pay an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_pay_builder()` + `post_v1_invoices_invoice_pay_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_pay(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoicePayArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_pay_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_pay_execute(builder)
}

/// POST /v1/invoices/{invoice}/remove_lines
/// Bulk remove invoice line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_remove_lines_execute()` to send, or `post_v1_invoices_invoice_remove_lines` for simplest API.

pub fn post_v1_invoices_invoice_remove_lines_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoices/{}/remove_lines",
        invoice,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/remove_lines
/// Bulk remove invoice line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_remove_lines_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_remove_lines_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_remove_lines_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_remove_lines`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceRemoveLinesArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/remove_lines
/// Bulk remove invoice line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_remove_lines_builder()` + `post_v1_invoices_invoice_remove_lines_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_remove_lines(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceRemoveLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_remove_lines_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_remove_lines_execute(builder)
}

/// POST /v1/invoices/{invoice}/send
/// Send an invoice for manual payment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_send_execute()` to send, or `post_v1_invoices_invoice_send` for simplest API.

pub fn post_v1_invoices_invoice_send_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}/send", invoice,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/send
/// Send an invoice for manual payment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_send_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_send_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_send_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_send`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceSendArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/send
/// Send an invoice for manual payment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_send_builder()` + `post_v1_invoices_invoice_send_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_send(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceSendArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_send_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_send_execute(builder)
}

/// POST /v1/invoices/{invoice}/update_lines
/// Bulk update invoice line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_update_lines_execute()` to send, or `post_v1_invoices_invoice_update_lines` for simplest API.

pub fn post_v1_invoices_invoice_update_lines_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/invoices/{}/update_lines",
        invoice,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/update_lines
/// Bulk update invoice line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_update_lines_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_update_lines_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_update_lines_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_update_lines`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceUpdateLinesArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/update_lines
/// Bulk update invoice line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_update_lines_builder()` + `post_v1_invoices_invoice_update_lines_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_update_lines(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceUpdateLinesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_update_lines_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_update_lines_execute(builder)
}

/// POST /v1/invoices/{invoice}/void
/// Void an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_invoices_invoice_void_execute()` to send, or `post_v1_invoices_invoice_void` for simplest API.

pub fn post_v1_invoices_invoice_void_builder(
    client: &SimpleHttpClient,
    invoice: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/invoices/{}/void", invoice,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/invoices/{invoice}/void
/// Void an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_invoices_invoice_void_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_invoices_invoice_void_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_void_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invoice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_invoices_invoice_void`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1InvoicesInvoiceVoidArgs {
    /// Path parameter: invoice
    pub invoice: String,
}

/// POST /v1/invoices/{invoice}/void
/// Void an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_invoices_invoice_void_builder()` + `post_v1_invoices_invoice_void_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_invoices_invoice_void(
    client: &SimpleHttpClient,
    args: &PostV1InvoicesInvoiceVoidArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invoice>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_invoices_invoice_void_builder(client, &args.invoice)?;
    post_v1_invoices_invoice_void_execute(builder)
}

/// GET /v1/issuing/authorizations
/// List all authorizations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_authorizations_execute()` to send, or `get_v1_issuing_authorizations` for simplest API.

pub fn get_v1_issuing_authorizations_builder(
    client: &SimpleHttpClient,
    card: Option<&str>,
    cardholder: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/authorizations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = card {
        query_parts.push(format!("card={}", val));
    }
    if let Some(val) = cardholder {
        query_parts.push(format!("cardholder={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/authorizations
/// List all authorizations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_authorizations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_authorizations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_authorizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_authorizations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingAuthorizationsArgs {
    /// Query parameter: card
    pub card: Option<String>,
    /// Query parameter: cardholder
    pub cardholder: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/issuing/authorizations
/// List all authorizations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_authorizations_builder()` + `get_v1_issuing_authorizations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_authorizations(
    client: &SimpleHttpClient,
    args: &GetV1IssuingAuthorizationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_authorizations_builder(
        client,
        args.card.as_deref(),
        args.cardholder.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_issuing_authorizations_execute(builder)
}

/// GET /v1/issuing/authorizations/{authorization}
/// Retrieve an authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_authorizations_authorization_execute()` to send, or `get_v1_issuing_authorizations_authorization` for simplest API.

pub fn get_v1_issuing_authorizations_authorization_builder(
    client: &SimpleHttpClient,
    authorization: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/authorizations/{}",
        authorization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/authorizations/{authorization}
/// Retrieve an authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_authorizations_authorization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_authorizations_authorization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_authorizations_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_authorizations_authorization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingAuthorizationsAuthorizationArgs {
    /// Path parameter: authorization
    pub authorization: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/authorizations/{authorization}
/// Retrieve an authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_authorizations_authorization_builder()` + `get_v1_issuing_authorizations_authorization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_authorizations_authorization(
    client: &SimpleHttpClient,
    args: &GetV1IssuingAuthorizationsAuthorizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_authorizations_authorization_builder(
        client,
        &args.authorization,
        args.expand,
    )?;
    get_v1_issuing_authorizations_authorization_execute(builder)
}

/// POST /v1/issuing/authorizations/{authorization}
/// Update an authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_authorizations_authorization_execute()` to send, or `post_v1_issuing_authorizations_authorization` for simplest API.

pub fn post_v1_issuing_authorizations_authorization_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/authorizations/{}",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/authorizations/{authorization}
/// Update an authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_authorizations_authorization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_authorizations_authorization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_authorizations_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_authorizations_authorization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingAuthorizationsAuthorizationArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/issuing/authorizations/{authorization}
/// Update an authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_authorizations_authorization_builder()` + `post_v1_issuing_authorizations_authorization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_authorizations_authorization(
    client: &SimpleHttpClient,
    args: &PostV1IssuingAuthorizationsAuthorizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_issuing_authorizations_authorization_builder(client, &args.authorization)?;
    post_v1_issuing_authorizations_authorization_execute(builder)
}

/// POST /v1/issuing/authorizations/{authorization}/approve
/// Approve an authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_authorizations_authorization_approve_execute()` to send, or `post_v1_issuing_authorizations_authorization_approve` for simplest API.

pub fn post_v1_issuing_authorizations_authorization_approve_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/authorizations/{}/approve",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/authorizations/{authorization}/approve
/// Approve an authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_authorizations_authorization_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_authorizations_authorization_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_authorizations_authorization_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_authorizations_authorization_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingAuthorizationsAuthorizationApproveArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/issuing/authorizations/{authorization}/approve
/// Approve an authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_authorizations_authorization_approve_builder()` + `post_v1_issuing_authorizations_authorization_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_authorizations_authorization_approve(
    client: &SimpleHttpClient,
    args: &PostV1IssuingAuthorizationsAuthorizationApproveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_issuing_authorizations_authorization_approve_builder(client, &args.authorization)?;
    post_v1_issuing_authorizations_authorization_approve_execute(builder)
}

/// POST /v1/issuing/authorizations/{authorization}/decline
/// Decline an authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_authorizations_authorization_decline_execute()` to send, or `post_v1_issuing_authorizations_authorization_decline` for simplest API.

pub fn post_v1_issuing_authorizations_authorization_decline_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/authorizations/{}/decline",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/authorizations/{authorization}/decline
/// Decline an authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_authorizations_authorization_decline_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_authorizations_authorization_decline_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_authorizations_authorization_decline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_authorizations_authorization_decline`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingAuthorizationsAuthorizationDeclineArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/issuing/authorizations/{authorization}/decline
/// Decline an authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_authorizations_authorization_decline_builder()` + `post_v1_issuing_authorizations_authorization_decline_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_authorizations_authorization_decline(
    client: &SimpleHttpClient,
    args: &PostV1IssuingAuthorizationsAuthorizationDeclineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_issuing_authorizations_authorization_decline_builder(client, &args.authorization)?;
    post_v1_issuing_authorizations_authorization_decline_execute(builder)
}

/// GET /v1/issuing/cardholders
/// List all cardholders
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_cardholders_execute()` to send, or `get_v1_issuing_cardholders` for simplest API.

pub fn get_v1_issuing_cardholders_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    email: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    phone_number: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/cardholders",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = email {
        query_parts.push(format!("email={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = phone_number {
        query_parts.push(format!("phone_number={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/cardholders
/// List all cardholders
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_cardholders_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_cardholders_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cardholders_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_cardholders`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingCardholdersArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: email
    pub email: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: phone_number
    pub phone_number: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/issuing/cardholders
/// List all cardholders
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_cardholders_builder()` + `get_v1_issuing_cardholders_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cardholders(
    client: &SimpleHttpClient,
    args: &GetV1IssuingCardholdersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_cardholders_builder(
        client,
        args.created.as_deref(),
        args.email.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.phone_number.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_issuing_cardholders_execute(builder)
}

/// POST /v1/issuing/cardholders
/// Create a cardholder
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_cardholders_execute()` to send, or `post_v1_issuing_cardholders` for simplest API.

pub fn post_v1_issuing_cardholders_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/cardholders",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/cardholders
/// Create a cardholder
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_cardholders_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_cardholders_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cardholders_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCardholder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/cardholders
/// Create a cardholder
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_cardholders_builder()` + `post_v1_issuing_cardholders_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cardholders(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_cardholders_builder(client)?;
    post_v1_issuing_cardholders_execute(builder)
}

/// GET /v1/issuing/cardholders/{cardholder}
/// Retrieve a cardholder
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_cardholders_cardholder_execute()` to send, or `get_v1_issuing_cardholders_cardholder` for simplest API.

pub fn get_v1_issuing_cardholders_cardholder_builder(
    client: &SimpleHttpClient,
    cardholder: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/cardholders/{}",
        cardholder,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/cardholders/{cardholder}
/// Retrieve a cardholder
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_cardholders_cardholder_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_cardholders_cardholder_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cardholders_cardholder_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCardholder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_cardholders_cardholder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingCardholdersCardholderArgs {
    /// Path parameter: cardholder
    pub cardholder: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/cardholders/{cardholder}
/// Retrieve a cardholder
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_cardholders_cardholder_builder()` + `get_v1_issuing_cardholders_cardholder_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cardholders_cardholder(
    client: &SimpleHttpClient,
    args: &GetV1IssuingCardholdersCardholderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_issuing_cardholders_cardholder_builder(client, &args.cardholder, args.expand)?;
    get_v1_issuing_cardholders_cardholder_execute(builder)
}

/// POST /v1/issuing/cardholders/{cardholder}
/// Update a cardholder
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_cardholders_cardholder_execute()` to send, or `post_v1_issuing_cardholders_cardholder` for simplest API.

pub fn post_v1_issuing_cardholders_cardholder_builder(
    client: &SimpleHttpClient,
    cardholder: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/cardholders/{}",
        cardholder,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/cardholders/{cardholder}
/// Update a cardholder
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_cardholders_cardholder_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_cardholders_cardholder_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cardholders_cardholder_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCardholder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_cardholders_cardholder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingCardholdersCardholderArgs {
    /// Path parameter: cardholder
    pub cardholder: String,
}

/// POST /v1/issuing/cardholders/{cardholder}
/// Update a cardholder
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_cardholders_cardholder_builder()` + `post_v1_issuing_cardholders_cardholder_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cardholders_cardholder(
    client: &SimpleHttpClient,
    args: &PostV1IssuingCardholdersCardholderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCardholder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_cardholders_cardholder_builder(client, &args.cardholder)?;
    post_v1_issuing_cardholders_cardholder_execute(builder)
}

/// GET /v1/issuing/cards
/// List all cards
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_cards_execute()` to send, or `get_v1_issuing_cards` for simplest API.

pub fn get_v1_issuing_cards_builder(
    client: &SimpleHttpClient,
    cardholder: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    exp_month: Option<i32>,
    exp_year: Option<i32>,
    expand: Option<Vec<String>>,
    last4: Option<&str>,
    limit: Option<i32>,
    personalization_design: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/cards",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cardholder {
        query_parts.push(format!("cardholder={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = exp_month {
        query_parts.push(format!("exp_month={}", val));
    }
    if let Some(val) = exp_year {
        query_parts.push(format!("exp_year={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = last4 {
        query_parts.push(format!("last4={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = personalization_design {
        query_parts.push(format!("personalization_design={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/cards
/// List all cards
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_cards_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_cards_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cards_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_cards`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingCardsArgs {
    /// Query parameter: cardholder
    pub cardholder: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: exp_month
    pub exp_month: Option<i32>,
    /// Query parameter: exp_year
    pub exp_year: Option<i32>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: last4
    pub last4: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: personalization_design
    pub personalization_design: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/issuing/cards
/// List all cards
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_cards_builder()` + `get_v1_issuing_cards_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cards(
    client: &SimpleHttpClient,
    args: &GetV1IssuingCardsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_cards_builder(
        client,
        args.cardholder.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.exp_month,
        args.exp_year,
        args.expand,
        args.last4.as_deref(),
        args.limit,
        args.personalization_design.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_issuing_cards_execute(builder)
}

/// POST /v1/issuing/cards
/// Create a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_cards_execute()` to send, or `post_v1_issuing_cards` for simplest API.

pub fn post_v1_issuing_cards_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/cards",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/cards
/// Create a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_cards_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_cards_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cards_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/cards
/// Create a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_cards_builder()` + `post_v1_issuing_cards_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cards(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_cards_builder(client)?;
    post_v1_issuing_cards_execute(builder)
}

/// GET /v1/issuing/cards/{card}
/// Retrieve a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_cards_card_execute()` to send, or `get_v1_issuing_cards_card` for simplest API.

pub fn get_v1_issuing_cards_card_builder(
    client: &SimpleHttpClient,
    card: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/cards/{}", card,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/cards/{card}
/// Retrieve a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_cards_card_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_cards_card_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cards_card_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_cards_card`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingCardsCardArgs {
    /// Path parameter: card
    pub card: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/cards/{card}
/// Retrieve a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_cards_card_builder()` + `get_v1_issuing_cards_card_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_cards_card(
    client: &SimpleHttpClient,
    args: &GetV1IssuingCardsCardArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_cards_card_builder(client, &args.card, args.expand)?;
    get_v1_issuing_cards_card_execute(builder)
}

/// POST /v1/issuing/cards/{card}
/// Update a card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_cards_card_execute()` to send, or `post_v1_issuing_cards_card` for simplest API.

pub fn post_v1_issuing_cards_card_builder(
    client: &SimpleHttpClient,
    card: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/cards/{}", card,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/cards/{card}
/// Update a card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_cards_card_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_cards_card_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cards_card_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_cards_card`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingCardsCardArgs {
    /// Path parameter: card
    pub card: String,
}

/// POST /v1/issuing/cards/{card}
/// Update a card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_cards_card_builder()` + `post_v1_issuing_cards_card_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_cards_card(
    client: &SimpleHttpClient,
    args: &PostV1IssuingCardsCardArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_cards_card_builder(client, &args.card)?;
    post_v1_issuing_cards_card_execute(builder)
}

/// GET /v1/issuing/disputes
/// List all disputes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_disputes_execute()` to send, or `get_v1_issuing_disputes` for simplest API.

pub fn get_v1_issuing_disputes_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
    transaction: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/disputes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = transaction {
        query_parts.push(format!("transaction={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/disputes
/// List all disputes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_disputes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_disputes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_disputes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_disputes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingDisputesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: transaction
    pub transaction: Option<String>,
}

/// GET /v1/issuing/disputes
/// List all disputes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_disputes_builder()` + `get_v1_issuing_disputes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_disputes(
    client: &SimpleHttpClient,
    args: &GetV1IssuingDisputesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_disputes_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.transaction.as_deref(),
    )?;
    get_v1_issuing_disputes_execute(builder)
}

/// POST /v1/issuing/disputes
/// Create a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_disputes_execute()` to send, or `post_v1_issuing_disputes` for simplest API.

pub fn post_v1_issuing_disputes_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/disputes",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/disputes
/// Create a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_disputes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_disputes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_disputes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingDispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/disputes
/// Create a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_disputes_builder()` + `post_v1_issuing_disputes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_disputes(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_disputes_builder(client)?;
    post_v1_issuing_disputes_execute(builder)
}

/// GET /v1/issuing/disputes/{dispute}
/// Retrieve a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_disputes_dispute_execute()` to send, or `get_v1_issuing_disputes_dispute` for simplest API.

pub fn get_v1_issuing_disputes_dispute_builder(
    client: &SimpleHttpClient,
    dispute: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/disputes/{}", dispute,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/disputes/{dispute}
/// Retrieve a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_disputes_dispute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_disputes_dispute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_disputes_dispute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingDispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_disputes_dispute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingDisputesDisputeArgs {
    /// Path parameter: dispute
    pub dispute: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/disputes/{dispute}
/// Retrieve a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_disputes_dispute_builder()` + `get_v1_issuing_disputes_dispute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_disputes_dispute(
    client: &SimpleHttpClient,
    args: &GetV1IssuingDisputesDisputeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_disputes_dispute_builder(client, &args.dispute, args.expand)?;
    get_v1_issuing_disputes_dispute_execute(builder)
}

/// POST /v1/issuing/disputes/{dispute}
/// Update a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_disputes_dispute_execute()` to send, or `post_v1_issuing_disputes_dispute` for simplest API.

pub fn post_v1_issuing_disputes_dispute_builder(
    client: &SimpleHttpClient,
    dispute: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/disputes/{}", dispute,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/disputes/{dispute}
/// Update a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_disputes_dispute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_disputes_dispute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_disputes_dispute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingDispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_disputes_dispute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingDisputesDisputeArgs {
    /// Path parameter: dispute
    pub dispute: String,
}

/// POST /v1/issuing/disputes/{dispute}
/// Update a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_disputes_dispute_builder()` + `post_v1_issuing_disputes_dispute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_disputes_dispute(
    client: &SimpleHttpClient,
    args: &PostV1IssuingDisputesDisputeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_disputes_dispute_builder(client, &args.dispute)?;
    post_v1_issuing_disputes_dispute_execute(builder)
}

/// POST /v1/issuing/disputes/{dispute}/submit
/// Submit a dispute
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_disputes_dispute_submit_execute()` to send, or `post_v1_issuing_disputes_dispute_submit` for simplest API.

pub fn post_v1_issuing_disputes_dispute_submit_builder(
    client: &SimpleHttpClient,
    dispute: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/disputes/{}/submit",
        dispute,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/disputes/{dispute}/submit
/// Submit a dispute
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_disputes_dispute_submit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_disputes_dispute_submit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_disputes_dispute_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingDispute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_disputes_dispute_submit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingDisputesDisputeSubmitArgs {
    /// Path parameter: dispute
    pub dispute: String,
}

/// POST /v1/issuing/disputes/{dispute}/submit
/// Submit a dispute
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_disputes_dispute_submit_builder()` + `post_v1_issuing_disputes_dispute_submit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_disputes_dispute_submit(
    client: &SimpleHttpClient,
    args: &PostV1IssuingDisputesDisputeSubmitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingDispute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_disputes_dispute_submit_builder(client, &args.dispute)?;
    post_v1_issuing_disputes_dispute_submit_execute(builder)
}

/// GET /v1/issuing/personalization_designs
/// List all personalization designs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_personalization_designs_execute()` to send, or `get_v1_issuing_personalization_designs` for simplest API.

pub fn get_v1_issuing_personalization_designs_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    lookup_keys: Option<Vec<String>>,
    preferences: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/personalization_designs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = lookup_keys {
        query_parts.push(format!("lookup_keys={}", val));
    }
    if let Some(val) = preferences {
        query_parts.push(format!("preferences={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/personalization_designs
/// List all personalization designs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_personalization_designs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_personalization_designs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_personalization_designs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_personalization_designs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingPersonalizationDesignsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: lookup_keys
    pub lookup_keys: Option<Vec<String>>,
    /// Query parameter: preferences
    pub preferences: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/issuing/personalization_designs
/// List all personalization designs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_personalization_designs_builder()` + `get_v1_issuing_personalization_designs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_personalization_designs(
    client: &SimpleHttpClient,
    args: &GetV1IssuingPersonalizationDesignsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_personalization_designs_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.lookup_keys,
        args.preferences.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_issuing_personalization_designs_execute(builder)
}

/// POST /v1/issuing/personalization_designs
/// Create a personalization design
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_personalization_designs_execute()` to send, or `post_v1_issuing_personalization_designs` for simplest API.

pub fn post_v1_issuing_personalization_designs_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/personalization_designs",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/personalization_designs
/// Create a personalization design
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_personalization_designs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_personalization_designs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_personalization_designs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPersonalization_design = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/personalization_designs
/// Create a personalization design
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_personalization_designs_builder()` + `post_v1_issuing_personalization_designs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_personalization_designs(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_personalization_designs_builder(client)?;
    post_v1_issuing_personalization_designs_execute(builder)
}

/// GET /v1/issuing/personalization_designs/{personalization_design}
/// Retrieve a personalization design
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_personalization_designs_personalization_design_execute()` to send, or `get_v1_issuing_personalization_designs_personalization_design` for simplest API.

pub fn get_v1_issuing_personalization_designs_personalization_design_builder(
    client: &SimpleHttpClient,
    personalization_design: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/personalization_designs/{}",
        personalization_design,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/personalization_designs/{personalization_design}
/// Retrieve a personalization design
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_personalization_designs_personalization_design_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_personalization_designs_personalization_design_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_personalization_designs_personalization_design_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPersonalization_design = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_personalization_designs_personalization_design`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingPersonalizationDesignsPersonalizationDesignArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/personalization_designs/{personalization_design}
/// Retrieve a personalization design
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_personalization_designs_personalization_design_builder()` + `get_v1_issuing_personalization_designs_personalization_design_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_personalization_designs_personalization_design(
    client: &SimpleHttpClient,
    args: &GetV1IssuingPersonalizationDesignsPersonalizationDesignArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_personalization_designs_personalization_design_builder(
        client,
        &args.personalization_design,
        args.expand,
    )?;
    get_v1_issuing_personalization_designs_personalization_design_execute(builder)
}

/// POST /v1/issuing/personalization_designs/{personalization_design}
/// Update a personalization design
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_personalization_designs_personalization_design_execute()` to send, or `post_v1_issuing_personalization_designs_personalization_design` for simplest API.

pub fn post_v1_issuing_personalization_designs_personalization_design_builder(
    client: &SimpleHttpClient,
    personalization_design: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/personalization_designs/{}",
        personalization_design,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/personalization_designs/{personalization_design}
/// Update a personalization design
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_personalization_designs_personalization_design_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_personalization_designs_personalization_design_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_personalization_designs_personalization_design_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPersonalization_design = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_personalization_designs_personalization_design`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingPersonalizationDesignsPersonalizationDesignArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
}

/// POST /v1/issuing/personalization_designs/{personalization_design}
/// Update a personalization design
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_personalization_designs_personalization_design_builder()` + `post_v1_issuing_personalization_designs_personalization_design_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_personalization_designs_personalization_design(
    client: &SimpleHttpClient,
    args: &PostV1IssuingPersonalizationDesignsPersonalizationDesignArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_personalization_designs_personalization_design_builder(
        client,
        &args.personalization_design,
    )?;
    post_v1_issuing_personalization_designs_personalization_design_execute(builder)
}

/// GET /v1/issuing/physical_bundles
/// List all physical bundles
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_physical_bundles_execute()` to send, or `get_v1_issuing_physical_bundles` for simplest API.

pub fn get_v1_issuing_physical_bundles_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/physical_bundles",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/physical_bundles
/// List all physical bundles
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_physical_bundles_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_physical_bundles_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_physical_bundles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_physical_bundles`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingPhysicalBundlesArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/issuing/physical_bundles
/// List all physical bundles
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_physical_bundles_builder()` + `get_v1_issuing_physical_bundles_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_physical_bundles(
    client: &SimpleHttpClient,
    args: &GetV1IssuingPhysicalBundlesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_physical_bundles_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_issuing_physical_bundles_execute(builder)
}

/// GET /v1/issuing/physical_bundles/{physical_bundle}
/// Retrieve a physical bundle
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_physical_bundles_physical_bundle_execute()` to send, or `get_v1_issuing_physical_bundles_physical_bundle` for simplest API.

pub fn get_v1_issuing_physical_bundles_physical_bundle_builder(
    client: &SimpleHttpClient,
    physical_bundle: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/physical_bundles/{}",
        physical_bundle,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/physical_bundles/{physical_bundle}
/// Retrieve a physical bundle
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_physical_bundles_physical_bundle_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_physical_bundles_physical_bundle_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_physical_bundles_physical_bundle_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingPhysical_bundle>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPhysical_bundle = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_physical_bundles_physical_bundle`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingPhysicalBundlesPhysicalBundleArgs {
    /// Path parameter: physical_bundle
    pub physical_bundle: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/physical_bundles/{physical_bundle}
/// Retrieve a physical bundle
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_physical_bundles_physical_bundle_builder()` + `get_v1_issuing_physical_bundles_physical_bundle_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_physical_bundles_physical_bundle(
    client: &SimpleHttpClient,
    args: &GetV1IssuingPhysicalBundlesPhysicalBundleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingPhysical_bundle>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_physical_bundles_physical_bundle_builder(
        client,
        &args.physical_bundle,
        args.expand,
    )?;
    get_v1_issuing_physical_bundles_physical_bundle_execute(builder)
}

/// GET /v1/issuing/settlements/{settlement}
/// Retrieve a settlement
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_settlements_settlement_execute()` to send, or `get_v1_issuing_settlements_settlement` for simplest API.

pub fn get_v1_issuing_settlements_settlement_builder(
    client: &SimpleHttpClient,
    settlement: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/settlements/{}",
        settlement,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/settlements/{settlement}
/// Retrieve a settlement
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_settlements_settlement_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_settlements_settlement_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_settlements_settlement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingSettlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_settlements_settlement`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingSettlementsSettlementArgs {
    /// Path parameter: settlement
    pub settlement: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/settlements/{settlement}
/// Retrieve a settlement
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_settlements_settlement_builder()` + `get_v1_issuing_settlements_settlement_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_settlements_settlement(
    client: &SimpleHttpClient,
    args: &GetV1IssuingSettlementsSettlementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_issuing_settlements_settlement_builder(client, &args.settlement, args.expand)?;
    get_v1_issuing_settlements_settlement_execute(builder)
}

/// POST /v1/issuing/settlements/{settlement}
/// Update a settlement
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_settlements_settlement_execute()` to send, or `post_v1_issuing_settlements_settlement` for simplest API.

pub fn post_v1_issuing_settlements_settlement_builder(
    client: &SimpleHttpClient,
    settlement: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/settlements/{}",
        settlement,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/settlements/{settlement}
/// Update a settlement
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_settlements_settlement_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_settlements_settlement_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_settlements_settlement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingSettlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_settlements_settlement`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingSettlementsSettlementArgs {
    /// Path parameter: settlement
    pub settlement: String,
}

/// POST /v1/issuing/settlements/{settlement}
/// Update a settlement
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_settlements_settlement_builder()` + `post_v1_issuing_settlements_settlement_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_settlements_settlement(
    client: &SimpleHttpClient,
    args: &PostV1IssuingSettlementsSettlementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_settlements_settlement_builder(client, &args.settlement)?;
    post_v1_issuing_settlements_settlement_execute(builder)
}

/// GET /v1/issuing/tokens
/// List all issuing tokens for card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_tokens_execute()` to send, or `get_v1_issuing_tokens` for simplest API.

pub fn get_v1_issuing_tokens_builder(
    client: &SimpleHttpClient,
    card: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/tokens",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = card {
        query_parts.push(format!("card={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/tokens
/// List all issuing tokens for card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingTokensArgs {
    /// Query parameter: card
    pub card: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/issuing/tokens
/// List all issuing tokens for card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_tokens_builder()` + `get_v1_issuing_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_tokens(
    client: &SimpleHttpClient,
    args: &GetV1IssuingTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_tokens_builder(
        client,
        args.card.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_issuing_tokens_execute(builder)
}

/// GET /v1/issuing/tokens/{token}
/// Retrieve an issuing token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_tokens_token_execute()` to send, or `get_v1_issuing_tokens_token` for simplest API.

pub fn get_v1_issuing_tokens_token_builder(
    client: &SimpleHttpClient,
    token: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/tokens/{}", token,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/tokens/{token}
/// Retrieve an issuing token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_tokens_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_tokens_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_tokens_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_tokens_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingTokensTokenArgs {
    /// Path parameter: token
    pub token: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/tokens/{token}
/// Retrieve an issuing token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_tokens_token_builder()` + `get_v1_issuing_tokens_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_tokens_token(
    client: &SimpleHttpClient,
    args: &GetV1IssuingTokensTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_tokens_token_builder(client, &args.token, args.expand)?;
    get_v1_issuing_tokens_token_execute(builder)
}

/// POST /v1/issuing/tokens/{token}
/// Update a token status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_tokens_token_execute()` to send, or `post_v1_issuing_tokens_token` for simplest API.

pub fn post_v1_issuing_tokens_token_builder(
    client: &SimpleHttpClient,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/tokens/{}", token,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/tokens/{token}
/// Update a token status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_tokens_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_tokens_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_tokens_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_tokens_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingTokensTokenArgs {
    /// Path parameter: token
    pub token: String,
}

/// POST /v1/issuing/tokens/{token}
/// Update a token status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_tokens_token_builder()` + `post_v1_issuing_tokens_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_tokens_token(
    client: &SimpleHttpClient,
    args: &PostV1IssuingTokensTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_tokens_token_builder(client, &args.token)?;
    post_v1_issuing_tokens_token_execute(builder)
}

/// GET /v1/issuing/transactions
/// List all transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_transactions_execute()` to send, or `get_v1_issuing_transactions` for simplest API.

pub fn get_v1_issuing_transactions_builder(
    client: &SimpleHttpClient,
    card: Option<&str>,
    cardholder: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/issuing/transactions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = card {
        query_parts.push(format!("card={}", val));
    }
    if let Some(val) = cardholder {
        query_parts.push(format!("cardholder={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/transactions
/// List all transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingTransactionsArgs {
    /// Query parameter: card
    pub card: Option<String>,
    /// Query parameter: cardholder
    pub cardholder: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/issuing/transactions
/// List all transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_transactions_builder()` + `get_v1_issuing_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_transactions(
    client: &SimpleHttpClient,
    args: &GetV1IssuingTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_issuing_transactions_builder(
        client,
        args.card.as_deref(),
        args.cardholder.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_issuing_transactions_execute(builder)
}

/// GET /v1/issuing/transactions/{transaction}
/// Retrieve a transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_issuing_transactions_transaction_execute()` to send, or `get_v1_issuing_transactions_transaction` for simplest API.

pub fn get_v1_issuing_transactions_transaction_builder(
    client: &SimpleHttpClient,
    transaction: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/transactions/{}",
        transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/transactions/{transaction}
/// Retrieve a transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_issuing_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_issuing_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_issuing_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1IssuingTransactionsTransactionArgs {
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/issuing/transactions/{transaction}
/// Retrieve a transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_issuing_transactions_transaction_builder()` + `get_v1_issuing_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_issuing_transactions_transaction(
    client: &SimpleHttpClient,
    args: &GetV1IssuingTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_issuing_transactions_transaction_builder(client, &args.transaction, args.expand)?;
    get_v1_issuing_transactions_transaction_execute(builder)
}

/// POST /v1/issuing/transactions/{transaction}
/// Update a transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_issuing_transactions_transaction_execute()` to send, or `post_v1_issuing_transactions_transaction` for simplest API.

pub fn post_v1_issuing_transactions_transaction_builder(
    client: &SimpleHttpClient,
    transaction: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/issuing/transactions/{}",
        transaction,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/transactions/{transaction}
/// Update a transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_issuing_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_issuing_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_issuing_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1IssuingTransactionsTransactionArgs {
    /// Path parameter: transaction
    pub transaction: String,
}

/// POST /v1/issuing/transactions/{transaction}
/// Update a transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_issuing_transactions_transaction_builder()` + `post_v1_issuing_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_issuing_transactions_transaction(
    client: &SimpleHttpClient,
    args: &PostV1IssuingTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_issuing_transactions_transaction_builder(client, &args.transaction)?;
    post_v1_issuing_transactions_transaction_execute(builder)
}

/// POST /v1/link_account_sessions
/// Create a Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_link_account_sessions_execute()` to send, or `post_v1_link_account_sessions` for simplest API.

pub fn post_v1_link_account_sessions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/link_account_sessions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/link_account_sessions
/// Create a Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_link_account_sessions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_link_account_sessions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_link_account_sessions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/link_account_sessions
/// Create a Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_link_account_sessions_builder()` + `post_v1_link_account_sessions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_link_account_sessions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_link_account_sessions_builder(client)?;
    post_v1_link_account_sessions_execute(builder)
}

/// GET /v1/link_account_sessions/{session}
/// Retrieve a Session
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_link_account_sessions_session_execute()` to send, or `get_v1_link_account_sessions_session` for simplest API.

pub fn get_v1_link_account_sessions_session_builder(
    client: &SimpleHttpClient,
    session: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/link_account_sessions/{}",
        session,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/link_account_sessions/{session}
/// Retrieve a Session
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_link_account_sessions_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_link_account_sessions_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_link_account_sessions_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_link_account_sessions_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1LinkAccountSessionsSessionArgs {
    /// Path parameter: session
    pub session: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/link_account_sessions/{session}
/// Retrieve a Session
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_link_account_sessions_session_builder()` + `get_v1_link_account_sessions_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_link_account_sessions_session(
    client: &SimpleHttpClient,
    args: &GetV1LinkAccountSessionsSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_link_account_sessions_session_builder(client, &args.session, args.expand)?;
    get_v1_link_account_sessions_session_execute(builder)
}

/// GET /v1/linked_accounts
/// List Accounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_linked_accounts_execute()` to send, or `get_v1_linked_accounts` for simplest API.

pub fn get_v1_linked_accounts_builder(
    client: &SimpleHttpClient,
    account_holder: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    session: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/linked_accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = account_holder {
        query_parts.push(format!("account_holder={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = session {
        query_parts.push(format!("session={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/linked_accounts
/// List Accounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_linked_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_linked_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_linked_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_linked_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1LinkedAccountsArgs {
    /// Query parameter: account_holder
    pub account_holder: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: session
    pub session: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/linked_accounts
/// List Accounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_linked_accounts_builder()` + `get_v1_linked_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_linked_accounts(
    client: &SimpleHttpClient,
    args: &GetV1LinkedAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_linked_accounts_builder(
        client,
        args.account_holder.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.session.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_linked_accounts_execute(builder)
}

/// GET /v1/linked_accounts/{account}
/// Retrieve an Account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_linked_accounts_account_execute()` to send, or `get_v1_linked_accounts_account` for simplest API.

pub fn get_v1_linked_accounts_account_builder(
    client: &SimpleHttpClient,
    account: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/linked_accounts/{}", account,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/linked_accounts/{account}
/// Retrieve an Account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_linked_accounts_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_linked_accounts_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_linked_accounts_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_linked_accounts_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1LinkedAccountsAccountArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/linked_accounts/{account}
/// Retrieve an Account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_linked_accounts_account_builder()` + `get_v1_linked_accounts_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_linked_accounts_account(
    client: &SimpleHttpClient,
    args: &GetV1LinkedAccountsAccountArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_linked_accounts_account_builder(client, &args.account, args.expand)?;
    get_v1_linked_accounts_account_execute(builder)
}

/// POST /v1/linked_accounts/{account}/disconnect
/// Disconnect an Account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_linked_accounts_account_disconnect_execute()` to send, or `post_v1_linked_accounts_account_disconnect` for simplest API.

pub fn post_v1_linked_accounts_account_disconnect_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/linked_accounts/{}/disconnect",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/linked_accounts/{account}/disconnect
/// Disconnect an Account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_linked_accounts_account_disconnect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_linked_accounts_account_disconnect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_linked_accounts_account_disconnect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_linked_accounts_account_disconnect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1LinkedAccountsAccountDisconnectArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/linked_accounts/{account}/disconnect
/// Disconnect an Account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_linked_accounts_account_disconnect_builder()` + `post_v1_linked_accounts_account_disconnect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_linked_accounts_account_disconnect(
    client: &SimpleHttpClient,
    args: &PostV1LinkedAccountsAccountDisconnectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_linked_accounts_account_disconnect_builder(client, &args.account)?;
    post_v1_linked_accounts_account_disconnect_execute(builder)
}

/// GET /v1/linked_accounts/{account}/owners
/// List Account Owners
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_linked_accounts_account_owners_execute()` to send, or `get_v1_linked_accounts_account_owners` for simplest API.

pub fn get_v1_linked_accounts_account_owners_builder(
    client: &SimpleHttpClient,
    account: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    ownership: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/linked_accounts/{}/owners",
        account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = ownership {
        query_parts.push(format!("ownership={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/linked_accounts/{account}/owners
/// List Account Owners
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_linked_accounts_account_owners_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_linked_accounts_account_owners_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_linked_accounts_account_owners_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_linked_accounts_account_owners`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1LinkedAccountsAccountOwnersArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: ownership
    pub ownership: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/linked_accounts/{account}/owners
/// List Account Owners
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_linked_accounts_account_owners_builder()` + `get_v1_linked_accounts_account_owners_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_linked_accounts_account_owners(
    client: &SimpleHttpClient,
    args: &GetV1LinkedAccountsAccountOwnersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_linked_accounts_account_owners_builder(
        client,
        &args.account,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.ownership.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_linked_accounts_account_owners_execute(builder)
}

/// POST /v1/linked_accounts/{account}/refresh
/// Refresh Account data
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_linked_accounts_account_refresh_execute()` to send, or `post_v1_linked_accounts_account_refresh` for simplest API.

pub fn post_v1_linked_accounts_account_refresh_builder(
    client: &SimpleHttpClient,
    account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/linked_accounts/{}/refresh",
        account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/linked_accounts/{account}/refresh
/// Refresh Account data
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_linked_accounts_account_refresh_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_linked_accounts_account_refresh_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_linked_accounts_account_refresh_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Financial_connectionsAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_linked_accounts_account_refresh`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1LinkedAccountsAccountRefreshArgs {
    /// Path parameter: account
    pub account: String,
}

/// POST /v1/linked_accounts/{account}/refresh
/// Refresh Account data
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_linked_accounts_account_refresh_builder()` + `post_v1_linked_accounts_account_refresh_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_linked_accounts_account_refresh(
    client: &SimpleHttpClient,
    args: &PostV1LinkedAccountsAccountRefreshArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Financial_connectionsAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_linked_accounts_account_refresh_builder(client, &args.account)?;
    post_v1_linked_accounts_account_refresh_execute(builder)
}

/// GET /v1/mandates/{mandate}
/// Retrieve a Mandate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_mandates_mandate_execute()` to send, or `get_v1_mandates_mandate` for simplest API.

pub fn get_v1_mandates_mandate_builder(
    client: &SimpleHttpClient,
    mandate: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/mandates/{}", mandate,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/mandates/{mandate}
/// Retrieve a Mandate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_mandates_mandate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_mandates_mandate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_mandates_mandate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Mandate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Mandate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_mandates_mandate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1MandatesMandateArgs {
    /// Path parameter: mandate
    pub mandate: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/mandates/{mandate}
/// Retrieve a Mandate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_mandates_mandate_builder()` + `get_v1_mandates_mandate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_mandates_mandate(
    client: &SimpleHttpClient,
    args: &GetV1MandatesMandateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Mandate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_mandates_mandate_builder(client, &args.mandate, args.expand)?;
    get_v1_mandates_mandate_execute(builder)
}

/// GET /v1/payment_attempt_records
/// List Payment Attempt Records
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_attempt_records_execute()` to send, or `get_v1_payment_attempt_records` for simplest API.

pub fn get_v1_payment_attempt_records_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_record: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_attempt_records",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_record {
        query_parts.push(format!("payment_record={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_attempt_records
/// List Payment Attempt Records
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_attempt_records_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_attempt_records_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_attempt_records_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_attempt_records`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentAttemptRecordsArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_record
    pub payment_record: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_attempt_records
/// List Payment Attempt Records
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_attempt_records_builder()` + `get_v1_payment_attempt_records_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_attempt_records(
    client: &SimpleHttpClient,
    args: &GetV1PaymentAttemptRecordsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_attempt_records_builder(
        client,
        args.expand,
        args.limit,
        args.payment_record.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_attempt_records_execute(builder)
}

/// GET /v1/payment_attempt_records/{id}
/// Retrieve a Payment Attempt Record
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_attempt_records_id_execute()` to send, or `get_v1_payment_attempt_records_id` for simplest API.

pub fn get_v1_payment_attempt_records_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_attempt_records/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_attempt_records/{id}
/// Retrieve a Payment Attempt Record
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_attempt_records_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_attempt_records_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_attempt_records_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_attempt_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_attempt_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_attempt_records_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentAttemptRecordsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_attempt_records/{id}
/// Retrieve a Payment Attempt Record
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_attempt_records_id_builder()` + `get_v1_payment_attempt_records_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_attempt_records_id(
    client: &SimpleHttpClient,
    args: &GetV1PaymentAttemptRecordsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_attempt_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_payment_attempt_records_id_builder(client, &args.id, args.expand)?;
    get_v1_payment_attempt_records_id_execute(builder)
}

/// GET /v1/payment_intents
/// List all PaymentIntents
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_intents_execute()` to send, or `get_v1_payment_intents` for simplest API.

pub fn get_v1_payment_intents_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_intents",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_intents
/// List all PaymentIntents
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_intents_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_intents_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_intents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentIntentsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_intents
/// List all PaymentIntents
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_intents_builder()` + `get_v1_payment_intents_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents(
    client: &SimpleHttpClient,
    args: &GetV1PaymentIntentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_intents_builder(
        client,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_intents_execute(builder)
}

/// POST /v1/payment_intents
/// Create a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_execute()` to send, or `post_v1_payment_intents` for simplest API.

pub fn post_v1_payment_intents_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_intents",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents
/// Create a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_intents
/// Create a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_builder()` + `post_v1_payment_intents_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_intents_builder(client)?;
    post_v1_payment_intents_execute(builder)
}

/// GET /v1/payment_intents/search
/// Search PaymentIntents
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_intents_search_execute()` to send, or `get_v1_payment_intents_search` for simplest API.

pub fn get_v1_payment_intents_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_intents/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_intents/search
/// Search PaymentIntents
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_intents_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_intents_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_intents_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentIntentsSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/payment_intents/search
/// Search PaymentIntents
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_intents_search_builder()` + `get_v1_payment_intents_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_search(
    client: &SimpleHttpClient,
    args: &GetV1PaymentIntentsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_intents_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_payment_intents_search_execute(builder)
}

/// GET /v1/payment_intents/{intent}
/// Retrieve a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_intents_intent_execute()` to send, or `get_v1_payment_intents_intent` for simplest API.

pub fn get_v1_payment_intents_intent_builder(
    client: &SimpleHttpClient,
    intent: &str,
    client_secret: Option<&str>,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_intents/{}", intent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_secret {
        query_parts.push(format!("client_secret={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_intents/{intent}
/// Retrieve a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_intents_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_intents_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_intents_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
    /// Query parameter: client_secret
    pub client_secret: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_intents/{intent}
/// Retrieve a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_intents_intent_builder()` + `get_v1_payment_intents_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_intent(
    client: &SimpleHttpClient,
    args: &GetV1PaymentIntentsIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_payment_intents_intent_builder(
        client,
        &args.intent,
        args.client_secret.as_deref(),
        args.expand,
    )?;
    get_v1_payment_intents_intent_execute(builder)
}

/// POST /v1/payment_intents/{intent}
/// Update a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_execute()` to send, or `post_v1_payment_intents_intent` for simplest API.

pub fn post_v1_payment_intents_intent_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_intents/{}", intent,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}
/// Update a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}
/// Update a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_builder()` + `post_v1_payment_intents_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_intents_intent_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_execute(builder)
}

/// GET /v1/payment_intents/{intent}/amount_details_line_items
/// List all PaymentIntent LineItems
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_intents_intent_amount_details_line_items_execute()` to send, or `get_v1_payment_intents_intent_amount_details_line_items` for simplest API.

pub fn get_v1_payment_intents_intent_amount_details_line_items_builder(
    client: &SimpleHttpClient,
    intent: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/amount_details_line_items",
        intent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_intents/{intent}/amount_details_line_items
/// List all PaymentIntent LineItems
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_intents_intent_amount_details_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_intents_intent_amount_details_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_intent_amount_details_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_intents_intent_amount_details_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentIntentsIntentAmountDetailsLineItemsArgs {
    /// Path parameter: intent
    pub intent: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_intents/{intent}/amount_details_line_items
/// List all PaymentIntent LineItems
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_intents_intent_amount_details_line_items_builder()` + `get_v1_payment_intents_intent_amount_details_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_intents_intent_amount_details_line_items(
    client: &SimpleHttpClient,
    args: &GetV1PaymentIntentsIntentAmountDetailsLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_intents_intent_amount_details_line_items_builder(
        client,
        &args.intent,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_intents_intent_amount_details_line_items_execute(builder)
}

/// POST /v1/payment_intents/{intent}/apply_customer_balance
/// Reconcile a customer_balance PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_apply_customer_balance_execute()` to send, or `post_v1_payment_intents_intent_apply_customer_balance` for simplest API.

pub fn post_v1_payment_intents_intent_apply_customer_balance_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/apply_customer_balance",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/apply_customer_balance
/// Reconcile a customer_balance PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_apply_customer_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_apply_customer_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_apply_customer_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent_apply_customer_balance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentApplyCustomerBalanceArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}/apply_customer_balance
/// Reconcile a customer_balance PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_apply_customer_balance_builder()` + `post_v1_payment_intents_intent_apply_customer_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_apply_customer_balance(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentApplyCustomerBalanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_intents_intent_apply_customer_balance_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_apply_customer_balance_execute(builder)
}

/// POST /v1/payment_intents/{intent}/cancel
/// Cancel a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_cancel_execute()` to send, or `post_v1_payment_intents_intent_cancel` for simplest API.

pub fn post_v1_payment_intents_intent_cancel_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/cancel",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/cancel
/// Cancel a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentCancelArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}/cancel
/// Cancel a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_cancel_builder()` + `post_v1_payment_intents_intent_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_cancel(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_intents_intent_cancel_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_cancel_execute(builder)
}

/// POST /v1/payment_intents/{intent}/capture
/// Capture a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_capture_execute()` to send, or `post_v1_payment_intents_intent_capture` for simplest API.

pub fn post_v1_payment_intents_intent_capture_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/capture",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/capture
/// Capture a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_capture_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_capture_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent_capture`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentCaptureArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}/capture
/// Capture a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_capture_builder()` + `post_v1_payment_intents_intent_capture_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_capture(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentCaptureArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_intents_intent_capture_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_capture_execute(builder)
}

/// POST /v1/payment_intents/{intent}/confirm
/// Confirm a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_confirm_execute()` to send, or `post_v1_payment_intents_intent_confirm` for simplest API.

pub fn post_v1_payment_intents_intent_confirm_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/confirm",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/confirm
/// Confirm a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_confirm_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_confirm_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_confirm_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent_confirm`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentConfirmArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}/confirm
/// Confirm a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_confirm_builder()` + `post_v1_payment_intents_intent_confirm_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_confirm(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentConfirmArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_intents_intent_confirm_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_confirm_execute(builder)
}

/// POST /v1/payment_intents/{intent}/increment_authorization
/// Increment an authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_increment_authorization_execute()` to send, or `post_v1_payment_intents_intent_increment_authorization` for simplest API.

pub fn post_v1_payment_intents_intent_increment_authorization_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/increment_authorization",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/increment_authorization
/// Increment an authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_increment_authorization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_increment_authorization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_increment_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent_increment_authorization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentIncrementAuthorizationArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}/increment_authorization
/// Increment an authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_increment_authorization_builder()` + `post_v1_payment_intents_intent_increment_authorization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_increment_authorization(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentIncrementAuthorizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_intents_intent_increment_authorization_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_increment_authorization_execute(builder)
}

/// POST /v1/payment_intents/{intent}/verify_microdeposits
/// Verify microdeposits on a PaymentIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_intents_intent_verify_microdeposits_execute()` to send, or `post_v1_payment_intents_intent_verify_microdeposits` for simplest API.

pub fn post_v1_payment_intents_intent_verify_microdeposits_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_intents/{}/verify_microdeposits",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/verify_microdeposits
/// Verify microdeposits on a PaymentIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_intents_intent_verify_microdeposits_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_intents_intent_verify_microdeposits_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_verify_microdeposits_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_intents_intent_verify_microdeposits`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentIntentsIntentVerifyMicrodepositsArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/payment_intents/{intent}/verify_microdeposits
/// Verify microdeposits on a PaymentIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_intents_intent_verify_microdeposits_builder()` + `post_v1_payment_intents_intent_verify_microdeposits_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_intents_intent_verify_microdeposits(
    client: &SimpleHttpClient,
    args: &PostV1PaymentIntentsIntentVerifyMicrodepositsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_intents_intent_verify_microdeposits_builder(client, &args.intent)?;
    post_v1_payment_intents_intent_verify_microdeposits_execute(builder)
}

/// GET /v1/payment_links
/// List all payment links
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_links_execute()` to send, or `get_v1_payment_links` for simplest API.

pub fn get_v1_payment_links_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_links",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_links
/// List all payment links
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_links`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentLinksArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_links
/// List all payment links
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_links_builder()` + `get_v1_payment_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_links(
    client: &SimpleHttpClient,
    args: &GetV1PaymentLinksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_links_builder(
        client,
        args.active,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_links_execute(builder)
}

/// POST /v1/payment_links
/// Create a payment link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_links_execute()` to send, or `post_v1_payment_links` for simplest API.

pub fn post_v1_payment_links_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_links",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_links
/// Create a payment link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_links
/// Create a payment link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_links_builder()` + `post_v1_payment_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_links(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_links_builder(client)?;
    post_v1_payment_links_execute(builder)
}

/// GET /v1/payment_links/{payment_link}
/// Retrieve payment link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_links_payment_link_execute()` to send, or `get_v1_payment_links_payment_link` for simplest API.

pub fn get_v1_payment_links_payment_link_builder(
    client: &SimpleHttpClient,
    payment_link: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_links/{}", payment_link,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_links/{payment_link}
/// Retrieve payment link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_links_payment_link_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_links_payment_link_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_links_payment_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_links_payment_link`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentLinksPaymentLinkArgs {
    /// Path parameter: payment_link
    pub payment_link: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_links/{payment_link}
/// Retrieve payment link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_links_payment_link_builder()` + `get_v1_payment_links_payment_link_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_links_payment_link(
    client: &SimpleHttpClient,
    args: &GetV1PaymentLinksPaymentLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_payment_links_payment_link_builder(client, &args.payment_link, args.expand)?;
    get_v1_payment_links_payment_link_execute(builder)
}

/// POST /v1/payment_links/{payment_link}
/// Update a payment link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_links_payment_link_execute()` to send, or `post_v1_payment_links_payment_link` for simplest API.

pub fn post_v1_payment_links_payment_link_builder(
    client: &SimpleHttpClient,
    payment_link: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_links/{}", payment_link,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_links/{payment_link}
/// Update a payment link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_links_payment_link_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_links_payment_link_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_links_payment_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_links_payment_link`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentLinksPaymentLinkArgs {
    /// Path parameter: payment_link
    pub payment_link: String,
}

/// POST /v1/payment_links/{payment_link}
/// Update a payment link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_links_payment_link_builder()` + `post_v1_payment_links_payment_link_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_links_payment_link(
    client: &SimpleHttpClient,
    args: &PostV1PaymentLinksPaymentLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_links_payment_link_builder(client, &args.payment_link)?;
    post_v1_payment_links_payment_link_execute(builder)
}

/// GET /v1/payment_links/{payment_link}/line_items
/// Retrieve a payment link's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_links_payment_link_line_items_execute()` to send, or `get_v1_payment_links_payment_link_line_items` for simplest API.

pub fn get_v1_payment_links_payment_link_line_items_builder(
    client: &SimpleHttpClient,
    payment_link: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_links/{}/line_items",
        payment_link,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_links/{payment_link}/line_items
/// Retrieve a payment link's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_links_payment_link_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_links_payment_link_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_links_payment_link_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_links_payment_link_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentLinksPaymentLinkLineItemsArgs {
    /// Path parameter: payment_link
    pub payment_link: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_links/{payment_link}/line_items
/// Retrieve a payment link's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_links_payment_link_line_items_builder()` + `get_v1_payment_links_payment_link_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_links_payment_link_line_items(
    client: &SimpleHttpClient,
    args: &GetV1PaymentLinksPaymentLinkLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_links_payment_link_line_items_builder(
        client,
        &args.payment_link,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_links_payment_link_line_items_execute(builder)
}

/// GET /v1/payment_method_configurations
/// List payment method configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_method_configurations_execute()` to send, or `get_v1_payment_method_configurations` for simplest API.

pub fn get_v1_payment_method_configurations_builder(
    client: &SimpleHttpClient,
    application: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_method_configurations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = application {
        query_parts.push(format!("application={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_configurations
/// List payment method configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_method_configurations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_method_configurations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_configurations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_method_configurations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentMethodConfigurationsArgs {
    /// Query parameter: application
    pub application: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_method_configurations
/// List payment method configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_method_configurations_builder()` + `get_v1_payment_method_configurations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_configurations(
    client: &SimpleHttpClient,
    args: &GetV1PaymentMethodConfigurationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_method_configurations_builder(
        client,
        args.application.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_method_configurations_execute(builder)
}

/// POST /v1/payment_method_configurations
/// Create a payment method configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_method_configurations_execute()` to send, or `post_v1_payment_method_configurations` for simplest API.

pub fn post_v1_payment_method_configurations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_method_configurations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_configurations
/// Create a payment method configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_method_configurations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_method_configurations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_configurations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Payment_method_configuration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_method_configurations
/// Create a payment method configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_method_configurations_builder()` + `post_v1_payment_method_configurations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_configurations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Payment_method_configuration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_method_configurations_builder(client)?;
    post_v1_payment_method_configurations_execute(builder)
}

/// GET /v1/payment_method_configurations/{configuration}
/// Retrieve payment method configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_method_configurations_configuration_execute()` to send, or `get_v1_payment_method_configurations_configuration` for simplest API.

pub fn get_v1_payment_method_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_method_configurations/{}",
        configuration,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_configurations/{configuration}
/// Retrieve payment method configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_method_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_method_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Payment_method_configuration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_method_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentMethodConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_method_configurations/{configuration}
/// Retrieve payment method configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_method_configurations_configuration_builder()` + `get_v1_payment_method_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_configurations_configuration(
    client: &SimpleHttpClient,
    args: &GetV1PaymentMethodConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Payment_method_configuration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_payment_method_configurations_configuration_builder(
        client,
        &args.configuration,
        args.expand,
    )?;
    get_v1_payment_method_configurations_configuration_execute(builder)
}

/// POST /v1/payment_method_configurations/{configuration}
/// Update payment method configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_method_configurations_configuration_execute()` to send, or `post_v1_payment_method_configurations_configuration` for simplest API.

pub fn post_v1_payment_method_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_method_configurations/{}",
        configuration,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_configurations/{configuration}
/// Update payment method configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_method_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_method_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Payment_method_configuration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_method_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentMethodConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
}

/// POST /v1/payment_method_configurations/{configuration}
/// Update payment method configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_method_configurations_configuration_builder()` + `post_v1_payment_method_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_configurations_configuration(
    client: &SimpleHttpClient,
    args: &PostV1PaymentMethodConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Payment_method_configuration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_method_configurations_configuration_builder(client, &args.configuration)?;
    post_v1_payment_method_configurations_configuration_execute(builder)
}

/// GET /v1/payment_method_domains
/// List payment method domains
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_method_domains_execute()` to send, or `get_v1_payment_method_domains` for simplest API.

pub fn get_v1_payment_method_domains_builder(
    client: &SimpleHttpClient,
    domain_name: Option<&str>,
    enabled: Option<bool>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_method_domains",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = domain_name {
        query_parts.push(format!("domain_name={}", val));
    }
    if let Some(val) = enabled {
        query_parts.push(format!("enabled={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_domains
/// List payment method domains
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_method_domains_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_method_domains_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_domains_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_method_domains`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentMethodDomainsArgs {
    /// Query parameter: domain_name
    pub domain_name: Option<String>,
    /// Query parameter: enabled
    pub enabled: Option<bool>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/payment_method_domains
/// List payment method domains
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_method_domains_builder()` + `get_v1_payment_method_domains_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_domains(
    client: &SimpleHttpClient,
    args: &GetV1PaymentMethodDomainsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_method_domains_builder(
        client,
        args.domain_name.as_deref(),
        args.enabled,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_payment_method_domains_execute(builder)
}

/// POST /v1/payment_method_domains
/// Create a payment method domain
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_method_domains_execute()` to send, or `post_v1_payment_method_domains` for simplest API.

pub fn post_v1_payment_method_domains_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_method_domains",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_domains
/// Create a payment method domain
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_method_domains_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_method_domains_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_domains_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_method_domains
/// Create a payment method domain
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_method_domains_builder()` + `post_v1_payment_method_domains_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_domains(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_method_domains_builder(client)?;
    post_v1_payment_method_domains_execute(builder)
}

/// GET /v1/payment_method_domains/{payment_method_domain}
/// Retrieve a payment method domain
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_method_domains_payment_method_domain_execute()` to send, or `get_v1_payment_method_domains_payment_method_domain` for simplest API.

pub fn get_v1_payment_method_domains_payment_method_domain_builder(
    client: &SimpleHttpClient,
    payment_method_domain: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_method_domains/{}",
        payment_method_domain,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_method_domains/{payment_method_domain}
/// Retrieve a payment method domain
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_method_domains_payment_method_domain_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_method_domains_payment_method_domain_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_domains_payment_method_domain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_method_domains_payment_method_domain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentMethodDomainsPaymentMethodDomainArgs {
    /// Path parameter: payment_method_domain
    pub payment_method_domain: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_method_domains/{payment_method_domain}
/// Retrieve a payment method domain
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_method_domains_payment_method_domain_builder()` + `get_v1_payment_method_domains_payment_method_domain_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_method_domains_payment_method_domain(
    client: &SimpleHttpClient,
    args: &GetV1PaymentMethodDomainsPaymentMethodDomainArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_payment_method_domains_payment_method_domain_builder(
        client,
        &args.payment_method_domain,
        args.expand,
    )?;
    get_v1_payment_method_domains_payment_method_domain_execute(builder)
}

/// POST /v1/payment_method_domains/{payment_method_domain}
/// Update a payment method domain
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_method_domains_payment_method_domain_execute()` to send, or `post_v1_payment_method_domains_payment_method_domain` for simplest API.

pub fn post_v1_payment_method_domains_payment_method_domain_builder(
    client: &SimpleHttpClient,
    payment_method_domain: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_method_domains/{}",
        payment_method_domain,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_domains/{payment_method_domain}
/// Update a payment method domain
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_method_domains_payment_method_domain_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_method_domains_payment_method_domain_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_domains_payment_method_domain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_method_domains_payment_method_domain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentMethodDomainsPaymentMethodDomainArgs {
    /// Path parameter: payment_method_domain
    pub payment_method_domain: String,
}

/// POST /v1/payment_method_domains/{payment_method_domain}
/// Update a payment method domain
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_method_domains_payment_method_domain_builder()` + `post_v1_payment_method_domains_payment_method_domain_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_domains_payment_method_domain(
    client: &SimpleHttpClient,
    args: &PostV1PaymentMethodDomainsPaymentMethodDomainArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_method_domains_payment_method_domain_builder(
        client,
        &args.payment_method_domain,
    )?;
    post_v1_payment_method_domains_payment_method_domain_execute(builder)
}

/// POST /v1/payment_method_domains/{payment_method_domain}/validate
/// Validate an existing payment method domain
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_method_domains_payment_method_domain_validate_execute()` to send, or `post_v1_payment_method_domains_payment_method_domain_validate` for simplest API.

pub fn post_v1_payment_method_domains_payment_method_domain_validate_builder(
    client: &SimpleHttpClient,
    payment_method_domain: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_method_domains/{}/validate",
        payment_method_domain,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_method_domains/{payment_method_domain}/validate
/// Validate an existing payment method domain
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_method_domains_payment_method_domain_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_method_domains_payment_method_domain_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_domains_payment_method_domain_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method_domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_method_domains_payment_method_domain_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentMethodDomainsPaymentMethodDomainValidateArgs {
    /// Path parameter: payment_method_domain
    pub payment_method_domain: String,
}

/// POST /v1/payment_method_domains/{payment_method_domain}/validate
/// Validate an existing payment method domain
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_method_domains_payment_method_domain_validate_builder()` + `post_v1_payment_method_domains_payment_method_domain_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_method_domains_payment_method_domain_validate(
    client: &SimpleHttpClient,
    args: &PostV1PaymentMethodDomainsPaymentMethodDomainValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method_domain>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_method_domains_payment_method_domain_validate_builder(
        client,
        &args.payment_method_domain,
    )?;
    post_v1_payment_method_domains_payment_method_domain_validate_execute(builder)
}

/// GET /v1/payment_methods
/// List PaymentMethods
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_methods_execute()` to send, or `get_v1_payment_methods` for simplest API.

pub fn get_v1_payment_methods_builder(
    client: &SimpleHttpClient,
    allow_redisplay: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_methods",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allow_redisplay {
        query_parts.push(format!("allow_redisplay={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_methods
/// List PaymentMethods
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_methods_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_methods_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_methods_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_methods`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentMethodsArgs {
    /// Query parameter: allow_redisplay
    pub allow_redisplay: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/payment_methods
/// List PaymentMethods
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_methods_builder()` + `get_v1_payment_methods_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_methods(
    client: &SimpleHttpClient,
    args: &GetV1PaymentMethodsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payment_methods_builder(
        client,
        args.allow_redisplay.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_payment_methods_execute(builder)
}

/// POST /v1/payment_methods
/// Shares a PaymentMethod
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_methods_execute()` to send, or `post_v1_payment_methods` for simplest API.

pub fn post_v1_payment_methods_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_methods",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_methods
/// Shares a PaymentMethod
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_methods_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_methods_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_methods
/// Shares a PaymentMethod
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_methods_builder()` + `post_v1_payment_methods_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_methods_builder(client)?;
    post_v1_payment_methods_execute(builder)
}

/// GET /v1/payment_methods/{payment_method}
/// Retrieve a PaymentMethod
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_methods_payment_method_execute()` to send, or `get_v1_payment_methods_payment_method` for simplest API.

pub fn get_v1_payment_methods_payment_method_builder(
    client: &SimpleHttpClient,
    payment_method: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_methods/{}",
        payment_method,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_methods/{payment_method}
/// Retrieve a PaymentMethod
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_methods_payment_method_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_methods_payment_method_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_methods_payment_method_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_methods_payment_method`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentMethodsPaymentMethodArgs {
    /// Path parameter: payment_method
    pub payment_method: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_methods/{payment_method}
/// Retrieve a PaymentMethod
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_methods_payment_method_builder()` + `get_v1_payment_methods_payment_method_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_methods_payment_method(
    client: &SimpleHttpClient,
    args: &GetV1PaymentMethodsPaymentMethodArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_payment_methods_payment_method_builder(client, &args.payment_method, args.expand)?;
    get_v1_payment_methods_payment_method_execute(builder)
}

/// POST /v1/payment_methods/{payment_method}
/// Update a PaymentMethod
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_methods_payment_method_execute()` to send, or `post_v1_payment_methods_payment_method` for simplest API.

pub fn post_v1_payment_methods_payment_method_builder(
    client: &SimpleHttpClient,
    payment_method: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_methods/{}",
        payment_method,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_methods/{payment_method}
/// Update a PaymentMethod
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_methods_payment_method_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_methods_payment_method_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_payment_method_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_methods_payment_method`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentMethodsPaymentMethodArgs {
    /// Path parameter: payment_method
    pub payment_method: String,
}

/// POST /v1/payment_methods/{payment_method}
/// Update a PaymentMethod
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_methods_payment_method_builder()` + `post_v1_payment_methods_payment_method_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_payment_method(
    client: &SimpleHttpClient,
    args: &PostV1PaymentMethodsPaymentMethodArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_methods_payment_method_builder(client, &args.payment_method)?;
    post_v1_payment_methods_payment_method_execute(builder)
}

/// POST /v1/payment_methods/{payment_method}/attach
/// Attach a PaymentMethod to a Customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_methods_payment_method_attach_execute()` to send, or `post_v1_payment_methods_payment_method_attach` for simplest API.

pub fn post_v1_payment_methods_payment_method_attach_builder(
    client: &SimpleHttpClient,
    payment_method: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_methods/{}/attach",
        payment_method,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_methods/{payment_method}/attach
/// Attach a PaymentMethod to a Customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_methods_payment_method_attach_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_methods_payment_method_attach_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_payment_method_attach_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_methods_payment_method_attach`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentMethodsPaymentMethodAttachArgs {
    /// Path parameter: payment_method
    pub payment_method: String,
}

/// POST /v1/payment_methods/{payment_method}/attach
/// Attach a PaymentMethod to a Customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_methods_payment_method_attach_builder()` + `post_v1_payment_methods_payment_method_attach_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_payment_method_attach(
    client: &SimpleHttpClient,
    args: &PostV1PaymentMethodsPaymentMethodAttachArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_methods_payment_method_attach_builder(client, &args.payment_method)?;
    post_v1_payment_methods_payment_method_attach_execute(builder)
}

/// POST /v1/payment_methods/{payment_method}/detach
/// Detach a PaymentMethod from a Customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_methods_payment_method_detach_execute()` to send, or `post_v1_payment_methods_payment_method_detach` for simplest API.

pub fn post_v1_payment_methods_payment_method_detach_builder(
    client: &SimpleHttpClient,
    payment_method: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_methods/{}/detach",
        payment_method,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_methods/{payment_method}/detach
/// Detach a PaymentMethod from a Customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_methods_payment_method_detach_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_methods_payment_method_detach_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_payment_method_detach_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_method = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_methods_payment_method_detach`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentMethodsPaymentMethodDetachArgs {
    /// Path parameter: payment_method
    pub payment_method: String,
}

/// POST /v1/payment_methods/{payment_method}/detach
/// Detach a PaymentMethod from a Customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_methods_payment_method_detach_builder()` + `post_v1_payment_methods_payment_method_detach_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_methods_payment_method_detach(
    client: &SimpleHttpClient,
    args: &PostV1PaymentMethodsPaymentMethodDetachArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_method>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_methods_payment_method_detach_builder(client, &args.payment_method)?;
    post_v1_payment_methods_payment_method_detach_execute(builder)
}

/// POST /v1/payment_records/report_payment
/// Report a payment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_report_payment_execute()` to send, or `post_v1_payment_records_report_payment` for simplest API.

pub fn post_v1_payment_records_report_payment_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_records/report_payment",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/report_payment
/// Report a payment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_report_payment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_report_payment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_report_payment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_records/report_payment
/// Report a payment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_report_payment_builder()` + `post_v1_payment_records_report_payment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_report_payment(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_records_report_payment_builder(client)?;
    post_v1_payment_records_report_payment_execute(builder)
}

/// GET /v1/payment_records/{id}
/// Retrieve a Payment Record
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payment_records_id_execute()` to send, or `get_v1_payment_records_id` for simplest API.

pub fn get_v1_payment_records_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payment_records/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payment_records/{id}
/// Retrieve a Payment Record
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payment_records_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payment_records_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_records_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payment_records_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PaymentRecordsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payment_records/{id}
/// Retrieve a Payment Record
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payment_records_id_builder()` + `get_v1_payment_records_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payment_records_id(
    client: &SimpleHttpClient,
    args: &GetV1PaymentRecordsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_payment_records_id_builder(client, &args.id, args.expand)?;
    get_v1_payment_records_id_execute(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt
/// Report a payment attempt
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_id_report_payment_attempt_execute()` to send, or `post_v1_payment_records_id_report_payment_attempt` for simplest API.

pub fn post_v1_payment_records_id_report_payment_attempt_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_records/{}/report_payment_attempt",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt
/// Report a payment attempt
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_id_report_payment_attempt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_records_id_report_payment_attempt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentRecordsIdReportPaymentAttemptArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/payment_records/{id}/report_payment_attempt
/// Report a payment attempt
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_builder()` + `post_v1_payment_records_id_report_payment_attempt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt(
    client: &SimpleHttpClient,
    args: &PostV1PaymentRecordsIdReportPaymentAttemptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_records_id_report_payment_attempt_builder(client, &args.id)?;
    post_v1_payment_records_id_report_payment_attempt_execute(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_canceled
/// Report payment attempt canceled
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_id_report_payment_attempt_canceled_execute()` to send, or `post_v1_payment_records_id_report_payment_attempt_canceled` for simplest API.

pub fn post_v1_payment_records_id_report_payment_attempt_canceled_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_records/{}/report_payment_attempt_canceled",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_canceled
/// Report payment attempt canceled
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_canceled_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_id_report_payment_attempt_canceled_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_canceled_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_records_id_report_payment_attempt_canceled`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentRecordsIdReportPaymentAttemptCanceledArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/payment_records/{id}/report_payment_attempt_canceled
/// Report payment attempt canceled
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_canceled_builder()` + `post_v1_payment_records_id_report_payment_attempt_canceled_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_canceled(
    client: &SimpleHttpClient,
    args: &PostV1PaymentRecordsIdReportPaymentAttemptCanceledArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_records_id_report_payment_attempt_canceled_builder(client, &args.id)?;
    post_v1_payment_records_id_report_payment_attempt_canceled_execute(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_failed
/// Report payment attempt failed
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_id_report_payment_attempt_failed_execute()` to send, or `post_v1_payment_records_id_report_payment_attempt_failed` for simplest API.

pub fn post_v1_payment_records_id_report_payment_attempt_failed_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_records/{}/report_payment_attempt_failed",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_failed
/// Report payment attempt failed
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_failed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_id_report_payment_attempt_failed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_failed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_records_id_report_payment_attempt_failed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentRecordsIdReportPaymentAttemptFailedArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/payment_records/{id}/report_payment_attempt_failed
/// Report payment attempt failed
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_failed_builder()` + `post_v1_payment_records_id_report_payment_attempt_failed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_failed(
    client: &SimpleHttpClient,
    args: &PostV1PaymentRecordsIdReportPaymentAttemptFailedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_records_id_report_payment_attempt_failed_builder(client, &args.id)?;
    post_v1_payment_records_id_report_payment_attempt_failed_execute(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_guaranteed
/// Report payment attempt guaranteed
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_id_report_payment_attempt_guaranteed_execute()` to send, or `post_v1_payment_records_id_report_payment_attempt_guaranteed` for simplest API.

pub fn post_v1_payment_records_id_report_payment_attempt_guaranteed_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_records/{}/report_payment_attempt_guaranteed",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_guaranteed
/// Report payment attempt guaranteed
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_guaranteed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_id_report_payment_attempt_guaranteed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_guaranteed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_records_id_report_payment_attempt_guaranteed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentRecordsIdReportPaymentAttemptGuaranteedArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/payment_records/{id}/report_payment_attempt_guaranteed
/// Report payment attempt guaranteed
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_guaranteed_builder()` + `post_v1_payment_records_id_report_payment_attempt_guaranteed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_guaranteed(
    client: &SimpleHttpClient,
    args: &PostV1PaymentRecordsIdReportPaymentAttemptGuaranteedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_records_id_report_payment_attempt_guaranteed_builder(client, &args.id)?;
    post_v1_payment_records_id_report_payment_attempt_guaranteed_execute(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_informational
/// Report payment attempt informational
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_id_report_payment_attempt_informational_execute()` to send, or `post_v1_payment_records_id_report_payment_attempt_informational` for simplest API.

pub fn post_v1_payment_records_id_report_payment_attempt_informational_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_records/{}/report_payment_attempt_informational",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/{id}/report_payment_attempt_informational
/// Report payment attempt informational
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_informational_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_id_report_payment_attempt_informational_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_informational_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_records_id_report_payment_attempt_informational`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentRecordsIdReportPaymentAttemptInformationalArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/payment_records/{id}/report_payment_attempt_informational
/// Report payment attempt informational
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_id_report_payment_attempt_informational_builder()` + `post_v1_payment_records_id_report_payment_attempt_informational_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_payment_attempt_informational(
    client: &SimpleHttpClient,
    args: &PostV1PaymentRecordsIdReportPaymentAttemptInformationalArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_payment_records_id_report_payment_attempt_informational_builder(client, &args.id)?;
    post_v1_payment_records_id_report_payment_attempt_informational_execute(builder)
}

/// POST /v1/payment_records/{id}/report_refund
/// Report a refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payment_records_id_report_refund_execute()` to send, or `post_v1_payment_records_id_report_refund` for simplest API.

pub fn post_v1_payment_records_id_report_refund_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/payment_records/{}/report_refund",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_records/{id}/report_refund
/// Report a refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payment_records_id_report_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payment_records_id_report_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payment_record = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payment_records_id_report_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PaymentRecordsIdReportRefundArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/payment_records/{id}/report_refund
/// Report a refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payment_records_id_report_refund_builder()` + `post_v1_payment_records_id_report_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payment_records_id_report_refund(
    client: &SimpleHttpClient,
    args: &PostV1PaymentRecordsIdReportRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payment_record>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_payment_records_id_report_refund_builder(client, &args.id)?;
    post_v1_payment_records_id_report_refund_execute(builder)
}

/// GET /v1/payouts
/// List all payouts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payouts_execute()` to send, or `get_v1_payouts` for simplest API.

pub fn get_v1_payouts_builder(
    client: &SimpleHttpClient,
    arrival_date: Option<&str>,
    created: Option<&str>,
    destination: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payouts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = arrival_date {
        query_parts.push(format!("arrival_date={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = destination {
        query_parts.push(format!("destination={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payouts
/// List all payouts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payouts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payouts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payouts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payouts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PayoutsArgs {
    /// Query parameter: arrival_date
    pub arrival_date: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: destination
    pub destination: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/payouts
/// List all payouts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payouts_builder()` + `get_v1_payouts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payouts(
    client: &SimpleHttpClient,
    args: &GetV1PayoutsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payouts_builder(
        client,
        args.arrival_date.as_deref(),
        args.created.as_deref(),
        args.destination.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_payouts_execute(builder)
}

/// POST /v1/payouts
/// Create a payout
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payouts_execute()` to send, or `post_v1_payouts` for simplest API.

pub fn post_v1_payouts_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payouts",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts
/// Create a payout
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payouts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payouts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payouts
/// Create a payout
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payouts_builder()` + `post_v1_payouts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_payouts_builder(client)?;
    post_v1_payouts_execute(builder)
}

/// GET /v1/payouts/{payout}
/// Retrieve a payout
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_payouts_payout_execute()` to send, or `get_v1_payouts_payout` for simplest API.

pub fn get_v1_payouts_payout_builder(
    client: &SimpleHttpClient,
    payout: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payouts/{}", payout,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payouts/{payout}
/// Retrieve a payout
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_payouts_payout_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_payouts_payout_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payouts_payout_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_payouts_payout`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PayoutsPayoutArgs {
    /// Path parameter: payout
    pub payout: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/payouts/{payout}
/// Retrieve a payout
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_payouts_payout_builder()` + `get_v1_payouts_payout_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_payouts_payout(
    client: &SimpleHttpClient,
    args: &GetV1PayoutsPayoutArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_payouts_payout_builder(client, &args.payout, args.expand)?;
    get_v1_payouts_payout_execute(builder)
}

/// POST /v1/payouts/{payout}
/// Update a payout
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payouts_payout_execute()` to send, or `post_v1_payouts_payout` for simplest API.

pub fn post_v1_payouts_payout_builder(
    client: &SimpleHttpClient,
    payout: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payouts/{}", payout,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts/{payout}
/// Update a payout
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payouts_payout_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payouts_payout_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_payout_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payouts_payout`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PayoutsPayoutArgs {
    /// Path parameter: payout
    pub payout: String,
}

/// POST /v1/payouts/{payout}
/// Update a payout
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payouts_payout_builder()` + `post_v1_payouts_payout_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_payout(
    client: &SimpleHttpClient,
    args: &PostV1PayoutsPayoutArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_payouts_payout_builder(client, &args.payout)?;
    post_v1_payouts_payout_execute(builder)
}

/// POST /v1/payouts/{payout}/cancel
/// Cancel a payout
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payouts_payout_cancel_execute()` to send, or `post_v1_payouts_payout_cancel` for simplest API.

pub fn post_v1_payouts_payout_cancel_builder(
    client: &SimpleHttpClient,
    payout: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payouts/{}/cancel", payout,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts/{payout}/cancel
/// Cancel a payout
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payouts_payout_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payouts_payout_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_payout_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payouts_payout_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PayoutsPayoutCancelArgs {
    /// Path parameter: payout
    pub payout: String,
}

/// POST /v1/payouts/{payout}/cancel
/// Cancel a payout
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payouts_payout_cancel_builder()` + `post_v1_payouts_payout_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_payout_cancel(
    client: &SimpleHttpClient,
    args: &PostV1PayoutsPayoutCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_payouts_payout_cancel_builder(client, &args.payout)?;
    post_v1_payouts_payout_cancel_execute(builder)
}

/// POST /v1/payouts/{payout}/reverse
/// Reverse a payout
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_payouts_payout_reverse_execute()` to send, or `post_v1_payouts_payout_reverse` for simplest API.

pub fn post_v1_payouts_payout_reverse_builder(
    client: &SimpleHttpClient,
    payout: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/payouts/{}/reverse", payout,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts/{payout}/reverse
/// Reverse a payout
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_payouts_payout_reverse_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_payouts_payout_reverse_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_payout_reverse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Payout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_payouts_payout_reverse`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PayoutsPayoutReverseArgs {
    /// Path parameter: payout
    pub payout: String,
}

/// POST /v1/payouts/{payout}/reverse
/// Reverse a payout
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_payouts_payout_reverse_builder()` + `post_v1_payouts_payout_reverse_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_payouts_payout_reverse(
    client: &SimpleHttpClient,
    args: &PostV1PayoutsPayoutReverseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_payouts_payout_reverse_builder(client, &args.payout)?;
    post_v1_payouts_payout_reverse_execute(builder)
}

/// GET /v1/plans
/// List all plans
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_plans_execute()` to send, or `get_v1_plans` for simplest API.

pub fn get_v1_plans_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    product: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/plans",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = product {
        query_parts.push(format!("product={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/plans
/// List all plans
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_plans_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_plans_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_plans_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_plans`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PlansArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: product
    pub product: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/plans
/// List all plans
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_plans_builder()` + `get_v1_plans_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_plans(
    client: &SimpleHttpClient,
    args: &GetV1PlansArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_plans_builder(
        client,
        args.active,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.product.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_plans_execute(builder)
}

/// POST /v1/plans
/// Create a plan
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_plans_execute()` to send, or `post_v1_plans` for simplest API.

pub fn post_v1_plans_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/plans",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/plans
/// Create a plan
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_plans_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_plans_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_plans_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Plan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/plans
/// Create a plan
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_plans_builder()` + `post_v1_plans_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_plans(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_plans_builder(client)?;
    post_v1_plans_execute(builder)
}

/// GET /v1/plans/{plan}
/// Retrieve a plan
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_plans_plan_execute()` to send, or `get_v1_plans_plan` for simplest API.

pub fn get_v1_plans_plan_builder(
    client: &SimpleHttpClient,
    plan: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/plans/{}", plan,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/plans/{plan}
/// Retrieve a plan
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_plans_plan_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_plans_plan_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_plans_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Plan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_plans_plan`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PlansPlanArgs {
    /// Path parameter: plan
    pub plan: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/plans/{plan}
/// Retrieve a plan
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_plans_plan_builder()` + `get_v1_plans_plan_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_plans_plan(
    client: &SimpleHttpClient,
    args: &GetV1PlansPlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_plans_plan_builder(client, &args.plan, args.expand)?;
    get_v1_plans_plan_execute(builder)
}

/// POST /v1/plans/{plan}
/// Update a plan
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_plans_plan_execute()` to send, or `post_v1_plans_plan` for simplest API.

pub fn post_v1_plans_plan_builder(
    client: &SimpleHttpClient,
    plan: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/plans/{}", plan,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/plans/{plan}
/// Update a plan
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_plans_plan_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_plans_plan_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_plans_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Plan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_plans_plan`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PlansPlanArgs {
    /// Path parameter: plan
    pub plan: String,
}

/// POST /v1/plans/{plan}
/// Update a plan
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_plans_plan_builder()` + `post_v1_plans_plan_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_plans_plan(
    client: &SimpleHttpClient,
    args: &PostV1PlansPlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Plan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_plans_plan_builder(client, &args.plan)?;
    post_v1_plans_plan_execute(builder)
}

/// DELETE /v1/plans/{plan}
/// Delete a plan
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_plans_plan_execute()` to send, or `delete_v1_plans_plan` for simplest API.

pub fn delete_v1_plans_plan_builder(
    client: &SimpleHttpClient,
    plan: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/plans/{}", plan,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/plans/{plan}
/// Delete a plan
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_plans_plan_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_plans_plan_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_plans_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_plan>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_plan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_plans_plan`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1PlansPlanArgs {
    /// Path parameter: plan
    pub plan: String,
}

/// DELETE /v1/plans/{plan}
/// Delete a plan
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_plans_plan_builder()` + `delete_v1_plans_plan_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_plans_plan(
    client: &SimpleHttpClient,
    args: &DeleteV1PlansPlanArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_plan>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_plans_plan_builder(client, &args.plan)?;
    delete_v1_plans_plan_execute(builder)
}

/// GET /v1/prices
/// List all prices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_prices_execute()` to send, or `get_v1_prices` for simplest API.

pub fn get_v1_prices_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    created: Option<&str>,
    currency: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    lookup_keys: Option<Vec<String>>,
    product: Option<&str>,
    recurring: Option<&str>,
    starting_after: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/prices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = currency {
        query_parts.push(format!("currency={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = lookup_keys {
        query_parts.push(format!("lookup_keys={}", val));
    }
    if let Some(val) = product {
        query_parts.push(format!("product={}", val));
    }
    if let Some(val) = recurring {
        query_parts.push(format!("recurring={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/prices
/// List all prices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_prices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_prices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_prices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PricesArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: lookup_keys
    pub lookup_keys: Option<Vec<String>>,
    /// Query parameter: product
    pub product: Option<String>,
    /// Query parameter: recurring
    pub recurring: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET /v1/prices
/// List all prices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_prices_builder()` + `get_v1_prices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_prices(
    client: &SimpleHttpClient,
    args: &GetV1PricesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_prices_builder(
        client,
        args.active,
        args.created.as_deref(),
        args.currency.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.lookup_keys,
        args.product.as_deref(),
        args.recurring.as_deref(),
        args.starting_after.as_deref(),
        args.type_rs.as_deref(),
    )?;
    get_v1_prices_execute(builder)
}

/// POST /v1/prices
/// Create a price
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_prices_execute()` to send, or `post_v1_prices` for simplest API.

pub fn post_v1_prices_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/prices",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/prices
/// Create a price
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_prices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_prices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Price>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Price = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/prices
/// Create a price
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_prices_builder()` + `post_v1_prices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_prices(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Price>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_prices_builder(client)?;
    post_v1_prices_execute(builder)
}

/// GET /v1/prices/search
/// Search prices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_prices_search_execute()` to send, or `get_v1_prices_search` for simplest API.

pub fn get_v1_prices_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/prices/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/prices/search
/// Search prices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_prices_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_prices_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_prices_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_prices_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PricesSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/prices/search
/// Search prices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_prices_search_builder()` + `get_v1_prices_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_prices_search(
    client: &SimpleHttpClient,
    args: &GetV1PricesSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_prices_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_prices_search_execute(builder)
}

/// GET /v1/prices/{price}
/// Retrieve a price
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_prices_price_execute()` to send, or `get_v1_prices_price` for simplest API.

pub fn get_v1_prices_price_builder(
    client: &SimpleHttpClient,
    price: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/prices/{}", price,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/prices/{price}
/// Retrieve a price
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_prices_price_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_prices_price_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_prices_price_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Price>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Price = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_prices_price`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PricesPriceArgs {
    /// Path parameter: price
    pub price: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/prices/{price}
/// Retrieve a price
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_prices_price_builder()` + `get_v1_prices_price_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_prices_price(
    client: &SimpleHttpClient,
    args: &GetV1PricesPriceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Price>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_prices_price_builder(client, &args.price, args.expand)?;
    get_v1_prices_price_execute(builder)
}

/// POST /v1/prices/{price}
/// Update a price
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_prices_price_execute()` to send, or `post_v1_prices_price` for simplest API.

pub fn post_v1_prices_price_builder(
    client: &SimpleHttpClient,
    price: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/prices/{}", price,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/prices/{price}
/// Update a price
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_prices_price_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_prices_price_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_prices_price_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Price>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Price = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_prices_price`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PricesPriceArgs {
    /// Path parameter: price
    pub price: String,
}

/// POST /v1/prices/{price}
/// Update a price
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_prices_price_builder()` + `post_v1_prices_price_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_prices_price(
    client: &SimpleHttpClient,
    args: &PostV1PricesPriceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Price>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_prices_price_builder(client, &args.price)?;
    post_v1_prices_price_execute(builder)
}

/// GET /v1/products
/// List all products
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_products_execute()` to send, or `get_v1_products` for simplest API.

pub fn get_v1_products_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    ids: Option<Vec<String>>,
    limit: Option<i32>,
    shippable: Option<bool>,
    starting_after: Option<&str>,
    url: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = ids {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = shippable {
        query_parts.push(format!("shippable={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = url {
        query_parts.push(format!("url={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/products
/// List all products
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_products_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_products_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_products`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProductsArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: ids
    pub ids: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: shippable
    pub shippable: Option<bool>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: url
    pub url: Option<String>,
}

/// GET /v1/products
/// List all products
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_products_builder()` + `get_v1_products_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products(
    client: &SimpleHttpClient,
    args: &GetV1ProductsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_products_builder(
        client,
        args.active,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.ids,
        args.limit,
        args.shippable,
        args.starting_after.as_deref(),
        args.url.as_deref(),
    )?;
    get_v1_products_execute(builder)
}

/// POST /v1/products
/// Create a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_products_execute()` to send, or `post_v1_products` for simplest API.

pub fn post_v1_products_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/products
/// Create a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_products_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_products_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_products_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/products
/// Create a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_products_builder()` + `post_v1_products_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_products(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_products_builder(client)?;
    post_v1_products_execute(builder)
}

/// GET /v1/products/search
/// Search products
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_products_search_execute()` to send, or `get_v1_products_search` for simplest API.

pub fn get_v1_products_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/products/search
/// Search products
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_products_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_products_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_products_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProductsSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/products/search
/// Search products
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_products_search_builder()` + `get_v1_products_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_search(
    client: &SimpleHttpClient,
    args: &GetV1ProductsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_products_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_products_search_execute(builder)
}

/// GET /v1/products/{id}
/// Retrieve a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_products_id_execute()` to send, or `get_v1_products_id` for simplest API.

pub fn get_v1_products_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/products/{id}
/// Retrieve a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_products_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_products_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_products_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProductsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/products/{id}
/// Retrieve a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_products_id_builder()` + `get_v1_products_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_id(
    client: &SimpleHttpClient,
    args: &GetV1ProductsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_products_id_builder(client, &args.id, args.expand)?;
    get_v1_products_id_execute(builder)
}

/// POST /v1/products/{id}
/// Update a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_products_id_execute()` to send, or `post_v1_products_id` for simplest API.

pub fn post_v1_products_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/products/{id}
/// Update a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_products_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_products_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_products_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_products_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProductsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/products/{id}
/// Update a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_products_id_builder()` + `post_v1_products_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_products_id(
    client: &SimpleHttpClient,
    args: &PostV1ProductsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_products_id_builder(client, &args.id)?;
    post_v1_products_id_execute(builder)
}

/// DELETE /v1/products/{id}
/// Delete a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_products_id_execute()` to send, or `delete_v1_products_id` for simplest API.

pub fn delete_v1_products_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products/{}", id,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/products/{id}
/// Delete a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_products_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_products_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_products_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_product>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_products_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProductsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/products/{id}
/// Delete a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_products_id_builder()` + `delete_v1_products_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_products_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProductsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_product>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_products_id_builder(client, &args.id)?;
    delete_v1_products_id_execute(builder)
}

/// GET /v1/products/{product}/features
/// List all features attached to a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_products_product_features_execute()` to send, or `get_v1_products_product_features` for simplest API.

pub fn get_v1_products_product_features_builder(
    client: &SimpleHttpClient,
    product: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products/{}/features", product,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/products/{product}/features
/// List all features attached to a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_products_product_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_products_product_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_product_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_products_product_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProductsProductFeaturesArgs {
    /// Path parameter: product
    pub product: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/products/{product}/features
/// List all features attached to a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_products_product_features_builder()` + `get_v1_products_product_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_product_features(
    client: &SimpleHttpClient,
    args: &GetV1ProductsProductFeaturesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_products_product_features_builder(
        client,
        &args.product,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_products_product_features_execute(builder)
}

/// POST /v1/products/{product}/features
/// Attach a feature to a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_products_product_features_execute()` to send, or `post_v1_products_product_features` for simplest API.

pub fn post_v1_products_product_features_builder(
    client: &SimpleHttpClient,
    product: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/products/{}/features", product,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/products/{product}/features
/// Attach a feature to a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_products_product_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_products_product_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_products_product_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product_feature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product_feature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_products_product_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProductsProductFeaturesArgs {
    /// Path parameter: product
    pub product: String,
}

/// POST /v1/products/{product}/features
/// Attach a feature to a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_products_product_features_builder()` + `post_v1_products_product_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_products_product_features(
    client: &SimpleHttpClient,
    args: &PostV1ProductsProductFeaturesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product_feature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_products_product_features_builder(client, &args.product)?;
    post_v1_products_product_features_execute(builder)
}

/// GET /v1/products/{product}/features/{id}
/// Retrieve a product_feature
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_products_product_features_id_execute()` to send, or `get_v1_products_product_features_id` for simplest API.

pub fn get_v1_products_product_features_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    product: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/products/{}/features/{}",
        id, product,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/products/{product}/features/{id}
/// Retrieve a product_feature
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_products_product_features_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_products_product_features_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_product_features_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product_feature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product_feature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_products_product_features_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProductsProductFeaturesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: product
    pub product: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/products/{product}/features/{id}
/// Retrieve a product_feature
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_products_product_features_id_builder()` + `get_v1_products_product_features_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_products_product_features_id(
    client: &SimpleHttpClient,
    args: &GetV1ProductsProductFeaturesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product_feature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_products_product_features_id_builder(client, &args.id, &args.product, args.expand)?;
    get_v1_products_product_features_id_execute(builder)
}

/// DELETE /v1/products/{product}/features/{id}
/// Remove a feature from a product
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_products_product_features_id_execute()` to send, or `delete_v1_products_product_features_id` for simplest API.

pub fn delete_v1_products_product_features_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    product: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/products/{}/features/{}",
        id, product,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/products/{product}/features/{id}
/// Remove a feature from a product
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_products_product_features_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_products_product_features_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_products_product_features_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_product_feature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_product_feature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_products_product_features_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ProductsProductFeaturesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: product
    pub product: String,
}

/// DELETE /v1/products/{product}/features/{id}
/// Remove a feature from a product
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_products_product_features_id_builder()` + `delete_v1_products_product_features_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_products_product_features_id(
    client: &SimpleHttpClient,
    args: &DeleteV1ProductsProductFeaturesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_product_feature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_products_product_features_id_builder(client, &args.id, &args.product)?;
    delete_v1_products_product_features_id_execute(builder)
}

/// GET /v1/promotion_codes
/// List all promotion codes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_promotion_codes_execute()` to send, or `get_v1_promotion_codes` for simplest API.

pub fn get_v1_promotion_codes_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    code: Option<&str>,
    coupon: Option<&str>,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/promotion_codes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = code {
        query_parts.push(format!("code={}", val));
    }
    if let Some(val) = coupon {
        query_parts.push(format!("coupon={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/promotion_codes
/// List all promotion codes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_promotion_codes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_promotion_codes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_promotion_codes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_promotion_codes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PromotionCodesArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: code
    pub code: Option<String>,
    /// Query parameter: coupon
    pub coupon: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/promotion_codes
/// List all promotion codes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_promotion_codes_builder()` + `get_v1_promotion_codes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_promotion_codes(
    client: &SimpleHttpClient,
    args: &GetV1PromotionCodesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_promotion_codes_builder(
        client,
        args.active,
        args.code.as_deref(),
        args.coupon.as_deref(),
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_promotion_codes_execute(builder)
}

/// POST /v1/promotion_codes
/// Create a promotion code
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_promotion_codes_execute()` to send, or `post_v1_promotion_codes` for simplest API.

pub fn post_v1_promotion_codes_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/promotion_codes",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/promotion_codes
/// Create a promotion code
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_promotion_codes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_promotion_codes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_promotion_codes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion_code>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Promotion_code = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/promotion_codes
/// Create a promotion code
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_promotion_codes_builder()` + `post_v1_promotion_codes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_promotion_codes(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion_code>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_promotion_codes_builder(client)?;
    post_v1_promotion_codes_execute(builder)
}

/// GET /v1/promotion_codes/{promotion_code}
/// Retrieve a promotion code
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_promotion_codes_promotion_code_execute()` to send, or `get_v1_promotion_codes_promotion_code` for simplest API.

pub fn get_v1_promotion_codes_promotion_code_builder(
    client: &SimpleHttpClient,
    promotion_code: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/promotion_codes/{}",
        promotion_code,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/promotion_codes/{promotion_code}
/// Retrieve a promotion code
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_promotion_codes_promotion_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_promotion_codes_promotion_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_promotion_codes_promotion_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion_code>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Promotion_code = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_promotion_codes_promotion_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1PromotionCodesPromotionCodeArgs {
    /// Path parameter: promotion_code
    pub promotion_code: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/promotion_codes/{promotion_code}
/// Retrieve a promotion code
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_promotion_codes_promotion_code_builder()` + `get_v1_promotion_codes_promotion_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_promotion_codes_promotion_code(
    client: &SimpleHttpClient,
    args: &GetV1PromotionCodesPromotionCodeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion_code>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_promotion_codes_promotion_code_builder(client, &args.promotion_code, args.expand)?;
    get_v1_promotion_codes_promotion_code_execute(builder)
}

/// POST /v1/promotion_codes/{promotion_code}
/// Update a promotion code
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_promotion_codes_promotion_code_execute()` to send, or `post_v1_promotion_codes_promotion_code` for simplest API.

pub fn post_v1_promotion_codes_promotion_code_builder(
    client: &SimpleHttpClient,
    promotion_code: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/promotion_codes/{}",
        promotion_code,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/promotion_codes/{promotion_code}
/// Update a promotion code
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_promotion_codes_promotion_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_promotion_codes_promotion_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_promotion_codes_promotion_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion_code>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Promotion_code = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_promotion_codes_promotion_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1PromotionCodesPromotionCodeArgs {
    /// Path parameter: promotion_code
    pub promotion_code: String,
}

/// POST /v1/promotion_codes/{promotion_code}
/// Update a promotion code
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_promotion_codes_promotion_code_builder()` + `post_v1_promotion_codes_promotion_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_promotion_codes_promotion_code(
    client: &SimpleHttpClient,
    args: &PostV1PromotionCodesPromotionCodeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion_code>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_promotion_codes_promotion_code_builder(client, &args.promotion_code)?;
    post_v1_promotion_codes_promotion_code_execute(builder)
}

/// GET /v1/quotes
/// List all quotes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_quotes_execute()` to send, or `get_v1_quotes` for simplest API.

pub fn get_v1_quotes_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
    test_clock: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = test_clock {
        query_parts.push(format!("test_clock={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes
/// List all quotes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_quotes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_quotes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_quotes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1QuotesArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: test_clock
    pub test_clock: Option<String>,
}

/// GET /v1/quotes
/// List all quotes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_quotes_builder()` + `get_v1_quotes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes(
    client: &SimpleHttpClient,
    args: &GetV1QuotesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_quotes_builder(
        client,
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.test_clock.as_deref(),
    )?;
    get_v1_quotes_execute(builder)
}

/// POST /v1/quotes
/// Create a quote
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_quotes_execute()` to send, or `post_v1_quotes` for simplest API.

pub fn post_v1_quotes_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes
/// Create a quote
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_quotes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_quotes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/quotes
/// Create a quote
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_quotes_builder()` + `post_v1_quotes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_quotes_builder(client)?;
    post_v1_quotes_execute(builder)
}

/// GET /v1/quotes/{quote}
/// Retrieve a quote
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_quotes_quote_execute()` to send, or `get_v1_quotes_quote` for simplest API.

pub fn get_v1_quotes_quote_builder(
    client: &SimpleHttpClient,
    quote: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}", quote,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes/{quote}
/// Retrieve a quote
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_quotes_quote_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_quotes_quote_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_quotes_quote`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1QuotesQuoteArgs {
    /// Path parameter: quote
    pub quote: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/quotes/{quote}
/// Retrieve a quote
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_quotes_quote_builder()` + `get_v1_quotes_quote_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote(
    client: &SimpleHttpClient,
    args: &GetV1QuotesQuoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_quotes_quote_builder(client, &args.quote, args.expand)?;
    get_v1_quotes_quote_execute(builder)
}

/// POST /v1/quotes/{quote}
/// Update a quote
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_quotes_quote_execute()` to send, or `post_v1_quotes_quote` for simplest API.

pub fn post_v1_quotes_quote_builder(
    client: &SimpleHttpClient,
    quote: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}", quote,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes/{quote}
/// Update a quote
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_quotes_quote_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_quotes_quote_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_quotes_quote`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1QuotesQuoteArgs {
    /// Path parameter: quote
    pub quote: String,
}

/// POST /v1/quotes/{quote}
/// Update a quote
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_quotes_quote_builder()` + `post_v1_quotes_quote_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote(
    client: &SimpleHttpClient,
    args: &PostV1QuotesQuoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_quotes_quote_builder(client, &args.quote)?;
    post_v1_quotes_quote_execute(builder)
}

/// POST /v1/quotes/{quote}/accept
/// Accept a quote
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_quotes_quote_accept_execute()` to send, or `post_v1_quotes_quote_accept` for simplest API.

pub fn post_v1_quotes_quote_accept_builder(
    client: &SimpleHttpClient,
    quote: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}/accept", quote,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes/{quote}/accept
/// Accept a quote
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_quotes_quote_accept_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_quotes_quote_accept_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_quotes_quote_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1QuotesQuoteAcceptArgs {
    /// Path parameter: quote
    pub quote: String,
}

/// POST /v1/quotes/{quote}/accept
/// Accept a quote
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_quotes_quote_accept_builder()` + `post_v1_quotes_quote_accept_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_accept(
    client: &SimpleHttpClient,
    args: &PostV1QuotesQuoteAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_quotes_quote_accept_builder(client, &args.quote)?;
    post_v1_quotes_quote_accept_execute(builder)
}

/// POST /v1/quotes/{quote}/cancel
/// Cancel a quote
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_quotes_quote_cancel_execute()` to send, or `post_v1_quotes_quote_cancel` for simplest API.

pub fn post_v1_quotes_quote_cancel_builder(
    client: &SimpleHttpClient,
    quote: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}/cancel", quote,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes/{quote}/cancel
/// Cancel a quote
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_quotes_quote_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_quotes_quote_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_quotes_quote_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1QuotesQuoteCancelArgs {
    /// Path parameter: quote
    pub quote: String,
}

/// POST /v1/quotes/{quote}/cancel
/// Cancel a quote
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_quotes_quote_cancel_builder()` + `post_v1_quotes_quote_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_cancel(
    client: &SimpleHttpClient,
    args: &PostV1QuotesQuoteCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_quotes_quote_cancel_builder(client, &args.quote)?;
    post_v1_quotes_quote_cancel_execute(builder)
}

/// GET /v1/quotes/{quote}/computed_upfront_line_items
/// Retrieve a quote's upfront line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_quotes_quote_computed_upfront_line_items_execute()` to send, or `get_v1_quotes_quote_computed_upfront_line_items` for simplest API.

pub fn get_v1_quotes_quote_computed_upfront_line_items_builder(
    client: &SimpleHttpClient,
    quote: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/quotes/{}/computed_upfront_line_items",
        quote,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes/{quote}/computed_upfront_line_items
/// Retrieve a quote's upfront line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_quotes_quote_computed_upfront_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_quotes_quote_computed_upfront_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_computed_upfront_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_quotes_quote_computed_upfront_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1QuotesQuoteComputedUpfrontLineItemsArgs {
    /// Path parameter: quote
    pub quote: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/quotes/{quote}/computed_upfront_line_items
/// Retrieve a quote's upfront line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_quotes_quote_computed_upfront_line_items_builder()` + `get_v1_quotes_quote_computed_upfront_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_computed_upfront_line_items(
    client: &SimpleHttpClient,
    args: &GetV1QuotesQuoteComputedUpfrontLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_quotes_quote_computed_upfront_line_items_builder(
        client,
        &args.quote,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_quotes_quote_computed_upfront_line_items_execute(builder)
}

/// POST /v1/quotes/{quote}/finalize
/// Finalize a quote
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_quotes_quote_finalize_execute()` to send, or `post_v1_quotes_quote_finalize` for simplest API.

pub fn post_v1_quotes_quote_finalize_builder(
    client: &SimpleHttpClient,
    quote: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}/finalize", quote,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes/{quote}/finalize
/// Finalize a quote
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_quotes_quote_finalize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_quotes_quote_finalize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_finalize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_quotes_quote_finalize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1QuotesQuoteFinalizeArgs {
    /// Path parameter: quote
    pub quote: String,
}

/// POST /v1/quotes/{quote}/finalize
/// Finalize a quote
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_quotes_quote_finalize_builder()` + `post_v1_quotes_quote_finalize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_quotes_quote_finalize(
    client: &SimpleHttpClient,
    args: &PostV1QuotesQuoteFinalizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_quotes_quote_finalize_builder(client, &args.quote)?;
    post_v1_quotes_quote_finalize_execute(builder)
}

/// GET /v1/quotes/{quote}/line_items
/// Retrieve a quote's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_quotes_quote_line_items_execute()` to send, or `get_v1_quotes_quote_line_items` for simplest API.

pub fn get_v1_quotes_quote_line_items_builder(
    client: &SimpleHttpClient,
    quote: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}/line_items", quote,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes/{quote}/line_items
/// Retrieve a quote's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_quotes_quote_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_quotes_quote_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_quotes_quote_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1QuotesQuoteLineItemsArgs {
    /// Path parameter: quote
    pub quote: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/quotes/{quote}/line_items
/// Retrieve a quote's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_quotes_quote_line_items_builder()` + `get_v1_quotes_quote_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_line_items(
    client: &SimpleHttpClient,
    args: &GetV1QuotesQuoteLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_quotes_quote_line_items_builder(
        client,
        &args.quote,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_quotes_quote_line_items_execute(builder)
}

/// GET /v1/quotes/{quote}/pdf
/// Download quote PDF
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_quotes_quote_pdf_execute()` to send, or `get_v1_quotes_quote_pdf` for simplest API.

pub fn get_v1_quotes_quote_pdf_builder(
    client: &SimpleHttpClient,
    quote: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/quotes/{}/pdf", quote,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes/{quote}/pdf
/// Download quote PDF
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_quotes_quote_pdf_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_quotes_quote_pdf_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_pdf_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_quotes_quote_pdf`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1QuotesQuotePdfArgs {
    /// Path parameter: quote
    pub quote: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/quotes/{quote}/pdf
/// Download quote PDF
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_quotes_quote_pdf_builder()` + `get_v1_quotes_quote_pdf_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_quotes_quote_pdf(
    client: &SimpleHttpClient,
    args: &GetV1QuotesQuotePdfArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_quotes_quote_pdf_builder(client, &args.quote, args.expand)?;
    get_v1_quotes_quote_pdf_execute(builder)
}

/// GET /v1/radar/early_fraud_warnings
/// List all early fraud warnings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_radar_early_fraud_warnings_execute()` to send, or `get_v1_radar_early_fraud_warnings` for simplest API.

pub fn get_v1_radar_early_fraud_warnings_builder(
    client: &SimpleHttpClient,
    charge: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_intent: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/early_fraud_warnings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = charge {
        query_parts.push(format!("charge={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_intent {
        query_parts.push(format!("payment_intent={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/early_fraud_warnings
/// List all early fraud warnings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_radar_early_fraud_warnings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_radar_early_fraud_warnings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_early_fraud_warnings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_radar_early_fraud_warnings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RadarEarlyFraudWarningsArgs {
    /// Query parameter: charge
    pub charge: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/radar/early_fraud_warnings
/// List all early fraud warnings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_radar_early_fraud_warnings_builder()` + `get_v1_radar_early_fraud_warnings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_early_fraud_warnings(
    client: &SimpleHttpClient,
    args: &GetV1RadarEarlyFraudWarningsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_radar_early_fraud_warnings_builder(
        client,
        args.charge.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payment_intent.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_radar_early_fraud_warnings_execute(builder)
}

/// GET /v1/radar/early_fraud_warnings/{early_fraud_warning}
/// Retrieve an early fraud warning
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_radar_early_fraud_warnings_early_fraud_warning_execute()` to send, or `get_v1_radar_early_fraud_warnings_early_fraud_warning` for simplest API.

pub fn get_v1_radar_early_fraud_warnings_early_fraud_warning_builder(
    client: &SimpleHttpClient,
    early_fraud_warning: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/radar/early_fraud_warnings/{}",
        early_fraud_warning,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/early_fraud_warnings/{early_fraud_warning}
/// Retrieve an early fraud warning
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_radar_early_fraud_warnings_early_fraud_warning_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_radar_early_fraud_warnings_early_fraud_warning_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_early_fraud_warnings_early_fraud_warning_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarEarly_fraud_warning>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarEarly_fraud_warning = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_radar_early_fraud_warnings_early_fraud_warning`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RadarEarlyFraudWarningsEarlyFraudWarningArgs {
    /// Path parameter: early_fraud_warning
    pub early_fraud_warning: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/radar/early_fraud_warnings/{early_fraud_warning}
/// Retrieve an early fraud warning
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_radar_early_fraud_warnings_early_fraud_warning_builder()` + `get_v1_radar_early_fraud_warnings_early_fraud_warning_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_early_fraud_warnings_early_fraud_warning(
    client: &SimpleHttpClient,
    args: &GetV1RadarEarlyFraudWarningsEarlyFraudWarningArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarEarly_fraud_warning>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_radar_early_fraud_warnings_early_fraud_warning_builder(
        client,
        &args.early_fraud_warning,
        args.expand,
    )?;
    get_v1_radar_early_fraud_warnings_early_fraud_warning_execute(builder)
}

/// POST /v1/radar/payment_evaluations
/// Create a Payment Evaluation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_radar_payment_evaluations_execute()` to send, or `post_v1_radar_payment_evaluations` for simplest API.

pub fn post_v1_radar_payment_evaluations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/payment_evaluations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/payment_evaluations
/// Create a Payment Evaluation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_radar_payment_evaluations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_radar_payment_evaluations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_payment_evaluations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarPayment_evaluation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarPayment_evaluation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/radar/payment_evaluations
/// Create a Payment Evaluation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_radar_payment_evaluations_builder()` + `post_v1_radar_payment_evaluations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_payment_evaluations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarPayment_evaluation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_radar_payment_evaluations_builder(client)?;
    post_v1_radar_payment_evaluations_execute(builder)
}

/// GET /v1/radar/value_list_items
/// List all value list items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_radar_value_list_items_execute()` to send, or `get_v1_radar_value_list_items` for simplest API.

pub fn get_v1_radar_value_list_items_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    value: Option<&str>,
    value_list: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/value_list_items",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = value {
        query_parts.push(format!("value={}", val));
    }
    if let Some(val) = value_list {
        query_parts.push(format!("value_list={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_list_items
/// List all value list items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_radar_value_list_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_radar_value_list_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_list_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_radar_value_list_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RadarValueListItemsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: value
    pub value: Option<String>,
    /// Query parameter: value_list
    pub value_list: Option<String>,
}

/// GET /v1/radar/value_list_items
/// List all value list items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_radar_value_list_items_builder()` + `get_v1_radar_value_list_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_list_items(
    client: &SimpleHttpClient,
    args: &GetV1RadarValueListItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_radar_value_list_items_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.value.as_deref(),
        args.value_list.as_deref(),
    )?;
    get_v1_radar_value_list_items_execute(builder)
}

/// POST /v1/radar/value_list_items
/// Create a value list item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_radar_value_list_items_execute()` to send, or `post_v1_radar_value_list_items` for simplest API.

pub fn post_v1_radar_value_list_items_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/value_list_items",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/value_list_items
/// Create a value list item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_radar_value_list_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_radar_value_list_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_value_list_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarValue_list_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/radar/value_list_items
/// Create a value list item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_radar_value_list_items_builder()` + `post_v1_radar_value_list_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_value_list_items(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_radar_value_list_items_builder(client)?;
    post_v1_radar_value_list_items_execute(builder)
}

/// GET /v1/radar/value_list_items/{item}
/// Retrieve a value list item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_radar_value_list_items_item_execute()` to send, or `get_v1_radar_value_list_items_item` for simplest API.

pub fn get_v1_radar_value_list_items_item_builder(
    client: &SimpleHttpClient,
    item: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/value_list_items/{}", item,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_list_items/{item}
/// Retrieve a value list item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_radar_value_list_items_item_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_radar_value_list_items_item_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_list_items_item_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarValue_list_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_radar_value_list_items_item`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RadarValueListItemsItemArgs {
    /// Path parameter: item
    pub item: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/radar/value_list_items/{item}
/// Retrieve a value list item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_radar_value_list_items_item_builder()` + `get_v1_radar_value_list_items_item_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_list_items_item(
    client: &SimpleHttpClient,
    args: &GetV1RadarValueListItemsItemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_radar_value_list_items_item_builder(client, &args.item, args.expand)?;
    get_v1_radar_value_list_items_item_execute(builder)
}

/// DELETE /v1/radar/value_list_items/{item}
/// Delete a value list item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_radar_value_list_items_item_execute()` to send, or `delete_v1_radar_value_list_items_item` for simplest API.

pub fn delete_v1_radar_value_list_items_item_builder(
    client: &SimpleHttpClient,
    item: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/value_list_items/{}", item,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/radar/value_list_items/{item}
/// Delete a value list item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_radar_value_list_items_item_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_radar_value_list_items_item_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_radar_value_list_items_item_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Deleted_radarValue_list_item>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_radarValue_list_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_radar_value_list_items_item`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1RadarValueListItemsItemArgs {
    /// Path parameter: item
    pub item: String,
}

/// DELETE /v1/radar/value_list_items/{item}
/// Delete a value list item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_radar_value_list_items_item_builder()` + `delete_v1_radar_value_list_items_item_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_radar_value_list_items_item(
    client: &SimpleHttpClient,
    args: &DeleteV1RadarValueListItemsItemArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Deleted_radarValue_list_item>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_radar_value_list_items_item_builder(client, &args.item)?;
    delete_v1_radar_value_list_items_item_execute(builder)
}

/// GET /v1/radar/value_lists
/// List all value lists
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_radar_value_lists_execute()` to send, or `get_v1_radar_value_lists` for simplest API.

pub fn get_v1_radar_value_lists_builder(
    client: &SimpleHttpClient,
    alias: Option<&str>,
    contains: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/value_lists",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = alias {
        query_parts.push(format!("alias={}", val));
    }
    if let Some(val) = contains {
        query_parts.push(format!("contains={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_lists
/// List all value lists
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_radar_value_lists_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_radar_value_lists_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_lists_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_radar_value_lists`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RadarValueListsArgs {
    /// Query parameter: alias
    pub alias: Option<String>,
    /// Query parameter: contains
    pub contains: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/radar/value_lists
/// List all value lists
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_radar_value_lists_builder()` + `get_v1_radar_value_lists_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_lists(
    client: &SimpleHttpClient,
    args: &GetV1RadarValueListsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_radar_value_lists_builder(
        client,
        args.alias.as_deref(),
        args.contains.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_radar_value_lists_execute(builder)
}

/// POST /v1/radar/value_lists
/// Create a value list
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_radar_value_lists_execute()` to send, or `post_v1_radar_value_lists` for simplest API.

pub fn post_v1_radar_value_lists_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/radar/value_lists",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/value_lists
/// Create a value list
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_radar_value_lists_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_radar_value_lists_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_value_lists_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarValue_list = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/radar/value_lists
/// Create a value list
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_radar_value_lists_builder()` + `post_v1_radar_value_lists_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_value_lists(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_radar_value_lists_builder(client)?;
    post_v1_radar_value_lists_execute(builder)
}

/// GET /v1/radar/value_lists/{value_list}
/// Retrieve a value list
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_radar_value_lists_value_list_execute()` to send, or `get_v1_radar_value_lists_value_list` for simplest API.

pub fn get_v1_radar_value_lists_value_list_builder(
    client: &SimpleHttpClient,
    value_list: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/radar/value_lists/{}",
        value_list,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/radar/value_lists/{value_list}
/// Retrieve a value list
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_radar_value_lists_value_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_radar_value_lists_value_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_lists_value_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarValue_list = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_radar_value_lists_value_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RadarValueListsValueListArgs {
    /// Path parameter: value_list
    pub value_list: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/radar/value_lists/{value_list}
/// Retrieve a value list
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_radar_value_lists_value_list_builder()` + `get_v1_radar_value_lists_value_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_radar_value_lists_value_list(
    client: &SimpleHttpClient,
    args: &GetV1RadarValueListsValueListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_radar_value_lists_value_list_builder(client, &args.value_list, args.expand)?;
    get_v1_radar_value_lists_value_list_execute(builder)
}

/// POST /v1/radar/value_lists/{value_list}
/// Update a value list
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_radar_value_lists_value_list_execute()` to send, or `post_v1_radar_value_lists_value_list` for simplest API.

pub fn post_v1_radar_value_lists_value_list_builder(
    client: &SimpleHttpClient,
    value_list: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/radar/value_lists/{}",
        value_list,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/radar/value_lists/{value_list}
/// Update a value list
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_radar_value_lists_value_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_radar_value_lists_value_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_value_lists_value_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RadarValue_list = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_radar_value_lists_value_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1RadarValueListsValueListArgs {
    /// Path parameter: value_list
    pub value_list: String,
}

/// POST /v1/radar/value_lists/{value_list}
/// Update a value list
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_radar_value_lists_value_list_builder()` + `post_v1_radar_value_lists_value_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_radar_value_lists_value_list(
    client: &SimpleHttpClient,
    args: &PostV1RadarValueListsValueListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RadarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_radar_value_lists_value_list_builder(client, &args.value_list)?;
    post_v1_radar_value_lists_value_list_execute(builder)
}

/// DELETE /v1/radar/value_lists/{value_list}
/// Delete a value list
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_radar_value_lists_value_list_execute()` to send, or `delete_v1_radar_value_lists_value_list` for simplest API.

pub fn delete_v1_radar_value_lists_value_list_builder(
    client: &SimpleHttpClient,
    value_list: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/radar/value_lists/{}",
        value_list,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/radar/value_lists/{value_list}
/// Delete a value list
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_radar_value_lists_value_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_radar_value_lists_value_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_radar_value_lists_value_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_radarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_radarValue_list = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_radar_value_lists_value_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1RadarValueListsValueListArgs {
    /// Path parameter: value_list
    pub value_list: String,
}

/// DELETE /v1/radar/value_lists/{value_list}
/// Delete a value list
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_radar_value_lists_value_list_builder()` + `delete_v1_radar_value_lists_value_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_radar_value_lists_value_list(
    client: &SimpleHttpClient,
    args: &DeleteV1RadarValueListsValueListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_radarValue_list>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_radar_value_lists_value_list_builder(client, &args.value_list)?;
    delete_v1_radar_value_lists_value_list_execute(builder)
}

/// GET /v1/refunds
/// List all refunds
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_refunds_execute()` to send, or `get_v1_refunds` for simplest API.

pub fn get_v1_refunds_builder(
    client: &SimpleHttpClient,
    charge: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_intent: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/refunds",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = charge {
        query_parts.push(format!("charge={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_intent {
        query_parts.push(format!("payment_intent={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/refunds
/// List all refunds
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_refunds`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RefundsArgs {
    /// Query parameter: charge
    pub charge: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/refunds
/// List all refunds
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_refunds_builder()` + `get_v1_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_refunds(
    client: &SimpleHttpClient,
    args: &GetV1RefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_refunds_builder(
        client,
        args.charge.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payment_intent.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_refunds_execute(builder)
}

/// POST /v1/refunds
/// Create customer balance refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_refunds_execute()` to send, or `post_v1_refunds` for simplest API.

pub fn post_v1_refunds_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/refunds",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/refunds
/// Create customer balance refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/refunds
/// Create customer balance refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_refunds_builder()` + `post_v1_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_refunds(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_refunds_builder(client)?;
    post_v1_refunds_execute(builder)
}

/// GET /v1/refunds/{refund}
/// Retrieve a refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_refunds_refund_execute()` to send, or `get_v1_refunds_refund` for simplest API.

pub fn get_v1_refunds_refund_builder(
    client: &SimpleHttpClient,
    refund: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/refunds/{}", refund,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/refunds/{refund}
/// Retrieve a refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_refunds_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_refunds_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_refunds_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_refunds_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1RefundsRefundArgs {
    /// Path parameter: refund
    pub refund: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/refunds/{refund}
/// Retrieve a refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_refunds_refund_builder()` + `get_v1_refunds_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_refunds_refund(
    client: &SimpleHttpClient,
    args: &GetV1RefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_refunds_refund_builder(client, &args.refund, args.expand)?;
    get_v1_refunds_refund_execute(builder)
}

/// POST /v1/refunds/{refund}
/// Update a refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_refunds_refund_execute()` to send, or `post_v1_refunds_refund` for simplest API.

pub fn post_v1_refunds_refund_builder(
    client: &SimpleHttpClient,
    refund: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/refunds/{}", refund,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/refunds/{refund}
/// Update a refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_refunds_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_refunds_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_refunds_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_refunds_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1RefundsRefundArgs {
    /// Path parameter: refund
    pub refund: String,
}

/// POST /v1/refunds/{refund}
/// Update a refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_refunds_refund_builder()` + `post_v1_refunds_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_refunds_refund(
    client: &SimpleHttpClient,
    args: &PostV1RefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_refunds_refund_builder(client, &args.refund)?;
    post_v1_refunds_refund_execute(builder)
}

/// POST /v1/refunds/{refund}/cancel
/// Cancel a refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_refunds_refund_cancel_execute()` to send, or `post_v1_refunds_refund_cancel` for simplest API.

pub fn post_v1_refunds_refund_cancel_builder(
    client: &SimpleHttpClient,
    refund: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/refunds/{}/cancel", refund,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/refunds/{refund}/cancel
/// Cancel a refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_refunds_refund_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_refunds_refund_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_refunds_refund_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_refunds_refund_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1RefundsRefundCancelArgs {
    /// Path parameter: refund
    pub refund: String,
}

/// POST /v1/refunds/{refund}/cancel
/// Cancel a refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_refunds_refund_cancel_builder()` + `post_v1_refunds_refund_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_refunds_refund_cancel(
    client: &SimpleHttpClient,
    args: &PostV1RefundsRefundCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_refunds_refund_cancel_builder(client, &args.refund)?;
    post_v1_refunds_refund_cancel_execute(builder)
}

/// GET /v1/reporting/report_runs
/// List all Report Runs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_reporting_report_runs_execute()` to send, or `get_v1_reporting_report_runs` for simplest API.

pub fn get_v1_reporting_report_runs_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/reporting/report_runs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reporting/report_runs
/// List all Report Runs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_reporting_report_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_reporting_report_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_reporting_report_runs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ReportingReportRunsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/reporting/report_runs
/// List all Report Runs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_reporting_report_runs_builder()` + `get_v1_reporting_report_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_runs(
    client: &SimpleHttpClient,
    args: &GetV1ReportingReportRunsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_reporting_report_runs_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_reporting_report_runs_execute(builder)
}

/// POST /v1/reporting/report_runs
/// Create a Report Run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_reporting_report_runs_execute()` to send, or `post_v1_reporting_report_runs` for simplest API.

pub fn post_v1_reporting_report_runs_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/reporting/report_runs",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/reporting/report_runs
/// Create a Report Run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_reporting_report_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_reporting_report_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_reporting_report_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportingReport_run>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportingReport_run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/reporting/report_runs
/// Create a Report Run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_reporting_report_runs_builder()` + `post_v1_reporting_report_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_reporting_report_runs(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportingReport_run>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_reporting_report_runs_builder(client)?;
    post_v1_reporting_report_runs_execute(builder)
}

/// GET /v1/reporting/report_runs/{report_run}
/// Retrieve a Report Run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_reporting_report_runs_report_run_execute()` to send, or `get_v1_reporting_report_runs_report_run` for simplest API.

pub fn get_v1_reporting_report_runs_report_run_builder(
    client: &SimpleHttpClient,
    report_run: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/reporting/report_runs/{}",
        report_run,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reporting/report_runs/{report_run}
/// Retrieve a Report Run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_reporting_report_runs_report_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_reporting_report_runs_report_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_runs_report_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportingReport_run>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportingReport_run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_reporting_report_runs_report_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ReportingReportRunsReportRunArgs {
    /// Path parameter: report_run
    pub report_run: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/reporting/report_runs/{report_run}
/// Retrieve a Report Run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_reporting_report_runs_report_run_builder()` + `get_v1_reporting_report_runs_report_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_runs_report_run(
    client: &SimpleHttpClient,
    args: &GetV1ReportingReportRunsReportRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportingReport_run>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_reporting_report_runs_report_run_builder(client, &args.report_run, args.expand)?;
    get_v1_reporting_report_runs_report_run_execute(builder)
}

/// GET /v1/reporting/report_types
/// List all Report Types
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_reporting_report_types_execute()` to send, or `get_v1_reporting_report_types` for simplest API.

pub fn get_v1_reporting_report_types_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/reporting/report_types",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reporting/report_types
/// List all Report Types
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_reporting_report_types_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_reporting_report_types_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_types_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_reporting_report_types`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ReportingReportTypesArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/reporting/report_types
/// List all Report Types
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_reporting_report_types_builder()` + `get_v1_reporting_report_types_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_types(
    client: &SimpleHttpClient,
    args: &GetV1ReportingReportTypesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_reporting_report_types_builder(client, args.expand)?;
    get_v1_reporting_report_types_execute(builder)
}

/// GET /v1/reporting/report_types/{report_type}
/// Retrieve a Report Type
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_reporting_report_types_report_type_execute()` to send, or `get_v1_reporting_report_types_report_type` for simplest API.

pub fn get_v1_reporting_report_types_report_type_builder(
    client: &SimpleHttpClient,
    report_type: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/reporting/report_types/{}",
        report_type,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reporting/report_types/{report_type}
/// Retrieve a Report Type
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_reporting_report_types_report_type_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_reporting_report_types_report_type_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_types_report_type_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportingReport_type>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportingReport_type = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_reporting_report_types_report_type`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ReportingReportTypesReportTypeArgs {
    /// Path parameter: report_type
    pub report_type: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/reporting/report_types/{report_type}
/// Retrieve a Report Type
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_reporting_report_types_report_type_builder()` + `get_v1_reporting_report_types_report_type_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reporting_report_types_report_type(
    client: &SimpleHttpClient,
    args: &GetV1ReportingReportTypesReportTypeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportingReport_type>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_reporting_report_types_report_type_builder(client, &args.report_type, args.expand)?;
    get_v1_reporting_report_types_report_type_execute(builder)
}

/// GET /v1/reviews
/// List all open reviews
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_reviews_execute()` to send, or `get_v1_reviews` for simplest API.

pub fn get_v1_reviews_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/reviews",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reviews
/// List all open reviews
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_reviews_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_reviews_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reviews_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_reviews`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ReviewsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/reviews
/// List all open reviews
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_reviews_builder()` + `get_v1_reviews_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reviews(
    client: &SimpleHttpClient,
    args: &GetV1ReviewsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_reviews_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_reviews_execute(builder)
}

/// GET /v1/reviews/{review}
/// Retrieve a review
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_reviews_review_execute()` to send, or `get_v1_reviews_review` for simplest API.

pub fn get_v1_reviews_review_builder(
    client: &SimpleHttpClient,
    review: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/reviews/{}", review,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/reviews/{review}
/// Retrieve a review
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_reviews_review_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_reviews_review_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reviews_review_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Review = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_reviews_review`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ReviewsReviewArgs {
    /// Path parameter: review
    pub review: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/reviews/{review}
/// Retrieve a review
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_reviews_review_builder()` + `get_v1_reviews_review_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_reviews_review(
    client: &SimpleHttpClient,
    args: &GetV1ReviewsReviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_reviews_review_builder(client, &args.review, args.expand)?;
    get_v1_reviews_review_execute(builder)
}

/// POST /v1/reviews/{review}/approve
/// Approve a review
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_reviews_review_approve_execute()` to send, or `post_v1_reviews_review_approve` for simplest API.

pub fn post_v1_reviews_review_approve_builder(
    client: &SimpleHttpClient,
    review: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/reviews/{}/approve", review,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/reviews/{review}/approve
/// Approve a review
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_reviews_review_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_reviews_review_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_reviews_review_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Review = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_reviews_review_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ReviewsReviewApproveArgs {
    /// Path parameter: review
    pub review: String,
}

/// POST /v1/reviews/{review}/approve
/// Approve a review
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_reviews_review_approve_builder()` + `post_v1_reviews_review_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_reviews_review_approve(
    client: &SimpleHttpClient,
    args: &PostV1ReviewsReviewApproveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_reviews_review_approve_builder(client, &args.review)?;
    post_v1_reviews_review_approve_execute(builder)
}

/// GET /v1/setup_attempts
/// List all SetupAttempts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_setup_attempts_execute()` to send, or `get_v1_setup_attempts` for simplest API.

pub fn get_v1_setup_attempts_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    setup_intent: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/setup_attempts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = setup_intent {
        query_parts.push(format!("setup_intent={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/setup_attempts
/// List all SetupAttempts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_setup_attempts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_setup_attempts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_setup_attempts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_setup_attempts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SetupAttemptsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: setup_intent
    pub setup_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/setup_attempts
/// List all SetupAttempts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_setup_attempts_builder()` + `get_v1_setup_attempts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_setup_attempts(
    client: &SimpleHttpClient,
    args: &GetV1SetupAttemptsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_setup_attempts_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.setup_intent.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_setup_attempts_execute(builder)
}

/// GET /v1/setup_intents
/// List all SetupIntents
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_setup_intents_execute()` to send, or `get_v1_setup_intents` for simplest API.

pub fn get_v1_setup_intents_builder(
    client: &SimpleHttpClient,
    attach_to_self: Option<bool>,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    payment_method: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/setup_intents",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = attach_to_self {
        query_parts.push(format!("attach_to_self={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = payment_method {
        query_parts.push(format!("payment_method={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/setup_intents
/// List all SetupIntents
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_setup_intents_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_setup_intents_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_setup_intents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_setup_intents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SetupIntentsArgs {
    /// Query parameter: attach_to_self
    pub attach_to_self: Option<bool>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: payment_method
    pub payment_method: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/setup_intents
/// List all SetupIntents
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_setup_intents_builder()` + `get_v1_setup_intents_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_setup_intents(
    client: &SimpleHttpClient,
    args: &GetV1SetupIntentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_setup_intents_builder(
        client,
        args.attach_to_self,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.payment_method.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_setup_intents_execute(builder)
}

/// POST /v1/setup_intents
/// Create a SetupIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_setup_intents_execute()` to send, or `post_v1_setup_intents` for simplest API.

pub fn post_v1_setup_intents_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/setup_intents",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents
/// Create a SetupIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_setup_intents_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_setup_intents_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setup_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/setup_intents
/// Create a SetupIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_setup_intents_builder()` + `post_v1_setup_intents_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_setup_intents_builder(client)?;
    post_v1_setup_intents_execute(builder)
}

/// GET /v1/setup_intents/{intent}
/// Retrieve a SetupIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_setup_intents_intent_execute()` to send, or `get_v1_setup_intents_intent` for simplest API.

pub fn get_v1_setup_intents_intent_builder(
    client: &SimpleHttpClient,
    intent: &str,
    client_secret: Option<&str>,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/setup_intents/{}", intent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_secret {
        query_parts.push(format!("client_secret={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/setup_intents/{intent}
/// Retrieve a SetupIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_setup_intents_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_setup_intents_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_setup_intents_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setup_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_setup_intents_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SetupIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
    /// Query parameter: client_secret
    pub client_secret: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/setup_intents/{intent}
/// Retrieve a SetupIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_setup_intents_intent_builder()` + `get_v1_setup_intents_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_setup_intents_intent(
    client: &SimpleHttpClient,
    args: &GetV1SetupIntentsIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_setup_intents_intent_builder(
        client,
        &args.intent,
        args.client_secret.as_deref(),
        args.expand,
    )?;
    get_v1_setup_intents_intent_execute(builder)
}

/// POST /v1/setup_intents/{intent}
/// Update a SetupIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_setup_intents_intent_execute()` to send, or `post_v1_setup_intents_intent` for simplest API.

pub fn post_v1_setup_intents_intent_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/setup_intents/{}", intent,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents/{intent}
/// Update a SetupIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_setup_intents_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_setup_intents_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setup_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_setup_intents_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SetupIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/setup_intents/{intent}
/// Update a SetupIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_setup_intents_intent_builder()` + `post_v1_setup_intents_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent(
    client: &SimpleHttpClient,
    args: &PostV1SetupIntentsIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_setup_intents_intent_builder(client, &args.intent)?;
    post_v1_setup_intents_intent_execute(builder)
}

/// POST /v1/setup_intents/{intent}/cancel
/// Cancel a SetupIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_setup_intents_intent_cancel_execute()` to send, or `post_v1_setup_intents_intent_cancel` for simplest API.

pub fn post_v1_setup_intents_intent_cancel_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/setup_intents/{}/cancel", intent,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents/{intent}/cancel
/// Cancel a SetupIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_setup_intents_intent_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_setup_intents_intent_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setup_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_setup_intents_intent_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SetupIntentsIntentCancelArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/setup_intents/{intent}/cancel
/// Cancel a SetupIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_setup_intents_intent_cancel_builder()` + `post_v1_setup_intents_intent_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_cancel(
    client: &SimpleHttpClient,
    args: &PostV1SetupIntentsIntentCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_setup_intents_intent_cancel_builder(client, &args.intent)?;
    post_v1_setup_intents_intent_cancel_execute(builder)
}

/// POST /v1/setup_intents/{intent}/confirm
/// Confirm a SetupIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_setup_intents_intent_confirm_execute()` to send, or `post_v1_setup_intents_intent_confirm` for simplest API.

pub fn post_v1_setup_intents_intent_confirm_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/setup_intents/{}/confirm",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents/{intent}/confirm
/// Confirm a SetupIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_setup_intents_intent_confirm_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_setup_intents_intent_confirm_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_confirm_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setup_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_setup_intents_intent_confirm`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SetupIntentsIntentConfirmArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/setup_intents/{intent}/confirm
/// Confirm a SetupIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_setup_intents_intent_confirm_builder()` + `post_v1_setup_intents_intent_confirm_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_confirm(
    client: &SimpleHttpClient,
    args: &PostV1SetupIntentsIntentConfirmArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_setup_intents_intent_confirm_builder(client, &args.intent)?;
    post_v1_setup_intents_intent_confirm_execute(builder)
}

/// POST /v1/setup_intents/{intent}/verify_microdeposits
/// Verify microdeposits on a SetupIntent
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_setup_intents_intent_verify_microdeposits_execute()` to send, or `post_v1_setup_intents_intent_verify_microdeposits` for simplest API.

pub fn post_v1_setup_intents_intent_verify_microdeposits_builder(
    client: &SimpleHttpClient,
    intent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/setup_intents/{}/verify_microdeposits",
        intent,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents/{intent}/verify_microdeposits
/// Verify microdeposits on a SetupIntent
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_setup_intents_intent_verify_microdeposits_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_setup_intents_intent_verify_microdeposits_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_verify_microdeposits_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setup_intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_setup_intents_intent_verify_microdeposits`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SetupIntentsIntentVerifyMicrodepositsArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// POST /v1/setup_intents/{intent}/verify_microdeposits
/// Verify microdeposits on a SetupIntent
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_setup_intents_intent_verify_microdeposits_builder()` + `post_v1_setup_intents_intent_verify_microdeposits_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_setup_intents_intent_verify_microdeposits(
    client: &SimpleHttpClient,
    args: &PostV1SetupIntentsIntentVerifyMicrodepositsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setup_intent>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_setup_intents_intent_verify_microdeposits_builder(client, &args.intent)?;
    post_v1_setup_intents_intent_verify_microdeposits_execute(builder)
}

/// GET /v1/shipping_rates
/// List all shipping rates
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_shipping_rates_execute()` to send, or `get_v1_shipping_rates` for simplest API.

pub fn get_v1_shipping_rates_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    created: Option<&str>,
    currency: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/shipping_rates",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = currency {
        query_parts.push(format!("currency={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/shipping_rates
/// List all shipping rates
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_shipping_rates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_shipping_rates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_shipping_rates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_shipping_rates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ShippingRatesArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/shipping_rates
/// List all shipping rates
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_shipping_rates_builder()` + `get_v1_shipping_rates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_shipping_rates(
    client: &SimpleHttpClient,
    args: &GetV1ShippingRatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_shipping_rates_builder(
        client,
        args.active,
        args.created.as_deref(),
        args.currency.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_shipping_rates_execute(builder)
}

/// POST /v1/shipping_rates
/// Create a shipping rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_shipping_rates_execute()` to send, or `post_v1_shipping_rates` for simplest API.

pub fn post_v1_shipping_rates_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/shipping_rates",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/shipping_rates
/// Create a shipping rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_shipping_rates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_shipping_rates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_shipping_rates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Shipping_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Shipping_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/shipping_rates
/// Create a shipping rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_shipping_rates_builder()` + `post_v1_shipping_rates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_shipping_rates(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Shipping_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_shipping_rates_builder(client)?;
    post_v1_shipping_rates_execute(builder)
}

/// GET /v1/shipping_rates/{shipping_rate_token}
/// Retrieve a shipping rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_shipping_rates_shipping_rate_token_execute()` to send, or `get_v1_shipping_rates_shipping_rate_token` for simplest API.

pub fn get_v1_shipping_rates_shipping_rate_token_builder(
    client: &SimpleHttpClient,
    shipping_rate_token: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/shipping_rates/{}",
        shipping_rate_token,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/shipping_rates/{shipping_rate_token}
/// Retrieve a shipping rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_shipping_rates_shipping_rate_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_shipping_rates_shipping_rate_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_shipping_rates_shipping_rate_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Shipping_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Shipping_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_shipping_rates_shipping_rate_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ShippingRatesShippingRateTokenArgs {
    /// Path parameter: shipping_rate_token
    pub shipping_rate_token: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/shipping_rates/{shipping_rate_token}
/// Retrieve a shipping rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_shipping_rates_shipping_rate_token_builder()` + `get_v1_shipping_rates_shipping_rate_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_shipping_rates_shipping_rate_token(
    client: &SimpleHttpClient,
    args: &GetV1ShippingRatesShippingRateTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Shipping_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_shipping_rates_shipping_rate_token_builder(
        client,
        &args.shipping_rate_token,
        args.expand,
    )?;
    get_v1_shipping_rates_shipping_rate_token_execute(builder)
}

/// POST /v1/shipping_rates/{shipping_rate_token}
/// Update a shipping rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_shipping_rates_shipping_rate_token_execute()` to send, or `post_v1_shipping_rates_shipping_rate_token` for simplest API.

pub fn post_v1_shipping_rates_shipping_rate_token_builder(
    client: &SimpleHttpClient,
    shipping_rate_token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/shipping_rates/{}",
        shipping_rate_token,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/shipping_rates/{shipping_rate_token}
/// Update a shipping rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_shipping_rates_shipping_rate_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_shipping_rates_shipping_rate_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_shipping_rates_shipping_rate_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Shipping_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Shipping_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_shipping_rates_shipping_rate_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ShippingRatesShippingRateTokenArgs {
    /// Path parameter: shipping_rate_token
    pub shipping_rate_token: String,
}

/// POST /v1/shipping_rates/{shipping_rate_token}
/// Update a shipping rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_shipping_rates_shipping_rate_token_builder()` + `post_v1_shipping_rates_shipping_rate_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_shipping_rates_shipping_rate_token(
    client: &SimpleHttpClient,
    args: &PostV1ShippingRatesShippingRateTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Shipping_rate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_shipping_rates_shipping_rate_token_builder(client, &args.shipping_rate_token)?;
    post_v1_shipping_rates_shipping_rate_token_execute(builder)
}

/// POST /v1/sigma/saved_queries/{id}
/// Update an existing Sigma Query
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_sigma_saved_queries_id_execute()` to send, or `post_v1_sigma_saved_queries_id` for simplest API.

pub fn post_v1_sigma_saved_queries_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/sigma/saved_queries/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/sigma/saved_queries/{id}
/// Update an existing Sigma Query
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_sigma_saved_queries_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_sigma_saved_queries_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sigma_saved_queries_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigmaSigma_api_query>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SigmaSigma_api_query = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_sigma_saved_queries_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SigmaSavedQueriesIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/sigma/saved_queries/{id}
/// Update an existing Sigma Query
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_sigma_saved_queries_id_builder()` + `post_v1_sigma_saved_queries_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sigma_saved_queries_id(
    client: &SimpleHttpClient,
    args: &PostV1SigmaSavedQueriesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SigmaSigma_api_query>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_sigma_saved_queries_id_builder(client, &args.id)?;
    post_v1_sigma_saved_queries_id_execute(builder)
}

/// GET /v1/sigma/scheduled_query_runs
/// List all scheduled query runs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_sigma_scheduled_query_runs_execute()` to send, or `get_v1_sigma_scheduled_query_runs` for simplest API.

pub fn get_v1_sigma_scheduled_query_runs_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/sigma/scheduled_query_runs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/sigma/scheduled_query_runs
/// List all scheduled query runs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_sigma_scheduled_query_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_sigma_scheduled_query_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sigma_scheduled_query_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_sigma_scheduled_query_runs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SigmaScheduledQueryRunsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/sigma/scheduled_query_runs
/// List all scheduled query runs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_sigma_scheduled_query_runs_builder()` + `get_v1_sigma_scheduled_query_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sigma_scheduled_query_runs(
    client: &SimpleHttpClient,
    args: &GetV1SigmaScheduledQueryRunsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_sigma_scheduled_query_runs_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_sigma_scheduled_query_runs_execute(builder)
}

/// GET /v1/sigma/scheduled_query_runs/{scheduled_query_run}
/// Retrieve a scheduled query run
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_sigma_scheduled_query_runs_scheduled_query_run_execute()` to send, or `get_v1_sigma_scheduled_query_runs_scheduled_query_run` for simplest API.

pub fn get_v1_sigma_scheduled_query_runs_scheduled_query_run_builder(
    client: &SimpleHttpClient,
    scheduled_query_run: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/sigma/scheduled_query_runs/{}",
        scheduled_query_run,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/sigma/scheduled_query_runs/{scheduled_query_run}
/// Retrieve a scheduled query run
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_sigma_scheduled_query_runs_scheduled_query_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_sigma_scheduled_query_runs_scheduled_query_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sigma_scheduled_query_runs_scheduled_query_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Scheduled_query_run>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Scheduled_query_run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_sigma_scheduled_query_runs_scheduled_query_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SigmaScheduledQueryRunsScheduledQueryRunArgs {
    /// Path parameter: scheduled_query_run
    pub scheduled_query_run: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/sigma/scheduled_query_runs/{scheduled_query_run}
/// Retrieve a scheduled query run
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_sigma_scheduled_query_runs_scheduled_query_run_builder()` + `get_v1_sigma_scheduled_query_runs_scheduled_query_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sigma_scheduled_query_runs_scheduled_query_run(
    client: &SimpleHttpClient,
    args: &GetV1SigmaScheduledQueryRunsScheduledQueryRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Scheduled_query_run>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_sigma_scheduled_query_runs_scheduled_query_run_builder(
        client,
        &args.scheduled_query_run,
        args.expand,
    )?;
    get_v1_sigma_scheduled_query_runs_scheduled_query_run_execute(builder)
}

/// POST /v1/sources
/// Shares a source
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_sources_execute()` to send, or `post_v1_sources` for simplest API.

pub fn post_v1_sources_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/sources",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/sources
/// Shares a source
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/sources
/// Shares a source
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_sources_builder()` + `post_v1_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sources(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_sources_builder(client)?;
    post_v1_sources_execute(builder)
}

/// GET /v1/sources/{source}
/// Retrieve a source
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_sources_source_execute()` to send, or `get_v1_sources_source` for simplest API.

pub fn get_v1_sources_source_builder(
    client: &SimpleHttpClient,
    source: &str,
    client_secret: Option<&str>,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/sources/{}", source,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = client_secret {
        query_parts.push(format!("client_secret={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/sources/{source}
/// Retrieve a source
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_sources_source_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_sources_source_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_sources_source`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SourcesSourceArgs {
    /// Path parameter: source
    pub source: String,
    /// Query parameter: client_secret
    pub client_secret: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/sources/{source}
/// Retrieve a source
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_sources_source_builder()` + `get_v1_sources_source_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source(
    client: &SimpleHttpClient,
    args: &GetV1SourcesSourceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_sources_source_builder(
        client,
        &args.source,
        args.client_secret.as_deref(),
        args.expand,
    )?;
    get_v1_sources_source_execute(builder)
}

/// POST /v1/sources/{source}
/// Update a source
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_sources_source_execute()` to send, or `post_v1_sources_source` for simplest API.

pub fn post_v1_sources_source_builder(
    client: &SimpleHttpClient,
    source: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/sources/{}", source,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/sources/{source}
/// Update a source
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_sources_source_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_sources_source_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sources_source_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_sources_source`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SourcesSourceArgs {
    /// Path parameter: source
    pub source: String,
}

/// POST /v1/sources/{source}
/// Update a source
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_sources_source_builder()` + `post_v1_sources_source_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sources_source(
    client: &SimpleHttpClient,
    args: &PostV1SourcesSourceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_sources_source_builder(client, &args.source)?;
    post_v1_sources_source_execute(builder)
}

/// GET /v1/sources/{source}/mandate_notifications/{mandate_notification}
/// Retrieve a Source MandateNotification
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_sources_source_mandate_notifications_mandate_notification_execute()` to send, or `get_v1_sources_source_mandate_notifications_mandate_notification` for simplest API.

pub fn get_v1_sources_source_mandate_notifications_mandate_notification_builder(
    client: &SimpleHttpClient,
    mandate_notification: &str,
    source: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/sources/{}/mandate_notifications/{}",
        mandate_notification, source,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/sources/{source}/mandate_notifications/{mandate_notification}
/// Retrieve a Source MandateNotification
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_sources_source_mandate_notifications_mandate_notification_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_sources_source_mandate_notifications_mandate_notification_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_mandate_notifications_mandate_notification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Source_mandate_notification>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source_mandate_notification = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_sources_source_mandate_notifications_mandate_notification`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SourcesSourceMandateNotificationsMandateNotificationArgs {
    /// Path parameter: mandate_notification
    pub mandate_notification: String,
    /// Path parameter: source
    pub source: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/sources/{source}/mandate_notifications/{mandate_notification}
/// Retrieve a Source MandateNotification
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_sources_source_mandate_notifications_mandate_notification_builder()` + `get_v1_sources_source_mandate_notifications_mandate_notification_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_mandate_notifications_mandate_notification(
    client: &SimpleHttpClient,
    args: &GetV1SourcesSourceMandateNotificationsMandateNotificationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Source_mandate_notification>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_sources_source_mandate_notifications_mandate_notification_builder(
        client,
        &args.mandate_notification,
        &args.source,
        args.expand,
    )?;
    get_v1_sources_source_mandate_notifications_mandate_notification_execute(builder)
}

/// GET /v1/sources/{source}/source_transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_sources_source_source_transactions_execute()` to send, or `get_v1_sources_source_source_transactions` for simplest API.

pub fn get_v1_sources_source_source_transactions_builder(
    client: &SimpleHttpClient,
    source: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/sources/{}/source_transactions",
        source,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/sources/{source}/source_transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_sources_source_source_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_sources_source_source_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_source_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_sources_source_source_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SourcesSourceSourceTransactionsArgs {
    /// Path parameter: source
    pub source: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/sources/{source}/source_transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_sources_source_source_transactions_builder()` + `get_v1_sources_source_source_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_source_transactions(
    client: &SimpleHttpClient,
    args: &GetV1SourcesSourceSourceTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_sources_source_source_transactions_builder(
        client,
        &args.source,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_sources_source_source_transactions_execute(builder)
}

/// GET /v1/sources/{source}/source_transactions/{source_transaction}
/// Retrieve a source transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_sources_source_source_transactions_source_transaction_execute()` to send, or `get_v1_sources_source_source_transactions_source_transaction` for simplest API.

pub fn get_v1_sources_source_source_transactions_source_transaction_builder(
    client: &SimpleHttpClient,
    source: &str,
    source_transaction: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/sources/{}/source_transactions/{}",
        source, source_transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/sources/{source}/source_transactions/{source_transaction}
/// Retrieve a source transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_sources_source_source_transactions_source_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_sources_source_source_transactions_source_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_source_transactions_source_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source_transaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_sources_source_source_transactions_source_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SourcesSourceSourceTransactionsSourceTransactionArgs {
    /// Path parameter: source
    pub source: String,
    /// Path parameter: source_transaction
    pub source_transaction: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/sources/{source}/source_transactions/{source_transaction}
/// Retrieve a source transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_sources_source_source_transactions_source_transaction_builder()` + `get_v1_sources_source_source_transactions_source_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_sources_source_source_transactions_source_transaction(
    client: &SimpleHttpClient,
    args: &GetV1SourcesSourceSourceTransactionsSourceTransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source_transaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_sources_source_source_transactions_source_transaction_builder(
        client,
        &args.source,
        &args.source_transaction,
        args.expand,
    )?;
    get_v1_sources_source_source_transactions_source_transaction_execute(builder)
}

/// POST /v1/sources/{source}/verify
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_sources_source_verify_execute()` to send, or `post_v1_sources_source_verify` for simplest API.

pub fn post_v1_sources_source_verify_builder(
    client: &SimpleHttpClient,
    source: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/sources/{}/verify", source,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/sources/{source}/verify
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_sources_source_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_sources_source_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sources_source_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_sources_source_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SourcesSourceVerifyArgs {
    /// Path parameter: source
    pub source: String,
}

/// POST /v1/sources/{source}/verify
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_sources_source_verify_builder()` + `post_v1_sources_source_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_sources_source_verify(
    client: &SimpleHttpClient,
    args: &PostV1SourcesSourceVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_sources_source_verify_builder(client, &args.source)?;
    post_v1_sources_source_verify_execute(builder)
}

/// GET /v1/subscription_items
/// List all subscription items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscription_items_execute()` to send, or `get_v1_subscription_items` for simplest API.

pub fn get_v1_subscription_items_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    subscription: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_items",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = subscription {
        query_parts.push(format!("subscription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_items
/// List all subscription items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscription_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscription_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscription_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionItemsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: subscription
    pub subscription: Option<String>,
}

/// GET /v1/subscription_items
/// List all subscription items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscription_items_builder()` + `get_v1_subscription_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_items(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_subscription_items_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.subscription.as_deref(),
    )?;
    get_v1_subscription_items_execute(builder)
}

/// POST /v1/subscription_items
/// Create a subscription item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscription_items_execute()` to send, or `post_v1_subscription_items` for simplest API.

pub fn post_v1_subscription_items_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_items",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_items
/// Create a subscription item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscription_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscription_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/subscription_items
/// Create a subscription item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscription_items_builder()` + `post_v1_subscription_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_items(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscription_items_builder(client)?;
    post_v1_subscription_items_execute(builder)
}

/// GET /v1/subscription_items/{item}
/// Retrieve a subscription item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscription_items_item_execute()` to send, or `get_v1_subscription_items_item` for simplest API.

pub fn get_v1_subscription_items_item_builder(
    client: &SimpleHttpClient,
    item: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_items/{}", item,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_items/{item}
/// Retrieve a subscription item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscription_items_item_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscription_items_item_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_items_item_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscription_items_item`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionItemsItemArgs {
    /// Path parameter: item
    pub item: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/subscription_items/{item}
/// Retrieve a subscription item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscription_items_item_builder()` + `get_v1_subscription_items_item_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_items_item(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionItemsItemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_subscription_items_item_builder(client, &args.item, args.expand)?;
    get_v1_subscription_items_item_execute(builder)
}

/// POST /v1/subscription_items/{item}
/// Update a subscription item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscription_items_item_execute()` to send, or `post_v1_subscription_items_item` for simplest API.

pub fn post_v1_subscription_items_item_builder(
    client: &SimpleHttpClient,
    item: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_items/{}", item,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_items/{item}
/// Update a subscription item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscription_items_item_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscription_items_item_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_items_item_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscription_items_item`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionItemsItemArgs {
    /// Path parameter: item
    pub item: String,
}

/// POST /v1/subscription_items/{item}
/// Update a subscription item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscription_items_item_builder()` + `post_v1_subscription_items_item_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_items_item(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionItemsItemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscription_items_item_builder(client, &args.item)?;
    post_v1_subscription_items_item_execute(builder)
}

/// DELETE /v1/subscription_items/{item}
/// Delete a subscription item
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_subscription_items_item_execute()` to send, or `delete_v1_subscription_items_item` for simplest API.

pub fn delete_v1_subscription_items_item_builder(
    client: &SimpleHttpClient,
    item: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_items/{}", item,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/subscription_items/{item}
/// Delete a subscription item
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_subscription_items_item_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_subscription_items_item_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_subscription_items_item_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_subscription_item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_subscription_items_item`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1SubscriptionItemsItemArgs {
    /// Path parameter: item
    pub item: String,
}

/// DELETE /v1/subscription_items/{item}
/// Delete a subscription item
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_subscription_items_item_builder()` + `delete_v1_subscription_items_item_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_subscription_items_item(
    client: &SimpleHttpClient,
    args: &DeleteV1SubscriptionItemsItemArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_subscription_item>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_subscription_items_item_builder(client, &args.item)?;
    delete_v1_subscription_items_item_execute(builder)
}

/// GET /v1/subscription_schedules
/// List all schedules
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscription_schedules_execute()` to send, or `get_v1_subscription_schedules` for simplest API.

pub fn get_v1_subscription_schedules_builder(
    client: &SimpleHttpClient,
    canceled_at: Option<&str>,
    completed_at: Option<&str>,
    created: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    released_at: Option<&str>,
    scheduled: Option<bool>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_schedules",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = canceled_at {
        query_parts.push(format!("canceled_at={}", val));
    }
    if let Some(val) = completed_at {
        query_parts.push(format!("completed_at={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = released_at {
        query_parts.push(format!("released_at={}", val));
    }
    if let Some(val) = scheduled {
        query_parts.push(format!("scheduled={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_schedules
/// List all schedules
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscription_schedules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscription_schedules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_schedules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscription_schedules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionSchedulesArgs {
    /// Query parameter: canceled_at
    pub canceled_at: Option<String>,
    /// Query parameter: completed_at
    pub completed_at: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: released_at
    pub released_at: Option<String>,
    /// Query parameter: scheduled
    pub scheduled: Option<bool>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/subscription_schedules
/// List all schedules
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscription_schedules_builder()` + `get_v1_subscription_schedules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_schedules(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionSchedulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_subscription_schedules_builder(
        client,
        args.canceled_at.as_deref(),
        args.completed_at.as_deref(),
        args.created.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.released_at.as_deref(),
        args.scheduled,
        args.starting_after.as_deref(),
    )?;
    get_v1_subscription_schedules_execute(builder)
}

/// POST /v1/subscription_schedules
/// Create a schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscription_schedules_execute()` to send, or `post_v1_subscription_schedules` for simplest API.

pub fn post_v1_subscription_schedules_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscription_schedules",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules
/// Create a schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscription_schedules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscription_schedules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/subscription_schedules
/// Create a schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscription_schedules_builder()` + `post_v1_subscription_schedules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscription_schedules_builder(client)?;
    post_v1_subscription_schedules_execute(builder)
}

/// GET /v1/subscription_schedules/{schedule}
/// Retrieve a schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscription_schedules_schedule_execute()` to send, or `get_v1_subscription_schedules_schedule` for simplest API.

pub fn get_v1_subscription_schedules_schedule_builder(
    client: &SimpleHttpClient,
    schedule: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{}",
        schedule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_schedules/{schedule}
/// Retrieve a schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscription_schedules_schedule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscription_schedules_schedule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_schedules_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscription_schedules_schedule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionSchedulesScheduleArgs {
    /// Path parameter: schedule
    pub schedule: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/subscription_schedules/{schedule}
/// Retrieve a schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscription_schedules_schedule_builder()` + `get_v1_subscription_schedules_schedule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscription_schedules_schedule(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionSchedulesScheduleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_subscription_schedules_schedule_builder(client, &args.schedule, args.expand)?;
    get_v1_subscription_schedules_schedule_execute(builder)
}

/// POST /v1/subscription_schedules/{schedule}
/// Update a schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscription_schedules_schedule_execute()` to send, or `post_v1_subscription_schedules_schedule` for simplest API.

pub fn post_v1_subscription_schedules_schedule_builder(
    client: &SimpleHttpClient,
    schedule: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{}",
        schedule,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules/{schedule}
/// Update a schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscription_schedules_schedule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscription_schedules_schedule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscription_schedules_schedule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionSchedulesScheduleArgs {
    /// Path parameter: schedule
    pub schedule: String,
}

/// POST /v1/subscription_schedules/{schedule}
/// Update a schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscription_schedules_schedule_builder()` + `post_v1_subscription_schedules_schedule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_schedule(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionSchedulesScheduleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscription_schedules_schedule_builder(client, &args.schedule)?;
    post_v1_subscription_schedules_schedule_execute(builder)
}

/// POST /v1/subscription_schedules/{schedule}/cancel
/// Cancel a schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscription_schedules_schedule_cancel_execute()` to send, or `post_v1_subscription_schedules_schedule_cancel` for simplest API.

pub fn post_v1_subscription_schedules_schedule_cancel_builder(
    client: &SimpleHttpClient,
    schedule: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{}/cancel",
        schedule,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules/{schedule}/cancel
/// Cancel a schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscription_schedules_schedule_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscription_schedules_schedule_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_schedule_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscription_schedules_schedule_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionSchedulesScheduleCancelArgs {
    /// Path parameter: schedule
    pub schedule: String,
}

/// POST /v1/subscription_schedules/{schedule}/cancel
/// Cancel a schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscription_schedules_schedule_cancel_builder()` + `post_v1_subscription_schedules_schedule_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_schedule_cancel(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionSchedulesScheduleCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscription_schedules_schedule_cancel_builder(client, &args.schedule)?;
    post_v1_subscription_schedules_schedule_cancel_execute(builder)
}

/// POST /v1/subscription_schedules/{schedule}/release
/// Release a schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscription_schedules_schedule_release_execute()` to send, or `post_v1_subscription_schedules_schedule_release` for simplest API.

pub fn post_v1_subscription_schedules_schedule_release_builder(
    client: &SimpleHttpClient,
    schedule: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{}/release",
        schedule,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules/{schedule}/release
/// Release a schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscription_schedules_schedule_release_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscription_schedules_schedule_release_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_schedule_release_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription_schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscription_schedules_schedule_release`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionSchedulesScheduleReleaseArgs {
    /// Path parameter: schedule
    pub schedule: String,
}

/// POST /v1/subscription_schedules/{schedule}/release
/// Release a schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscription_schedules_schedule_release_builder()` + `post_v1_subscription_schedules_schedule_release_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscription_schedules_schedule_release(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionSchedulesScheduleReleaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription_schedule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscription_schedules_schedule_release_builder(client, &args.schedule)?;
    post_v1_subscription_schedules_schedule_release_execute(builder)
}

/// GET /v1/subscriptions
/// List subscriptions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscriptions_execute()` to send, or `get_v1_subscriptions` for simplest API.

pub fn get_v1_subscriptions_builder(
    client: &SimpleHttpClient,
    automatic_tax: Option<&str>,
    collection_method: Option<&str>,
    created: Option<&str>,
    current_period_end: Option<&str>,
    current_period_start: Option<&str>,
    customer: Option<&str>,
    customer_account: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    price: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
    test_clock: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscriptions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = automatic_tax {
        query_parts.push(format!("automatic_tax={}", val));
    }
    if let Some(val) = collection_method {
        query_parts.push(format!("collection_method={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = current_period_end {
        query_parts.push(format!("current_period_end={}", val));
    }
    if let Some(val) = current_period_start {
        query_parts.push(format!("current_period_start={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = customer_account {
        query_parts.push(format!("customer_account={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = price {
        query_parts.push(format!("price={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = test_clock {
        query_parts.push(format!("test_clock={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscriptions
/// List subscriptions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscriptions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscriptions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscriptions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscriptions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionsArgs {
    /// Query parameter: automatic_tax
    pub automatic_tax: Option<String>,
    /// Query parameter: collection_method
    pub collection_method: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: current_period_end
    pub current_period_end: Option<String>,
    /// Query parameter: current_period_start
    pub current_period_start: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: price
    pub price: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: test_clock
    pub test_clock: Option<String>,
}

/// GET /v1/subscriptions
/// List subscriptions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscriptions_builder()` + `get_v1_subscriptions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscriptions(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_subscriptions_builder(
        client,
        args.automatic_tax.as_deref(),
        args.collection_method.as_deref(),
        args.created.as_deref(),
        args.current_period_end.as_deref(),
        args.current_period_start.as_deref(),
        args.customer.as_deref(),
        args.customer_account.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.price.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.test_clock.as_deref(),
    )?;
    get_v1_subscriptions_execute(builder)
}

/// POST /v1/subscriptions
/// Create a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscriptions_execute()` to send, or `post_v1_subscriptions` for simplest API.

pub fn post_v1_subscriptions_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscriptions",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscriptions
/// Create a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscriptions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscriptions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/subscriptions
/// Create a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscriptions_builder()` + `post_v1_subscriptions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscriptions_builder(client)?;
    post_v1_subscriptions_execute(builder)
}

/// GET /v1/subscriptions/search
/// Search subscriptions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscriptions_search_execute()` to send, or `get_v1_subscriptions_search` for simplest API.

pub fn get_v1_subscriptions_search_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    page: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/subscriptions/search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscriptions/search
/// Search subscriptions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscriptions_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscriptions_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscriptions_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscriptions_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionsSearchArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET /v1/subscriptions/search
/// Search subscriptions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscriptions_search_builder()` + `get_v1_subscriptions_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscriptions_search(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_subscriptions_search_builder(
        client,
        args.expand,
        args.limit,
        args.page.as_deref(),
        args.query.as_deref(),
    )?;
    get_v1_subscriptions_search_execute(builder)
}

/// GET /v1/subscriptions/{subscription_exposed_id}
/// Retrieve a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_subscriptions_subscription_exposed_id_execute()` to send, or `get_v1_subscriptions_subscription_exposed_id` for simplest API.

pub fn get_v1_subscriptions_subscription_exposed_id_builder(
    client: &SimpleHttpClient,
    subscription_exposed_id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscriptions/{}",
        subscription_exposed_id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscriptions/{subscription_exposed_id}
/// Retrieve a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_subscriptions_subscription_exposed_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_subscriptions_subscription_exposed_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscriptions_subscription_exposed_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_subscriptions_subscription_exposed_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1SubscriptionsSubscriptionExposedIdArgs {
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/subscriptions/{subscription_exposed_id}
/// Retrieve a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_subscriptions_subscription_exposed_id_builder()` + `get_v1_subscriptions_subscription_exposed_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_subscriptions_subscription_exposed_id(
    client: &SimpleHttpClient,
    args: &GetV1SubscriptionsSubscriptionExposedIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_subscriptions_subscription_exposed_id_builder(
        client,
        &args.subscription_exposed_id,
        args.expand,
    )?;
    get_v1_subscriptions_subscription_exposed_id_execute(builder)
}

/// POST /v1/subscriptions/{subscription_exposed_id}
/// Update a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscriptions_subscription_exposed_id_execute()` to send, or `post_v1_subscriptions_subscription_exposed_id` for simplest API.

pub fn post_v1_subscriptions_subscription_exposed_id_builder(
    client: &SimpleHttpClient,
    subscription_exposed_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscriptions/{}",
        subscription_exposed_id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscriptions/{subscription_exposed_id}
/// Update a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscriptions_subscription_exposed_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscriptions_subscription_exposed_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_subscription_exposed_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscriptions_subscription_exposed_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionsSubscriptionExposedIdArgs {
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// POST /v1/subscriptions/{subscription_exposed_id}
/// Update a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscriptions_subscription_exposed_id_builder()` + `post_v1_subscriptions_subscription_exposed_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_subscription_exposed_id(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionsSubscriptionExposedIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscriptions_subscription_exposed_id_builder(
        client,
        &args.subscription_exposed_id,
    )?;
    post_v1_subscriptions_subscription_exposed_id_execute(builder)
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}
/// Cancel a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_subscriptions_subscription_exposed_id_execute()` to send, or `delete_v1_subscriptions_subscription_exposed_id` for simplest API.

pub fn delete_v1_subscriptions_subscription_exposed_id_builder(
    client: &SimpleHttpClient,
    subscription_exposed_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscriptions/{}",
        subscription_exposed_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}
/// Cancel a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_subscriptions_subscription_exposed_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_subscriptions_subscription_exposed_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_subscriptions_subscription_exposed_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_subscriptions_subscription_exposed_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1SubscriptionsSubscriptionExposedIdArgs {
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}
/// Cancel a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_subscriptions_subscription_exposed_id_builder()` + `delete_v1_subscriptions_subscription_exposed_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_subscriptions_subscription_exposed_id(
    client: &SimpleHttpClient,
    args: &DeleteV1SubscriptionsSubscriptionExposedIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_subscriptions_subscription_exposed_id_builder(
        client,
        &args.subscription_exposed_id,
    )?;
    delete_v1_subscriptions_subscription_exposed_id_execute(builder)
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount
/// Delete a subscription discount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_subscriptions_subscription_exposed_id_discount_execute()` to send, or `delete_v1_subscriptions_subscription_exposed_id_discount` for simplest API.

pub fn delete_v1_subscriptions_subscription_exposed_id_discount_builder(
    client: &SimpleHttpClient,
    subscription_exposed_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscriptions/{}/discount",
        subscription_exposed_id,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount
/// Delete a subscription discount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_subscriptions_subscription_exposed_id_discount_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_subscriptions_subscription_exposed_id_discount_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_subscriptions_subscription_exposed_id_discount_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_discount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_discount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_subscriptions_subscription_exposed_id_discount`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1SubscriptionsSubscriptionExposedIdDiscountArgs {
    /// Path parameter: subscription_exposed_id
    pub subscription_exposed_id: String,
}

/// DELETE /v1/subscriptions/{subscription_exposed_id}/discount
/// Delete a subscription discount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_subscriptions_subscription_exposed_id_discount_builder()` + `delete_v1_subscriptions_subscription_exposed_id_discount_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_subscriptions_subscription_exposed_id_discount(
    client: &SimpleHttpClient,
    args: &DeleteV1SubscriptionsSubscriptionExposedIdDiscountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_discount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_subscriptions_subscription_exposed_id_discount_builder(
        client,
        &args.subscription_exposed_id,
    )?;
    delete_v1_subscriptions_subscription_exposed_id_discount_execute(builder)
}

/// POST /v1/subscriptions/{subscription}/migrate
/// Migrate a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscriptions_subscription_migrate_execute()` to send, or `post_v1_subscriptions_subscription_migrate` for simplest API.

pub fn post_v1_subscriptions_subscription_migrate_builder(
    client: &SimpleHttpClient,
    subscription: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscriptions/{}/migrate",
        subscription,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscriptions/{subscription}/migrate
/// Migrate a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscriptions_subscription_migrate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscriptions_subscription_migrate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_subscription_migrate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscriptions_subscription_migrate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionsSubscriptionMigrateArgs {
    /// Path parameter: subscription
    pub subscription: String,
}

/// POST /v1/subscriptions/{subscription}/migrate
/// Migrate a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscriptions_subscription_migrate_builder()` + `post_v1_subscriptions_subscription_migrate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_subscription_migrate(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionsSubscriptionMigrateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscriptions_subscription_migrate_builder(client, &args.subscription)?;
    post_v1_subscriptions_subscription_migrate_execute(builder)
}

/// POST /v1/subscriptions/{subscription}/resume
/// Resume a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_subscriptions_subscription_resume_execute()` to send, or `post_v1_subscriptions_subscription_resume` for simplest API.

pub fn post_v1_subscriptions_subscription_resume_builder(
    client: &SimpleHttpClient,
    subscription: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/subscriptions/{}/resume",
        subscription,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscriptions/{subscription}/resume
/// Resume a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_subscriptions_subscription_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_subscriptions_subscription_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_subscription_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_subscriptions_subscription_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1SubscriptionsSubscriptionResumeArgs {
    /// Path parameter: subscription
    pub subscription: String,
}

/// POST /v1/subscriptions/{subscription}/resume
/// Resume a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_subscriptions_subscription_resume_builder()` + `post_v1_subscriptions_subscription_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_subscriptions_subscription_resume(
    client: &SimpleHttpClient,
    args: &PostV1SubscriptionsSubscriptionResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_subscriptions_subscription_resume_builder(client, &args.subscription)?;
    post_v1_subscriptions_subscription_resume_execute(builder)
}

/// GET /v1/tax/associations/find
/// Find a Tax Association
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_associations_find_execute()` to send, or `get_v1_tax_associations_find` for simplest API.

pub fn get_v1_tax_associations_find_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
    payment_intent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/associations/find",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = payment_intent {
        query_parts.push(format!("payment_intent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/associations/find
/// Find a Tax Association
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_associations_find_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_associations_find_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_associations_find_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_associations_find`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxAssociationsFindArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
}

/// GET /v1/tax/associations/find
/// Find a Tax Association
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_associations_find_builder()` + `get_v1_tax_associations_find_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_associations_find(
    client: &SimpleHttpClient,
    args: &GetV1TaxAssociationsFindArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_tax_associations_find_builder(client, args.expand, args.payment_intent.as_deref())?;
    get_v1_tax_associations_find_execute(builder)
}

/// POST /v1/tax/calculations
/// Create a Tax Calculation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_calculations_execute()` to send, or `post_v1_tax_calculations` for simplest API.

pub fn post_v1_tax_calculations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/calculations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/calculations
/// Create a Tax Calculation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_calculations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_calculations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_calculations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxCalculation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxCalculation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax/calculations
/// Create a Tax Calculation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_calculations_builder()` + `post_v1_tax_calculations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_calculations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxCalculation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_tax_calculations_builder(client)?;
    post_v1_tax_calculations_execute(builder)
}

/// GET /v1/tax/calculations/{calculation}
/// Retrieve a Tax Calculation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_calculations_calculation_execute()` to send, or `get_v1_tax_calculations_calculation` for simplest API.

pub fn get_v1_tax_calculations_calculation_builder(
    client: &SimpleHttpClient,
    calculation: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/tax/calculations/{}",
        calculation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/calculations/{calculation}
/// Retrieve a Tax Calculation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_calculations_calculation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_calculations_calculation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_calculations_calculation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxCalculation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxCalculation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_calculations_calculation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxCalculationsCalculationArgs {
    /// Path parameter: calculation
    pub calculation: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax/calculations/{calculation}
/// Retrieve a Tax Calculation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_calculations_calculation_builder()` + `get_v1_tax_calculations_calculation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_calculations_calculation(
    client: &SimpleHttpClient,
    args: &GetV1TaxCalculationsCalculationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxCalculation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_tax_calculations_calculation_builder(client, &args.calculation, args.expand)?;
    get_v1_tax_calculations_calculation_execute(builder)
}

/// GET /v1/tax/calculations/{calculation}/line_items
/// Retrieve a calculation's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_calculations_calculation_line_items_execute()` to send, or `get_v1_tax_calculations_calculation_line_items` for simplest API.

pub fn get_v1_tax_calculations_calculation_line_items_builder(
    client: &SimpleHttpClient,
    calculation: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/tax/calculations/{}/line_items",
        calculation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/calculations/{calculation}/line_items
/// Retrieve a calculation's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_calculations_calculation_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_calculations_calculation_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_calculations_calculation_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_calculations_calculation_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxCalculationsCalculationLineItemsArgs {
    /// Path parameter: calculation
    pub calculation: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/tax/calculations/{calculation}/line_items
/// Retrieve a calculation's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_calculations_calculation_line_items_builder()` + `get_v1_tax_calculations_calculation_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_calculations_calculation_line_items(
    client: &SimpleHttpClient,
    args: &GetV1TaxCalculationsCalculationLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_calculations_calculation_line_items_builder(
        client,
        &args.calculation,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_tax_calculations_calculation_line_items_execute(builder)
}

/// GET /v1/tax/registrations
/// List registrations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_registrations_execute()` to send, or `get_v1_tax_registrations` for simplest API.

pub fn get_v1_tax_registrations_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/registrations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/registrations
/// List registrations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_registrations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_registrations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_registrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_registrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxRegistrationsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/tax/registrations
/// List registrations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_registrations_builder()` + `get_v1_tax_registrations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_registrations(
    client: &SimpleHttpClient,
    args: &GetV1TaxRegistrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_registrations_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_tax_registrations_execute(builder)
}

/// POST /v1/tax/registrations
/// Create a registration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_registrations_execute()` to send, or `post_v1_tax_registrations` for simplest API.

pub fn post_v1_tax_registrations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/registrations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/registrations
/// Create a registration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_registrations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_registrations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_registrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxRegistration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax/registrations
/// Create a registration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_registrations_builder()` + `post_v1_tax_registrations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_registrations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_tax_registrations_builder(client)?;
    post_v1_tax_registrations_execute(builder)
}

/// GET /v1/tax/registrations/{id}
/// Retrieve a registration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_registrations_id_execute()` to send, or `get_v1_tax_registrations_id` for simplest API.

pub fn get_v1_tax_registrations_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/registrations/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/registrations/{id}
/// Retrieve a registration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_registrations_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_registrations_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_registrations_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxRegistration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_registrations_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxRegistrationsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax/registrations/{id}
/// Retrieve a registration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_registrations_id_builder()` + `get_v1_tax_registrations_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_registrations_id(
    client: &SimpleHttpClient,
    args: &GetV1TaxRegistrationsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_tax_registrations_id_builder(client, &args.id, args.expand)?;
    get_v1_tax_registrations_id_execute(builder)
}

/// POST /v1/tax/registrations/{id}
/// Update a registration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_registrations_id_execute()` to send, or `post_v1_tax_registrations_id` for simplest API.

pub fn post_v1_tax_registrations_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/registrations/{}", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/registrations/{id}
/// Update a registration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_registrations_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_registrations_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_registrations_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxRegistration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_tax_registrations_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TaxRegistrationsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/tax/registrations/{id}
/// Update a registration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_registrations_id_builder()` + `post_v1_tax_registrations_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_registrations_id(
    client: &SimpleHttpClient,
    args: &PostV1TaxRegistrationsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_tax_registrations_id_builder(client, &args.id)?;
    post_v1_tax_registrations_id_execute(builder)
}

/// GET /v1/tax/settings
/// Retrieve settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_settings_execute()` to send, or `get_v1_tax_settings` for simplest API.

pub fn get_v1_tax_settings_builder(
    client: &SimpleHttpClient,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/settings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/settings
/// Retrieve settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxSettingsArgs {
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax/settings
/// Retrieve settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_settings_builder()` + `get_v1_tax_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_settings(
    client: &SimpleHttpClient,
    args: &GetV1TaxSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_settings_builder(client, args.expand)?;
    get_v1_tax_settings_execute(builder)
}

/// POST /v1/tax/settings
/// Update settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_settings_execute()` to send, or `post_v1_tax_settings` for simplest API.

pub fn post_v1_tax_settings_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/settings",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/settings
/// Update settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax/settings
/// Update settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_settings_builder()` + `post_v1_tax_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_settings(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_tax_settings_builder(client)?;
    post_v1_tax_settings_execute(builder)
}

/// POST /v1/tax/transactions/create_from_calculation
/// Create a transaction from a calculation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_transactions_create_from_calculation_execute()` to send, or `post_v1_tax_transactions_create_from_calculation` for simplest API.

pub fn post_v1_tax_transactions_create_from_calculation_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/transactions/create_from_calculation",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/transactions/create_from_calculation
/// Create a transaction from a calculation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_transactions_create_from_calculation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_transactions_create_from_calculation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_transactions_create_from_calculation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax/transactions/create_from_calculation
/// Create a transaction from a calculation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_transactions_create_from_calculation_builder()` + `post_v1_tax_transactions_create_from_calculation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_transactions_create_from_calculation(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_tax_transactions_create_from_calculation_builder(client)?;
    post_v1_tax_transactions_create_from_calculation_execute(builder)
}

/// POST /v1/tax/transactions/create_reversal
/// Create a reversal transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_transactions_create_reversal_execute()` to send, or `post_v1_tax_transactions_create_reversal` for simplest API.

pub fn post_v1_tax_transactions_create_reversal_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax/transactions/create_reversal",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/transactions/create_reversal
/// Create a reversal transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_transactions_create_reversal_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_transactions_create_reversal_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_transactions_create_reversal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax/transactions/create_reversal
/// Create a reversal transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_transactions_create_reversal_builder()` + `post_v1_tax_transactions_create_reversal_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_transactions_create_reversal(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_tax_transactions_create_reversal_builder(client)?;
    post_v1_tax_transactions_create_reversal_execute(builder)
}

/// GET /v1/tax/transactions/{transaction}
/// Retrieve a transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_transactions_transaction_execute()` to send, or `get_v1_tax_transactions_transaction` for simplest API.

pub fn get_v1_tax_transactions_transaction_builder(
    client: &SimpleHttpClient,
    transaction: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/tax/transactions/{}",
        transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/transactions/{transaction}
/// Retrieve a transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_transactions_transaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_transactions_transaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_transactions_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaxTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_transactions_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxTransactionsTransactionArgs {
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax/transactions/{transaction}
/// Retrieve a transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_transactions_transaction_builder()` + `get_v1_tax_transactions_transaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_transactions_transaction(
    client: &SimpleHttpClient,
    args: &GetV1TaxTransactionsTransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TaxTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_tax_transactions_transaction_builder(client, &args.transaction, args.expand)?;
    get_v1_tax_transactions_transaction_execute(builder)
}

/// GET /v1/tax/transactions/{transaction}/line_items
/// Retrieve a transaction's line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_transactions_transaction_line_items_execute()` to send, or `get_v1_tax_transactions_transaction_line_items` for simplest API.

pub fn get_v1_tax_transactions_transaction_line_items_builder(
    client: &SimpleHttpClient,
    transaction: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/tax/transactions/{}/line_items",
        transaction,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/transactions/{transaction}/line_items
/// Retrieve a transaction's line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_transactions_transaction_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_transactions_transaction_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_transactions_transaction_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_transactions_transaction_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxTransactionsTransactionLineItemsArgs {
    /// Path parameter: transaction
    pub transaction: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/tax/transactions/{transaction}/line_items
/// Retrieve a transaction's line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_transactions_transaction_line_items_builder()` + `get_v1_tax_transactions_transaction_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_transactions_transaction_line_items(
    client: &SimpleHttpClient,
    args: &GetV1TaxTransactionsTransactionLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_transactions_transaction_line_items_builder(
        client,
        &args.transaction,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_tax_transactions_transaction_line_items_execute(builder)
}

/// GET /v1/tax_codes
/// List all tax codes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_codes_execute()` to send, or `get_v1_tax_codes` for simplest API.

pub fn get_v1_tax_codes_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_codes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_codes
/// List all tax codes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_codes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_codes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_codes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_codes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxCodesArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/tax_codes
/// List all tax codes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_codes_builder()` + `get_v1_tax_codes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_codes(
    client: &SimpleHttpClient,
    args: &GetV1TaxCodesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_codes_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_tax_codes_execute(builder)
}

/// GET /v1/tax_codes/{id}
/// Retrieve a tax code
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_codes_id_execute()` to send, or `get_v1_tax_codes_id` for simplest API.

pub fn get_v1_tax_codes_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_codes/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_codes/{id}
/// Retrieve a tax code
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_codes_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_codes_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_codes_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_code>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_code = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_codes_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxCodesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax_codes/{id}
/// Retrieve a tax code
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_codes_id_builder()` + `get_v1_tax_codes_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_codes_id(
    client: &SimpleHttpClient,
    args: &GetV1TaxCodesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_code>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_codes_id_builder(client, &args.id, args.expand)?;
    get_v1_tax_codes_id_execute(builder)
}

/// GET /v1/tax_ids
/// List all tax IDs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_ids_execute()` to send, or `get_v1_tax_ids` for simplest API.

pub fn get_v1_tax_ids_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    owner: Option<&str>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_ids",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = owner {
        query_parts.push(format!("owner={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_ids
/// List all tax IDs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxIdsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: owner
    pub owner: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/tax_ids
/// List all tax IDs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_ids_builder()` + `get_v1_tax_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_ids(
    client: &SimpleHttpClient,
    args: &GetV1TaxIdsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_ids_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.owner.as_deref(),
        args.starting_after.as_deref(),
    )?;
    get_v1_tax_ids_execute(builder)
}

/// POST /v1/tax_ids
/// Create a tax ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_ids_execute()` to send, or `post_v1_tax_ids` for simplest API.

pub fn post_v1_tax_ids_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_ids",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax_ids
/// Create a tax ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_id = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax_ids
/// Create a tax ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_ids_builder()` + `post_v1_tax_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_ids(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_tax_ids_builder(client)?;
    post_v1_tax_ids_execute(builder)
}

/// GET /v1/tax_ids/{id}
/// Retrieve a tax ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_ids_id_execute()` to send, or `get_v1_tax_ids_id` for simplest API.

pub fn get_v1_tax_ids_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_ids/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_ids/{id}
/// Retrieve a tax ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_ids_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_ids_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_ids_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_id = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_ids_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxIdsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax_ids/{id}
/// Retrieve a tax ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_ids_id_builder()` + `get_v1_tax_ids_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_ids_id(
    client: &SimpleHttpClient,
    args: &GetV1TaxIdsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_id>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_ids_id_builder(client, &args.id, args.expand)?;
    get_v1_tax_ids_id_execute(builder)
}

/// DELETE /v1/tax_ids/{id}
/// Delete a tax ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_tax_ids_id_execute()` to send, or `delete_v1_tax_ids_id` for simplest API.

pub fn delete_v1_tax_ids_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_ids/{}", id,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/tax_ids/{id}
/// Delete a tax ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_tax_ids_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_tax_ids_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_tax_ids_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_tax_id>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_tax_id = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_tax_ids_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1TaxIdsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// DELETE /v1/tax_ids/{id}
/// Delete a tax ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_tax_ids_id_builder()` + `delete_v1_tax_ids_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_tax_ids_id(
    client: &SimpleHttpClient,
    args: &DeleteV1TaxIdsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_tax_id>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_tax_ids_id_builder(client, &args.id)?;
    delete_v1_tax_ids_id_execute(builder)
}

/// GET /v1/tax_rates
/// List all tax rates
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_rates_execute()` to send, or `get_v1_tax_rates` for simplest API.

pub fn get_v1_tax_rates_builder(
    client: &SimpleHttpClient,
    active: Option<bool>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    inclusive: Option<bool>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_rates",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = inclusive {
        query_parts.push(format!("inclusive={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_rates
/// List all tax rates
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_rates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_rates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_rates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_rates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxRatesArgs {
    /// Query parameter: active
    pub active: Option<bool>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: inclusive
    pub inclusive: Option<bool>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/tax_rates
/// List all tax rates
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_rates_builder()` + `get_v1_tax_rates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_rates(
    client: &SimpleHttpClient,
    args: &GetV1TaxRatesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_rates_builder(
        client,
        args.active,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.inclusive,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_tax_rates_execute(builder)
}

/// POST /v1/tax_rates
/// Create a tax rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_rates_execute()` to send, or `post_v1_tax_rates` for simplest API.

pub fn post_v1_tax_rates_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_rates",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax_rates
/// Create a tax rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_rates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_rates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_rates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_rate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax_rates
/// Create a tax rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_rates_builder()` + `post_v1_tax_rates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_rates(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_rate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_tax_rates_builder(client)?;
    post_v1_tax_rates_execute(builder)
}

/// GET /v1/tax_rates/{tax_rate}
/// Retrieve a tax rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tax_rates_tax_rate_execute()` to send, or `get_v1_tax_rates_tax_rate` for simplest API.

pub fn get_v1_tax_rates_tax_rate_builder(
    client: &SimpleHttpClient,
    tax_rate: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_rates/{}", tax_rate,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_rates/{tax_rate}
/// Retrieve a tax rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tax_rates_tax_rate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tax_rates_tax_rate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_rates_tax_rate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_rate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tax_rates_tax_rate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TaxRatesTaxRateArgs {
    /// Path parameter: tax_rate
    pub tax_rate: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tax_rates/{tax_rate}
/// Retrieve a tax rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tax_rates_tax_rate_builder()` + `get_v1_tax_rates_tax_rate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tax_rates_tax_rate(
    client: &SimpleHttpClient,
    args: &GetV1TaxRatesTaxRateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_rate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tax_rates_tax_rate_builder(client, &args.tax_rate, args.expand)?;
    get_v1_tax_rates_tax_rate_execute(builder)
}

/// POST /v1/tax_rates/{tax_rate}
/// Update a tax rate
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tax_rates_tax_rate_execute()` to send, or `post_v1_tax_rates_tax_rate` for simplest API.

pub fn post_v1_tax_rates_tax_rate_builder(
    client: &SimpleHttpClient,
    tax_rate: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tax_rates/{}", tax_rate,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax_rates/{tax_rate}
/// Update a tax rate
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tax_rates_tax_rate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tax_rates_tax_rate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_rates_tax_rate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_rate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tax_rate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_tax_rates_tax_rate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TaxRatesTaxRateArgs {
    /// Path parameter: tax_rate
    pub tax_rate: String,
}

/// POST /v1/tax_rates/{tax_rate}
/// Update a tax rate
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tax_rates_tax_rate_builder()` + `post_v1_tax_rates_tax_rate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tax_rates_tax_rate(
    client: &SimpleHttpClient,
    args: &PostV1TaxRatesTaxRateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tax_rate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_tax_rates_tax_rate_builder(client, &args.tax_rate)?;
    post_v1_tax_rates_tax_rate_execute(builder)
}

/// GET /v1/terminal/configurations
/// List all Configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_terminal_configurations_execute()` to send, or `get_v1_terminal_configurations` for simplest API.

pub fn get_v1_terminal_configurations_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    is_account_default: Option<bool>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/configurations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = is_account_default {
        query_parts.push(format!("is_account_default={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/configurations
/// List all Configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_terminal_configurations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_terminal_configurations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_configurations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_terminal_configurations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TerminalConfigurationsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: is_account_default
    pub is_account_default: Option<bool>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/terminal/configurations
/// List all Configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_terminal_configurations_builder()` + `get_v1_terminal_configurations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_configurations(
    client: &SimpleHttpClient,
    args: &GetV1TerminalConfigurationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_terminal_configurations_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.is_account_default,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_terminal_configurations_execute(builder)
}

/// POST /v1/terminal/configurations
/// Create a Configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_configurations_execute()` to send, or `post_v1_terminal_configurations` for simplest API.

pub fn post_v1_terminal_configurations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/configurations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/configurations
/// Create a Configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_configurations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_configurations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_configurations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/configurations
/// Create a Configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_configurations_builder()` + `post_v1_terminal_configurations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_configurations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_configurations_builder(client)?;
    post_v1_terminal_configurations_execute(builder)
}

/// GET /v1/terminal/configurations/{configuration}
/// Retrieve a Configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_terminal_configurations_configuration_execute()` to send, or `get_v1_terminal_configurations_configuration` for simplest API.

pub fn get_v1_terminal_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/configurations/{}",
        configuration,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/configurations/{configuration}
/// Retrieve a Configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_terminal_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_terminal_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_terminal_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TerminalConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/terminal/configurations/{configuration}
/// Retrieve a Configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_terminal_configurations_configuration_builder()` + `get_v1_terminal_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_configurations_configuration(
    client: &SimpleHttpClient,
    args: &GetV1TerminalConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_terminal_configurations_configuration_builder(
        client,
        &args.configuration,
        args.expand,
    )?;
    get_v1_terminal_configurations_configuration_execute(builder)
}

/// POST /v1/terminal/configurations/{configuration}
/// Update a Configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_configurations_configuration_execute()` to send, or `post_v1_terminal_configurations_configuration` for simplest API.

pub fn post_v1_terminal_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/configurations/{}",
        configuration,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/configurations/{configuration}
/// Update a Configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
}

/// POST /v1/terminal/configurations/{configuration}
/// Update a Configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_configurations_configuration_builder()` + `post_v1_terminal_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_configurations_configuration(
    client: &SimpleHttpClient,
    args: &PostV1TerminalConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_terminal_configurations_configuration_builder(client, &args.configuration)?;
    post_v1_terminal_configurations_configuration_execute(builder)
}

/// DELETE /v1/terminal/configurations/{configuration}
/// Delete a Configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_terminal_configurations_configuration_execute()` to send, or `delete_v1_terminal_configurations_configuration` for simplest API.

pub fn delete_v1_terminal_configurations_configuration_builder(
    client: &SimpleHttpClient,
    configuration: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/configurations/{}",
        configuration,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/terminal/configurations/{configuration}
/// Delete a Configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_terminal_configurations_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_terminal_configurations_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_terminal_configurations_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Deleted_terminalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_terminalConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_terminal_configurations_configuration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1TerminalConfigurationsConfigurationArgs {
    /// Path parameter: configuration
    pub configuration: String,
}

/// DELETE /v1/terminal/configurations/{configuration}
/// Delete a Configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_terminal_configurations_configuration_builder()` + `delete_v1_terminal_configurations_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_terminal_configurations_configuration(
    client: &SimpleHttpClient,
    args: &DeleteV1TerminalConfigurationsConfigurationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Deleted_terminalConfiguration>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_terminal_configurations_configuration_builder(client, &args.configuration)?;
    delete_v1_terminal_configurations_configuration_execute(builder)
}

/// POST /v1/terminal/connection_tokens
/// Create a Connection Token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_connection_tokens_execute()` to send, or `post_v1_terminal_connection_tokens` for simplest API.

pub fn post_v1_terminal_connection_tokens_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/connection_tokens",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/connection_tokens
/// Create a Connection Token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_connection_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_connection_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_connection_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalConnection_token>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalConnection_token = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/connection_tokens
/// Create a Connection Token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_connection_tokens_builder()` + `post_v1_terminal_connection_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_connection_tokens(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalConnection_token>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_connection_tokens_builder(client)?;
    post_v1_terminal_connection_tokens_execute(builder)
}

/// GET /v1/terminal/locations
/// List all Locations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_terminal_locations_execute()` to send, or `get_v1_terminal_locations` for simplest API.

pub fn get_v1_terminal_locations_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/locations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/locations
/// List all Locations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_terminal_locations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_terminal_locations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_terminal_locations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TerminalLocationsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/terminal/locations
/// List all Locations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_terminal_locations_builder()` + `get_v1_terminal_locations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_locations(
    client: &SimpleHttpClient,
    args: &GetV1TerminalLocationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_terminal_locations_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_terminal_locations_execute(builder)
}

/// POST /v1/terminal/locations
/// Create a Location
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_locations_execute()` to send, or `post_v1_terminal_locations` for simplest API.

pub fn post_v1_terminal_locations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/locations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/locations
/// Create a Location
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_locations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_locations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/locations
/// Create a Location
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_locations_builder()` + `post_v1_terminal_locations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_locations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_locations_builder(client)?;
    post_v1_terminal_locations_execute(builder)
}

/// GET /v1/terminal/locations/{location}
/// Retrieve a Location
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_terminal_locations_location_execute()` to send, or `get_v1_terminal_locations_location` for simplest API.

pub fn get_v1_terminal_locations_location_builder(
    client: &SimpleHttpClient,
    location: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/locations/{}", location,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/locations/{location}
/// Retrieve a Location
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_terminal_locations_location_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_terminal_locations_location_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_locations_location_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_terminal_locations_location`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TerminalLocationsLocationArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/terminal/locations/{location}
/// Retrieve a Location
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_terminal_locations_location_builder()` + `get_v1_terminal_locations_location_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_locations_location(
    client: &SimpleHttpClient,
    args: &GetV1TerminalLocationsLocationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_terminal_locations_location_builder(client, &args.location, args.expand)?;
    get_v1_terminal_locations_location_execute(builder)
}

/// POST /v1/terminal/locations/{location}
/// Update a Location
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_locations_location_execute()` to send, or `post_v1_terminal_locations_location` for simplest API.

pub fn post_v1_terminal_locations_location_builder(
    client: &SimpleHttpClient,
    location: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/locations/{}", location,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/locations/{location}
/// Update a Location
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_locations_location_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_locations_location_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_locations_location_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_locations_location`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalLocationsLocationArgs {
    /// Path parameter: location
    pub location: String,
}

/// POST /v1/terminal/locations/{location}
/// Update a Location
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_locations_location_builder()` + `post_v1_terminal_locations_location_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_locations_location(
    client: &SimpleHttpClient,
    args: &PostV1TerminalLocationsLocationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_locations_location_builder(client, &args.location)?;
    post_v1_terminal_locations_location_execute(builder)
}

/// DELETE /v1/terminal/locations/{location}
/// Delete a Location
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_terminal_locations_location_execute()` to send, or `delete_v1_terminal_locations_location` for simplest API.

pub fn delete_v1_terminal_locations_location_builder(
    client: &SimpleHttpClient,
    location: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/locations/{}", location,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/terminal/locations/{location}
/// Delete a Location
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_terminal_locations_location_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_terminal_locations_location_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_terminal_locations_location_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_terminalLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_terminalLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_terminal_locations_location`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1TerminalLocationsLocationArgs {
    /// Path parameter: location
    pub location: String,
}

/// DELETE /v1/terminal/locations/{location}
/// Delete a Location
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_terminal_locations_location_builder()` + `delete_v1_terminal_locations_location_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_terminal_locations_location(
    client: &SimpleHttpClient,
    args: &DeleteV1TerminalLocationsLocationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_terminalLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_terminal_locations_location_builder(client, &args.location)?;
    delete_v1_terminal_locations_location_execute(builder)
}

/// POST /v1/terminal/onboarding_links
/// Create an Onboarding Link
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_onboarding_links_execute()` to send, or `post_v1_terminal_onboarding_links` for simplest API.

pub fn post_v1_terminal_onboarding_links_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/onboarding_links",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/onboarding_links
/// Create an Onboarding Link
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_onboarding_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_onboarding_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_onboarding_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalOnboarding_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalOnboarding_link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/onboarding_links
/// Create an Onboarding Link
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_onboarding_links_builder()` + `post_v1_terminal_onboarding_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_onboarding_links(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalOnboarding_link>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_onboarding_links_builder(client)?;
    post_v1_terminal_onboarding_links_execute(builder)
}

/// GET /v1/terminal/readers
/// List all Readers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_terminal_readers_execute()` to send, or `get_v1_terminal_readers` for simplest API.

pub fn get_v1_terminal_readers_builder(
    client: &SimpleHttpClient,
    device_type: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    location: Option<&str>,
    serial_number: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/readers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = device_type {
        query_parts.push(format!("device_type={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = serial_number {
        query_parts.push(format!("serial_number={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/readers
/// List all Readers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_terminal_readers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_terminal_readers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_readers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_terminal_readers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TerminalReadersArgs {
    /// Query parameter: device_type
    pub device_type: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: serial_number
    pub serial_number: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/terminal/readers
/// List all Readers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_terminal_readers_builder()` + `get_v1_terminal_readers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_readers(
    client: &SimpleHttpClient,
    args: &GetV1TerminalReadersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_terminal_readers_builder(
        client,
        args.device_type.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.location.as_deref(),
        args.serial_number.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_terminal_readers_execute(builder)
}

/// POST /v1/terminal/readers
/// Create a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_execute()` to send, or `post_v1_terminal_readers` for simplest API.

pub fn post_v1_terminal_readers_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/readers",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers
/// Create a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/readers
/// Create a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_builder()` + `post_v1_terminal_readers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_readers_builder(client)?;
    post_v1_terminal_readers_execute(builder)
}

/// GET /v1/terminal/readers/{reader}
/// Retrieve a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_terminal_readers_reader_execute()` to send, or `get_v1_terminal_readers_reader` for simplest API.

pub fn get_v1_terminal_readers_reader_builder(
    client: &SimpleHttpClient,
    reader: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/readers/{}", reader,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/terminal/readers/{reader}
/// Retrieve a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_terminal_readers_reader_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_terminal_readers_reader_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_readers_reader_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_terminal_readers_reader`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TerminalReadersReaderArgs {
    /// Path parameter: reader
    pub reader: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/terminal/readers/{reader}
/// Retrieve a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_terminal_readers_reader_builder()` + `get_v1_terminal_readers_reader_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_terminal_readers_reader(
    client: &SimpleHttpClient,
    args: &GetV1TerminalReadersReaderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_terminal_readers_reader_builder(client, &args.reader, args.expand)?;
    get_v1_terminal_readers_reader_execute(builder)
}

/// POST /v1/terminal/readers/{reader}
/// Update a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_execute()` to send, or `post_v1_terminal_readers_reader` for simplest API.

pub fn post_v1_terminal_readers_reader_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/readers/{}", reader,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}
/// Update a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}
/// Update a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_builder()` + `post_v1_terminal_readers_reader_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_readers_reader_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_execute(builder)
}

/// DELETE /v1/terminal/readers/{reader}
/// Delete a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_terminal_readers_reader_execute()` to send, or `delete_v1_terminal_readers_reader` for simplest API.

pub fn delete_v1_terminal_readers_reader_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/readers/{}", reader,);

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/terminal/readers/{reader}
/// Delete a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_terminal_readers_reader_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_terminal_readers_reader_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_terminal_readers_reader_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_terminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_terminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_terminal_readers_reader`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1TerminalReadersReaderArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// DELETE /v1/terminal/readers/{reader}
/// Delete a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_terminal_readers_reader_builder()` + `delete_v1_terminal_readers_reader_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_terminal_readers_reader(
    client: &SimpleHttpClient,
    args: &DeleteV1TerminalReadersReaderArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_terminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_terminal_readers_reader_builder(client, &args.reader)?;
    delete_v1_terminal_readers_reader_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/cancel_action
/// Cancel the current reader action
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_cancel_action_execute()` to send, or `post_v1_terminal_readers_reader_cancel_action` for simplest API.

pub fn post_v1_terminal_readers_reader_cancel_action_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/cancel_action",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/cancel_action
/// Cancel the current reader action
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_cancel_action_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_cancel_action_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_cancel_action_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_cancel_action`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderCancelActionArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/cancel_action
/// Cancel the current reader action
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_cancel_action_builder()` + `post_v1_terminal_readers_reader_cancel_action_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_cancel_action(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderCancelActionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_readers_reader_cancel_action_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_cancel_action_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/collect_inputs
/// Collect inputs using a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_collect_inputs_execute()` to send, or `post_v1_terminal_readers_reader_collect_inputs` for simplest API.

pub fn post_v1_terminal_readers_reader_collect_inputs_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/collect_inputs",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/collect_inputs
/// Collect inputs using a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_collect_inputs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_collect_inputs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_collect_inputs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_collect_inputs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderCollectInputsArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/collect_inputs
/// Collect inputs using a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_collect_inputs_builder()` + `post_v1_terminal_readers_reader_collect_inputs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_collect_inputs(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderCollectInputsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_readers_reader_collect_inputs_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_collect_inputs_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/collect_payment_method
/// Hand off a PaymentIntent to a Reader and collect card details
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_collect_payment_method_execute()` to send, or `post_v1_terminal_readers_reader_collect_payment_method` for simplest API.

pub fn post_v1_terminal_readers_reader_collect_payment_method_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/collect_payment_method",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/collect_payment_method
/// Hand off a PaymentIntent to a Reader and collect card details
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_collect_payment_method_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_collect_payment_method_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_collect_payment_method_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_collect_payment_method`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderCollectPaymentMethodArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/collect_payment_method
/// Hand off a PaymentIntent to a Reader and collect card details
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_collect_payment_method_builder()` + `post_v1_terminal_readers_reader_collect_payment_method_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_collect_payment_method(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderCollectPaymentMethodArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_terminal_readers_reader_collect_payment_method_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_collect_payment_method_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/confirm_payment_intent
/// Confirm a PaymentIntent on the Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_confirm_payment_intent_execute()` to send, or `post_v1_terminal_readers_reader_confirm_payment_intent` for simplest API.

pub fn post_v1_terminal_readers_reader_confirm_payment_intent_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/confirm_payment_intent",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/confirm_payment_intent
/// Confirm a PaymentIntent on the Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_confirm_payment_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_confirm_payment_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_confirm_payment_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_confirm_payment_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderConfirmPaymentIntentArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/confirm_payment_intent
/// Confirm a PaymentIntent on the Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_confirm_payment_intent_builder()` + `post_v1_terminal_readers_reader_confirm_payment_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_confirm_payment_intent(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderConfirmPaymentIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_terminal_readers_reader_confirm_payment_intent_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_confirm_payment_intent_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/process_payment_intent
/// Hand-off a PaymentIntent to a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_process_payment_intent_execute()` to send, or `post_v1_terminal_readers_reader_process_payment_intent` for simplest API.

pub fn post_v1_terminal_readers_reader_process_payment_intent_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/process_payment_intent",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/process_payment_intent
/// Hand-off a PaymentIntent to a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_process_payment_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_process_payment_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_process_payment_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_process_payment_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderProcessPaymentIntentArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/process_payment_intent
/// Hand-off a PaymentIntent to a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_process_payment_intent_builder()` + `post_v1_terminal_readers_reader_process_payment_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_process_payment_intent(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderProcessPaymentIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_terminal_readers_reader_process_payment_intent_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_process_payment_intent_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/process_setup_intent
/// Hand-off a SetupIntent to a Reader
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_process_setup_intent_execute()` to send, or `post_v1_terminal_readers_reader_process_setup_intent` for simplest API.

pub fn post_v1_terminal_readers_reader_process_setup_intent_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/process_setup_intent",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/process_setup_intent
/// Hand-off a SetupIntent to a Reader
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_process_setup_intent_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_process_setup_intent_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_process_setup_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_process_setup_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderProcessSetupIntentArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/process_setup_intent
/// Hand-off a SetupIntent to a Reader
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_process_setup_intent_builder()` + `post_v1_terminal_readers_reader_process_setup_intent_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_process_setup_intent(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderProcessSetupIntentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_terminal_readers_reader_process_setup_intent_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_process_setup_intent_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/refund_payment
/// Refund a Charge or a PaymentIntent in-person
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_refund_payment_execute()` to send, or `post_v1_terminal_readers_reader_refund_payment` for simplest API.

pub fn post_v1_terminal_readers_reader_refund_payment_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/refund_payment",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/refund_payment
/// Refund a Charge or a PaymentIntent in-person
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_refund_payment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_refund_payment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_refund_payment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_refund_payment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderRefundPaymentArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/refund_payment
/// Refund a Charge or a PaymentIntent in-person
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_refund_payment_builder()` + `post_v1_terminal_readers_reader_refund_payment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_refund_payment(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderRefundPaymentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_readers_reader_refund_payment_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_refund_payment_execute(builder)
}

/// POST /v1/terminal/readers/{reader}/set_reader_display
/// Set reader display
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_readers_reader_set_reader_display_execute()` to send, or `post_v1_terminal_readers_reader_set_reader_display` for simplest API.

pub fn post_v1_terminal_readers_reader_set_reader_display_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/terminal/readers/{}/set_reader_display",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/readers/{reader}/set_reader_display
/// Set reader display
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_readers_reader_set_reader_display_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_readers_reader_set_reader_display_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_set_reader_display_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_terminal_readers_reader_set_reader_display`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TerminalReadersReaderSetReaderDisplayArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/terminal/readers/{reader}/set_reader_display
/// Set reader display
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_readers_reader_set_reader_display_builder()` + `post_v1_terminal_readers_reader_set_reader_display_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_readers_reader_set_reader_display(
    client: &SimpleHttpClient,
    args: &PostV1TerminalReadersReaderSetReaderDisplayArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_readers_reader_set_reader_display_builder(client, &args.reader)?;
    post_v1_terminal_readers_reader_set_reader_display_execute(builder)
}

/// POST /v1/terminal/refunds
/// Create a refund using a Terminal-supported device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_terminal_refunds_execute()` to send, or `post_v1_terminal_refunds` for simplest API.

pub fn post_v1_terminal_refunds_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/terminal/refunds",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/refunds
/// Create a refund using a Terminal-supported device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_terminal_refunds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_terminal_refunds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_refunds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalRefund>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalRefund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/terminal/refunds
/// Create a refund using a Terminal-supported device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_terminal_refunds_builder()` + `post_v1_terminal_refunds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_terminal_refunds(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalRefund>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_terminal_refunds_builder(client)?;
    post_v1_terminal_refunds_execute(builder)
}

/// POST /v1/test_helpers/confirmation_tokens
/// Create a test Confirmation Token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_confirmation_tokens_execute()` to send, or `post_v1_test_helpers_confirmation_tokens` for simplest API.

pub fn post_v1_test_helpers_confirmation_tokens_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/confirmation_tokens",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/confirmation_tokens
/// Create a test Confirmation Token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_confirmation_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_confirmation_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_confirmation_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Confirmation_token>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Confirmation_token = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/confirmation_tokens
/// Create a test Confirmation Token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_confirmation_tokens_builder()` + `post_v1_test_helpers_confirmation_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_confirmation_tokens(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Confirmation_token>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_confirmation_tokens_builder(client)?;
    post_v1_test_helpers_confirmation_tokens_execute(builder)
}

/// POST /v1/test_helpers/customers/{customer}/fund_cash_balance
/// Fund a test mode cash balance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_customers_customer_fund_cash_balance_execute()` to send, or `post_v1_test_helpers_customers_customer_fund_cash_balance` for simplest API.

pub fn post_v1_test_helpers_customers_customer_fund_cash_balance_builder(
    client: &SimpleHttpClient,
    customer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/customers/{}/fund_cash_balance",
        customer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/customers/{customer}/fund_cash_balance
/// Fund a test mode cash balance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_customers_customer_fund_cash_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_customers_customer_fund_cash_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_customers_customer_fund_cash_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_cash_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer_cash_balance_transaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_customers_customer_fund_cash_balance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersCustomersCustomerFundCashBalanceArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST /v1/test_helpers/customers/{customer}/fund_cash_balance
/// Fund a test mode cash balance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_customers_customer_fund_cash_balance_builder()` + `post_v1_test_helpers_customers_customer_fund_cash_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_customers_customer_fund_cash_balance(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersCustomersCustomerFundCashBalanceArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Customer_cash_balance_transaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_customers_customer_fund_cash_balance_builder(client, &args.customer)?;
    post_v1_test_helpers_customers_customer_fund_cash_balance_execute(builder)
}

/// POST /v1/test_helpers/issuing/authorizations
/// Create a test-mode authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_execute()` to send, or `post_v1_test_helpers_issuing_authorizations` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/issuing/authorizations",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations
/// Create a test-mode authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/authorizations
/// Create a test-mode authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_builder()` + `post_v1_test_helpers_issuing_authorizations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_authorizations_builder(client)?;
    post_v1_test_helpers_issuing_authorizations_execute(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/capture
/// Capture a test-mode authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_authorization_capture_execute()` to send, or `post_v1_test_helpers_issuing_authorizations_authorization_capture` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_capture_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{}/capture",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/capture
/// Capture a test-mode authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_capture_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_authorization_capture_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_authorizations_authorization_capture`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingAuthorizationsAuthorizationCaptureArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/capture
/// Capture a test-mode authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_capture_builder()` + `post_v1_test_helpers_issuing_authorizations_authorization_capture_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_capture(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingAuthorizationsAuthorizationCaptureArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_authorizations_authorization_capture_builder(
        client,
        &args.authorization,
    )?;
    post_v1_test_helpers_issuing_authorizations_authorization_capture_execute(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire
/// Expire a test-mode authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_authorization_expire_execute()` to send, or `post_v1_test_helpers_issuing_authorizations_authorization_expire` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_expire_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{}/expire",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire
/// Expire a test-mode authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_expire_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_authorization_expire_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_expire_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_authorizations_authorization_expire`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingAuthorizationsAuthorizationExpireArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/expire
/// Expire a test-mode authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_expire_builder()` + `post_v1_test_helpers_issuing_authorizations_authorization_expire_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_expire(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingAuthorizationsAuthorizationExpireArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_authorizations_authorization_expire_builder(
        client,
        &args.authorization,
    )?;
    post_v1_test_helpers_issuing_authorizations_authorization_expire_execute(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/finalize_amount
/// Finalize a test-mode authorization's amount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_execute()` to send, or `post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{}/finalize_amount",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/finalize_amount
/// Finalize a test-mode authorization's amount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingAuthorizationsAuthorizationFinalizeAmountArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/finalize_amount
/// Finalize a test-mode authorization's amount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_builder()` + `post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingAuthorizationsAuthorizationFinalizeAmountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_builder(
            client,
            &args.authorization,
        )?;
    post_v1_test_helpers_issuing_authorizations_authorization_finalize_amount_execute(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/fraud_challenges/respond
/// Respond to fraud challenge
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_execute()` to send, or `post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{}/fraud_challenges/respond",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/fraud_challenges/respond
/// Respond to fraud challenge
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespondArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/fraud_challenges/respond
/// Respond to fraud challenge
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_builder()` + `post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespondArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_builder(
            client,
            &args.authorization,
        )?;
    post_v1_test_helpers_issuing_authorizations_authorization_fraud_challenges_respond_execute(
        builder,
    )
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/increment
/// Increment a test-mode authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_authorization_increment_execute()` to send, or `post_v1_test_helpers_issuing_authorizations_authorization_increment` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_increment_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{}/increment",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/increment
/// Increment a test-mode authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_increment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_authorization_increment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_increment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_authorizations_authorization_increment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingAuthorizationsAuthorizationIncrementArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/increment
/// Increment a test-mode authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_increment_builder()` + `post_v1_test_helpers_issuing_authorizations_authorization_increment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_increment(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingAuthorizationsAuthorizationIncrementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_authorizations_authorization_increment_builder(
        client,
        &args.authorization,
    )?;
    post_v1_test_helpers_issuing_authorizations_authorization_increment_execute(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/reverse
/// Reverse a test-mode authorization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_authorizations_authorization_reverse_execute()` to send, or `post_v1_test_helpers_issuing_authorizations_authorization_reverse` for simplest API.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_reverse_builder(
    client: &SimpleHttpClient,
    authorization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/authorizations/{}/reverse",
        authorization,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/reverse
/// Reverse a test-mode authorization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_reverse_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_authorizations_authorization_reverse_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_reverse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_authorizations_authorization_reverse`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingAuthorizationsAuthorizationReverseArgs {
    /// Path parameter: authorization
    pub authorization: String,
}

/// POST /v1/test_helpers/issuing/authorizations/{authorization}/reverse
/// Reverse a test-mode authorization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_authorizations_authorization_reverse_builder()` + `post_v1_test_helpers_issuing_authorizations_authorization_reverse_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_authorizations_authorization_reverse(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingAuthorizationsAuthorizationReverseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_authorizations_authorization_reverse_builder(
        client,
        &args.authorization,
    )?;
    post_v1_test_helpers_issuing_authorizations_authorization_reverse_execute(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/deliver
/// Deliver a testmode card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_cards_card_shipping_deliver_execute()` to send, or `post_v1_test_helpers_issuing_cards_card_shipping_deliver` for simplest API.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_deliver_builder(
    client: &SimpleHttpClient,
    card: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{}/shipping/deliver",
        card,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/deliver
/// Deliver a testmode card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_deliver_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_cards_card_shipping_deliver_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_deliver_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_cards_card_shipping_deliver`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingCardsCardShippingDeliverArgs {
    /// Path parameter: card
    pub card: String,
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/deliver
/// Deliver a testmode card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_deliver_builder()` + `post_v1_test_helpers_issuing_cards_card_shipping_deliver_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_deliver(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingCardsCardShippingDeliverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_cards_card_shipping_deliver_builder(client, &args.card)?;
    post_v1_test_helpers_issuing_cards_card_shipping_deliver_execute(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail
/// Fail a testmode card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_cards_card_shipping_fail_execute()` to send, or `post_v1_test_helpers_issuing_cards_card_shipping_fail` for simplest API.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_fail_builder(
    client: &SimpleHttpClient,
    card: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{}/shipping/fail",
        card,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail
/// Fail a testmode card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_fail_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_cards_card_shipping_fail_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_fail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_cards_card_shipping_fail`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingCardsCardShippingFailArgs {
    /// Path parameter: card
    pub card: String,
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/fail
/// Fail a testmode card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_fail_builder()` + `post_v1_test_helpers_issuing_cards_card_shipping_fail_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_fail(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingCardsCardShippingFailArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_cards_card_shipping_fail_builder(client, &args.card)?;
    post_v1_test_helpers_issuing_cards_card_shipping_fail_execute(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return
/// Return a testmode card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_cards_card_shipping_return_execute()` to send, or `post_v1_test_helpers_issuing_cards_card_shipping_return` for simplest API.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_return_builder(
    client: &SimpleHttpClient,
    card: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{}/shipping/return",
        card,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return
/// Return a testmode card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_return_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_cards_card_shipping_return_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_return_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_cards_card_shipping_return`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingCardsCardShippingReturnArgs {
    /// Path parameter: card
    pub card: String,
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return
/// Return a testmode card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_return_builder()` + `post_v1_test_helpers_issuing_cards_card_shipping_return_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_return(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingCardsCardShippingReturnArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_cards_card_shipping_return_builder(client, &args.card)?;
    post_v1_test_helpers_issuing_cards_card_shipping_return_execute(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/ship
/// Ship a testmode card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_cards_card_shipping_ship_execute()` to send, or `post_v1_test_helpers_issuing_cards_card_shipping_ship` for simplest API.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_ship_builder(
    client: &SimpleHttpClient,
    card: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{}/shipping/ship",
        card,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/ship
/// Ship a testmode card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_ship_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_cards_card_shipping_ship_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_ship_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_cards_card_shipping_ship`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingCardsCardShippingShipArgs {
    /// Path parameter: card
    pub card: String,
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/ship
/// Ship a testmode card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_ship_builder()` + `post_v1_test_helpers_issuing_cards_card_shipping_ship_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_ship(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingCardsCardShippingShipArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_cards_card_shipping_ship_builder(client, &args.card)?;
    post_v1_test_helpers_issuing_cards_card_shipping_ship_execute(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/submit
/// Submit a testmode card
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_cards_card_shipping_submit_execute()` to send, or `post_v1_test_helpers_issuing_cards_card_shipping_submit` for simplest API.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_submit_builder(
    client: &SimpleHttpClient,
    card: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{}/shipping/submit",
        card,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/submit
/// Submit a testmode card
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_submit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_cards_card_shipping_submit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_cards_card_shipping_submit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingCardsCardShippingSubmitArgs {
    /// Path parameter: card
    pub card: String,
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/submit
/// Submit a testmode card
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_cards_card_shipping_submit_builder()` + `post_v1_test_helpers_issuing_cards_card_shipping_submit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_cards_card_shipping_submit(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingCardsCardShippingSubmitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingCard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_cards_card_shipping_submit_builder(client, &args.card)?;
    post_v1_test_helpers_issuing_cards_card_shipping_submit_execute(builder)
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/activate
/// Activate a testmode personalization design
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_execute()` to send, or `post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate` for simplest API.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_builder(
    client: &SimpleHttpClient,
    personalization_design: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/personalization_designs/{}/activate",
        personalization_design,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/activate
/// Activate a testmode personalization design
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPersonalization_design = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivateArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/activate
/// Activate a testmode personalization design
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_builder()` + `post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_builder(client, &args.personalization_design)?;
    post_v1_test_helpers_issuing_personalization_designs_personalization_design_activate_execute(
        builder,
    )
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/deactivate
/// Deactivate a testmode personalization design
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_execute()` to send, or `post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate` for simplest API.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_builder(
    client: &SimpleHttpClient,
    personalization_design: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/personalization_designs/{}/deactivate",
        personalization_design,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/deactivate
/// Deactivate a testmode personalization design
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPersonalization_design = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivateArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/deactivate
/// Deactivate a testmode personalization design
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_builder()` + `post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_builder(client, &args.personalization_design)?;
    post_v1_test_helpers_issuing_personalization_designs_personalization_design_deactivate_execute(
        builder,
    )
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/reject
/// Reject a testmode personalization design
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_execute()` to send, or `post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject` for simplest API.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_builder(
    client: &SimpleHttpClient,
    personalization_design: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/personalization_designs/{}/reject",
        personalization_design,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/reject
/// Reject a testmode personalization design
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingPersonalization_design = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingPersonalizationDesignsPersonalizationDesignRejectArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
}

/// POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/reject
/// Reject a testmode personalization design
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_builder()` + `post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingPersonalizationDesignsPersonalizationDesignRejectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IssuingPersonalization_design>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_builder(
            client,
            &args.personalization_design,
        )?;
    post_v1_test_helpers_issuing_personalization_designs_personalization_design_reject_execute(
        builder,
    )
}

/// POST /v1/test_helpers/issuing/settlements
/// Create a test-mode settlement
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_settlements_execute()` to send, or `post_v1_test_helpers_issuing_settlements` for simplest API.

pub fn post_v1_test_helpers_issuing_settlements_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/issuing/settlements",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/settlements
/// Create a test-mode settlement
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_settlements_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_settlements_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_settlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingSettlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/settlements
/// Create a test-mode settlement
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_settlements_builder()` + `post_v1_test_helpers_issuing_settlements_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_settlements(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_settlements_builder(client)?;
    post_v1_test_helpers_issuing_settlements_execute(builder)
}

/// POST /v1/test_helpers/issuing/settlements/{settlement}/complete
/// Complete a test-mode settlement
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_settlements_settlement_complete_execute()` to send, or `post_v1_test_helpers_issuing_settlements_settlement_complete` for simplest API.

pub fn post_v1_test_helpers_issuing_settlements_settlement_complete_builder(
    client: &SimpleHttpClient,
    settlement: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/settlements/{}/complete",
        settlement,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/settlements/{settlement}/complete
/// Complete a test-mode settlement
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_settlements_settlement_complete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_settlements_settlement_complete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_settlements_settlement_complete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingSettlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_settlements_settlement_complete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingSettlementsSettlementCompleteArgs {
    /// Path parameter: settlement
    pub settlement: String,
}

/// POST /v1/test_helpers/issuing/settlements/{settlement}/complete
/// Complete a test-mode settlement
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_settlements_settlement_complete_builder()` + `post_v1_test_helpers_issuing_settlements_settlement_complete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_settlements_settlement_complete(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingSettlementsSettlementCompleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingSettlement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_settlements_settlement_complete_builder(
        client,
        &args.settlement,
    )?;
    post_v1_test_helpers_issuing_settlements_settlement_complete_execute(builder)
}

/// POST /v1/test_helpers/issuing/transactions/create_force_capture
/// Create a test-mode force capture
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_transactions_create_force_capture_execute()` to send, or `post_v1_test_helpers_issuing_transactions_create_force_capture` for simplest API.

pub fn post_v1_test_helpers_issuing_transactions_create_force_capture_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/transactions/create_force_capture",
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/transactions/create_force_capture
/// Create a test-mode force capture
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_transactions_create_force_capture_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_transactions_create_force_capture_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_transactions_create_force_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/transactions/create_force_capture
/// Create a test-mode force capture
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_transactions_create_force_capture_builder()` + `post_v1_test_helpers_issuing_transactions_create_force_capture_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_transactions_create_force_capture(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_transactions_create_force_capture_builder(client)?;
    post_v1_test_helpers_issuing_transactions_create_force_capture_execute(builder)
}

/// POST /v1/test_helpers/issuing/transactions/create_unlinked_refund
/// Create a test-mode unlinked refund
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_transactions_create_unlinked_refund_execute()` to send, or `post_v1_test_helpers_issuing_transactions_create_unlinked_refund` for simplest API.

pub fn post_v1_test_helpers_issuing_transactions_create_unlinked_refund_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/transactions/create_unlinked_refund",
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/transactions/create_unlinked_refund
/// Create a test-mode unlinked refund
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_transactions_create_unlinked_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_transactions_create_unlinked_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_transactions_create_unlinked_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/transactions/create_unlinked_refund
/// Create a test-mode unlinked refund
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_transactions_create_unlinked_refund_builder()` + `post_v1_test_helpers_issuing_transactions_create_unlinked_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_transactions_create_unlinked_refund(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_transactions_create_unlinked_refund_builder(client)?;
    post_v1_test_helpers_issuing_transactions_create_unlinked_refund_execute(builder)
}

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund
/// Refund a test-mode transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_issuing_transactions_transaction_refund_execute()` to send, or `post_v1_test_helpers_issuing_transactions_transaction_refund` for simplest API.

pub fn post_v1_test_helpers_issuing_transactions_transaction_refund_builder(
    client: &SimpleHttpClient,
    transaction: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/transactions/{}/refund",
        transaction,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund
/// Refund a test-mode transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_issuing_transactions_transaction_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_issuing_transactions_transaction_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_transactions_transaction_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuingTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_issuing_transactions_transaction_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersIssuingTransactionsTransactionRefundArgs {
    /// Path parameter: transaction
    pub transaction: String,
}

/// POST /v1/test_helpers/issuing/transactions/{transaction}/refund
/// Refund a test-mode transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_issuing_transactions_transaction_refund_builder()` + `post_v1_test_helpers_issuing_transactions_transaction_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_issuing_transactions_transaction_refund(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersIssuingTransactionsTransactionRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuingTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_issuing_transactions_transaction_refund_builder(
        client,
        &args.transaction,
    )?;
    post_v1_test_helpers_issuing_transactions_transaction_refund_execute(builder)
}

/// POST /v1/test_helpers/refunds/{refund}/expire
/// Expire a pending refund.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_refunds_refund_expire_execute()` to send, or `post_v1_test_helpers_refunds_refund_expire` for simplest API.

pub fn post_v1_test_helpers_refunds_refund_expire_builder(
    client: &SimpleHttpClient,
    refund: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/refunds/{}/expire",
        refund,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/refunds/{refund}/expire
/// Expire a pending refund.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_refunds_refund_expire_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_refunds_refund_expire_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_refunds_refund_expire_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_refunds_refund_expire`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersRefundsRefundExpireArgs {
    /// Path parameter: refund
    pub refund: String,
}

/// POST /v1/test_helpers/refunds/{refund}/expire
/// Expire a pending refund.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_refunds_refund_expire_builder()` + `post_v1_test_helpers_refunds_refund_expire_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_refunds_refund_expire(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersRefundsRefundExpireArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_refunds_refund_expire_builder(client, &args.refund)?;
    post_v1_test_helpers_refunds_refund_expire_execute(builder)
}

/// POST /v1/test_helpers/terminal/readers/{reader}/present_payment_method
/// Simulate presenting a payment method
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_terminal_readers_reader_present_payment_method_execute()` to send, or `post_v1_test_helpers_terminal_readers_reader_present_payment_method` for simplest API.

pub fn post_v1_test_helpers_terminal_readers_reader_present_payment_method_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/terminal/readers/{}/present_payment_method",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/terminal/readers/{reader}/present_payment_method
/// Simulate presenting a payment method
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_terminal_readers_reader_present_payment_method_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_terminal_readers_reader_present_payment_method_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_terminal_readers_reader_present_payment_method_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_terminal_readers_reader_present_payment_method`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTerminalReadersReaderPresentPaymentMethodArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/test_helpers/terminal/readers/{reader}/present_payment_method
/// Simulate presenting a payment method
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_terminal_readers_reader_present_payment_method_builder()` + `post_v1_test_helpers_terminal_readers_reader_present_payment_method_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_terminal_readers_reader_present_payment_method(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTerminalReadersReaderPresentPaymentMethodArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_terminal_readers_reader_present_payment_method_builder(
        client,
        &args.reader,
    )?;
    post_v1_test_helpers_terminal_readers_reader_present_payment_method_execute(builder)
}

/// POST /v1/test_helpers/terminal/readers/{reader}/succeed_input_collection
/// Simulate a successful input collection
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_execute()` to send, or `post_v1_test_helpers_terminal_readers_reader_succeed_input_collection` for simplest API.

pub fn post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/terminal/readers/{}/succeed_input_collection",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/terminal/readers/{reader}/succeed_input_collection
/// Simulate a successful input collection
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_terminal_readers_reader_succeed_input_collection`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTerminalReadersReaderSucceedInputCollectionArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/test_helpers/terminal/readers/{reader}/succeed_input_collection
/// Simulate a successful input collection
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_builder()` + `post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_terminal_readers_reader_succeed_input_collection(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTerminalReadersReaderSucceedInputCollectionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_builder(
        client,
        &args.reader,
    )?;
    post_v1_test_helpers_terminal_readers_reader_succeed_input_collection_execute(builder)
}

/// POST /v1/test_helpers/terminal/readers/{reader}/timeout_input_collection
/// Simulate an input collection timeout
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_execute()` to send, or `post_v1_test_helpers_terminal_readers_reader_timeout_input_collection` for simplest API.

pub fn post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_builder(
    client: &SimpleHttpClient,
    reader: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/terminal/readers/{}/timeout_input_collection",
        reader,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/terminal/readers/{reader}/timeout_input_collection
/// Simulate an input collection timeout
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminalReader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_terminal_readers_reader_timeout_input_collection`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTerminalReadersReaderTimeoutInputCollectionArgs {
    /// Path parameter: reader
    pub reader: String,
}

/// POST /v1/test_helpers/terminal/readers/{reader}/timeout_input_collection
/// Simulate an input collection timeout
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_builder()` + `post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_terminal_readers_reader_timeout_input_collection(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTerminalReadersReaderTimeoutInputCollectionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminalReader>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_builder(
        client,
        &args.reader,
    )?;
    post_v1_test_helpers_terminal_readers_reader_timeout_input_collection_execute(builder)
}

/// GET /v1/test_helpers/test_clocks
/// List all test clocks
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_test_helpers_test_clocks_execute()` to send, or `get_v1_test_helpers_test_clocks` for simplest API.

pub fn get_v1_test_helpers_test_clocks_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/test_clocks",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/test_helpers/test_clocks
/// List all test clocks
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_test_helpers_test_clocks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_test_helpers_test_clocks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_test_helpers_test_clocks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_test_helpers_test_clocks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TestHelpersTestClocksArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/test_helpers/test_clocks
/// List all test clocks
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_test_helpers_test_clocks_builder()` + `get_v1_test_helpers_test_clocks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_test_helpers_test_clocks(
    client: &SimpleHttpClient,
    args: &GetV1TestHelpersTestClocksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_test_helpers_test_clocks_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_test_helpers_test_clocks_execute(builder)
}

/// POST /v1/test_helpers/test_clocks
/// Create a test clock
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_test_clocks_execute()` to send, or `post_v1_test_helpers_test_clocks` for simplest API.

pub fn post_v1_test_helpers_test_clocks_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/test_clocks",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/test_clocks
/// Create a test clock
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_test_clocks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_test_clocks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_test_clocks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Test_helpersTest_clock>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Test_helpersTest_clock = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/test_clocks
/// Create a test clock
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_test_clocks_builder()` + `post_v1_test_helpers_test_clocks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_test_clocks(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Test_helpersTest_clock>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_test_clocks_builder(client)?;
    post_v1_test_helpers_test_clocks_execute(builder)
}

/// GET /v1/test_helpers/test_clocks/{test_clock}
/// Retrieve a test clock
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_test_helpers_test_clocks_test_clock_execute()` to send, or `get_v1_test_helpers_test_clocks_test_clock` for simplest API.

pub fn get_v1_test_helpers_test_clocks_test_clock_builder(
    client: &SimpleHttpClient,
    test_clock: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/test_clocks/{}",
        test_clock,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/test_helpers/test_clocks/{test_clock}
/// Retrieve a test clock
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_test_helpers_test_clocks_test_clock_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_test_helpers_test_clocks_test_clock_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_test_helpers_test_clocks_test_clock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Test_helpersTest_clock>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Test_helpersTest_clock = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_test_helpers_test_clocks_test_clock`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TestHelpersTestClocksTestClockArgs {
    /// Path parameter: test_clock
    pub test_clock: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/test_helpers/test_clocks/{test_clock}
/// Retrieve a test clock
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_test_helpers_test_clocks_test_clock_builder()` + `get_v1_test_helpers_test_clocks_test_clock_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_test_helpers_test_clocks_test_clock(
    client: &SimpleHttpClient,
    args: &GetV1TestHelpersTestClocksTestClockArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Test_helpersTest_clock>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        get_v1_test_helpers_test_clocks_test_clock_builder(client, &args.test_clock, args.expand)?;
    get_v1_test_helpers_test_clocks_test_clock_execute(builder)
}

/// DELETE /v1/test_helpers/test_clocks/{test_clock}
/// Delete a test clock
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_test_helpers_test_clocks_test_clock_execute()` to send, or `delete_v1_test_helpers_test_clocks_test_clock` for simplest API.

pub fn delete_v1_test_helpers_test_clocks_test_clock_builder(
    client: &SimpleHttpClient,
    test_clock: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/test_clocks/{}",
        test_clock,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/test_helpers/test_clocks/{test_clock}
/// Delete a test clock
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_test_helpers_test_clocks_test_clock_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_test_helpers_test_clocks_test_clock_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_test_helpers_test_clocks_test_clock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Deleted_test_helpersTest_clock>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_test_helpersTest_clock = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_test_helpers_test_clocks_test_clock`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1TestHelpersTestClocksTestClockArgs {
    /// Path parameter: test_clock
    pub test_clock: String,
}

/// DELETE /v1/test_helpers/test_clocks/{test_clock}
/// Delete a test clock
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_test_helpers_test_clocks_test_clock_builder()` + `delete_v1_test_helpers_test_clocks_test_clock_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_test_helpers_test_clocks_test_clock(
    client: &SimpleHttpClient,
    args: &DeleteV1TestHelpersTestClocksTestClockArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<Deleted_test_helpersTest_clock>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = delete_v1_test_helpers_test_clocks_test_clock_builder(client, &args.test_clock)?;
    delete_v1_test_helpers_test_clocks_test_clock_execute(builder)
}

/// POST /v1/test_helpers/test_clocks/{test_clock}/advance
/// Advance a test clock
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_test_clocks_test_clock_advance_execute()` to send, or `post_v1_test_helpers_test_clocks_test_clock_advance` for simplest API.

pub fn post_v1_test_helpers_test_clocks_test_clock_advance_builder(
    client: &SimpleHttpClient,
    test_clock: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/test_clocks/{}/advance",
        test_clock,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/test_clocks/{test_clock}/advance
/// Advance a test clock
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_test_clocks_test_clock_advance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_test_clocks_test_clock_advance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_test_clocks_test_clock_advance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Test_helpersTest_clock>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Test_helpersTest_clock = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_test_clocks_test_clock_advance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTestClocksTestClockAdvanceArgs {
    /// Path parameter: test_clock
    pub test_clock: String,
}

/// POST /v1/test_helpers/test_clocks/{test_clock}/advance
/// Advance a test clock
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_test_clocks_test_clock_advance_builder()` + `post_v1_test_helpers_test_clocks_test_clock_advance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_test_clocks_test_clock_advance(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTestClocksTestClockAdvanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Test_helpersTest_clock>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_test_clocks_test_clock_advance_builder(client, &args.test_clock)?;
    post_v1_test_helpers_test_clocks_test_clock_advance_execute(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail
/// Test mode: Fail an InboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_inbound_transfers_id_fail_execute()` to send, or `post_v1_test_helpers_treasury_inbound_transfers_id_fail` for simplest API.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_fail_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/inbound_transfers/{}/fail",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail
/// Test mode: Fail an InboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_inbound_transfers_id_fail_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_inbound_transfers_id_fail_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_fail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryInbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_inbound_transfers_id_fail`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryInboundTransfersIdFailArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail
/// Test mode: Fail an InboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_inbound_transfers_id_fail_builder()` + `post_v1_test_helpers_treasury_inbound_transfers_id_fail_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_fail(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryInboundTransfersIdFailArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_inbound_transfers_id_fail_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_inbound_transfers_id_fail_execute(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return
/// Test mode: Return an InboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_inbound_transfers_id_return_execute()` to send, or `post_v1_test_helpers_treasury_inbound_transfers_id_return` for simplest API.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_return_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/inbound_transfers/{}/return",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return
/// Test mode: Return an InboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_inbound_transfers_id_return_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_inbound_transfers_id_return_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_return_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryInbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_inbound_transfers_id_return`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryInboundTransfersIdReturnArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return
/// Test mode: Return an InboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_inbound_transfers_id_return_builder()` + `post_v1_test_helpers_treasury_inbound_transfers_id_return_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_return(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryInboundTransfersIdReturnArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_inbound_transfers_id_return_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_inbound_transfers_id_return_execute(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/succeed
/// Test mode: Succeed an InboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_inbound_transfers_id_succeed_execute()` to send, or `post_v1_test_helpers_treasury_inbound_transfers_id_succeed` for simplest API.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_succeed_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/inbound_transfers/{}/succeed",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/succeed
/// Test mode: Succeed an InboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_inbound_transfers_id_succeed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_inbound_transfers_id_succeed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_succeed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryInbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_inbound_transfers_id_succeed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryInboundTransfersIdSucceedArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/succeed
/// Test mode: Succeed an InboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_inbound_transfers_id_succeed_builder()` + `post_v1_test_helpers_treasury_inbound_transfers_id_succeed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_inbound_transfers_id_succeed(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryInboundTransfersIdSucceedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_inbound_transfers_id_succeed_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_inbound_transfers_id_succeed_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}
/// Test mode: Update an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_payments_id_execute()` to send, or `post_v1_test_helpers_treasury_outbound_payments_id` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{}",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}
/// Test mode: Update an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_payments_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_payments_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundPaymentsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}
/// Test mode: Update an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_builder()` + `post_v1_test_helpers_treasury_outbound_payments_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundPaymentsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_treasury_outbound_payments_id_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_outbound_payments_id_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail
/// Test mode: Fail an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_payments_id_fail_execute()` to send, or `post_v1_test_helpers_treasury_outbound_payments_id_fail` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_fail_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{}/fail",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail
/// Test mode: Fail an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_fail_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_payments_id_fail_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_fail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_payments_id_fail`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundPaymentsIdFailArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/fail
/// Test mode: Fail an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_fail_builder()` + `post_v1_test_helpers_treasury_outbound_payments_id_fail_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_fail(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundPaymentsIdFailArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_outbound_payments_id_fail_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_outbound_payments_id_fail_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/post
/// Test mode: Post an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_payments_id_post_execute()` to send, or `post_v1_test_helpers_treasury_outbound_payments_id_post` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_post_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{}/post",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/post
/// Test mode: Post an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_post_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_payments_id_post_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_post_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_payments_id_post`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundPaymentsIdPostArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/post
/// Test mode: Post an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_post_builder()` + `post_v1_test_helpers_treasury_outbound_payments_id_post_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_post(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundPaymentsIdPostArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_outbound_payments_id_post_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_outbound_payments_id_post_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return
/// Test mode: Return an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_payments_id_return_execute()` to send, or `post_v1_test_helpers_treasury_outbound_payments_id_return` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_return_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{}/return",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return
/// Test mode: Return an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_return_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_payments_id_return_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_return_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_payments_id_return`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundPaymentsIdReturnArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return
/// Test mode: Return an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_payments_id_return_builder()` + `post_v1_test_helpers_treasury_outbound_payments_id_return_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_payments_id_return(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundPaymentsIdReturnArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_outbound_payments_id_return_builder(client, &args.id)?;
    post_v1_test_helpers_treasury_outbound_payments_id_return_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}
/// Test mode: Update an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_execute()` to send, or `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_builder(
    client: &SimpleHttpClient,
    outbound_transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{}",
        outbound_transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}
/// Test mode: Update an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}
/// Test mode: Update an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_builder()` + `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_builder(
        client,
        &args.outbound_transfer,
    )?;
    post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail
/// Test mode: Fail an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute()` to send, or `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder(
    client: &SimpleHttpClient,
    outbound_transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{}/fail",
        outbound_transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail
/// Test mode: Fail an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferFailArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail
/// Test mode: Fail an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder()` + `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferFailArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_builder(
        client,
        &args.outbound_transfer,
    )?;
    post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_fail_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/post
/// Test mode: Post an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_execute()` to send, or `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_builder(
    client: &SimpleHttpClient,
    outbound_transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{}/post",
        outbound_transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/post
/// Test mode: Post an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferPostArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/post
/// Test mode: Post an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_builder()` + `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferPostArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_builder(
        client,
        &args.outbound_transfer,
    )?;
    post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_post_execute(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return
/// Test mode: Return an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute()` to send, or `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return` for simplest API.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder(
    client: &SimpleHttpClient,
    outbound_transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{}/return",
        outbound_transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return
/// Test mode: Return an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferReturnArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return
/// Test mode: Return an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder()` + `post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return(
    client: &SimpleHttpClient,
    args: &PostV1TestHelpersTreasuryOutboundTransfersOutboundTransferReturnArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder(
            client,
            &args.outbound_transfer,
        )?;
    post_v1_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute(builder)
}

/// POST /v1/test_helpers/treasury/received_credits
/// Test mode: Create a ReceivedCredit
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_received_credits_execute()` to send, or `post_v1_test_helpers_treasury_received_credits` for simplest API.

pub fn post_v1_test_helpers_treasury_received_credits_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/treasury/received_credits",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/received_credits
/// Test mode: Create a ReceivedCredit
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_received_credits_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_received_credits_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_received_credits_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_credit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryReceived_credit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/received_credits
/// Test mode: Create a ReceivedCredit
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_received_credits_builder()` + `post_v1_test_helpers_treasury_received_credits_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_received_credits(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_credit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_treasury_received_credits_builder(client)?;
    post_v1_test_helpers_treasury_received_credits_execute(builder)
}

/// POST /v1/test_helpers/treasury/received_debits
/// Test mode: Create a ReceivedDebit
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_test_helpers_treasury_received_debits_execute()` to send, or `post_v1_test_helpers_treasury_received_debits` for simplest API.

pub fn post_v1_test_helpers_treasury_received_debits_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/test_helpers/treasury/received_debits",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/received_debits
/// Test mode: Create a ReceivedDebit
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_test_helpers_treasury_received_debits_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_test_helpers_treasury_received_debits_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_received_debits_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_debit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryReceived_debit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/received_debits
/// Test mode: Create a ReceivedDebit
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_test_helpers_treasury_received_debits_builder()` + `post_v1_test_helpers_treasury_received_debits_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_test_helpers_treasury_received_debits(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_debit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_test_helpers_treasury_received_debits_builder(client)?;
    post_v1_test_helpers_treasury_received_debits_execute(builder)
}

/// POST /v1/tokens
/// Create a CVC update token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_tokens_execute()` to send, or `post_v1_tokens` for simplest API.

pub fn post_v1_tokens_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tokens",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tokens
/// Create a CVC update token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Token = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tokens
/// Create a CVC update token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_tokens_builder()` + `post_v1_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_tokens(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_tokens_builder(client)?;
    post_v1_tokens_execute(builder)
}

/// GET /v1/tokens/{token}
/// Retrieve a token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_tokens_token_execute()` to send, or `get_v1_tokens_token` for simplest API.

pub fn get_v1_tokens_token_builder(
    client: &SimpleHttpClient,
    token: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/tokens/{}", token,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tokens/{token}
/// Retrieve a token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_tokens_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_tokens_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tokens_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Token = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_tokens_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TokensTokenArgs {
    /// Path parameter: token
    pub token: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/tokens/{token}
/// Retrieve a token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_tokens_token_builder()` + `get_v1_tokens_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_tokens_token(
    client: &SimpleHttpClient,
    args: &GetV1TokensTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Token>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_tokens_token_builder(client, &args.token, args.expand)?;
    get_v1_tokens_token_execute(builder)
}

/// GET /v1/topups
/// List all top-ups
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_topups_execute()` to send, or `get_v1_topups` for simplest API.

pub fn get_v1_topups_builder(
    client: &SimpleHttpClient,
    amount: Option<&str>,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/topups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = amount {
        query_parts.push(format!("amount={}", val));
    }
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/topups
/// List all top-ups
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_topups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_topups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_topups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_topups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TopupsArgs {
    /// Query parameter: amount
    pub amount: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/topups
/// List all top-ups
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_topups_builder()` + `get_v1_topups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_topups(
    client: &SimpleHttpClient,
    args: &GetV1TopupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_topups_builder(
        client,
        args.amount.as_deref(),
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_topups_execute(builder)
}

/// POST /v1/topups
/// Create a top-up
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_topups_execute()` to send, or `post_v1_topups` for simplest API.

pub fn post_v1_topups_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/topups",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/topups
/// Create a top-up
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_topups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_topups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_topups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/topups
/// Create a top-up
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_topups_builder()` + `post_v1_topups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_topups(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_topups_builder(client)?;
    post_v1_topups_execute(builder)
}

/// GET /v1/topups/{topup}
/// Retrieve a top-up
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_topups_topup_execute()` to send, or `get_v1_topups_topup` for simplest API.

pub fn get_v1_topups_topup_builder(
    client: &SimpleHttpClient,
    topup: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/topups/{}", topup,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/topups/{topup}
/// Retrieve a top-up
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_topups_topup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_topups_topup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_topups_topup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_topups_topup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TopupsTopupArgs {
    /// Path parameter: topup
    pub topup: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/topups/{topup}
/// Retrieve a top-up
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_topups_topup_builder()` + `get_v1_topups_topup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_topups_topup(
    client: &SimpleHttpClient,
    args: &GetV1TopupsTopupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_topups_topup_builder(client, &args.topup, args.expand)?;
    get_v1_topups_topup_execute(builder)
}

/// POST /v1/topups/{topup}
/// Update a top-up
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_topups_topup_execute()` to send, or `post_v1_topups_topup` for simplest API.

pub fn post_v1_topups_topup_builder(
    client: &SimpleHttpClient,
    topup: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/topups/{}", topup,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/topups/{topup}
/// Update a top-up
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_topups_topup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_topups_topup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_topups_topup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_topups_topup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TopupsTopupArgs {
    /// Path parameter: topup
    pub topup: String,
}

/// POST /v1/topups/{topup}
/// Update a top-up
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_topups_topup_builder()` + `post_v1_topups_topup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_topups_topup(
    client: &SimpleHttpClient,
    args: &PostV1TopupsTopupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_topups_topup_builder(client, &args.topup)?;
    post_v1_topups_topup_execute(builder)
}

/// POST /v1/topups/{topup}/cancel
/// Cancel a top-up
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_topups_topup_cancel_execute()` to send, or `post_v1_topups_topup_cancel` for simplest API.

pub fn post_v1_topups_topup_cancel_builder(
    client: &SimpleHttpClient,
    topup: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/topups/{}/cancel", topup,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/topups/{topup}/cancel
/// Cancel a top-up
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_topups_topup_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_topups_topup_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_topups_topup_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_topups_topup_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TopupsTopupCancelArgs {
    /// Path parameter: topup
    pub topup: String,
}

/// POST /v1/topups/{topup}/cancel
/// Cancel a top-up
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_topups_topup_cancel_builder()` + `post_v1_topups_topup_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_topups_topup_cancel(
    client: &SimpleHttpClient,
    args: &PostV1TopupsTopupCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_topups_topup_cancel_builder(client, &args.topup)?;
    post_v1_topups_topup_cancel_execute(builder)
}

/// GET /v1/transfers
/// List all transfers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_transfers_execute()` to send, or `get_v1_transfers` for simplest API.

pub fn get_v1_transfers_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    destination: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    transfer_group: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/transfers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = destination {
        query_parts.push(format!("destination={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = transfer_group {
        query_parts.push(format!("transfer_group={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers
/// List all transfers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_transfers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_transfers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_transfers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TransfersArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: destination
    pub destination: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: transfer_group
    pub transfer_group: Option<String>,
}

/// GET /v1/transfers
/// List all transfers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_transfers_builder()` + `get_v1_transfers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers(
    client: &SimpleHttpClient,
    args: &GetV1TransfersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_transfers_builder(
        client,
        args.created.as_deref(),
        args.destination.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.transfer_group.as_deref(),
    )?;
    get_v1_transfers_execute(builder)
}

/// POST /v1/transfers
/// Create a transfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_transfers_execute()` to send, or `post_v1_transfers` for simplest API.

pub fn post_v1_transfers_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/transfers",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers
/// Create a transfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_transfers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_transfers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/transfers
/// Create a transfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_transfers_builder()` + `post_v1_transfers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_transfers_builder(client)?;
    post_v1_transfers_execute(builder)
}

/// GET /v1/transfers/{id}/reversals
/// List all reversals
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_transfers_id_reversals_execute()` to send, or `get_v1_transfers_id_reversals` for simplest API.

pub fn get_v1_transfers_id_reversals_builder(
    client: &SimpleHttpClient,
    id: &str,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/transfers/{}/reversals", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers/{id}/reversals
/// List all reversals
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_transfers_id_reversals_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_transfers_id_reversals_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_id_reversals_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_transfers_id_reversals`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TransfersIdReversalsArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/transfers/{id}/reversals
/// List all reversals
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_transfers_id_reversals_builder()` + `get_v1_transfers_id_reversals_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_id_reversals(
    client: &SimpleHttpClient,
    args: &GetV1TransfersIdReversalsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_transfers_id_reversals_builder(
        client,
        &args.id,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_transfers_id_reversals_execute(builder)
}

/// POST /v1/transfers/{id}/reversals
/// Create a transfer reversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_transfers_id_reversals_execute()` to send, or `post_v1_transfers_id_reversals` for simplest API.

pub fn post_v1_transfers_id_reversals_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/transfers/{}/reversals", id,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers/{id}/reversals
/// Create a transfer reversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_transfers_id_reversals_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_transfers_id_reversals_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_id_reversals_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transfer_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_transfers_id_reversals`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TransfersIdReversalsArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/transfers/{id}/reversals
/// Create a transfer reversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_transfers_id_reversals_builder()` + `post_v1_transfers_id_reversals_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_id_reversals(
    client: &SimpleHttpClient,
    args: &PostV1TransfersIdReversalsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_transfers_id_reversals_builder(client, &args.id)?;
    post_v1_transfers_id_reversals_execute(builder)
}

/// GET /v1/transfers/{transfer}
/// Retrieve a transfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_transfers_transfer_execute()` to send, or `get_v1_transfers_transfer` for simplest API.

pub fn get_v1_transfers_transfer_builder(
    client: &SimpleHttpClient,
    transfer: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/transfers/{}", transfer,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers/{transfer}
/// Retrieve a transfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_transfers_transfer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_transfers_transfer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_transfer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_transfers_transfer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TransfersTransferArgs {
    /// Path parameter: transfer
    pub transfer: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/transfers/{transfer}
/// Retrieve a transfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_transfers_transfer_builder()` + `get_v1_transfers_transfer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_transfer(
    client: &SimpleHttpClient,
    args: &GetV1TransfersTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_transfers_transfer_builder(client, &args.transfer, args.expand)?;
    get_v1_transfers_transfer_execute(builder)
}

/// POST /v1/transfers/{transfer}
/// Update a transfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_transfers_transfer_execute()` to send, or `post_v1_transfers_transfer` for simplest API.

pub fn post_v1_transfers_transfer_builder(
    client: &SimpleHttpClient,
    transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/transfers/{}", transfer,);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers/{transfer}
/// Update a transfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_transfers_transfer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_transfers_transfer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_transfer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_transfers_transfer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TransfersTransferArgs {
    /// Path parameter: transfer
    pub transfer: String,
}

/// POST /v1/transfers/{transfer}
/// Update a transfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_transfers_transfer_builder()` + `post_v1_transfers_transfer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_transfer(
    client: &SimpleHttpClient,
    args: &PostV1TransfersTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_v1_transfers_transfer_builder(client, &args.transfer)?;
    post_v1_transfers_transfer_execute(builder)
}

/// GET /v1/transfers/{transfer}/reversals/{id}
/// Retrieve a reversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_transfers_transfer_reversals_id_execute()` to send, or `get_v1_transfers_transfer_reversals_id` for simplest API.

pub fn get_v1_transfers_transfer_reversals_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    transfer: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/transfers/{}/reversals/{}",
        id, transfer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers/{transfer}/reversals/{id}
/// Retrieve a reversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_transfers_transfer_reversals_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_transfers_transfer_reversals_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_transfer_reversals_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transfer_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_transfers_transfer_reversals_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TransfersTransferReversalsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: transfer
    pub transfer: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/transfers/{transfer}/reversals/{id}
/// Retrieve a reversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_transfers_transfer_reversals_id_builder()` + `get_v1_transfers_transfer_reversals_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_transfers_transfer_reversals_id(
    client: &SimpleHttpClient,
    args: &GetV1TransfersTransferReversalsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_transfers_transfer_reversals_id_builder(
        client,
        &args.id,
        &args.transfer,
        args.expand,
    )?;
    get_v1_transfers_transfer_reversals_id_execute(builder)
}

/// POST /v1/transfers/{transfer}/reversals/{id}
/// Update a reversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_transfers_transfer_reversals_id_execute()` to send, or `post_v1_transfers_transfer_reversals_id` for simplest API.

pub fn post_v1_transfers_transfer_reversals_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/transfers/{}/reversals/{}",
        id, transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers/{transfer}/reversals/{id}
/// Update a reversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_transfers_transfer_reversals_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_transfers_transfer_reversals_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_transfer_reversals_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transfer_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_transfers_transfer_reversals_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TransfersTransferReversalsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: transfer
    pub transfer: String,
}

/// POST /v1/transfers/{transfer}/reversals/{id}
/// Update a reversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_transfers_transfer_reversals_id_builder()` + `post_v1_transfers_transfer_reversals_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_transfers_transfer_reversals_id(
    client: &SimpleHttpClient,
    args: &PostV1TransfersTransferReversalsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transfer_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_transfers_transfer_reversals_id_builder(client, &args.id, &args.transfer)?;
    post_v1_transfers_transfer_reversals_id_execute(builder)
}

/// GET /v1/treasury/credit_reversals
/// List all CreditReversals
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_credit_reversals_execute()` to send, or `get_v1_treasury_credit_reversals` for simplest API.

pub fn get_v1_treasury_credit_reversals_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    received_credit: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/credit_reversals",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = received_credit {
        query_parts.push(format!("received_credit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/credit_reversals
/// List all CreditReversals
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_credit_reversals_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_credit_reversals_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_credit_reversals_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_credit_reversals`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryCreditReversalsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: received_credit
    pub received_credit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/credit_reversals
/// List all CreditReversals
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_credit_reversals_builder()` + `get_v1_treasury_credit_reversals_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_credit_reversals(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryCreditReversalsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_credit_reversals_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.received_credit.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_credit_reversals_execute(builder)
}

/// POST /v1/treasury/credit_reversals
/// Create a CreditReversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_credit_reversals_execute()` to send, or `post_v1_treasury_credit_reversals` for simplest API.

pub fn post_v1_treasury_credit_reversals_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/credit_reversals",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/credit_reversals
/// Create a CreditReversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_credit_reversals_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_credit_reversals_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_credit_reversals_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryCredit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryCredit_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/credit_reversals
/// Create a CreditReversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_credit_reversals_builder()` + `post_v1_treasury_credit_reversals_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_credit_reversals(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryCredit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_credit_reversals_builder(client)?;
    post_v1_treasury_credit_reversals_execute(builder)
}

/// GET /v1/treasury/credit_reversals/{credit_reversal}
/// Retrieve a CreditReversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_credit_reversals_credit_reversal_execute()` to send, or `get_v1_treasury_credit_reversals_credit_reversal` for simplest API.

pub fn get_v1_treasury_credit_reversals_credit_reversal_builder(
    client: &SimpleHttpClient,
    credit_reversal: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/credit_reversals/{}",
        credit_reversal,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/credit_reversals/{credit_reversal}
/// Retrieve a CreditReversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_credit_reversals_credit_reversal_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_credit_reversals_credit_reversal_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_credit_reversals_credit_reversal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryCredit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryCredit_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_credit_reversals_credit_reversal`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryCreditReversalsCreditReversalArgs {
    /// Path parameter: credit_reversal
    pub credit_reversal: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/credit_reversals/{credit_reversal}
/// Retrieve a CreditReversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_credit_reversals_credit_reversal_builder()` + `get_v1_treasury_credit_reversals_credit_reversal_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_credit_reversals_credit_reversal(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryCreditReversalsCreditReversalArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryCredit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_credit_reversals_credit_reversal_builder(
        client,
        &args.credit_reversal,
        args.expand,
    )?;
    get_v1_treasury_credit_reversals_credit_reversal_execute(builder)
}

/// GET /v1/treasury/debit_reversals
/// List all DebitReversals
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_debit_reversals_execute()` to send, or `get_v1_treasury_debit_reversals` for simplest API.

pub fn get_v1_treasury_debit_reversals_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    received_debit: Option<&str>,
    resolution: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/debit_reversals",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = received_debit {
        query_parts.push(format!("received_debit={}", val));
    }
    if let Some(val) = resolution {
        query_parts.push(format!("resolution={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/debit_reversals
/// List all DebitReversals
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_debit_reversals_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_debit_reversals_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_debit_reversals_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_debit_reversals`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryDebitReversalsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: received_debit
    pub received_debit: Option<String>,
    /// Query parameter: resolution
    pub resolution: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/debit_reversals
/// List all DebitReversals
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_debit_reversals_builder()` + `get_v1_treasury_debit_reversals_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_debit_reversals(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryDebitReversalsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_debit_reversals_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.received_debit.as_deref(),
        args.resolution.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_debit_reversals_execute(builder)
}

/// POST /v1/treasury/debit_reversals
/// Create a DebitReversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_debit_reversals_execute()` to send, or `post_v1_treasury_debit_reversals` for simplest API.

pub fn post_v1_treasury_debit_reversals_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/debit_reversals",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/debit_reversals
/// Create a DebitReversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_debit_reversals_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_debit_reversals_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_debit_reversals_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryDebit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryDebit_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/debit_reversals
/// Create a DebitReversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_debit_reversals_builder()` + `post_v1_treasury_debit_reversals_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_debit_reversals(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryDebit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_debit_reversals_builder(client)?;
    post_v1_treasury_debit_reversals_execute(builder)
}

/// GET /v1/treasury/debit_reversals/{debit_reversal}
/// Retrieve a DebitReversal
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_debit_reversals_debit_reversal_execute()` to send, or `get_v1_treasury_debit_reversals_debit_reversal` for simplest API.

pub fn get_v1_treasury_debit_reversals_debit_reversal_builder(
    client: &SimpleHttpClient,
    debit_reversal: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/debit_reversals/{}",
        debit_reversal,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/debit_reversals/{debit_reversal}
/// Retrieve a DebitReversal
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_debit_reversals_debit_reversal_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_debit_reversals_debit_reversal_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_debit_reversals_debit_reversal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryDebit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryDebit_reversal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_debit_reversals_debit_reversal`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryDebitReversalsDebitReversalArgs {
    /// Path parameter: debit_reversal
    pub debit_reversal: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/debit_reversals/{debit_reversal}
/// Retrieve a DebitReversal
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_debit_reversals_debit_reversal_builder()` + `get_v1_treasury_debit_reversals_debit_reversal_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_debit_reversals_debit_reversal(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryDebitReversalsDebitReversalArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryDebit_reversal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_debit_reversals_debit_reversal_builder(
        client,
        &args.debit_reversal,
        args.expand,
    )?;
    get_v1_treasury_debit_reversals_debit_reversal_execute(builder)
}

/// GET /v1/treasury/financial_accounts
/// List all FinancialAccounts
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_financial_accounts_execute()` to send, or `get_v1_treasury_financial_accounts` for simplest API.

pub fn get_v1_treasury_financial_accounts_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/financial_accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/financial_accounts
/// List all FinancialAccounts
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_financial_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_financial_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_financial_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_financial_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryFinancialAccountsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/financial_accounts
/// List all FinancialAccounts
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_financial_accounts_builder()` + `get_v1_treasury_financial_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_financial_accounts(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryFinancialAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_financial_accounts_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_financial_accounts_execute(builder)
}

/// POST /v1/treasury/financial_accounts
/// Create a FinancialAccount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_financial_accounts_execute()` to send, or `post_v1_treasury_financial_accounts` for simplest API.

pub fn post_v1_treasury_financial_accounts_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/financial_accounts",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/financial_accounts
/// Create a FinancialAccount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_financial_accounts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_financial_accounts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryFinancial_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/financial_accounts
/// Create a FinancialAccount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_financial_accounts_builder()` + `post_v1_treasury_financial_accounts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_financial_accounts_builder(client)?;
    post_v1_treasury_financial_accounts_execute(builder)
}

/// GET /v1/treasury/financial_accounts/{financial_account}
/// Retrieve a FinancialAccount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_financial_accounts_financial_account_execute()` to send, or `get_v1_treasury_financial_accounts_financial_account` for simplest API.

pub fn get_v1_treasury_financial_accounts_financial_account_builder(
    client: &SimpleHttpClient,
    financial_account: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{}",
        financial_account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/financial_accounts/{financial_account}
/// Retrieve a FinancialAccount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_financial_accounts_financial_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_financial_accounts_financial_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_financial_accounts_financial_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryFinancial_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_financial_accounts_financial_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryFinancialAccountsFinancialAccountArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/financial_accounts/{financial_account}
/// Retrieve a FinancialAccount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_financial_accounts_financial_account_builder()` + `get_v1_treasury_financial_accounts_financial_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_financial_accounts_financial_account(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryFinancialAccountsFinancialAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_financial_accounts_financial_account_builder(
        client,
        &args.financial_account,
        args.expand,
    )?;
    get_v1_treasury_financial_accounts_financial_account_execute(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account}
/// Update a FinancialAccount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_financial_accounts_financial_account_execute()` to send, or `post_v1_treasury_financial_accounts_financial_account` for simplest API.

pub fn post_v1_treasury_financial_accounts_financial_account_builder(
    client: &SimpleHttpClient,
    financial_account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{}",
        financial_account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account}
/// Update a FinancialAccount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_financial_accounts_financial_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_financial_accounts_financial_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_financial_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryFinancial_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_treasury_financial_accounts_financial_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TreasuryFinancialAccountsFinancialAccountArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
}

/// POST /v1/treasury/financial_accounts/{financial_account}
/// Update a FinancialAccount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_financial_accounts_financial_account_builder()` + `post_v1_treasury_financial_accounts_financial_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_financial_account(
    client: &SimpleHttpClient,
    args: &PostV1TreasuryFinancialAccountsFinancialAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_financial_accounts_financial_account_builder(
        client,
        &args.financial_account,
    )?;
    post_v1_treasury_financial_accounts_financial_account_execute(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account}/close
/// Close a FinancialAccount
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_financial_accounts_financial_account_close_execute()` to send, or `post_v1_treasury_financial_accounts_financial_account_close` for simplest API.

pub fn post_v1_treasury_financial_accounts_financial_account_close_builder(
    client: &SimpleHttpClient,
    financial_account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{}/close",
        financial_account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account}/close
/// Close a FinancialAccount
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_financial_accounts_financial_account_close_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_financial_accounts_financial_account_close_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_financial_account_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryFinancial_account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_treasury_financial_accounts_financial_account_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TreasuryFinancialAccountsFinancialAccountCloseArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
}

/// POST /v1/treasury/financial_accounts/{financial_account}/close
/// Close a FinancialAccount
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_financial_accounts_financial_account_close_builder()` + `post_v1_treasury_financial_accounts_financial_account_close_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_financial_account_close(
    client: &SimpleHttpClient,
    args: &PostV1TreasuryFinancialAccountsFinancialAccountCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryFinancial_account>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_financial_accounts_financial_account_close_builder(
        client,
        &args.financial_account,
    )?;
    post_v1_treasury_financial_accounts_financial_account_close_execute(builder)
}

/// GET /v1/treasury/financial_accounts/{financial_account}/features
/// Retrieve FinancialAccount Features
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_financial_accounts_financial_account_features_execute()` to send, or `get_v1_treasury_financial_accounts_financial_account_features` for simplest API.

pub fn get_v1_treasury_financial_accounts_financial_account_features_builder(
    client: &SimpleHttpClient,
    financial_account: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{}/features",
        financial_account,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/financial_accounts/{financial_account}/features
/// Retrieve FinancialAccount Features
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_financial_accounts_financial_account_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_financial_accounts_financial_account_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_financial_accounts_financial_account_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancial_account_features>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryFinancial_account_features = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_financial_accounts_financial_account_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryFinancialAccountsFinancialAccountFeaturesArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/financial_accounts/{financial_account}/features
/// Retrieve FinancialAccount Features
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_financial_accounts_financial_account_features_builder()` + `get_v1_treasury_financial_accounts_financial_account_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_financial_accounts_financial_account_features(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryFinancialAccountsFinancialAccountFeaturesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancial_account_features>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_financial_accounts_financial_account_features_builder(
        client,
        &args.financial_account,
        args.expand,
    )?;
    get_v1_treasury_financial_accounts_financial_account_features_execute(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account}/features
/// Update FinancialAccount Features
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_financial_accounts_financial_account_features_execute()` to send, or `post_v1_treasury_financial_accounts_financial_account_features` for simplest API.

pub fn post_v1_treasury_financial_accounts_financial_account_features_builder(
    client: &SimpleHttpClient,
    financial_account: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/financial_accounts/{}/features",
        financial_account,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/financial_accounts/{financial_account}/features
/// Update FinancialAccount Features
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_financial_accounts_financial_account_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_financial_accounts_financial_account_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_financial_account_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancial_account_features>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryFinancial_account_features = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_treasury_financial_accounts_financial_account_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TreasuryFinancialAccountsFinancialAccountFeaturesArgs {
    /// Path parameter: financial_account
    pub financial_account: String,
}

/// POST /v1/treasury/financial_accounts/{financial_account}/features
/// Update FinancialAccount Features
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_financial_accounts_financial_account_features_builder()` + `post_v1_treasury_financial_accounts_financial_account_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_financial_accounts_financial_account_features(
    client: &SimpleHttpClient,
    args: &PostV1TreasuryFinancialAccountsFinancialAccountFeaturesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryFinancial_account_features>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_financial_accounts_financial_account_features_builder(
        client,
        &args.financial_account,
    )?;
    post_v1_treasury_financial_accounts_financial_account_features_execute(builder)
}

/// GET /v1/treasury/inbound_transfers
/// List all InboundTransfers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_inbound_transfers_execute()` to send, or `get_v1_treasury_inbound_transfers` for simplest API.

pub fn get_v1_treasury_inbound_transfers_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/inbound_transfers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/inbound_transfers
/// List all InboundTransfers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_inbound_transfers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_inbound_transfers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_inbound_transfers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_inbound_transfers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryInboundTransfersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/inbound_transfers
/// List all InboundTransfers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_inbound_transfers_builder()` + `get_v1_treasury_inbound_transfers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_inbound_transfers(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryInboundTransfersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_inbound_transfers_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_inbound_transfers_execute(builder)
}

/// POST /v1/treasury/inbound_transfers
/// Create an InboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_inbound_transfers_execute()` to send, or `post_v1_treasury_inbound_transfers` for simplest API.

pub fn post_v1_treasury_inbound_transfers_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/inbound_transfers",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/inbound_transfers
/// Create an InboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_inbound_transfers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_inbound_transfers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_inbound_transfers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryInbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/inbound_transfers
/// Create an InboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_inbound_transfers_builder()` + `post_v1_treasury_inbound_transfers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_inbound_transfers(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_inbound_transfers_builder(client)?;
    post_v1_treasury_inbound_transfers_execute(builder)
}

/// GET /v1/treasury/inbound_transfers/{id}
/// Retrieve an InboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_inbound_transfers_id_execute()` to send, or `get_v1_treasury_inbound_transfers_id` for simplest API.

pub fn get_v1_treasury_inbound_transfers_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/inbound_transfers/{}",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/inbound_transfers/{id}
/// Retrieve an InboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_inbound_transfers_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_inbound_transfers_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_inbound_transfers_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryInbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_inbound_transfers_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryInboundTransfersIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/inbound_transfers/{id}
/// Retrieve an InboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_inbound_transfers_id_builder()` + `get_v1_treasury_inbound_transfers_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_inbound_transfers_id(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryInboundTransfersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_inbound_transfers_id_builder(client, &args.id, args.expand)?;
    get_v1_treasury_inbound_transfers_id_execute(builder)
}

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel
/// Cancel an InboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_inbound_transfers_inbound_transfer_cancel_execute()` to send, or `post_v1_treasury_inbound_transfers_inbound_transfer_cancel` for simplest API.

pub fn post_v1_treasury_inbound_transfers_inbound_transfer_cancel_builder(
    client: &SimpleHttpClient,
    inbound_transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/inbound_transfers/{}/cancel",
        inbound_transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel
/// Cancel an InboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_inbound_transfers_inbound_transfer_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_inbound_transfers_inbound_transfer_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_inbound_transfers_inbound_transfer_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryInbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_treasury_inbound_transfers_inbound_transfer_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TreasuryInboundTransfersInboundTransferCancelArgs {
    /// Path parameter: inbound_transfer
    pub inbound_transfer: String,
}

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel
/// Cancel an InboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_inbound_transfers_inbound_transfer_cancel_builder()` + `post_v1_treasury_inbound_transfers_inbound_transfer_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_inbound_transfers_inbound_transfer_cancel(
    client: &SimpleHttpClient,
    args: &PostV1TreasuryInboundTransfersInboundTransferCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryInbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_inbound_transfers_inbound_transfer_cancel_builder(
        client,
        &args.inbound_transfer,
    )?;
    post_v1_treasury_inbound_transfers_inbound_transfer_cancel_execute(builder)
}

/// GET /v1/treasury/outbound_payments
/// List all OutboundPayments
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_outbound_payments_execute()` to send, or `get_v1_treasury_outbound_payments` for simplest API.

pub fn get_v1_treasury_outbound_payments_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    customer: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/outbound_payments",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = customer {
        query_parts.push(format!("customer={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_payments
/// List all OutboundPayments
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_outbound_payments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_outbound_payments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_payments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_outbound_payments`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryOutboundPaymentsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/outbound_payments
/// List all OutboundPayments
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_outbound_payments_builder()` + `get_v1_treasury_outbound_payments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_payments(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryOutboundPaymentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_outbound_payments_builder(
        client,
        args.created.as_deref(),
        args.customer.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_outbound_payments_execute(builder)
}

/// POST /v1/treasury/outbound_payments
/// Create an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_outbound_payments_execute()` to send, or `post_v1_treasury_outbound_payments` for simplest API.

pub fn post_v1_treasury_outbound_payments_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/outbound_payments",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_payments
/// Create an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_outbound_payments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_outbound_payments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_payments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/outbound_payments
/// Create an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_outbound_payments_builder()` + `post_v1_treasury_outbound_payments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_payments(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_outbound_payments_builder(client)?;
    post_v1_treasury_outbound_payments_execute(builder)
}

/// GET /v1/treasury/outbound_payments/{id}
/// Retrieve an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_outbound_payments_id_execute()` to send, or `get_v1_treasury_outbound_payments_id` for simplest API.

pub fn get_v1_treasury_outbound_payments_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/outbound_payments/{}",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_payments/{id}
/// Retrieve an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_outbound_payments_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_outbound_payments_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_payments_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_outbound_payments_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryOutboundPaymentsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/outbound_payments/{id}
/// Retrieve an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_outbound_payments_id_builder()` + `get_v1_treasury_outbound_payments_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_payments_id(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryOutboundPaymentsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_outbound_payments_id_builder(client, &args.id, args.expand)?;
    get_v1_treasury_outbound_payments_id_execute(builder)
}

/// POST /v1/treasury/outbound_payments/{id}/cancel
/// Cancel an OutboundPayment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_outbound_payments_id_cancel_execute()` to send, or `post_v1_treasury_outbound_payments_id_cancel` for simplest API.

pub fn post_v1_treasury_outbound_payments_id_cancel_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/outbound_payments/{}/cancel",
        id,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_payments/{id}/cancel
/// Cancel an OutboundPayment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_outbound_payments_id_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_outbound_payments_id_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_payments_id_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_payment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_treasury_outbound_payments_id_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TreasuryOutboundPaymentsIdCancelArgs {
    /// Path parameter: id
    pub id: String,
}

/// POST /v1/treasury/outbound_payments/{id}/cancel
/// Cancel an OutboundPayment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_outbound_payments_id_cancel_builder()` + `post_v1_treasury_outbound_payments_id_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_payments_id_cancel(
    client: &SimpleHttpClient,
    args: &PostV1TreasuryOutboundPaymentsIdCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_payment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_outbound_payments_id_cancel_builder(client, &args.id)?;
    post_v1_treasury_outbound_payments_id_cancel_execute(builder)
}

/// GET /v1/treasury/outbound_transfers
/// List all OutboundTransfers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_outbound_transfers_execute()` to send, or `get_v1_treasury_outbound_transfers` for simplest API.

pub fn get_v1_treasury_outbound_transfers_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/outbound_transfers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_transfers
/// List all OutboundTransfers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_outbound_transfers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_outbound_transfers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_transfers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_outbound_transfers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryOutboundTransfersArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/outbound_transfers
/// List all OutboundTransfers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_outbound_transfers_builder()` + `get_v1_treasury_outbound_transfers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_transfers(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryOutboundTransfersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_outbound_transfers_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_outbound_transfers_execute(builder)
}

/// POST /v1/treasury/outbound_transfers
/// Create an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_outbound_transfers_execute()` to send, or `post_v1_treasury_outbound_transfers` for simplest API.

pub fn post_v1_treasury_outbound_transfers_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/outbound_transfers",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_transfers
/// Create an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_outbound_transfers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_outbound_transfers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_transfers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/outbound_transfers
/// Create an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_outbound_transfers_builder()` + `post_v1_treasury_outbound_transfers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_transfers(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_outbound_transfers_builder(client)?;
    post_v1_treasury_outbound_transfers_execute(builder)
}

/// GET /v1/treasury/outbound_transfers/{outbound_transfer}
/// Retrieve an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_outbound_transfers_outbound_transfer_execute()` to send, or `get_v1_treasury_outbound_transfers_outbound_transfer` for simplest API.

pub fn get_v1_treasury_outbound_transfers_outbound_transfer_builder(
    client: &SimpleHttpClient,
    outbound_transfer: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/outbound_transfers/{}",
        outbound_transfer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/outbound_transfers/{outbound_transfer}
/// Retrieve an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_outbound_transfers_outbound_transfer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_outbound_transfers_outbound_transfer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_transfers_outbound_transfer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_outbound_transfers_outbound_transfer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryOutboundTransfersOutboundTransferArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/outbound_transfers/{outbound_transfer}
/// Retrieve an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_outbound_transfers_outbound_transfer_builder()` + `get_v1_treasury_outbound_transfers_outbound_transfer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_outbound_transfers_outbound_transfer(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryOutboundTransfersOutboundTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_outbound_transfers_outbound_transfer_builder(
        client,
        &args.outbound_transfer,
        args.expand,
    )?;
    get_v1_treasury_outbound_transfers_outbound_transfer_execute(builder)
}

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel
/// Cancel an OutboundTransfer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_treasury_outbound_transfers_outbound_transfer_cancel_execute()` to send, or `post_v1_treasury_outbound_transfers_outbound_transfer_cancel` for simplest API.

pub fn post_v1_treasury_outbound_transfers_outbound_transfer_cancel_builder(
    client: &SimpleHttpClient,
    outbound_transfer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/outbound_transfers/{}/cancel",
        outbound_transfer,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel
/// Cancel an OutboundTransfer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_treasury_outbound_transfers_outbound_transfer_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_treasury_outbound_transfers_outbound_transfer_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_transfers_outbound_transfer_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryOutbound_transfer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_treasury_outbound_transfers_outbound_transfer_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1TreasuryOutboundTransfersOutboundTransferCancelArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel
/// Cancel an OutboundTransfer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_treasury_outbound_transfers_outbound_transfer_cancel_builder()` + `post_v1_treasury_outbound_transfers_outbound_transfer_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_treasury_outbound_transfers_outbound_transfer_cancel(
    client: &SimpleHttpClient,
    args: &PostV1TreasuryOutboundTransfersOutboundTransferCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryOutbound_transfer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_treasury_outbound_transfers_outbound_transfer_cancel_builder(
        client,
        &args.outbound_transfer,
    )?;
    post_v1_treasury_outbound_transfers_outbound_transfer_cancel_execute(builder)
}

/// GET /v1/treasury/received_credits
/// List all ReceivedCredits
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_received_credits_execute()` to send, or `get_v1_treasury_received_credits` for simplest API.

pub fn get_v1_treasury_received_credits_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    linked_flows: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/received_credits",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = linked_flows {
        query_parts.push(format!("linked_flows={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/received_credits
/// List all ReceivedCredits
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_received_credits_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_received_credits_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_credits_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_received_credits`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryReceivedCreditsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: linked_flows
    pub linked_flows: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/received_credits
/// List all ReceivedCredits
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_received_credits_builder()` + `get_v1_treasury_received_credits_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_credits(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryReceivedCreditsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_received_credits_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.linked_flows.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_received_credits_execute(builder)
}

/// GET /v1/treasury/received_credits/{id}
/// Retrieve a ReceivedCredit
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_received_credits_id_execute()` to send, or `get_v1_treasury_received_credits_id` for simplest API.

pub fn get_v1_treasury_received_credits_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/received_credits/{}",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/received_credits/{id}
/// Retrieve a ReceivedCredit
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_received_credits_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_received_credits_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_credits_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_credit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryReceived_credit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_received_credits_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryReceivedCreditsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/received_credits/{id}
/// Retrieve a ReceivedCredit
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_received_credits_id_builder()` + `get_v1_treasury_received_credits_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_credits_id(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryReceivedCreditsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_credit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_received_credits_id_builder(client, &args.id, args.expand)?;
    get_v1_treasury_received_credits_id_execute(builder)
}

/// GET /v1/treasury/received_debits
/// List all ReceivedDebits
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_received_debits_execute()` to send, or `get_v1_treasury_received_debits` for simplest API.

pub fn get_v1_treasury_received_debits_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    starting_after: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/received_debits",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/received_debits
/// List all ReceivedDebits
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_received_debits_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_received_debits_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_debits_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_received_debits`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryReceivedDebitsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET /v1/treasury/received_debits
/// List all ReceivedDebits
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_received_debits_builder()` + `get_v1_treasury_received_debits_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_debits(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryReceivedDebitsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_received_debits_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.starting_after.as_deref(),
        args.status.as_deref(),
    )?;
    get_v1_treasury_received_debits_execute(builder)
}

/// GET /v1/treasury/received_debits/{id}
/// Retrieve a ReceivedDebit
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_received_debits_id_execute()` to send, or `get_v1_treasury_received_debits_id` for simplest API.

pub fn get_v1_treasury_received_debits_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/received_debits/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/received_debits/{id}
/// Retrieve a ReceivedDebit
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_received_debits_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_received_debits_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_debits_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_debit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryReceived_debit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_received_debits_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryReceivedDebitsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/received_debits/{id}
/// Retrieve a ReceivedDebit
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_received_debits_id_builder()` + `get_v1_treasury_received_debits_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_received_debits_id(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryReceivedDebitsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryReceived_debit>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_received_debits_id_builder(client, &args.id, args.expand)?;
    get_v1_treasury_received_debits_id_execute(builder)
}

/// GET /v1/treasury/transaction_entries
/// List all TransactionEntries
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_transaction_entries_execute()` to send, or `get_v1_treasury_transaction_entries` for simplest API.

pub fn get_v1_treasury_transaction_entries_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    effective_at: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    order_by: Option<&str>,
    starting_after: Option<&str>,
    transaction: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/transaction_entries",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = effective_at {
        query_parts.push(format!("effective_at={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = order_by {
        query_parts.push(format!("order_by={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = transaction {
        query_parts.push(format!("transaction={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/transaction_entries
/// List all TransactionEntries
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_transaction_entries_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_transaction_entries_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transaction_entries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_transaction_entries`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryTransactionEntriesArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: effective_at
    pub effective_at: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: order_by
    pub order_by: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: transaction
    pub transaction: Option<String>,
}

/// GET /v1/treasury/transaction_entries
/// List all TransactionEntries
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_transaction_entries_builder()` + `get_v1_treasury_transaction_entries_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transaction_entries(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryTransactionEntriesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_transaction_entries_builder(
        client,
        args.created.as_deref(),
        args.effective_at.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.order_by.as_deref(),
        args.starting_after.as_deref(),
        args.transaction.as_deref(),
    )?;
    get_v1_treasury_transaction_entries_execute(builder)
}

/// GET /v1/treasury/transaction_entries/{id}
/// Retrieve a TransactionEntry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_transaction_entries_id_execute()` to send, or `get_v1_treasury_transaction_entries_id` for simplest API.

pub fn get_v1_treasury_transaction_entries_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/treasury/transaction_entries/{}",
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/transaction_entries/{id}
/// Retrieve a TransactionEntry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_transaction_entries_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_transaction_entries_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transaction_entries_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryTransaction_entry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryTransaction_entry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_transaction_entries_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryTransactionEntriesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/transaction_entries/{id}
/// Retrieve a TransactionEntry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_transaction_entries_id_builder()` + `get_v1_treasury_transaction_entries_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transaction_entries_id(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryTransactionEntriesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryTransaction_entry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_transaction_entries_id_builder(client, &args.id, args.expand)?;
    get_v1_treasury_transaction_entries_id_execute(builder)
}

/// GET /v1/treasury/transactions
/// List all Transactions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_transactions_execute()` to send, or `get_v1_treasury_transactions` for simplest API.

pub fn get_v1_treasury_transactions_builder(
    client: &SimpleHttpClient,
    created: Option<&str>,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    financial_account: Option<&str>,
    limit: Option<i32>,
    order_by: Option<&str>,
    starting_after: Option<&str>,
    status: Option<&str>,
    status_transitions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/transactions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = created {
        query_parts.push(format!("created={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = financial_account {
        query_parts.push(format!("financial_account={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = order_by {
        query_parts.push(format!("order_by={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = status_transitions {
        query_parts.push(format!("status_transitions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/transactions
/// List all Transactions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_transactions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_transactions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transactions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_transactions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryTransactionsArgs {
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: financial_account
    pub financial_account: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: order_by
    pub order_by: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: status_transitions
    pub status_transitions: Option<String>,
}

/// GET /v1/treasury/transactions
/// List all Transactions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_transactions_builder()` + `get_v1_treasury_transactions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transactions(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryTransactionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_transactions_builder(
        client,
        args.created.as_deref(),
        args.ending_before.as_deref(),
        args.expand,
        args.financial_account.as_deref(),
        args.limit,
        args.order_by.as_deref(),
        args.starting_after.as_deref(),
        args.status.as_deref(),
        args.status_transitions.as_deref(),
    )?;
    get_v1_treasury_transactions_execute(builder)
}

/// GET /v1/treasury/transactions/{id}
/// Retrieve a Transaction
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_treasury_transactions_id_execute()` to send, or `get_v1_treasury_transactions_id` for simplest API.

pub fn get_v1_treasury_transactions_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/treasury/transactions/{}", id,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/treasury/transactions/{id}
/// Retrieve a Transaction
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_treasury_transactions_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_treasury_transactions_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transactions_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TreasuryTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_treasury_transactions_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1TreasuryTransactionsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/treasury/transactions/{id}
/// Retrieve a Transaction
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_treasury_transactions_id_builder()` + `get_v1_treasury_transactions_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_treasury_transactions_id(
    client: &SimpleHttpClient,
    args: &GetV1TreasuryTransactionsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TreasuryTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_treasury_transactions_id_builder(client, &args.id, args.expand)?;
    get_v1_treasury_transactions_id_execute(builder)
}

/// GET /v1/webhook_endpoints
/// List all webhook endpoints
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_webhook_endpoints_execute()` to send, or `get_v1_webhook_endpoints` for simplest API.

pub fn get_v1_webhook_endpoints_builder(
    client: &SimpleHttpClient,
    ending_before: Option<&str>,
    expand: Option<Vec<String>>,
    limit: Option<i32>,
    starting_after: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/webhook_endpoints",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/webhook_endpoints
/// List all webhook endpoints
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_webhook_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_webhook_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_webhook_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_webhook_endpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1WebhookEndpointsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// GET /v1/webhook_endpoints
/// List all webhook endpoints
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_webhook_endpoints_builder()` + `get_v1_webhook_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_webhook_endpoints(
    client: &SimpleHttpClient,
    args: &GetV1WebhookEndpointsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_v1_webhook_endpoints_builder(
        client,
        args.ending_before.as_deref(),
        args.expand,
        args.limit,
        args.starting_after.as_deref(),
    )?;
    get_v1_webhook_endpoints_execute(builder)
}

/// POST /v1/webhook_endpoints
/// Create a webhook endpoint
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_webhook_endpoints_execute()` to send, or `post_v1_webhook_endpoints` for simplest API.

pub fn post_v1_webhook_endpoints_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.stripe.com//v1/webhook_endpoints",);

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/webhook_endpoints
/// Create a webhook endpoint
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_webhook_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_webhook_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_webhook_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webhook_endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/webhook_endpoints
/// Create a webhook endpoint
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_webhook_endpoints_builder()` + `post_v1_webhook_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_webhook_endpoints(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = post_v1_webhook_endpoints_builder(client)?;
    post_v1_webhook_endpoints_execute(builder)
}

/// GET /v1/webhook_endpoints/{webhook_endpoint}
/// Retrieve a webhook endpoint
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_v1_webhook_endpoints_webhook_endpoint_execute()` to send, or `get_v1_webhook_endpoints_webhook_endpoint` for simplest API.

pub fn get_v1_webhook_endpoints_webhook_endpoint_builder(
    client: &SimpleHttpClient,
    webhook_endpoint: &str,
    expand: Option<Vec<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/webhook_endpoints/{}",
        webhook_endpoint,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/webhook_endpoints/{webhook_endpoint}
/// Retrieve a webhook endpoint
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_v1_webhook_endpoints_webhook_endpoint_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_v1_webhook_endpoints_webhook_endpoint_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_webhook_endpoints_webhook_endpoint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webhook_endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_v1_webhook_endpoints_webhook_endpoint`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1WebhookEndpointsWebhookEndpointArgs {
    /// Path parameter: webhook_endpoint
    pub webhook_endpoint: String,
    /// Query parameter: expand
    pub expand: Option<Vec<String>>,
}

/// GET /v1/webhook_endpoints/{webhook_endpoint}
/// Retrieve a webhook endpoint
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_v1_webhook_endpoints_webhook_endpoint_builder()` + `get_v1_webhook_endpoints_webhook_endpoint_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_v1_webhook_endpoints_webhook_endpoint(
    client: &SimpleHttpClient,
    args: &GetV1WebhookEndpointsWebhookEndpointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = get_v1_webhook_endpoints_webhook_endpoint_builder(
        client,
        &args.webhook_endpoint,
        args.expand,
    )?;
    get_v1_webhook_endpoints_webhook_endpoint_execute(builder)
}

/// POST /v1/webhook_endpoints/{webhook_endpoint}
/// Update a webhook endpoint
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_v1_webhook_endpoints_webhook_endpoint_execute()` to send, or `post_v1_webhook_endpoints_webhook_endpoint` for simplest API.

pub fn post_v1_webhook_endpoints_webhook_endpoint_builder(
    client: &SimpleHttpClient,
    webhook_endpoint: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/webhook_endpoints/{}",
        webhook_endpoint,
    );

    // Build request
    let builder = client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/webhook_endpoints/{webhook_endpoint}
/// Update a webhook endpoint
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_v1_webhook_endpoints_webhook_endpoint_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_v1_webhook_endpoints_webhook_endpoint_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_webhook_endpoints_webhook_endpoint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Webhook_endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_v1_webhook_endpoints_webhook_endpoint`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1WebhookEndpointsWebhookEndpointArgs {
    /// Path parameter: webhook_endpoint
    pub webhook_endpoint: String,
}

/// POST /v1/webhook_endpoints/{webhook_endpoint}
/// Update a webhook endpoint
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_v1_webhook_endpoints_webhook_endpoint_builder()` + `post_v1_webhook_endpoints_webhook_endpoint_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_v1_webhook_endpoints_webhook_endpoint(
    client: &SimpleHttpClient,
    args: &PostV1WebhookEndpointsWebhookEndpointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        post_v1_webhook_endpoints_webhook_endpoint_builder(client, &args.webhook_endpoint)?;
    post_v1_webhook_endpoints_webhook_endpoint_execute(builder)
}

/// DELETE /v1/webhook_endpoints/{webhook_endpoint}
/// Delete a webhook endpoint
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_v1_webhook_endpoints_webhook_endpoint_execute()` to send, or `delete_v1_webhook_endpoints_webhook_endpoint` for simplest API.

pub fn delete_v1_webhook_endpoints_webhook_endpoint_builder(
    client: &SimpleHttpClient,
    webhook_endpoint: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.stripe.com//v1/webhook_endpoints/{}",
        webhook_endpoint,
    );

    // Build request
    let builder = client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /v1/webhook_endpoints/{webhook_endpoint}
/// Delete a webhook endpoint
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_v1_webhook_endpoints_webhook_endpoint_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_v1_webhook_endpoints_webhook_endpoint_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_webhook_endpoints_webhook_endpoint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deleted_webhook_endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_v1_webhook_endpoints_webhook_endpoint`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1WebhookEndpointsWebhookEndpointArgs {
    /// Path parameter: webhook_endpoint
    pub webhook_endpoint: String,
}

/// DELETE /v1/webhook_endpoints/{webhook_endpoint}
/// Delete a webhook endpoint
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_v1_webhook_endpoints_webhook_endpoint_builder()` + `delete_v1_webhook_endpoints_webhook_endpoint_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_v1_webhook_endpoints_webhook_endpoint(
    client: &SimpleHttpClient,
    args: &DeleteV1WebhookEndpointsWebhookEndpointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deleted_webhook_endpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        delete_v1_webhook_endpoints_webhook_endpoint_builder(client, &args.webhook_endpoint)?;
    delete_v1_webhook_endpoints_webhook_endpoint_execute(builder)
}
