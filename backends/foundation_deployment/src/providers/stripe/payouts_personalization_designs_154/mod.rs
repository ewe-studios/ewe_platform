//! Auto-generated API module for stripe payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_payouts_personalization_designs_154 `

#![cfg(feature = "stripe_payouts_personalization_designs_154")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// IssuingCard response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingCard {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IssuingPersonalizationDesign response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuingPersonalizationDesign {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Payout response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Payout {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Price response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Price {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// PromotionCode response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PromotionCode {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Quote response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Quote {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SetupIntent response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetupIntent {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// ShippingRate response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShippingRate {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SubscriptionSchedule response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionSchedule {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TaxRate response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaxRate {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TaxRegistration response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaxRegistration {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TransferReversal response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TransferReversal {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryInboundTransfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryInboundTransfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryOutboundPayment response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryOutboundPayment {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryOutboundTransfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryOutboundTransfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`GetPayouts_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPayoutsArgs {
    /// Query parameter: arrival_date
    pub arrival_date: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: destination
    pub destination: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetPayoutsPayout_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPayoutsPayoutArgs {
    /// Path parameter: payout
    pub payout: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPayoutsPayout_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPayoutsPayoutArgs {
    /// Path parameter: payout
    pub payout: String,
}

/// Arguments for [`GetIssuingPersonalizationDesigns_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingPersonalizationDesignsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: lookup_keys
    pub lookup_keys: Option<String>,
    /// Query parameter: preferences
    pub preferences: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetIssuingPersonalizationDesignsPersonalizationDesign_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetIssuingPersonalizationDesignsPersonalizationDesignArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostIssuingPersonalizationDesignsPersonalizationDesign_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIssuingPersonalizationDesignsPersonalizationDesignArgs {
    /// Path parameter: personalization_design
    pub personalization_design: String,
}

/// Arguments for [`GetPrices_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPricesArgs {
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: lookup_keys
    pub lookup_keys: Option<String>,
    /// Query parameter: product
    pub product: Option<String>,
    /// Query parameter: recurring
    pub recurring: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: type
    pub type: Option<String>,
}

/// Arguments for [`GetPricesPrice_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPricesPriceArgs {
    /// Path parameter: price
    pub price: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPricesPrice_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPricesPriceArgs {
    /// Path parameter: price
    pub price: String,
}

/// Arguments for [`GetPromotionCodes_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPromotionCodesArgs {
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: code
    pub code: Option<String>,
    /// Query parameter: coupon
    pub coupon: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetPromotionCodesPromotionCode_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPromotionCodesPromotionCodeArgs {
    /// Path parameter: promotion_code
    pub promotion_code: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostPromotionCodesPromotionCode_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPromotionCodesPromotionCodeArgs {
    /// Path parameter: promotion_code
    pub promotion_code: String,
}

/// Arguments for [`GetQuotes_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetQuotesArgs {
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: test_clock
    pub test_clock: Option<String>,
}

/// Arguments for [`GetQuotesQuote_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetQuotesQuoteArgs {
    /// Path parameter: quote
    pub quote: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostQuotesQuote_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostQuotesQuoteArgs {
    /// Path parameter: quote
    pub quote: String,
}

/// Arguments for [`GetTaxRegistrations_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTaxRegistrationsArgs {
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// Arguments for [`GetTaxRegistrationsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTaxRegistrationsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTaxRegistrationsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTaxRegistrationsIdArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostTestHelpersIssuingCardsCardShippingReturn_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersIssuingCardsCardShippingReturnArgs {
    /// Path parameter: card
    pub card: String,
}

/// Arguments for [`PostTestHelpersTreasuryInboundTransfersIdReturn_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryInboundTransfersIdReturnArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostTestHelpersTreasuryOutboundPaymentsIdReturn_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryOutboundPaymentsIdReturnArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturn_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturnArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

/// Arguments for [`GetTransfersIdReversals_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTransfersIdReversalsArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`PostTransfersIdReversals_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTransfersIdReversalsArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetTransfersTransferReversalsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTransfersTransferReversalsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: transfer
    pub transfer: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTransfersTransferReversalsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTransfersTransferReversalsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: transfer
    pub transfer: String,
}

/// Arguments for [`GetSetupIntents_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSetupIntentsArgs {
    /// Query parameter: attach_to_self
    pub attach_to_self: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: payment_method
    pub payment_method: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetSetupIntentsIntent_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSetupIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
    /// Query parameter: client_secret
    pub client_secret: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostSetupIntentsIntent_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostSetupIntentsIntentArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// Arguments for [`GetShippingRates_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetShippingRatesArgs {
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetShippingRatesShippingRateToken_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetShippingRatesShippingRateTokenArgs {
    /// Path parameter: shipping_rate_token
    pub shipping_rate_token: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostShippingRatesShippingRateToken_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostShippingRatesShippingRateTokenArgs {
    /// Path parameter: shipping_rate_token
    pub shipping_rate_token: String,
}

/// Arguments for [`GetSubscriptionSchedules_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSubscriptionSchedulesArgs {
    /// Query parameter: canceled_at
    pub canceled_at: Option<String>,
    /// Query parameter: completed_at
    pub completed_at: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: customer
    pub customer: Option<String>,
    /// Query parameter: customer_account
    pub customer_account: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: released_at
    pub released_at: Option<String>,
    /// Query parameter: scheduled
    pub scheduled: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetSubscriptionSchedulesSchedule_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSubscriptionSchedulesScheduleArgs {
    /// Path parameter: schedule
    pub schedule: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostSubscriptionSchedulesSchedule_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostSubscriptionSchedulesScheduleArgs {
    /// Path parameter: schedule
    pub schedule: String,
}

/// Arguments for [`GetTaxRates_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTaxRatesArgs {
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: inclusive
    pub inclusive: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetTaxRatesTaxRate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTaxRatesTaxRateArgs {
    /// Path parameter: tax_rate
    pub tax_rate: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostTaxRatesTaxRate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTaxRatesTaxRateArgs {
    /// Path parameter: tax_rate
    pub tax_rate: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/payouts
// -----------------------------------------------------------------------------

/// GET /v1/payouts - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payouts_builder<R>(client: &SimpleHttpClient<R>, args: &GetPayoutsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payouts",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payouts - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payouts_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/payouts - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payouts_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_payouts_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payouts - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payouts(
    client: &SimpleHttpClient,
    args: &GetPayoutsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_payouts_builder(client, args)?;
    get_payouts_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payouts
// -----------------------------------------------------------------------------

/// POST /v1/payouts - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payouts_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payouts",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payouts_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Payout>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Payout = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/payouts - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payouts_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_payouts_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/payouts/{payout}
// -----------------------------------------------------------------------------

/// GET /v1/payouts/{payout} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_payouts_payout_builder<R>(client: &SimpleHttpClient<R>, args: &GetPayoutsPayoutArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payouts/{payout}",
        args.payout,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/payouts/{payout} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_payouts_payout_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Payout>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Payout = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/payouts/{payout} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_payouts_payout_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_payouts_payout_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/payouts/{payout} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_payouts_payout(
    client: &SimpleHttpClient,
    args: &GetPayoutsPayoutArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_payouts_payout_builder(client, args)?;
    get_payouts_payout_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payouts/{payout}
// -----------------------------------------------------------------------------

/// POST /v1/payouts/{payout} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payouts_payout_builder<R>(client: &SimpleHttpClient<R>, args: &PostPayoutsPayoutArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payouts/{payout}",
        args.payout,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts/{payout} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payouts_payout_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Payout>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Payout = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/payouts/{payout} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payouts_payout_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_payouts_payout_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payouts/{payout} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payouts_payout(
    client: &SimpleHttpClient,
    args: &PostPayoutsPayoutArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_payouts_payout_builder(client, args)?;
    post_payouts_payout_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/personalization_designs
// -----------------------------------------------------------------------------

/// GET /v1/issuing/personalization_designs - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_personalization_designs_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingPersonalizationDesignsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/personalization_designs",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/personalization_designs - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_personalization_designs_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/personalization_designs - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_personalization_designs_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_personalization_designs_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/personalization_designs - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_personalization_designs(
    client: &SimpleHttpClient,
    args: &GetIssuingPersonalizationDesignsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_personalization_designs_builder(client, args)?;
    get_issuing_personalization_designs_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/personalization_designs
// -----------------------------------------------------------------------------

/// POST /v1/issuing/personalization_designs - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_personalization_designs_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/personalization_designs",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/personalization_designs - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_personalization_designs_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingPersonalizationDesign = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/issuing/personalization_designs - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_personalization_designs_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_issuing_personalization_designs_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/issuing/personalization_designs/{personalization_design}
// -----------------------------------------------------------------------------

/// GET /v1/issuing/personalization_designs/{personalization_design} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_issuing_personalization_designs_personalization_design_builder<R>(client: &SimpleHttpClient<R>, args: &GetIssuingPersonalizationDesignsPersonalizationDesignArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/personalization_designs/{personalization_design}",
        args.personalization_design,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/issuing/personalization_designs/{personalization_design} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_issuing_personalization_designs_personalization_design_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingPersonalizationDesign = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/issuing/personalization_designs/{personalization_design} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_issuing_personalization_designs_personalization_design_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_issuing_personalization_designs_personalization_design_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/issuing/personalization_designs/{personalization_design} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_issuing_personalization_designs_personalization_design(
    client: &SimpleHttpClient,
    args: &GetIssuingPersonalizationDesignsPersonalizationDesignArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_issuing_personalization_designs_personalization_design_builder(client, args)?;
    get_issuing_personalization_designs_personalization_design_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/issuing/personalization_designs/{personalization_design}
// -----------------------------------------------------------------------------

/// POST /v1/issuing/personalization_designs/{personalization_design} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_issuing_personalization_designs_personalization_design_builder<R>(client: &SimpleHttpClient<R>, args: &PostIssuingPersonalizationDesignsPersonalizationDesignArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/issuing/personalization_designs/{personalization_design}",
        args.personalization_design,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/issuing/personalization_designs/{personalization_design} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_issuing_personalization_designs_personalization_design_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingPersonalizationDesign = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/issuing/personalization_designs/{personalization_design} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_issuing_personalization_designs_personalization_design_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_issuing_personalization_designs_personalization_design_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/issuing/personalization_designs/{personalization_design} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_issuing_personalization_designs_personalization_design(
    client: &SimpleHttpClient,
    args: &PostIssuingPersonalizationDesignsPersonalizationDesignArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_issuing_personalization_designs_personalization_design_builder(client, args)?;
    post_issuing_personalization_designs_personalization_design_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/prices
// -----------------------------------------------------------------------------

/// GET /v1/prices - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_prices_builder<R>(client: &SimpleHttpClient<R>, args: &GetPricesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/prices",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/prices - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_prices_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/prices - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_prices_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_prices_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/prices - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_prices(
    client: &SimpleHttpClient,
    args: &GetPricesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_prices_builder(client, args)?;
    get_prices_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/prices
// -----------------------------------------------------------------------------

/// POST /v1/prices - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_prices_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/prices",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/prices - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_prices_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Price>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Price = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/prices - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_prices_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_prices_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/prices/{price}
// -----------------------------------------------------------------------------

/// GET /v1/prices/{price} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_prices_price_builder<R>(client: &SimpleHttpClient<R>, args: &GetPricesPriceArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/prices/{price}",
        args.price,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/prices/{price} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_prices_price_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Price>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Price = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/prices/{price} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_prices_price_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_prices_price_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/prices/{price} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_prices_price(
    client: &SimpleHttpClient,
    args: &GetPricesPriceArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_prices_price_builder(client, args)?;
    get_prices_price_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/prices/{price}
// -----------------------------------------------------------------------------

/// POST /v1/prices/{price} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_prices_price_builder<R>(client: &SimpleHttpClient<R>, args: &PostPricesPriceArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/prices/{price}",
        args.price,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/prices/{price} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_prices_price_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Price>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Price = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/prices/{price} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_prices_price_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_prices_price_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/prices/{price} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_prices_price(
    client: &SimpleHttpClient,
    args: &PostPricesPriceArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_prices_price_builder(client, args)?;
    post_prices_price_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/promotion_codes
// -----------------------------------------------------------------------------

/// GET /v1/promotion_codes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_promotion_codes_builder<R>(client: &SimpleHttpClient<R>, args: &GetPromotionCodesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/promotion_codes",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/promotion_codes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_promotion_codes_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/promotion_codes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_promotion_codes_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_promotion_codes_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/promotion_codes - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_promotion_codes(
    client: &SimpleHttpClient,
    args: &GetPromotionCodesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_promotion_codes_builder(client, args)?;
    get_promotion_codes_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/promotion_codes
// -----------------------------------------------------------------------------

/// POST /v1/promotion_codes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_promotion_codes_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/promotion_codes",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/promotion_codes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_promotion_codes_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<PromotionCode>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: PromotionCode = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/promotion_codes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_promotion_codes_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_promotion_codes_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/promotion_codes/{promotion_code}
// -----------------------------------------------------------------------------

/// GET /v1/promotion_codes/{promotion_code} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_promotion_codes_promotion_code_builder<R>(client: &SimpleHttpClient<R>, args: &GetPromotionCodesPromotionCodeArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/promotion_codes/{promotion_code}",
        args.promotion_code,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/promotion_codes/{promotion_code} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_promotion_codes_promotion_code_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<PromotionCode>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: PromotionCode = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/promotion_codes/{promotion_code} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_promotion_codes_promotion_code_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_promotion_codes_promotion_code_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/promotion_codes/{promotion_code} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_promotion_codes_promotion_code(
    client: &SimpleHttpClient,
    args: &GetPromotionCodesPromotionCodeArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_promotion_codes_promotion_code_builder(client, args)?;
    get_promotion_codes_promotion_code_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/promotion_codes/{promotion_code}
// -----------------------------------------------------------------------------

/// POST /v1/promotion_codes/{promotion_code} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_promotion_codes_promotion_code_builder<R>(client: &SimpleHttpClient<R>, args: &PostPromotionCodesPromotionCodeArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/promotion_codes/{promotion_code}",
        args.promotion_code,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/promotion_codes/{promotion_code} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_promotion_codes_promotion_code_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<PromotionCode>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: PromotionCode = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/promotion_codes/{promotion_code} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_promotion_codes_promotion_code_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_promotion_codes_promotion_code_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/promotion_codes/{promotion_code} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_promotion_codes_promotion_code(
    client: &SimpleHttpClient,
    args: &PostPromotionCodesPromotionCodeArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_promotion_codes_promotion_code_builder(client, args)?;
    post_promotion_codes_promotion_code_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/quotes
// -----------------------------------------------------------------------------

/// GET /v1/quotes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_quotes_builder<R>(client: &SimpleHttpClient<R>, args: &GetQuotesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/quotes",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_quotes_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/quotes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_quotes_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_quotes_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/quotes - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_quotes(
    client: &SimpleHttpClient,
    args: &GetQuotesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_quotes_builder(client, args)?;
    get_quotes_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/quotes
// -----------------------------------------------------------------------------

/// POST /v1/quotes - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_quotes_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/quotes",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_quotes_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Quote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Quote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/quotes - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_quotes_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_quotes_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/quotes/{quote}
// -----------------------------------------------------------------------------

/// GET /v1/quotes/{quote} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_quotes_quote_builder<R>(client: &SimpleHttpClient<R>, args: &GetQuotesQuoteArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/quotes/{quote}",
        args.quote,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/quotes/{quote} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_quotes_quote_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Quote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Quote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/quotes/{quote} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_quotes_quote_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_quotes_quote_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/quotes/{quote} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_quotes_quote(
    client: &SimpleHttpClient,
    args: &GetQuotesQuoteArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_quotes_quote_builder(client, args)?;
    get_quotes_quote_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/quotes/{quote}
// -----------------------------------------------------------------------------

/// POST /v1/quotes/{quote} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_quotes_quote_builder<R>(client: &SimpleHttpClient<R>, args: &PostQuotesQuoteArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/quotes/{quote}",
        args.quote,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes/{quote} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_quotes_quote_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<Quote>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: Quote = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/quotes/{quote} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_quotes_quote_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_quotes_quote_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/quotes/{quote} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_quotes_quote(
    client: &SimpleHttpClient,
    args: &PostQuotesQuoteArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_quotes_quote_builder(client, args)?;
    post_quotes_quote_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/tax/registrations
// -----------------------------------------------------------------------------

/// GET /v1/tax/registrations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tax_registrations_builder<R>(client: &SimpleHttpClient<R>, args: &GetTaxRegistrationsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax/registrations",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/registrations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tax_registrations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/tax/registrations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tax_registrations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_tax_registrations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tax/registrations - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tax_registrations(
    client: &SimpleHttpClient,
    args: &GetTaxRegistrationsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_tax_registrations_builder(client, args)?;
    get_tax_registrations_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/tax/registrations
// -----------------------------------------------------------------------------

/// POST /v1/tax/registrations - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_tax_registrations_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax/registrations",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/registrations - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_tax_registrations_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TaxRegistration>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TaxRegistration = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/tax/registrations - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_tax_registrations_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_tax_registrations_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/tax/registrations/{id}
// -----------------------------------------------------------------------------

/// GET /v1/tax/registrations/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tax_registrations_id_builder<R>(client: &SimpleHttpClient<R>, args: &GetTaxRegistrationsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax/registrations/{id}",
        args.id,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax/registrations/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tax_registrations_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TaxRegistration>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TaxRegistration = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/tax/registrations/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tax_registrations_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_tax_registrations_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tax/registrations/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tax_registrations_id(
    client: &SimpleHttpClient,
    args: &GetTaxRegistrationsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_tax_registrations_id_builder(client, args)?;
    get_tax_registrations_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/tax/registrations/{id}
// -----------------------------------------------------------------------------

/// POST /v1/tax/registrations/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_tax_registrations_id_builder<R>(client: &SimpleHttpClient<R>, args: &PostTaxRegistrationsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax/registrations/{id}",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax/registrations/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_tax_registrations_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TaxRegistration>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TaxRegistration = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/tax/registrations/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_tax_registrations_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_tax_registrations_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax/registrations/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_tax_registrations_id(
    client: &SimpleHttpClient,
    args: &PostTaxRegistrationsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_tax_registrations_id_builder(client, args)?;
    post_tax_registrations_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/issuing/cards/{card}/shipping/return
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_issuing_cards_card_shipping_return_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersIssuingCardsCardShippingReturnArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/issuing/cards/{card}/shipping/return",
        args.card,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_issuing_cards_card_shipping_return_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<IssuingCard>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: IssuingCard = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_issuing_cards_card_shipping_return_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCard>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_issuing_cards_card_shipping_return_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/issuing/cards/{card}/shipping/return - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_issuing_cards_card_shipping_return(
    client: &SimpleHttpClient,
    args: &PostTestHelpersIssuingCardsCardShippingReturnArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCard>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_issuing_cards_card_shipping_return_builder(client, args)?;
    post_test_helpers_issuing_cards_card_shipping_return_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_inbound_transfers_id_return_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersTreasuryInboundTransfersIdReturnArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/inbound_transfers/{id}/return",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_inbound_transfers_id_return_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TreasuryInboundTransfer = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_inbound_transfers_id_return_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_treasury_inbound_transfers_id_return_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_inbound_transfers_id_return(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryInboundTransfersIdReturnArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_treasury_inbound_transfers_id_return_builder(client, args)?;
    post_test_helpers_treasury_inbound_transfers_id_return_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/outbound_payments/{id}/return
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_outbound_payments_id_return_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersTreasuryOutboundPaymentsIdReturnArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_payments/{id}/return",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_outbound_payments_id_return_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TreasuryOutboundPayment = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_outbound_payments_id_return_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_treasury_outbound_payments_id_return_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/outbound_payments/{id}/return - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_outbound_payments_id_return(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryOutboundPaymentsIdReturnArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_treasury_outbound_payments_id_return_builder(client, args)?;
    post_test_helpers_treasury_outbound_payments_id_return_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return
// -----------------------------------------------------------------------------

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder<R>(client: &SimpleHttpClient<R>, args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturnArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return",
        args.outbound_transfer,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TreasuryOutboundTransfer = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_test_helpers_treasury_outbound_transfers_outbound_transfer_return(
    client: &SimpleHttpClient,
    args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturnArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder(client, args)?;
    post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/transfers/{id}/reversals
// -----------------------------------------------------------------------------

/// GET /v1/transfers/{id}/reversals - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_transfers_id_reversals_builder<R>(client: &SimpleHttpClient<R>, args: &GetTransfersIdReversalsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/transfers/{id}/reversals",
        args.id,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers/{id}/reversals - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_transfers_id_reversals_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/transfers/{id}/reversals - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_transfers_id_reversals_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_transfers_id_reversals_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/transfers/{id}/reversals - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_transfers_id_reversals(
    client: &SimpleHttpClient,
    args: &GetTransfersIdReversalsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_transfers_id_reversals_builder(client, args)?;
    get_transfers_id_reversals_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/transfers/{id}/reversals
// -----------------------------------------------------------------------------

/// POST /v1/transfers/{id}/reversals - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_transfers_id_reversals_builder<R>(client: &SimpleHttpClient<R>, args: &PostTransfersIdReversalsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/transfers/{id}/reversals",
        args.id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers/{id}/reversals - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_transfers_id_reversals_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TransferReversal>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TransferReversal = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/transfers/{id}/reversals - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_transfers_id_reversals_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_transfers_id_reversals_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/transfers/{id}/reversals - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_transfers_id_reversals(
    client: &SimpleHttpClient,
    args: &PostTransfersIdReversalsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_transfers_id_reversals_builder(client, args)?;
    post_transfers_id_reversals_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/transfers/{transfer}/reversals/{id}
// -----------------------------------------------------------------------------

/// GET /v1/transfers/{transfer}/reversals/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_transfers_transfer_reversals_id_builder<R>(client: &SimpleHttpClient<R>, args: &GetTransfersTransferReversalsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/transfers/{transfer}/reversals/{id}",
        args.id,
        args.transfer,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/transfers/{transfer}/reversals/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_transfers_transfer_reversals_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TransferReversal>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TransferReversal = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/transfers/{transfer}/reversals/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_transfers_transfer_reversals_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_transfers_transfer_reversals_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/transfers/{transfer}/reversals/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_transfers_transfer_reversals_id(
    client: &SimpleHttpClient,
    args: &GetTransfersTransferReversalsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_transfers_transfer_reversals_id_builder(client, args)?;
    get_transfers_transfer_reversals_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/transfers/{transfer}/reversals/{id}
// -----------------------------------------------------------------------------

/// POST /v1/transfers/{transfer}/reversals/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_transfers_transfer_reversals_id_builder<R>(client: &SimpleHttpClient<R>, args: &PostTransfersTransferReversalsIdArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/transfers/{transfer}/reversals/{id}",
        args.id,
        args.transfer,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/transfers/{transfer}/reversals/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_transfers_transfer_reversals_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TransferReversal>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TransferReversal = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/transfers/{transfer}/reversals/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_transfers_transfer_reversals_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_transfers_transfer_reversals_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/transfers/{transfer}/reversals/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_transfers_transfer_reversals_id(
    client: &SimpleHttpClient,
    args: &PostTransfersTransferReversalsIdArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_transfers_transfer_reversals_id_builder(client, args)?;
    post_transfers_transfer_reversals_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/setup_intents
// -----------------------------------------------------------------------------

/// GET /v1/setup_intents - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_setup_intents_builder<R>(client: &SimpleHttpClient<R>, args: &GetSetupIntentsArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/setup_intents",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/setup_intents - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_setup_intents_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/setup_intents - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_setup_intents_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_setup_intents_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/setup_intents - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_setup_intents(
    client: &SimpleHttpClient,
    args: &GetSetupIntentsArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_setup_intents_builder(client, args)?;
    get_setup_intents_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/setup_intents
// -----------------------------------------------------------------------------

/// POST /v1/setup_intents - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_setup_intents_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/setup_intents",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_setup_intents_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<SetupIntent>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: SetupIntent = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/setup_intents - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_setup_intents_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_setup_intents_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/setup_intents/{intent}
// -----------------------------------------------------------------------------

/// GET /v1/setup_intents/{intent} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_setup_intents_intent_builder<R>(client: &SimpleHttpClient<R>, args: &GetSetupIntentsIntentArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/setup_intents/{intent}",
        args.intent,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/setup_intents/{intent} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_setup_intents_intent_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<SetupIntent>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: SetupIntent = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/setup_intents/{intent} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_setup_intents_intent_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_setup_intents_intent_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/setup_intents/{intent} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_setup_intents_intent(
    client: &SimpleHttpClient,
    args: &GetSetupIntentsIntentArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_setup_intents_intent_builder(client, args)?;
    get_setup_intents_intent_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/setup_intents/{intent}
// -----------------------------------------------------------------------------

/// POST /v1/setup_intents/{intent} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_setup_intents_intent_builder<R>(client: &SimpleHttpClient<R>, args: &PostSetupIntentsIntentArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/setup_intents/{intent}",
        args.intent,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents/{intent} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_setup_intents_intent_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<SetupIntent>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: SetupIntent = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/setup_intents/{intent} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_setup_intents_intent_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_setup_intents_intent_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/setup_intents/{intent} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_setup_intents_intent(
    client: &SimpleHttpClient,
    args: &PostSetupIntentsIntentArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_setup_intents_intent_builder(client, args)?;
    post_setup_intents_intent_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/shipping_rates
// -----------------------------------------------------------------------------

/// GET /v1/shipping_rates - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_shipping_rates_builder<R>(client: &SimpleHttpClient<R>, args: &GetShippingRatesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/shipping_rates",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/shipping_rates - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_shipping_rates_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/shipping_rates - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_shipping_rates_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_shipping_rates_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/shipping_rates - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_shipping_rates(
    client: &SimpleHttpClient,
    args: &GetShippingRatesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_shipping_rates_builder(client, args)?;
    get_shipping_rates_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/shipping_rates
// -----------------------------------------------------------------------------

/// POST /v1/shipping_rates - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_shipping_rates_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/shipping_rates",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/shipping_rates - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_shipping_rates_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ShippingRate>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ShippingRate = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/shipping_rates - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_shipping_rates_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_shipping_rates_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/shipping_rates/{shipping_rate_token}
// -----------------------------------------------------------------------------

/// GET /v1/shipping_rates/{shipping_rate_token} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_shipping_rates_shipping_rate_token_builder<R>(client: &SimpleHttpClient<R>, args: &GetShippingRatesShippingRateTokenArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/shipping_rates/{shipping_rate_token}",
        args.shipping_rate_token,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/shipping_rates/{shipping_rate_token} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_shipping_rates_shipping_rate_token_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ShippingRate>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ShippingRate = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/shipping_rates/{shipping_rate_token} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_shipping_rates_shipping_rate_token_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_shipping_rates_shipping_rate_token_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/shipping_rates/{shipping_rate_token} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_shipping_rates_shipping_rate_token(
    client: &SimpleHttpClient,
    args: &GetShippingRatesShippingRateTokenArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_shipping_rates_shipping_rate_token_builder(client, args)?;
    get_shipping_rates_shipping_rate_token_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/shipping_rates/{shipping_rate_token}
// -----------------------------------------------------------------------------

/// POST /v1/shipping_rates/{shipping_rate_token} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_shipping_rates_shipping_rate_token_builder<R>(client: &SimpleHttpClient<R>, args: &PostShippingRatesShippingRateTokenArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/shipping_rates/{shipping_rate_token}",
        args.shipping_rate_token,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/shipping_rates/{shipping_rate_token} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_shipping_rates_shipping_rate_token_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<ShippingRate>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: ShippingRate = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/shipping_rates/{shipping_rate_token} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_shipping_rates_shipping_rate_token_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_shipping_rates_shipping_rate_token_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/shipping_rates/{shipping_rate_token} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_shipping_rates_shipping_rate_token(
    client: &SimpleHttpClient,
    args: &PostShippingRatesShippingRateTokenArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_shipping_rates_shipping_rate_token_builder(client, args)?;
    post_shipping_rates_shipping_rate_token_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/subscription_schedules
// -----------------------------------------------------------------------------

/// GET /v1/subscription_schedules - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_subscription_schedules_builder<R>(client: &SimpleHttpClient<R>, args: &GetSubscriptionSchedulesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_schedules",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_schedules - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_subscription_schedules_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/subscription_schedules - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_subscription_schedules_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_subscription_schedules_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/subscription_schedules - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_subscription_schedules(
    client: &SimpleHttpClient,
    args: &GetSubscriptionSchedulesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_subscription_schedules_builder(client, args)?;
    get_subscription_schedules_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/subscription_schedules
// -----------------------------------------------------------------------------

/// POST /v1/subscription_schedules - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_subscription_schedules_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_schedules",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_subscription_schedules_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: SubscriptionSchedule = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/subscription_schedules - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_subscription_schedules_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_subscription_schedules_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/subscription_schedules/{schedule}
// -----------------------------------------------------------------------------

/// GET /v1/subscription_schedules/{schedule} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_subscription_schedules_schedule_builder<R>(client: &SimpleHttpClient<R>, args: &GetSubscriptionSchedulesScheduleArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{schedule}",
        args.schedule,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/subscription_schedules/{schedule} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_subscription_schedules_schedule_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: SubscriptionSchedule = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/subscription_schedules/{schedule} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_subscription_schedules_schedule_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_subscription_schedules_schedule_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/subscription_schedules/{schedule} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_subscription_schedules_schedule(
    client: &SimpleHttpClient,
    args: &GetSubscriptionSchedulesScheduleArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_subscription_schedules_schedule_builder(client, args)?;
    get_subscription_schedules_schedule_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/subscription_schedules/{schedule}
// -----------------------------------------------------------------------------

/// POST /v1/subscription_schedules/{schedule} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_subscription_schedules_schedule_builder<R>(client: &SimpleHttpClient<R>, args: &PostSubscriptionSchedulesScheduleArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{schedule}",
        args.schedule,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules/{schedule} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_subscription_schedules_schedule_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: SubscriptionSchedule = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/subscription_schedules/{schedule} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_subscription_schedules_schedule_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_subscription_schedules_schedule_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/subscription_schedules/{schedule} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_subscription_schedules_schedule(
    client: &SimpleHttpClient,
    args: &PostSubscriptionSchedulesScheduleArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_subscription_schedules_schedule_builder(client, args)?;
    post_subscription_schedules_schedule_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/tax_rates
// -----------------------------------------------------------------------------

/// GET /v1/tax_rates - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tax_rates_builder<R>(client: &SimpleHttpClient<R>, args: &GetTaxRatesArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax_rates",
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_rates - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tax_rates_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/tax_rates - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tax_rates_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_tax_rates_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tax_rates - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tax_rates(
    client: &SimpleHttpClient,
    args: &GetTaxRatesArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_tax_rates_builder(client, args)?;
    get_tax_rates_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/tax_rates
// -----------------------------------------------------------------------------

/// POST /v1/tax_rates - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_tax_rates_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax_rates",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax_rates - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_tax_rates_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TaxRate>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TaxRate = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/tax_rates - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_tax_rates_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_tax_rates_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/tax_rates/{tax_rate}
// -----------------------------------------------------------------------------

/// GET /v1/tax_rates/{tax_rate} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_tax_rates_tax_rate_builder<R>(client: &SimpleHttpClient<R>, args: &GetTaxRatesTaxRateArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax_rates/{tax_rate}",
        args.tax_rate,
    );

    let builder = client.get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/tax_rates/{tax_rate} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_tax_rates_tax_rate_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TaxRate>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TaxRate = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// GET /v1/tax_rates/{tax_rate} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_tax_rates_tax_rate_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = get_tax_rates_tax_rate_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/tax_rates/{tax_rate} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_tax_rates_tax_rate(
    client: &SimpleHttpClient,
    args: &GetTaxRatesTaxRateArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = get_tax_rates_tax_rate_builder(client, args)?;
    get_tax_rates_tax_rate_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/tax_rates/{tax_rate}
// -----------------------------------------------------------------------------

/// POST /v1/tax_rates/{tax_rate} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_tax_rates_tax_rate_builder<R>(client: &SimpleHttpClient<R>, args: &PostTaxRatesTaxRateArgs) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/tax_rates/{tax_rate}",
        args.tax_rate,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/tax_rates/{tax_rate} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_tax_rates_tax_rate_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<TaxRate>, crate::ApiError>, Pending = crate::ApiPending, Spawner = crate::BoxedSendExecutionAction> + Send + 'static, crate::ApiError> {
    Ok(
        builder
            .build_send_request()
            .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                crate::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(crate::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                    let parsed: TaxRate = serde_json::from_str(&body).map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                crate::RequestIntro::Failed(e) => Err(crate::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| crate::ApiPending::Sending)
    )
}

/// POST /v1/tax_rates/{tax_rate} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_tax_rates_tax_rate_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let task = post_tax_rates_tax_rate_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/tax_rates/{tax_rate} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_tax_rates_tax_rate(
    client: &SimpleHttpClient,
    args: &PostTaxRatesTaxRateArgs,
) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
    let builder = post_tax_rates_tax_rate_builder(client, args)?;
    post_tax_rates_tax_rate_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// GET /v1/payouts.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_payouts(&self, 
        args: &GetPayoutsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_payouts_builder(&self.http_client
            , args
        )?;
        get_payouts_execute(builder)
    }

    /// POST /v1/payouts.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_payouts(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_payouts_builder(&self.http_client
        )?;
        post_payouts_execute(builder)
    }

    /// GET /v1/payouts/{payout}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_payouts_payout(&self, 
        args: &GetPayoutsPayoutArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_payouts_payout_builder(&self.http_client
            , args
        )?;
        get_payouts_payout_execute(builder)
    }

    /// POST /v1/payouts/{payout}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_payouts_payout(&self, 
        args: &PostPayoutsPayoutArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_payouts_payout_builder(&self.http_client
            , args
        )?;
        post_payouts_payout_execute(builder)
    }

    /// GET /v1/issuing/personalization_designs.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_issuing_personalization_designs(&self, 
        args: &GetIssuingPersonalizationDesignsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_personalization_designs_builder(&self.http_client
            , args
        )?;
        get_issuing_personalization_designs_execute(builder)
    }

    /// POST /v1/issuing/personalization_designs.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_issuing_personalization_designs(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_issuing_personalization_designs_builder(&self.http_client
        )?;
        post_issuing_personalization_designs_execute(builder)
    }

    /// GET /v1/issuing/personalization_designs/{personalization_design}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_issuing_personalization_designs_personalization_design(&self, 
        args: &GetIssuingPersonalizationDesignsPersonalizationDesignArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_issuing_personalization_designs_personalization_design_builder(&self.http_client
            , args
        )?;
        get_issuing_personalization_designs_personalization_design_execute(builder)
    }

    /// POST /v1/issuing/personalization_designs/{personalization_design}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_issuing_personalization_designs_personalization_design(&self, 
        args: &PostIssuingPersonalizationDesignsPersonalizationDesignArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingPersonalizationDesign>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_issuing_personalization_designs_personalization_design_builder(&self.http_client
            , args
        )?;
        post_issuing_personalization_designs_personalization_design_execute(builder)
    }

    /// GET /v1/prices.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_prices(&self, 
        args: &GetPricesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_prices_builder(&self.http_client
            , args
        )?;
        get_prices_execute(builder)
    }

    /// POST /v1/prices.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_prices(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_prices_builder(&self.http_client
        )?;
        post_prices_execute(builder)
    }

    /// GET /v1/prices/{price}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_prices_price(&self, 
        args: &GetPricesPriceArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_prices_price_builder(&self.http_client
            , args
        )?;
        get_prices_price_execute(builder)
    }

    /// POST /v1/prices/{price}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_prices_price(&self, 
        args: &PostPricesPriceArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Price>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_prices_price_builder(&self.http_client
            , args
        )?;
        post_prices_price_execute(builder)
    }

    /// GET /v1/promotion_codes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_promotion_codes(&self, 
        args: &GetPromotionCodesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_promotion_codes_builder(&self.http_client
            , args
        )?;
        get_promotion_codes_execute(builder)
    }

    /// POST /v1/promotion_codes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_promotion_codes(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_promotion_codes_builder(&self.http_client
        )?;
        post_promotion_codes_execute(builder)
    }

    /// GET /v1/promotion_codes/{promotion_code}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_promotion_codes_promotion_code(&self, 
        args: &GetPromotionCodesPromotionCodeArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_promotion_codes_promotion_code_builder(&self.http_client
            , args
        )?;
        get_promotion_codes_promotion_code_execute(builder)
    }

    /// POST /v1/promotion_codes/{promotion_code}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_promotion_codes_promotion_code(&self, 
        args: &PostPromotionCodesPromotionCodeArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<PromotionCode>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_promotion_codes_promotion_code_builder(&self.http_client
            , args
        )?;
        post_promotion_codes_promotion_code_execute(builder)
    }

    /// GET /v1/quotes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_quotes(&self, 
        args: &GetQuotesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_quotes_builder(&self.http_client
            , args
        )?;
        get_quotes_execute(builder)
    }

    /// POST /v1/quotes.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_quotes(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_quotes_builder(&self.http_client
        )?;
        post_quotes_execute(builder)
    }

    /// GET /v1/quotes/{quote}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_quotes_quote(&self, 
        args: &GetQuotesQuoteArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_quotes_quote_builder(&self.http_client
            , args
        )?;
        get_quotes_quote_execute(builder)
    }

    /// POST /v1/quotes/{quote}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_quotes_quote(&self, 
        args: &PostQuotesQuoteArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_quotes_quote_builder(&self.http_client
            , args
        )?;
        post_quotes_quote_execute(builder)
    }

    /// GET /v1/tax/registrations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_tax_registrations(&self, 
        args: &GetTaxRegistrationsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_tax_registrations_builder(&self.http_client
            , args
        )?;
        get_tax_registrations_execute(builder)
    }

    /// POST /v1/tax/registrations.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_tax_registrations(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_tax_registrations_builder(&self.http_client
        )?;
        post_tax_registrations_execute(builder)
    }

    /// GET /v1/tax/registrations/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_tax_registrations_id(&self, 
        args: &GetTaxRegistrationsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_tax_registrations_id_builder(&self.http_client
            , args
        )?;
        get_tax_registrations_id_execute(builder)
    }

    /// POST /v1/tax/registrations/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_tax_registrations_id(&self, 
        args: &PostTaxRegistrationsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRegistration>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_tax_registrations_id_builder(&self.http_client
            , args
        )?;
        post_tax_registrations_id_execute(builder)
    }

    /// POST /v1/test_helpers/issuing/cards/{card}/shipping/return.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_test_helpers_issuing_cards_card_shipping_return(&self, 
        args: &PostTestHelpersIssuingCardsCardShippingReturnArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<IssuingCard>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_issuing_cards_card_shipping_return_builder(&self.http_client
            , args
        )?;
        post_test_helpers_issuing_cards_card_shipping_return_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/inbound_transfers/{id}/return.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_test_helpers_treasury_inbound_transfers_id_return(&self, 
        args: &PostTestHelpersTreasuryInboundTransfersIdReturnArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_treasury_inbound_transfers_id_return_builder(&self.http_client
            , args
        )?;
        post_test_helpers_treasury_inbound_transfers_id_return_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/outbound_payments/{id}/return.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_test_helpers_treasury_outbound_payments_id_return(&self, 
        args: &PostTestHelpersTreasuryOutboundPaymentsIdReturnArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_treasury_outbound_payments_id_return_builder(&self.http_client
            , args
        )?;
        post_test_helpers_treasury_outbound_payments_id_return_execute(builder)
    }

    /// POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_test_helpers_treasury_outbound_transfers_outbound_transfer_return(&self, 
        args: &PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturnArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_builder(&self.http_client
            , args
        )?;
        post_test_helpers_treasury_outbound_transfers_outbound_transfer_return_execute(builder)
    }

    /// GET /v1/transfers/{id}/reversals.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_transfers_id_reversals(&self, 
        args: &GetTransfersIdReversalsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_transfers_id_reversals_builder(&self.http_client
            , args
        )?;
        get_transfers_id_reversals_execute(builder)
    }

    /// POST /v1/transfers/{id}/reversals.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_transfers_id_reversals(&self, 
        args: &PostTransfersIdReversalsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_transfers_id_reversals_builder(&self.http_client
            , args
        )?;
        post_transfers_id_reversals_execute(builder)
    }

    /// GET /v1/transfers/{transfer}/reversals/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_transfers_transfer_reversals_id(&self, 
        args: &GetTransfersTransferReversalsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_transfers_transfer_reversals_id_builder(&self.http_client
            , args
        )?;
        get_transfers_transfer_reversals_id_execute(builder)
    }

    /// POST /v1/transfers/{transfer}/reversals/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_transfers_transfer_reversals_id(&self, 
        args: &PostTransfersTransferReversalsIdArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TransferReversal>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_transfers_transfer_reversals_id_builder(&self.http_client
            , args
        )?;
        post_transfers_transfer_reversals_id_execute(builder)
    }

    /// GET /v1/setup_intents.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_setup_intents(&self, 
        args: &GetSetupIntentsArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_setup_intents_builder(&self.http_client
            , args
        )?;
        get_setup_intents_execute(builder)
    }

    /// POST /v1/setup_intents.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_setup_intents(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_setup_intents_builder(&self.http_client
        )?;
        post_setup_intents_execute(builder)
    }

    /// GET /v1/setup_intents/{intent}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_setup_intents_intent(&self, 
        args: &GetSetupIntentsIntentArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_setup_intents_intent_builder(&self.http_client
            , args
        )?;
        get_setup_intents_intent_execute(builder)
    }

    /// POST /v1/setup_intents/{intent}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_setup_intents_intent(&self, 
        args: &PostSetupIntentsIntentArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_setup_intents_intent_builder(&self.http_client
            , args
        )?;
        post_setup_intents_intent_execute(builder)
    }

    /// GET /v1/shipping_rates.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_shipping_rates(&self, 
        args: &GetShippingRatesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_shipping_rates_builder(&self.http_client
            , args
        )?;
        get_shipping_rates_execute(builder)
    }

    /// POST /v1/shipping_rates.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_shipping_rates(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_shipping_rates_builder(&self.http_client
        )?;
        post_shipping_rates_execute(builder)
    }

    /// GET /v1/shipping_rates/{shipping_rate_token}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_shipping_rates_shipping_rate_token(&self, 
        args: &GetShippingRatesShippingRateTokenArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_shipping_rates_shipping_rate_token_builder(&self.http_client
            , args
        )?;
        get_shipping_rates_shipping_rate_token_execute(builder)
    }

    /// POST /v1/shipping_rates/{shipping_rate_token}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_shipping_rates_shipping_rate_token(&self, 
        args: &PostShippingRatesShippingRateTokenArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<ShippingRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_shipping_rates_shipping_rate_token_builder(&self.http_client
            , args
        )?;
        post_shipping_rates_shipping_rate_token_execute(builder)
    }

    /// GET /v1/subscription_schedules.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_subscription_schedules(&self, 
        args: &GetSubscriptionSchedulesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_subscription_schedules_builder(&self.http_client
            , args
        )?;
        get_subscription_schedules_execute(builder)
    }

    /// POST /v1/subscription_schedules.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_subscription_schedules(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_subscription_schedules_builder(&self.http_client
        )?;
        post_subscription_schedules_execute(builder)
    }

    /// GET /v1/subscription_schedules/{schedule}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_subscription_schedules_schedule(&self, 
        args: &GetSubscriptionSchedulesScheduleArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_subscription_schedules_schedule_builder(&self.http_client
            , args
        )?;
        get_subscription_schedules_schedule_execute(builder)
    }

    /// POST /v1/subscription_schedules/{schedule}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_subscription_schedules_schedule(&self, 
        args: &PostSubscriptionSchedulesScheduleArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_subscription_schedules_schedule_builder(&self.http_client
            , args
        )?;
        post_subscription_schedules_schedule_execute(builder)
    }

    /// GET /v1/tax_rates.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_tax_rates(&self, 
        args: &GetTaxRatesArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_tax_rates_builder(&self.http_client
            , args
        )?;
        get_tax_rates_execute(builder)
    }

    /// POST /v1/tax_rates.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_tax_rates(&self, 
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_tax_rates_builder(&self.http_client
        )?;
        post_tax_rates_execute(builder)
    }

    /// GET /v1/tax_rates/{tax_rate}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_get_tax_rates_tax_rate(&self, 
        args: &GetTaxRatesTaxRateArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = get_tax_rates_tax_rate_builder(&self.http_client
            , args
        )?;
        get_tax_rates_tax_rate_execute(builder)
    }

    /// POST /v1/tax_rates/{tax_rate}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_payouts_personalization_designs_prices_promotion_codes_quotes_registrations_return_reversals_setup_intents_shipping_rates_subscription_schedules_tax_rates_post_tax_rates_tax_rate(&self, 
        args: &PostTaxRatesTaxRateArgs,
    ) -> Result<impl StreamIterator<D = Result<ApiResponse<TaxRate>, crate::ApiError>, P = crate::ApiPending> + Send + 'static, crate::ApiError> {
        let builder = post_tax_rates_tax_rate_builder(&self.http_client
            , args
        )?;
        post_tax_rates_tax_rate_execute(builder)
    }

}

