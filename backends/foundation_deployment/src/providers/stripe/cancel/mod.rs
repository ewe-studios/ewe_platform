//! Auto-generated API module for stripe cancel.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_cancel `

#![cfg(feature = "stripe_cancel")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// ClimateOrder response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClimateOrder {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// IdentityVerificationSession response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IdentityVerificationSession {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// PaymentIntent response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentIntent {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Payout response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Payout {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Quote response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Quote {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Refund response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Refund {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SetupIntent response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetupIntent {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// SubscriptionSchedule response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionSchedule {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Topup response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Topup {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryInboundTransfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryInboundTransfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryOutboundPayment response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryOutboundPayment {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TreasuryOutboundTransfer response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TreasuryOutboundTransfer {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`PostClimateOrdersOrderCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostClimateOrdersOrderCancelArgs {
    /// Path parameter: order
    pub order: String,
}

/// Arguments for [`PostIdentityVerificationSessionsSessionCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostIdentityVerificationSessionsSessionCancelArgs {
    /// Path parameter: session
    pub session: String,
}

/// Arguments for [`PostPaymentIntentsIntentCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPaymentIntentsIntentCancelArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// Arguments for [`PostPayoutsPayoutCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostPayoutsPayoutCancelArgs {
    /// Path parameter: payout
    pub payout: String,
}

/// Arguments for [`PostQuotesQuoteCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostQuotesQuoteCancelArgs {
    /// Path parameter: quote
    pub quote: String,
}

/// Arguments for [`PostRefundsRefundCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostRefundsRefundCancelArgs {
    /// Path parameter: refund
    pub refund: String,
}

/// Arguments for [`PostSetupIntentsIntentCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostSetupIntentsIntentCancelArgs {
    /// Path parameter: intent
    pub intent: String,
}

/// Arguments for [`PostSubscriptionSchedulesScheduleCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostSubscriptionSchedulesScheduleCancelArgs {
    /// Path parameter: schedule
    pub schedule: String,
}

/// Arguments for [`PostTopupsTopupCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTopupsTopupCancelArgs {
    /// Path parameter: topup
    pub topup: String,
}

/// Arguments for [`PostTreasuryInboundTransfersInboundTransferCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTreasuryInboundTransfersInboundTransferCancelArgs {
    /// Path parameter: inbound_transfer
    pub inbound_transfer: String,
}

/// Arguments for [`PostTreasuryOutboundPaymentsIdCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTreasuryOutboundPaymentsIdCancelArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`PostTreasuryOutboundTransfersOutboundTransferCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostTreasuryOutboundTransfersOutboundTransferCancelArgs {
    /// Path parameter: outbound_transfer
    pub outbound_transfer: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST /v1/climate/orders/{order}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/climate/orders/{order}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_climate_orders_order_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostClimateOrdersOrderCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/climate/orders/{order}/cancel",
        args.order,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/climate/orders/{order}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_climate_orders_order_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: ClimateOrder = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/climate/orders/{order}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_climate_orders_order_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_climate_orders_order_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/climate/orders/{order}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_climate_orders_order_cancel(
    client: &SimpleHttpClient,
    args: &PostClimateOrdersOrderCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_climate_orders_order_cancel_builder(client, args)?;
    post_climate_orders_order_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/identity/verification_sessions/{session}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/identity/verification_sessions/{session}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_identity_verification_sessions_session_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostIdentityVerificationSessionsSessionCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/identity/verification_sessions/{session}/cancel",
        args.session,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/identity/verification_sessions/{session}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_identity_verification_sessions_session_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: IdentityVerificationSession = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/identity/verification_sessions/{session}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_identity_verification_sessions_session_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_identity_verification_sessions_session_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/identity/verification_sessions/{session}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_identity_verification_sessions_session_cancel(
    client: &SimpleHttpClient,
    args: &PostIdentityVerificationSessionsSessionCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_identity_verification_sessions_session_cancel_builder(client, args)?;
    post_identity_verification_sessions_session_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payment_intents/{intent}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/payment_intents/{intent}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payment_intents_intent_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPaymentIntentsIntentCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payment_intents/{intent}/cancel",
        args.intent,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payment_intents/{intent}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payment_intents_intent_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: PaymentIntent = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payment_intents/{intent}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payment_intents_intent_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payment_intents_intent_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payment_intents/{intent}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payment_intents_intent_cancel(
    client: &SimpleHttpClient,
    args: &PostPaymentIntentsIntentCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_payment_intents_intent_cancel_builder(client, args)?;
    post_payment_intents_intent_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/payouts/{payout}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/payouts/{payout}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_payouts_payout_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostPayoutsPayoutCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/payouts/{payout}/cancel",
        args.payout,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/payouts/{payout}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_payouts_payout_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Payout>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Payout = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/payouts/{payout}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_payouts_payout_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_payouts_payout_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/payouts/{payout}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_payouts_payout_cancel(
    client: &SimpleHttpClient,
    args: &PostPayoutsPayoutCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_payouts_payout_cancel_builder(client, args)?;
    post_payouts_payout_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/quotes/{quote}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/quotes/{quote}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_quotes_quote_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostQuotesQuoteCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/quotes/{quote}/cancel",
        args.quote,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/quotes/{quote}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_quotes_quote_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Quote>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Quote = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/quotes/{quote}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_quotes_quote_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_quotes_quote_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/quotes/{quote}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_quotes_quote_cancel(
    client: &SimpleHttpClient,
    args: &PostQuotesQuoteCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_quotes_quote_cancel_builder(client, args)?;
    post_quotes_quote_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/refunds/{refund}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/refunds/{refund}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_refunds_refund_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostRefundsRefundCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/refunds/{refund}/cancel",
        args.refund,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/refunds/{refund}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_refunds_refund_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/refunds/{refund}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_refunds_refund_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_refunds_refund_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/refunds/{refund}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_refunds_refund_cancel(
    client: &SimpleHttpClient,
    args: &PostRefundsRefundCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_refunds_refund_cancel_builder(client, args)?;
    post_refunds_refund_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/setup_intents/{intent}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/setup_intents/{intent}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_setup_intents_intent_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostSetupIntentsIntentCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/setup_intents/{intent}/cancel",
        args.intent,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/setup_intents/{intent}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_setup_intents_intent_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetupIntent>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SetupIntent = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/setup_intents/{intent}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_setup_intents_intent_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_setup_intents_intent_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/setup_intents/{intent}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_setup_intents_intent_cancel(
    client: &SimpleHttpClient,
    args: &PostSetupIntentsIntentCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SetupIntent>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_setup_intents_intent_cancel_builder(client, args)?;
    post_setup_intents_intent_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/subscription_schedules/{schedule}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/subscription_schedules/{schedule}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_subscription_schedules_schedule_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostSubscriptionSchedulesScheduleCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/subscription_schedules/{schedule}/cancel",
        args.schedule,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/subscription_schedules/{schedule}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_subscription_schedules_schedule_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: SubscriptionSchedule = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/subscription_schedules/{schedule}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_subscription_schedules_schedule_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_subscription_schedules_schedule_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/subscription_schedules/{schedule}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_subscription_schedules_schedule_cancel(
    client: &SimpleHttpClient,
    args: &PostSubscriptionSchedulesScheduleCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_subscription_schedules_schedule_cancel_builder(client, args)?;
    post_subscription_schedules_schedule_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/topups/{topup}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/topups/{topup}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_topups_topup_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTopupsTopupCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/topups/{topup}/cancel",
        args.topup,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/topups/{topup}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_topups_topup_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topup>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Topup = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/topups/{topup}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_topups_topup_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_topups_topup_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/topups/{topup}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_topups_topup_cancel(
    client: &SimpleHttpClient,
    args: &PostTopupsTopupCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_topups_topup_cancel_builder(client, args)?;
    post_topups_topup_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_inbound_transfers_inbound_transfer_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTreasuryInboundTransfersInboundTransferCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/inbound_transfers/{inbound_transfer}/cancel",
        args.inbound_transfer,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_inbound_transfers_inbound_transfer_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryInboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_inbound_transfers_inbound_transfer_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_inbound_transfers_inbound_transfer_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_treasury_inbound_transfers_inbound_transfer_cancel(
    client: &SimpleHttpClient,
    args: &PostTreasuryInboundTransfersInboundTransferCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_treasury_inbound_transfers_inbound_transfer_cancel_builder(client, args)?;
    post_treasury_inbound_transfers_inbound_transfer_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/outbound_payments/{id}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/treasury/outbound_payments/{id}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_outbound_payments_id_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTreasuryOutboundPaymentsIdCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/outbound_payments/{id}/cancel",
        args.id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_payments/{id}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_outbound_payments_id_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundPayment = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/outbound_payments/{id}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_outbound_payments_id_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_outbound_payments_id_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/outbound_payments/{id}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_treasury_outbound_payments_id_cancel(
    client: &SimpleHttpClient,
    args: &PostTreasuryOutboundPaymentsIdCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_treasury_outbound_payments_id_cancel_builder(client, args)?;
    post_treasury_outbound_payments_id_cancel_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel
// -----------------------------------------------------------------------------

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_treasury_outbound_transfers_outbound_transfer_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostTreasuryOutboundTransfersOutboundTransferCancelArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/treasury/outbound_transfers/{outbound_transfer}/cancel",
        args.outbound_transfer,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_treasury_outbound_transfers_outbound_transfer_cancel_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TreasuryOutboundTransfer = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_treasury_outbound_transfers_outbound_transfer_cancel_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_treasury_outbound_transfers_outbound_transfer_cancel_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_treasury_outbound_transfers_outbound_transfer_cancel(
    client: &SimpleHttpClient,
    args: &PostTreasuryOutboundTransfersOutboundTransferCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_treasury_outbound_transfers_outbound_transfer_cancel_builder(client, args)?;
    post_treasury_outbound_transfers_outbound_transfer_cancel_execute(builder)
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe cancel.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// POST /v1/climate/orders/{order}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_climate_orders_order_cancel(
        &self,
        args: &PostClimateOrdersOrderCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<ClimateOrder>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_climate_orders_order_cancel_builder(&self.http_client, args)?;
        post_climate_orders_order_cancel_execute(builder)
    }

    /// POST /v1/identity/verification_sessions/{session}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_identity_verification_sessions_session_cancel(
        &self,
        args: &PostIdentityVerificationSessionsSessionCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<IdentityVerificationSession>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder =
            post_identity_verification_sessions_session_cancel_builder(&self.http_client, args)?;
        post_identity_verification_sessions_session_cancel_execute(builder)
    }

    /// POST /v1/payment_intents/{intent}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_payment_intents_intent_cancel(
        &self,
        args: &PostPaymentIntentsIntentCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<PaymentIntent>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payment_intents_intent_cancel_builder(&self.http_client, args)?;
        post_payment_intents_intent_cancel_execute(builder)
    }

    /// POST /v1/payouts/{payout}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_payouts_payout_cancel(
        &self,
        args: &PostPayoutsPayoutCancelArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Payout>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_payouts_payout_cancel_builder(&self.http_client, args)?;
        post_payouts_payout_cancel_execute(builder)
    }

    /// POST /v1/quotes/{quote}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_quotes_quote_cancel(
        &self,
        args: &PostQuotesQuoteCancelArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Quote>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_quotes_quote_cancel_builder(&self.http_client, args)?;
        post_quotes_quote_cancel_execute(builder)
    }

    /// POST /v1/refunds/{refund}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_refunds_refund_cancel(
        &self,
        args: &PostRefundsRefundCancelArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_refunds_refund_cancel_builder(&self.http_client, args)?;
        post_refunds_refund_cancel_execute(builder)
    }

    /// POST /v1/setup_intents/{intent}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_setup_intents_intent_cancel(
        &self,
        args: &PostSetupIntentsIntentCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SetupIntent>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_setup_intents_intent_cancel_builder(&self.http_client, args)?;
        post_setup_intents_intent_cancel_execute(builder)
    }

    /// POST /v1/subscription_schedules/{schedule}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_subscription_schedules_schedule_cancel(
        &self,
        args: &PostSubscriptionSchedulesScheduleCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<SubscriptionSchedule>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_subscription_schedules_schedule_cancel_builder(&self.http_client, args)?;
        post_subscription_schedules_schedule_cancel_execute(builder)
    }

    /// POST /v1/topups/{topup}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_topups_topup_cancel(
        &self,
        args: &PostTopupsTopupCancelArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Topup>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_topups_topup_cancel_builder(&self.http_client, args)?;
        post_topups_topup_cancel_execute(builder)
    }

    /// POST /v1/treasury/inbound_transfers/{inbound_transfer}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_treasury_inbound_transfers_inbound_transfer_cancel(
        &self,
        args: &PostTreasuryInboundTransfersInboundTransferCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryInboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_treasury_inbound_transfers_inbound_transfer_cancel_builder(
            &self.http_client,
            args,
        )?;
        post_treasury_inbound_transfers_inbound_transfer_cancel_execute(builder)
    }

    /// POST /v1/treasury/outbound_payments/{id}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_treasury_outbound_payments_id_cancel(
        &self,
        args: &PostTreasuryOutboundPaymentsIdCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundPayment>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_treasury_outbound_payments_id_cancel_builder(&self.http_client, args)?;
        post_treasury_outbound_payments_id_cancel_execute(builder)
    }

    /// POST /v1/treasury/outbound_transfers/{outbound_transfer}/cancel.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_cancel_post_treasury_outbound_transfers_outbound_transfer_cancel(
        &self,
        args: &PostTreasuryOutboundTransfersOutboundTransferCancelArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TreasuryOutboundTransfer>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_treasury_outbound_transfers_outbound_transfer_cancel_builder(
            &self.http_client,
            args,
        )?;
        post_treasury_outbound_transfers_outbound_transfer_cancel_execute(builder)
    }
}
