//! Auto-generated API module for stripe refunds.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `stripe_refunds `

#![cfg(feature = "stripe_refunds")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// FeeRefund response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FeeRefund {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// Refund response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Refund {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// TerminalRefund response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TerminalRefund {
    /// Raw JSON value - full schema generated from OpenAPI
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`GetApplicationFeesFeeRefundsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetApplicationFeesFeeRefundsIdArgs {
    /// Path parameter: fee
    pub fee: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostApplicationFeesFeeRefundsId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostApplicationFeesFeeRefundsIdArgs {
    /// Path parameter: fee
    pub fee: String,
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetApplicationFeesIdRefunds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetApplicationFeesIdRefundsArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`PostApplicationFeesIdRefunds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostApplicationFeesIdRefundsArgs {
    /// Path parameter: id
    pub id: String,
}

/// Arguments for [`GetChargesChargeRefunds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetChargesChargeRefundsArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`PostChargesChargeRefunds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostChargesChargeRefundsArgs {
    /// Path parameter: charge
    pub charge: String,
}

/// Arguments for [`GetChargesChargeRefundsRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetChargesChargeRefundsRefundArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Path parameter: refund
    pub refund: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostChargesChargeRefundsRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostChargesChargeRefundsRefundArgs {
    /// Path parameter: charge
    pub charge: String,
    /// Path parameter: refund
    pub refund: String,
}

/// Arguments for [`GetRefunds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRefundsArgs {
    /// Query parameter: charge
    pub charge: Option<String>,
    /// Query parameter: created
    pub created: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: expand
    pub expand: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
    /// Query parameter: payment_intent
    pub payment_intent: Option<String>,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
}

/// Arguments for [`GetRefundsRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRefundsRefundArgs {
    /// Path parameter: refund
    pub refund: String,
    /// Query parameter: expand
    pub expand: Option<String>,
}

/// Arguments for [`PostRefundsRefund_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostRefundsRefundArgs {
    /// Path parameter: refund
    pub refund: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/application_fees/{fee}/refunds/{id}
// -----------------------------------------------------------------------------

/// GET /v1/application_fees/{fee}/refunds/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_application_fees_fee_refunds_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetApplicationFeesFeeRefundsIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/application_fees/{fee}/refunds/{id}",
        args.fee, args.id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/application_fees/{fee}/refunds/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_application_fees_fee_refunds_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FeeRefund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: FeeRefund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/application_fees/{fee}/refunds/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_application_fees_fee_refunds_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FeeRefund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_application_fees_fee_refunds_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/application_fees/{fee}/refunds/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_application_fees_fee_refunds_id(
    client: &SimpleHttpClient,
    args: &GetApplicationFeesFeeRefundsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FeeRefund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_application_fees_fee_refunds_id_builder(client, args)?;
    get_application_fees_fee_refunds_id_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/application_fees/{fee}/refunds/{id}
// -----------------------------------------------------------------------------

/// POST /v1/application_fees/{fee}/refunds/{id} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_application_fees_fee_refunds_id_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostApplicationFeesFeeRefundsIdArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/application_fees/{fee}/refunds/{id}",
        args.fee, args.id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/application_fees/{fee}/refunds/{id} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_application_fees_fee_refunds_id_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FeeRefund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: FeeRefund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/application_fees/{fee}/refunds/{id} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_application_fees_fee_refunds_id_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FeeRefund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_application_fees_fee_refunds_id_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/application_fees/{fee}/refunds/{id} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_application_fees_fee_refunds_id(
    client: &SimpleHttpClient,
    args: &PostApplicationFeesFeeRefundsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FeeRefund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_application_fees_fee_refunds_id_builder(client, args)?;
    post_application_fees_fee_refunds_id_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/application_fees/{id}/refunds
// -----------------------------------------------------------------------------

/// GET /v1/application_fees/{id}/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_application_fees_id_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetApplicationFeesIdRefundsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/application_fees/{id}/refunds",
        args.id,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/application_fees/{id}/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_application_fees_id_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/application_fees/{id}/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_application_fees_id_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_application_fees_id_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/application_fees/{id}/refunds - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_application_fees_id_refunds(
    client: &SimpleHttpClient,
    args: &GetApplicationFeesIdRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_application_fees_id_refunds_builder(client, args)?;
    get_application_fees_id_refunds_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/application_fees/{id}/refunds
// -----------------------------------------------------------------------------

/// POST /v1/application_fees/{id}/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_application_fees_id_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostApplicationFeesIdRefundsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/application_fees/{id}/refunds",
        args.id,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/application_fees/{id}/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_application_fees_id_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FeeRefund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: FeeRefund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/application_fees/{id}/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_application_fees_id_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FeeRefund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_application_fees_id_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/application_fees/{id}/refunds - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_application_fees_id_refunds(
    client: &SimpleHttpClient,
    args: &PostApplicationFeesIdRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FeeRefund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_application_fees_id_refunds_builder(client, args)?;
    post_application_fees_id_refunds_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/charges/{charge}/refunds
// -----------------------------------------------------------------------------

/// GET /v1/charges/{charge}/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_charges_charge_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetChargesChargeRefundsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}/refunds",
        args.charge,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge}/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_charges_charge_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/charges/{charge}/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_charges_charge_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_charges_charge_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/charges/{charge}/refunds - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_charges_charge_refunds(
    client: &SimpleHttpClient,
    args: &GetChargesChargeRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_charges_charge_refunds_builder(client, args)?;
    get_charges_charge_refunds_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/charges/{charge}/refunds
// -----------------------------------------------------------------------------

/// POST /v1/charges/{charge}/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_charges_charge_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostChargesChargeRefundsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}/refunds",
        args.charge,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_charges_charge_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/charges/{charge}/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_charges_charge_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_charges_charge_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/charges/{charge}/refunds - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_charges_charge_refunds(
    client: &SimpleHttpClient,
    args: &PostChargesChargeRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_charges_charge_refunds_builder(client, args)?;
    post_charges_charge_refunds_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/charges/{charge}/refunds/{refund}
// -----------------------------------------------------------------------------

/// GET /v1/charges/{charge}/refunds/{refund} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_charges_charge_refunds_refund_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetChargesChargeRefundsRefundArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}/refunds/{refund}",
        args.charge, args.refund,
    );

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/charges/{charge}/refunds/{refund} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_charges_charge_refunds_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/charges/{charge}/refunds/{refund} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_charges_charge_refunds_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_charges_charge_refunds_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/charges/{charge}/refunds/{refund} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_charges_charge_refunds_refund(
    client: &SimpleHttpClient,
    args: &GetChargesChargeRefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_charges_charge_refunds_refund_builder(client, args)?;
    get_charges_charge_refunds_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/charges/{charge}/refunds/{refund}
// -----------------------------------------------------------------------------

/// POST /v1/charges/{charge}/refunds/{refund} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_charges_charge_refunds_refund_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostChargesChargeRefundsRefundArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.stripe.com//v1/charges/{charge}/refunds/{refund}",
        args.charge, args.refund,
    );

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/charges/{charge}/refunds/{refund} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_charges_charge_refunds_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/charges/{charge}/refunds/{refund} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_charges_charge_refunds_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_charges_charge_refunds_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/charges/{charge}/refunds/{refund} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_charges_charge_refunds_refund(
    client: &SimpleHttpClient,
    args: &PostChargesChargeRefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_charges_charge_refunds_refund_builder(client, args)?;
    post_charges_charge_refunds_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// GET /v1/refunds
// -----------------------------------------------------------------------------

/// GET /v1/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetRefundsArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/refunds",);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/refunds - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_refunds(
    client: &SimpleHttpClient,
    args: &GetRefundsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_refunds_builder(client, args)?;
    get_refunds_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/refunds
// -----------------------------------------------------------------------------

/// POST /v1/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/refunds",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// -----------------------------------------------------------------------------
// GET /v1/refunds/{refund}
// -----------------------------------------------------------------------------

/// GET /v1/refunds/{refund} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn get_refunds_refund_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &GetRefundsRefundArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/refunds/{refund}", args.refund,);

    let builder = client
        .get(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /v1/refunds/{refund} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn get_refunds_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// GET /v1/refunds/{refund} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn get_refunds_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = get_refunds_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /v1/refunds/{refund} - convenience function.
///
/// Combines builder and execute in one call.

pub fn get_refunds_refund(
    client: &SimpleHttpClient,
    args: &GetRefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = get_refunds_refund_builder(client, args)?;
    get_refunds_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/refunds/{refund}
// -----------------------------------------------------------------------------

/// POST /v1/refunds/{refund} - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_refunds_refund_builder<R>(
    client: &SimpleHttpClient<R>,
    args: &PostRefundsRefundArgs,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/refunds/{refund}", args.refund,);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/refunds/{refund} - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_refunds_refund_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Refund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: Refund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/refunds/{refund} - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_refunds_refund_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_refunds_refund_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /v1/refunds/{refund} - convenience function.
///
/// Combines builder and execute in one call.

pub fn post_refunds_refund(
    client: &SimpleHttpClient,
    args: &PostRefundsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
        + Send
        + 'static,
    crate::ApiError,
> {
    let builder = post_refunds_refund_builder(client, args)?;
    post_refunds_refund_execute(builder)
}

// -----------------------------------------------------------------------------
// POST /v1/terminal/refunds
// -----------------------------------------------------------------------------

/// POST /v1/terminal/refunds - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

pub fn post_terminal_refunds_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, crate::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!("https://api.stripe.com//v1/terminal/refunds",);

    let builder = client
        .post(&endpoint_url)
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /v1/terminal/refunds - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

pub fn post_terminal_refunds_task(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TerminalRefund>, crate::ApiError>,
            Pending = crate::ApiPending,
            Spawner = crate::BoxedSendExecutionAction,
        > + Send
        + 'static,
    crate::ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            crate::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(crate::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body = foundation_core::wire::simple_http::body_reader::collect_string(stream);
                let parsed: TerminalRefund = serde_json::from_str(&body)
                    .map_err(|e| crate::ApiError::ParseFailed(e.to_string()))?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            crate::RequestIntro::Failed(e) => {
                Err(crate::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| crate::ApiPending::Sending))
}

/// POST /v1/terminal/refunds - execute function.
///
/// Takes a `ClientRequestBuilder` and executes it.

pub fn post_terminal_refunds_execute(
    builder: ClientRequestBuilder<crate::SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TerminalRefund>, crate::ApiError>,
            P = crate::ApiPending,
        > + Send
        + 'static,
    crate::ApiError,
> {
    let task = post_terminal_refunds_task(builder)?;
    execute(task, None).map_err(|e| crate::ApiError::RequestBuildFailed(e.to_string()))
}

// =============================================================================
// PROVIDERCLIENT IMPL (wrapper methods)
// =============================================================================

/// ProviderClient extension methods for stripe refunds.
///
/// These wrapper methods provide state-aware API access.

impl<S, R> crate::ProviderClient<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// GET /v1/application_fees/{fee}/refunds/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_get_application_fees_fee_refunds_id(
        &self,
        args: &GetApplicationFeesFeeRefundsIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<FeeRefund>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_application_fees_fee_refunds_id_builder(&self.http_client, args)?;
        get_application_fees_fee_refunds_id_execute(builder)
    }

    /// POST /v1/application_fees/{fee}/refunds/{id}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_application_fees_fee_refunds_id(
        &self,
        args: &PostApplicationFeesFeeRefundsIdArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<FeeRefund>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_application_fees_fee_refunds_id_builder(&self.http_client, args)?;
        post_application_fees_fee_refunds_id_execute(builder)
    }

    /// GET /v1/application_fees/{id}/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_get_application_fees_id_refunds(
        &self,
        args: &GetApplicationFeesIdRefundsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_application_fees_id_refunds_builder(&self.http_client, args)?;
        get_application_fees_id_refunds_execute(builder)
    }

    /// POST /v1/application_fees/{id}/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_application_fees_id_refunds(
        &self,
        args: &PostApplicationFeesIdRefundsArgs,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<FeeRefund>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_application_fees_id_refunds_builder(&self.http_client, args)?;
        post_application_fees_id_refunds_execute(builder)
    }

    /// GET /v1/charges/{charge}/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_get_charges_charge_refunds(
        &self,
        args: &GetChargesChargeRefundsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_charges_charge_refunds_builder(&self.http_client, args)?;
        get_charges_charge_refunds_execute(builder)
    }

    /// POST /v1/charges/{charge}/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_charges_charge_refunds(
        &self,
        args: &PostChargesChargeRefundsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_charges_charge_refunds_builder(&self.http_client, args)?;
        post_charges_charge_refunds_execute(builder)
    }

    /// GET /v1/charges/{charge}/refunds/{refund}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_get_charges_charge_refunds_refund(
        &self,
        args: &GetChargesChargeRefundsRefundArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_charges_charge_refunds_refund_builder(&self.http_client, args)?;
        get_charges_charge_refunds_refund_execute(builder)
    }

    /// POST /v1/charges/{charge}/refunds/{refund}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_charges_charge_refunds_refund(
        &self,
        args: &PostChargesChargeRefundsRefundArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_charges_charge_refunds_refund_builder(&self.http_client, args)?;
        post_charges_charge_refunds_refund_execute(builder)
    }

    /// GET /v1/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_get_refunds(
        &self,
        args: &GetRefundsArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<()>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_refunds_builder(&self.http_client, args)?;
        get_refunds_execute(builder)
    }

    /// POST /v1/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_refunds(
        &self,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_refunds_builder(&self.http_client)?;
        post_refunds_execute(builder)
    }

    /// GET /v1/refunds/{refund}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_get_refunds_refund(
        &self,
        args: &GetRefundsRefundArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = get_refunds_refund_builder(&self.http_client, args)?;
        get_refunds_refund_execute(builder)
    }

    /// POST /v1/refunds/{refund}.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_refunds_refund(
        &self,
        args: &PostRefundsRefundArgs,
    ) -> Result<
        impl StreamIterator<D = Result<ApiResponse<Refund>, crate::ApiError>, P = crate::ApiPending>
            + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_refunds_refund_builder(&self.http_client, args)?;
        post_refunds_refund_execute(builder)
    }

    /// POST /v1/terminal/refunds.
    ///
    /// Wrapper method with automatic state tracking.
    pub fn stripe_refunds_post_terminal_refunds(
        &self,
    ) -> Result<
        impl StreamIterator<
                D = Result<ApiResponse<TerminalRefund>, crate::ApiError>,
                P = crate::ApiPending,
            > + Send
            + 'static,
        crate::ApiError,
    > {
        let builder = post_terminal_refunds_builder(&self.http_client)?;
        post_terminal_refunds_execute(builder)
    }
}
