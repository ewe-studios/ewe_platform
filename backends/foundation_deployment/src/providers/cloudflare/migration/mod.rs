//! Auto-generated API module for cloudflare migration.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `cloudflare_migration `

#![cfg(feature = "cloudflare_migration")]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`Accounts_moveAccounts_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AccountsMoveAccountsArgs {
    /// Path parameter: account_id
    pub account_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST /accounts/move
// -----------------------------------------------------------------------------

/// POST /accounts/move - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

#[inline]
pub fn accounts_batch_move_accounts_builder<R>(client: &SimpleHttpClient<R>) -> Result<ClientRequestBuilder<R>, super::shared::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/move",
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /accounts/move - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

#[inline]
pub fn accounts_batch_move_accounts_task<R>(
    builder: ClientRequestBuilder<R>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, super::shared::ApiError>, Pending = super::shared::ApiPending, Spawner = super::shared::BoxedSendExecutionAction> + Send + 'static, super::shared::ApiError> {
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
    Ok(
        builder
            .build_send_request()
            .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/move
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/move - builder function.
///
/// Returns `ClientRequestBuilder` for customization.

#[inline]
pub fn accounts_move_accounts_builder<R>(client: &SimpleHttpClient<R>, args: &AccountsMoveAccountsArgs) -> Result<ClientRequestBuilder<R>, super::shared::ApiError>
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone,
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/move",
        args.account_id,
    );

    let builder = client.post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /accounts/{account_id}/move - task function.
///
/// Takes a `ClientRequestBuilder` and returns a `TaskIterator`.

#[inline]
pub fn accounts_move_accounts_task<R>(
    builder: ClientRequestBuilder<R>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<()>, super::shared::ApiError>, Pending = super::shared::ApiPending, Spawner = super::shared::BoxedSendExecutionAction> + Send + 'static, super::shared::ApiError> {
where R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
    Ok(
        builder
            .build_send_request()
            .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: () })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

